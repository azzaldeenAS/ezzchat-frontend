import {
  require_react
} from "./chunk-DRWLMN53.js";
import {
  __toESM
} from "./chunk-G3PMV62Z.js";

// node_modules/@livekit/components-react/dist/hooks-C7fu1g_i.mjs
var r = __toESM(require_react(), 1);
var import_react = __toESM(require_react(), 1);

// node_modules/livekit-client/dist/livekit-client.esm.mjs
function _mergeNamespaces(n, m) {
  m.forEach(function(e4) {
    e4 && typeof e4 !== "string" && !Array.isArray(e4) && Object.keys(e4).forEach(function(k) {
      if (k !== "default" && !(k in n)) {
        var d = Object.getOwnPropertyDescriptor(e4, k);
        Object.defineProperty(n, k, d.get ? d : { enumerable: true, get: function() {
          return e4[k];
        } });
      }
    });
  });
  return Object.freeze(n);
}
var e = Object.defineProperty;
var h = (i2, s, t) => s in i2 ? e(i2, s, {
  enumerable: true,
  configurable: true,
  writable: true,
  value: t
}) : i2[s] = t;
var o = (i2, s, t) => h(i2, typeof s != "symbol" ? s + "" : s, t);
var _ = class {
  constructor() {
    o(this, "_locking");
    o(this, "_locks");
    this._locking = Promise.resolve(), this._locks = 0;
  }
  isLocked() {
    return this._locks > 0;
  }
  lock() {
    this._locks += 1;
    let s;
    const t = new Promise((l) => s = () => {
      this._locks -= 1, l();
    }), c = this._locking.then(() => s);
    return this._locking = this._locking.then(() => t), c;
  }
};
function assert(condition, msg) {
  if (!condition) {
    throw new Error(msg);
  }
}
var FLOAT32_MAX = 34028234663852886e22;
var FLOAT32_MIN = -34028234663852886e22;
var UINT32_MAX = 4294967295;
var INT32_MAX = 2147483647;
var INT32_MIN = -2147483648;
function assertInt32(arg) {
  if (typeof arg !== "number") throw new Error("invalid int 32: " + typeof arg);
  if (!Number.isInteger(arg) || arg > INT32_MAX || arg < INT32_MIN) throw new Error("invalid int 32: " + arg);
}
function assertUInt32(arg) {
  if (typeof arg !== "number") throw new Error("invalid uint 32: " + typeof arg);
  if (!Number.isInteger(arg) || arg > UINT32_MAX || arg < 0) throw new Error("invalid uint 32: " + arg);
}
function assertFloat32(arg) {
  if (typeof arg !== "number") throw new Error("invalid float 32: " + typeof arg);
  if (!Number.isFinite(arg)) return;
  if (arg > FLOAT32_MAX || arg < FLOAT32_MIN) throw new Error("invalid float 32: " + arg);
}
var enumTypeSymbol = Symbol("@bufbuild/protobuf/enum-type");
function getEnumType(enumObject) {
  const t = enumObject[enumTypeSymbol];
  assert(t, "missing enum type on enum object");
  return t;
}
function setEnumType(enumObject, typeName, values, opt) {
  enumObject[enumTypeSymbol] = makeEnumType(typeName, values.map((v) => ({
    no: v.no,
    name: v.name,
    localName: enumObject[v.no]
  })));
}
function makeEnumType(typeName, values, _opt) {
  const names = /* @__PURE__ */ Object.create(null);
  const numbers = /* @__PURE__ */ Object.create(null);
  const normalValues = [];
  for (const value of values) {
    const n = normalizeEnumValue(value);
    normalValues.push(n);
    names[value.name] = n;
    numbers[value.no] = n;
  }
  return {
    typeName,
    values: normalValues,
    // We do not surface options at this time
    // options: opt?.options ?? Object.create(null),
    findName(name) {
      return names[name];
    },
    findNumber(no2) {
      return numbers[no2];
    }
  };
}
function makeEnum(typeName, values, opt) {
  const enumObject = {};
  for (const value of values) {
    const n = normalizeEnumValue(value);
    enumObject[n.localName] = n.no;
    enumObject[n.no] = n.localName;
  }
  setEnumType(enumObject, typeName, values);
  return enumObject;
}
function normalizeEnumValue(value) {
  if ("localName" in value) {
    return value;
  }
  return Object.assign(Object.assign({}, value), {
    localName: value.name
  });
}
var Message = class {
  /**
   * Compare with a message of the same type.
   * Note that this function disregards extensions and unknown fields.
   */
  equals(other) {
    return this.getType().runtime.util.equals(this.getType(), this, other);
  }
  /**
   * Create a deep copy.
   */
  clone() {
    return this.getType().runtime.util.clone(this);
  }
  /**
   * Parse from binary data, merging fields.
   *
   * Repeated fields are appended. Map entries are added, overwriting
   * existing keys.
   *
   * If a message field is already present, it will be merged with the
   * new data.
   */
  fromBinary(bytes, options) {
    const type = this.getType(), format = type.runtime.bin, opt = format.makeReadOptions(options);
    format.readMessage(this, opt.readerFactory(bytes), bytes.byteLength, opt);
    return this;
  }
  /**
   * Parse a message from a JSON value.
   */
  fromJson(jsonValue, options) {
    const type = this.getType(), format = type.runtime.json, opt = format.makeReadOptions(options);
    format.readMessage(type, jsonValue, opt, this);
    return this;
  }
  /**
   * Parse a message from a JSON string.
   */
  fromJsonString(jsonString, options) {
    let json2;
    try {
      json2 = JSON.parse(jsonString);
    } catch (e4) {
      throw new Error("cannot decode ".concat(this.getType().typeName, " from JSON: ").concat(e4 instanceof Error ? e4.message : String(e4)));
    }
    return this.fromJson(json2, options);
  }
  /**
   * Serialize the message to binary data.
   */
  toBinary(options) {
    const type = this.getType(), bin = type.runtime.bin, opt = bin.makeWriteOptions(options), writer2 = opt.writerFactory();
    bin.writeMessage(this, writer2, opt);
    return writer2.finish();
  }
  /**
   * Serialize the message to a JSON value, a JavaScript value that can be
   * passed to JSON.stringify().
   */
  toJson(options) {
    const type = this.getType(), json2 = type.runtime.json, opt = json2.makeWriteOptions(options);
    return json2.writeMessage(this, opt);
  }
  /**
   * Serialize the message to a JSON string.
   */
  toJsonString(options) {
    var _a2;
    const value = this.toJson(options);
    return JSON.stringify(value, null, (_a2 = options === null || options === void 0 ? void 0 : options.prettySpaces) !== null && _a2 !== void 0 ? _a2 : 0);
  }
  /**
   * Override for serialization behavior. This will be invoked when calling
   * JSON.stringify on this message (i.e. JSON.stringify(msg)).
   *
   * Note that this will not serialize google.protobuf.Any with a packed
   * message because the protobuf JSON format specifies that it needs to be
   * unpacked, and this is only possible with a type registry to look up the
   * message type.  As a result, attempting to serialize a message with this
   * type will throw an Error.
   *
   * This method is protected because you should not need to invoke it
   * directly -- instead use JSON.stringify or toJsonString for
   * stringified JSON.  Alternatively, if actual JSON is desired, you should
   * use toJson.
   */
  toJSON() {
    return this.toJson({
      emitDefaultValues: true
    });
  }
  /**
   * Retrieve the MessageType of this message - a singleton that represents
   * the protobuf message declaration and provides metadata for reflection-
   * based operations.
   */
  getType() {
    return Object.getPrototypeOf(this).constructor;
  }
};
function makeMessageType(runtime, typeName, fields, opt) {
  var _a2;
  const localName = (_a2 = opt === null || opt === void 0 ? void 0 : opt.localName) !== null && _a2 !== void 0 ? _a2 : typeName.substring(typeName.lastIndexOf(".") + 1);
  const type = {
    [localName]: function(data) {
      runtime.util.initFields(this);
      runtime.util.initPartial(data, this);
    }
  }[localName];
  Object.setPrototypeOf(type.prototype, new Message());
  Object.assign(type, {
    runtime,
    typeName,
    fields: runtime.util.newFieldList(fields),
    fromBinary(bytes, options) {
      return new type().fromBinary(bytes, options);
    },
    fromJson(jsonValue, options) {
      return new type().fromJson(jsonValue, options);
    },
    fromJsonString(jsonString, options) {
      return new type().fromJsonString(jsonString, options);
    },
    equals(a, b) {
      return runtime.util.equals(type, a, b);
    }
  });
  return type;
}
function varint64read() {
  let lowBits = 0;
  let highBits = 0;
  for (let shift = 0; shift < 28; shift += 7) {
    let b = this.buf[this.pos++];
    lowBits |= (b & 127) << shift;
    if ((b & 128) == 0) {
      this.assertBounds();
      return [lowBits, highBits];
    }
  }
  let middleByte = this.buf[this.pos++];
  lowBits |= (middleByte & 15) << 28;
  highBits = (middleByte & 112) >> 4;
  if ((middleByte & 128) == 0) {
    this.assertBounds();
    return [lowBits, highBits];
  }
  for (let shift = 3; shift <= 31; shift += 7) {
    let b = this.buf[this.pos++];
    highBits |= (b & 127) << shift;
    if ((b & 128) == 0) {
      this.assertBounds();
      return [lowBits, highBits];
    }
  }
  throw new Error("invalid varint");
}
function varint64write(lo2, hi2, bytes) {
  for (let i2 = 0; i2 < 28; i2 = i2 + 7) {
    const shift = lo2 >>> i2;
    const hasNext = !(shift >>> 7 == 0 && hi2 == 0);
    const byte = (hasNext ? shift | 128 : shift) & 255;
    bytes.push(byte);
    if (!hasNext) {
      return;
    }
  }
  const splitBits = lo2 >>> 28 & 15 | (hi2 & 7) << 4;
  const hasMoreBits = !(hi2 >> 3 == 0);
  bytes.push((hasMoreBits ? splitBits | 128 : splitBits) & 255);
  if (!hasMoreBits) {
    return;
  }
  for (let i2 = 3; i2 < 31; i2 = i2 + 7) {
    const shift = hi2 >>> i2;
    const hasNext = !(shift >>> 7 == 0);
    const byte = (hasNext ? shift | 128 : shift) & 255;
    bytes.push(byte);
    if (!hasNext) {
      return;
    }
  }
  bytes.push(hi2 >>> 31 & 1);
}
var TWO_PWR_32_DBL = 4294967296;
function int64FromString(dec) {
  const minus = dec[0] === "-";
  if (minus) {
    dec = dec.slice(1);
  }
  const base2 = 1e6;
  let lowBits = 0;
  let highBits = 0;
  function add1e6digit(begin, end) {
    const digit1e6 = Number(dec.slice(begin, end));
    highBits *= base2;
    lowBits = lowBits * base2 + digit1e6;
    if (lowBits >= TWO_PWR_32_DBL) {
      highBits = highBits + (lowBits / TWO_PWR_32_DBL | 0);
      lowBits = lowBits % TWO_PWR_32_DBL;
    }
  }
  add1e6digit(-24, -18);
  add1e6digit(-18, -12);
  add1e6digit(-12, -6);
  add1e6digit(-6);
  return minus ? negate(lowBits, highBits) : newBits(lowBits, highBits);
}
function int64ToString(lo2, hi2) {
  let bits = newBits(lo2, hi2);
  const negative = bits.hi & 2147483648;
  if (negative) {
    bits = negate(bits.lo, bits.hi);
  }
  const result = uInt64ToString(bits.lo, bits.hi);
  return negative ? "-" + result : result;
}
function uInt64ToString(lo2, hi2) {
  var _toUnsigned = toUnsigned(lo2, hi2);
  lo2 = _toUnsigned.lo;
  hi2 = _toUnsigned.hi;
  if (hi2 <= 2097151) {
    return String(TWO_PWR_32_DBL * hi2 + lo2);
  }
  const low = lo2 & 16777215;
  const mid = (lo2 >>> 24 | hi2 << 8) & 16777215;
  const high = hi2 >> 16 & 65535;
  let digitA = low + mid * 6777216 + high * 6710656;
  let digitB = mid + high * 8147497;
  let digitC = high * 2;
  const base2 = 1e7;
  if (digitA >= base2) {
    digitB += Math.floor(digitA / base2);
    digitA %= base2;
  }
  if (digitB >= base2) {
    digitC += Math.floor(digitB / base2);
    digitB %= base2;
  }
  return digitC.toString() + decimalFrom1e7WithLeadingZeros(digitB) + decimalFrom1e7WithLeadingZeros(digitA);
}
function toUnsigned(lo2, hi2) {
  return {
    lo: lo2 >>> 0,
    hi: hi2 >>> 0
  };
}
function newBits(lo2, hi2) {
  return {
    lo: lo2 | 0,
    hi: hi2 | 0
  };
}
function negate(lowBits, highBits) {
  highBits = ~highBits;
  if (lowBits) {
    lowBits = ~lowBits + 1;
  } else {
    highBits += 1;
  }
  return newBits(lowBits, highBits);
}
var decimalFrom1e7WithLeadingZeros = (digit1e7) => {
  const partial = String(digit1e7);
  return "0000000".slice(partial.length) + partial;
};
function varint32write(value, bytes) {
  if (value >= 0) {
    while (value > 127) {
      bytes.push(value & 127 | 128);
      value = value >>> 7;
    }
    bytes.push(value);
  } else {
    for (let i2 = 0; i2 < 9; i2++) {
      bytes.push(value & 127 | 128);
      value = value >> 7;
    }
    bytes.push(1);
  }
}
function varint32read() {
  let b = this.buf[this.pos++];
  let result = b & 127;
  if ((b & 128) == 0) {
    this.assertBounds();
    return result;
  }
  b = this.buf[this.pos++];
  result |= (b & 127) << 7;
  if ((b & 128) == 0) {
    this.assertBounds();
    return result;
  }
  b = this.buf[this.pos++];
  result |= (b & 127) << 14;
  if ((b & 128) == 0) {
    this.assertBounds();
    return result;
  }
  b = this.buf[this.pos++];
  result |= (b & 127) << 21;
  if ((b & 128) == 0) {
    this.assertBounds();
    return result;
  }
  b = this.buf[this.pos++];
  result |= (b & 15) << 28;
  for (let readBytes = 5; (b & 128) !== 0 && readBytes < 10; readBytes++) b = this.buf[this.pos++];
  if ((b & 128) != 0) throw new Error("invalid varint");
  this.assertBounds();
  return result >>> 0;
}
function makeInt64Support() {
  const dv = new DataView(new ArrayBuffer(8));
  const ok = typeof BigInt === "function" && typeof dv.getBigInt64 === "function" && typeof dv.getBigUint64 === "function" && typeof dv.setBigInt64 === "function" && typeof dv.setBigUint64 === "function" && (typeof process != "object" || typeof process.env != "object" || process.env.BUF_BIGINT_DISABLE !== "1");
  if (ok) {
    const MIN = BigInt("-9223372036854775808"), MAX = BigInt("9223372036854775807"), UMIN = BigInt("0"), UMAX = BigInt("18446744073709551615");
    return {
      zero: BigInt(0),
      supported: true,
      parse(value) {
        const bi2 = typeof value == "bigint" ? value : BigInt(value);
        if (bi2 > MAX || bi2 < MIN) {
          throw new Error("int64 invalid: ".concat(value));
        }
        return bi2;
      },
      uParse(value) {
        const bi2 = typeof value == "bigint" ? value : BigInt(value);
        if (bi2 > UMAX || bi2 < UMIN) {
          throw new Error("uint64 invalid: ".concat(value));
        }
        return bi2;
      },
      enc(value) {
        dv.setBigInt64(0, this.parse(value), true);
        return {
          lo: dv.getInt32(0, true),
          hi: dv.getInt32(4, true)
        };
      },
      uEnc(value) {
        dv.setBigInt64(0, this.uParse(value), true);
        return {
          lo: dv.getInt32(0, true),
          hi: dv.getInt32(4, true)
        };
      },
      dec(lo2, hi2) {
        dv.setInt32(0, lo2, true);
        dv.setInt32(4, hi2, true);
        return dv.getBigInt64(0, true);
      },
      uDec(lo2, hi2) {
        dv.setInt32(0, lo2, true);
        dv.setInt32(4, hi2, true);
        return dv.getBigUint64(0, true);
      }
    };
  }
  const assertInt64String = (value) => assert(/^-?[0-9]+$/.test(value), "int64 invalid: ".concat(value));
  const assertUInt64String = (value) => assert(/^[0-9]+$/.test(value), "uint64 invalid: ".concat(value));
  return {
    zero: "0",
    supported: false,
    parse(value) {
      if (typeof value != "string") {
        value = value.toString();
      }
      assertInt64String(value);
      return value;
    },
    uParse(value) {
      if (typeof value != "string") {
        value = value.toString();
      }
      assertUInt64String(value);
      return value;
    },
    enc(value) {
      if (typeof value != "string") {
        value = value.toString();
      }
      assertInt64String(value);
      return int64FromString(value);
    },
    uEnc(value) {
      if (typeof value != "string") {
        value = value.toString();
      }
      assertUInt64String(value);
      return int64FromString(value);
    },
    dec(lo2, hi2) {
      return int64ToString(lo2, hi2);
    },
    uDec(lo2, hi2) {
      return uInt64ToString(lo2, hi2);
    }
  };
}
var protoInt64 = makeInt64Support();
var ScalarType;
(function(ScalarType2) {
  ScalarType2[ScalarType2["DOUBLE"] = 1] = "DOUBLE";
  ScalarType2[ScalarType2["FLOAT"] = 2] = "FLOAT";
  ScalarType2[ScalarType2["INT64"] = 3] = "INT64";
  ScalarType2[ScalarType2["UINT64"] = 4] = "UINT64";
  ScalarType2[ScalarType2["INT32"] = 5] = "INT32";
  ScalarType2[ScalarType2["FIXED64"] = 6] = "FIXED64";
  ScalarType2[ScalarType2["FIXED32"] = 7] = "FIXED32";
  ScalarType2[ScalarType2["BOOL"] = 8] = "BOOL";
  ScalarType2[ScalarType2["STRING"] = 9] = "STRING";
  ScalarType2[ScalarType2["BYTES"] = 12] = "BYTES";
  ScalarType2[ScalarType2["UINT32"] = 13] = "UINT32";
  ScalarType2[ScalarType2["SFIXED32"] = 15] = "SFIXED32";
  ScalarType2[ScalarType2["SFIXED64"] = 16] = "SFIXED64";
  ScalarType2[ScalarType2["SINT32"] = 17] = "SINT32";
  ScalarType2[ScalarType2["SINT64"] = 18] = "SINT64";
})(ScalarType || (ScalarType = {}));
var LongType;
(function(LongType2) {
  LongType2[LongType2["BIGINT"] = 0] = "BIGINT";
  LongType2[LongType2["STRING"] = 1] = "STRING";
})(LongType || (LongType = {}));
function scalarEquals(type, a, b) {
  if (a === b) {
    return true;
  }
  if (type == ScalarType.BYTES) {
    if (!(a instanceof Uint8Array) || !(b instanceof Uint8Array)) {
      return false;
    }
    if (a.length !== b.length) {
      return false;
    }
    for (let i2 = 0; i2 < a.length; i2++) {
      if (a[i2] !== b[i2]) {
        return false;
      }
    }
    return true;
  }
  switch (type) {
    case ScalarType.UINT64:
    case ScalarType.FIXED64:
    case ScalarType.INT64:
    case ScalarType.SFIXED64:
    case ScalarType.SINT64:
      return a == b;
  }
  return false;
}
function scalarZeroValue(type, longType) {
  switch (type) {
    case ScalarType.BOOL:
      return false;
    case ScalarType.UINT64:
    case ScalarType.FIXED64:
    case ScalarType.INT64:
    case ScalarType.SFIXED64:
    case ScalarType.SINT64:
      return longType == 0 ? protoInt64.zero : "0";
    case ScalarType.DOUBLE:
    case ScalarType.FLOAT:
      return 0;
    case ScalarType.BYTES:
      return new Uint8Array(0);
    case ScalarType.STRING:
      return "";
    default:
      return 0;
  }
}
function isScalarZeroValue(type, value) {
  switch (type) {
    case ScalarType.BOOL:
      return value === false;
    case ScalarType.STRING:
      return value === "";
    case ScalarType.BYTES:
      return value instanceof Uint8Array && !value.byteLength;
    default:
      return value == 0;
  }
}
function _arrayLikeToArray(r2, a) {
  (null == a || a > r2.length) && (a = r2.length);
  for (var e4 = 0, n = Array(a); e4 < a; e4++) n[e4] = r2[e4];
  return n;
}
function _arrayWithHoles(r2) {
  if (Array.isArray(r2)) return r2;
}
function _defineProperty(e4, r2, t) {
  return (r2 = _toPropertyKey(r2)) in e4 ? Object.defineProperty(e4, r2, {
    value: t,
    enumerable: true,
    configurable: true,
    writable: true
  }) : e4[r2] = t, e4;
}
function _iterableToArrayLimit(r2, l) {
  var t = null == r2 ? null : "undefined" != typeof Symbol && r2[Symbol.iterator] || r2["@@iterator"];
  if (null != t) {
    var e4, n, i2, u, a = [], f = true, o2 = false;
    try {
      if (i2 = (t = t.call(r2)).next, 0 === l) {
        if (Object(t) !== t) return;
        f = false;
      } else for (; !(f = (e4 = i2.call(t)).done) && (a.push(e4.value), a.length !== l); f = true) ;
    } catch (r3) {
      o2 = true, n = r3;
    } finally {
      try {
        if (!f && null != t.return && (u = t.return(), Object(u) !== u)) return;
      } finally {
        if (o2) throw n;
      }
    }
    return a;
  }
}
function _nonIterableRest() {
  throw new TypeError("Invalid attempt to destructure non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method.");
}
function _slicedToArray(r2, e4) {
  return _arrayWithHoles(r2) || _iterableToArrayLimit(r2, e4) || _unsupportedIterableToArray(r2, e4) || _nonIterableRest();
}
function _toPrimitive(t, r2) {
  if ("object" != typeof t || !t) return t;
  var e4 = t[Symbol.toPrimitive];
  if (void 0 !== e4) {
    var i2 = e4.call(t, r2);
    if ("object" != typeof i2) return i2;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return ("string" === r2 ? String : Number)(t);
}
function _toPropertyKey(t) {
  var i2 = _toPrimitive(t, "string");
  return "symbol" == typeof i2 ? i2 : i2 + "";
}
function _unsupportedIterableToArray(r2, a) {
  if (r2) {
    if ("string" == typeof r2) return _arrayLikeToArray(r2, a);
    var t = {}.toString.call(r2).slice(8, -1);
    return "Object" === t && r2.constructor && (t = r2.constructor.name), "Map" === t || "Set" === t ? Array.from(r2) : "Arguments" === t || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(t) ? _arrayLikeToArray(r2, a) : void 0;
  }
}
var WireType;
(function(WireType2) {
  WireType2[WireType2["Varint"] = 0] = "Varint";
  WireType2[WireType2["Bit64"] = 1] = "Bit64";
  WireType2[WireType2["LengthDelimited"] = 2] = "LengthDelimited";
  WireType2[WireType2["StartGroup"] = 3] = "StartGroup";
  WireType2[WireType2["EndGroup"] = 4] = "EndGroup";
  WireType2[WireType2["Bit32"] = 5] = "Bit32";
})(WireType || (WireType = {}));
var BinaryWriter = class {
  constructor(textEncoder2) {
    this.stack = [];
    this.textEncoder = textEncoder2 !== null && textEncoder2 !== void 0 ? textEncoder2 : new TextEncoder();
    this.chunks = [];
    this.buf = [];
  }
  /**
   * Return all bytes written and reset this writer.
   */
  finish() {
    this.chunks.push(new Uint8Array(this.buf));
    let len = 0;
    for (let i2 = 0; i2 < this.chunks.length; i2++) len += this.chunks[i2].length;
    let bytes = new Uint8Array(len);
    let offset = 0;
    for (let i2 = 0; i2 < this.chunks.length; i2++) {
      bytes.set(this.chunks[i2], offset);
      offset += this.chunks[i2].length;
    }
    this.chunks = [];
    return bytes;
  }
  /**
   * Start a new fork for length-delimited data like a message
   * or a packed repeated field.
   *
   * Must be joined later with `join()`.
   */
  fork() {
    this.stack.push({
      chunks: this.chunks,
      buf: this.buf
    });
    this.chunks = [];
    this.buf = [];
    return this;
  }
  /**
   * Join the last fork. Write its length and bytes, then
   * return to the previous state.
   */
  join() {
    let chunk = this.finish();
    let prev = this.stack.pop();
    if (!prev) throw new Error("invalid state, fork stack empty");
    this.chunks = prev.chunks;
    this.buf = prev.buf;
    this.uint32(chunk.byteLength);
    return this.raw(chunk);
  }
  /**
   * Writes a tag (field number and wire type).
   *
   * Equivalent to `uint32( (fieldNo << 3 | type) >>> 0 )`.
   *
   * Generated code should compute the tag ahead of time and call `uint32()`.
   */
  tag(fieldNo, type) {
    return this.uint32((fieldNo << 3 | type) >>> 0);
  }
  /**
   * Write a chunk of raw bytes.
   */
  raw(chunk) {
    if (this.buf.length) {
      this.chunks.push(new Uint8Array(this.buf));
      this.buf = [];
    }
    this.chunks.push(chunk);
    return this;
  }
  /**
   * Write a `uint32` value, an unsigned 32 bit varint.
   */
  uint32(value) {
    assertUInt32(value);
    while (value > 127) {
      this.buf.push(value & 127 | 128);
      value = value >>> 7;
    }
    this.buf.push(value);
    return this;
  }
  /**
   * Write a `int32` value, a signed 32 bit varint.
   */
  int32(value) {
    assertInt32(value);
    varint32write(value, this.buf);
    return this;
  }
  /**
   * Write a `bool` value, a variant.
   */
  bool(value) {
    this.buf.push(value ? 1 : 0);
    return this;
  }
  /**
   * Write a `bytes` value, length-delimited arbitrary data.
   */
  bytes(value) {
    this.uint32(value.byteLength);
    return this.raw(value);
  }
  /**
   * Write a `string` value, length-delimited data converted to UTF-8 text.
   */
  string(value) {
    let chunk = this.textEncoder.encode(value);
    this.uint32(chunk.byteLength);
    return this.raw(chunk);
  }
  /**
   * Write a `float` value, 32-bit floating point number.
   */
  float(value) {
    assertFloat32(value);
    let chunk = new Uint8Array(4);
    new DataView(chunk.buffer).setFloat32(0, value, true);
    return this.raw(chunk);
  }
  /**
   * Write a `double` value, a 64-bit floating point number.
   */
  double(value) {
    let chunk = new Uint8Array(8);
    new DataView(chunk.buffer).setFloat64(0, value, true);
    return this.raw(chunk);
  }
  /**
   * Write a `fixed32` value, an unsigned, fixed-length 32-bit integer.
   */
  fixed32(value) {
    assertUInt32(value);
    let chunk = new Uint8Array(4);
    new DataView(chunk.buffer).setUint32(0, value, true);
    return this.raw(chunk);
  }
  /**
   * Write a `sfixed32` value, a signed, fixed-length 32-bit integer.
   */
  sfixed32(value) {
    assertInt32(value);
    let chunk = new Uint8Array(4);
    new DataView(chunk.buffer).setInt32(0, value, true);
    return this.raw(chunk);
  }
  /**
   * Write a `sint32` value, a signed, zigzag-encoded 32-bit varint.
   */
  sint32(value) {
    assertInt32(value);
    value = (value << 1 ^ value >> 31) >>> 0;
    varint32write(value, this.buf);
    return this;
  }
  /**
   * Write a `fixed64` value, a signed, fixed-length 64-bit integer.
   */
  sfixed64(value) {
    let chunk = new Uint8Array(8), view = new DataView(chunk.buffer), tc = protoInt64.enc(value);
    view.setInt32(0, tc.lo, true);
    view.setInt32(4, tc.hi, true);
    return this.raw(chunk);
  }
  /**
   * Write a `fixed64` value, an unsigned, fixed-length 64 bit integer.
   */
  fixed64(value) {
    let chunk = new Uint8Array(8), view = new DataView(chunk.buffer), tc = protoInt64.uEnc(value);
    view.setInt32(0, tc.lo, true);
    view.setInt32(4, tc.hi, true);
    return this.raw(chunk);
  }
  /**
   * Write a `int64` value, a signed 64-bit varint.
   */
  int64(value) {
    let tc = protoInt64.enc(value);
    varint64write(tc.lo, tc.hi, this.buf);
    return this;
  }
  /**
   * Write a `sint64` value, a signed, zig-zag-encoded 64-bit varint.
   */
  sint64(value) {
    let tc = protoInt64.enc(value), sign = tc.hi >> 31, lo2 = tc.lo << 1 ^ sign, hi2 = (tc.hi << 1 | tc.lo >>> 31) ^ sign;
    varint64write(lo2, hi2, this.buf);
    return this;
  }
  /**
   * Write a `uint64` value, an unsigned 64-bit varint.
   */
  uint64(value) {
    let tc = protoInt64.uEnc(value);
    varint64write(tc.lo, tc.hi, this.buf);
    return this;
  }
};
var BinaryReader = class {
  constructor(buf, textDecoder) {
    this.varint64 = varint64read;
    this.uint32 = varint32read;
    this.buf = buf;
    this.len = buf.length;
    this.pos = 0;
    this.view = new DataView(buf.buffer, buf.byteOffset, buf.byteLength);
    this.textDecoder = textDecoder !== null && textDecoder !== void 0 ? textDecoder : new TextDecoder();
  }
  /**
   * Reads a tag - field number and wire type.
   */
  tag() {
    let tag = this.uint32(), fieldNo = tag >>> 3, wireType = tag & 7;
    if (fieldNo <= 0 || wireType < 0 || wireType > 5) throw new Error("illegal tag: field no " + fieldNo + " wire type " + wireType);
    return [fieldNo, wireType];
  }
  /**
   * Skip one element and return the skipped data.
   *
   * When skipping StartGroup, provide the tags field number to check for
   * matching field number in the EndGroup tag.
   */
  skip(wireType, fieldNo) {
    let start = this.pos;
    switch (wireType) {
      case WireType.Varint:
        while (this.buf[this.pos++] & 128) {
        }
        break;
      case WireType.Bit64:
        this.pos += 4;
      case WireType.Bit32:
        this.pos += 4;
        break;
      case WireType.LengthDelimited:
        let len = this.uint32();
        this.pos += len;
        break;
      case WireType.StartGroup:
        for (; ; ) {
          const _this$tag = this.tag(), _this$tag2 = _slicedToArray(_this$tag, 2), fn4 = _this$tag2[0], wt3 = _this$tag2[1];
          if (wt3 === WireType.EndGroup) {
            if (fieldNo !== void 0 && fn4 !== fieldNo) {
              throw new Error("invalid end group tag");
            }
            break;
          }
          this.skip(wt3, fn4);
        }
        break;
      default:
        throw new Error("cant skip wire type " + wireType);
    }
    this.assertBounds();
    return this.buf.subarray(start, this.pos);
  }
  /**
   * Throws error if position in byte array is out of range.
   */
  assertBounds() {
    if (this.pos > this.len) throw new RangeError("premature EOF");
  }
  /**
   * Read a `int32` field, a signed 32 bit varint.
   */
  int32() {
    return this.uint32() | 0;
  }
  /**
   * Read a `sint32` field, a signed, zigzag-encoded 32-bit varint.
   */
  sint32() {
    let zze = this.uint32();
    return zze >>> 1 ^ -(zze & 1);
  }
  /**
   * Read a `int64` field, a signed 64-bit varint.
   */
  int64() {
    return protoInt64.dec(...this.varint64());
  }
  /**
   * Read a `uint64` field, an unsigned 64-bit varint.
   */
  uint64() {
    return protoInt64.uDec(...this.varint64());
  }
  /**
   * Read a `sint64` field, a signed, zig-zag-encoded 64-bit varint.
   */
  sint64() {
    let _this$varint = this.varint64(), _this$varint2 = _slicedToArray(_this$varint, 2), lo2 = _this$varint2[0], hi2 = _this$varint2[1];
    let s = -(lo2 & 1);
    lo2 = (lo2 >>> 1 | (hi2 & 1) << 31) ^ s;
    hi2 = hi2 >>> 1 ^ s;
    return protoInt64.dec(lo2, hi2);
  }
  /**
   * Read a `bool` field, a variant.
   */
  bool() {
    let _this$varint3 = this.varint64(), _this$varint4 = _slicedToArray(_this$varint3, 2), lo2 = _this$varint4[0], hi2 = _this$varint4[1];
    return lo2 !== 0 || hi2 !== 0;
  }
  /**
   * Read a `fixed32` field, an unsigned, fixed-length 32-bit integer.
   */
  fixed32() {
    return this.view.getUint32((this.pos += 4) - 4, true);
  }
  /**
   * Read a `sfixed32` field, a signed, fixed-length 32-bit integer.
   */
  sfixed32() {
    return this.view.getInt32((this.pos += 4) - 4, true);
  }
  /**
   * Read a `fixed64` field, an unsigned, fixed-length 64 bit integer.
   */
  fixed64() {
    return protoInt64.uDec(this.sfixed32(), this.sfixed32());
  }
  /**
   * Read a `fixed64` field, a signed, fixed-length 64-bit integer.
   */
  sfixed64() {
    return protoInt64.dec(this.sfixed32(), this.sfixed32());
  }
  /**
   * Read a `float` field, 32-bit floating point number.
   */
  float() {
    return this.view.getFloat32((this.pos += 4) - 4, true);
  }
  /**
   * Read a `double` field, a 64-bit floating point number.
   */
  double() {
    return this.view.getFloat64((this.pos += 8) - 8, true);
  }
  /**
   * Read a `bytes` field, length-delimited arbitrary data.
   */
  bytes() {
    let len = this.uint32(), start = this.pos;
    this.pos += len;
    this.assertBounds();
    return this.buf.subarray(start, start + len);
  }
  /**
   * Read a `string` field, length-delimited data converted to UTF-8 text.
   */
  string() {
    return this.textDecoder.decode(this.bytes());
  }
};
function makeExtension(runtime, typeName, extendee, field) {
  let fi2;
  return {
    typeName,
    extendee,
    get field() {
      if (!fi2) {
        const i2 = typeof field == "function" ? field() : field;
        i2.name = typeName.split(".").pop();
        i2.jsonName = "[".concat(typeName, "]");
        fi2 = runtime.util.newFieldList([i2]).list()[0];
      }
      return fi2;
    },
    runtime
  };
}
function createExtensionContainer(extension) {
  const localName = extension.field.localName;
  const container = /* @__PURE__ */ Object.create(null);
  container[localName] = initExtensionField(extension);
  return [container, () => container[localName]];
}
function initExtensionField(ext) {
  const field = ext.field;
  if (field.repeated) {
    return [];
  }
  if (field.default !== void 0) {
    return field.default;
  }
  switch (field.kind) {
    case "enum":
      return field.T.values[0].no;
    case "scalar":
      return scalarZeroValue(field.T, field.L);
    case "message":
      const T2 = field.T, value = new T2();
      return T2.fieldWrapper ? T2.fieldWrapper.unwrapField(value) : value;
    case "map":
      throw "map fields are not allowed to be extensions";
  }
}
function filterUnknownFields(unknownFields, field) {
  if (!field.repeated && (field.kind == "enum" || field.kind == "scalar")) {
    for (let i2 = unknownFields.length - 1; i2 >= 0; --i2) {
      if (unknownFields[i2].no == field.no) {
        return [unknownFields[i2]];
      }
    }
    return [];
  }
  return unknownFields.filter((uf) => uf.no === field.no);
}
var encTable = "ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/".split("");
var decTable = [];
for (let i2 = 0; i2 < encTable.length; i2++) decTable[encTable[i2].charCodeAt(0)] = i2;
decTable["-".charCodeAt(0)] = encTable.indexOf("+");
decTable["_".charCodeAt(0)] = encTable.indexOf("/");
var protoBase64 = {
  /**
   * Decodes a base64 string to a byte array.
   *
   * - ignores white-space, including line breaks and tabs
   * - allows inner padding (can decode concatenated base64 strings)
   * - does not require padding
   * - understands base64url encoding:
   *   "-" instead of "+",
   *   "_" instead of "/",
   *   no padding
   */
  dec(base64Str) {
    let es2 = base64Str.length * 3 / 4;
    if (base64Str[base64Str.length - 2] == "=") es2 -= 2;
    else if (base64Str[base64Str.length - 1] == "=") es2 -= 1;
    let bytes = new Uint8Array(es2), bytePos = 0, groupPos = 0, b, p = 0;
    for (let i2 = 0; i2 < base64Str.length; i2++) {
      b = decTable[base64Str.charCodeAt(i2)];
      if (b === void 0) {
        switch (base64Str[i2]) {
          case "=":
            groupPos = 0;
          case "\n":
          case "\r":
          case "	":
          case " ":
            continue;
          default:
            throw Error("invalid base64 string.");
        }
      }
      switch (groupPos) {
        case 0:
          p = b;
          groupPos = 1;
          break;
        case 1:
          bytes[bytePos++] = p << 2 | (b & 48) >> 4;
          p = b;
          groupPos = 2;
          break;
        case 2:
          bytes[bytePos++] = (p & 15) << 4 | (b & 60) >> 2;
          p = b;
          groupPos = 3;
          break;
        case 3:
          bytes[bytePos++] = (p & 3) << 6 | b;
          groupPos = 0;
          break;
      }
    }
    if (groupPos == 1) throw Error("invalid base64 string.");
    return bytes.subarray(0, bytePos);
  },
  /**
   * Encode a byte array to a base64 string.
   */
  enc(bytes) {
    let base64 = "", groupPos = 0, b, p = 0;
    for (let i2 = 0; i2 < bytes.length; i2++) {
      b = bytes[i2];
      switch (groupPos) {
        case 0:
          base64 += encTable[b >> 2];
          p = (b & 3) << 4;
          groupPos = 1;
          break;
        case 1:
          base64 += encTable[p | b >> 4];
          p = (b & 15) << 2;
          groupPos = 2;
          break;
        case 2:
          base64 += encTable[p | b >> 6];
          base64 += encTable[b & 63];
          groupPos = 0;
          break;
      }
    }
    if (groupPos) {
      base64 += encTable[p];
      base64 += "=";
      if (groupPos == 1) base64 += "=";
    }
    return base64;
  }
};
function getExtension(message, extension, options) {
  assertExtendee(extension, message);
  const opt = extension.runtime.bin.makeReadOptions(options);
  const ufs = filterUnknownFields(message.getType().runtime.bin.listUnknownFields(message), extension.field);
  const _createExtensionConta = createExtensionContainer(extension), _createExtensionConta2 = _slicedToArray(_createExtensionConta, 2), container = _createExtensionConta2[0], get = _createExtensionConta2[1];
  for (const uf of ufs) {
    extension.runtime.bin.readField(container, opt.readerFactory(uf.data), extension.field, uf.wireType, opt);
  }
  return get();
}
function setExtension(message, extension, value, options) {
  assertExtendee(extension, message);
  const readOpt = extension.runtime.bin.makeReadOptions(options);
  const writeOpt = extension.runtime.bin.makeWriteOptions(options);
  if (hasExtension(message, extension)) {
    const ufs = message.getType().runtime.bin.listUnknownFields(message).filter((uf) => uf.no != extension.field.no);
    message.getType().runtime.bin.discardUnknownFields(message);
    for (const uf of ufs) {
      message.getType().runtime.bin.onUnknownField(message, uf.no, uf.wireType, uf.data);
    }
  }
  const writer2 = writeOpt.writerFactory();
  let f = extension.field;
  if (!f.opt && !f.repeated && (f.kind == "enum" || f.kind == "scalar")) {
    f = Object.assign(Object.assign({}, extension.field), {
      opt: true
    });
  }
  extension.runtime.bin.writeField(f, value, writer2, writeOpt);
  const reader = readOpt.readerFactory(writer2.finish());
  while (reader.pos < reader.len) {
    const _reader$tag = reader.tag(), _reader$tag2 = _slicedToArray(_reader$tag, 2), no2 = _reader$tag2[0], wireType = _reader$tag2[1];
    const data = reader.skip(wireType, no2);
    message.getType().runtime.bin.onUnknownField(message, no2, wireType, data);
  }
}
function hasExtension(message, extension) {
  const messageType = message.getType();
  return extension.extendee.typeName === messageType.typeName && !!messageType.runtime.bin.listUnknownFields(message).find((uf) => uf.no == extension.field.no);
}
function assertExtendee(extension, message) {
  assert(extension.extendee.typeName == message.getType().typeName, "extension ".concat(extension.typeName, " can only be applied to message ").concat(extension.extendee.typeName));
}
function isFieldSet(field, target) {
  const localName = field.localName;
  if (field.repeated) {
    return target[localName].length > 0;
  }
  if (field.oneof) {
    return target[field.oneof.localName].case === localName;
  }
  switch (field.kind) {
    case "enum":
    case "scalar":
      if (field.opt || field.req) {
        return target[localName] !== void 0;
      }
      if (field.kind == "enum") {
        return target[localName] !== field.T.values[0].no;
      }
      return !isScalarZeroValue(field.T, target[localName]);
    case "message":
      return target[localName] !== void 0;
    case "map":
      return Object.keys(target[localName]).length > 0;
  }
}
function clearField(field, target) {
  const localName = field.localName;
  const implicitPresence = !field.opt && !field.req;
  if (field.repeated) {
    target[localName] = [];
  } else if (field.oneof) {
    target[field.oneof.localName] = {
      case: void 0
    };
  } else {
    switch (field.kind) {
      case "map":
        target[localName] = {};
        break;
      case "enum":
        target[localName] = implicitPresence ? field.T.values[0].no : void 0;
        break;
      case "scalar":
        target[localName] = implicitPresence ? scalarZeroValue(field.T, field.L) : void 0;
        break;
      case "message":
        target[localName] = void 0;
        break;
    }
  }
}
function isMessage(arg, type) {
  if (arg === null || typeof arg != "object") {
    return false;
  }
  if (!Object.getOwnPropertyNames(Message.prototype).every((m) => m in arg && typeof arg[m] == "function")) {
    return false;
  }
  const actualType = arg.getType();
  if (actualType === null || typeof actualType != "function" || !("typeName" in actualType) || typeof actualType.typeName != "string") {
    return false;
  }
  return type === void 0 ? true : actualType.typeName == type.typeName;
}
function wrapField(type, value) {
  if (isMessage(value) || !type.fieldWrapper) {
    return value;
  }
  return type.fieldWrapper.wrapField(value);
}
({
  "google.protobuf.DoubleValue": ScalarType.DOUBLE,
  "google.protobuf.FloatValue": ScalarType.FLOAT,
  "google.protobuf.Int64Value": ScalarType.INT64,
  "google.protobuf.UInt64Value": ScalarType.UINT64,
  "google.protobuf.Int32Value": ScalarType.INT32,
  "google.protobuf.UInt32Value": ScalarType.UINT32,
  "google.protobuf.BoolValue": ScalarType.BOOL,
  "google.protobuf.StringValue": ScalarType.STRING,
  "google.protobuf.BytesValue": ScalarType.BYTES
});
var jsonReadDefaults = {
  ignoreUnknownFields: false
};
var jsonWriteDefaults = {
  emitDefaultValues: false,
  enumAsInteger: false,
  useProtoFieldName: false,
  prettySpaces: 0
};
function makeReadOptions$1(options) {
  return options ? Object.assign(Object.assign({}, jsonReadDefaults), options) : jsonReadDefaults;
}
function makeWriteOptions$1(options) {
  return options ? Object.assign(Object.assign({}, jsonWriteDefaults), options) : jsonWriteDefaults;
}
var tokenNull = Symbol();
var tokenIgnoredUnknownEnum = Symbol();
function makeJsonFormat() {
  return {
    makeReadOptions: makeReadOptions$1,
    makeWriteOptions: makeWriteOptions$1,
    readMessage(type, json2, options, message) {
      if (json2 == null || Array.isArray(json2) || typeof json2 != "object") {
        throw new Error("cannot decode message ".concat(type.typeName, " from JSON: ").concat(debugJsonValue(json2)));
      }
      message = message !== null && message !== void 0 ? message : new type();
      const oneofSeen = /* @__PURE__ */ new Map();
      const registry = options.typeRegistry;
      for (const _ref of Object.entries(json2)) {
        var _ref2 = _slicedToArray(_ref, 2);
        const jsonKey = _ref2[0];
        const jsonValue = _ref2[1];
        const field = type.fields.findJsonName(jsonKey);
        if (field) {
          if (field.oneof) {
            if (jsonValue === null && field.kind == "scalar") {
              continue;
            }
            const seen = oneofSeen.get(field.oneof);
            if (seen !== void 0) {
              throw new Error("cannot decode message ".concat(type.typeName, ' from JSON: multiple keys for oneof "').concat(field.oneof.name, '" present: "').concat(seen, '", "').concat(jsonKey, '"'));
            }
            oneofSeen.set(field.oneof, jsonKey);
          }
          readField$1(message, jsonValue, field, options, type);
        } else {
          let found = false;
          if ((registry === null || registry === void 0 ? void 0 : registry.findExtension) && jsonKey.startsWith("[") && jsonKey.endsWith("]")) {
            const ext = registry.findExtension(jsonKey.substring(1, jsonKey.length - 1));
            if (ext && ext.extendee.typeName == type.typeName) {
              found = true;
              const _createExtensionConta = createExtensionContainer(ext), _createExtensionConta2 = _slicedToArray(_createExtensionConta, 2), container = _createExtensionConta2[0], get = _createExtensionConta2[1];
              readField$1(container, jsonValue, ext.field, options, ext);
              setExtension(message, ext, get(), options);
            }
          }
          if (!found && !options.ignoreUnknownFields) {
            throw new Error("cannot decode message ".concat(type.typeName, ' from JSON: key "').concat(jsonKey, '" is unknown'));
          }
        }
      }
      return message;
    },
    writeMessage(message, options) {
      const type = message.getType();
      const json2 = {};
      let field;
      try {
        for (field of type.fields.byNumber()) {
          if (!isFieldSet(field, message)) {
            if (field.req) {
              throw "required field not set";
            }
            if (!options.emitDefaultValues) {
              continue;
            }
            if (!canEmitFieldDefaultValue(field)) {
              continue;
            }
          }
          const value = field.oneof ? message[field.oneof.localName].value : message[field.localName];
          const jsonValue = writeField$1(field, value, options);
          if (jsonValue !== void 0) {
            json2[options.useProtoFieldName ? field.name : field.jsonName] = jsonValue;
          }
        }
        const registry = options.typeRegistry;
        if (registry === null || registry === void 0 ? void 0 : registry.findExtensionFor) {
          for (const uf of type.runtime.bin.listUnknownFields(message)) {
            const ext = registry.findExtensionFor(type.typeName, uf.no);
            if (ext && hasExtension(message, ext)) {
              const value = getExtension(message, ext, options);
              const jsonValue = writeField$1(ext.field, value, options);
              if (jsonValue !== void 0) {
                json2[ext.field.jsonName] = jsonValue;
              }
            }
          }
        }
      } catch (e4) {
        const m = field ? "cannot encode field ".concat(type.typeName, ".").concat(field.name, " to JSON") : "cannot encode message ".concat(type.typeName, " to JSON");
        const r2 = e4 instanceof Error ? e4.message : String(e4);
        throw new Error(m + (r2.length > 0 ? ": ".concat(r2) : ""));
      }
      return json2;
    },
    readScalar(type, json2, longType) {
      return readScalar$1(type, json2, longType !== null && longType !== void 0 ? longType : LongType.BIGINT, true);
    },
    writeScalar(type, value, emitDefaultValues) {
      if (value === void 0) {
        return void 0;
      }
      if (emitDefaultValues || isScalarZeroValue(type, value)) {
        return writeScalar$1(type, value);
      }
      return void 0;
    },
    debug: debugJsonValue
  };
}
function debugJsonValue(json2) {
  if (json2 === null) {
    return "null";
  }
  switch (typeof json2) {
    case "object":
      return Array.isArray(json2) ? "array" : "object";
    case "string":
      return json2.length > 100 ? "string" : '"'.concat(json2.split('"').join('\\"'), '"');
    default:
      return String(json2);
  }
}
function readField$1(target, jsonValue, field, options, parentType) {
  let localName = field.localName;
  if (field.repeated) {
    assert(field.kind != "map");
    if (jsonValue === null) {
      return;
    }
    if (!Array.isArray(jsonValue)) {
      throw new Error("cannot decode field ".concat(parentType.typeName, ".").concat(field.name, " from JSON: ").concat(debugJsonValue(jsonValue)));
    }
    const targetArray = target[localName];
    for (const jsonItem of jsonValue) {
      if (jsonItem === null) {
        throw new Error("cannot decode field ".concat(parentType.typeName, ".").concat(field.name, " from JSON: ").concat(debugJsonValue(jsonItem)));
      }
      switch (field.kind) {
        case "message":
          targetArray.push(field.T.fromJson(jsonItem, options));
          break;
        case "enum":
          const enumValue = readEnum(field.T, jsonItem, options.ignoreUnknownFields, true);
          if (enumValue !== tokenIgnoredUnknownEnum) {
            targetArray.push(enumValue);
          }
          break;
        case "scalar":
          try {
            targetArray.push(readScalar$1(field.T, jsonItem, field.L, true));
          } catch (e4) {
            let m = "cannot decode field ".concat(parentType.typeName, ".").concat(field.name, " from JSON: ").concat(debugJsonValue(jsonItem));
            if (e4 instanceof Error && e4.message.length > 0) {
              m += ": ".concat(e4.message);
            }
            throw new Error(m);
          }
          break;
      }
    }
  } else if (field.kind == "map") {
    if (jsonValue === null) {
      return;
    }
    if (typeof jsonValue != "object" || Array.isArray(jsonValue)) {
      throw new Error("cannot decode field ".concat(parentType.typeName, ".").concat(field.name, " from JSON: ").concat(debugJsonValue(jsonValue)));
    }
    const targetMap = target[localName];
    for (const _ref3 of Object.entries(jsonValue)) {
      var _ref4 = _slicedToArray(_ref3, 2);
      const jsonMapKey = _ref4[0];
      const jsonMapValue = _ref4[1];
      if (jsonMapValue === null) {
        throw new Error("cannot decode field ".concat(parentType.typeName, ".").concat(field.name, " from JSON: map value null"));
      }
      let key;
      try {
        key = readMapKey(field.K, jsonMapKey);
      } catch (e4) {
        let m = "cannot decode map key for field ".concat(parentType.typeName, ".").concat(field.name, " from JSON: ").concat(debugJsonValue(jsonValue));
        if (e4 instanceof Error && e4.message.length > 0) {
          m += ": ".concat(e4.message);
        }
        throw new Error(m);
      }
      switch (field.V.kind) {
        case "message":
          targetMap[key] = field.V.T.fromJson(jsonMapValue, options);
          break;
        case "enum":
          const enumValue = readEnum(field.V.T, jsonMapValue, options.ignoreUnknownFields, true);
          if (enumValue !== tokenIgnoredUnknownEnum) {
            targetMap[key] = enumValue;
          }
          break;
        case "scalar":
          try {
            targetMap[key] = readScalar$1(field.V.T, jsonMapValue, LongType.BIGINT, true);
          } catch (e4) {
            let m = "cannot decode map value for field ".concat(parentType.typeName, ".").concat(field.name, " from JSON: ").concat(debugJsonValue(jsonValue));
            if (e4 instanceof Error && e4.message.length > 0) {
              m += ": ".concat(e4.message);
            }
            throw new Error(m);
          }
          break;
      }
    }
  } else {
    if (field.oneof) {
      target = target[field.oneof.localName] = {
        case: localName
      };
      localName = "value";
    }
    switch (field.kind) {
      case "message":
        const messageType = field.T;
        if (jsonValue === null && messageType.typeName != "google.protobuf.Value") {
          return;
        }
        let currentValue = target[localName];
        if (isMessage(currentValue)) {
          currentValue.fromJson(jsonValue, options);
        } else {
          target[localName] = currentValue = messageType.fromJson(jsonValue, options);
          if (messageType.fieldWrapper && !field.oneof) {
            target[localName] = messageType.fieldWrapper.unwrapField(currentValue);
          }
        }
        break;
      case "enum":
        const enumValue = readEnum(field.T, jsonValue, options.ignoreUnknownFields, false);
        switch (enumValue) {
          case tokenNull:
            clearField(field, target);
            break;
          case tokenIgnoredUnknownEnum:
            break;
          default:
            target[localName] = enumValue;
            break;
        }
        break;
      case "scalar":
        try {
          const scalarValue = readScalar$1(field.T, jsonValue, field.L, false);
          switch (scalarValue) {
            case tokenNull:
              clearField(field, target);
              break;
            default:
              target[localName] = scalarValue;
              break;
          }
        } catch (e4) {
          let m = "cannot decode field ".concat(parentType.typeName, ".").concat(field.name, " from JSON: ").concat(debugJsonValue(jsonValue));
          if (e4 instanceof Error && e4.message.length > 0) {
            m += ": ".concat(e4.message);
          }
          throw new Error(m);
        }
        break;
    }
  }
}
function readMapKey(type, json2) {
  if (type === ScalarType.BOOL) {
    switch (json2) {
      case "true":
        json2 = true;
        break;
      case "false":
        json2 = false;
        break;
    }
  }
  return readScalar$1(type, json2, LongType.BIGINT, true).toString();
}
function readScalar$1(type, json2, longType, nullAsZeroValue) {
  if (json2 === null) {
    if (nullAsZeroValue) {
      return scalarZeroValue(type, longType);
    }
    return tokenNull;
  }
  switch (type) {
    case ScalarType.DOUBLE:
    case ScalarType.FLOAT:
      if (json2 === "NaN") return Number.NaN;
      if (json2 === "Infinity") return Number.POSITIVE_INFINITY;
      if (json2 === "-Infinity") return Number.NEGATIVE_INFINITY;
      if (json2 === "") {
        break;
      }
      if (typeof json2 == "string" && json2.trim().length !== json2.length) {
        break;
      }
      if (typeof json2 != "string" && typeof json2 != "number") {
        break;
      }
      const float = Number(json2);
      if (Number.isNaN(float)) {
        break;
      }
      if (!Number.isFinite(float)) {
        break;
      }
      if (type == ScalarType.FLOAT) assertFloat32(float);
      return float;
    case ScalarType.INT32:
    case ScalarType.FIXED32:
    case ScalarType.SFIXED32:
    case ScalarType.SINT32:
    case ScalarType.UINT32:
      let int32;
      if (typeof json2 == "number") int32 = json2;
      else if (typeof json2 == "string" && json2.length > 0) {
        if (json2.trim().length === json2.length) int32 = Number(json2);
      }
      if (int32 === void 0) break;
      if (type == ScalarType.UINT32 || type == ScalarType.FIXED32) assertUInt32(int32);
      else assertInt32(int32);
      return int32;
    case ScalarType.INT64:
    case ScalarType.SFIXED64:
    case ScalarType.SINT64:
      if (typeof json2 != "number" && typeof json2 != "string") break;
      const long = protoInt64.parse(json2);
      return longType ? long.toString() : long;
    case ScalarType.FIXED64:
    case ScalarType.UINT64:
      if (typeof json2 != "number" && typeof json2 != "string") break;
      const uLong = protoInt64.uParse(json2);
      return longType ? uLong.toString() : uLong;
    case ScalarType.BOOL:
      if (typeof json2 !== "boolean") break;
      return json2;
    case ScalarType.STRING:
      if (typeof json2 !== "string") {
        break;
      }
      try {
        encodeURIComponent(json2);
      } catch (e4) {
        throw new Error("invalid UTF8");
      }
      return json2;
    case ScalarType.BYTES:
      if (json2 === "") return new Uint8Array(0);
      if (typeof json2 !== "string") break;
      return protoBase64.dec(json2);
  }
  throw new Error();
}
function readEnum(type, json2, ignoreUnknownFields, nullAsZeroValue) {
  if (json2 === null) {
    if (type.typeName == "google.protobuf.NullValue") {
      return 0;
    }
    return nullAsZeroValue ? type.values[0].no : tokenNull;
  }
  switch (typeof json2) {
    case "number":
      if (Number.isInteger(json2)) {
        return json2;
      }
      break;
    case "string":
      const value = type.findName(json2);
      if (value !== void 0) {
        return value.no;
      }
      if (ignoreUnknownFields) {
        return tokenIgnoredUnknownEnum;
      }
      break;
  }
  throw new Error("cannot decode enum ".concat(type.typeName, " from JSON: ").concat(debugJsonValue(json2)));
}
function canEmitFieldDefaultValue(field) {
  if (field.repeated || field.kind == "map") {
    return true;
  }
  if (field.oneof) {
    return false;
  }
  if (field.kind == "message") {
    return false;
  }
  if (field.opt || field.req) {
    return false;
  }
  return true;
}
function writeField$1(field, value, options) {
  if (field.kind == "map") {
    assert(typeof value == "object" && value != null);
    const jsonObj = {};
    const entries = Object.entries(value);
    switch (field.V.kind) {
      case "scalar":
        for (const _ref5 of entries) {
          var _ref6 = _slicedToArray(_ref5, 2);
          const entryKey = _ref6[0];
          const entryValue = _ref6[1];
          jsonObj[entryKey.toString()] = writeScalar$1(field.V.T, entryValue);
        }
        break;
      case "message":
        for (const _ref7 of entries) {
          var _ref8 = _slicedToArray(_ref7, 2);
          const entryKey = _ref8[0];
          const entryValue = _ref8[1];
          jsonObj[entryKey.toString()] = entryValue.toJson(options);
        }
        break;
      case "enum":
        const enumType = field.V.T;
        for (const _ref9 of entries) {
          var _ref0 = _slicedToArray(_ref9, 2);
          const entryKey = _ref0[0];
          const entryValue = _ref0[1];
          jsonObj[entryKey.toString()] = writeEnum(enumType, entryValue, options.enumAsInteger);
        }
        break;
    }
    return options.emitDefaultValues || entries.length > 0 ? jsonObj : void 0;
  }
  if (field.repeated) {
    assert(Array.isArray(value));
    const jsonArr = [];
    switch (field.kind) {
      case "scalar":
        for (let i2 = 0; i2 < value.length; i2++) {
          jsonArr.push(writeScalar$1(field.T, value[i2]));
        }
        break;
      case "enum":
        for (let i2 = 0; i2 < value.length; i2++) {
          jsonArr.push(writeEnum(field.T, value[i2], options.enumAsInteger));
        }
        break;
      case "message":
        for (let i2 = 0; i2 < value.length; i2++) {
          jsonArr.push(value[i2].toJson(options));
        }
        break;
    }
    return options.emitDefaultValues || jsonArr.length > 0 ? jsonArr : void 0;
  }
  switch (field.kind) {
    case "scalar":
      return writeScalar$1(field.T, value);
    case "enum":
      return writeEnum(field.T, value, options.enumAsInteger);
    case "message":
      return wrapField(field.T, value).toJson(options);
  }
}
function writeEnum(type, value, enumAsInteger) {
  var _a2;
  assert(typeof value == "number");
  if (type.typeName == "google.protobuf.NullValue") {
    return null;
  }
  if (enumAsInteger) {
    return value;
  }
  const val = type.findNumber(value);
  return (_a2 = val === null || val === void 0 ? void 0 : val.name) !== null && _a2 !== void 0 ? _a2 : value;
}
function writeScalar$1(type, value) {
  switch (type) {
    case ScalarType.INT32:
    case ScalarType.SFIXED32:
    case ScalarType.SINT32:
    case ScalarType.FIXED32:
    case ScalarType.UINT32:
      assert(typeof value == "number");
      return value;
    case ScalarType.FLOAT:
    case ScalarType.DOUBLE:
      assert(typeof value == "number");
      if (Number.isNaN(value)) return "NaN";
      if (value === Number.POSITIVE_INFINITY) return "Infinity";
      if (value === Number.NEGATIVE_INFINITY) return "-Infinity";
      return value;
    case ScalarType.STRING:
      assert(typeof value == "string");
      return value;
    case ScalarType.BOOL:
      assert(typeof value == "boolean");
      return value;
    case ScalarType.UINT64:
    case ScalarType.FIXED64:
    case ScalarType.INT64:
    case ScalarType.SFIXED64:
    case ScalarType.SINT64:
      assert(typeof value == "bigint" || typeof value == "string" || typeof value == "number");
      return value.toString();
    case ScalarType.BYTES:
      assert(value instanceof Uint8Array);
      return protoBase64.enc(value);
  }
}
var unknownFieldsSymbol = Symbol("@bufbuild/protobuf/unknown-fields");
var readDefaults = {
  readUnknownFields: true,
  readerFactory: (bytes) => new BinaryReader(bytes)
};
var writeDefaults = {
  writeUnknownFields: true,
  writerFactory: () => new BinaryWriter()
};
function makeReadOptions(options) {
  return options ? Object.assign(Object.assign({}, readDefaults), options) : readDefaults;
}
function makeWriteOptions(options) {
  return options ? Object.assign(Object.assign({}, writeDefaults), options) : writeDefaults;
}
function makeBinaryFormat() {
  return {
    makeReadOptions,
    makeWriteOptions,
    listUnknownFields(message) {
      var _a2;
      return (_a2 = message[unknownFieldsSymbol]) !== null && _a2 !== void 0 ? _a2 : [];
    },
    discardUnknownFields(message) {
      delete message[unknownFieldsSymbol];
    },
    writeUnknownFields(message, writer2) {
      const m = message;
      const c = m[unknownFieldsSymbol];
      if (c) {
        for (const f of c) {
          writer2.tag(f.no, f.wireType).raw(f.data);
        }
      }
    },
    onUnknownField(message, no2, wireType, data) {
      const m = message;
      if (!Array.isArray(m[unknownFieldsSymbol])) {
        m[unknownFieldsSymbol] = [];
      }
      m[unknownFieldsSymbol].push({
        no: no2,
        wireType,
        data
      });
    },
    readMessage(message, reader, lengthOrEndTagFieldNo, options, delimitedMessageEncoding) {
      const type = message.getType();
      const end = delimitedMessageEncoding ? reader.len : reader.pos + lengthOrEndTagFieldNo;
      let fieldNo, wireType;
      while (reader.pos < end) {
        var _reader$tag = reader.tag();
        var _reader$tag2 = _slicedToArray(_reader$tag, 2);
        fieldNo = _reader$tag2[0];
        wireType = _reader$tag2[1];
        if (delimitedMessageEncoding === true && wireType == WireType.EndGroup) {
          break;
        }
        const field = type.fields.find(fieldNo);
        if (!field) {
          const data = reader.skip(wireType, fieldNo);
          if (options.readUnknownFields) {
            this.onUnknownField(message, fieldNo, wireType, data);
          }
          continue;
        }
        readField(message, reader, field, wireType, options);
      }
      if (delimitedMessageEncoding && // eslint-disable-line @typescript-eslint/strict-boolean-expressions
      (wireType != WireType.EndGroup || fieldNo !== lengthOrEndTagFieldNo)) {
        throw new Error("invalid end group tag");
      }
    },
    readField,
    writeMessage(message, writer2, options) {
      const type = message.getType();
      for (const field of type.fields.byNumber()) {
        if (!isFieldSet(field, message)) {
          if (field.req) {
            throw new Error("cannot encode field ".concat(type.typeName, ".").concat(field.name, " to binary: required field not set"));
          }
          continue;
        }
        const value = field.oneof ? message[field.oneof.localName].value : message[field.localName];
        writeField(field, value, writer2, options);
      }
      if (options.writeUnknownFields) {
        this.writeUnknownFields(message, writer2);
      }
      return writer2;
    },
    writeField(field, value, writer2, options) {
      if (value === void 0) {
        return void 0;
      }
      writeField(field, value, writer2, options);
    }
  };
}
function readField(target, reader, field, wireType, options) {
  let repeated = field.repeated, localName = field.localName;
  if (field.oneof) {
    target = target[field.oneof.localName];
    if (target.case != localName) {
      delete target.value;
    }
    target.case = localName;
    localName = "value";
  }
  switch (field.kind) {
    case "scalar":
    case "enum":
      const scalarType = field.kind == "enum" ? ScalarType.INT32 : field.T;
      let read = readScalar;
      if (field.kind == "scalar" && field.L > 0) {
        read = readScalarLTString;
      }
      if (repeated) {
        let arr = target[localName];
        const isPacked = wireType == WireType.LengthDelimited && scalarType != ScalarType.STRING && scalarType != ScalarType.BYTES;
        if (isPacked) {
          let e4 = reader.uint32() + reader.pos;
          while (reader.pos < e4) {
            arr.push(read(reader, scalarType));
          }
        } else {
          arr.push(read(reader, scalarType));
        }
      } else {
        target[localName] = read(reader, scalarType);
      }
      break;
    case "message":
      const messageType = field.T;
      if (repeated) {
        target[localName].push(readMessageField(reader, new messageType(), options, field));
      } else {
        if (isMessage(target[localName])) {
          readMessageField(reader, target[localName], options, field);
        } else {
          target[localName] = readMessageField(reader, new messageType(), options, field);
          if (messageType.fieldWrapper && !field.oneof && !field.repeated) {
            target[localName] = messageType.fieldWrapper.unwrapField(target[localName]);
          }
        }
      }
      break;
    case "map":
      let _readMapEntry = readMapEntry(field, reader, options), _readMapEntry2 = _slicedToArray(_readMapEntry, 2), mapKey = _readMapEntry2[0], mapVal = _readMapEntry2[1];
      target[localName][mapKey] = mapVal;
      break;
  }
}
function readMessageField(reader, message, options, field) {
  const format = message.getType().runtime.bin;
  const delimited = field === null || field === void 0 ? void 0 : field.delimited;
  format.readMessage(
    message,
    reader,
    delimited ? field.no : reader.uint32(),
    // eslint-disable-line @typescript-eslint/strict-boolean-expressions
    options,
    delimited
  );
  return message;
}
function readMapEntry(field, reader, options) {
  const length = reader.uint32(), end = reader.pos + length;
  let key, val;
  while (reader.pos < end) {
    const _reader$tag3 = reader.tag(), _reader$tag4 = _slicedToArray(_reader$tag3, 1), fieldNo = _reader$tag4[0];
    switch (fieldNo) {
      case 1:
        key = readScalar(reader, field.K);
        break;
      case 2:
        switch (field.V.kind) {
          case "scalar":
            val = readScalar(reader, field.V.T);
            break;
          case "enum":
            val = reader.int32();
            break;
          case "message":
            val = readMessageField(reader, new field.V.T(), options, void 0);
            break;
        }
        break;
    }
  }
  if (key === void 0) {
    key = scalarZeroValue(field.K, LongType.BIGINT);
  }
  if (typeof key != "string" && typeof key != "number") {
    key = key.toString();
  }
  if (val === void 0) {
    switch (field.V.kind) {
      case "scalar":
        val = scalarZeroValue(field.V.T, LongType.BIGINT);
        break;
      case "enum":
        val = field.V.T.values[0].no;
        break;
      case "message":
        val = new field.V.T();
        break;
    }
  }
  return [key, val];
}
function readScalarLTString(reader, type) {
  const v = readScalar(reader, type);
  return typeof v == "bigint" ? v.toString() : v;
}
function readScalar(reader, type) {
  switch (type) {
    case ScalarType.STRING:
      return reader.string();
    case ScalarType.BOOL:
      return reader.bool();
    case ScalarType.DOUBLE:
      return reader.double();
    case ScalarType.FLOAT:
      return reader.float();
    case ScalarType.INT32:
      return reader.int32();
    case ScalarType.INT64:
      return reader.int64();
    case ScalarType.UINT64:
      return reader.uint64();
    case ScalarType.FIXED64:
      return reader.fixed64();
    case ScalarType.BYTES:
      return reader.bytes();
    case ScalarType.FIXED32:
      return reader.fixed32();
    case ScalarType.SFIXED32:
      return reader.sfixed32();
    case ScalarType.SFIXED64:
      return reader.sfixed64();
    case ScalarType.SINT64:
      return reader.sint64();
    case ScalarType.UINT32:
      return reader.uint32();
    case ScalarType.SINT32:
      return reader.sint32();
  }
}
function writeField(field, value, writer2, options) {
  assert(value !== void 0);
  const repeated = field.repeated;
  switch (field.kind) {
    case "scalar":
    case "enum":
      let scalarType = field.kind == "enum" ? ScalarType.INT32 : field.T;
      if (repeated) {
        assert(Array.isArray(value));
        if (field.packed) {
          writePacked(writer2, scalarType, field.no, value);
        } else {
          for (const item of value) {
            writeScalar(writer2, scalarType, field.no, item);
          }
        }
      } else {
        writeScalar(writer2, scalarType, field.no, value);
      }
      break;
    case "message":
      if (repeated) {
        assert(Array.isArray(value));
        for (const item of value) {
          writeMessageField(writer2, options, field, item);
        }
      } else {
        writeMessageField(writer2, options, field, value);
      }
      break;
    case "map":
      assert(typeof value == "object" && value != null);
      for (const _ref of Object.entries(value)) {
        var _ref2 = _slicedToArray(_ref, 2);
        const key = _ref2[0];
        const val = _ref2[1];
        writeMapEntry(writer2, options, field, key, val);
      }
      break;
  }
}
function writeMapEntry(writer2, options, field, key, value) {
  writer2.tag(field.no, WireType.LengthDelimited);
  writer2.fork();
  let keyValue = key;
  switch (field.K) {
    case ScalarType.INT32:
    case ScalarType.FIXED32:
    case ScalarType.UINT32:
    case ScalarType.SFIXED32:
    case ScalarType.SINT32:
      keyValue = Number.parseInt(key);
      break;
    case ScalarType.BOOL:
      assert(key == "true" || key == "false");
      keyValue = key == "true";
      break;
  }
  writeScalar(writer2, field.K, 1, keyValue);
  switch (field.V.kind) {
    case "scalar":
      writeScalar(writer2, field.V.T, 2, value);
      break;
    case "enum":
      writeScalar(writer2, ScalarType.INT32, 2, value);
      break;
    case "message":
      assert(value !== void 0);
      writer2.tag(2, WireType.LengthDelimited).bytes(value.toBinary(options));
      break;
  }
  writer2.join();
}
function writeMessageField(writer2, options, field, value) {
  const message = wrapField(field.T, value);
  if (field.delimited) writer2.tag(field.no, WireType.StartGroup).raw(message.toBinary(options)).tag(field.no, WireType.EndGroup);
  else writer2.tag(field.no, WireType.LengthDelimited).bytes(message.toBinary(options));
}
function writeScalar(writer2, type, fieldNo, value) {
  assert(value !== void 0);
  let _scalarTypeInfo = scalarTypeInfo(type), _scalarTypeInfo2 = _slicedToArray(_scalarTypeInfo, 2), wireType = _scalarTypeInfo2[0], method = _scalarTypeInfo2[1];
  writer2.tag(fieldNo, wireType)[method](value);
}
function writePacked(writer2, type, fieldNo, value) {
  if (!value.length) {
    return;
  }
  writer2.tag(fieldNo, WireType.LengthDelimited).fork();
  let _scalarTypeInfo3 = scalarTypeInfo(type), _scalarTypeInfo4 = _slicedToArray(_scalarTypeInfo3, 2), method = _scalarTypeInfo4[1];
  for (let i2 = 0; i2 < value.length; i2++) {
    writer2[method](value[i2]);
  }
  writer2.join();
}
function scalarTypeInfo(type) {
  let wireType = WireType.Varint;
  switch (type) {
    case ScalarType.BYTES:
    case ScalarType.STRING:
      wireType = WireType.LengthDelimited;
      break;
    case ScalarType.DOUBLE:
    case ScalarType.FIXED64:
    case ScalarType.SFIXED64:
      wireType = WireType.Bit64;
      break;
    case ScalarType.FIXED32:
    case ScalarType.SFIXED32:
    case ScalarType.FLOAT:
      wireType = WireType.Bit32;
      break;
  }
  const method = ScalarType[type].toLowerCase();
  return [wireType, method];
}
function makeUtilCommon() {
  return {
    setEnumType,
    initPartial(source, target) {
      if (source === void 0) {
        return;
      }
      const type = target.getType();
      for (const member of type.fields.byMember()) {
        const localName = member.localName, t = target, s = source;
        if (s[localName] == null) {
          continue;
        }
        switch (member.kind) {
          case "oneof":
            const sk = s[localName].case;
            if (sk === void 0) {
              continue;
            }
            const sourceField = member.findField(sk);
            let val = s[localName].value;
            if (sourceField && sourceField.kind == "message" && !isMessage(val, sourceField.T)) {
              val = new sourceField.T(val);
            } else if (sourceField && sourceField.kind === "scalar" && sourceField.T === ScalarType.BYTES) {
              val = toU8Arr(val);
            }
            t[localName] = {
              case: sk,
              value: val
            };
            break;
          case "scalar":
          case "enum":
            let copy = s[localName];
            if (member.T === ScalarType.BYTES) {
              copy = member.repeated ? copy.map(toU8Arr) : toU8Arr(copy);
            }
            t[localName] = copy;
            break;
          case "map":
            switch (member.V.kind) {
              case "scalar":
              case "enum":
                if (member.V.T === ScalarType.BYTES) {
                  for (const _ref of Object.entries(s[localName])) {
                    var _ref2 = _slicedToArray(_ref, 2);
                    const k = _ref2[0];
                    const v = _ref2[1];
                    t[localName][k] = toU8Arr(v);
                  }
                } else {
                  Object.assign(t[localName], s[localName]);
                }
                break;
              case "message":
                const messageType = member.V.T;
                for (const k of Object.keys(s[localName])) {
                  let val2 = s[localName][k];
                  if (!messageType.fieldWrapper) {
                    val2 = new messageType(val2);
                  }
                  t[localName][k] = val2;
                }
                break;
            }
            break;
          case "message":
            const mt2 = member.T;
            if (member.repeated) {
              t[localName] = s[localName].map((val2) => isMessage(val2, mt2) ? val2 : new mt2(val2));
            } else {
              const val2 = s[localName];
              if (mt2.fieldWrapper) {
                if (
                  // We can't use BytesValue.typeName as that will create a circular import
                  mt2.typeName === "google.protobuf.BytesValue"
                ) {
                  t[localName] = toU8Arr(val2);
                } else {
                  t[localName] = val2;
                }
              } else {
                t[localName] = isMessage(val2, mt2) ? val2 : new mt2(val2);
              }
            }
            break;
        }
      }
    },
    // TODO use isFieldSet() here to support future field presence
    equals(type, a, b) {
      if (a === b) {
        return true;
      }
      if (!a || !b) {
        return false;
      }
      return type.fields.byMember().every((m) => {
        const va = a[m.localName];
        const vb = b[m.localName];
        if (m.repeated) {
          if (va.length !== vb.length) {
            return false;
          }
          switch (m.kind) {
            case "message":
              return va.every((a2, i2) => m.T.equals(a2, vb[i2]));
            case "scalar":
              return va.every((a2, i2) => scalarEquals(m.T, a2, vb[i2]));
            case "enum":
              return va.every((a2, i2) => scalarEquals(ScalarType.INT32, a2, vb[i2]));
          }
          throw new Error("repeated cannot contain ".concat(m.kind));
        }
        switch (m.kind) {
          case "message":
            let a2 = va;
            let b2 = vb;
            if (m.T.fieldWrapper) {
              if (a2 !== void 0 && !isMessage(a2)) {
                a2 = m.T.fieldWrapper.wrapField(a2);
              }
              if (b2 !== void 0 && !isMessage(b2)) {
                b2 = m.T.fieldWrapper.wrapField(b2);
              }
            }
            return m.T.equals(a2, b2);
          case "enum":
            return scalarEquals(ScalarType.INT32, va, vb);
          case "scalar":
            return scalarEquals(m.T, va, vb);
          case "oneof":
            if (va.case !== vb.case) {
              return false;
            }
            const s = m.findField(va.case);
            if (s === void 0) {
              return true;
            }
            switch (s.kind) {
              case "message":
                return s.T.equals(va.value, vb.value);
              case "enum":
                return scalarEquals(ScalarType.INT32, va.value, vb.value);
              case "scalar":
                return scalarEquals(s.T, va.value, vb.value);
            }
            throw new Error("oneof cannot contain ".concat(s.kind));
          case "map":
            const keys = Object.keys(va).concat(Object.keys(vb));
            switch (m.V.kind) {
              case "message":
                const messageType = m.V.T;
                return keys.every((k) => messageType.equals(va[k], vb[k]));
              case "enum":
                return keys.every((k) => scalarEquals(ScalarType.INT32, va[k], vb[k]));
              case "scalar":
                const scalarType = m.V.T;
                return keys.every((k) => scalarEquals(scalarType, va[k], vb[k]));
            }
            break;
        }
      });
    },
    // TODO use isFieldSet() here to support future field presence
    clone(message) {
      const type = message.getType(), target = new type(), any = target;
      for (const member of type.fields.byMember()) {
        const source = message[member.localName];
        let copy;
        if (member.repeated) {
          copy = source.map(cloneSingularField);
        } else if (member.kind == "map") {
          copy = any[member.localName];
          for (const _ref3 of Object.entries(source)) {
            var _ref4 = _slicedToArray(_ref3, 2);
            const key = _ref4[0];
            const v = _ref4[1];
            copy[key] = cloneSingularField(v);
          }
        } else if (member.kind == "oneof") {
          const f = member.findField(source.case);
          copy = f ? {
            case: source.case,
            value: cloneSingularField(source.value)
          } : {
            case: void 0
          };
        } else {
          copy = cloneSingularField(source);
        }
        any[member.localName] = copy;
      }
      for (const uf of type.runtime.bin.listUnknownFields(message)) {
        type.runtime.bin.onUnknownField(any, uf.no, uf.wireType, uf.data);
      }
      return target;
    }
  };
}
function cloneSingularField(value) {
  if (value === void 0) {
    return value;
  }
  if (isMessage(value)) {
    return value.clone();
  }
  if (value instanceof Uint8Array) {
    const c = new Uint8Array(value.byteLength);
    c.set(value);
    return c;
  }
  return value;
}
function toU8Arr(input) {
  return input instanceof Uint8Array ? input : new Uint8Array(input);
}
function makeProtoRuntime(syntax, newFieldList, initFields) {
  return {
    syntax,
    json: makeJsonFormat(),
    bin: makeBinaryFormat(),
    util: Object.assign(Object.assign({}, makeUtilCommon()), {
      newFieldList,
      initFields
    }),
    makeMessageType(typeName, fields, opt) {
      return makeMessageType(this, typeName, fields, opt);
    },
    makeEnum,
    makeEnumType,
    getEnumType,
    makeExtension(typeName, extendee, field) {
      return makeExtension(this, typeName, extendee, field);
    }
  };
}
var InternalFieldList = class {
  constructor(fields, normalizer) {
    this._fields = fields;
    this._normalizer = normalizer;
  }
  findJsonName(jsonName) {
    if (!this.jsonNames) {
      const t = {};
      for (const f of this.list()) {
        t[f.jsonName] = t[f.name] = f;
      }
      this.jsonNames = t;
    }
    return this.jsonNames[jsonName];
  }
  find(fieldNo) {
    if (!this.numbers) {
      const t = {};
      for (const f of this.list()) {
        t[f.no] = f;
      }
      this.numbers = t;
    }
    return this.numbers[fieldNo];
  }
  list() {
    if (!this.all) {
      this.all = this._normalizer(this._fields);
    }
    return this.all;
  }
  byNumber() {
    if (!this.numbersAsc) {
      this.numbersAsc = this.list().concat().sort((a, b) => a.no - b.no);
    }
    return this.numbersAsc;
  }
  byMember() {
    if (!this.members) {
      this.members = [];
      const a = this.members;
      let o2;
      for (const f of this.list()) {
        if (f.oneof) {
          if (f.oneof !== o2) {
            o2 = f.oneof;
            a.push(o2);
          }
        } else {
          a.push(f);
        }
      }
    }
    return this.members;
  }
};
function localFieldName(protoName, inOneof) {
  const name = protoCamelCase(protoName);
  if (inOneof) {
    return name;
  }
  return safeObjectProperty(safeMessageProperty(name));
}
function localOneofName(protoName) {
  return localFieldName(protoName, false);
}
var fieldJsonName = protoCamelCase;
function protoCamelCase(snakeCase) {
  let capNext = false;
  const b = [];
  for (let i2 = 0; i2 < snakeCase.length; i2++) {
    let c = snakeCase.charAt(i2);
    switch (c) {
      case "_":
        capNext = true;
        break;
      case "0":
      case "1":
      case "2":
      case "3":
      case "4":
      case "5":
      case "6":
      case "7":
      case "8":
      case "9":
        b.push(c);
        capNext = false;
        break;
      default:
        if (capNext) {
          capNext = false;
          c = c.toUpperCase();
        }
        b.push(c);
        break;
    }
  }
  return b.join("");
}
var reservedObjectProperties = /* @__PURE__ */ new Set([
  // names reserved by JavaScript
  "constructor",
  "toString",
  "toJSON",
  "valueOf"
]);
var reservedMessageProperties = /* @__PURE__ */ new Set([
  // names reserved by the runtime
  "getType",
  "clone",
  "equals",
  "fromBinary",
  "fromJson",
  "fromJsonString",
  "toBinary",
  "toJson",
  "toJsonString",
  // names reserved by the runtime for the future
  "toObject"
]);
var fallback = (name) => "".concat(name, "$");
var safeMessageProperty = (name) => {
  if (reservedMessageProperties.has(name)) {
    return fallback(name);
  }
  return name;
};
var safeObjectProperty = (name) => {
  if (reservedObjectProperties.has(name)) {
    return fallback(name);
  }
  return name;
};
var InternalOneofInfo = class {
  constructor(name) {
    this.kind = "oneof";
    this.repeated = false;
    this.packed = false;
    this.opt = false;
    this.req = false;
    this.default = void 0;
    this.fields = [];
    this.name = name;
    this.localName = localOneofName(name);
  }
  addField(field) {
    assert(field.oneof === this, "field ".concat(field.name, " not one of ").concat(this.name));
    this.fields.push(field);
  }
  findField(localName) {
    if (!this._lookup) {
      this._lookup = /* @__PURE__ */ Object.create(null);
      for (let i2 = 0; i2 < this.fields.length; i2++) {
        this._lookup[this.fields[i2].localName] = this.fields[i2];
      }
    }
    return this._lookup[localName];
  }
};
function normalizeFieldInfos(fieldInfos, packedByDefault) {
  var _a2, _b2, _c, _d, _e2, _f;
  const r2 = [];
  let o2;
  for (const field of typeof fieldInfos == "function" ? fieldInfos() : fieldInfos) {
    const f = field;
    f.localName = localFieldName(field.name, field.oneof !== void 0);
    f.jsonName = (_a2 = field.jsonName) !== null && _a2 !== void 0 ? _a2 : fieldJsonName(field.name);
    f.repeated = (_b2 = field.repeated) !== null && _b2 !== void 0 ? _b2 : false;
    if (field.kind == "scalar") {
      f.L = (_c = field.L) !== null && _c !== void 0 ? _c : LongType.BIGINT;
    }
    f.delimited = (_d = field.delimited) !== null && _d !== void 0 ? _d : false;
    f.req = (_e2 = field.req) !== null && _e2 !== void 0 ? _e2 : false;
    f.opt = (_f = field.opt) !== null && _f !== void 0 ? _f : false;
    if (field.packed === void 0) {
      {
        f.packed = field.kind == "enum" || field.kind == "scalar" && field.T != ScalarType.BYTES && field.T != ScalarType.STRING;
      }
    }
    if (field.oneof !== void 0) {
      const ooname = typeof field.oneof == "string" ? field.oneof : field.oneof.name;
      if (!o2 || o2.name != ooname) {
        o2 = new InternalOneofInfo(ooname);
      }
      f.oneof = o2;
      o2.addField(f);
    }
    r2.push(f);
  }
  return r2;
}
var proto3 = makeProtoRuntime(
  "proto3",
  (fields) => {
    return new InternalFieldList(fields, (source) => normalizeFieldInfos(source));
  },
  // TODO merge with proto2 and initExtensionField, also see initPartial, equals, clone
  (target) => {
    for (const member of target.getType().fields.byMember()) {
      if (member.opt) {
        continue;
      }
      const name = member.localName, t = target;
      if (member.repeated) {
        t[name] = [];
        continue;
      }
      switch (member.kind) {
        case "oneof":
          t[name] = {
            case: void 0
          };
          break;
        case "enum":
          t[name] = 0;
          break;
        case "map":
          t[name] = {};
          break;
        case "scalar":
          t[name] = scalarZeroValue(member.T, member.L);
          break;
      }
    }
  }
);
var Timestamp = class _Timestamp extends Message {
  constructor(data) {
    super();
    this.seconds = protoInt64.zero;
    this.nanos = 0;
    proto3.util.initPartial(data, this);
  }
  fromJson(json2, options) {
    if (typeof json2 !== "string") {
      throw new Error("cannot decode google.protobuf.Timestamp from JSON: ".concat(proto3.json.debug(json2)));
    }
    const matches = json2.match(/^([0-9]{4})-([0-9]{2})-([0-9]{2})T([0-9]{2}):([0-9]{2}):([0-9]{2})(?:Z|\.([0-9]{3,9})Z|([+-][0-9][0-9]:[0-9][0-9]))$/);
    if (!matches) {
      throw new Error("cannot decode google.protobuf.Timestamp from JSON: invalid RFC 3339 string");
    }
    const ms2 = Date.parse(matches[1] + "-" + matches[2] + "-" + matches[3] + "T" + matches[4] + ":" + matches[5] + ":" + matches[6] + (matches[8] ? matches[8] : "Z"));
    if (Number.isNaN(ms2)) {
      throw new Error("cannot decode google.protobuf.Timestamp from JSON: invalid RFC 3339 string");
    }
    if (ms2 < Date.parse("0001-01-01T00:00:00Z") || ms2 > Date.parse("9999-12-31T23:59:59Z")) {
      throw new Error("cannot decode message google.protobuf.Timestamp from JSON: must be from 0001-01-01T00:00:00Z to 9999-12-31T23:59:59Z inclusive");
    }
    this.seconds = protoInt64.parse(ms2 / 1e3);
    this.nanos = 0;
    if (matches[7]) {
      this.nanos = parseInt("1" + matches[7] + "0".repeat(9 - matches[7].length)) - 1e9;
    }
    return this;
  }
  toJson(options) {
    const ms2 = Number(this.seconds) * 1e3;
    if (ms2 < Date.parse("0001-01-01T00:00:00Z") || ms2 > Date.parse("9999-12-31T23:59:59Z")) {
      throw new Error("cannot encode google.protobuf.Timestamp to JSON: must be from 0001-01-01T00:00:00Z to 9999-12-31T23:59:59Z inclusive");
    }
    if (this.nanos < 0) {
      throw new Error("cannot encode google.protobuf.Timestamp to JSON: nanos must not be negative");
    }
    let z3 = "Z";
    if (this.nanos > 0) {
      const nanosStr = (this.nanos + 1e9).toString().substring(1);
      if (nanosStr.substring(3) === "000000") {
        z3 = "." + nanosStr.substring(0, 3) + "Z";
      } else if (nanosStr.substring(6) === "000") {
        z3 = "." + nanosStr.substring(0, 6) + "Z";
      } else {
        z3 = "." + nanosStr + "Z";
      }
    }
    return new Date(ms2).toISOString().replace(".000Z", z3);
  }
  toDate() {
    return new Date(Number(this.seconds) * 1e3 + Math.ceil(this.nanos / 1e6));
  }
  static now() {
    return _Timestamp.fromDate(/* @__PURE__ */ new Date());
  }
  static fromDate(date) {
    const ms2 = date.getTime();
    return new _Timestamp({
      seconds: protoInt64.parse(Math.floor(ms2 / 1e3)),
      nanos: ms2 % 1e3 * 1e6
    });
  }
  static fromBinary(bytes, options) {
    return new _Timestamp().fromBinary(bytes, options);
  }
  static fromJson(jsonValue, options) {
    return new _Timestamp().fromJson(jsonValue, options);
  }
  static fromJsonString(jsonString, options) {
    return new _Timestamp().fromJsonString(jsonString, options);
  }
  static equals(a, b) {
    return proto3.util.equals(_Timestamp, a, b);
  }
};
Timestamp.runtime = proto3;
Timestamp.typeName = "google.protobuf.Timestamp";
Timestamp.fields = proto3.util.newFieldList(() => [{
  no: 1,
  name: "seconds",
  kind: "scalar",
  T: 3
  /* ScalarType.INT64 */
}, {
  no: 2,
  name: "nanos",
  kind: "scalar",
  T: 5
  /* ScalarType.INT32 */
}]);
var MetricsBatch = proto3.makeMessageType("livekit.MetricsBatch", () => [{
  no: 1,
  name: "timestamp_ms",
  kind: "scalar",
  T: 3
  /* ScalarType.INT64 */
}, {
  no: 2,
  name: "normalized_timestamp",
  kind: "message",
  T: Timestamp
}, {
  no: 3,
  name: "str_data",
  kind: "scalar",
  T: 9,
  repeated: true
}, {
  no: 4,
  name: "time_series",
  kind: "message",
  T: TimeSeriesMetric,
  repeated: true
}, {
  no: 5,
  name: "events",
  kind: "message",
  T: EventMetric,
  repeated: true
}]);
var TimeSeriesMetric = proto3.makeMessageType("livekit.TimeSeriesMetric", () => [{
  no: 1,
  name: "label",
  kind: "scalar",
  T: 13
  /* ScalarType.UINT32 */
}, {
  no: 2,
  name: "participant_identity",
  kind: "scalar",
  T: 13
  /* ScalarType.UINT32 */
}, {
  no: 3,
  name: "track_sid",
  kind: "scalar",
  T: 13
  /* ScalarType.UINT32 */
}, {
  no: 4,
  name: "samples",
  kind: "message",
  T: MetricSample,
  repeated: true
}, {
  no: 5,
  name: "rid",
  kind: "scalar",
  T: 13
  /* ScalarType.UINT32 */
}]);
var MetricSample = proto3.makeMessageType("livekit.MetricSample", () => [{
  no: 1,
  name: "timestamp_ms",
  kind: "scalar",
  T: 3
  /* ScalarType.INT64 */
}, {
  no: 2,
  name: "normalized_timestamp",
  kind: "message",
  T: Timestamp
}, {
  no: 3,
  name: "value",
  kind: "scalar",
  T: 2
  /* ScalarType.FLOAT */
}]);
var EventMetric = proto3.makeMessageType("livekit.EventMetric", () => [{
  no: 1,
  name: "label",
  kind: "scalar",
  T: 13
  /* ScalarType.UINT32 */
}, {
  no: 2,
  name: "participant_identity",
  kind: "scalar",
  T: 13
  /* ScalarType.UINT32 */
}, {
  no: 3,
  name: "track_sid",
  kind: "scalar",
  T: 13
  /* ScalarType.UINT32 */
}, {
  no: 4,
  name: "start_timestamp_ms",
  kind: "scalar",
  T: 3
  /* ScalarType.INT64 */
}, {
  no: 5,
  name: "end_timestamp_ms",
  kind: "scalar",
  T: 3,
  opt: true
}, {
  no: 6,
  name: "normalized_start_timestamp",
  kind: "message",
  T: Timestamp
}, {
  no: 7,
  name: "normalized_end_timestamp",
  kind: "message",
  T: Timestamp,
  opt: true
}, {
  no: 8,
  name: "metadata",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 9,
  name: "rid",
  kind: "scalar",
  T: 13
  /* ScalarType.UINT32 */
}]);
var AudioCodec = proto3.makeEnum("livekit.AudioCodec", [{
  no: 0,
  name: "DEFAULT_AC"
}, {
  no: 1,
  name: "OPUS"
}, {
  no: 2,
  name: "AAC"
}, {
  no: 3,
  name: "AC_MP3"
}]);
var VideoCodec = proto3.makeEnum("livekit.VideoCodec", [{
  no: 0,
  name: "DEFAULT_VC"
}, {
  no: 1,
  name: "H264_BASELINE"
}, {
  no: 2,
  name: "H264_MAIN"
}, {
  no: 3,
  name: "H264_HIGH"
}, {
  no: 4,
  name: "VP8"
}]);
var ImageCodec = proto3.makeEnum("livekit.ImageCodec", [{
  no: 0,
  name: "IC_DEFAULT"
}, {
  no: 1,
  name: "IC_JPEG"
}]);
var BackupCodecPolicy$1 = proto3.makeEnum("livekit.BackupCodecPolicy", [{
  no: 0,
  name: "PREFER_REGRESSION"
}, {
  no: 1,
  name: "SIMULCAST"
}, {
  no: 2,
  name: "REGRESSION"
}]);
var TrackType = proto3.makeEnum("livekit.TrackType", [{
  no: 0,
  name: "AUDIO"
}, {
  no: 1,
  name: "VIDEO"
}, {
  no: 2,
  name: "DATA"
}]);
var TrackSource = proto3.makeEnum("livekit.TrackSource", [{
  no: 0,
  name: "UNKNOWN"
}, {
  no: 1,
  name: "CAMERA"
}, {
  no: 2,
  name: "MICROPHONE"
}, {
  no: 3,
  name: "SCREEN_SHARE"
}, {
  no: 4,
  name: "SCREEN_SHARE_AUDIO"
}]);
var VideoQuality$1 = proto3.makeEnum("livekit.VideoQuality", [{
  no: 0,
  name: "LOW"
}, {
  no: 1,
  name: "MEDIUM"
}, {
  no: 2,
  name: "HIGH"
}, {
  no: 3,
  name: "OFF"
}]);
var ConnectionQuality$1 = proto3.makeEnum("livekit.ConnectionQuality", [{
  no: 0,
  name: "POOR"
}, {
  no: 1,
  name: "GOOD"
}, {
  no: 2,
  name: "EXCELLENT"
}, {
  no: 3,
  name: "LOST"
}]);
var ClientConfigSetting = proto3.makeEnum("livekit.ClientConfigSetting", [{
  no: 0,
  name: "UNSET"
}, {
  no: 1,
  name: "DISABLED"
}, {
  no: 2,
  name: "ENABLED"
}]);
var DisconnectReason = proto3.makeEnum("livekit.DisconnectReason", [{
  no: 0,
  name: "UNKNOWN_REASON"
}, {
  no: 1,
  name: "CLIENT_INITIATED"
}, {
  no: 2,
  name: "DUPLICATE_IDENTITY"
}, {
  no: 3,
  name: "SERVER_SHUTDOWN"
}, {
  no: 4,
  name: "PARTICIPANT_REMOVED"
}, {
  no: 5,
  name: "ROOM_DELETED"
}, {
  no: 6,
  name: "STATE_MISMATCH"
}, {
  no: 7,
  name: "JOIN_FAILURE"
}, {
  no: 8,
  name: "MIGRATION"
}, {
  no: 9,
  name: "SIGNAL_CLOSE"
}, {
  no: 10,
  name: "ROOM_CLOSED"
}, {
  no: 11,
  name: "USER_UNAVAILABLE"
}, {
  no: 12,
  name: "USER_REJECTED"
}, {
  no: 13,
  name: "SIP_TRUNK_FAILURE"
}, {
  no: 14,
  name: "CONNECTION_TIMEOUT"
}, {
  no: 15,
  name: "MEDIA_FAILURE"
}, {
  no: 16,
  name: "AGENT_ERROR"
}]);
var ReconnectReason = proto3.makeEnum("livekit.ReconnectReason", [{
  no: 0,
  name: "RR_UNKNOWN"
}, {
  no: 1,
  name: "RR_SIGNAL_DISCONNECTED"
}, {
  no: 2,
  name: "RR_PUBLISHER_FAILED"
}, {
  no: 3,
  name: "RR_SUBSCRIBER_FAILED"
}, {
  no: 4,
  name: "RR_SWITCH_CANDIDATE"
}]);
var SubscriptionError = proto3.makeEnum("livekit.SubscriptionError", [{
  no: 0,
  name: "SE_UNKNOWN"
}, {
  no: 1,
  name: "SE_CODEC_UNSUPPORTED"
}, {
  no: 2,
  name: "SE_TRACK_NOTFOUND"
}]);
var AudioTrackFeature = proto3.makeEnum("livekit.AudioTrackFeature", [{
  no: 0,
  name: "TF_STEREO"
}, {
  no: 1,
  name: "TF_NO_DTX"
}, {
  no: 2,
  name: "TF_AUTO_GAIN_CONTROL"
}, {
  no: 3,
  name: "TF_ECHO_CANCELLATION"
}, {
  no: 4,
  name: "TF_NOISE_SUPPRESSION"
}, {
  no: 5,
  name: "TF_ENHANCED_NOISE_CANCELLATION"
}, {
  no: 6,
  name: "TF_PRECONNECT_BUFFER"
}]);
var PacketTrailerFeature = proto3.makeEnum("livekit.PacketTrailerFeature", [{
  no: 0,
  name: "PTF_USER_TIMESTAMP"
}, {
  no: 1,
  name: "PTF_FRAME_ID"
}, {
  no: 2,
  name: "PTF_USER_DATA"
}]);
var Room$1 = proto3.makeMessageType("livekit.Room", () => [{
  no: 1,
  name: "sid",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 2,
  name: "name",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 3,
  name: "empty_timeout",
  kind: "scalar",
  T: 13
  /* ScalarType.UINT32 */
}, {
  no: 14,
  name: "departure_timeout",
  kind: "scalar",
  T: 13
  /* ScalarType.UINT32 */
}, {
  no: 4,
  name: "max_participants",
  kind: "scalar",
  T: 13
  /* ScalarType.UINT32 */
}, {
  no: 5,
  name: "creation_time",
  kind: "scalar",
  T: 3
  /* ScalarType.INT64 */
}, {
  no: 15,
  name: "creation_time_ms",
  kind: "scalar",
  T: 3
  /* ScalarType.INT64 */
}, {
  no: 6,
  name: "turn_password",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 7,
  name: "enabled_codecs",
  kind: "message",
  T: Codec,
  repeated: true
}, {
  no: 8,
  name: "metadata",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 9,
  name: "num_participants",
  kind: "scalar",
  T: 13
  /* ScalarType.UINT32 */
}, {
  no: 11,
  name: "num_publishers",
  kind: "scalar",
  T: 13
  /* ScalarType.UINT32 */
}, {
  no: 10,
  name: "active_recording",
  kind: "scalar",
  T: 8
  /* ScalarType.BOOL */
}, {
  no: 13,
  name: "version",
  kind: "message",
  T: TimedVersion
}]);
var Codec = proto3.makeMessageType("livekit.Codec", () => [{
  no: 1,
  name: "mime",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 2,
  name: "fmtp_line",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}]);
var ParticipantPermission = proto3.makeMessageType("livekit.ParticipantPermission", () => [{
  no: 1,
  name: "can_subscribe",
  kind: "scalar",
  T: 8
  /* ScalarType.BOOL */
}, {
  no: 2,
  name: "can_publish",
  kind: "scalar",
  T: 8
  /* ScalarType.BOOL */
}, {
  no: 3,
  name: "can_publish_data",
  kind: "scalar",
  T: 8
  /* ScalarType.BOOL */
}, {
  no: 9,
  name: "can_publish_sources",
  kind: "enum",
  T: proto3.getEnumType(TrackSource),
  repeated: true
}, {
  no: 7,
  name: "hidden",
  kind: "scalar",
  T: 8
  /* ScalarType.BOOL */
}, {
  no: 8,
  name: "recorder",
  kind: "scalar",
  T: 8
  /* ScalarType.BOOL */
}, {
  no: 10,
  name: "can_update_metadata",
  kind: "scalar",
  T: 8
  /* ScalarType.BOOL */
}, {
  no: 11,
  name: "agent",
  kind: "scalar",
  T: 8
  /* ScalarType.BOOL */
}, {
  no: 12,
  name: "can_subscribe_metrics",
  kind: "scalar",
  T: 8
  /* ScalarType.BOOL */
}, {
  no: 13,
  name: "can_manage_agent_session",
  kind: "scalar",
  T: 8
  /* ScalarType.BOOL */
}]);
var ParticipantInfo = proto3.makeMessageType("livekit.ParticipantInfo", () => [{
  no: 1,
  name: "sid",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 2,
  name: "identity",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 3,
  name: "state",
  kind: "enum",
  T: proto3.getEnumType(ParticipantInfo_State)
}, {
  no: 4,
  name: "tracks",
  kind: "message",
  T: TrackInfo,
  repeated: true
}, {
  no: 5,
  name: "metadata",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 6,
  name: "joined_at",
  kind: "scalar",
  T: 3
  /* ScalarType.INT64 */
}, {
  no: 17,
  name: "joined_at_ms",
  kind: "scalar",
  T: 3
  /* ScalarType.INT64 */
}, {
  no: 9,
  name: "name",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 10,
  name: "version",
  kind: "scalar",
  T: 13
  /* ScalarType.UINT32 */
}, {
  no: 11,
  name: "permission",
  kind: "message",
  T: ParticipantPermission
}, {
  no: 12,
  name: "region",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 13,
  name: "is_publisher",
  kind: "scalar",
  T: 8
  /* ScalarType.BOOL */
}, {
  no: 14,
  name: "kind",
  kind: "enum",
  T: proto3.getEnumType(ParticipantInfo_Kind)
}, {
  no: 15,
  name: "attributes",
  kind: "map",
  K: 9,
  V: {
    kind: "scalar",
    T: 9
    /* ScalarType.STRING */
  }
}, {
  no: 16,
  name: "disconnect_reason",
  kind: "enum",
  T: proto3.getEnumType(DisconnectReason)
}, {
  no: 18,
  name: "kind_details",
  kind: "enum",
  T: proto3.getEnumType(ParticipantInfo_KindDetail),
  repeated: true
}, {
  no: 19,
  name: "data_tracks",
  kind: "message",
  T: DataTrackInfo$1,
  repeated: true
}, {
  no: 20,
  name: "client_protocol",
  kind: "scalar",
  T: 5
  /* ScalarType.INT32 */
}, {
  no: 21,
  name: "capabilities",
  kind: "enum",
  T: proto3.getEnumType(ClientInfo_Capability),
  repeated: true
}]);
var ParticipantInfo_State = proto3.makeEnum("livekit.ParticipantInfo.State", [{
  no: 0,
  name: "JOINING"
}, {
  no: 1,
  name: "JOINED"
}, {
  no: 2,
  name: "ACTIVE"
}, {
  no: 3,
  name: "DISCONNECTED"
}]);
var ParticipantInfo_Kind = proto3.makeEnum("livekit.ParticipantInfo.Kind", [{
  no: 0,
  name: "STANDARD"
}, {
  no: 1,
  name: "INGRESS"
}, {
  no: 2,
  name: "EGRESS"
}, {
  no: 3,
  name: "SIP"
}, {
  no: 4,
  name: "AGENT"
}, {
  no: 7,
  name: "CONNECTOR"
}, {
  no: 8,
  name: "BRIDGE"
}]);
var ParticipantInfo_KindDetail = proto3.makeEnum("livekit.ParticipantInfo.KindDetail", [{
  no: 0,
  name: "CLOUD_AGENT"
}, {
  no: 1,
  name: "FORWARDED"
}, {
  no: 2,
  name: "CONNECTOR_WHATSAPP"
}, {
  no: 3,
  name: "CONNECTOR_TWILIO"
}, {
  no: 4,
  name: "BRIDGE_RTSP"
}, {
  no: 5,
  name: "SIMULATION"
}]);
var Encryption_Type = proto3.makeEnum("livekit.Encryption.Type", [{
  no: 0,
  name: "NONE"
}, {
  no: 1,
  name: "GCM"
}, {
  no: 2,
  name: "CUSTOM"
}]);
var SimulcastCodecInfo = proto3.makeMessageType("livekit.SimulcastCodecInfo", () => [{
  no: 1,
  name: "mime_type",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 2,
  name: "mid",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 3,
  name: "cid",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 4,
  name: "layers",
  kind: "message",
  T: VideoLayer,
  repeated: true
}, {
  no: 5,
  name: "video_layer_mode",
  kind: "enum",
  T: proto3.getEnumType(VideoLayer_Mode)
}, {
  no: 6,
  name: "sdp_cid",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}]);
var TrackInfo = proto3.makeMessageType("livekit.TrackInfo", () => [{
  no: 1,
  name: "sid",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 2,
  name: "type",
  kind: "enum",
  T: proto3.getEnumType(TrackType)
}, {
  no: 3,
  name: "name",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 4,
  name: "muted",
  kind: "scalar",
  T: 8
  /* ScalarType.BOOL */
}, {
  no: 5,
  name: "width",
  kind: "scalar",
  T: 13
  /* ScalarType.UINT32 */
}, {
  no: 6,
  name: "height",
  kind: "scalar",
  T: 13
  /* ScalarType.UINT32 */
}, {
  no: 7,
  name: "simulcast",
  kind: "scalar",
  T: 8
  /* ScalarType.BOOL */
}, {
  no: 8,
  name: "disable_dtx",
  kind: "scalar",
  T: 8
  /* ScalarType.BOOL */
}, {
  no: 9,
  name: "source",
  kind: "enum",
  T: proto3.getEnumType(TrackSource)
}, {
  no: 10,
  name: "layers",
  kind: "message",
  T: VideoLayer,
  repeated: true
}, {
  no: 11,
  name: "mime_type",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 12,
  name: "mid",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 13,
  name: "codecs",
  kind: "message",
  T: SimulcastCodecInfo,
  repeated: true
}, {
  no: 14,
  name: "stereo",
  kind: "scalar",
  T: 8
  /* ScalarType.BOOL */
}, {
  no: 15,
  name: "disable_red",
  kind: "scalar",
  T: 8
  /* ScalarType.BOOL */
}, {
  no: 16,
  name: "encryption",
  kind: "enum",
  T: proto3.getEnumType(Encryption_Type)
}, {
  no: 17,
  name: "stream",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 18,
  name: "version",
  kind: "message",
  T: TimedVersion
}, {
  no: 19,
  name: "audio_features",
  kind: "enum",
  T: proto3.getEnumType(AudioTrackFeature),
  repeated: true
}, {
  no: 20,
  name: "backup_codec_policy",
  kind: "enum",
  T: proto3.getEnumType(BackupCodecPolicy$1)
}, {
  no: 21,
  name: "packet_trailer_features",
  kind: "enum",
  T: proto3.getEnumType(PacketTrailerFeature),
  repeated: true
}]);
var DataTrackInfo$1 = proto3.makeMessageType("livekit.DataTrackInfo", () => [{
  no: 1,
  name: "pub_handle",
  kind: "scalar",
  T: 13
  /* ScalarType.UINT32 */
}, {
  no: 2,
  name: "sid",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 3,
  name: "name",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 4,
  name: "encryption",
  kind: "enum",
  T: proto3.getEnumType(Encryption_Type)
}, {
  no: 5,
  name: "frame_encoding",
  kind: "message",
  T: DataTrackFrameEncoding,
  opt: true
}, {
  no: 6,
  name: "schema",
  kind: "message",
  T: DataTrackSchemaId,
  opt: true
}]);
var DataTrackFrameEncoding = proto3.makeMessageType("livekit.DataTrackFrameEncoding", () => [{
  no: 1,
  name: "well_known",
  kind: "enum",
  T: proto3.getEnumType(DataTrackFrameEncoding_WellKnownFrameEncoding),
  oneof: "value"
}, {
  no: 2,
  name: "custom",
  kind: "scalar",
  T: 9,
  oneof: "value"
}]);
var DataTrackFrameEncoding_WellKnownFrameEncoding = proto3.makeEnum("livekit.DataTrackFrameEncoding.WellKnownFrameEncoding", [{
  no: 0,
  name: "WELL_KNOWN_FRAME_ENCODING_UNSPECIFIED",
  localName: "UNSPECIFIED"
}, {
  no: 1,
  name: "WELL_KNOWN_FRAME_ENCODING_ROS1",
  localName: "ROS1"
}, {
  no: 2,
  name: "WELL_KNOWN_FRAME_ENCODING_CDR",
  localName: "CDR"
}, {
  no: 3,
  name: "WELL_KNOWN_FRAME_ENCODING_PROTOBUF",
  localName: "PROTOBUF"
}, {
  no: 4,
  name: "WELL_KNOWN_FRAME_ENCODING_FLATBUFFER",
  localName: "FLATBUFFER"
}, {
  no: 5,
  name: "WELL_KNOWN_FRAME_ENCODING_CBOR",
  localName: "CBOR"
}, {
  no: 6,
  name: "WELL_KNOWN_FRAME_ENCODING_MSGPACK",
  localName: "MSGPACK"
}, {
  no: 7,
  name: "WELL_KNOWN_FRAME_ENCODING_JSON",
  localName: "JSON"
}]);
var DataTrackSchemaEncoding = proto3.makeMessageType("livekit.DataTrackSchemaEncoding", () => [{
  no: 1,
  name: "well_known",
  kind: "enum",
  T: proto3.getEnumType(DataTrackSchemaEncoding_WellKnownSchemaEncoding),
  oneof: "value"
}, {
  no: 2,
  name: "custom",
  kind: "scalar",
  T: 9,
  oneof: "value"
}]);
var DataTrackSchemaEncoding_WellKnownSchemaEncoding = proto3.makeEnum("livekit.DataTrackSchemaEncoding.WellKnownSchemaEncoding", [{
  no: 0,
  name: "WELL_KNOWN_SCHEMA_ENCODING_UNSPECIFIED",
  localName: "UNSPECIFIED"
}, {
  no: 1,
  name: "WELL_KNOWN_SCHEMA_ENCODING_PROTOBUF",
  localName: "PROTOBUF"
}, {
  no: 2,
  name: "WELL_KNOWN_SCHEMA_ENCODING_FLATBUFFER",
  localName: "FLATBUFFER"
}, {
  no: 3,
  name: "WELL_KNOWN_SCHEMA_ENCODING_ROS1_MSG",
  localName: "ROS1_MSG"
}, {
  no: 4,
  name: "WELL_KNOWN_SCHEMA_ENCODING_ROS2_MSG",
  localName: "ROS2_MSG"
}, {
  no: 5,
  name: "WELL_KNOWN_SCHEMA_ENCODING_ROS2_IDL",
  localName: "ROS2_IDL"
}, {
  no: 6,
  name: "WELL_KNOWN_SCHEMA_ENCODING_OMG_IDL",
  localName: "OMG_IDL"
}, {
  no: 7,
  name: "WELL_KNOWN_SCHEMA_ENCODING_JSON_SCHEMA",
  localName: "JSON_SCHEMA"
}]);
var DataTrackSchemaId = proto3.makeMessageType("livekit.DataTrackSchemaId", () => [{
  no: 1,
  name: "name",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 2,
  name: "encoding",
  kind: "message",
  T: DataTrackSchemaEncoding
}]);
var DataTrackSubscriptionOptions = proto3.makeMessageType("livekit.DataTrackSubscriptionOptions", () => [{
  no: 1,
  name: "target_fps",
  kind: "scalar",
  T: 13,
  opt: true
}]);
var DataBlobKey = proto3.makeMessageType("livekit.DataBlobKey", () => [{
  no: 1,
  name: "generic",
  kind: "scalar",
  T: 9,
  oneof: "key"
}, {
  no: 2,
  name: "schema_id",
  kind: "message",
  T: DataTrackSchemaId,
  oneof: "key"
}]);
var DataBlob = proto3.makeMessageType("livekit.DataBlob", () => [{
  no: 1,
  name: "key",
  kind: "message",
  T: DataBlobKey
}, {
  no: 2,
  name: "contents",
  kind: "scalar",
  T: 12
  /* ScalarType.BYTES */
}]);
var VideoLayer = proto3.makeMessageType("livekit.VideoLayer", () => [{
  no: 1,
  name: "quality",
  kind: "enum",
  T: proto3.getEnumType(VideoQuality$1)
}, {
  no: 2,
  name: "width",
  kind: "scalar",
  T: 13
  /* ScalarType.UINT32 */
}, {
  no: 3,
  name: "height",
  kind: "scalar",
  T: 13
  /* ScalarType.UINT32 */
}, {
  no: 4,
  name: "bitrate",
  kind: "scalar",
  T: 13
  /* ScalarType.UINT32 */
}, {
  no: 5,
  name: "ssrc",
  kind: "scalar",
  T: 13
  /* ScalarType.UINT32 */
}, {
  no: 6,
  name: "spatial_layer",
  kind: "scalar",
  T: 5
  /* ScalarType.INT32 */
}, {
  no: 7,
  name: "rid",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 8,
  name: "repair_ssrc",
  kind: "scalar",
  T: 13
  /* ScalarType.UINT32 */
}]);
var VideoLayer_Mode = proto3.makeEnum("livekit.VideoLayer.Mode", [{
  no: 0,
  name: "MODE_UNUSED"
}, {
  no: 1,
  name: "ONE_SPATIAL_LAYER_PER_STREAM"
}, {
  no: 2,
  name: "MULTIPLE_SPATIAL_LAYERS_PER_STREAM"
}, {
  no: 3,
  name: "ONE_SPATIAL_LAYER_PER_STREAM_INCOMPLETE_RTCP_SR"
}]);
var DataPacket = proto3.makeMessageType("livekit.DataPacket", () => [{
  no: 1,
  name: "kind",
  kind: "enum",
  T: proto3.getEnumType(DataPacket_Kind)
}, {
  no: 4,
  name: "participant_identity",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 5,
  name: "destination_identities",
  kind: "scalar",
  T: 9,
  repeated: true
}, {
  no: 2,
  name: "user",
  kind: "message",
  T: UserPacket,
  oneof: "value"
}, {
  no: 3,
  name: "speaker",
  kind: "message",
  T: ActiveSpeakerUpdate,
  oneof: "value"
}, {
  no: 6,
  name: "sip_dtmf",
  kind: "message",
  T: SipDTMF,
  oneof: "value"
}, {
  no: 7,
  name: "transcription",
  kind: "message",
  T: Transcription,
  oneof: "value"
}, {
  no: 8,
  name: "metrics",
  kind: "message",
  T: MetricsBatch,
  oneof: "value"
}, {
  no: 9,
  name: "chat_message",
  kind: "message",
  T: ChatMessage,
  oneof: "value"
}, {
  no: 10,
  name: "rpc_request",
  kind: "message",
  T: RpcRequest,
  oneof: "value"
}, {
  no: 11,
  name: "rpc_ack",
  kind: "message",
  T: RpcAck,
  oneof: "value"
}, {
  no: 12,
  name: "rpc_response",
  kind: "message",
  T: RpcResponse,
  oneof: "value"
}, {
  no: 13,
  name: "stream_header",
  kind: "message",
  T: DataStream_Header,
  oneof: "value"
}, {
  no: 14,
  name: "stream_chunk",
  kind: "message",
  T: DataStream_Chunk,
  oneof: "value"
}, {
  no: 15,
  name: "stream_trailer",
  kind: "message",
  T: DataStream_Trailer,
  oneof: "value"
}, {
  no: 18,
  name: "encrypted_packet",
  kind: "message",
  T: EncryptedPacket,
  oneof: "value"
}, {
  no: 16,
  name: "sequence",
  kind: "scalar",
  T: 13
  /* ScalarType.UINT32 */
}, {
  no: 17,
  name: "participant_sid",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}]);
var DataPacket_Kind = proto3.makeEnum("livekit.DataPacket.Kind", [{
  no: 0,
  name: "RELIABLE"
}, {
  no: 1,
  name: "LOSSY"
}]);
var EncryptedPacket = proto3.makeMessageType("livekit.EncryptedPacket", () => [{
  no: 1,
  name: "encryption_type",
  kind: "enum",
  T: proto3.getEnumType(Encryption_Type)
}, {
  no: 2,
  name: "iv",
  kind: "scalar",
  T: 12
  /* ScalarType.BYTES */
}, {
  no: 3,
  name: "key_index",
  kind: "scalar",
  T: 13
  /* ScalarType.UINT32 */
}, {
  no: 4,
  name: "encrypted_value",
  kind: "scalar",
  T: 12
  /* ScalarType.BYTES */
}]);
var EncryptedPacketPayload = proto3.makeMessageType("livekit.EncryptedPacketPayload", () => [{
  no: 1,
  name: "user",
  kind: "message",
  T: UserPacket,
  oneof: "value"
}, {
  no: 3,
  name: "chat_message",
  kind: "message",
  T: ChatMessage,
  oneof: "value"
}, {
  no: 4,
  name: "rpc_request",
  kind: "message",
  T: RpcRequest,
  oneof: "value"
}, {
  no: 5,
  name: "rpc_ack",
  kind: "message",
  T: RpcAck,
  oneof: "value"
}, {
  no: 6,
  name: "rpc_response",
  kind: "message",
  T: RpcResponse,
  oneof: "value"
}, {
  no: 7,
  name: "stream_header",
  kind: "message",
  T: DataStream_Header,
  oneof: "value"
}, {
  no: 8,
  name: "stream_chunk",
  kind: "message",
  T: DataStream_Chunk,
  oneof: "value"
}, {
  no: 9,
  name: "stream_trailer",
  kind: "message",
  T: DataStream_Trailer,
  oneof: "value"
}]);
var ActiveSpeakerUpdate = proto3.makeMessageType("livekit.ActiveSpeakerUpdate", () => [{
  no: 1,
  name: "speakers",
  kind: "message",
  T: SpeakerInfo,
  repeated: true
}]);
var SpeakerInfo = proto3.makeMessageType("livekit.SpeakerInfo", () => [{
  no: 1,
  name: "sid",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 2,
  name: "level",
  kind: "scalar",
  T: 2
  /* ScalarType.FLOAT */
}, {
  no: 3,
  name: "active",
  kind: "scalar",
  T: 8
  /* ScalarType.BOOL */
}]);
var UserPacket = proto3.makeMessageType("livekit.UserPacket", () => [{
  no: 1,
  name: "participant_sid",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 5,
  name: "participant_identity",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 2,
  name: "payload",
  kind: "scalar",
  T: 12
  /* ScalarType.BYTES */
}, {
  no: 3,
  name: "destination_sids",
  kind: "scalar",
  T: 9,
  repeated: true
}, {
  no: 6,
  name: "destination_identities",
  kind: "scalar",
  T: 9,
  repeated: true
}, {
  no: 4,
  name: "topic",
  kind: "scalar",
  T: 9,
  opt: true
}, {
  no: 8,
  name: "id",
  kind: "scalar",
  T: 9,
  opt: true
}, {
  no: 9,
  name: "start_time",
  kind: "scalar",
  T: 4,
  opt: true
}, {
  no: 10,
  name: "end_time",
  kind: "scalar",
  T: 4,
  opt: true
}, {
  no: 11,
  name: "nonce",
  kind: "scalar",
  T: 12
  /* ScalarType.BYTES */
}]);
var SipDTMF = proto3.makeMessageType("livekit.SipDTMF", () => [{
  no: 3,
  name: "code",
  kind: "scalar",
  T: 13
  /* ScalarType.UINT32 */
}, {
  no: 4,
  name: "digit",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}]);
var Transcription = proto3.makeMessageType("livekit.Transcription", () => [{
  no: 2,
  name: "transcribed_participant_identity",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 3,
  name: "track_id",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 4,
  name: "segments",
  kind: "message",
  T: TranscriptionSegment,
  repeated: true
}]);
var TranscriptionSegment = proto3.makeMessageType("livekit.TranscriptionSegment", () => [{
  no: 1,
  name: "id",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 2,
  name: "text",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 3,
  name: "start_time",
  kind: "scalar",
  T: 4
  /* ScalarType.UINT64 */
}, {
  no: 4,
  name: "end_time",
  kind: "scalar",
  T: 4
  /* ScalarType.UINT64 */
}, {
  no: 5,
  name: "final",
  kind: "scalar",
  T: 8
  /* ScalarType.BOOL */
}, {
  no: 6,
  name: "language",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}]);
var ChatMessage = proto3.makeMessageType("livekit.ChatMessage", () => [{
  no: 1,
  name: "id",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 2,
  name: "timestamp",
  kind: "scalar",
  T: 3
  /* ScalarType.INT64 */
}, {
  no: 3,
  name: "edit_timestamp",
  kind: "scalar",
  T: 3,
  opt: true
}, {
  no: 4,
  name: "message",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 5,
  name: "deleted",
  kind: "scalar",
  T: 8
  /* ScalarType.BOOL */
}, {
  no: 6,
  name: "generated",
  kind: "scalar",
  T: 8
  /* ScalarType.BOOL */
}]);
var RpcRequest = proto3.makeMessageType("livekit.RpcRequest", () => [{
  no: 1,
  name: "id",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 2,
  name: "method",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 3,
  name: "payload",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 4,
  name: "response_timeout_ms",
  kind: "scalar",
  T: 13
  /* ScalarType.UINT32 */
}, {
  no: 5,
  name: "version",
  kind: "scalar",
  T: 13
  /* ScalarType.UINT32 */
}, {
  no: 6,
  name: "compressed_payload",
  kind: "scalar",
  T: 12
  /* ScalarType.BYTES */
}]);
var RpcAck = proto3.makeMessageType("livekit.RpcAck", () => [{
  no: 1,
  name: "request_id",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}]);
var RpcResponse = proto3.makeMessageType("livekit.RpcResponse", () => [{
  no: 1,
  name: "request_id",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 2,
  name: "payload",
  kind: "scalar",
  T: 9,
  oneof: "value"
}, {
  no: 3,
  name: "error",
  kind: "message",
  T: RpcError$1,
  oneof: "value"
}, {
  no: 4,
  name: "compressed_payload",
  kind: "scalar",
  T: 12,
  oneof: "value"
}]);
var RpcError$1 = proto3.makeMessageType("livekit.RpcError", () => [{
  no: 1,
  name: "code",
  kind: "scalar",
  T: 13
  /* ScalarType.UINT32 */
}, {
  no: 2,
  name: "message",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 3,
  name: "data",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}]);
var ParticipantTracks = proto3.makeMessageType("livekit.ParticipantTracks", () => [{
  no: 1,
  name: "participant_sid",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 2,
  name: "track_sids",
  kind: "scalar",
  T: 9,
  repeated: true
}]);
var ServerInfo = proto3.makeMessageType("livekit.ServerInfo", () => [{
  no: 1,
  name: "edition",
  kind: "enum",
  T: proto3.getEnumType(ServerInfo_Edition)
}, {
  no: 2,
  name: "version",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 3,
  name: "protocol",
  kind: "scalar",
  T: 5
  /* ScalarType.INT32 */
}, {
  no: 4,
  name: "region",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 5,
  name: "node_id",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 6,
  name: "debug_info",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 7,
  name: "agent_protocol",
  kind: "scalar",
  T: 5
  /* ScalarType.INT32 */
}]);
var ServerInfo_Edition = proto3.makeEnum("livekit.ServerInfo.Edition", [{
  no: 0,
  name: "Standard"
}, {
  no: 1,
  name: "Cloud"
}]);
var ClientInfo = proto3.makeMessageType("livekit.ClientInfo", () => [{
  no: 1,
  name: "sdk",
  kind: "enum",
  T: proto3.getEnumType(ClientInfo_SDK)
}, {
  no: 2,
  name: "version",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 3,
  name: "protocol",
  kind: "scalar",
  T: 5
  /* ScalarType.INT32 */
}, {
  no: 4,
  name: "os",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 5,
  name: "os_version",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 6,
  name: "device_model",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 7,
  name: "browser",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 8,
  name: "browser_version",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 9,
  name: "address",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 10,
  name: "network",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 11,
  name: "other_sdks",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 12,
  name: "client_protocol",
  kind: "scalar",
  T: 5
  /* ScalarType.INT32 */
}, {
  no: 13,
  name: "capabilities",
  kind: "enum",
  T: proto3.getEnumType(ClientInfo_Capability),
  repeated: true
}]);
var ClientInfo_SDK = proto3.makeEnum("livekit.ClientInfo.SDK", [{
  no: 0,
  name: "UNKNOWN"
}, {
  no: 1,
  name: "JS"
}, {
  no: 2,
  name: "SWIFT"
}, {
  no: 3,
  name: "ANDROID"
}, {
  no: 4,
  name: "FLUTTER"
}, {
  no: 5,
  name: "GO"
}, {
  no: 6,
  name: "UNITY"
}, {
  no: 7,
  name: "REACT_NATIVE"
}, {
  no: 8,
  name: "RUST"
}, {
  no: 9,
  name: "PYTHON"
}, {
  no: 10,
  name: "CPP"
}, {
  no: 11,
  name: "UNITY_WEB"
}, {
  no: 12,
  name: "NODE"
}, {
  no: 13,
  name: "UNREAL"
}, {
  no: 14,
  name: "ESP32"
}]);
var ClientInfo_Capability = proto3.makeEnum("livekit.ClientInfo.Capability", [{
  no: 0,
  name: "CAP_UNUSED"
}, {
  no: 1,
  name: "CAP_PACKET_TRAILER"
}, {
  no: 2,
  name: "CAP_COMPRESSION_DEFLATE_RAW"
}]);
var ClientConfiguration = proto3.makeMessageType("livekit.ClientConfiguration", () => [{
  no: 1,
  name: "video",
  kind: "message",
  T: VideoConfiguration
}, {
  no: 2,
  name: "screen",
  kind: "message",
  T: VideoConfiguration
}, {
  no: 3,
  name: "resume_connection",
  kind: "enum",
  T: proto3.getEnumType(ClientConfigSetting)
}, {
  no: 4,
  name: "disabled_codecs",
  kind: "message",
  T: DisabledCodecs
}, {
  no: 5,
  name: "force_relay",
  kind: "enum",
  T: proto3.getEnumType(ClientConfigSetting)
}]);
var VideoConfiguration = proto3.makeMessageType("livekit.VideoConfiguration", () => [{
  no: 1,
  name: "hardware_encoder",
  kind: "enum",
  T: proto3.getEnumType(ClientConfigSetting)
}]);
var DisabledCodecs = proto3.makeMessageType("livekit.DisabledCodecs", () => [{
  no: 1,
  name: "codecs",
  kind: "message",
  T: Codec,
  repeated: true
}, {
  no: 2,
  name: "publish",
  kind: "message",
  T: Codec,
  repeated: true
}]);
var TimedVersion = proto3.makeMessageType("livekit.TimedVersion", () => [{
  no: 1,
  name: "unix_micro",
  kind: "scalar",
  T: 3
  /* ScalarType.INT64 */
}, {
  no: 2,
  name: "ticks",
  kind: "scalar",
  T: 5
  /* ScalarType.INT32 */
}]);
var DataStream_OperationType = proto3.makeEnum("livekit.DataStream.OperationType", [{
  no: 0,
  name: "CREATE"
}, {
  no: 1,
  name: "UPDATE"
}, {
  no: 2,
  name: "DELETE"
}, {
  no: 3,
  name: "REACTION"
}]);
var DataStream_CompressionType = proto3.makeEnum("livekit.DataStream.CompressionType", [{
  no: 0,
  name: "NONE"
}, {
  no: 1,
  name: "DEFLATE_RAW"
}]);
var DataStream_TextHeader = proto3.makeMessageType("livekit.DataStream.TextHeader", () => [{
  no: 1,
  name: "operation_type",
  kind: "enum",
  T: proto3.getEnumType(DataStream_OperationType)
}, {
  no: 2,
  name: "version",
  kind: "scalar",
  T: 5
  /* ScalarType.INT32 */
}, {
  no: 3,
  name: "reply_to_stream_id",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 4,
  name: "attached_stream_ids",
  kind: "scalar",
  T: 9,
  repeated: true
}, {
  no: 5,
  name: "generated",
  kind: "scalar",
  T: 8
  /* ScalarType.BOOL */
}], {
  localName: "DataStream_TextHeader"
});
var DataStream_ByteHeader = proto3.makeMessageType("livekit.DataStream.ByteHeader", () => [{
  no: 1,
  name: "name",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}], {
  localName: "DataStream_ByteHeader"
});
var DataStream_Header = proto3.makeMessageType("livekit.DataStream.Header", () => [{
  no: 1,
  name: "stream_id",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 2,
  name: "timestamp",
  kind: "scalar",
  T: 3
  /* ScalarType.INT64 */
}, {
  no: 3,
  name: "topic",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 4,
  name: "mime_type",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 5,
  name: "total_length",
  kind: "scalar",
  T: 4,
  opt: true
}, {
  no: 7,
  name: "encryption_type",
  kind: "enum",
  T: proto3.getEnumType(Encryption_Type)
}, {
  no: 8,
  name: "attributes",
  kind: "map",
  K: 9,
  V: {
    kind: "scalar",
    T: 9
    /* ScalarType.STRING */
  }
}, {
  no: 9,
  name: "text_header",
  kind: "message",
  T: DataStream_TextHeader,
  oneof: "content_header"
}, {
  no: 10,
  name: "byte_header",
  kind: "message",
  T: DataStream_ByteHeader,
  oneof: "content_header"
}, {
  no: 11,
  name: "inline_content",
  kind: "scalar",
  T: 12,
  opt: true
}, {
  no: 12,
  name: "compression",
  kind: "enum",
  T: proto3.getEnumType(DataStream_CompressionType)
}], {
  localName: "DataStream_Header"
});
var DataStream_Chunk = proto3.makeMessageType("livekit.DataStream.Chunk", () => [{
  no: 1,
  name: "stream_id",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 2,
  name: "chunk_index",
  kind: "scalar",
  T: 4
  /* ScalarType.UINT64 */
}, {
  no: 3,
  name: "content",
  kind: "scalar",
  T: 12
  /* ScalarType.BYTES */
}, {
  no: 4,
  name: "version",
  kind: "scalar",
  T: 5
  /* ScalarType.INT32 */
}, {
  no: 5,
  name: "iv",
  kind: "scalar",
  T: 12,
  opt: true
}], {
  localName: "DataStream_Chunk"
});
var DataStream_Trailer = proto3.makeMessageType("livekit.DataStream.Trailer", () => [{
  no: 1,
  name: "stream_id",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 2,
  name: "reason",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 3,
  name: "attributes",
  kind: "map",
  K: 9,
  V: {
    kind: "scalar",
    T: 9
    /* ScalarType.STRING */
  }
}], {
  localName: "DataStream_Trailer"
});
var FilterParams = proto3.makeMessageType("livekit.FilterParams", () => [{
  no: 1,
  name: "include_events",
  kind: "scalar",
  T: 9,
  repeated: true
}, {
  no: 2,
  name: "exclude_events",
  kind: "scalar",
  T: 9,
  repeated: true
}]);
var WebhookConfig = proto3.makeMessageType("livekit.WebhookConfig", () => [{
  no: 1,
  name: "url",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 2,
  name: "signing_key",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 3,
  name: "filter_params",
  kind: "message",
  T: FilterParams
}]);
var SubscribedAudioCodec = proto3.makeMessageType("livekit.SubscribedAudioCodec", () => [{
  no: 1,
  name: "codec",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 2,
  name: "enabled",
  kind: "scalar",
  T: 8
  /* ScalarType.BOOL */
}]);
var JobRestartPolicy = proto3.makeEnum("livekit.JobRestartPolicy", [{
  no: 0,
  name: "JRP_ON_FAILURE"
}, {
  no: 1,
  name: "JRP_NEVER"
}]);
var RoomAgentDispatch = proto3.makeMessageType("livekit.RoomAgentDispatch", () => [{
  no: 1,
  name: "agent_name",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 2,
  name: "metadata",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 3,
  name: "restart_policy",
  kind: "enum",
  T: proto3.getEnumType(JobRestartPolicy)
}, {
  no: 4,
  name: "deployment",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 5,
  name: "attributes",
  kind: "map",
  K: 9,
  V: {
    kind: "scalar",
    T: 9
    /* ScalarType.STRING */
  }
}]);
var EncodingOptionsPreset = proto3.makeEnum("livekit.EncodingOptionsPreset", [{
  no: 0,
  name: "H264_720P_30"
}, {
  no: 1,
  name: "H264_720P_60"
}, {
  no: 2,
  name: "H264_1080P_30"
}, {
  no: 3,
  name: "H264_1080P_60"
}, {
  no: 4,
  name: "PORTRAIT_H264_720P_30"
}, {
  no: 5,
  name: "PORTRAIT_H264_720P_60"
}, {
  no: 6,
  name: "PORTRAIT_H264_1080P_30"
}, {
  no: 7,
  name: "PORTRAIT_H264_1080P_60"
}]);
var EncodedFileType = proto3.makeEnum("livekit.EncodedFileType", [{
  no: 0,
  name: "DEFAULT_FILETYPE"
}, {
  no: 1,
  name: "MP4"
}, {
  no: 2,
  name: "OGG"
}, {
  no: 3,
  name: "MP3"
}]);
var StreamProtocol = proto3.makeEnum("livekit.StreamProtocol", [{
  no: 0,
  name: "DEFAULT_PROTOCOL"
}, {
  no: 1,
  name: "RTMP"
}, {
  no: 2,
  name: "SRT"
}, {
  no: 3,
  name: "WEBSOCKET"
}]);
var SegmentedFileProtocol = proto3.makeEnum("livekit.SegmentedFileProtocol", [{
  no: 0,
  name: "DEFAULT_SEGMENTED_FILE_PROTOCOL"
}, {
  no: 1,
  name: "HLS_PROTOCOL"
}]);
var SegmentedFileSuffix = proto3.makeEnum("livekit.SegmentedFileSuffix", [{
  no: 0,
  name: "INDEX"
}, {
  no: 1,
  name: "TIMESTAMP"
}]);
var ImageFileSuffix = proto3.makeEnum("livekit.ImageFileSuffix", [{
  no: 0,
  name: "IMAGE_SUFFIX_INDEX"
}, {
  no: 1,
  name: "IMAGE_SUFFIX_TIMESTAMP"
}, {
  no: 2,
  name: "IMAGE_SUFFIX_NONE_OVERWRITE"
}]);
var AudioMixing = proto3.makeEnum("livekit.AudioMixing", [{
  no: 0,
  name: "DEFAULT_MIXING"
}, {
  no: 1,
  name: "DUAL_CHANNEL_AGENT"
}, {
  no: 2,
  name: "DUAL_CHANNEL_ALTERNATE"
}]);
var EncodingOptions = proto3.makeMessageType("livekit.EncodingOptions", () => [{
  no: 1,
  name: "width",
  kind: "scalar",
  T: 5
  /* ScalarType.INT32 */
}, {
  no: 2,
  name: "height",
  kind: "scalar",
  T: 5
  /* ScalarType.INT32 */
}, {
  no: 3,
  name: "depth",
  kind: "scalar",
  T: 5
  /* ScalarType.INT32 */
}, {
  no: 4,
  name: "framerate",
  kind: "scalar",
  T: 5
  /* ScalarType.INT32 */
}, {
  no: 5,
  name: "audio_codec",
  kind: "enum",
  T: proto3.getEnumType(AudioCodec)
}, {
  no: 6,
  name: "audio_bitrate",
  kind: "scalar",
  T: 5
  /* ScalarType.INT32 */
}, {
  no: 7,
  name: "audio_frequency",
  kind: "scalar",
  T: 5
  /* ScalarType.INT32 */
}, {
  no: 8,
  name: "video_codec",
  kind: "enum",
  T: proto3.getEnumType(VideoCodec)
}, {
  no: 9,
  name: "video_bitrate",
  kind: "scalar",
  T: 5
  /* ScalarType.INT32 */
}, {
  no: 10,
  name: "key_frame_interval",
  kind: "scalar",
  T: 1
  /* ScalarType.DOUBLE */
}, {
  no: 11,
  name: "audio_quality",
  kind: "scalar",
  T: 5
  /* ScalarType.INT32 */
}, {
  no: 12,
  name: "video_quality",
  kind: "scalar",
  T: 5
  /* ScalarType.INT32 */
}]);
var StreamOutput = proto3.makeMessageType("livekit.StreamOutput", () => [{
  no: 1,
  name: "protocol",
  kind: "enum",
  T: proto3.getEnumType(StreamProtocol)
}, {
  no: 2,
  name: "urls",
  kind: "scalar",
  T: 9,
  repeated: true
}]);
var SegmentedFileOutput = proto3.makeMessageType("livekit.SegmentedFileOutput", () => [{
  no: 1,
  name: "protocol",
  kind: "enum",
  T: proto3.getEnumType(SegmentedFileProtocol)
}, {
  no: 2,
  name: "filename_prefix",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 3,
  name: "playlist_name",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 11,
  name: "live_playlist_name",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 4,
  name: "segment_duration",
  kind: "scalar",
  T: 13
  /* ScalarType.UINT32 */
}, {
  no: 10,
  name: "filename_suffix",
  kind: "enum",
  T: proto3.getEnumType(SegmentedFileSuffix)
}, {
  no: 8,
  name: "disable_manifest",
  kind: "scalar",
  T: 8
  /* ScalarType.BOOL */
}, {
  no: 5,
  name: "s3",
  kind: "message",
  T: S3Upload,
  oneof: "output"
}, {
  no: 6,
  name: "gcp",
  kind: "message",
  T: GCPUpload,
  oneof: "output"
}, {
  no: 7,
  name: "azure",
  kind: "message",
  T: AzureBlobUpload,
  oneof: "output"
}, {
  no: 9,
  name: "aliOSS",
  kind: "message",
  T: AliOSSUpload,
  oneof: "output"
}]);
var ImageOutput = proto3.makeMessageType("livekit.ImageOutput", () => [{
  no: 1,
  name: "capture_interval",
  kind: "scalar",
  T: 13
  /* ScalarType.UINT32 */
}, {
  no: 2,
  name: "width",
  kind: "scalar",
  T: 5
  /* ScalarType.INT32 */
}, {
  no: 3,
  name: "height",
  kind: "scalar",
  T: 5
  /* ScalarType.INT32 */
}, {
  no: 4,
  name: "filename_prefix",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 5,
  name: "filename_suffix",
  kind: "enum",
  T: proto3.getEnumType(ImageFileSuffix)
}, {
  no: 6,
  name: "image_codec",
  kind: "enum",
  T: proto3.getEnumType(ImageCodec)
}, {
  no: 7,
  name: "disable_manifest",
  kind: "scalar",
  T: 8
  /* ScalarType.BOOL */
}, {
  no: 8,
  name: "s3",
  kind: "message",
  T: S3Upload,
  oneof: "output"
}, {
  no: 9,
  name: "gcp",
  kind: "message",
  T: GCPUpload,
  oneof: "output"
}, {
  no: 10,
  name: "azure",
  kind: "message",
  T: AzureBlobUpload,
  oneof: "output"
}, {
  no: 11,
  name: "aliOSS",
  kind: "message",
  T: AliOSSUpload,
  oneof: "output"
}]);
var S3Upload = proto3.makeMessageType("livekit.S3Upload", () => [{
  no: 1,
  name: "access_key",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 2,
  name: "secret",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 11,
  name: "session_token",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 12,
  name: "assume_role_arn",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 13,
  name: "assume_role_external_id",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 3,
  name: "region",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 4,
  name: "endpoint",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 5,
  name: "bucket",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 6,
  name: "force_path_style",
  kind: "scalar",
  T: 8
  /* ScalarType.BOOL */
}, {
  no: 7,
  name: "metadata",
  kind: "map",
  K: 9,
  V: {
    kind: "scalar",
    T: 9
    /* ScalarType.STRING */
  }
}, {
  no: 8,
  name: "tagging",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 9,
  name: "content_disposition",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 10,
  name: "proxy",
  kind: "message",
  T: ProxyConfig
}]);
var GCPUpload = proto3.makeMessageType("livekit.GCPUpload", () => [{
  no: 1,
  name: "credentials",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 2,
  name: "bucket",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 3,
  name: "proxy",
  kind: "message",
  T: ProxyConfig
}]);
var AzureBlobUpload = proto3.makeMessageType("livekit.AzureBlobUpload", () => [{
  no: 1,
  name: "account_name",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 2,
  name: "account_key",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 3,
  name: "container_name",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}]);
var AliOSSUpload = proto3.makeMessageType("livekit.AliOSSUpload", () => [{
  no: 1,
  name: "access_key",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 2,
  name: "secret",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 3,
  name: "region",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 4,
  name: "endpoint",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 5,
  name: "bucket",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}]);
var ProxyConfig = proto3.makeMessageType("livekit.ProxyConfig", () => [{
  no: 1,
  name: "url",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 2,
  name: "username",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 3,
  name: "password",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}]);
var AutoParticipantEgress = proto3.makeMessageType("livekit.AutoParticipantEgress", () => [{
  no: 1,
  name: "preset",
  kind: "enum",
  T: proto3.getEnumType(EncodingOptionsPreset),
  oneof: "options"
}, {
  no: 2,
  name: "advanced",
  kind: "message",
  T: EncodingOptions,
  oneof: "options"
}, {
  no: 3,
  name: "file_outputs",
  kind: "message",
  T: EncodedFileOutput,
  repeated: true
}, {
  no: 4,
  name: "segment_outputs",
  kind: "message",
  T: SegmentedFileOutput,
  repeated: true
}]);
var AutoTrackEgress = proto3.makeMessageType("livekit.AutoTrackEgress", () => [{
  no: 1,
  name: "filepath",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 5,
  name: "disable_manifest",
  kind: "scalar",
  T: 8
  /* ScalarType.BOOL */
}, {
  no: 2,
  name: "s3",
  kind: "message",
  T: S3Upload,
  oneof: "output"
}, {
  no: 3,
  name: "gcp",
  kind: "message",
  T: GCPUpload,
  oneof: "output"
}, {
  no: 4,
  name: "azure",
  kind: "message",
  T: AzureBlobUpload,
  oneof: "output"
}, {
  no: 6,
  name: "aliOSS",
  kind: "message",
  T: AliOSSUpload,
  oneof: "output"
}]);
var RoomCompositeEgressRequest = proto3.makeMessageType("livekit.RoomCompositeEgressRequest", () => [{
  no: 1,
  name: "room_name",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 2,
  name: "layout",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 3,
  name: "audio_only",
  kind: "scalar",
  T: 8
  /* ScalarType.BOOL */
}, {
  no: 15,
  name: "audio_mixing",
  kind: "enum",
  T: proto3.getEnumType(AudioMixing)
}, {
  no: 4,
  name: "video_only",
  kind: "scalar",
  T: 8
  /* ScalarType.BOOL */
}, {
  no: 5,
  name: "custom_base_url",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 6,
  name: "file",
  kind: "message",
  T: EncodedFileOutput,
  oneof: "output"
}, {
  no: 7,
  name: "stream",
  kind: "message",
  T: StreamOutput,
  oneof: "output"
}, {
  no: 10,
  name: "segments",
  kind: "message",
  T: SegmentedFileOutput,
  oneof: "output"
}, {
  no: 8,
  name: "preset",
  kind: "enum",
  T: proto3.getEnumType(EncodingOptionsPreset),
  oneof: "options"
}, {
  no: 9,
  name: "advanced",
  kind: "message",
  T: EncodingOptions,
  oneof: "options"
}, {
  no: 11,
  name: "file_outputs",
  kind: "message",
  T: EncodedFileOutput,
  repeated: true
}, {
  no: 12,
  name: "stream_outputs",
  kind: "message",
  T: StreamOutput,
  repeated: true
}, {
  no: 13,
  name: "segment_outputs",
  kind: "message",
  T: SegmentedFileOutput,
  repeated: true
}, {
  no: 14,
  name: "image_outputs",
  kind: "message",
  T: ImageOutput,
  repeated: true
}, {
  no: 16,
  name: "webhooks",
  kind: "message",
  T: WebhookConfig,
  repeated: true
}]);
var EncodedFileOutput = proto3.makeMessageType("livekit.EncodedFileOutput", () => [{
  no: 1,
  name: "file_type",
  kind: "enum",
  T: proto3.getEnumType(EncodedFileType)
}, {
  no: 2,
  name: "filepath",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 6,
  name: "disable_manifest",
  kind: "scalar",
  T: 8
  /* ScalarType.BOOL */
}, {
  no: 3,
  name: "s3",
  kind: "message",
  T: S3Upload,
  oneof: "output"
}, {
  no: 4,
  name: "gcp",
  kind: "message",
  T: GCPUpload,
  oneof: "output"
}, {
  no: 5,
  name: "azure",
  kind: "message",
  T: AzureBlobUpload,
  oneof: "output"
}, {
  no: 7,
  name: "aliOSS",
  kind: "message",
  T: AliOSSUpload,
  oneof: "output"
}]);
var RoomEgress = proto3.makeMessageType("livekit.RoomEgress", () => [{
  no: 1,
  name: "room",
  kind: "message",
  T: RoomCompositeEgressRequest
}, {
  no: 3,
  name: "participant",
  kind: "message",
  T: AutoParticipantEgress
}, {
  no: 2,
  name: "tracks",
  kind: "message",
  T: AutoTrackEgress
}]);
var RoomConfiguration = proto3.makeMessageType("livekit.RoomConfiguration", () => [{
  no: 1,
  name: "name",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 2,
  name: "empty_timeout",
  kind: "scalar",
  T: 13
  /* ScalarType.UINT32 */
}, {
  no: 3,
  name: "departure_timeout",
  kind: "scalar",
  T: 13
  /* ScalarType.UINT32 */
}, {
  no: 4,
  name: "max_participants",
  kind: "scalar",
  T: 13
  /* ScalarType.UINT32 */
}, {
  no: 11,
  name: "metadata",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 5,
  name: "egress",
  kind: "message",
  T: RoomEgress
}, {
  no: 7,
  name: "min_playout_delay",
  kind: "scalar",
  T: 13
  /* ScalarType.UINT32 */
}, {
  no: 8,
  name: "max_playout_delay",
  kind: "scalar",
  T: 13
  /* ScalarType.UINT32 */
}, {
  no: 9,
  name: "sync_streams",
  kind: "scalar",
  T: 8
  /* ScalarType.BOOL */
}, {
  no: 10,
  name: "agents",
  kind: "message",
  T: RoomAgentDispatch,
  repeated: true
}, {
  no: 12,
  name: "tags",
  kind: "map",
  K: 9,
  V: {
    kind: "scalar",
    T: 9
    /* ScalarType.STRING */
  }
}]);
var SignalTarget = proto3.makeEnum("livekit.SignalTarget", [{
  no: 0,
  name: "PUBLISHER"
}, {
  no: 1,
  name: "SUBSCRIBER"
}]);
var StreamState = proto3.makeEnum("livekit.StreamState", [{
  no: 0,
  name: "ACTIVE"
}, {
  no: 1,
  name: "PAUSED"
}]);
var CandidateProtocol = proto3.makeEnum("livekit.CandidateProtocol", [{
  no: 0,
  name: "UDP"
}, {
  no: 1,
  name: "TCP"
}, {
  no: 2,
  name: "TLS"
}]);
var SignalRequest = proto3.makeMessageType("livekit.SignalRequest", () => [{
  no: 1,
  name: "offer",
  kind: "message",
  T: SessionDescription,
  oneof: "message"
}, {
  no: 2,
  name: "answer",
  kind: "message",
  T: SessionDescription,
  oneof: "message"
}, {
  no: 3,
  name: "trickle",
  kind: "message",
  T: TrickleRequest,
  oneof: "message"
}, {
  no: 4,
  name: "add_track",
  kind: "message",
  T: AddTrackRequest,
  oneof: "message"
}, {
  no: 5,
  name: "mute",
  kind: "message",
  T: MuteTrackRequest,
  oneof: "message"
}, {
  no: 6,
  name: "subscription",
  kind: "message",
  T: UpdateSubscription,
  oneof: "message"
}, {
  no: 7,
  name: "track_setting",
  kind: "message",
  T: UpdateTrackSettings,
  oneof: "message"
}, {
  no: 8,
  name: "leave",
  kind: "message",
  T: LeaveRequest,
  oneof: "message"
}, {
  no: 10,
  name: "update_layers",
  kind: "message",
  T: UpdateVideoLayers,
  oneof: "message"
}, {
  no: 11,
  name: "subscription_permission",
  kind: "message",
  T: SubscriptionPermission,
  oneof: "message"
}, {
  no: 12,
  name: "sync_state",
  kind: "message",
  T: SyncState,
  oneof: "message"
}, {
  no: 13,
  name: "simulate",
  kind: "message",
  T: SimulateScenario,
  oneof: "message"
}, {
  no: 14,
  name: "ping",
  kind: "scalar",
  T: 3,
  oneof: "message"
}, {
  no: 15,
  name: "update_metadata",
  kind: "message",
  T: UpdateParticipantMetadata,
  oneof: "message"
}, {
  no: 16,
  name: "ping_req",
  kind: "message",
  T: Ping,
  oneof: "message"
}, {
  no: 17,
  name: "update_audio_track",
  kind: "message",
  T: UpdateLocalAudioTrack,
  oneof: "message"
}, {
  no: 18,
  name: "update_video_track",
  kind: "message",
  T: UpdateLocalVideoTrack,
  oneof: "message"
}, {
  no: 19,
  name: "publish_data_track_request",
  kind: "message",
  T: PublishDataTrackRequest,
  oneof: "message"
}, {
  no: 20,
  name: "unpublish_data_track_request",
  kind: "message",
  T: UnpublishDataTrackRequest,
  oneof: "message"
}, {
  no: 21,
  name: "update_data_subscription",
  kind: "message",
  T: UpdateDataSubscription,
  oneof: "message"
}, {
  no: 22,
  name: "store_data_blob_request",
  kind: "message",
  T: StoreDataBlobRequest,
  oneof: "message"
}, {
  no: 23,
  name: "get_data_blob_request",
  kind: "message",
  T: GetDataBlobRequest,
  oneof: "message"
}]);
var SignalResponse = proto3.makeMessageType("livekit.SignalResponse", () => [{
  no: 1,
  name: "join",
  kind: "message",
  T: JoinResponse,
  oneof: "message"
}, {
  no: 2,
  name: "answer",
  kind: "message",
  T: SessionDescription,
  oneof: "message"
}, {
  no: 3,
  name: "offer",
  kind: "message",
  T: SessionDescription,
  oneof: "message"
}, {
  no: 4,
  name: "trickle",
  kind: "message",
  T: TrickleRequest,
  oneof: "message"
}, {
  no: 5,
  name: "update",
  kind: "message",
  T: ParticipantUpdate,
  oneof: "message"
}, {
  no: 6,
  name: "track_published",
  kind: "message",
  T: TrackPublishedResponse,
  oneof: "message"
}, {
  no: 8,
  name: "leave",
  kind: "message",
  T: LeaveRequest,
  oneof: "message"
}, {
  no: 9,
  name: "mute",
  kind: "message",
  T: MuteTrackRequest,
  oneof: "message"
}, {
  no: 10,
  name: "speakers_changed",
  kind: "message",
  T: SpeakersChanged,
  oneof: "message"
}, {
  no: 11,
  name: "room_update",
  kind: "message",
  T: RoomUpdate,
  oneof: "message"
}, {
  no: 12,
  name: "connection_quality",
  kind: "message",
  T: ConnectionQualityUpdate,
  oneof: "message"
}, {
  no: 13,
  name: "stream_state_update",
  kind: "message",
  T: StreamStateUpdate,
  oneof: "message"
}, {
  no: 14,
  name: "subscribed_quality_update",
  kind: "message",
  T: SubscribedQualityUpdate,
  oneof: "message"
}, {
  no: 15,
  name: "subscription_permission_update",
  kind: "message",
  T: SubscriptionPermissionUpdate,
  oneof: "message"
}, {
  no: 16,
  name: "refresh_token",
  kind: "scalar",
  T: 9,
  oneof: "message"
}, {
  no: 17,
  name: "track_unpublished",
  kind: "message",
  T: TrackUnpublishedResponse,
  oneof: "message"
}, {
  no: 18,
  name: "pong",
  kind: "scalar",
  T: 3,
  oneof: "message"
}, {
  no: 19,
  name: "reconnect",
  kind: "message",
  T: ReconnectResponse,
  oneof: "message"
}, {
  no: 20,
  name: "pong_resp",
  kind: "message",
  T: Pong,
  oneof: "message"
}, {
  no: 21,
  name: "subscription_response",
  kind: "message",
  T: SubscriptionResponse,
  oneof: "message"
}, {
  no: 22,
  name: "request_response",
  kind: "message",
  T: RequestResponse,
  oneof: "message"
}, {
  no: 23,
  name: "track_subscribed",
  kind: "message",
  T: TrackSubscribed,
  oneof: "message"
}, {
  no: 24,
  name: "room_moved",
  kind: "message",
  T: RoomMovedResponse,
  oneof: "message"
}, {
  no: 25,
  name: "media_sections_requirement",
  kind: "message",
  T: MediaSectionsRequirement,
  oneof: "message"
}, {
  no: 26,
  name: "subscribed_audio_codec_update",
  kind: "message",
  T: SubscribedAudioCodecUpdate,
  oneof: "message"
}, {
  no: 27,
  name: "publish_data_track_response",
  kind: "message",
  T: PublishDataTrackResponse,
  oneof: "message"
}, {
  no: 28,
  name: "unpublish_data_track_response",
  kind: "message",
  T: UnpublishDataTrackResponse,
  oneof: "message"
}, {
  no: 29,
  name: "data_track_subscriber_handles",
  kind: "message",
  T: DataTrackSubscriberHandles,
  oneof: "message"
}, {
  no: 30,
  name: "store_data_blob_response",
  kind: "message",
  T: StoreDataBlobResponse,
  oneof: "message"
}, {
  no: 31,
  name: "get_data_blob_response",
  kind: "message",
  T: GetDataBlobResponse,
  oneof: "message"
}]);
var SimulcastCodec = proto3.makeMessageType("livekit.SimulcastCodec", () => [{
  no: 1,
  name: "codec",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 2,
  name: "cid",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 4,
  name: "layers",
  kind: "message",
  T: VideoLayer,
  repeated: true
}, {
  no: 5,
  name: "video_layer_mode",
  kind: "enum",
  T: proto3.getEnumType(VideoLayer_Mode)
}]);
var AddTrackRequest = proto3.makeMessageType("livekit.AddTrackRequest", () => [{
  no: 1,
  name: "cid",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 2,
  name: "name",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 3,
  name: "type",
  kind: "enum",
  T: proto3.getEnumType(TrackType)
}, {
  no: 4,
  name: "width",
  kind: "scalar",
  T: 13
  /* ScalarType.UINT32 */
}, {
  no: 5,
  name: "height",
  kind: "scalar",
  T: 13
  /* ScalarType.UINT32 */
}, {
  no: 6,
  name: "muted",
  kind: "scalar",
  T: 8
  /* ScalarType.BOOL */
}, {
  no: 7,
  name: "disable_dtx",
  kind: "scalar",
  T: 8
  /* ScalarType.BOOL */
}, {
  no: 8,
  name: "source",
  kind: "enum",
  T: proto3.getEnumType(TrackSource)
}, {
  no: 9,
  name: "layers",
  kind: "message",
  T: VideoLayer,
  repeated: true
}, {
  no: 10,
  name: "simulcast_codecs",
  kind: "message",
  T: SimulcastCodec,
  repeated: true
}, {
  no: 11,
  name: "sid",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 12,
  name: "stereo",
  kind: "scalar",
  T: 8
  /* ScalarType.BOOL */
}, {
  no: 13,
  name: "disable_red",
  kind: "scalar",
  T: 8
  /* ScalarType.BOOL */
}, {
  no: 14,
  name: "encryption",
  kind: "enum",
  T: proto3.getEnumType(Encryption_Type)
}, {
  no: 15,
  name: "stream",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 16,
  name: "backup_codec_policy",
  kind: "enum",
  T: proto3.getEnumType(BackupCodecPolicy$1)
}, {
  no: 17,
  name: "audio_features",
  kind: "enum",
  T: proto3.getEnumType(AudioTrackFeature),
  repeated: true
}, {
  no: 18,
  name: "packet_trailer_features",
  kind: "enum",
  T: proto3.getEnumType(PacketTrailerFeature),
  repeated: true
}]);
var PublishDataTrackRequest = proto3.makeMessageType("livekit.PublishDataTrackRequest", () => [{
  no: 1,
  name: "pub_handle",
  kind: "scalar",
  T: 13
  /* ScalarType.UINT32 */
}, {
  no: 2,
  name: "name",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 3,
  name: "encryption",
  kind: "enum",
  T: proto3.getEnumType(Encryption_Type)
}, {
  no: 4,
  name: "frame_encoding",
  kind: "message",
  T: DataTrackFrameEncoding,
  opt: true
}, {
  no: 5,
  name: "schema",
  kind: "message",
  T: DataTrackSchemaId,
  opt: true
}]);
var PublishDataTrackResponse = proto3.makeMessageType("livekit.PublishDataTrackResponse", () => [{
  no: 1,
  name: "info",
  kind: "message",
  T: DataTrackInfo$1
}]);
var UnpublishDataTrackRequest = proto3.makeMessageType("livekit.UnpublishDataTrackRequest", () => [{
  no: 1,
  name: "pub_handle",
  kind: "scalar",
  T: 13
  /* ScalarType.UINT32 */
}]);
var UnpublishDataTrackResponse = proto3.makeMessageType("livekit.UnpublishDataTrackResponse", () => [{
  no: 1,
  name: "info",
  kind: "message",
  T: DataTrackInfo$1
}]);
var DataTrackSubscriberHandles = proto3.makeMessageType("livekit.DataTrackSubscriberHandles", () => [{
  no: 1,
  name: "sub_handles",
  kind: "map",
  K: 13,
  V: {
    kind: "message",
    T: DataTrackSubscriberHandles_PublishedDataTrack
  }
}]);
var DataTrackSubscriberHandles_PublishedDataTrack = proto3.makeMessageType("livekit.DataTrackSubscriberHandles.PublishedDataTrack", () => [{
  no: 1,
  name: "publisher_identity",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 2,
  name: "publisher_sid",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 3,
  name: "track_sid",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}], {
  localName: "DataTrackSubscriberHandles_PublishedDataTrack"
});
var TrickleRequest = proto3.makeMessageType("livekit.TrickleRequest", () => [{
  no: 1,
  name: "candidateInit",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 2,
  name: "target",
  kind: "enum",
  T: proto3.getEnumType(SignalTarget)
}, {
  no: 3,
  name: "final",
  kind: "scalar",
  T: 8
  /* ScalarType.BOOL */
}]);
var MuteTrackRequest = proto3.makeMessageType("livekit.MuteTrackRequest", () => [{
  no: 1,
  name: "sid",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 2,
  name: "muted",
  kind: "scalar",
  T: 8
  /* ScalarType.BOOL */
}]);
var JoinResponse = proto3.makeMessageType("livekit.JoinResponse", () => [{
  no: 1,
  name: "room",
  kind: "message",
  T: Room$1
}, {
  no: 2,
  name: "participant",
  kind: "message",
  T: ParticipantInfo
}, {
  no: 3,
  name: "other_participants",
  kind: "message",
  T: ParticipantInfo,
  repeated: true
}, {
  no: 4,
  name: "server_version",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 5,
  name: "ice_servers",
  kind: "message",
  T: ICEServer,
  repeated: true
}, {
  no: 6,
  name: "subscriber_primary",
  kind: "scalar",
  T: 8
  /* ScalarType.BOOL */
}, {
  no: 7,
  name: "alternative_url",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 8,
  name: "client_configuration",
  kind: "message",
  T: ClientConfiguration
}, {
  no: 9,
  name: "server_region",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 10,
  name: "ping_timeout",
  kind: "scalar",
  T: 5
  /* ScalarType.INT32 */
}, {
  no: 11,
  name: "ping_interval",
  kind: "scalar",
  T: 5
  /* ScalarType.INT32 */
}, {
  no: 12,
  name: "server_info",
  kind: "message",
  T: ServerInfo
}, {
  no: 13,
  name: "sif_trailer",
  kind: "scalar",
  T: 12
  /* ScalarType.BYTES */
}, {
  no: 14,
  name: "enabled_publish_codecs",
  kind: "message",
  T: Codec,
  repeated: true
}, {
  no: 15,
  name: "fast_publish",
  kind: "scalar",
  T: 8
  /* ScalarType.BOOL */
}]);
var ReconnectResponse = proto3.makeMessageType("livekit.ReconnectResponse", () => [{
  no: 1,
  name: "ice_servers",
  kind: "message",
  T: ICEServer,
  repeated: true
}, {
  no: 2,
  name: "client_configuration",
  kind: "message",
  T: ClientConfiguration
}, {
  no: 3,
  name: "server_info",
  kind: "message",
  T: ServerInfo
}, {
  no: 4,
  name: "last_message_seq",
  kind: "scalar",
  T: 13
  /* ScalarType.UINT32 */
}]);
var TrackPublishedResponse = proto3.makeMessageType("livekit.TrackPublishedResponse", () => [{
  no: 1,
  name: "cid",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 2,
  name: "track",
  kind: "message",
  T: TrackInfo
}]);
var TrackUnpublishedResponse = proto3.makeMessageType("livekit.TrackUnpublishedResponse", () => [{
  no: 1,
  name: "track_sid",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}]);
var SessionDescription = proto3.makeMessageType("livekit.SessionDescription", () => [{
  no: 1,
  name: "type",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 2,
  name: "sdp",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 3,
  name: "id",
  kind: "scalar",
  T: 13
  /* ScalarType.UINT32 */
}, {
  no: 4,
  name: "mid_to_track_id",
  kind: "map",
  K: 9,
  V: {
    kind: "scalar",
    T: 9
    /* ScalarType.STRING */
  }
}]);
var ParticipantUpdate = proto3.makeMessageType("livekit.ParticipantUpdate", () => [{
  no: 1,
  name: "participants",
  kind: "message",
  T: ParticipantInfo,
  repeated: true
}]);
var UpdateSubscription = proto3.makeMessageType("livekit.UpdateSubscription", () => [{
  no: 1,
  name: "track_sids",
  kind: "scalar",
  T: 9,
  repeated: true
}, {
  no: 2,
  name: "subscribe",
  kind: "scalar",
  T: 8
  /* ScalarType.BOOL */
}, {
  no: 3,
  name: "participant_tracks",
  kind: "message",
  T: ParticipantTracks,
  repeated: true
}]);
var UpdateDataSubscription = proto3.makeMessageType("livekit.UpdateDataSubscription", () => [{
  no: 1,
  name: "updates",
  kind: "message",
  T: UpdateDataSubscription_Update,
  repeated: true
}]);
var UpdateDataSubscription_Update = proto3.makeMessageType("livekit.UpdateDataSubscription.Update", () => [{
  no: 1,
  name: "track_sid",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 2,
  name: "subscribe",
  kind: "scalar",
  T: 8
  /* ScalarType.BOOL */
}, {
  no: 3,
  name: "options",
  kind: "message",
  T: DataTrackSubscriptionOptions
}], {
  localName: "UpdateDataSubscription_Update"
});
var StoreDataBlobRequest = proto3.makeMessageType("livekit.StoreDataBlobRequest", () => [{
  no: 1,
  name: "request_id",
  kind: "scalar",
  T: 13
  /* ScalarType.UINT32 */
}, {
  no: 2,
  name: "blob",
  kind: "message",
  T: DataBlob
}]);
var StoreDataBlobResponse = proto3.makeMessageType("livekit.StoreDataBlobResponse", () => [{
  no: 1,
  name: "request_id",
  kind: "scalar",
  T: 13
  /* ScalarType.UINT32 */
}, {
  no: 2,
  name: "key",
  kind: "message",
  T: DataBlobKey
}]);
var GetDataBlobRequest = proto3.makeMessageType("livekit.GetDataBlobRequest", () => [{
  no: 1,
  name: "request_id",
  kind: "scalar",
  T: 13
  /* ScalarType.UINT32 */
}, {
  no: 2,
  name: "participant_identity",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 3,
  name: "key",
  kind: "message",
  T: DataBlobKey
}]);
var GetDataBlobResponse = proto3.makeMessageType("livekit.GetDataBlobResponse", () => [{
  no: 1,
  name: "request_id",
  kind: "scalar",
  T: 13
  /* ScalarType.UINT32 */
}, {
  no: 2,
  name: "blob",
  kind: "message",
  T: DataBlob
}]);
var UpdateTrackSettings = proto3.makeMessageType("livekit.UpdateTrackSettings", () => [{
  no: 1,
  name: "track_sids",
  kind: "scalar",
  T: 9,
  repeated: true
}, {
  no: 3,
  name: "disabled",
  kind: "scalar",
  T: 8
  /* ScalarType.BOOL */
}, {
  no: 4,
  name: "quality",
  kind: "enum",
  T: proto3.getEnumType(VideoQuality$1)
}, {
  no: 5,
  name: "width",
  kind: "scalar",
  T: 13
  /* ScalarType.UINT32 */
}, {
  no: 6,
  name: "height",
  kind: "scalar",
  T: 13
  /* ScalarType.UINT32 */
}, {
  no: 7,
  name: "fps",
  kind: "scalar",
  T: 13
  /* ScalarType.UINT32 */
}, {
  no: 8,
  name: "priority",
  kind: "scalar",
  T: 13
  /* ScalarType.UINT32 */
}]);
var UpdateLocalAudioTrack = proto3.makeMessageType("livekit.UpdateLocalAudioTrack", () => [{
  no: 1,
  name: "track_sid",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 2,
  name: "features",
  kind: "enum",
  T: proto3.getEnumType(AudioTrackFeature),
  repeated: true
}]);
var UpdateLocalVideoTrack = proto3.makeMessageType("livekit.UpdateLocalVideoTrack", () => [{
  no: 1,
  name: "track_sid",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 2,
  name: "width",
  kind: "scalar",
  T: 13
  /* ScalarType.UINT32 */
}, {
  no: 3,
  name: "height",
  kind: "scalar",
  T: 13
  /* ScalarType.UINT32 */
}]);
var LeaveRequest = proto3.makeMessageType("livekit.LeaveRequest", () => [{
  no: 1,
  name: "can_reconnect",
  kind: "scalar",
  T: 8
  /* ScalarType.BOOL */
}, {
  no: 2,
  name: "reason",
  kind: "enum",
  T: proto3.getEnumType(DisconnectReason)
}, {
  no: 3,
  name: "action",
  kind: "enum",
  T: proto3.getEnumType(LeaveRequest_Action)
}, {
  no: 4,
  name: "regions",
  kind: "message",
  T: RegionSettings
}]);
var LeaveRequest_Action = proto3.makeEnum("livekit.LeaveRequest.Action", [{
  no: 0,
  name: "DISCONNECT"
}, {
  no: 1,
  name: "RESUME"
}, {
  no: 2,
  name: "RECONNECT"
}]);
var UpdateVideoLayers = proto3.makeMessageType("livekit.UpdateVideoLayers", () => [{
  no: 1,
  name: "track_sid",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 2,
  name: "layers",
  kind: "message",
  T: VideoLayer,
  repeated: true
}]);
var UpdateParticipantMetadata = proto3.makeMessageType("livekit.UpdateParticipantMetadata", () => [{
  no: 1,
  name: "metadata",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 2,
  name: "name",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 3,
  name: "attributes",
  kind: "map",
  K: 9,
  V: {
    kind: "scalar",
    T: 9
    /* ScalarType.STRING */
  }
}, {
  no: 4,
  name: "request_id",
  kind: "scalar",
  T: 13
  /* ScalarType.UINT32 */
}]);
var ICEServer = proto3.makeMessageType("livekit.ICEServer", () => [{
  no: 1,
  name: "urls",
  kind: "scalar",
  T: 9,
  repeated: true
}, {
  no: 2,
  name: "username",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 3,
  name: "credential",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}]);
var SpeakersChanged = proto3.makeMessageType("livekit.SpeakersChanged", () => [{
  no: 1,
  name: "speakers",
  kind: "message",
  T: SpeakerInfo,
  repeated: true
}]);
var RoomUpdate = proto3.makeMessageType("livekit.RoomUpdate", () => [{
  no: 1,
  name: "room",
  kind: "message",
  T: Room$1
}]);
var ConnectionQualityInfo = proto3.makeMessageType("livekit.ConnectionQualityInfo", () => [{
  no: 1,
  name: "participant_sid",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 2,
  name: "quality",
  kind: "enum",
  T: proto3.getEnumType(ConnectionQuality$1)
}, {
  no: 3,
  name: "score",
  kind: "scalar",
  T: 2
  /* ScalarType.FLOAT */
}]);
var ConnectionQualityUpdate = proto3.makeMessageType("livekit.ConnectionQualityUpdate", () => [{
  no: 1,
  name: "updates",
  kind: "message",
  T: ConnectionQualityInfo,
  repeated: true
}]);
var StreamStateInfo = proto3.makeMessageType("livekit.StreamStateInfo", () => [{
  no: 1,
  name: "participant_sid",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 2,
  name: "track_sid",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 3,
  name: "state",
  kind: "enum",
  T: proto3.getEnumType(StreamState)
}]);
var StreamStateUpdate = proto3.makeMessageType("livekit.StreamStateUpdate", () => [{
  no: 1,
  name: "stream_states",
  kind: "message",
  T: StreamStateInfo,
  repeated: true
}]);
var SubscribedQuality = proto3.makeMessageType("livekit.SubscribedQuality", () => [{
  no: 1,
  name: "quality",
  kind: "enum",
  T: proto3.getEnumType(VideoQuality$1)
}, {
  no: 2,
  name: "enabled",
  kind: "scalar",
  T: 8
  /* ScalarType.BOOL */
}]);
var SubscribedCodec = proto3.makeMessageType("livekit.SubscribedCodec", () => [{
  no: 1,
  name: "codec",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 2,
  name: "qualities",
  kind: "message",
  T: SubscribedQuality,
  repeated: true
}]);
var SubscribedQualityUpdate = proto3.makeMessageType("livekit.SubscribedQualityUpdate", () => [{
  no: 1,
  name: "track_sid",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 2,
  name: "subscribed_qualities",
  kind: "message",
  T: SubscribedQuality,
  repeated: true
}, {
  no: 3,
  name: "subscribed_codecs",
  kind: "message",
  T: SubscribedCodec,
  repeated: true
}]);
var SubscribedAudioCodecUpdate = proto3.makeMessageType("livekit.SubscribedAudioCodecUpdate", () => [{
  no: 1,
  name: "track_sid",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 2,
  name: "subscribed_audio_codecs",
  kind: "message",
  T: SubscribedAudioCodec,
  repeated: true
}]);
var TrackPermission = proto3.makeMessageType("livekit.TrackPermission", () => [{
  no: 1,
  name: "participant_sid",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 2,
  name: "all_tracks",
  kind: "scalar",
  T: 8
  /* ScalarType.BOOL */
}, {
  no: 3,
  name: "track_sids",
  kind: "scalar",
  T: 9,
  repeated: true
}, {
  no: 4,
  name: "participant_identity",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}]);
var SubscriptionPermission = proto3.makeMessageType("livekit.SubscriptionPermission", () => [{
  no: 1,
  name: "all_participants",
  kind: "scalar",
  T: 8
  /* ScalarType.BOOL */
}, {
  no: 2,
  name: "track_permissions",
  kind: "message",
  T: TrackPermission,
  repeated: true
}]);
var SubscriptionPermissionUpdate = proto3.makeMessageType("livekit.SubscriptionPermissionUpdate", () => [{
  no: 1,
  name: "participant_sid",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 2,
  name: "track_sid",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 3,
  name: "allowed",
  kind: "scalar",
  T: 8
  /* ScalarType.BOOL */
}]);
var RoomMovedResponse = proto3.makeMessageType("livekit.RoomMovedResponse", () => [{
  no: 1,
  name: "room",
  kind: "message",
  T: Room$1
}, {
  no: 2,
  name: "token",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 3,
  name: "participant",
  kind: "message",
  T: ParticipantInfo
}, {
  no: 4,
  name: "other_participants",
  kind: "message",
  T: ParticipantInfo,
  repeated: true
}]);
var SyncState = proto3.makeMessageType("livekit.SyncState", () => [{
  no: 1,
  name: "answer",
  kind: "message",
  T: SessionDescription
}, {
  no: 2,
  name: "subscription",
  kind: "message",
  T: UpdateSubscription
}, {
  no: 3,
  name: "publish_tracks",
  kind: "message",
  T: TrackPublishedResponse,
  repeated: true
}, {
  no: 4,
  name: "data_channels",
  kind: "message",
  T: DataChannelInfo,
  repeated: true
}, {
  no: 5,
  name: "offer",
  kind: "message",
  T: SessionDescription
}, {
  no: 6,
  name: "track_sids_disabled",
  kind: "scalar",
  T: 9,
  repeated: true
}, {
  no: 7,
  name: "datachannel_receive_states",
  kind: "message",
  T: DataChannelReceiveState,
  repeated: true
}, {
  no: 8,
  name: "publish_data_tracks",
  kind: "message",
  T: PublishDataTrackResponse,
  repeated: true
}]);
var DataChannelReceiveState = proto3.makeMessageType("livekit.DataChannelReceiveState", () => [{
  no: 1,
  name: "publisher_sid",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 2,
  name: "last_seq",
  kind: "scalar",
  T: 13
  /* ScalarType.UINT32 */
}]);
var DataChannelInfo = proto3.makeMessageType("livekit.DataChannelInfo", () => [{
  no: 1,
  name: "label",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 2,
  name: "id",
  kind: "scalar",
  T: 13
  /* ScalarType.UINT32 */
}, {
  no: 3,
  name: "target",
  kind: "enum",
  T: proto3.getEnumType(SignalTarget)
}]);
var SimulateScenario = proto3.makeMessageType("livekit.SimulateScenario", () => [{
  no: 1,
  name: "speaker_update",
  kind: "scalar",
  T: 5,
  oneof: "scenario"
}, {
  no: 2,
  name: "node_failure",
  kind: "scalar",
  T: 8,
  oneof: "scenario"
}, {
  no: 3,
  name: "migration",
  kind: "scalar",
  T: 8,
  oneof: "scenario"
}, {
  no: 4,
  name: "server_leave",
  kind: "scalar",
  T: 8,
  oneof: "scenario"
}, {
  no: 5,
  name: "switch_candidate_protocol",
  kind: "enum",
  T: proto3.getEnumType(CandidateProtocol),
  oneof: "scenario"
}, {
  no: 6,
  name: "subscriber_bandwidth",
  kind: "scalar",
  T: 3,
  oneof: "scenario"
}, {
  no: 7,
  name: "disconnect_signal_on_resume",
  kind: "scalar",
  T: 8,
  oneof: "scenario"
}, {
  no: 8,
  name: "disconnect_signal_on_resume_no_messages",
  kind: "scalar",
  T: 8,
  oneof: "scenario"
}, {
  no: 9,
  name: "leave_request_full_reconnect",
  kind: "scalar",
  T: 8,
  oneof: "scenario"
}]);
var Ping = proto3.makeMessageType("livekit.Ping", () => [{
  no: 1,
  name: "timestamp",
  kind: "scalar",
  T: 3
  /* ScalarType.INT64 */
}, {
  no: 2,
  name: "rtt",
  kind: "scalar",
  T: 3
  /* ScalarType.INT64 */
}]);
var Pong = proto3.makeMessageType("livekit.Pong", () => [{
  no: 1,
  name: "last_ping_timestamp",
  kind: "scalar",
  T: 3
  /* ScalarType.INT64 */
}, {
  no: 2,
  name: "timestamp",
  kind: "scalar",
  T: 3
  /* ScalarType.INT64 */
}]);
var RegionSettings = proto3.makeMessageType("livekit.RegionSettings", () => [{
  no: 1,
  name: "regions",
  kind: "message",
  T: RegionInfo,
  repeated: true
}]);
var RegionInfo = proto3.makeMessageType("livekit.RegionInfo", () => [{
  no: 1,
  name: "region",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 2,
  name: "url",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 3,
  name: "distance",
  kind: "scalar",
  T: 3
  /* ScalarType.INT64 */
}]);
var SubscriptionResponse = proto3.makeMessageType("livekit.SubscriptionResponse", () => [{
  no: 1,
  name: "track_sid",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 2,
  name: "err",
  kind: "enum",
  T: proto3.getEnumType(SubscriptionError)
}]);
var RequestResponse = proto3.makeMessageType("livekit.RequestResponse", () => [{
  no: 1,
  name: "request_id",
  kind: "scalar",
  T: 13
  /* ScalarType.UINT32 */
}, {
  no: 2,
  name: "reason",
  kind: "enum",
  T: proto3.getEnumType(RequestResponse_Reason)
}, {
  no: 3,
  name: "message",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 4,
  name: "trickle",
  kind: "message",
  T: TrickleRequest,
  oneof: "request"
}, {
  no: 5,
  name: "add_track",
  kind: "message",
  T: AddTrackRequest,
  oneof: "request"
}, {
  no: 6,
  name: "mute",
  kind: "message",
  T: MuteTrackRequest,
  oneof: "request"
}, {
  no: 7,
  name: "update_metadata",
  kind: "message",
  T: UpdateParticipantMetadata,
  oneof: "request"
}, {
  no: 8,
  name: "update_audio_track",
  kind: "message",
  T: UpdateLocalAudioTrack,
  oneof: "request"
}, {
  no: 9,
  name: "update_video_track",
  kind: "message",
  T: UpdateLocalVideoTrack,
  oneof: "request"
}, {
  no: 10,
  name: "publish_data_track",
  kind: "message",
  T: PublishDataTrackRequest,
  oneof: "request"
}, {
  no: 11,
  name: "unpublish_data_track",
  kind: "message",
  T: UnpublishDataTrackRequest,
  oneof: "request"
}]);
var RequestResponse_Reason = proto3.makeEnum("livekit.RequestResponse.Reason", [{
  no: 0,
  name: "OK"
}, {
  no: 1,
  name: "NOT_FOUND"
}, {
  no: 2,
  name: "NOT_ALLOWED"
}, {
  no: 3,
  name: "LIMIT_EXCEEDED"
}, {
  no: 4,
  name: "QUEUED"
}, {
  no: 5,
  name: "UNSUPPORTED_TYPE"
}, {
  no: 6,
  name: "UNCLASSIFIED_ERROR"
}, {
  no: 7,
  name: "INVALID_HANDLE"
}, {
  no: 8,
  name: "INVALID_NAME"
}, {
  no: 9,
  name: "DUPLICATE_HANDLE"
}, {
  no: 10,
  name: "DUPLICATE_NAME"
}, {
  no: 11,
  name: "INVALID_REQUEST"
}]);
var TrackSubscribed = proto3.makeMessageType("livekit.TrackSubscribed", () => [{
  no: 1,
  name: "track_sid",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}]);
var ConnectionSettings = proto3.makeMessageType("livekit.ConnectionSettings", () => [{
  no: 1,
  name: "auto_subscribe",
  kind: "scalar",
  T: 8
  /* ScalarType.BOOL */
}, {
  no: 2,
  name: "adaptive_stream",
  kind: "scalar",
  T: 8
  /* ScalarType.BOOL */
}, {
  no: 3,
  name: "subscriber_allow_pause",
  kind: "scalar",
  T: 8,
  opt: true
}, {
  no: 4,
  name: "disable_ice_lite",
  kind: "scalar",
  T: 8
  /* ScalarType.BOOL */
}, {
  no: 5,
  name: "auto_subscribe_data_track",
  kind: "scalar",
  T: 8,
  opt: true
}]);
var JoinRequest = proto3.makeMessageType("livekit.JoinRequest", () => [{
  no: 1,
  name: "client_info",
  kind: "message",
  T: ClientInfo
}, {
  no: 2,
  name: "connection_settings",
  kind: "message",
  T: ConnectionSettings
}, {
  no: 3,
  name: "metadata",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 4,
  name: "participant_attributes",
  kind: "map",
  K: 9,
  V: {
    kind: "scalar",
    T: 9
    /* ScalarType.STRING */
  }
}, {
  no: 5,
  name: "add_track_requests",
  kind: "message",
  T: AddTrackRequest,
  repeated: true
}, {
  no: 6,
  name: "publisher_offer",
  kind: "message",
  T: SessionDescription
}, {
  no: 7,
  name: "reconnect",
  kind: "scalar",
  T: 8
  /* ScalarType.BOOL */
}, {
  no: 8,
  name: "reconnect_reason",
  kind: "enum",
  T: proto3.getEnumType(ReconnectReason)
}, {
  no: 9,
  name: "participant_sid",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 10,
  name: "sync_state",
  kind: "message",
  T: SyncState
}]);
var WrappedJoinRequest = proto3.makeMessageType("livekit.WrappedJoinRequest", () => [{
  no: 1,
  name: "compression",
  kind: "enum",
  T: proto3.getEnumType(WrappedJoinRequest_Compression)
}, {
  no: 2,
  name: "join_request",
  kind: "scalar",
  T: 12
  /* ScalarType.BYTES */
}]);
var WrappedJoinRequest_Compression = proto3.makeEnum("livekit.WrappedJoinRequest.Compression", [{
  no: 0,
  name: "NONE"
}, {
  no: 1,
  name: "GZIP"
}]);
var MediaSectionsRequirement = proto3.makeMessageType("livekit.MediaSectionsRequirement", () => [{
  no: 1,
  name: "num_audios",
  kind: "scalar",
  T: 13
  /* ScalarType.UINT32 */
}, {
  no: 2,
  name: "num_videos",
  kind: "scalar",
  T: 13
  /* ScalarType.UINT32 */
}]);
var TokenSourceRequest = proto3.makeMessageType("livekit.TokenSourceRequest", () => [{
  no: 1,
  name: "room_name",
  kind: "scalar",
  T: 9,
  opt: true
}, {
  no: 2,
  name: "participant_name",
  kind: "scalar",
  T: 9,
  opt: true
}, {
  no: 3,
  name: "participant_identity",
  kind: "scalar",
  T: 9,
  opt: true
}, {
  no: 4,
  name: "participant_metadata",
  kind: "scalar",
  T: 9,
  opt: true
}, {
  no: 5,
  name: "participant_attributes",
  kind: "map",
  K: 9,
  V: {
    kind: "scalar",
    T: 9
    /* ScalarType.STRING */
  }
}, {
  no: 6,
  name: "room_config",
  kind: "message",
  T: RoomConfiguration,
  opt: true
}]);
var TokenSourceResponse = proto3.makeMessageType("livekit.TokenSourceResponse", () => [{
  no: 1,
  name: "server_url",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 2,
  name: "participant_token",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}]);
function getDefaultExportFromCjs(x) {
  return x && x.__esModule && Object.prototype.hasOwnProperty.call(x, "default") ? x["default"] : x;
}
var loglevel$1 = { exports: {} };
var loglevel = loglevel$1.exports;
var hasRequiredLoglevel;
function requireLoglevel() {
  if (hasRequiredLoglevel) return loglevel$1.exports;
  hasRequiredLoglevel = 1;
  (function(module) {
    (function(root, definition) {
      if (module.exports) {
        module.exports = definition();
      } else {
        root.log = definition();
      }
    })(loglevel, function() {
      var noop = function() {
      };
      var undefinedType = "undefined";
      var isIE = typeof window !== undefinedType && typeof window.navigator !== undefinedType && /Trident\/|MSIE /.test(window.navigator.userAgent);
      var logMethods = ["trace", "debug", "info", "warn", "error"];
      var _loggersByName = {};
      var defaultLogger = null;
      function bindMethod(obj, methodName) {
        var method = obj[methodName];
        if (typeof method.bind === "function") {
          return method.bind(obj);
        } else {
          try {
            return Function.prototype.bind.call(method, obj);
          } catch (e4) {
            return function() {
              return Function.prototype.apply.apply(method, [obj, arguments]);
            };
          }
        }
      }
      function traceForIE() {
        if (console.log) {
          if (console.log.apply) {
            console.log.apply(console, arguments);
          } else {
            Function.prototype.apply.apply(console.log, [console, arguments]);
          }
        }
        if (console.trace) console.trace();
      }
      function realMethod(methodName) {
        if (methodName === "debug") {
          methodName = "log";
        }
        if (typeof console === undefinedType) {
          return false;
        } else if (methodName === "trace" && isIE) {
          return traceForIE;
        } else if (console[methodName] !== void 0) {
          return bindMethod(console, methodName);
        } else if (console.log !== void 0) {
          return bindMethod(console, "log");
        } else {
          return noop;
        }
      }
      function replaceLoggingMethods() {
        var level = this.getLevel();
        for (var i2 = 0; i2 < logMethods.length; i2++) {
          var methodName = logMethods[i2];
          this[methodName] = i2 < level ? noop : this.methodFactory(methodName, level, this.name);
        }
        this.log = this.debug;
        if (typeof console === undefinedType && level < this.levels.SILENT) {
          return "No console available for logging";
        }
      }
      function enableLoggingWhenConsoleArrives(methodName) {
        return function() {
          if (typeof console !== undefinedType) {
            replaceLoggingMethods.call(this);
            this[methodName].apply(this, arguments);
          }
        };
      }
      function defaultMethodFactory(methodName, _level, _loggerName) {
        return realMethod(methodName) || enableLoggingWhenConsoleArrives.apply(this, arguments);
      }
      function Logger(name, factory) {
        var self2 = this;
        var inheritedLevel;
        var defaultLevel;
        var userLevel;
        var storageKey = "loglevel";
        if (typeof name === "string") {
          storageKey += ":" + name;
        } else if (typeof name === "symbol") {
          storageKey = void 0;
        }
        function persistLevelIfPossible(levelNum) {
          var levelName = (logMethods[levelNum] || "silent").toUpperCase();
          if (typeof window === undefinedType || !storageKey) return;
          try {
            window.localStorage[storageKey] = levelName;
            return;
          } catch (ignore) {
          }
          try {
            window.document.cookie = encodeURIComponent(storageKey) + "=" + levelName + ";";
          } catch (ignore) {
          }
        }
        function getPersistedLevel() {
          var storedLevel;
          if (typeof window === undefinedType || !storageKey) return;
          try {
            storedLevel = window.localStorage[storageKey];
          } catch (ignore) {
          }
          if (typeof storedLevel === undefinedType) {
            try {
              var cookie = window.document.cookie;
              var cookieName = encodeURIComponent(storageKey);
              var location = cookie.indexOf(cookieName + "=");
              if (location !== -1) {
                storedLevel = /^([^;]+)/.exec(cookie.slice(location + cookieName.length + 1))[1];
              }
            } catch (ignore) {
            }
          }
          if (self2.levels[storedLevel] === void 0) {
            storedLevel = void 0;
          }
          return storedLevel;
        }
        function clearPersistedLevel() {
          if (typeof window === undefinedType || !storageKey) return;
          try {
            window.localStorage.removeItem(storageKey);
          } catch (ignore) {
          }
          try {
            window.document.cookie = encodeURIComponent(storageKey) + "=; expires=Thu, 01 Jan 1970 00:00:00 UTC";
          } catch (ignore) {
          }
        }
        function normalizeLevel(input) {
          var level = input;
          if (typeof level === "string" && self2.levels[level.toUpperCase()] !== void 0) {
            level = self2.levels[level.toUpperCase()];
          }
          if (typeof level === "number" && level >= 0 && level <= self2.levels.SILENT) {
            return level;
          } else {
            throw new TypeError("log.setLevel() called with invalid level: " + input);
          }
        }
        self2.name = name;
        self2.levels = {
          "TRACE": 0,
          "DEBUG": 1,
          "INFO": 2,
          "WARN": 3,
          "ERROR": 4,
          "SILENT": 5
        };
        self2.methodFactory = factory || defaultMethodFactory;
        self2.getLevel = function() {
          if (userLevel != null) {
            return userLevel;
          } else if (defaultLevel != null) {
            return defaultLevel;
          } else {
            return inheritedLevel;
          }
        };
        self2.setLevel = function(level, persist) {
          userLevel = normalizeLevel(level);
          if (persist !== false) {
            persistLevelIfPossible(userLevel);
          }
          return replaceLoggingMethods.call(self2);
        };
        self2.setDefaultLevel = function(level) {
          defaultLevel = normalizeLevel(level);
          if (!getPersistedLevel()) {
            self2.setLevel(level, false);
          }
        };
        self2.resetLevel = function() {
          userLevel = null;
          clearPersistedLevel();
          replaceLoggingMethods.call(self2);
        };
        self2.enableAll = function(persist) {
          self2.setLevel(self2.levels.TRACE, persist);
        };
        self2.disableAll = function(persist) {
          self2.setLevel(self2.levels.SILENT, persist);
        };
        self2.rebuild = function() {
          if (defaultLogger !== self2) {
            inheritedLevel = normalizeLevel(defaultLogger.getLevel());
          }
          replaceLoggingMethods.call(self2);
          if (defaultLogger === self2) {
            for (var childName in _loggersByName) {
              _loggersByName[childName].rebuild();
            }
          }
        };
        inheritedLevel = normalizeLevel(defaultLogger ? defaultLogger.getLevel() : "WARN");
        var initialLevel = getPersistedLevel();
        if (initialLevel != null) {
          userLevel = normalizeLevel(initialLevel);
        }
        replaceLoggingMethods.call(self2);
      }
      defaultLogger = new Logger();
      defaultLogger.getLogger = function getLogger2(name) {
        if (typeof name !== "symbol" && typeof name !== "string" || name === "") {
          throw new TypeError("You must supply a name when creating a logger.");
        }
        var logger = _loggersByName[name];
        if (!logger) {
          logger = _loggersByName[name] = new Logger(name, defaultLogger.methodFactory);
        }
        return logger;
      };
      var _log = typeof window !== undefinedType ? window.log : void 0;
      defaultLogger.noConflict = function() {
        if (typeof window !== undefinedType && window.log === defaultLogger) {
          window.log = _log;
        }
        return defaultLogger;
      };
      defaultLogger.getLoggers = function getLoggers() {
        return _loggersByName;
      };
      defaultLogger["default"] = defaultLogger;
      return defaultLogger;
    });
  })(loglevel$1);
  return loglevel$1.exports;
}
var loglevelExports = requireLoglevel();
var LogLevel;
(function(LogLevel2) {
  LogLevel2[LogLevel2["trace"] = 0] = "trace";
  LogLevel2[LogLevel2["debug"] = 1] = "debug";
  LogLevel2[LogLevel2["info"] = 2] = "info";
  LogLevel2[LogLevel2["warn"] = 3] = "warn";
  LogLevel2[LogLevel2["error"] = 4] = "error";
  LogLevel2[LogLevel2["silent"] = 5] = "silent";
})(LogLevel || (LogLevel = {}));
var LoggerNames;
(function(LoggerNames2) {
  LoggerNames2["Default"] = "livekit";
  LoggerNames2["Room"] = "livekit-room";
  LoggerNames2["TokenSource"] = "livekit-token-source";
  LoggerNames2["Participant"] = "livekit-participant";
  LoggerNames2["Track"] = "livekit-track";
  LoggerNames2["Publication"] = "livekit-track-publication";
  LoggerNames2["Engine"] = "livekit-engine";
  LoggerNames2["Signal"] = "livekit-signal";
  LoggerNames2["PCManager"] = "livekit-pc-manager";
  LoggerNames2["PCTransport"] = "livekit-pc-transport";
  LoggerNames2["E2EE"] = "lk-e2ee";
  LoggerNames2["DataTracks"] = "livekit-data-tracks";
  LoggerNames2["Region"] = "livekit-region";
  LoggerNames2["ICE"] = "livekit-ice";
})(LoggerNames || (LoggerNames = {}));
var livekitLogger = loglevelExports.getLogger(LoggerNames.Default);
var livekitLoggers = Object.values(LoggerNames).map((name) => loglevelExports.getLogger(name));
livekitLogger.setDefaultLevel(LogLevel.info);
function getLogger(name, ctxFn) {
  const logger = loglevelExports.getLogger(name);
  logger.setDefaultLevel(livekitLogger.getLevel());
  if (!ctxFn) {
    return logger;
  }
  return wrapWithContext(logger, ctxFn);
}
function wrapWithContext(base2, ctxFn) {
  const wrap = (method) => (msg, extra) => {
    const ctx = ctxFn();
    const merged = ctx || extra ? Object.assign(Object.assign({}, ctx), extra) : void 0;
    base2[method](msg, merged);
  };
  const proxy = Object.create(base2);
  proxy.trace = wrap("trace");
  proxy.debug = wrap("debug");
  proxy.info = wrap("info");
  proxy.warn = wrap("warn");
  proxy.error = wrap("error");
  return proxy;
}
function setLogLevel(level, loggerName) {
  if (loggerName) {
    loglevelExports.getLogger(loggerName).setLevel(level);
  } else {
    for (const logger of livekitLoggers) {
      logger.setLevel(level);
    }
  }
}
function setLogExtension(extension, logger) {
  const loggers = logger ? [logger] : livekitLoggers;
  loggers.forEach((logR) => {
    const originalFactory = logR.methodFactory;
    logR.methodFactory = (methodName, configLevel, loggerName) => {
      const rawMethod = originalFactory(methodName, configLevel, loggerName);
      const logLevel = LogLevel[methodName];
      const needLog = logLevel >= configLevel && logLevel < LogLevel.silent;
      return (msg, context) => {
        if (context) rawMethod(msg, context);
        else rawMethod(msg);
        if (needLog) {
          extension(logLevel, msg, context);
        }
      };
    };
    logR.setLevel(logR.getLevel());
  });
}
var workerLogger = loglevelExports.getLogger(LoggerNames.E2EE);
var maxRetryDelay = 7e3;
var DEFAULT_RETRY_DELAYS_IN_MS = [0, 300, 2 * 2 * 300, 3 * 3 * 300, 4 * 4 * 300, maxRetryDelay, maxRetryDelay, maxRetryDelay, maxRetryDelay, maxRetryDelay];
var DefaultReconnectPolicy = class {
  constructor(retryDelays) {
    this._retryDelays = retryDelays !== void 0 ? [...retryDelays] : DEFAULT_RETRY_DELAYS_IN_MS;
  }
  nextRetryDelayInMs(context) {
    if (context.retryCount >= this._retryDelays.length) return null;
    const retryDelay = this._retryDelays[context.retryCount];
    if (context.retryCount <= 1) return retryDelay;
    return retryDelay + Math.random() * 1e3;
  }
};
function __rest(s, e4) {
  var t = {};
  for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e4.indexOf(p) < 0)
    t[p] = s[p];
  if (s != null && typeof Object.getOwnPropertySymbols === "function")
    for (var i2 = 0, p = Object.getOwnPropertySymbols(s); i2 < p.length; i2++) {
      if (e4.indexOf(p[i2]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i2]))
        t[p[i2]] = s[p[i2]];
    }
  return t;
}
function __awaiter(thisArg, _arguments, P, generator) {
  function adopt(value) {
    return value instanceof P ? value : new P(function(resolve) {
      resolve(value);
    });
  }
  return new (P || (P = Promise))(function(resolve, reject) {
    function fulfilled(value) {
      try {
        step(generator.next(value));
      } catch (e4) {
        reject(e4);
      }
    }
    function rejected(value) {
      try {
        step(generator["throw"](value));
      } catch (e4) {
        reject(e4);
      }
    }
    function step(result) {
      result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected);
    }
    step((generator = generator.apply(thisArg, _arguments || [])).next());
  });
}
function __values(o2) {
  var s = typeof Symbol === "function" && Symbol.iterator, m = s && o2[s], i2 = 0;
  if (m) return m.call(o2);
  if (o2 && typeof o2.length === "number") return {
    next: function() {
      if (o2 && i2 >= o2.length) o2 = void 0;
      return { value: o2 && o2[i2++], done: !o2 };
    }
  };
  throw new TypeError(s ? "Object is not iterable." : "Symbol.iterator is not defined.");
}
function __await(v) {
  return this instanceof __await ? (this.v = v, this) : new __await(v);
}
function __asyncGenerator(thisArg, _arguments, generator) {
  if (!Symbol.asyncIterator) throw new TypeError("Symbol.asyncIterator is not defined.");
  var g = generator.apply(thisArg, _arguments || []), i2, q3 = [];
  return i2 = Object.create((typeof AsyncIterator === "function" ? AsyncIterator : Object).prototype), verb("next"), verb("throw"), verb("return", awaitReturn), i2[Symbol.asyncIterator] = function() {
    return this;
  }, i2;
  function awaitReturn(f) {
    return function(v) {
      return Promise.resolve(v).then(f, reject);
    };
  }
  function verb(n, f) {
    if (g[n]) {
      i2[n] = function(v) {
        return new Promise(function(a, b) {
          q3.push([n, v, a, b]) > 1 || resume(n, v);
        });
      };
      if (f) i2[n] = f(i2[n]);
    }
  }
  function resume(n, v) {
    try {
      step(g[n](v));
    } catch (e4) {
      settle(q3[0][3], e4);
    }
  }
  function step(r2) {
    r2.value instanceof __await ? Promise.resolve(r2.value.v).then(fulfill, reject) : settle(q3[0][2], r2);
  }
  function fulfill(value) {
    resume("next", value);
  }
  function reject(value) {
    resume("throw", value);
  }
  function settle(f, v) {
    if (f(v), q3.shift(), q3.length) resume(q3[0][0], q3[0][1]);
  }
}
function __asyncDelegator(o2) {
  var i2, p;
  return i2 = {}, verb("next"), verb("throw", function(e4) {
    throw e4;
  }), verb("return"), i2[Symbol.iterator] = function() {
    return this;
  }, i2;
  function verb(n, f) {
    i2[n] = o2[n] ? function(v) {
      return (p = !p) ? { value: __await(o2[n](v)), done: false } : f ? f(v) : v;
    } : f;
  }
}
function __asyncValues(o2) {
  if (!Symbol.asyncIterator) throw new TypeError("Symbol.asyncIterator is not defined.");
  var m = o2[Symbol.asyncIterator], i2;
  return m ? m.call(o2) : (o2 = typeof __values === "function" ? __values(o2) : o2[Symbol.iterator](), i2 = {}, verb("next"), verb("throw"), verb("return"), i2[Symbol.asyncIterator] = function() {
    return this;
  }, i2);
  function verb(n) {
    i2[n] = o2[n] && function(v) {
      return new Promise(function(resolve, reject) {
        v = o2[n](v), settle(resolve, reject, v.done, v.value);
      });
    };
  }
  function settle(resolve, reject, d, v) {
    Promise.resolve(v).then(function(v2) {
      resolve({ value: v2, done: d });
    }, reject);
  }
}
var events = { exports: {} };
var hasRequiredEvents;
function requireEvents() {
  if (hasRequiredEvents) return events.exports;
  hasRequiredEvents = 1;
  var R = typeof Reflect === "object" ? Reflect : null;
  var ReflectApply = R && typeof R.apply === "function" ? R.apply : function ReflectApply2(target, receiver, args) {
    return Function.prototype.apply.call(target, receiver, args);
  };
  var ReflectOwnKeys;
  if (R && typeof R.ownKeys === "function") {
    ReflectOwnKeys = R.ownKeys;
  } else if (Object.getOwnPropertySymbols) {
    ReflectOwnKeys = function ReflectOwnKeys2(target) {
      return Object.getOwnPropertyNames(target).concat(Object.getOwnPropertySymbols(target));
    };
  } else {
    ReflectOwnKeys = function ReflectOwnKeys2(target) {
      return Object.getOwnPropertyNames(target);
    };
  }
  function ProcessEmitWarning(warning) {
    if (console && console.warn) console.warn(warning);
  }
  var NumberIsNaN = Number.isNaN || function NumberIsNaN2(value) {
    return value !== value;
  };
  function EventEmitter() {
    EventEmitter.init.call(this);
  }
  events.exports = EventEmitter;
  events.exports.once = once;
  EventEmitter.EventEmitter = EventEmitter;
  EventEmitter.prototype._events = void 0;
  EventEmitter.prototype._eventsCount = 0;
  EventEmitter.prototype._maxListeners = void 0;
  var defaultMaxListeners = 10;
  function checkListener(listener) {
    if (typeof listener !== "function") {
      throw new TypeError('The "listener" argument must be of type Function. Received type ' + typeof listener);
    }
  }
  Object.defineProperty(EventEmitter, "defaultMaxListeners", {
    enumerable: true,
    get: function() {
      return defaultMaxListeners;
    },
    set: function(arg) {
      if (typeof arg !== "number" || arg < 0 || NumberIsNaN(arg)) {
        throw new RangeError('The value of "defaultMaxListeners" is out of range. It must be a non-negative number. Received ' + arg + ".");
      }
      defaultMaxListeners = arg;
    }
  });
  EventEmitter.init = function() {
    if (this._events === void 0 || this._events === Object.getPrototypeOf(this)._events) {
      this._events = /* @__PURE__ */ Object.create(null);
      this._eventsCount = 0;
    }
    this._maxListeners = this._maxListeners || void 0;
  };
  EventEmitter.prototype.setMaxListeners = function setMaxListeners(n) {
    if (typeof n !== "number" || n < 0 || NumberIsNaN(n)) {
      throw new RangeError('The value of "n" is out of range. It must be a non-negative number. Received ' + n + ".");
    }
    this._maxListeners = n;
    return this;
  };
  function _getMaxListeners(that) {
    if (that._maxListeners === void 0) return EventEmitter.defaultMaxListeners;
    return that._maxListeners;
  }
  EventEmitter.prototype.getMaxListeners = function getMaxListeners() {
    return _getMaxListeners(this);
  };
  EventEmitter.prototype.emit = function emit(type) {
    var args = [];
    for (var i2 = 1; i2 < arguments.length; i2++) args.push(arguments[i2]);
    var doError = type === "error";
    var events2 = this._events;
    if (events2 !== void 0) doError = doError && events2.error === void 0;
    else if (!doError) return false;
    if (doError) {
      var er3;
      if (args.length > 0) er3 = args[0];
      if (er3 instanceof Error) {
        throw er3;
      }
      var err = new Error("Unhandled error." + (er3 ? " (" + er3.message + ")" : ""));
      err.context = er3;
      throw err;
    }
    var handler = events2[type];
    if (handler === void 0) return false;
    if (typeof handler === "function") {
      ReflectApply(handler, this, args);
    } else {
      var len = handler.length;
      var listeners = arrayClone(handler, len);
      for (var i2 = 0; i2 < len; ++i2) ReflectApply(listeners[i2], this, args);
    }
    return true;
  };
  function _addListener(target, type, listener, prepend) {
    var m;
    var events2;
    var existing;
    checkListener(listener);
    events2 = target._events;
    if (events2 === void 0) {
      events2 = target._events = /* @__PURE__ */ Object.create(null);
      target._eventsCount = 0;
    } else {
      if (events2.newListener !== void 0) {
        target.emit("newListener", type, listener.listener ? listener.listener : listener);
        events2 = target._events;
      }
      existing = events2[type];
    }
    if (existing === void 0) {
      existing = events2[type] = listener;
      ++target._eventsCount;
    } else {
      if (typeof existing === "function") {
        existing = events2[type] = prepend ? [listener, existing] : [existing, listener];
      } else if (prepend) {
        existing.unshift(listener);
      } else {
        existing.push(listener);
      }
      m = _getMaxListeners(target);
      if (m > 0 && existing.length > m && !existing.warned) {
        existing.warned = true;
        var w = new Error("Possible EventEmitter memory leak detected. " + existing.length + " " + String(type) + " listeners added. Use emitter.setMaxListeners() to increase limit");
        w.name = "MaxListenersExceededWarning";
        w.emitter = target;
        w.type = type;
        w.count = existing.length;
        ProcessEmitWarning(w);
      }
    }
    return target;
  }
  EventEmitter.prototype.addListener = function addListener(type, listener) {
    return _addListener(this, type, listener, false);
  };
  EventEmitter.prototype.on = EventEmitter.prototype.addListener;
  EventEmitter.prototype.prependListener = function prependListener(type, listener) {
    return _addListener(this, type, listener, true);
  };
  function onceWrapper() {
    if (!this.fired) {
      this.target.removeListener(this.type, this.wrapFn);
      this.fired = true;
      if (arguments.length === 0) return this.listener.call(this.target);
      return this.listener.apply(this.target, arguments);
    }
  }
  function _onceWrap(target, type, listener) {
    var state = {
      fired: false,
      wrapFn: void 0,
      target,
      type,
      listener
    };
    var wrapped = onceWrapper.bind(state);
    wrapped.listener = listener;
    state.wrapFn = wrapped;
    return wrapped;
  }
  EventEmitter.prototype.once = function once2(type, listener) {
    checkListener(listener);
    this.on(type, _onceWrap(this, type, listener));
    return this;
  };
  EventEmitter.prototype.prependOnceListener = function prependOnceListener(type, listener) {
    checkListener(listener);
    this.prependListener(type, _onceWrap(this, type, listener));
    return this;
  };
  EventEmitter.prototype.removeListener = function removeListener(type, listener) {
    var list, events2, position, i2, originalListener;
    checkListener(listener);
    events2 = this._events;
    if (events2 === void 0) return this;
    list = events2[type];
    if (list === void 0) return this;
    if (list === listener || list.listener === listener) {
      if (--this._eventsCount === 0) this._events = /* @__PURE__ */ Object.create(null);
      else {
        delete events2[type];
        if (events2.removeListener) this.emit("removeListener", type, list.listener || listener);
      }
    } else if (typeof list !== "function") {
      position = -1;
      for (i2 = list.length - 1; i2 >= 0; i2--) {
        if (list[i2] === listener || list[i2].listener === listener) {
          originalListener = list[i2].listener;
          position = i2;
          break;
        }
      }
      if (position < 0) return this;
      if (position === 0) list.shift();
      else {
        spliceOne(list, position);
      }
      if (list.length === 1) events2[type] = list[0];
      if (events2.removeListener !== void 0) this.emit("removeListener", type, originalListener || listener);
    }
    return this;
  };
  EventEmitter.prototype.off = EventEmitter.prototype.removeListener;
  EventEmitter.prototype.removeAllListeners = function removeAllListeners(type) {
    var listeners, events2, i2;
    events2 = this._events;
    if (events2 === void 0) return this;
    if (events2.removeListener === void 0) {
      if (arguments.length === 0) {
        this._events = /* @__PURE__ */ Object.create(null);
        this._eventsCount = 0;
      } else if (events2[type] !== void 0) {
        if (--this._eventsCount === 0) this._events = /* @__PURE__ */ Object.create(null);
        else delete events2[type];
      }
      return this;
    }
    if (arguments.length === 0) {
      var keys = Object.keys(events2);
      var key;
      for (i2 = 0; i2 < keys.length; ++i2) {
        key = keys[i2];
        if (key === "removeListener") continue;
        this.removeAllListeners(key);
      }
      this.removeAllListeners("removeListener");
      this._events = /* @__PURE__ */ Object.create(null);
      this._eventsCount = 0;
      return this;
    }
    listeners = events2[type];
    if (typeof listeners === "function") {
      this.removeListener(type, listeners);
    } else if (listeners !== void 0) {
      for (i2 = listeners.length - 1; i2 >= 0; i2--) {
        this.removeListener(type, listeners[i2]);
      }
    }
    return this;
  };
  function _listeners(target, type, unwrap) {
    var events2 = target._events;
    if (events2 === void 0) return [];
    var evlistener = events2[type];
    if (evlistener === void 0) return [];
    if (typeof evlistener === "function") return unwrap ? [evlistener.listener || evlistener] : [evlistener];
    return unwrap ? unwrapListeners(evlistener) : arrayClone(evlistener, evlistener.length);
  }
  EventEmitter.prototype.listeners = function listeners(type) {
    return _listeners(this, type, true);
  };
  EventEmitter.prototype.rawListeners = function rawListeners(type) {
    return _listeners(this, type, false);
  };
  EventEmitter.listenerCount = function(emitter, type) {
    if (typeof emitter.listenerCount === "function") {
      return emitter.listenerCount(type);
    } else {
      return listenerCount.call(emitter, type);
    }
  };
  EventEmitter.prototype.listenerCount = listenerCount;
  function listenerCount(type) {
    var events2 = this._events;
    if (events2 !== void 0) {
      var evlistener = events2[type];
      if (typeof evlistener === "function") {
        return 1;
      } else if (evlistener !== void 0) {
        return evlistener.length;
      }
    }
    return 0;
  }
  EventEmitter.prototype.eventNames = function eventNames() {
    return this._eventsCount > 0 ? ReflectOwnKeys(this._events) : [];
  };
  function arrayClone(arr, n) {
    var copy = new Array(n);
    for (var i2 = 0; i2 < n; ++i2) copy[i2] = arr[i2];
    return copy;
  }
  function spliceOne(list, index) {
    for (; index + 1 < list.length; index++) list[index] = list[index + 1];
    list.pop();
  }
  function unwrapListeners(arr) {
    var ret = new Array(arr.length);
    for (var i2 = 0; i2 < ret.length; ++i2) {
      ret[i2] = arr[i2].listener || arr[i2];
    }
    return ret;
  }
  function once(emitter, name) {
    return new Promise(function(resolve, reject) {
      function errorListener(err) {
        emitter.removeListener(name, resolver);
        reject(err);
      }
      function resolver() {
        if (typeof emitter.removeListener === "function") {
          emitter.removeListener("error", errorListener);
        }
        resolve([].slice.call(arguments));
      }
      eventTargetAgnosticAddListener(emitter, name, resolver, {
        once: true
      });
      if (name !== "error") {
        addErrorHandlerIfEventEmitter(emitter, errorListener, {
          once: true
        });
      }
    });
  }
  function addErrorHandlerIfEventEmitter(emitter, handler, flags) {
    if (typeof emitter.on === "function") {
      eventTargetAgnosticAddListener(emitter, "error", handler, flags);
    }
  }
  function eventTargetAgnosticAddListener(emitter, name, listener, flags) {
    if (typeof emitter.on === "function") {
      if (flags.once) {
        emitter.once(name, listener);
      } else {
        emitter.on(name, listener);
      }
    } else if (typeof emitter.addEventListener === "function") {
      emitter.addEventListener(name, function wrapListener(arg) {
        if (flags.once) {
          emitter.removeEventListener(name, wrapListener);
        }
        listener(arg);
      });
    } else {
      throw new TypeError('The "emitter" argument must be of type EventEmitter. Received type ' + typeof emitter);
    }
  }
  return events.exports;
}
var eventsExports = requireEvents();
var logDisabled_ = true;
var deprecationWarnings_ = true;
function extractVersion(uastring, expr, pos) {
  const match = uastring.match(expr);
  return match && match.length >= pos && parseFloat(match[pos], 10);
}
function wrapPeerConnectionEvent(window2, eventNameToWrap, wrapper) {
  if (!window2.RTCPeerConnection) {
    return;
  }
  const addEventListener = Object.getOwnPropertyDescriptor(EventTarget.prototype, "addEventListener");
  if (!addEventListener.writable) {
    log$5("Unable to polyfill events");
    return;
  }
  const proto = window2.RTCPeerConnection.prototype;
  const nativeAddEventListener = proto.addEventListener;
  proto.addEventListener = function(nativeEventName, cb) {
    if (nativeEventName !== eventNameToWrap) {
      return nativeAddEventListener.apply(this, arguments);
    }
    const wrappedCallback = (e4) => {
      const modifiedEvent = wrapper(e4);
      if (modifiedEvent) {
        if (cb.handleEvent) {
          cb.handleEvent(modifiedEvent);
        } else {
          cb(modifiedEvent);
        }
      }
    };
    this._eventMap = this._eventMap || {};
    if (!this._eventMap[eventNameToWrap]) {
      this._eventMap[eventNameToWrap] = /* @__PURE__ */ new Map();
    }
    this._eventMap[eventNameToWrap].set(cb, wrappedCallback);
    return nativeAddEventListener.apply(this, [nativeEventName, wrappedCallback]);
  };
  const nativeRemoveEventListener = proto.removeEventListener;
  proto.removeEventListener = function(nativeEventName, cb) {
    if (nativeEventName !== eventNameToWrap || !this._eventMap || !this._eventMap[eventNameToWrap]) {
      return nativeRemoveEventListener.apply(this, arguments);
    }
    if (!this._eventMap[eventNameToWrap].has(cb)) {
      return nativeRemoveEventListener.apply(this, arguments);
    }
    const unwrappedCb = this._eventMap[eventNameToWrap].get(cb);
    this._eventMap[eventNameToWrap].delete(cb);
    if (this._eventMap[eventNameToWrap].size === 0) {
      delete this._eventMap[eventNameToWrap];
    }
    if (Object.keys(this._eventMap).length === 0) {
      delete this._eventMap;
    }
    return nativeRemoveEventListener.apply(this, [nativeEventName, unwrappedCb]);
  };
  Object.defineProperty(proto, "on" + eventNameToWrap, {
    get() {
      return this["_on" + eventNameToWrap];
    },
    set(cb) {
      if (this["_on" + eventNameToWrap]) {
        this.removeEventListener(eventNameToWrap, this["_on" + eventNameToWrap]);
        delete this["_on" + eventNameToWrap];
      }
      if (cb) {
        this.addEventListener(eventNameToWrap, this["_on" + eventNameToWrap] = cb);
      }
    },
    enumerable: true,
    configurable: true
  });
}
function disableLog(bool) {
  if (typeof bool !== "boolean") {
    return new Error("Argument type: " + typeof bool + ". Please use a boolean.");
  }
  logDisabled_ = bool;
  return bool ? "adapter.js logging disabled" : "adapter.js logging enabled";
}
function disableWarnings(bool) {
  if (typeof bool !== "boolean") {
    return new Error("Argument type: " + typeof bool + ". Please use a boolean.");
  }
  deprecationWarnings_ = !bool;
  return "adapter.js deprecation warnings " + (bool ? "disabled" : "enabled");
}
function log$5() {
  if (typeof window === "object") {
    if (logDisabled_) {
      return;
    }
    if (typeof console !== "undefined" && typeof console.log === "function") {
      console.log.apply(console, arguments);
    }
  }
}
function deprecated(oldMethod, newMethod) {
  if (!deprecationWarnings_) {
    return;
  }
  console.warn(oldMethod + " is deprecated, please use " + newMethod + " instead.");
}
function detectBrowser(window2) {
  const result = {
    browser: null,
    version: null
  };
  if (typeof window2 === "undefined" || !window2.navigator || !window2.navigator.userAgent) {
    result.browser = "Not a browser.";
    return result;
  }
  const navigator2 = window2.navigator;
  if (navigator2.userAgentData && navigator2.userAgentData.brands) {
    const chromium = navigator2.userAgentData.brands.find((brand) => {
      return brand.brand === "Chromium";
    });
    if (chromium) {
      const version2 = parseInt(chromium.version, 10);
      if (version2 >= 90) {
        return {
          browser: "chrome",
          version: version2
        };
      }
    }
  }
  if (navigator2.mozGetUserMedia) {
    result.browser = "firefox";
    result.version = parseInt(extractVersion(navigator2.userAgent, /Firefox\/(\d+)\./, 1));
  } else if (navigator2.webkitGetUserMedia || window2.isSecureContext === false && window2.webkitRTCPeerConnection) {
    result.browser = "chrome";
    result.version = parseInt(extractVersion(navigator2.userAgent, /Chrom(e|ium)\/(\d+)\./, 2)) || null;
  } else if (window2.RTCPeerConnection && navigator2.userAgent.match(/AppleWebKit\/(\d+)\./)) {
    result.browser = "safari";
    result.version = parseInt(extractVersion(navigator2.userAgent, /AppleWebKit\/(\d+)\./, 1));
    result.supportsUnifiedPlan = window2.RTCRtpTransceiver && "currentDirection" in window2.RTCRtpTransceiver.prototype;
    result._safariVersion = extractVersion(navigator2.userAgent, /Version\/(\d+(\.?\d+))/, 1);
  } else {
    result.browser = "Not a supported browser.";
    return result;
  }
  return result;
}
function isObject$1(val) {
  return Object.prototype.toString.call(val) === "[object Object]";
}
function compactObject(data) {
  if (!isObject$1(data)) {
    return data;
  }
  return Object.keys(data).reduce(function(accumulator, key) {
    const isObj = isObject$1(data[key]);
    const value = isObj ? compactObject(data[key]) : data[key];
    const isEmptyObject = isObj && !Object.keys(value).length;
    if (value === void 0 || isEmptyObject) {
      return accumulator;
    }
    return Object.assign(accumulator, {
      [key]: value
    });
  }, {});
}
function walkStats(stats, base2, resultSet) {
  if (!base2 || resultSet.has(base2.id)) {
    return;
  }
  resultSet.set(base2.id, base2);
  Object.keys(base2).forEach((name) => {
    if (name.endsWith("Id")) {
      walkStats(stats, stats.get(base2[name]), resultSet);
    } else if (name.endsWith("Ids")) {
      base2[name].forEach((id) => {
        walkStats(stats, stats.get(id), resultSet);
      });
    }
  });
}
function filterStats(result, track, outbound) {
  const streamStatsType = outbound ? "outbound-rtp" : "inbound-rtp";
  const filteredResult = /* @__PURE__ */ new Map();
  if (track === null) {
    return filteredResult;
  }
  const trackStats = [];
  result.forEach((value) => {
    if (value.type === "track" && value.trackIdentifier === track.id) {
      trackStats.push(value);
    }
  });
  trackStats.forEach((trackStat) => {
    result.forEach((stats) => {
      if (stats.type === streamStatsType && stats.trackId === trackStat.id) {
        walkStats(result, stats, filteredResult);
      }
    });
  });
  return filteredResult;
}
var logging = log$5;
function shimGetUserMedia$2(window2, browserDetails2) {
  if (browserDetails2.version >= 64) {
    return;
  }
  const navigator2 = window2 && window2.navigator;
  if (!navigator2.mediaDevices) {
    return;
  }
  const constraintsToChrome_ = function(c) {
    if (typeof c !== "object" || c.mandatory || c.optional) {
      return c;
    }
    const cc = {};
    Object.keys(c).forEach((key) => {
      if (key === "require" || key === "advanced" || key === "mediaSource") {
        return;
      }
      const r2 = typeof c[key] === "object" ? c[key] : {
        ideal: c[key]
      };
      if (r2.exact !== void 0 && typeof r2.exact === "number") {
        r2.min = r2.max = r2.exact;
      }
      const oldname_ = function(prefix, name) {
        if (prefix) {
          return prefix + name.charAt(0).toUpperCase() + name.slice(1);
        }
        return name === "deviceId" ? "sourceId" : name;
      };
      if (r2.ideal !== void 0) {
        cc.optional = cc.optional || [];
        let oc = {};
        if (typeof r2.ideal === "number") {
          oc[oldname_("min", key)] = r2.ideal;
          cc.optional.push(oc);
          oc = {};
          oc[oldname_("max", key)] = r2.ideal;
          cc.optional.push(oc);
        } else {
          oc[oldname_("", key)] = r2.ideal;
          cc.optional.push(oc);
        }
      }
      if (r2.exact !== void 0 && typeof r2.exact !== "number") {
        cc.mandatory = cc.mandatory || {};
        cc.mandatory[oldname_("", key)] = r2.exact;
      } else {
        ["min", "max"].forEach((mix) => {
          if (r2[mix] !== void 0) {
            cc.mandatory = cc.mandatory || {};
            cc.mandatory[oldname_(mix, key)] = r2[mix];
          }
        });
      }
    });
    if (c.advanced) {
      cc.optional = (cc.optional || []).concat(c.advanced);
    }
    return cc;
  };
  const shimConstraints_ = function(constraints, func) {
    if (browserDetails2.version >= 61) {
      return func(constraints);
    }
    constraints = JSON.parse(JSON.stringify(constraints));
    if (constraints && typeof constraints.audio === "object") {
      const remap = function(obj, a, b) {
        if (a in obj && !(b in obj)) {
          obj[b] = obj[a];
          delete obj[a];
        }
      };
      constraints = JSON.parse(JSON.stringify(constraints));
      remap(constraints.audio, "autoGainControl", "googAutoGainControl");
      remap(constraints.audio, "noiseSuppression", "googNoiseSuppression");
      constraints.audio = constraintsToChrome_(constraints.audio);
    }
    if (constraints && typeof constraints.video === "object") {
      let face = constraints.video.facingMode;
      face = face && (typeof face === "object" ? face : {
        ideal: face
      });
      const getSupportedFacingModeLies = browserDetails2.version < 66;
      if (face && (face.exact === "user" || face.exact === "environment" || face.ideal === "user" || face.ideal === "environment") && !(navigator2.mediaDevices.getSupportedConstraints && navigator2.mediaDevices.getSupportedConstraints().facingMode && !getSupportedFacingModeLies)) {
        delete constraints.video.facingMode;
        let matches;
        if (face.exact === "environment" || face.ideal === "environment") {
          matches = ["back", "rear"];
        } else if (face.exact === "user" || face.ideal === "user") {
          matches = ["front"];
        }
        if (matches) {
          return navigator2.mediaDevices.enumerateDevices().then((devices) => {
            devices = devices.filter((d) => d.kind === "videoinput");
            let dev = devices.find((d) => matches.some((match) => d.label.toLowerCase().includes(match)));
            if (!dev && devices.length && matches.includes("back")) {
              dev = devices[devices.length - 1];
            }
            if (dev) {
              constraints.video.deviceId = face.exact ? {
                exact: dev.deviceId
              } : {
                ideal: dev.deviceId
              };
            }
            constraints.video = constraintsToChrome_(constraints.video);
            logging("chrome: " + JSON.stringify(constraints));
            return func(constraints);
          });
        }
      }
      constraints.video = constraintsToChrome_(constraints.video);
    }
    logging("chrome: " + JSON.stringify(constraints));
    return func(constraints);
  };
  const shimError_ = function(e4) {
    if (browserDetails2.version >= 64) {
      return e4;
    }
    return {
      name: {
        PermissionDeniedError: "NotAllowedError",
        PermissionDismissedError: "NotAllowedError",
        InvalidStateError: "NotAllowedError",
        DevicesNotFoundError: "NotFoundError",
        ConstraintNotSatisfiedError: "OverconstrainedError",
        TrackStartError: "NotReadableError",
        MediaDeviceFailedDueToShutdown: "NotAllowedError",
        MediaDeviceKillSwitchOn: "NotAllowedError",
        TabCaptureError: "AbortError",
        ScreenCaptureError: "AbortError",
        DeviceCaptureError: "AbortError"
      }[e4.name] || e4.name,
      message: e4.message,
      constraint: e4.constraint || e4.constraintName,
      toString() {
        return this.name + (this.message && ": ") + this.message;
      }
    };
  };
  const getUserMedia_ = function(constraints, onSuccess, onError) {
    shimConstraints_(constraints, (c) => {
      navigator2.webkitGetUserMedia(c, onSuccess, (e4) => {
        if (onError) {
          onError(shimError_(e4));
        }
      });
    });
  };
  navigator2.getUserMedia = getUserMedia_.bind(navigator2);
  if (navigator2.mediaDevices.getUserMedia) {
    const origGetUserMedia = navigator2.mediaDevices.getUserMedia.bind(navigator2.mediaDevices);
    navigator2.mediaDevices.getUserMedia = function(cs2) {
      return shimConstraints_(cs2, (c) => origGetUserMedia(c).then((stream) => {
        if (c.audio && !stream.getAudioTracks().length || c.video && !stream.getVideoTracks().length) {
          stream.getTracks().forEach((track) => {
            track.stop();
          });
          throw new DOMException("", "NotFoundError");
        }
        return stream;
      }, (e4) => Promise.reject(shimError_(e4))));
    };
  }
}
function shimMediaStream(window2) {
  window2.MediaStream = window2.MediaStream || window2.webkitMediaStream;
}
function shimOnTrack$1(window2, browserDetails2) {
  if (browserDetails2.version > 102) {
    return;
  }
  if (typeof window2 === "object" && window2.RTCPeerConnection && !("ontrack" in window2.RTCPeerConnection.prototype)) {
    Object.defineProperty(window2.RTCPeerConnection.prototype, "ontrack", {
      get() {
        return this._ontrack;
      },
      set(f) {
        if (this._ontrack) {
          this.removeEventListener("track", this._ontrack);
        }
        this.addEventListener("track", this._ontrack = f);
      },
      enumerable: true,
      configurable: true
    });
    const origSetRemoteDescription = window2.RTCPeerConnection.prototype.setRemoteDescription;
    window2.RTCPeerConnection.prototype.setRemoteDescription = function setRemoteDescription() {
      if (!this._ontrackpoly) {
        this._ontrackpoly = (e4) => {
          e4.stream.addEventListener("addtrack", (te2) => {
            let receiver;
            if (window2.RTCPeerConnection.prototype.getReceivers) {
              receiver = this.getReceivers().find((r2) => r2.track && r2.track.id === te2.track.id);
            } else {
              receiver = {
                track: te2.track
              };
            }
            const event = new Event("track");
            event.track = te2.track;
            event.receiver = receiver;
            event.transceiver = {
              receiver
            };
            event.streams = [e4.stream];
            this.dispatchEvent(event);
          });
          e4.stream.getTracks().forEach((track) => {
            let receiver;
            if (window2.RTCPeerConnection.prototype.getReceivers) {
              receiver = this.getReceivers().find((r2) => r2.track && r2.track.id === track.id);
            } else {
              receiver = {
                track
              };
            }
            const event = new Event("track");
            event.track = track;
            event.receiver = receiver;
            event.transceiver = {
              receiver
            };
            event.streams = [e4.stream];
            this.dispatchEvent(event);
          });
        };
        this.addEventListener("addstream", this._ontrackpoly);
      }
      return origSetRemoteDescription.apply(this, arguments);
    };
  } else {
    wrapPeerConnectionEvent(window2, "track", (e4) => {
      if (!e4.transceiver) {
        Object.defineProperty(e4, "transceiver", {
          value: {
            receiver: e4.receiver
          }
        });
      }
      return e4;
    });
  }
}
function shimGetSendersWithDtmf(window2) {
  if (typeof window2 === "object" && window2.RTCPeerConnection && !("getSenders" in window2.RTCPeerConnection.prototype) && "createDTMFSender" in window2.RTCPeerConnection.prototype) {
    const shimSenderWithDtmf = function(pc, track) {
      return {
        track,
        get dtmf() {
          if (this._dtmf === void 0) {
            if (track.kind === "audio") {
              this._dtmf = pc.createDTMFSender(track);
            } else {
              this._dtmf = null;
            }
          }
          return this._dtmf;
        },
        _pc: pc
      };
    };
    if (!window2.RTCPeerConnection.prototype.getSenders) {
      window2.RTCPeerConnection.prototype.getSenders = function getSenders() {
        this._senders = this._senders || [];
        return this._senders.slice();
      };
      const origAddTrack = window2.RTCPeerConnection.prototype.addTrack;
      window2.RTCPeerConnection.prototype.addTrack = function addTrack(track, stream) {
        let sender = origAddTrack.apply(this, arguments);
        if (!sender) {
          sender = shimSenderWithDtmf(this, track);
          this._senders.push(sender);
        }
        return sender;
      };
      const origRemoveTrack = window2.RTCPeerConnection.prototype.removeTrack;
      window2.RTCPeerConnection.prototype.removeTrack = function removeTrack(sender) {
        origRemoveTrack.apply(this, arguments);
        const idx = this._senders.indexOf(sender);
        if (idx !== -1) {
          this._senders.splice(idx, 1);
        }
      };
    }
    const origAddStream = window2.RTCPeerConnection.prototype.addStream;
    window2.RTCPeerConnection.prototype.addStream = function addStream(stream) {
      this._senders = this._senders || [];
      origAddStream.apply(this, [stream]);
      stream.getTracks().forEach((track) => {
        this._senders.push(shimSenderWithDtmf(this, track));
      });
    };
    const origRemoveStream = window2.RTCPeerConnection.prototype.removeStream;
    window2.RTCPeerConnection.prototype.removeStream = function removeStream(stream) {
      this._senders = this._senders || [];
      origRemoveStream.apply(this, [stream]);
      stream.getTracks().forEach((track) => {
        const sender = this._senders.find((s) => s.track === track);
        if (sender) {
          this._senders.splice(this._senders.indexOf(sender), 1);
        }
      });
    };
  } else if (typeof window2 === "object" && window2.RTCPeerConnection && "getSenders" in window2.RTCPeerConnection.prototype && "createDTMFSender" in window2.RTCPeerConnection.prototype && window2.RTCRtpSender && !("dtmf" in window2.RTCRtpSender.prototype)) {
    const origGetSenders = window2.RTCPeerConnection.prototype.getSenders;
    window2.RTCPeerConnection.prototype.getSenders = function getSenders() {
      const senders = origGetSenders.apply(this, []);
      senders.forEach((sender) => sender._pc = this);
      return senders;
    };
    Object.defineProperty(window2.RTCRtpSender.prototype, "dtmf", {
      get() {
        if (this._dtmf === void 0) {
          if (this.track.kind === "audio") {
            this._dtmf = this._pc.createDTMFSender(this.track);
          } else {
            this._dtmf = null;
          }
        }
        return this._dtmf;
      }
    });
  }
}
function shimSenderReceiverGetStats(window2, browserDetails2) {
  if (browserDetails2.version >= 67) {
    return;
  }
  if (!(typeof window2 === "object" && window2.RTCPeerConnection && window2.RTCRtpSender && window2.RTCRtpReceiver)) {
    return;
  }
  if (!("getStats" in window2.RTCRtpSender.prototype)) {
    const origGetSenders = window2.RTCPeerConnection.prototype.getSenders;
    if (origGetSenders) {
      window2.RTCPeerConnection.prototype.getSenders = function getSenders() {
        const senders = origGetSenders.apply(this, []);
        senders.forEach((sender) => sender._pc = this);
        return senders;
      };
    }
    const origAddTrack = window2.RTCPeerConnection.prototype.addTrack;
    if (origAddTrack) {
      window2.RTCPeerConnection.prototype.addTrack = function addTrack() {
        const sender = origAddTrack.apply(this, arguments);
        sender._pc = this;
        return sender;
      };
    }
    window2.RTCRtpSender.prototype.getStats = function getStats() {
      const sender = this;
      return this._pc.getStats().then((result) => (
        /* Note: this will include stats of all senders that
         *   send a track with the same id as sender.track as
         *   it is not possible to identify the RTCRtpSender.
         */
        filterStats(result, sender.track, true)
      ));
    };
  }
  if (!("getStats" in window2.RTCRtpReceiver.prototype)) {
    const origGetReceivers = window2.RTCPeerConnection.prototype.getReceivers;
    if (origGetReceivers) {
      window2.RTCPeerConnection.prototype.getReceivers = function getReceivers() {
        const receivers = origGetReceivers.apply(this, []);
        receivers.forEach((receiver) => receiver._pc = this);
        return receivers;
      };
    }
    wrapPeerConnectionEvent(window2, "track", (e4) => {
      e4.receiver._pc = e4.srcElement;
      return e4;
    });
    window2.RTCRtpReceiver.prototype.getStats = function getStats() {
      const receiver = this;
      return this._pc.getStats().then((result) => filterStats(result, receiver.track, false));
    };
  }
  if (!("getStats" in window2.RTCRtpSender.prototype && "getStats" in window2.RTCRtpReceiver.prototype)) {
    return;
  }
  const origGetStats = window2.RTCPeerConnection.prototype.getStats;
  window2.RTCPeerConnection.prototype.getStats = function getStats() {
    if (arguments.length > 0 && arguments[0] instanceof window2.MediaStreamTrack) {
      const track = arguments[0];
      let sender;
      let receiver;
      let err;
      this.getSenders().forEach((s) => {
        if (s.track === track) {
          if (sender) {
            err = true;
          } else {
            sender = s;
          }
        }
      });
      this.getReceivers().forEach((r2) => {
        if (r2.track === track) {
          if (receiver) {
            err = true;
          } else {
            receiver = r2;
          }
        }
        return r2.track === track;
      });
      if (err || sender && receiver) {
        return Promise.reject(new DOMException("There are more than one sender or receiver for the track.", "InvalidAccessError"));
      } else if (sender) {
        return sender.getStats();
      } else if (receiver) {
        return receiver.getStats();
      }
      return Promise.reject(new DOMException("There is no sender or receiver for the track.", "InvalidAccessError"));
    }
    return origGetStats.apply(this, arguments);
  };
}
function shimAddTrackRemoveTrackWithNative(window2) {
  window2.RTCPeerConnection.prototype.getLocalStreams = function getLocalStreams() {
    this._shimmedLocalStreams = this._shimmedLocalStreams || {};
    return Object.keys(this._shimmedLocalStreams).map((streamId) => this._shimmedLocalStreams[streamId][0]);
  };
  const origAddTrack = window2.RTCPeerConnection.prototype.addTrack;
  window2.RTCPeerConnection.prototype.addTrack = function addTrack(track, stream) {
    if (!stream) {
      return origAddTrack.apply(this, arguments);
    }
    this._shimmedLocalStreams = this._shimmedLocalStreams || {};
    const sender = origAddTrack.apply(this, arguments);
    if (!this._shimmedLocalStreams[stream.id]) {
      this._shimmedLocalStreams[stream.id] = [stream, sender];
    } else if (this._shimmedLocalStreams[stream.id].indexOf(sender) === -1) {
      this._shimmedLocalStreams[stream.id].push(sender);
    }
    return sender;
  };
  const origAddStream = window2.RTCPeerConnection.prototype.addStream;
  window2.RTCPeerConnection.prototype.addStream = function addStream(stream) {
    this._shimmedLocalStreams = this._shimmedLocalStreams || {};
    stream.getTracks().forEach((track) => {
      const alreadyExists = this.getSenders().find((s) => s.track === track);
      if (alreadyExists) {
        throw new DOMException("Track already exists.", "InvalidAccessError");
      }
    });
    const existingSenders = this.getSenders();
    origAddStream.apply(this, arguments);
    const newSenders = this.getSenders().filter((newSender) => existingSenders.indexOf(newSender) === -1);
    this._shimmedLocalStreams[stream.id] = [stream].concat(newSenders);
  };
  const origRemoveStream = window2.RTCPeerConnection.prototype.removeStream;
  window2.RTCPeerConnection.prototype.removeStream = function removeStream(stream) {
    this._shimmedLocalStreams = this._shimmedLocalStreams || {};
    delete this._shimmedLocalStreams[stream.id];
    return origRemoveStream.apply(this, arguments);
  };
  const origRemoveTrack = window2.RTCPeerConnection.prototype.removeTrack;
  window2.RTCPeerConnection.prototype.removeTrack = function removeTrack(sender) {
    this._shimmedLocalStreams = this._shimmedLocalStreams || {};
    if (sender) {
      Object.keys(this._shimmedLocalStreams).forEach((streamId) => {
        const idx = this._shimmedLocalStreams[streamId].indexOf(sender);
        if (idx !== -1) {
          this._shimmedLocalStreams[streamId].splice(idx, 1);
        }
        if (this._shimmedLocalStreams[streamId].length === 1) {
          delete this._shimmedLocalStreams[streamId];
        }
      });
    }
    return origRemoveTrack.apply(this, arguments);
  };
}
function shimAddTrackRemoveTrack(window2, browserDetails2) {
  if (!window2.RTCPeerConnection) {
    return;
  }
  if (window2.RTCPeerConnection.prototype.addTrack && browserDetails2.version >= 65) {
    return shimAddTrackRemoveTrackWithNative(window2);
  }
  const origGetLocalStreams = window2.RTCPeerConnection.prototype.getLocalStreams;
  window2.RTCPeerConnection.prototype.getLocalStreams = function getLocalStreams() {
    const nativeStreams = origGetLocalStreams.apply(this);
    this._reverseStreams = this._reverseStreams || {};
    return nativeStreams.map((stream) => this._reverseStreams[stream.id]);
  };
  const origAddStream = window2.RTCPeerConnection.prototype.addStream;
  window2.RTCPeerConnection.prototype.addStream = function addStream(stream) {
    this._streams = this._streams || {};
    this._reverseStreams = this._reverseStreams || {};
    stream.getTracks().forEach((track) => {
      const alreadyExists = this.getSenders().find((s) => s.track === track);
      if (alreadyExists) {
        throw new DOMException("Track already exists.", "InvalidAccessError");
      }
    });
    if (!this._reverseStreams[stream.id]) {
      const newStream = new window2.MediaStream(stream.getTracks());
      this._streams[stream.id] = newStream;
      this._reverseStreams[newStream.id] = stream;
      stream = newStream;
    }
    origAddStream.apply(this, [stream]);
  };
  const origRemoveStream = window2.RTCPeerConnection.prototype.removeStream;
  window2.RTCPeerConnection.prototype.removeStream = function removeStream(stream) {
    this._streams = this._streams || {};
    this._reverseStreams = this._reverseStreams || {};
    origRemoveStream.apply(this, [this._streams[stream.id] || stream]);
    delete this._reverseStreams[this._streams[stream.id] ? this._streams[stream.id].id : stream.id];
    delete this._streams[stream.id];
  };
  window2.RTCPeerConnection.prototype.addTrack = function addTrack(track, stream) {
    if (this.signalingState === "closed") {
      throw new DOMException("The RTCPeerConnection's signalingState is 'closed'.", "InvalidStateError");
    }
    const streams = [].slice.call(arguments, 1);
    if (streams.length !== 1 || !streams[0].getTracks().find((t) => t === track)) {
      throw new DOMException("The adapter.js addTrack polyfill only supports a single  stream which is associated with the specified track.", "NotSupportedError");
    }
    const alreadyExists = this.getSenders().find((s) => s.track === track);
    if (alreadyExists) {
      throw new DOMException("Track already exists.", "InvalidAccessError");
    }
    this._streams = this._streams || {};
    this._reverseStreams = this._reverseStreams || {};
    const oldStream = this._streams[stream.id];
    if (oldStream) {
      oldStream.addTrack(track);
      Promise.resolve().then(() => {
        this.dispatchEvent(new Event("negotiationneeded"));
      });
    } else {
      const newStream = new window2.MediaStream([track]);
      this._streams[stream.id] = newStream;
      this._reverseStreams[newStream.id] = stream;
      this.addStream(newStream);
    }
    return this.getSenders().find((s) => s.track === track);
  };
  function replaceInternalStreamId(pc, description) {
    let sdp2 = description.sdp;
    Object.keys(pc._reverseStreams || []).forEach((internalId) => {
      const externalStream = pc._reverseStreams[internalId];
      const internalStream = pc._streams[externalStream.id];
      sdp2 = sdp2.replace(new RegExp(internalStream.id, "g"), externalStream.id);
    });
    return new RTCSessionDescription({
      type: description.type,
      sdp: sdp2
    });
  }
  function replaceExternalStreamId(pc, description) {
    let sdp2 = description.sdp;
    Object.keys(pc._reverseStreams || []).forEach((internalId) => {
      const externalStream = pc._reverseStreams[internalId];
      const internalStream = pc._streams[externalStream.id];
      sdp2 = sdp2.replace(new RegExp(externalStream.id, "g"), internalStream.id);
    });
    return new RTCSessionDescription({
      type: description.type,
      sdp: sdp2
    });
  }
  ["createOffer", "createAnswer"].forEach(function(method) {
    const nativeMethod = window2.RTCPeerConnection.prototype[method];
    const methodObj = {
      [method]() {
        const args = arguments;
        const isLegacyCall = arguments.length && typeof arguments[0] === "function";
        if (isLegacyCall) {
          return nativeMethod.apply(this, [(description) => {
            const desc = replaceInternalStreamId(this, description);
            args[0].apply(null, [desc]);
          }, (err) => {
            if (args[1]) {
              args[1].apply(null, err);
            }
          }, arguments[2]]);
        }
        return nativeMethod.apply(this, arguments).then((description) => replaceInternalStreamId(this, description));
      }
    };
    window2.RTCPeerConnection.prototype[method] = methodObj[method];
  });
  const origSetLocalDescription = window2.RTCPeerConnection.prototype.setLocalDescription;
  window2.RTCPeerConnection.prototype.setLocalDescription = function setLocalDescription() {
    if (!arguments.length || !arguments[0].type) {
      return origSetLocalDescription.apply(this, arguments);
    }
    arguments[0] = replaceExternalStreamId(this, arguments[0]);
    return origSetLocalDescription.apply(this, arguments);
  };
  const origLocalDescription = Object.getOwnPropertyDescriptor(window2.RTCPeerConnection.prototype, "localDescription");
  Object.defineProperty(window2.RTCPeerConnection.prototype, "localDescription", {
    get() {
      const description = origLocalDescription.get.apply(this);
      if (description.type === "") {
        return description;
      }
      return replaceInternalStreamId(this, description);
    }
  });
  window2.RTCPeerConnection.prototype.removeTrack = function removeTrack(sender) {
    if (this.signalingState === "closed") {
      throw new DOMException("The RTCPeerConnection's signalingState is 'closed'.", "InvalidStateError");
    }
    if (!sender._pc) {
      throw new DOMException("Argument 1 of RTCPeerConnection.removeTrack does not implement interface RTCRtpSender.", "TypeError");
    }
    const isLocal = sender._pc === this;
    if (!isLocal) {
      throw new DOMException("Sender was not created by this connection.", "InvalidAccessError");
    }
    this._streams = this._streams || {};
    let stream;
    Object.keys(this._streams).forEach((streamid) => {
      const hasTrack = this._streams[streamid].getTracks().find((track) => sender.track === track);
      if (hasTrack) {
        stream = this._streams[streamid];
      }
    });
    if (stream) {
      if (stream.getTracks().length === 1) {
        this.removeStream(this._reverseStreams[stream.id]);
      } else {
        stream.removeTrack(sender.track);
      }
      this.dispatchEvent(new Event("negotiationneeded"));
    }
  };
}
function shimPeerConnection$1(window2, browserDetails2) {
  if (!window2.RTCPeerConnection && window2.webkitRTCPeerConnection) {
    window2.RTCPeerConnection = window2.webkitRTCPeerConnection;
  }
  if (!window2.RTCPeerConnection) {
    return;
  }
  if (browserDetails2.version < 53) {
    ["setLocalDescription", "setRemoteDescription", "addIceCandidate"].forEach(function(method) {
      const nativeMethod = window2.RTCPeerConnection.prototype[method];
      const methodObj = {
        [method]() {
          arguments[0] = new (method === "addIceCandidate" ? window2.RTCIceCandidate : window2.RTCSessionDescription)(arguments[0]);
          return nativeMethod.apply(this, arguments);
        }
      };
      window2.RTCPeerConnection.prototype[method] = methodObj[method];
    });
  }
}
function fixNegotiationNeeded(window2, browserDetails2) {
  if (browserDetails2.version > 102) {
    return;
  }
  wrapPeerConnectionEvent(window2, "negotiationneeded", (e4) => {
    const pc = e4.target;
    if (browserDetails2.version < 72 || pc.getConfiguration && pc.getConfiguration().sdpSemantics === "plan-b") {
      if (pc.signalingState !== "stable") {
        return;
      }
    }
    return e4;
  });
}
var chromeShim = Object.freeze({ __proto__: null, fixNegotiationNeeded, shimAddTrackRemoveTrack, shimAddTrackRemoveTrackWithNative, shimGetSendersWithDtmf, shimGetUserMedia: shimGetUserMedia$2, shimMediaStream, shimOnTrack: shimOnTrack$1, shimPeerConnection: shimPeerConnection$1, shimSenderReceiverGetStats });
function shimGetUserMedia$1(window2, browserDetails2) {
  const navigator2 = window2 && window2.navigator;
  if (!navigator2.mediaDevices) {
    return;
  }
  const MediaStreamTrack2 = window2 && window2.MediaStreamTrack;
  navigator2.getUserMedia = function(constraints, onSuccess, onError) {
    deprecated("navigator.getUserMedia", "navigator.mediaDevices.getUserMedia");
    navigator2.mediaDevices.getUserMedia(constraints).then(onSuccess, onError);
  };
  if (!(browserDetails2.version > 55 && "autoGainControl" in navigator2.mediaDevices.getSupportedConstraints())) {
    const remap = function(obj, a, b) {
      if (a in obj && !(b in obj)) {
        obj[b] = obj[a];
        delete obj[a];
      }
    };
    const nativeGetUserMedia = navigator2.mediaDevices.getUserMedia.bind(navigator2.mediaDevices);
    navigator2.mediaDevices.getUserMedia = function(c) {
      if (typeof c === "object" && typeof c.audio === "object") {
        c = JSON.parse(JSON.stringify(c));
        remap(c.audio, "autoGainControl", "mozAutoGainControl");
        remap(c.audio, "noiseSuppression", "mozNoiseSuppression");
      }
      return nativeGetUserMedia(c);
    };
    if (MediaStreamTrack2 && MediaStreamTrack2.prototype.getSettings) {
      const nativeGetSettings = MediaStreamTrack2.prototype.getSettings;
      MediaStreamTrack2.prototype.getSettings = function() {
        const obj = nativeGetSettings.apply(this, arguments);
        remap(obj, "mozAutoGainControl", "autoGainControl");
        remap(obj, "mozNoiseSuppression", "noiseSuppression");
        return obj;
      };
    }
    if (MediaStreamTrack2 && MediaStreamTrack2.prototype.applyConstraints) {
      const nativeApplyConstraints = MediaStreamTrack2.prototype.applyConstraints;
      MediaStreamTrack2.prototype.applyConstraints = function(c) {
        if (this.kind === "audio" && typeof c === "object") {
          c = JSON.parse(JSON.stringify(c));
          remap(c, "autoGainControl", "mozAutoGainControl");
          remap(c, "noiseSuppression", "mozNoiseSuppression");
        }
        return nativeApplyConstraints.apply(this, [c]);
      };
    }
  }
}
function shimGetDisplayMedia(window2, preferredMediaSource) {
  if (!window2.navigator.mediaDevices) {
    return;
  }
  if (window2.navigator.mediaDevices && "getDisplayMedia" in window2.navigator.mediaDevices) {
    return;
  }
  window2.navigator.mediaDevices.getDisplayMedia = function getDisplayMedia(constraints) {
    if (!(constraints && constraints.video)) {
      const err = new DOMException("getDisplayMedia without video constraints is undefined");
      err.name = "NotFoundError";
      err.code = 8;
      return Promise.reject(err);
    }
    if (constraints.video === true) {
      constraints.video = {
        mediaSource: preferredMediaSource
      };
    } else {
      constraints.video.mediaSource = preferredMediaSource;
    }
    return window2.navigator.mediaDevices.getUserMedia(constraints);
  };
}
function shimOnTrack(window2) {
  if (typeof window2 === "object" && window2.RTCTrackEvent && "receiver" in window2.RTCTrackEvent.prototype && !("transceiver" in window2.RTCTrackEvent.prototype)) {
    Object.defineProperty(window2.RTCTrackEvent.prototype, "transceiver", {
      get() {
        return {
          receiver: this.receiver
        };
      }
    });
  }
}
function shimPeerConnection(window2, browserDetails2) {
  if (typeof window2 !== "object" || !(window2.RTCPeerConnection || window2.mozRTCPeerConnection)) {
    return;
  }
  if (!window2.RTCPeerConnection && window2.mozRTCPeerConnection) {
    window2.RTCPeerConnection = window2.mozRTCPeerConnection;
  }
  if (browserDetails2.version < 53) {
    ["setLocalDescription", "setRemoteDescription", "addIceCandidate"].forEach(function(method) {
      const nativeMethod = window2.RTCPeerConnection.prototype[method];
      const methodObj = {
        [method]() {
          arguments[0] = new (method === "addIceCandidate" ? window2.RTCIceCandidate : window2.RTCSessionDescription)(arguments[0]);
          return nativeMethod.apply(this, arguments);
        }
      };
      window2.RTCPeerConnection.prototype[method] = methodObj[method];
    });
  }
}
function shimGetStats(window2, browserDetails2) {
  if (typeof window2 !== "object" || !(window2.RTCPeerConnection || window2.mozRTCPeerConnection)) {
    return;
  }
  if (browserDetails2.version >= 151) {
    return;
  }
  const modernStatsTypes = {
    inboundrtp: "inbound-rtp",
    outboundrtp: "outbound-rtp",
    candidatepair: "candidate-pair",
    localcandidate: "local-candidate",
    remotecandidate: "remote-candidate"
  };
  const nativeGetStats = window2.RTCPeerConnection.prototype.getStats;
  window2.RTCPeerConnection.prototype.getStats = function getStats() {
    const _arguments = Array.prototype.slice.call(arguments), selector = _arguments[0], onSucc = _arguments[1], onErr = _arguments[2];
    if (this.signalingState === "closed") {
      return Promise.resolve(/* @__PURE__ */ new Map());
    }
    return nativeGetStats.apply(this, [selector || null]).then((stats) => {
      if (browserDetails2.version < 53 && !onSucc) {
        try {
          stats.forEach((stat) => {
            stat.type = modernStatsTypes[stat.type] || stat.type;
          });
        } catch (e4) {
          if (e4.name !== "TypeError") {
            throw e4;
          }
          stats.forEach((stat, i2) => {
            stats.set(i2, Object.assign({}, stat, {
              type: modernStatsTypes[stat.type] || stat.type
            }));
          });
        }
      }
      return stats;
    }).then(onSucc, onErr);
  };
}
function shimSenderGetStats(window2) {
  if (!(typeof window2 === "object" && window2.RTCPeerConnection && window2.RTCRtpSender)) {
    return;
  }
  if (window2.RTCRtpSender && "getStats" in window2.RTCRtpSender.prototype) {
    return;
  }
  const origGetSenders = window2.RTCPeerConnection.prototype.getSenders;
  if (origGetSenders) {
    window2.RTCPeerConnection.prototype.getSenders = function getSenders() {
      const senders = origGetSenders.apply(this, []);
      senders.forEach((sender) => sender._pc = this);
      return senders;
    };
  }
  const origAddTrack = window2.RTCPeerConnection.prototype.addTrack;
  if (origAddTrack) {
    window2.RTCPeerConnection.prototype.addTrack = function addTrack() {
      const sender = origAddTrack.apply(this, arguments);
      sender._pc = this;
      return sender;
    };
  }
  window2.RTCRtpSender.prototype.getStats = function getStats() {
    return this.track ? this._pc.getStats(this.track) : Promise.resolve(/* @__PURE__ */ new Map());
  };
}
function shimReceiverGetStats(window2) {
  if (!(typeof window2 === "object" && window2.RTCPeerConnection && window2.RTCRtpSender)) {
    return;
  }
  if (window2.RTCRtpSender && "getStats" in window2.RTCRtpReceiver.prototype) {
    return;
  }
  const origGetReceivers = window2.RTCPeerConnection.prototype.getReceivers;
  if (origGetReceivers) {
    window2.RTCPeerConnection.prototype.getReceivers = function getReceivers() {
      const receivers = origGetReceivers.apply(this, []);
      receivers.forEach((receiver) => receiver._pc = this);
      return receivers;
    };
  }
  wrapPeerConnectionEvent(window2, "track", (e4) => {
    e4.receiver._pc = e4.srcElement;
    return e4;
  });
  window2.RTCRtpReceiver.prototype.getStats = function getStats() {
    return this._pc.getStats(this.track);
  };
}
function shimRemoveStream(window2) {
  if (!window2.RTCPeerConnection || "removeStream" in window2.RTCPeerConnection.prototype) {
    return;
  }
  window2.RTCPeerConnection.prototype.removeStream = function removeStream(stream) {
    deprecated("removeStream", "removeTrack");
    this.getSenders().forEach((sender) => {
      if (sender.track && stream.getTracks().includes(sender.track)) {
        this.removeTrack(sender);
      }
    });
  };
}
function shimRTCDataChannel(window2) {
  if (window2.DataChannel && !window2.RTCDataChannel) {
    window2.RTCDataChannel = window2.DataChannel;
  }
}
function shimAddTransceiver(window2, browserDetails2) {
  if (!(typeof window2 === "object" && window2.RTCPeerConnection)) {
    return;
  }
  if (browserDetails2.version >= 110) {
    return;
  }
  const origAddTransceiver = window2.RTCPeerConnection.prototype.addTransceiver;
  if (origAddTransceiver) {
    window2.RTCPeerConnection.prototype.addTransceiver = function addTransceiver() {
      this.setParametersPromises = [];
      let sendEncodings = arguments[1] && arguments[1].sendEncodings;
      if (sendEncodings === void 0) {
        sendEncodings = [];
      }
      sendEncodings = [...sendEncodings];
      const shouldPerformCheck = sendEncodings.length > 0;
      if (shouldPerformCheck) {
        sendEncodings.forEach((encodingParam) => {
          if ("rid" in encodingParam) {
            const ridRegex = /^[a-z0-9]{0,16}$/i;
            if (!ridRegex.test(encodingParam.rid)) {
              throw new TypeError("Invalid RID value provided.");
            }
          }
          if ("scaleResolutionDownBy" in encodingParam) {
            if (!(parseFloat(encodingParam.scaleResolutionDownBy) >= 1)) {
              throw new RangeError("scale_resolution_down_by must be >= 1.0");
            }
          }
          if ("maxFramerate" in encodingParam) {
            if (!(parseFloat(encodingParam.maxFramerate) >= 0)) {
              throw new RangeError("max_framerate must be >= 0.0");
            }
          }
        });
      }
      const transceiver = origAddTransceiver.apply(this, arguments);
      if (shouldPerformCheck) {
        const sender = transceiver.sender;
        const params = sender.getParameters();
        if (!("encodings" in params) || // Avoid being fooled by patched getParameters() below.
        params.encodings.length === 1 && Object.keys(params.encodings[0]).length === 0) {
          params.encodings = sendEncodings;
          sender.sendEncodings = sendEncodings;
          this.setParametersPromises.push(sender.setParameters(params).then(() => {
            delete sender.sendEncodings;
          }).catch(() => {
            delete sender.sendEncodings;
          }));
        }
      }
      return transceiver;
    };
  }
}
function shimGetParameters(window2, browserDetails2) {
  if (!(typeof window2 === "object" && window2.RTCRtpSender)) {
    return;
  }
  if (browserDetails2.version >= 110) {
    return;
  }
  const origGetParameters = window2.RTCRtpSender.prototype.getParameters;
  if (origGetParameters) {
    window2.RTCRtpSender.prototype.getParameters = function getParameters() {
      const params = origGetParameters.apply(this, arguments);
      if (!("encodings" in params)) {
        params.encodings = [].concat(this.sendEncodings || [{}]);
      }
      return params;
    };
  }
}
function shimCreateOffer(window2, browserDetails2) {
  if (!(typeof window2 === "object" && window2.RTCPeerConnection)) {
    return;
  }
  if (browserDetails2.version >= 110) {
    return;
  }
  const origCreateOffer = window2.RTCPeerConnection.prototype.createOffer;
  window2.RTCPeerConnection.prototype.createOffer = function createOffer() {
    if (this.setParametersPromises && this.setParametersPromises.length) {
      return Promise.all(this.setParametersPromises).then(() => {
        return origCreateOffer.apply(this, arguments);
      }).finally(() => {
        this.setParametersPromises = [];
      });
    }
    return origCreateOffer.apply(this, arguments);
  };
}
function shimCreateAnswer(window2, browserDetails2) {
  if (!(typeof window2 === "object" && window2.RTCPeerConnection)) {
    return;
  }
  if (browserDetails2.version >= 110) {
    return;
  }
  const origCreateAnswer = window2.RTCPeerConnection.prototype.createAnswer;
  window2.RTCPeerConnection.prototype.createAnswer = function createAnswer() {
    if (this.setParametersPromises && this.setParametersPromises.length) {
      return Promise.all(this.setParametersPromises).then(() => {
        return origCreateAnswer.apply(this, arguments);
      }).finally(() => {
        this.setParametersPromises = [];
      });
    }
    return origCreateAnswer.apply(this, arguments);
  };
}
var firefoxShim = Object.freeze({ __proto__: null, shimAddTransceiver, shimCreateAnswer, shimCreateOffer, shimGetDisplayMedia, shimGetParameters, shimGetStats, shimGetUserMedia: shimGetUserMedia$1, shimOnTrack, shimPeerConnection, shimRTCDataChannel, shimReceiverGetStats, shimRemoveStream, shimSenderGetStats });
function shimLocalStreamsAPI(window2) {
  if (typeof window2 !== "object" || !window2.RTCPeerConnection) {
    return;
  }
  if (!("getLocalStreams" in window2.RTCPeerConnection.prototype)) {
    window2.RTCPeerConnection.prototype.getLocalStreams = function getLocalStreams() {
      if (!this._localStreams) {
        this._localStreams = [];
      }
      return this._localStreams;
    };
  }
  if (!("addStream" in window2.RTCPeerConnection.prototype)) {
    const _addTrack = window2.RTCPeerConnection.prototype.addTrack;
    window2.RTCPeerConnection.prototype.addStream = function addStream(stream) {
      if (!this._localStreams) {
        this._localStreams = [];
      }
      if (!this._localStreams.includes(stream)) {
        this._localStreams.push(stream);
      }
      stream.getAudioTracks().forEach((track) => _addTrack.call(this, track, stream));
      stream.getVideoTracks().forEach((track) => _addTrack.call(this, track, stream));
    };
    window2.RTCPeerConnection.prototype.addTrack = function addTrack(track) {
      for (var _len = arguments.length, streams = new Array(_len > 1 ? _len - 1 : 0), _key = 1; _key < _len; _key++) {
        streams[_key - 1] = arguments[_key];
      }
      if (streams) {
        streams.forEach((stream) => {
          if (!this._localStreams) {
            this._localStreams = [stream];
          } else if (!this._localStreams.includes(stream)) {
            this._localStreams.push(stream);
          }
        });
      }
      return _addTrack.apply(this, arguments);
    };
  }
  if (!("removeStream" in window2.RTCPeerConnection.prototype)) {
    window2.RTCPeerConnection.prototype.removeStream = function removeStream(stream) {
      if (!this._localStreams) {
        this._localStreams = [];
      }
      const index = this._localStreams.indexOf(stream);
      if (index === -1) {
        return;
      }
      this._localStreams.splice(index, 1);
      const tracks = stream.getTracks();
      this.getSenders().forEach((sender) => {
        if (tracks.includes(sender.track)) {
          this.removeTrack(sender);
        }
      });
    };
  }
}
function shimRemoteStreamsAPI(window2) {
  if (typeof window2 !== "object" || !window2.RTCPeerConnection) {
    return;
  }
  if (!("getRemoteStreams" in window2.RTCPeerConnection.prototype)) {
    window2.RTCPeerConnection.prototype.getRemoteStreams = function getRemoteStreams() {
      return this._remoteStreams ? this._remoteStreams : [];
    };
  }
  if (!("onaddstream" in window2.RTCPeerConnection.prototype)) {
    Object.defineProperty(window2.RTCPeerConnection.prototype, "onaddstream", {
      get() {
        return this._onaddstream;
      },
      set(f) {
        if (this._onaddstream) {
          this.removeEventListener("addstream", this._onaddstream);
          this.removeEventListener("track", this._onaddstreampoly);
        }
        this.addEventListener("addstream", this._onaddstream = f);
        this.addEventListener("track", this._onaddstreampoly = (e4) => {
          e4.streams.forEach((stream) => {
            if (!this._remoteStreams) {
              this._remoteStreams = [];
            }
            if (this._remoteStreams.includes(stream)) {
              return;
            }
            this._remoteStreams.push(stream);
            const event = new Event("addstream");
            event.stream = stream;
            this.dispatchEvent(event);
          });
        });
      }
    });
    const origSetRemoteDescription = window2.RTCPeerConnection.prototype.setRemoteDescription;
    window2.RTCPeerConnection.prototype.setRemoteDescription = function setRemoteDescription() {
      const pc = this;
      if (!this._onaddstreampoly) {
        this.addEventListener("track", this._onaddstreampoly = function(e4) {
          e4.streams.forEach((stream) => {
            if (!pc._remoteStreams) {
              pc._remoteStreams = [];
            }
            if (pc._remoteStreams.indexOf(stream) >= 0) {
              return;
            }
            pc._remoteStreams.push(stream);
            const event = new Event("addstream");
            event.stream = stream;
            pc.dispatchEvent(event);
          });
        });
      }
      return origSetRemoteDescription.apply(pc, arguments);
    };
  }
}
function shimCallbacksAPI(window2) {
  if (typeof window2 !== "object" || !window2.RTCPeerConnection) {
    return;
  }
  const prototype = window2.RTCPeerConnection.prototype;
  const origCreateOffer = prototype.createOffer;
  const origCreateAnswer = prototype.createAnswer;
  const setLocalDescription = prototype.setLocalDescription;
  const setRemoteDescription = prototype.setRemoteDescription;
  const addIceCandidate = prototype.addIceCandidate;
  prototype.createOffer = function createOffer(successCallback, failureCallback) {
    const options = arguments.length >= 2 ? arguments[2] : arguments[0];
    const promise = origCreateOffer.apply(this, [options]);
    if (!failureCallback) {
      return promise;
    }
    promise.then(successCallback, failureCallback);
    return Promise.resolve();
  };
  prototype.createAnswer = function createAnswer(successCallback, failureCallback) {
    const options = arguments.length >= 2 ? arguments[2] : arguments[0];
    const promise = origCreateAnswer.apply(this, [options]);
    if (!failureCallback) {
      return promise;
    }
    promise.then(successCallback, failureCallback);
    return Promise.resolve();
  };
  let withCallback = function(description, successCallback, failureCallback) {
    const promise = setLocalDescription.apply(this, [description]);
    if (!failureCallback) {
      return promise;
    }
    promise.then(successCallback, failureCallback);
    return Promise.resolve();
  };
  prototype.setLocalDescription = withCallback;
  withCallback = function(description, successCallback, failureCallback) {
    const promise = setRemoteDescription.apply(this, [description]);
    if (!failureCallback) {
      return promise;
    }
    promise.then(successCallback, failureCallback);
    return Promise.resolve();
  };
  prototype.setRemoteDescription = withCallback;
  withCallback = function(candidate, successCallback, failureCallback) {
    const promise = addIceCandidate.apply(this, [candidate]);
    if (!failureCallback) {
      return promise;
    }
    promise.then(successCallback, failureCallback);
    return Promise.resolve();
  };
  prototype.addIceCandidate = withCallback;
}
function shimGetUserMedia(window2) {
  const navigator2 = window2 && window2.navigator;
  if (navigator2.mediaDevices && navigator2.mediaDevices.getUserMedia) {
    const mediaDevices = navigator2.mediaDevices;
    const _getUserMedia = mediaDevices.getUserMedia.bind(mediaDevices);
    navigator2.mediaDevices.getUserMedia = (constraints) => {
      return _getUserMedia(shimConstraints(constraints));
    };
  }
  if (!navigator2.getUserMedia && navigator2.mediaDevices && navigator2.mediaDevices.getUserMedia) {
    navigator2.getUserMedia = (function getUserMedia(constraints, cb, errcb) {
      navigator2.mediaDevices.getUserMedia(constraints).then(cb, errcb);
    }).bind(navigator2);
  }
}
function shimConstraints(constraints) {
  if (constraints && constraints.video !== void 0) {
    return Object.assign({}, constraints, {
      video: compactObject(constraints.video)
    });
  }
  return constraints;
}
function shimRTCIceServerUrls(window2) {
  if (!window2.RTCPeerConnection) {
    return;
  }
  const OrigPeerConnection = window2.RTCPeerConnection;
  window2.RTCPeerConnection = function RTCPeerConnection2(pcConfig, pcConstraints) {
    if (pcConfig && pcConfig.iceServers) {
      const newIceServers = [];
      for (let i2 = 0; i2 < pcConfig.iceServers.length; i2++) {
        let server = pcConfig.iceServers[i2];
        if (server.urls === void 0 && server.url) {
          deprecated("RTCIceServer.url", "RTCIceServer.urls");
          server = JSON.parse(JSON.stringify(server));
          server.urls = server.url;
          delete server.url;
          newIceServers.push(server);
        } else {
          newIceServers.push(pcConfig.iceServers[i2]);
        }
      }
      pcConfig.iceServers = newIceServers;
    }
    return new OrigPeerConnection(pcConfig, pcConstraints);
  };
  window2.RTCPeerConnection.prototype = OrigPeerConnection.prototype;
  if ("generateCertificate" in OrigPeerConnection) {
    Object.defineProperty(window2.RTCPeerConnection, "generateCertificate", {
      get() {
        return OrigPeerConnection.generateCertificate;
      }
    });
  }
}
function shimTrackEventTransceiver(window2) {
  if (typeof window2 === "object" && window2.RTCTrackEvent && "receiver" in window2.RTCTrackEvent.prototype && !("transceiver" in window2.RTCTrackEvent.prototype)) {
    Object.defineProperty(window2.RTCTrackEvent.prototype, "transceiver", {
      get() {
        return {
          receiver: this.receiver
        };
      }
    });
  }
}
function shimCreateOfferLegacy(window2) {
  const origCreateOffer = window2.RTCPeerConnection.prototype.createOffer;
  window2.RTCPeerConnection.prototype.createOffer = function createOffer(offerOptions) {
    if (offerOptions) {
      if (typeof offerOptions.offerToReceiveAudio !== "undefined") {
        offerOptions.offerToReceiveAudio = !!offerOptions.offerToReceiveAudio;
      }
      const audioTransceiver = this.getTransceivers().find((transceiver) => transceiver.receiver.track.kind === "audio");
      if (offerOptions.offerToReceiveAudio === false && audioTransceiver) {
        if (audioTransceiver.direction === "sendrecv") {
          if (audioTransceiver.setDirection) {
            audioTransceiver.setDirection("sendonly");
          } else {
            audioTransceiver.direction = "sendonly";
          }
        } else if (audioTransceiver.direction === "recvonly") {
          if (audioTransceiver.setDirection) {
            audioTransceiver.setDirection("inactive");
          } else {
            audioTransceiver.direction = "inactive";
          }
        }
      } else if (offerOptions.offerToReceiveAudio === true && !audioTransceiver) {
        this.addTransceiver("audio", {
          direction: "recvonly"
        });
      }
      if (typeof offerOptions.offerToReceiveVideo !== "undefined") {
        offerOptions.offerToReceiveVideo = !!offerOptions.offerToReceiveVideo;
      }
      const videoTransceiver = this.getTransceivers().find((transceiver) => transceiver.receiver.track.kind === "video");
      if (offerOptions.offerToReceiveVideo === false && videoTransceiver) {
        if (videoTransceiver.direction === "sendrecv") {
          if (videoTransceiver.setDirection) {
            videoTransceiver.setDirection("sendonly");
          } else {
            videoTransceiver.direction = "sendonly";
          }
        } else if (videoTransceiver.direction === "recvonly") {
          if (videoTransceiver.setDirection) {
            videoTransceiver.setDirection("inactive");
          } else {
            videoTransceiver.direction = "inactive";
          }
        }
      } else if (offerOptions.offerToReceiveVideo === true && !videoTransceiver) {
        this.addTransceiver("video", {
          direction: "recvonly"
        });
      }
    }
    return origCreateOffer.apply(this, arguments);
  };
}
function shimAudioContext(window2) {
  if (typeof window2 !== "object" || window2.AudioContext) {
    return;
  }
  window2.AudioContext = window2.webkitAudioContext;
}
var safariShim = Object.freeze({ __proto__: null, shimAudioContext, shimCallbacksAPI, shimConstraints, shimCreateOfferLegacy, shimGetUserMedia, shimLocalStreamsAPI, shimRTCIceServerUrls, shimRemoteStreamsAPI, shimTrackEventTransceiver });
var sdp$1 = { exports: {} };
var hasRequiredSdp;
function requireSdp() {
  if (hasRequiredSdp) return sdp$1.exports;
  hasRequiredSdp = 1;
  (function(module) {
    const SDPUtils2 = {};
    SDPUtils2.generateIdentifier = function() {
      return Math.random().toString(36).substring(2, 12);
    };
    SDPUtils2.localCName = SDPUtils2.generateIdentifier();
    SDPUtils2.splitLines = function(blob) {
      return blob.trim().split("\n").map((line) => line.trim());
    };
    SDPUtils2.splitSections = function(blob) {
      const parts = blob.split("\nm=");
      return parts.map((part, index) => (index > 0 ? "m=" + part : part).trim() + "\r\n");
    };
    SDPUtils2.getDescription = function(blob) {
      const sections = SDPUtils2.splitSections(blob);
      return sections && sections[0];
    };
    SDPUtils2.getMediaSections = function(blob) {
      const sections = SDPUtils2.splitSections(blob);
      sections.shift();
      return sections;
    };
    SDPUtils2.matchPrefix = function(blob, prefix) {
      return SDPUtils2.splitLines(blob).filter((line) => line.indexOf(prefix) === 0);
    };
    SDPUtils2.parseCandidate = function(line) {
      let parts;
      if (line.indexOf("a=candidate:") === 0) {
        parts = line.substring(12).split(" ");
      } else {
        parts = line.substring(10).split(" ");
      }
      const candidate = {
        foundation: parts[0],
        component: {
          1: "rtp",
          2: "rtcp"
        }[parts[1]] || parts[1],
        protocol: parts[2].toLowerCase(),
        priority: parseInt(parts[3], 10),
        ip: parts[4],
        address: parts[4],
        // address is an alias for ip.
        port: parseInt(parts[5], 10),
        // skip parts[6] == 'typ'
        type: parts[7]
      };
      for (let i2 = 8; i2 < parts.length; i2 += 2) {
        switch (parts[i2]) {
          case "raddr":
            candidate.relatedAddress = parts[i2 + 1];
            break;
          case "rport":
            candidate.relatedPort = parseInt(parts[i2 + 1], 10);
            break;
          case "tcptype":
            candidate.tcpType = parts[i2 + 1];
            break;
          case "ufrag":
            candidate.ufrag = parts[i2 + 1];
            candidate.usernameFragment = parts[i2 + 1];
            break;
          default:
            if (candidate[parts[i2]] === void 0) {
              candidate[parts[i2]] = parts[i2 + 1];
            }
            break;
        }
      }
      return candidate;
    };
    SDPUtils2.writeCandidate = function(candidate) {
      const sdp2 = [];
      sdp2.push(candidate.foundation);
      const component = candidate.component;
      if (component === "rtp") {
        sdp2.push(1);
      } else if (component === "rtcp") {
        sdp2.push(2);
      } else {
        sdp2.push(component);
      }
      sdp2.push(candidate.protocol.toUpperCase());
      sdp2.push(candidate.priority);
      sdp2.push(candidate.address || candidate.ip);
      sdp2.push(candidate.port);
      const type = candidate.type;
      sdp2.push("typ");
      sdp2.push(type);
      if (type !== "host" && candidate.relatedAddress && candidate.relatedPort !== void 0) {
        sdp2.push("raddr");
        sdp2.push(candidate.relatedAddress);
        sdp2.push("rport");
        sdp2.push(candidate.relatedPort);
      }
      if (candidate.tcpType && candidate.protocol.toLowerCase() === "tcp") {
        sdp2.push("tcptype");
        sdp2.push(candidate.tcpType);
      }
      if (candidate.usernameFragment || candidate.ufrag) {
        sdp2.push("ufrag");
        sdp2.push(candidate.usernameFragment || candidate.ufrag);
      }
      return "candidate:" + sdp2.join(" ");
    };
    SDPUtils2.parseIceOptions = function(line) {
      return line.substring(14).split(" ");
    };
    SDPUtils2.parseRtpMap = function(line) {
      let parts = line.substring(9).split(" ");
      const parsed = {
        payloadType: parseInt(parts.shift(), 10)
        // was: id
      };
      parts = parts[0].split("/");
      parsed.name = parts[0];
      parsed.clockRate = parseInt(parts[1], 10);
      parsed.channels = parts.length === 3 ? parseInt(parts[2], 10) : 1;
      parsed.numChannels = parsed.channels;
      return parsed;
    };
    SDPUtils2.writeRtpMap = function(codec) {
      let pt2 = codec.payloadType;
      if (codec.preferredPayloadType !== void 0) {
        pt2 = codec.preferredPayloadType;
      }
      const channels = codec.channels || codec.numChannels || 1;
      return "a=rtpmap:" + pt2 + " " + codec.name + "/" + codec.clockRate + (channels !== 1 ? "/" + channels : "") + "\r\n";
    };
    SDPUtils2.parseExtmap = function(line) {
      const parts = line.substring(9).split(" ");
      return {
        id: parseInt(parts[0], 10),
        direction: parts[0].indexOf("/") > 0 ? parts[0].split("/")[1] : "sendrecv",
        uri: parts[1],
        attributes: parts.slice(2).join(" ")
      };
    };
    SDPUtils2.writeExtmap = function(headerExtension) {
      return "a=extmap:" + (headerExtension.id || headerExtension.preferredId) + (headerExtension.direction && headerExtension.direction !== "sendrecv" ? "/" + headerExtension.direction : "") + " " + headerExtension.uri + (headerExtension.attributes ? " " + headerExtension.attributes : "") + "\r\n";
    };
    SDPUtils2.parseFmtp = function(line) {
      const parsed = {};
      let kv;
      const parts = line.substring(line.indexOf(" ") + 1).split(";");
      for (let j2 = 0; j2 < parts.length; j2++) {
        kv = parts[j2].trim().split("=");
        parsed[kv[0].trim()] = kv[1];
      }
      return parsed;
    };
    SDPUtils2.writeFmtp = function(codec) {
      let line = "";
      let pt2 = codec.payloadType;
      if (codec.preferredPayloadType !== void 0) {
        pt2 = codec.preferredPayloadType;
      }
      if (codec.parameters && Object.keys(codec.parameters).length) {
        const params = [];
        Object.keys(codec.parameters).forEach((param) => {
          if (codec.parameters[param] !== void 0) {
            params.push(param + "=" + codec.parameters[param]);
          } else {
            params.push(param);
          }
        });
        line += "a=fmtp:" + pt2 + " " + params.join(";") + "\r\n";
      }
      return line;
    };
    SDPUtils2.parseRtcpFb = function(line) {
      const parts = line.substring(line.indexOf(" ") + 1).split(" ");
      return {
        type: parts.shift(),
        parameter: parts.join(" ")
      };
    };
    SDPUtils2.writeRtcpFb = function(codec) {
      let lines = "";
      let pt2 = codec.payloadType;
      if (codec.preferredPayloadType !== void 0) {
        pt2 = codec.preferredPayloadType;
      }
      if (codec.rtcpFeedback && codec.rtcpFeedback.length) {
        codec.rtcpFeedback.forEach((fb) => {
          lines += "a=rtcp-fb:" + pt2 + " " + fb.type + (fb.parameter && fb.parameter.length ? " " + fb.parameter : "") + "\r\n";
        });
      }
      return lines;
    };
    SDPUtils2.parseSsrcMedia = function(line) {
      const sp = line.indexOf(" ");
      const parts = {
        ssrc: parseInt(line.substring(7, sp), 10)
      };
      const colon = line.indexOf(":", sp);
      if (colon > -1) {
        parts.attribute = line.substring(sp + 1, colon);
        parts.value = line.substring(colon + 1);
      } else {
        parts.attribute = line.substring(sp + 1);
      }
      return parts;
    };
    SDPUtils2.parseSsrcGroup = function(line) {
      const parts = line.substring(13).split(" ");
      return {
        semantics: parts.shift(),
        ssrcs: parts.map((ssrc) => parseInt(ssrc, 10))
      };
    };
    SDPUtils2.getMid = function(mediaSection) {
      const mid = SDPUtils2.matchPrefix(mediaSection, "a=mid:")[0];
      if (mid) {
        return mid.substring(6);
      }
    };
    SDPUtils2.parseFingerprint = function(line) {
      const parts = line.substring(14).split(" ");
      return {
        algorithm: parts[0].toLowerCase(),
        // algorithm is case-sensitive in Edge.
        value: parts[1].toUpperCase()
        // the definition is upper-case in RFC 4572.
      };
    };
    SDPUtils2.getDtlsParameters = function(mediaSection, sessionpart) {
      const lines = SDPUtils2.matchPrefix(mediaSection + sessionpart, "a=fingerprint:");
      return {
        role: "auto",
        fingerprints: lines.map(SDPUtils2.parseFingerprint)
      };
    };
    SDPUtils2.writeDtlsParameters = function(params, setupType) {
      let sdp2 = "a=setup:" + setupType + "\r\n";
      params.fingerprints.forEach((fp) => {
        sdp2 += "a=fingerprint:" + fp.algorithm + " " + fp.value + "\r\n";
      });
      return sdp2;
    };
    SDPUtils2.parseCryptoLine = function(line) {
      const parts = line.substring(9).split(" ");
      return {
        tag: parseInt(parts[0], 10),
        cryptoSuite: parts[1],
        keyParams: parts[2],
        sessionParams: parts.slice(3)
      };
    };
    SDPUtils2.writeCryptoLine = function(parameters) {
      return "a=crypto:" + parameters.tag + " " + parameters.cryptoSuite + " " + (typeof parameters.keyParams === "object" ? SDPUtils2.writeCryptoKeyParams(parameters.keyParams) : parameters.keyParams) + (parameters.sessionParams ? " " + parameters.sessionParams.join(" ") : "") + "\r\n";
    };
    SDPUtils2.parseCryptoKeyParams = function(keyParams) {
      if (keyParams.indexOf("inline:") !== 0) {
        return null;
      }
      const parts = keyParams.substring(7).split("|");
      return {
        keyMethod: "inline",
        keySalt: parts[0],
        lifeTime: parts[1],
        mkiValue: parts[2] ? parts[2].split(":")[0] : void 0,
        mkiLength: parts[2] ? parts[2].split(":")[1] : void 0
      };
    };
    SDPUtils2.writeCryptoKeyParams = function(keyParams) {
      return keyParams.keyMethod + ":" + keyParams.keySalt + (keyParams.lifeTime ? "|" + keyParams.lifeTime : "") + (keyParams.mkiValue && keyParams.mkiLength ? "|" + keyParams.mkiValue + ":" + keyParams.mkiLength : "");
    };
    SDPUtils2.getCryptoParameters = function(mediaSection, sessionpart) {
      const lines = SDPUtils2.matchPrefix(mediaSection + sessionpart, "a=crypto:");
      return lines.map(SDPUtils2.parseCryptoLine);
    };
    SDPUtils2.getIceParameters = function(mediaSection, sessionpart) {
      const ufrag = SDPUtils2.matchPrefix(mediaSection + sessionpart, "a=ice-ufrag:")[0];
      const pwd = SDPUtils2.matchPrefix(mediaSection + sessionpart, "a=ice-pwd:")[0];
      if (!(ufrag && pwd)) {
        return null;
      }
      return {
        usernameFragment: ufrag.substring(12),
        password: pwd.substring(10)
      };
    };
    SDPUtils2.writeIceParameters = function(params) {
      let sdp2 = "a=ice-ufrag:" + params.usernameFragment + "\r\na=ice-pwd:" + params.password + "\r\n";
      if (params.iceLite) {
        sdp2 += "a=ice-lite\r\n";
      }
      return sdp2;
    };
    SDPUtils2.parseRtpParameters = function(mediaSection) {
      const description = {
        codecs: [],
        headerExtensions: [],
        fecMechanisms: [],
        rtcp: []
      };
      const lines = SDPUtils2.splitLines(mediaSection);
      const mline = lines[0].split(" ");
      description.profile = mline[2];
      for (let i2 = 3; i2 < mline.length; i2++) {
        const pt2 = mline[i2];
        const rtpmapline = SDPUtils2.matchPrefix(mediaSection, "a=rtpmap:" + pt2 + " ")[0];
        if (rtpmapline) {
          const codec = SDPUtils2.parseRtpMap(rtpmapline);
          const fmtps = SDPUtils2.matchPrefix(mediaSection, "a=fmtp:" + pt2 + " ");
          codec.parameters = fmtps.length ? SDPUtils2.parseFmtp(fmtps[0]) : {};
          codec.rtcpFeedback = SDPUtils2.matchPrefix(mediaSection, "a=rtcp-fb:" + pt2 + " ").map(SDPUtils2.parseRtcpFb);
          description.codecs.push(codec);
          switch (codec.name.toUpperCase()) {
            case "RED":
            case "ULPFEC":
              description.fecMechanisms.push(codec.name.toUpperCase());
              break;
          }
        }
      }
      SDPUtils2.matchPrefix(mediaSection, "a=extmap:").forEach((line) => {
        description.headerExtensions.push(SDPUtils2.parseExtmap(line));
      });
      const wildcardRtcpFb = SDPUtils2.matchPrefix(mediaSection, "a=rtcp-fb:* ").map(SDPUtils2.parseRtcpFb);
      description.codecs.forEach((codec) => {
        wildcardRtcpFb.forEach((fb) => {
          const duplicate = codec.rtcpFeedback.find((existingFeedback) => {
            return existingFeedback.type === fb.type && existingFeedback.parameter === fb.parameter;
          });
          if (!duplicate) {
            codec.rtcpFeedback.push(fb);
          }
        });
      });
      return description;
    };
    SDPUtils2.writeRtpDescription = function(kind, caps) {
      let sdp2 = "";
      sdp2 += "m=" + kind + " ";
      sdp2 += caps.codecs.length > 0 ? "9" : "0";
      sdp2 += " " + (caps.profile || "UDP/TLS/RTP/SAVPF") + " ";
      sdp2 += caps.codecs.map((codec) => {
        if (codec.preferredPayloadType !== void 0) {
          return codec.preferredPayloadType;
        }
        return codec.payloadType;
      }).join(" ") + "\r\n";
      sdp2 += "c=IN IP4 0.0.0.0\r\n";
      sdp2 += "a=rtcp:9 IN IP4 0.0.0.0\r\n";
      caps.codecs.forEach((codec) => {
        sdp2 += SDPUtils2.writeRtpMap(codec);
        sdp2 += SDPUtils2.writeFmtp(codec);
        sdp2 += SDPUtils2.writeRtcpFb(codec);
      });
      let maxptime = 0;
      caps.codecs.forEach((codec) => {
        if (codec.maxptime > maxptime) {
          maxptime = codec.maxptime;
        }
      });
      if (maxptime > 0) {
        sdp2 += "a=maxptime:" + maxptime + "\r\n";
      }
      if (caps.headerExtensions) {
        caps.headerExtensions.forEach((extension) => {
          sdp2 += SDPUtils2.writeExtmap(extension);
        });
      }
      return sdp2;
    };
    SDPUtils2.parseRtpEncodingParameters = function(mediaSection) {
      const encodingParameters = [];
      const description = SDPUtils2.parseRtpParameters(mediaSection);
      const hasRed = description.fecMechanisms.indexOf("RED") !== -1;
      const hasUlpfec = description.fecMechanisms.indexOf("ULPFEC") !== -1;
      const ssrcs = SDPUtils2.matchPrefix(mediaSection, "a=ssrc:").map((line) => SDPUtils2.parseSsrcMedia(line)).filter((parts) => parts.attribute === "cname");
      const primarySsrc = ssrcs.length > 0 && ssrcs[0].ssrc;
      let secondarySsrc;
      const flows = SDPUtils2.matchPrefix(mediaSection, "a=ssrc-group:FID").map((line) => {
        const parts = line.substring(17).split(" ");
        return parts.map((part) => parseInt(part, 10));
      });
      if (flows.length > 0 && flows[0].length > 1 && flows[0][0] === primarySsrc) {
        secondarySsrc = flows[0][1];
      }
      description.codecs.forEach((codec) => {
        if (codec.name.toUpperCase() === "RTX" && codec.parameters.apt) {
          let encParam = {
            ssrc: primarySsrc,
            codecPayloadType: parseInt(codec.parameters.apt, 10)
          };
          if (primarySsrc && secondarySsrc) {
            encParam.rtx = {
              ssrc: secondarySsrc
            };
          }
          encodingParameters.push(encParam);
          if (hasRed) {
            encParam = JSON.parse(JSON.stringify(encParam));
            encParam.fec = {
              ssrc: primarySsrc,
              mechanism: hasUlpfec ? "red+ulpfec" : "red"
            };
            encodingParameters.push(encParam);
          }
        }
      });
      if (encodingParameters.length === 0 && primarySsrc) {
        encodingParameters.push({
          ssrc: primarySsrc
        });
      }
      let bandwidth = SDPUtils2.matchPrefix(mediaSection, "b=");
      if (bandwidth.length) {
        if (bandwidth[0].indexOf("b=TIAS:") === 0) {
          bandwidth = parseInt(bandwidth[0].substring(7), 10);
        } else if (bandwidth[0].indexOf("b=AS:") === 0) {
          bandwidth = parseInt(bandwidth[0].substring(5), 10) * 1e3 * 0.95 - 50 * 40 * 8;
        } else {
          bandwidth = void 0;
        }
        encodingParameters.forEach((params) => {
          params.maxBitrate = bandwidth;
        });
      }
      return encodingParameters;
    };
    SDPUtils2.parseRtcpParameters = function(mediaSection) {
      const rtcpParameters = {};
      const remoteSsrc = SDPUtils2.matchPrefix(mediaSection, "a=ssrc:").map((line) => SDPUtils2.parseSsrcMedia(line)).filter((obj) => obj.attribute === "cname")[0];
      if (remoteSsrc) {
        rtcpParameters.cname = remoteSsrc.value;
        rtcpParameters.ssrc = remoteSsrc.ssrc;
      }
      const rsize = SDPUtils2.matchPrefix(mediaSection, "a=rtcp-rsize");
      rtcpParameters.reducedSize = rsize.length > 0;
      rtcpParameters.compound = rsize.length === 0;
      const mux = SDPUtils2.matchPrefix(mediaSection, "a=rtcp-mux");
      rtcpParameters.mux = mux.length > 0;
      return rtcpParameters;
    };
    SDPUtils2.writeRtcpParameters = function(rtcpParameters) {
      let sdp2 = "";
      if (rtcpParameters.reducedSize) {
        sdp2 += "a=rtcp-rsize\r\n";
      }
      if (rtcpParameters.mux) {
        sdp2 += "a=rtcp-mux\r\n";
      }
      if (rtcpParameters.ssrc !== void 0 && rtcpParameters.cname) {
        sdp2 += "a=ssrc:" + rtcpParameters.ssrc + " cname:" + rtcpParameters.cname + "\r\n";
      }
      return sdp2;
    };
    SDPUtils2.parseMsid = function(mediaSection) {
      let parts;
      const spec = SDPUtils2.matchPrefix(mediaSection, "a=msid:");
      if (spec.length === 1) {
        parts = spec[0].substring(7).split(" ");
        return {
          stream: parts[0],
          track: parts[1]
        };
      }
      const planB = SDPUtils2.matchPrefix(mediaSection, "a=ssrc:").map((line) => SDPUtils2.parseSsrcMedia(line)).filter((msidParts) => msidParts.attribute === "msid");
      if (planB.length > 0) {
        parts = planB[0].value.split(" ");
        return {
          stream: parts[0],
          track: parts[1]
        };
      }
    };
    SDPUtils2.parseSctpDescription = function(mediaSection) {
      const mline = SDPUtils2.parseMLine(mediaSection);
      const maxSizeLine = SDPUtils2.matchPrefix(mediaSection, "a=max-message-size:");
      let maxMessageSize;
      if (maxSizeLine.length > 0) {
        maxMessageSize = parseInt(maxSizeLine[0].substring(19), 10);
      }
      if (isNaN(maxMessageSize)) {
        maxMessageSize = 65536;
      }
      const sctpPort = SDPUtils2.matchPrefix(mediaSection, "a=sctp-port:");
      if (sctpPort.length > 0) {
        return {
          port: parseInt(sctpPort[0].substring(12), 10),
          protocol: mline.fmt,
          maxMessageSize
        };
      }
      const sctpMapLines = SDPUtils2.matchPrefix(mediaSection, "a=sctpmap:");
      if (sctpMapLines.length > 0) {
        const parts = sctpMapLines[0].substring(10).split(" ");
        return {
          port: parseInt(parts[0], 10),
          protocol: parts[1],
          maxMessageSize
        };
      }
    };
    SDPUtils2.writeSctpDescription = function(media, sctp) {
      let output = [];
      if (media.protocol !== "DTLS/SCTP") {
        output = ["m=" + media.kind + " 9 " + media.protocol + " " + sctp.protocol + "\r\n", "c=IN IP4 0.0.0.0\r\n", "a=sctp-port:" + sctp.port + "\r\n"];
      } else {
        output = ["m=" + media.kind + " 9 " + media.protocol + " " + sctp.port + "\r\n", "c=IN IP4 0.0.0.0\r\n", "a=sctpmap:" + sctp.port + " " + sctp.protocol + " 65535\r\n"];
      }
      if (sctp.maxMessageSize !== void 0) {
        output.push("a=max-message-size:" + sctp.maxMessageSize + "\r\n");
      }
      return output.join("");
    };
    SDPUtils2.generateSessionId = function() {
      return Math.random().toString().substr(2, 22);
    };
    SDPUtils2.writeSessionBoilerplate = function(sessId, sessVer, sessUser) {
      let sessionId;
      const version2 = sessVer !== void 0 ? sessVer : 2;
      if (sessId) {
        sessionId = sessId;
      } else {
        sessionId = SDPUtils2.generateSessionId();
      }
      const user = sessUser || "thisisadapterortc";
      return "v=0\r\no=" + user + " " + sessionId + " " + version2 + " IN IP4 127.0.0.1\r\ns=-\r\nt=0 0\r\n";
    };
    SDPUtils2.getDirection = function(mediaSection, sessionpart) {
      const lines = SDPUtils2.splitLines(mediaSection);
      for (let i2 = 0; i2 < lines.length; i2++) {
        switch (lines[i2]) {
          case "a=sendrecv":
          case "a=sendonly":
          case "a=recvonly":
          case "a=inactive":
            return lines[i2].substring(2);
        }
      }
      if (sessionpart) {
        return SDPUtils2.getDirection(sessionpart);
      }
      return "sendrecv";
    };
    SDPUtils2.getKind = function(mediaSection) {
      const lines = SDPUtils2.splitLines(mediaSection);
      const mline = lines[0].split(" ");
      return mline[0].substring(2);
    };
    SDPUtils2.isRejected = function(mediaSection) {
      return mediaSection.split(" ", 2)[1] === "0";
    };
    SDPUtils2.parseMLine = function(mediaSection) {
      const lines = SDPUtils2.splitLines(mediaSection);
      const parts = lines[0].substring(2).split(" ");
      return {
        kind: parts[0],
        port: parseInt(parts[1], 10),
        protocol: parts[2],
        fmt: parts.slice(3).join(" ")
      };
    };
    SDPUtils2.parseOLine = function(mediaSection) {
      const line = SDPUtils2.matchPrefix(mediaSection, "o=")[0];
      const parts = line.substring(2).split(" ");
      return {
        username: parts[0],
        sessionId: parts[1],
        sessionVersion: parseInt(parts[2], 10),
        netType: parts[3],
        addressType: parts[4],
        address: parts[5]
      };
    };
    SDPUtils2.isValidSDP = function(blob) {
      if (typeof blob !== "string" || blob.length === 0) {
        return false;
      }
      const lines = SDPUtils2.splitLines(blob);
      for (let i2 = 0; i2 < lines.length; i2++) {
        if (lines[i2].length < 2 || lines[i2].charAt(1) !== "=") {
          return false;
        }
      }
      return true;
    };
    {
      module.exports = SDPUtils2;
    }
  })(sdp$1);
  return sdp$1.exports;
}
var sdpExports = requireSdp();
var SDPUtils = getDefaultExportFromCjs(sdpExports);
var sdp = _mergeNamespaces({ __proto__: null, default: SDPUtils }, [sdpExports]);
function shimRTCIceCandidate(window2) {
  if (!window2.RTCIceCandidate || window2.RTCIceCandidate && "foundation" in window2.RTCIceCandidate.prototype) {
    return;
  }
  const NativeRTCIceCandidate = window2.RTCIceCandidate;
  window2.RTCIceCandidate = function RTCIceCandidate2(args) {
    if (typeof args === "object" && args.candidate && args.candidate.indexOf("a=") === 0) {
      args = JSON.parse(JSON.stringify(args));
      args.candidate = args.candidate.substring(2);
    }
    if (args.candidate && args.candidate.length) {
      const nativeCandidate = new NativeRTCIceCandidate(args);
      const parsedCandidate = SDPUtils.parseCandidate(args.candidate);
      for (const key in parsedCandidate) {
        if (!(key in nativeCandidate)) {
          Object.defineProperty(nativeCandidate, key, {
            value: parsedCandidate[key]
          });
        }
      }
      nativeCandidate.toJSON = function toJSON() {
        return {
          candidate: nativeCandidate.candidate,
          sdpMid: nativeCandidate.sdpMid,
          sdpMLineIndex: nativeCandidate.sdpMLineIndex,
          usernameFragment: nativeCandidate.usernameFragment
        };
      };
      return nativeCandidate;
    }
    return new NativeRTCIceCandidate(args);
  };
  window2.RTCIceCandidate.prototype = NativeRTCIceCandidate.prototype;
  wrapPeerConnectionEvent(window2, "icecandidate", (e4) => {
    if (e4.candidate) {
      Object.defineProperty(e4, "candidate", {
        value: new window2.RTCIceCandidate(e4.candidate),
        writable: "false"
      });
    }
    return e4;
  });
}
function shimRTCIceCandidateRelayProtocol(window2) {
  if (!window2.RTCIceCandidate || window2.RTCIceCandidate && "relayProtocol" in window2.RTCIceCandidate.prototype) {
    return;
  }
  wrapPeerConnectionEvent(window2, "icecandidate", (e4) => {
    if (e4.candidate) {
      const parsedCandidate = SDPUtils.parseCandidate(e4.candidate.candidate);
      if (parsedCandidate.type === "relay") {
        e4.candidate.relayProtocol = {
          0: "tls",
          1: "tcp",
          2: "udp"
        }[parsedCandidate.priority >> 24];
      }
    }
    return e4;
  });
}
function shimMaxMessageSize(window2, browserDetails2) {
  if (!window2.RTCPeerConnection) {
    return;
  }
  if (browserDetails2.browser === "chrome" && browserDetails2.version > 102) {
    return;
  }
  if (browserDetails2.browser === "firefox" && browserDetails2.version >= 113) {
    return;
  }
  if (!("sctp" in window2.RTCPeerConnection.prototype)) {
    Object.defineProperty(window2.RTCPeerConnection.prototype, "sctp", {
      get() {
        return typeof this._sctp === "undefined" ? null : this._sctp;
      }
    });
  }
  const sctpInDescription = function(description) {
    if (!description || !description.sdp) {
      return false;
    }
    const sections = SDPUtils.splitSections(description.sdp);
    sections.shift();
    return sections.some((mediaSection) => {
      const mLine = SDPUtils.parseMLine(mediaSection);
      return mLine && mLine.kind === "application" && mLine.protocol.indexOf("SCTP") !== -1;
    });
  };
  const getRemoteFirefoxVersion = function(description) {
    const match = description.sdp.match(/mozilla...THIS_IS_SDPARTA-(\d+)/);
    if (match === null || match.length < 2) {
      return -1;
    }
    const version2 = parseInt(match[1], 10);
    return version2 !== version2 ? -1 : version2;
  };
  const getCanSendMaxMessageSize = function(remoteIsFirefox) {
    let canSendMaxMessageSize = 65536;
    if (browserDetails2.browser === "firefox") {
      if (browserDetails2.version < 57) {
        if (remoteIsFirefox === -1) {
          canSendMaxMessageSize = 16384;
        } else {
          canSendMaxMessageSize = 2147483637;
        }
      } else if (browserDetails2.version < 60) {
        canSendMaxMessageSize = browserDetails2.version === 57 ? 65535 : 65536;
      } else {
        canSendMaxMessageSize = 2147483637;
      }
    }
    return canSendMaxMessageSize;
  };
  const getMaxMessageSize = function(description, remoteIsFirefox) {
    let maxMessageSize = 65536;
    if (browserDetails2.browser === "firefox" && browserDetails2.version === 57) {
      maxMessageSize = 65535;
    }
    const match = SDPUtils.matchPrefix(description.sdp, "a=max-message-size:");
    if (match.length > 0) {
      maxMessageSize = parseInt(match[0].substring(19), 10);
    } else if (browserDetails2.browser === "firefox" && remoteIsFirefox !== -1) {
      maxMessageSize = 2147483637;
    }
    return maxMessageSize;
  };
  const origSetRemoteDescription = window2.RTCPeerConnection.prototype.setRemoteDescription;
  window2.RTCPeerConnection.prototype.setRemoteDescription = function setRemoteDescription() {
    this._sctp = null;
    if (browserDetails2.browser === "chrome" && browserDetails2.version >= 76) {
      const _this$getConfiguratio = this.getConfiguration(), sdpSemantics = _this$getConfiguratio.sdpSemantics;
      if (sdpSemantics === "plan-b") {
        Object.defineProperty(this, "sctp", {
          get() {
            return typeof this._sctp === "undefined" ? null : this._sctp;
          },
          enumerable: true,
          configurable: true
        });
      }
    }
    if (sctpInDescription(arguments[0])) {
      const isFirefox = getRemoteFirefoxVersion(arguments[0]);
      const canSendMMS = getCanSendMaxMessageSize(isFirefox);
      const remoteMMS = getMaxMessageSize(arguments[0], isFirefox);
      let maxMessageSize;
      if (canSendMMS === 0 && remoteMMS === 0) {
        maxMessageSize = Number.POSITIVE_INFINITY;
      } else if (canSendMMS === 0 || remoteMMS === 0) {
        maxMessageSize = Math.max(canSendMMS, remoteMMS);
      } else {
        maxMessageSize = Math.min(canSendMMS, remoteMMS);
      }
      const sctp = {};
      Object.defineProperty(sctp, "maxMessageSize", {
        get() {
          return maxMessageSize;
        }
      });
      this._sctp = sctp;
    }
    return origSetRemoteDescription.apply(this, arguments);
  };
}
function shimSendThrowTypeError(window2, browserDetails2) {
  if (!(window2.RTCPeerConnection && "createDataChannel" in window2.RTCPeerConnection.prototype)) {
    return;
  }
  if (browserDetails2.browser === "chrome" && browserDetails2.version >= 149) {
    return;
  }
  if (browserDetails2.browser === "firefox" && browserDetails2.version > 60) {
    return;
  }
  function wrapDcSend(dc, pc) {
    const origDataChannelSend = dc.send;
    dc.send = function send() {
      const data = arguments[0];
      const length = data.length || data.size || data.byteLength;
      if (dc.readyState === "open" && pc.sctp && length > pc.sctp.maxMessageSize) {
        throw new TypeError("Message too large (can send a maximum of " + pc.sctp.maxMessageSize + " bytes)");
      }
      return origDataChannelSend.apply(dc, arguments);
    };
  }
  const origCreateDataChannel = window2.RTCPeerConnection.prototype.createDataChannel;
  window2.RTCPeerConnection.prototype.createDataChannel = function createDataChannel() {
    const dataChannel = origCreateDataChannel.apply(this, arguments);
    wrapDcSend(dataChannel, this);
    return dataChannel;
  };
  wrapPeerConnectionEvent(window2, "datachannel", (e4) => {
    wrapDcSend(e4.channel, e4.target);
    return e4;
  });
}
function shimConnectionState(window2) {
  if (!window2.RTCPeerConnection || "connectionState" in window2.RTCPeerConnection.prototype) {
    return;
  }
  const proto = window2.RTCPeerConnection.prototype;
  Object.defineProperty(proto, "connectionState", {
    get() {
      return {
        completed: "connected",
        checking: "connecting"
      }[this.iceConnectionState] || this.iceConnectionState;
    },
    enumerable: true,
    configurable: true
  });
  Object.defineProperty(proto, "onconnectionstatechange", {
    get() {
      return this._onconnectionstatechange || null;
    },
    set(cb) {
      if (this._onconnectionstatechange) {
        this.removeEventListener("connectionstatechange", this._onconnectionstatechange);
        delete this._onconnectionstatechange;
      }
      if (cb) {
        this.addEventListener("connectionstatechange", this._onconnectionstatechange = cb);
      }
    },
    enumerable: true,
    configurable: true
  });
  ["setLocalDescription", "setRemoteDescription"].forEach((method) => {
    const origMethod = proto[method];
    proto[method] = function() {
      if (!this._connectionstatechangepoly) {
        this._connectionstatechangepoly = (e4) => {
          const pc = e4.target;
          if (pc._lastConnectionState !== pc.connectionState) {
            pc._lastConnectionState = pc.connectionState;
            const newEvent = new Event("connectionstatechange", e4);
            pc.dispatchEvent(newEvent);
          }
          return e4;
        };
        this.addEventListener("iceconnectionstatechange", this._connectionstatechangepoly);
      }
      return origMethod.apply(this, arguments);
    };
  });
}
function removeExtmapAllowMixed(window2, browserDetails2) {
  if (!window2.RTCPeerConnection) {
    return;
  }
  if (browserDetails2.browser === "chrome" && browserDetails2.version >= 71) {
    return;
  }
  if (browserDetails2.browser === "safari" && browserDetails2._safariVersion >= 13.1) {
    return;
  }
  const nativeSRD = window2.RTCPeerConnection.prototype.setRemoteDescription;
  window2.RTCPeerConnection.prototype.setRemoteDescription = function setRemoteDescription(desc) {
    if (desc && desc.sdp && desc.sdp.indexOf("\na=extmap-allow-mixed") !== -1) {
      const sdp2 = desc.sdp.split("\n").filter((line) => {
        return line.trim() !== "a=extmap-allow-mixed";
      }).join("\n");
      if (window2.RTCSessionDescription && desc instanceof window2.RTCSessionDescription) {
        arguments[0] = new window2.RTCSessionDescription({
          type: desc.type,
          sdp: sdp2
        });
      } else {
        desc.sdp = sdp2;
      }
    }
    return nativeSRD.apply(this, arguments);
  };
}
function shimAddIceCandidateNullOrEmpty(window2, browserDetails2) {
  if (!(window2.RTCPeerConnection && window2.RTCPeerConnection.prototype)) {
    return;
  }
  const nativeAddIceCandidate = window2.RTCPeerConnection.prototype.addIceCandidate;
  if (!nativeAddIceCandidate || nativeAddIceCandidate.length === 0) {
    return;
  }
  window2.RTCPeerConnection.prototype.addIceCandidate = function addIceCandidate() {
    if (!arguments[0]) {
      if (arguments[1]) {
        arguments[1].apply(null);
      }
      return Promise.resolve();
    }
    if ((browserDetails2.browser === "chrome" && browserDetails2.version < 78 || browserDetails2.browser === "firefox" && browserDetails2.version < 68 || browserDetails2.browser === "safari") && arguments[0] && arguments[0].candidate === "") {
      return Promise.resolve();
    }
    return nativeAddIceCandidate.apply(this, arguments);
  };
}
function shimParameterlessSetLocalDescription(window2, browserDetails2) {
  if (!(window2.RTCPeerConnection && window2.RTCPeerConnection.prototype)) {
    return;
  }
  const nativeSetLocalDescription = window2.RTCPeerConnection.prototype.setLocalDescription;
  if (!nativeSetLocalDescription || nativeSetLocalDescription.length === 0) {
    return;
  }
  window2.RTCPeerConnection.prototype.setLocalDescription = function setLocalDescription() {
    let desc = arguments[0] || {};
    if (typeof desc !== "object" || desc.type && desc.sdp) {
      return nativeSetLocalDescription.apply(this, arguments);
    }
    desc = {
      type: desc.type,
      sdp: desc.sdp
    };
    if (!desc.type) {
      switch (this.signalingState) {
        case "stable":
        case "have-local-offer":
        case "have-remote-pranswer":
          desc.type = "offer";
          break;
        default:
          desc.type = "answer";
          break;
      }
    }
    if (desc.sdp || desc.type !== "offer" && desc.type !== "answer") {
      return nativeSetLocalDescription.apply(this, [desc]);
    }
    const func = desc.type === "offer" ? this.createOffer : this.createAnswer;
    return func.apply(this).then((d) => nativeSetLocalDescription.apply(this, [d]));
  };
}
var commonShim = Object.freeze({ __proto__: null, removeExtmapAllowMixed, shimAddIceCandidateNullOrEmpty, shimConnectionState, shimMaxMessageSize, shimParameterlessSetLocalDescription, shimRTCIceCandidate, shimRTCIceCandidateRelayProtocol, shimSendThrowTypeError });
function adapterFactory() {
  let _ref = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : {}, window2 = _ref.window;
  let options = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : {
    shimChrome: true,
    shimFirefox: true,
    shimSafari: true
  };
  const logging2 = log$5;
  const browserDetails2 = detectBrowser(window2);
  const adapter = {
    browserDetails: browserDetails2,
    commonShim,
    extractVersion,
    disableLog,
    disableWarnings,
    // Expose sdp as a convenience. For production apps include directly.
    sdp
  };
  switch (browserDetails2.browser) {
    case "chrome":
      if (!chromeShim || !shimPeerConnection$1 || !options.shimChrome) {
        logging2("Chrome shim is not included in this adapter release.");
        return adapter;
      }
      if (browserDetails2.version === null) {
        logging2("Chrome shim can not determine version, not shimming.");
        return adapter;
      }
      logging2("adapter.js shimming chrome.");
      adapter.browserShim = chromeShim;
      shimAddIceCandidateNullOrEmpty(window2, browserDetails2);
      shimParameterlessSetLocalDescription(window2);
      shimGetUserMedia$2(window2, browserDetails2);
      shimMediaStream(window2);
      shimPeerConnection$1(window2, browserDetails2);
      shimOnTrack$1(window2, browserDetails2);
      shimAddTrackRemoveTrack(window2, browserDetails2);
      shimGetSendersWithDtmf(window2);
      shimSenderReceiverGetStats(window2, browserDetails2);
      fixNegotiationNeeded(window2, browserDetails2);
      shimRTCIceCandidate(window2);
      shimRTCIceCandidateRelayProtocol(window2);
      shimConnectionState(window2);
      shimMaxMessageSize(window2, browserDetails2);
      shimSendThrowTypeError(window2, browserDetails2);
      removeExtmapAllowMixed(window2, browserDetails2);
      break;
    case "firefox":
      if (!firefoxShim || !shimPeerConnection || !options.shimFirefox) {
        logging2("Firefox shim is not included in this adapter release.");
        return adapter;
      }
      logging2("adapter.js shimming firefox.");
      adapter.browserShim = firefoxShim;
      shimAddIceCandidateNullOrEmpty(window2, browserDetails2);
      shimParameterlessSetLocalDescription(window2);
      shimGetUserMedia$1(window2, browserDetails2);
      shimPeerConnection(window2, browserDetails2);
      shimGetStats(window2, browserDetails2);
      shimOnTrack(window2);
      shimRemoveStream(window2);
      shimSenderGetStats(window2);
      shimReceiverGetStats(window2);
      shimRTCDataChannel(window2);
      shimAddTransceiver(window2, browserDetails2);
      shimGetParameters(window2, browserDetails2);
      shimCreateOffer(window2, browserDetails2);
      shimCreateAnswer(window2, browserDetails2);
      shimRTCIceCandidate(window2);
      shimConnectionState(window2);
      shimMaxMessageSize(window2, browserDetails2);
      shimSendThrowTypeError(window2, browserDetails2);
      break;
    case "safari":
      if (!safariShim || !options.shimSafari) {
        logging2("Safari shim is not included in this adapter release.");
        return adapter;
      }
      logging2("adapter.js shimming safari.");
      adapter.browserShim = safariShim;
      shimAddIceCandidateNullOrEmpty(window2, browserDetails2);
      shimParameterlessSetLocalDescription(window2);
      shimRTCIceServerUrls(window2);
      shimCreateOfferLegacy(window2);
      shimCallbacksAPI(window2);
      shimLocalStreamsAPI(window2);
      shimRemoteStreamsAPI(window2);
      shimTrackEventTransceiver(window2);
      shimGetUserMedia(window2);
      shimAudioContext(window2);
      shimRTCIceCandidate(window2);
      shimRTCIceCandidateRelayProtocol(window2);
      shimMaxMessageSize(window2, browserDetails2);
      shimSendThrowTypeError(window2, browserDetails2);
      removeExtmapAllowMixed(window2, browserDetails2);
      break;
    default:
      logging2("Unsupported browser!");
      break;
  }
  return adapter;
}
adapterFactory({
  window: typeof window === "undefined" ? void 0 : window
});
var _a;
var _b;
var TypedPromise = class extends (_b = Promise) {
  // eslint-disable-next-line @typescript-eslint/no-useless-constructor
  constructor(executor) {
    super(executor);
  }
  catch(onrejected) {
    return super.catch(onrejected);
  }
  static reject(reason) {
    return super.reject(reason);
  }
  static all(values) {
    return super.all(values);
  }
  static race(values) {
    return super.race(values);
  }
};
_a = TypedPromise;
TypedPromise.resolve = (value) => {
  return Reflect.get(_b, "resolve", _a).call(_a, value);
};
var commonVersionIdentifier = /version\/(\d+(\.?_?\d+)+)/i;
var browserDetails;
function getBrowser(userAgent) {
  let force = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : true;
  if (typeof userAgent === "undefined" && typeof navigator === "undefined") {
    return;
  }
  const ua2 = (userAgent !== null && userAgent !== void 0 ? userAgent : navigator.userAgent).toLowerCase();
  if (browserDetails === void 0 || force) {
    const browser = browsersList.find((_ref) => {
      let test = _ref.test;
      return test.test(ua2);
    });
    browserDetails = browser === null || browser === void 0 ? void 0 : browser.describe(ua2);
  }
  return browserDetails;
}
var browsersList = [
  {
    test: /firefox|iceweasel|fxios/i,
    describe(ua2) {
      const browser = {
        name: "Firefox",
        version: getMatch(/(?:firefox|iceweasel|fxios)[\s/](\d+(\.?_?\d+)+)/i, ua2),
        os: ua2.toLowerCase().includes("fxios") ? "iOS" : void 0,
        osVersion: getOSVersion(ua2)
      };
      return browser;
    }
  },
  {
    test: /chrom|crios|crmo/i,
    describe(ua2) {
      const browser = {
        name: "Chrome",
        version: getMatch(/(?:chrome|chromium|crios|crmo)\/(\d+(\.?_?\d+)+)/i, ua2),
        os: ua2.toLowerCase().includes("crios") ? "iOS" : void 0,
        osVersion: getOSVersion(ua2)
      };
      return browser;
    }
  },
  /* Safari */
  {
    test: /safari|applewebkit/i,
    describe(ua2) {
      const browser = {
        name: "Safari",
        version: getMatch(commonVersionIdentifier, ua2),
        os: ua2.includes("mobile/") ? "iOS" : "macOS",
        osVersion: getOSVersion(ua2)
      };
      return browser;
    }
  }
];
function getMatch(exp, ua2) {
  let id = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : 1;
  const match = ua2.match(exp);
  return match && match.length >= id && match[id] || "";
}
function getOSVersion(ua2) {
  return ua2.includes("mac os") ? getMatch(/\(.+?(\d+_\d+(:?_\d+)?)/, ua2, 1).replace(/_/g, ".") : void 0;
}
var version$1 = "2.21.0";
var version = version$1;
var protocolVersion = 17;
var CLIENT_PROTOCOL_DEFAULT = 0;
var CLIENT_PROTOCOL_DATA_STREAM_RPC = 1;
var CLIENT_PROTOCOL_DATA_STREAM_V2 = 2;
var clientProtocol = CLIENT_PROTOCOL_DATA_STREAM_V2;
var LivekitError = class extends Error {
  constructor(code, message, options) {
    super(message || "an error has occurred");
    this.name = "LiveKitError";
    this.code = code;
    if (typeof (options === null || options === void 0 ? void 0 : options.cause) !== "undefined") {
      this.cause = options === null || options === void 0 ? void 0 : options.cause;
    }
  }
};
var LivekitReasonedError = class extends LivekitError {
};
var ConnectionErrorReason;
(function(ConnectionErrorReason2) {
  ConnectionErrorReason2[ConnectionErrorReason2["NotAllowed"] = 0] = "NotAllowed";
  ConnectionErrorReason2[ConnectionErrorReason2["ServerUnreachable"] = 1] = "ServerUnreachable";
  ConnectionErrorReason2[ConnectionErrorReason2["InternalError"] = 2] = "InternalError";
  ConnectionErrorReason2[ConnectionErrorReason2["Cancelled"] = 3] = "Cancelled";
  ConnectionErrorReason2[ConnectionErrorReason2["LeaveRequest"] = 4] = "LeaveRequest";
  ConnectionErrorReason2[ConnectionErrorReason2["Timeout"] = 5] = "Timeout";
  ConnectionErrorReason2[ConnectionErrorReason2["WebSocket"] = 6] = "WebSocket";
  ConnectionErrorReason2[ConnectionErrorReason2["ServiceNotFound"] = 7] = "ServiceNotFound";
})(ConnectionErrorReason || (ConnectionErrorReason = {}));
var ConnectionError = class _ConnectionError extends LivekitReasonedError {
  constructor(message, reason, status, context) {
    super(1, message);
    this.name = "ConnectionError";
    this.status = status;
    this.reason = reason;
    this.context = context;
    this.reasonName = ConnectionErrorReason[reason];
  }
  static notAllowed(message, status, context) {
    return new _ConnectionError(message, ConnectionErrorReason.NotAllowed, status, context);
  }
  static timeout(message) {
    return new _ConnectionError(message, ConnectionErrorReason.Timeout);
  }
  static leaveRequest(message, context) {
    return new _ConnectionError(message, ConnectionErrorReason.LeaveRequest, void 0, context);
  }
  static internal(message, context) {
    return new _ConnectionError(message, ConnectionErrorReason.InternalError, void 0, context);
  }
  static cancelled(message) {
    return new _ConnectionError(message, ConnectionErrorReason.Cancelled);
  }
  static serverUnreachable(message, status) {
    return new _ConnectionError(message, ConnectionErrorReason.ServerUnreachable, status);
  }
  static websocket(message, status, reason) {
    return new _ConnectionError(message, ConnectionErrorReason.WebSocket, status, reason);
  }
  static serviceNotFound(message, serviceName) {
    return new _ConnectionError(message, ConnectionErrorReason.ServiceNotFound, void 0, serviceName);
  }
};
var DeviceUnsupportedError = class extends LivekitError {
  constructor(message) {
    super(21, message !== null && message !== void 0 ? message : "device is unsupported");
    this.name = "DeviceUnsupportedError";
  }
};
var TrackInvalidError = class extends LivekitError {
  constructor(message) {
    super(20, message !== null && message !== void 0 ? message : "track is invalid");
    this.name = "TrackInvalidError";
  }
};
var UnsupportedServer = class extends LivekitError {
  constructor(message) {
    super(10, message || "unsupported server");
    this.name = "UnsupportedServer";
  }
};
var UnexpectedConnectionState = class extends LivekitError {
  constructor(message) {
    super(12, message || "unexpected connection state");
    this.name = "UnexpectedConnectionState";
  }
};
var NegotiationError = class extends LivekitError {
  constructor(message) {
    super(13, message || "unable to negotiate");
    this.name = "NegotiationError";
  }
};
var PublishDataError = class extends LivekitError {
  constructor(message) {
    super(14, message || "unable to publish data");
    this.name = "PublishDataError";
  }
};
var PublishTrackError = class extends LivekitError {
  constructor(message, status) {
    super(15, message);
    this.name = "PublishTrackError";
    this.status = status;
  }
};
var SignalRequestError = class extends LivekitReasonedError {
  constructor(message, reason) {
    super(15, message);
    this.name = "SignalRequestError";
    this.reason = reason;
    this.reasonName = typeof reason === "string" ? reason : RequestResponse_Reason[reason];
  }
};
var DataStreamErrorReason;
(function(DataStreamErrorReason2) {
  DataStreamErrorReason2[DataStreamErrorReason2["AlreadyOpened"] = 0] = "AlreadyOpened";
  DataStreamErrorReason2[DataStreamErrorReason2["AbnormalEnd"] = 1] = "AbnormalEnd";
  DataStreamErrorReason2[DataStreamErrorReason2["DecodeFailed"] = 2] = "DecodeFailed";
  DataStreamErrorReason2[DataStreamErrorReason2["LengthExceeded"] = 3] = "LengthExceeded";
  DataStreamErrorReason2[DataStreamErrorReason2["Incomplete"] = 4] = "Incomplete";
  DataStreamErrorReason2[DataStreamErrorReason2["HandlerAlreadyRegistered"] = 7] = "HandlerAlreadyRegistered";
  DataStreamErrorReason2[DataStreamErrorReason2["EncryptionTypeMismatch"] = 8] = "EncryptionTypeMismatch";
  DataStreamErrorReason2[DataStreamErrorReason2["HeaderTooLarge"] = 9] = "HeaderTooLarge";
  DataStreamErrorReason2[DataStreamErrorReason2["PayloadTooLarge"] = 10] = "PayloadTooLarge";
})(DataStreamErrorReason || (DataStreamErrorReason = {}));
var DataStreamError = class extends LivekitReasonedError {
  constructor(message, reason) {
    super(16, message);
    this.name = "DataStreamError";
    this.reason = reason;
    this.reasonName = DataStreamErrorReason[reason];
  }
};
var SignalReconnectError = class extends LivekitError {
  constructor(message) {
    super(18, message);
    this.name = "SignalReconnectError";
  }
};
var MediaDeviceFailure;
(function(MediaDeviceFailure2) {
  MediaDeviceFailure2["PermissionDenied"] = "PermissionDenied";
  MediaDeviceFailure2["NotFound"] = "NotFound";
  MediaDeviceFailure2["DeviceInUse"] = "DeviceInUse";
  MediaDeviceFailure2["Other"] = "Other";
})(MediaDeviceFailure || (MediaDeviceFailure = {}));
(function(MediaDeviceFailure2) {
  function getFailure(error) {
    if (error && "name" in error) {
      if (error.name === "NotFoundError" || error.name === "DevicesNotFoundError") {
        return MediaDeviceFailure2.NotFound;
      }
      if (error.name === "NotAllowedError" || error.name === "PermissionDeniedError") {
        return MediaDeviceFailure2.PermissionDenied;
      }
      if (error.name === "NotReadableError" || error.name === "TrackStartError") {
        return MediaDeviceFailure2.DeviceInUse;
      }
      return MediaDeviceFailure2.Other;
    }
  }
  MediaDeviceFailure2.getFailure = getFailure;
})(MediaDeviceFailure || (MediaDeviceFailure = {}));
var CriticalTimers = class {
};
CriticalTimers.setTimeout = function() {
  return setTimeout(...arguments);
};
CriticalTimers.setInterval = // eslint-disable-next-line @typescript-eslint/no-implied-eval
function() {
  return setInterval(...arguments);
};
CriticalTimers.clearTimeout = function() {
  return clearTimeout(...arguments);
};
CriticalTimers.clearInterval = function() {
  return clearInterval(...arguments);
};
var RoomEvent;
(function(RoomEvent2) {
  RoomEvent2["Connected"] = "connected";
  RoomEvent2["Reconnecting"] = "reconnecting";
  RoomEvent2["SignalReconnecting"] = "signalReconnecting";
  RoomEvent2["Reconnected"] = "reconnected";
  RoomEvent2["Disconnected"] = "disconnected";
  RoomEvent2["ConnectionStateChanged"] = "connectionStateChanged";
  RoomEvent2["Moved"] = "moved";
  RoomEvent2["MediaDevicesChanged"] = "mediaDevicesChanged";
  RoomEvent2["ParticipantConnected"] = "participantConnected";
  RoomEvent2["ParticipantDisconnected"] = "participantDisconnected";
  RoomEvent2["TrackPublished"] = "trackPublished";
  RoomEvent2["TrackSubscribed"] = "trackSubscribed";
  RoomEvent2["TrackSubscriptionFailed"] = "trackSubscriptionFailed";
  RoomEvent2["TrackUnpublished"] = "trackUnpublished";
  RoomEvent2["TrackUnsubscribed"] = "trackUnsubscribed";
  RoomEvent2["TrackMuted"] = "trackMuted";
  RoomEvent2["TrackUnmuted"] = "trackUnmuted";
  RoomEvent2["LocalTrackPublished"] = "localTrackPublished";
  RoomEvent2["LocalTrackUnpublished"] = "localTrackUnpublished";
  RoomEvent2["LocalAudioSilenceDetected"] = "localAudioSilenceDetected";
  RoomEvent2["ActiveSpeakersChanged"] = "activeSpeakersChanged";
  RoomEvent2["ParticipantMetadataChanged"] = "participantMetadataChanged";
  RoomEvent2["ParticipantNameChanged"] = "participantNameChanged";
  RoomEvent2["ParticipantAttributesChanged"] = "participantAttributesChanged";
  RoomEvent2["ParticipantActive"] = "participantActive";
  RoomEvent2["RoomMetadataChanged"] = "roomMetadataChanged";
  RoomEvent2["DataReceived"] = "dataReceived";
  RoomEvent2["SipDTMFReceived"] = "sipDTMFReceived";
  RoomEvent2["TranscriptionReceived"] = "transcriptionReceived";
  RoomEvent2["ConnectionQualityChanged"] = "connectionQualityChanged";
  RoomEvent2["TrackStreamStateChanged"] = "trackStreamStateChanged";
  RoomEvent2["TrackSubscriptionPermissionChanged"] = "trackSubscriptionPermissionChanged";
  RoomEvent2["TrackSubscriptionStatusChanged"] = "trackSubscriptionStatusChanged";
  RoomEvent2["AudioPlaybackStatusChanged"] = "audioPlaybackChanged";
  RoomEvent2["VideoPlaybackStatusChanged"] = "videoPlaybackChanged";
  RoomEvent2["MediaDevicesError"] = "mediaDevicesError";
  RoomEvent2["ParticipantPermissionsChanged"] = "participantPermissionsChanged";
  RoomEvent2["SignalConnected"] = "signalConnected";
  RoomEvent2["RecordingStatusChanged"] = "recordingStatusChanged";
  RoomEvent2["ParticipantEncryptionStatusChanged"] = "participantEncryptionStatusChanged";
  RoomEvent2["EncryptionError"] = "encryptionError";
  RoomEvent2["DCBufferStatusChanged"] = "dcBufferStatusChanged";
  RoomEvent2["ActiveDeviceChanged"] = "activeDeviceChanged";
  RoomEvent2["ChatMessage"] = "chatMessage";
  RoomEvent2["LocalTrackSubscribed"] = "localTrackSubscribed";
  RoomEvent2["MetricsReceived"] = "metricsReceived";
  RoomEvent2["DataTrackPublished"] = "dataTrackPublished";
  RoomEvent2["DataTrackUnpublished"] = "dataTrackUnpublished";
  RoomEvent2["LocalDataTrackPublished"] = "localDataTrackPublished";
  RoomEvent2["LocalDataTrackUnpublished"] = "localDataTrackUnpublished";
})(RoomEvent || (RoomEvent = {}));
var ParticipantEvent;
(function(ParticipantEvent2) {
  ParticipantEvent2["TrackPublished"] = "trackPublished";
  ParticipantEvent2["TrackSubscribed"] = "trackSubscribed";
  ParticipantEvent2["TrackSubscriptionFailed"] = "trackSubscriptionFailed";
  ParticipantEvent2["TrackUnpublished"] = "trackUnpublished";
  ParticipantEvent2["TrackUnsubscribed"] = "trackUnsubscribed";
  ParticipantEvent2["TrackMuted"] = "trackMuted";
  ParticipantEvent2["TrackUnmuted"] = "trackUnmuted";
  ParticipantEvent2["LocalTrackPublished"] = "localTrackPublished";
  ParticipantEvent2["LocalTrackUnpublished"] = "localTrackUnpublished";
  ParticipantEvent2["LocalTrackCpuConstrained"] = "localTrackCpuConstrained";
  ParticipantEvent2["LocalSenderCreated"] = "localSenderCreated";
  ParticipantEvent2["ParticipantMetadataChanged"] = "participantMetadataChanged";
  ParticipantEvent2["ParticipantNameChanged"] = "participantNameChanged";
  ParticipantEvent2["DataReceived"] = "dataReceived";
  ParticipantEvent2["SipDTMFReceived"] = "sipDTMFReceived";
  ParticipantEvent2["TranscriptionReceived"] = "transcriptionReceived";
  ParticipantEvent2["IsSpeakingChanged"] = "isSpeakingChanged";
  ParticipantEvent2["ConnectionQualityChanged"] = "connectionQualityChanged";
  ParticipantEvent2["TrackStreamStateChanged"] = "trackStreamStateChanged";
  ParticipantEvent2["TrackSubscriptionPermissionChanged"] = "trackSubscriptionPermissionChanged";
  ParticipantEvent2["TrackSubscriptionStatusChanged"] = "trackSubscriptionStatusChanged";
  ParticipantEvent2["TrackCpuConstrained"] = "trackCpuConstrained";
  ParticipantEvent2["MediaDevicesError"] = "mediaDevicesError";
  ParticipantEvent2["AudioStreamAcquired"] = "audioStreamAcquired";
  ParticipantEvent2["ParticipantPermissionsChanged"] = "participantPermissionsChanged";
  ParticipantEvent2["PCTrackAdded"] = "pcTrackAdded";
  ParticipantEvent2["AttributesChanged"] = "attributesChanged";
  ParticipantEvent2["LocalTrackSubscribed"] = "localTrackSubscribed";
  ParticipantEvent2["ChatMessage"] = "chatMessage";
  ParticipantEvent2["Active"] = "active";
})(ParticipantEvent || (ParticipantEvent = {}));
var EngineEvent;
(function(EngineEvent2) {
  EngineEvent2["TransportsCreated"] = "transportsCreated";
  EngineEvent2["Connected"] = "connected";
  EngineEvent2["Disconnected"] = "disconnected";
  EngineEvent2["Resuming"] = "resuming";
  EngineEvent2["Resumed"] = "resumed";
  EngineEvent2["Restarting"] = "restarting";
  EngineEvent2["Restarted"] = "restarted";
  EngineEvent2["SignalResumed"] = "signalResumed";
  EngineEvent2["SignalRestarted"] = "signalRestarted";
  EngineEvent2["Closing"] = "closing";
  EngineEvent2["MediaTrackAdded"] = "mediaTrackAdded";
  EngineEvent2["ActiveSpeakersUpdate"] = "activeSpeakersUpdate";
  EngineEvent2["DataPacketReceived"] = "dataPacketReceived";
  EngineEvent2["RTPVideoMapUpdate"] = "rtpVideoMapUpdate";
  EngineEvent2["DCBufferStatusChanged"] = "dcBufferStatusChanged";
  EngineEvent2["ParticipantUpdate"] = "participantUpdate";
  EngineEvent2["RoomUpdate"] = "roomUpdate";
  EngineEvent2["SpeakersChanged"] = "speakersChanged";
  EngineEvent2["StreamStateChanged"] = "streamStateChanged";
  EngineEvent2["ConnectionQualityUpdate"] = "connectionQualityUpdate";
  EngineEvent2["SubscriptionError"] = "subscriptionError";
  EngineEvent2["SubscriptionPermissionUpdate"] = "subscriptionPermissionUpdate";
  EngineEvent2["RemoteMute"] = "remoteMute";
  EngineEvent2["SubscribedQualityUpdate"] = "subscribedQualityUpdate";
  EngineEvent2["LocalTrackUnpublished"] = "localTrackUnpublished";
  EngineEvent2["LocalTrackSubscribed"] = "localTrackSubscribed";
  EngineEvent2["Offline"] = "offline";
  EngineEvent2["SignalRequestResponse"] = "signalRequestResponse";
  EngineEvent2["SignalConnected"] = "signalConnected";
  EngineEvent2["RoomMoved"] = "roomMoved";
  EngineEvent2["PublishDataTrackResponse"] = "publishDataTrackResponse";
  EngineEvent2["UnPublishDataTrackResponse"] = "unPublishDataTrackResponse";
  EngineEvent2["DataTrackSubscriberHandles"] = "dataTrackSubscriberHandles";
  EngineEvent2["DataTrackPacketReceived"] = "dataTrackPacketReceived";
  EngineEvent2["Joined"] = "joined";
  EngineEvent2["TokenRefreshed"] = "tokenRefreshed";
  EngineEvent2["ServerRegionsReported"] = "serverRegionsReported";
})(EngineEvent || (EngineEvent = {}));
var TrackEvent;
(function(TrackEvent2) {
  TrackEvent2["Message"] = "message";
  TrackEvent2["Muted"] = "muted";
  TrackEvent2["Unmuted"] = "unmuted";
  TrackEvent2["Restarted"] = "restarted";
  TrackEvent2["Ended"] = "ended";
  TrackEvent2["Subscribed"] = "subscribed";
  TrackEvent2["Unsubscribed"] = "unsubscribed";
  TrackEvent2["CpuConstrained"] = "cpuConstrained";
  TrackEvent2["UpdateSettings"] = "updateSettings";
  TrackEvent2["UpdateSubscription"] = "updateSubscription";
  TrackEvent2["AudioPlaybackStarted"] = "audioPlaybackStarted";
  TrackEvent2["AudioPlaybackFailed"] = "audioPlaybackFailed";
  TrackEvent2["AudioSilenceDetected"] = "audioSilenceDetected";
  TrackEvent2["VisibilityChanged"] = "visibilityChanged";
  TrackEvent2["VideoDimensionsChanged"] = "videoDimensionsChanged";
  TrackEvent2["VideoPlaybackStarted"] = "videoPlaybackStarted";
  TrackEvent2["VideoPlaybackFailed"] = "videoPlaybackFailed";
  TrackEvent2["ElementAttached"] = "elementAttached";
  TrackEvent2["ElementDetached"] = "elementDetached";
  TrackEvent2["UpstreamPaused"] = "upstreamPaused";
  TrackEvent2["UpstreamResumed"] = "upstreamResumed";
  TrackEvent2["SubscriptionPermissionChanged"] = "subscriptionPermissionChanged";
  TrackEvent2["SubscriptionStatusChanged"] = "subscriptionStatusChanged";
  TrackEvent2["SubscriptionFailed"] = "subscriptionFailed";
  TrackEvent2["TrackProcessorUpdate"] = "trackProcessorUpdate";
  TrackEvent2["AudioTrackFeatureUpdate"] = "audioTrackFeatureUpdate";
  TrackEvent2["TranscriptionReceived"] = "transcriptionReceived";
  TrackEvent2["TimeSyncUpdate"] = "timeSyncUpdate";
  TrackEvent2["PreConnectBufferFlushed"] = "preConnectBufferFlushed";
})(TrackEvent || (TrackEvent = {}));
function cloneDeep(value) {
  if (typeof value === "undefined") {
    return value;
  }
  if (typeof structuredClone === "function") {
    if (typeof value === "object" && value !== null) {
      return structuredClone(Object.assign({}, value));
    }
    return structuredClone(value);
  } else {
    return JSON.parse(JSON.stringify(value));
  }
}
var VideoPreset = class {
  constructor(widthOrOptions, height, maxBitrate, maxFramerate, priority) {
    if (typeof widthOrOptions === "object") {
      this.width = widthOrOptions.width;
      this.height = widthOrOptions.height;
      this.aspectRatio = widthOrOptions.aspectRatio;
      this.encoding = {
        maxBitrate: widthOrOptions.maxBitrate,
        maxFramerate: widthOrOptions.maxFramerate,
        priority: widthOrOptions.priority
      };
    } else if (height !== void 0 && maxBitrate !== void 0) {
      this.width = widthOrOptions;
      this.height = height;
      this.aspectRatio = widthOrOptions / height;
      this.encoding = {
        maxBitrate,
        maxFramerate,
        priority
      };
    } else {
      throw new TypeError("Unsupported options: provide at least width, height and maxBitrate");
    }
  }
  get resolution() {
    return {
      width: this.width,
      height: this.height,
      frameRate: this.encoding.maxFramerate,
      aspectRatio: this.aspectRatio
    };
  }
};
var backupVideoCodecs = ["vp8", "h264"];
var videoCodecs = ["vp8", "h264", "vp9", "av1", "h265"];
function isBackupVideoCodec(codec) {
  return !!backupVideoCodecs.find((backup) => backup === codec);
}
var isBackupCodec = isBackupVideoCodec;
var BackupCodecPolicy;
(function(BackupCodecPolicy2) {
  BackupCodecPolicy2[BackupCodecPolicy2["PREFER_REGRESSION"] = 0] = "PREFER_REGRESSION";
  BackupCodecPolicy2[BackupCodecPolicy2["SIMULCAST"] = 1] = "SIMULCAST";
  BackupCodecPolicy2[BackupCodecPolicy2["REGRESSION"] = 2] = "REGRESSION";
})(BackupCodecPolicy || (BackupCodecPolicy = {}));
var AudioPresets;
(function(AudioPresets2) {
  AudioPresets2.telephone = {
    maxBitrate: 12e3
  };
  AudioPresets2.speech = {
    maxBitrate: 24e3
  };
  AudioPresets2.music = {
    maxBitrate: 48e3
  };
  AudioPresets2.musicStereo = {
    maxBitrate: 64e3
  };
  AudioPresets2.musicHighQuality = {
    maxBitrate: 96e3
  };
  AudioPresets2.musicHighQualityStereo = {
    maxBitrate: 128e3
  };
})(AudioPresets || (AudioPresets = {}));
var VideoPresets = {
  h90: new VideoPreset(160, 90, 9e4, 20),
  h180: new VideoPreset(320, 180, 16e4, 20),
  h216: new VideoPreset(384, 216, 18e4, 20),
  h360: new VideoPreset(640, 360, 45e4, 20),
  h540: new VideoPreset(960, 540, 8e5, 25),
  h720: new VideoPreset(1280, 720, 17e5, 30),
  h1080: new VideoPreset(1920, 1080, 3e6, 30),
  h1440: new VideoPreset(2560, 1440, 5e6, 30),
  h2160: new VideoPreset(3840, 2160, 8e6, 30)
};
var VideoPresets43 = {
  h120: new VideoPreset(160, 120, 7e4, 20),
  h180: new VideoPreset(240, 180, 125e3, 20),
  h240: new VideoPreset(320, 240, 14e4, 20),
  h360: new VideoPreset(480, 360, 33e4, 20),
  h480: new VideoPreset(640, 480, 5e5, 20),
  h540: new VideoPreset(720, 540, 6e5, 25),
  h720: new VideoPreset(960, 720, 13e5, 30),
  h1080: new VideoPreset(1440, 1080, 23e5, 30),
  h1440: new VideoPreset(1920, 1440, 38e5, 30)
};
var ScreenSharePresets = {
  h360fps3: new VideoPreset(640, 360, 2e5, 3, "medium"),
  h360fps15: new VideoPreset(640, 360, 4e5, 15, "medium"),
  h720fps5: new VideoPreset(1280, 720, 8e5, 5, "medium"),
  h720fps15: new VideoPreset(1280, 720, 15e5, 15, "medium"),
  h720fps30: new VideoPreset(1280, 720, 2e6, 30, "medium"),
  h1080fps15: new VideoPreset(1920, 1080, 25e5, 15, "medium"),
  h1080fps30: new VideoPreset(1920, 1080, 5e6, 30, "medium"),
  // original resolution, without resizing
  original: new VideoPreset(0, 0, 7e6, 30, "medium")
};
function mergeDefaultOptions(options, audioDefaults2, videoDefaults2) {
  var _a2, _b2;
  var _c, _d;
  const _extractProcessorsFro = extractProcessorsFromOptions(options !== null && options !== void 0 ? options : {}), optionsWithoutProcessor = _extractProcessorsFro.optionsWithoutProcessor, audioProcessor = _extractProcessorsFro.audioProcessor, videoProcessor = _extractProcessorsFro.videoProcessor;
  const defaultAudioProcessor = audioDefaults2 === null || audioDefaults2 === void 0 ? void 0 : audioDefaults2.processor;
  const defaultVideoProcessor = videoDefaults2 === null || videoDefaults2 === void 0 ? void 0 : videoDefaults2.processor;
  const clonedOptions = optionsWithoutProcessor !== null && optionsWithoutProcessor !== void 0 ? optionsWithoutProcessor : {};
  if (clonedOptions.audio === true) clonedOptions.audio = {};
  if (clonedOptions.video === true) clonedOptions.video = {};
  if (clonedOptions.audio) {
    mergeObjectWithoutOverwriting(clonedOptions.audio, audioDefaults2);
    (_a2 = (_c = clonedOptions.audio).deviceId) !== null && _a2 !== void 0 ? _a2 : _c.deviceId = {
      ideal: "default"
    };
    if (audioProcessor || defaultAudioProcessor) {
      clonedOptions.audio.processor = audioProcessor !== null && audioProcessor !== void 0 ? audioProcessor : defaultAudioProcessor;
    }
  }
  if (clonedOptions.video) {
    mergeObjectWithoutOverwriting(clonedOptions.video, videoDefaults2);
    (_b2 = (_d = clonedOptions.video).deviceId) !== null && _b2 !== void 0 ? _b2 : _d.deviceId = {
      ideal: "default"
    };
    if (videoProcessor || defaultVideoProcessor) {
      clonedOptions.video.processor = videoProcessor !== null && videoProcessor !== void 0 ? videoProcessor : defaultVideoProcessor;
    }
  }
  return clonedOptions;
}
function mergeObjectWithoutOverwriting(mainObject, objectToMerge) {
  Object.keys(objectToMerge).forEach((key) => {
    if (mainObject[key] === void 0) mainObject[key] = objectToMerge[key];
  });
  return mainObject;
}
function constraintsForOptions(options) {
  var _a2, _b2;
  var _c, _d;
  const constraints = {};
  if (options.video) {
    if (typeof options.video === "object") {
      const videoOptions = {};
      const target = videoOptions;
      const source = options.video;
      Object.keys(source).forEach((key) => {
        switch (key) {
          case "resolution":
            mergeObjectWithoutOverwriting(target, source.resolution);
            break;
          default:
            target[key] = source[key];
        }
      });
      constraints.video = videoOptions;
      (_a2 = (_c = constraints.video).deviceId) !== null && _a2 !== void 0 ? _a2 : _c.deviceId = {
        ideal: "default"
      };
    } else {
      constraints.video = options.video ? {
        deviceId: {
          ideal: "default"
        }
      } : false;
    }
  } else {
    constraints.video = false;
  }
  if (options.audio) {
    if (typeof options.audio === "object") {
      constraints.audio = options.audio;
      (_b2 = (_d = constraints.audio).deviceId) !== null && _b2 !== void 0 ? _b2 : _d.deviceId = {
        ideal: "default"
      };
    } else {
      constraints.audio = {
        deviceId: {
          ideal: "default"
        }
      };
    }
  } else {
    constraints.audio = false;
  }
  return constraints;
}
function detectSilence(track_1) {
  return __awaiter(this, arguments, void 0, function(track) {
    let timeOffset = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : 200;
    return function* () {
      const ctx = getNewAudioContext();
      if (ctx) {
        const analyser = ctx.createAnalyser();
        analyser.fftSize = 2048;
        const bufferLength = analyser.frequencyBinCount;
        const dataArray = new Uint8Array(bufferLength);
        const source = ctx.createMediaStreamSource(new MediaStream([track.mediaStreamTrack]));
        source.connect(analyser);
        yield sleep(timeOffset);
        analyser.getByteTimeDomainData(dataArray);
        const someNoise = dataArray.some((sample) => sample !== 128 && sample !== 0);
        ctx.close();
        return !someNoise;
      }
      return false;
    }();
  });
}
function getNewAudioContext() {
  var _a2;
  const AudioContext2 = (
    // @ts-ignore
    typeof window !== "undefined" && (window.AudioContext || window.webkitAudioContext)
  );
  if (AudioContext2) {
    const audioContext = new AudioContext2({
      latencyHint: "interactive"
    });
    if (audioContext.state === "suspended" && typeof window !== "undefined" && ((_a2 = window.document) === null || _a2 === void 0 ? void 0 : _a2.body)) {
      const handleResume = () => __awaiter(this, void 0, void 0, function* () {
        var _a3;
        try {
          if (audioContext.state === "suspended") {
            yield audioContext.resume();
          }
        } catch (e4) {
          console.warn("Error trying to auto-resume audio context", e4);
        } finally {
          (_a3 = window.document.body) === null || _a3 === void 0 ? void 0 : _a3.removeEventListener("click", handleResume);
        }
      });
      audioContext.addEventListener("statechange", () => {
        var _a3;
        if (audioContext.state === "closed") {
          (_a3 = window.document.body) === null || _a3 === void 0 ? void 0 : _a3.removeEventListener("click", handleResume);
        }
      });
      window.document.body.addEventListener("click", handleResume);
    }
    return audioContext;
  }
}
function kindToSource(kind) {
  if (kind === "audioinput") {
    return Track.Source.Microphone;
  } else if (kind === "videoinput") {
    return Track.Source.Camera;
  } else {
    return Track.Source.Unknown;
  }
}
function sourceToKind(source) {
  if (source === Track.Source.Microphone) {
    return "audioinput";
  } else if (source === Track.Source.Camera) {
    return "videoinput";
  } else {
    return void 0;
  }
}
function screenCaptureToDisplayMediaStreamOptions(options) {
  var _a2, _b2;
  let videoConstraints = (_a2 = options.video) !== null && _a2 !== void 0 ? _a2 : true;
  if (options.resolution && options.resolution.width > 0 && options.resolution.height > 0) {
    videoConstraints = typeof videoConstraints === "boolean" ? {} : videoConstraints;
    if (isSafari()) {
      videoConstraints = Object.assign(Object.assign({}, videoConstraints), {
        width: {
          max: options.resolution.width
        },
        height: {
          max: options.resolution.height
        },
        frameRate: options.resolution.frameRate
      });
    } else {
      videoConstraints = Object.assign(Object.assign({}, videoConstraints), {
        width: {
          ideal: options.resolution.width
        },
        height: {
          ideal: options.resolution.height
        },
        frameRate: options.resolution.frameRate
      });
    }
  }
  return {
    audio: (_b2 = options.audio) !== null && _b2 !== void 0 ? _b2 : false,
    video: videoConstraints,
    // @ts-expect-error support for experimental display media features
    controller: options.controller,
    selfBrowserSurface: options.selfBrowserSurface,
    surfaceSwitching: options.surfaceSwitching,
    systemAudio: options.systemAudio,
    preferCurrentTab: options.preferCurrentTab
  };
}
function mimeTypeToVideoCodecString(mimeType) {
  return mimeType.split("/")[1].toLowerCase();
}
function getTrackPublicationInfo(tracks) {
  const infos = [];
  tracks.forEach((track) => {
    if (track.track !== void 0) {
      infos.push(new TrackPublishedResponse({
        cid: track.track.mediaStreamID,
        track: track.trackInfo
      }));
    }
  });
  return infos;
}
function getLogContextFromTrack(track) {
  if ("mediaStreamTrack" in track) {
    return {
      trackID: track.sid,
      source: track.source,
      muted: track.isMuted,
      enabled: track.mediaStreamTrack.enabled,
      kind: track.kind,
      streamID: track.mediaStreamID,
      streamTrackID: track.mediaStreamTrack.id
    };
  } else {
    return {
      trackID: track.trackSid,
      enabled: track.isEnabled,
      muted: track.isMuted,
      trackInfo: Object.assign({
        mimeType: track.mimeType,
        name: track.trackName,
        encrypted: track.isEncrypted,
        kind: track.kind,
        source: track.source
      }, track.track ? getLogContextFromTrack(track.track) : {})
    };
  }
}
function supportsSynchronizationSources() {
  return typeof RTCRtpReceiver !== "undefined" && typeof RTCRtpReceiver.prototype.getSynchronizationSources === "function";
}
function diffAttributes(oldValues, newValues) {
  var _a2;
  if (oldValues === void 0) {
    oldValues = {};
  }
  if (newValues === void 0) {
    newValues = {};
  }
  const allKeys = [...Object.keys(newValues), ...Object.keys(oldValues)];
  const diff = {};
  for (const key of allKeys) {
    if (oldValues[key] !== newValues[key]) {
      diff[key] = (_a2 = newValues[key]) !== null && _a2 !== void 0 ? _a2 : "";
    }
  }
  return diff;
}
function extractProcessorsFromOptions(options) {
  const newOptions = Object.assign({}, options);
  let audioProcessor;
  let videoProcessor;
  if (typeof newOptions.audio === "object" && newOptions.audio.processor) {
    audioProcessor = newOptions.audio.processor;
    newOptions.audio = Object.assign(Object.assign({}, newOptions.audio), {
      processor: void 0
    });
  }
  if (typeof newOptions.video === "object" && newOptions.video.processor) {
    videoProcessor = newOptions.video.processor;
    newOptions.video = Object.assign(Object.assign({}, newOptions.video), {
      processor: void 0
    });
  }
  return {
    audioProcessor,
    videoProcessor,
    optionsWithoutProcessor: cloneDeep(newOptions)
  };
}
function getTrackSourceFromProto(source) {
  switch (source) {
    case TrackSource.CAMERA:
      return Track.Source.Camera;
    case TrackSource.MICROPHONE:
      return Track.Source.Microphone;
    case TrackSource.SCREEN_SHARE:
      return Track.Source.ScreenShare;
    case TrackSource.SCREEN_SHARE_AUDIO:
      return Track.Source.ScreenShareAudio;
    default:
      return Track.Source.Unknown;
  }
}
function areDimensionsSmaller(a, b) {
  return a.width * a.height < b.width * b.height;
}
function layerDimensionsFor(trackInfo, quality) {
  var _a2;
  return (_a2 = trackInfo.layers) === null || _a2 === void 0 ? void 0 : _a2.find((l) => l.quality === quality);
}
var BACKGROUND_REACTION_DELAY = 5e3;
var recycledElements = [];
var VideoQuality;
(function(VideoQuality2) {
  VideoQuality2[VideoQuality2["LOW"] = 0] = "LOW";
  VideoQuality2[VideoQuality2["MEDIUM"] = 1] = "MEDIUM";
  VideoQuality2[VideoQuality2["HIGH"] = 2] = "HIGH";
})(VideoQuality || (VideoQuality = {}));
var Track = class _Track extends eventsExports.EventEmitter {
  /**
   * indicates current state of stream, it'll indicate `paused` if the track
   * has been paused by congestion controller
   */
  get streamState() {
    return this._streamState;
  }
  /** @internal */
  setStreamState(value) {
    if (this._streamState !== value) {
      this.log.debug("stream state changed: ".concat(this._streamState, " -> ").concat(value));
    }
    this._streamState = value;
  }
  constructor(mediaTrack, kind) {
    let loggerOptions = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : {};
    var _a2;
    super();
    this.attachedElements = [];
    this.isMuted = false;
    this._streamState = _Track.StreamState.Active;
    this.isInBackground = false;
    this._currentBitrate = 0;
    this.log = livekitLogger;
    this.appVisibilityChangedListener = () => {
      if (this.backgroundTimeout) {
        clearTimeout(this.backgroundTimeout);
      }
      if (document.visibilityState === "hidden") {
        this.backgroundTimeout = setTimeout(() => this.handleAppVisibilityChanged(), BACKGROUND_REACTION_DELAY);
      } else {
        this.handleAppVisibilityChanged();
      }
    };
    this.loggerContextCb = loggerOptions.loggerContextCb;
    this.log = getLogger((_a2 = loggerOptions.loggerName) !== null && _a2 !== void 0 ? _a2 : LoggerNames.Track, () => this.logContext);
    this.setMaxListeners(100);
    this.kind = kind;
    this._mediaStreamTrack = mediaTrack;
    this._mediaStreamID = mediaTrack.id;
    this.source = _Track.Source.Unknown;
  }
  get logContext() {
    var _a2;
    return Object.assign(Object.assign({}, (_a2 = this.loggerContextCb) === null || _a2 === void 0 ? void 0 : _a2.call(this)), getLogContextFromTrack(this));
  }
  /** current receive bits per second */
  get currentBitrate() {
    return this._currentBitrate;
  }
  get mediaStreamTrack() {
    return this._mediaStreamTrack;
  }
  /**
   * @internal
   * used for keep mediaStream's first id, since it's id might change
   * if we disable/enable a track
   */
  get mediaStreamID() {
    return this._mediaStreamID;
  }
  attach(element) {
    let elementType = "audio";
    if (this.kind === _Track.Kind.Video) {
      elementType = "video";
    }
    if (this.attachedElements.length === 0 && this.kind === _Track.Kind.Video) {
      this.addAppVisibilityListener();
    }
    if (!element) {
      if (elementType === "audio") {
        recycledElements.forEach((e4) => {
          if (e4.parentElement === null && !element) {
            element = e4;
          }
        });
        if (element) {
          recycledElements.splice(recycledElements.indexOf(element), 1);
        }
      }
      if (!element) {
        element = document.createElement(elementType);
      }
    }
    if (!this.attachedElements.includes(element)) {
      this.attachedElements.push(element);
    }
    attachToElement(this.mediaStreamTrack, element);
    const allMediaStreamTracks = element.srcObject.getTracks();
    const hasAudio = allMediaStreamTracks.some((tr3) => tr3.kind === "audio");
    element.play().then(() => {
      this.emit(hasAudio ? TrackEvent.AudioPlaybackStarted : TrackEvent.VideoPlaybackStarted);
    }).catch((e4) => {
      if (e4.name === "NotAllowedError") {
        this.emit(hasAudio ? TrackEvent.AudioPlaybackFailed : TrackEvent.VideoPlaybackFailed, e4);
      } else if (e4.name === "AbortError") {
        this.log.debug("".concat(hasAudio ? "audio" : "video", " playback aborted, likely due to new play request"));
      } else {
        this.log.warn("could not playback ".concat(hasAudio ? "audio" : "video"), {
          error: e4
        });
      }
      if (hasAudio && element && allMediaStreamTracks.some((tr3) => tr3.kind === "video") && e4.name === "NotAllowedError") {
        element.muted = true;
        element.play().catch(() => {
        });
      }
    });
    this.emit(TrackEvent.ElementAttached, element);
    return element;
  }
  detach(element) {
    try {
      if (element) {
        detachTrack(this.mediaStreamTrack, element);
        const idx = this.attachedElements.indexOf(element);
        if (idx >= 0) {
          this.attachedElements.splice(idx, 1);
          this.recycleElement(element);
          this.emit(TrackEvent.ElementDetached, element);
        }
        return element;
      }
      const detached = [];
      this.attachedElements.forEach((elm) => {
        detachTrack(this.mediaStreamTrack, elm);
        detached.push(elm);
        this.recycleElement(elm);
        this.emit(TrackEvent.ElementDetached, elm);
      });
      this.attachedElements = [];
      return detached;
    } finally {
      if (this.attachedElements.length === 0) {
        this.removeAppVisibilityListener();
      }
    }
  }
  stop() {
    this.log.debug("stopping track");
    this.stopMonitor();
    this._mediaStreamTrack.stop();
  }
  enable() {
    this._mediaStreamTrack.enabled = true;
  }
  disable() {
    this._mediaStreamTrack.enabled = false;
  }
  /* @internal */
  stopMonitor() {
    if (this.monitorInterval) {
      clearInterval(this.monitorInterval);
    }
    if (this.timeSyncHandle) {
      cancelAnimationFrame(this.timeSyncHandle);
    }
  }
  /** @internal */
  updateLoggerOptions(loggerOptions) {
    if (loggerOptions.loggerContextCb) {
      this.loggerContextCb = loggerOptions.loggerContextCb;
    }
    if (loggerOptions.loggerName) {
      this.log = getLogger(loggerOptions.loggerName, () => this.logContext);
    }
  }
  recycleElement(element) {
    if (element instanceof HTMLAudioElement) {
      let shouldCache = true;
      element.pause();
      recycledElements.forEach((e4) => {
        if (!e4.parentElement) {
          shouldCache = false;
        }
      });
      if (shouldCache) {
        recycledElements.push(element);
      }
    }
  }
  handleAppVisibilityChanged() {
    return __awaiter(this, void 0, void 0, function* () {
      this.isInBackground = document.visibilityState === "hidden";
      if (!this.isInBackground && this.kind === _Track.Kind.Video) {
        setTimeout(() => this.attachedElements.forEach((el) => el.play().catch(() => {
        })), 0);
      }
    });
  }
  addAppVisibilityListener() {
    if (isWeb()) {
      this.isInBackground = document.visibilityState === "hidden";
      document.addEventListener("visibilitychange", this.appVisibilityChangedListener);
    } else {
      this.isInBackground = false;
    }
  }
  removeAppVisibilityListener() {
    if (isWeb()) {
      document.removeEventListener("visibilitychange", this.appVisibilityChangedListener);
    }
  }
};
function attachToElement(track, element) {
  let mediaStream;
  if (element.srcObject instanceof MediaStream) {
    mediaStream = element.srcObject;
  } else {
    mediaStream = new MediaStream();
  }
  let existingTracks;
  if (track.kind === "audio") {
    existingTracks = mediaStream.getAudioTracks();
  } else {
    existingTracks = mediaStream.getVideoTracks();
  }
  if (!existingTracks.includes(track)) {
    existingTracks.forEach((et3) => {
      mediaStream.removeTrack(et3);
    });
    mediaStream.addTrack(track);
  }
  if (!isSafari() || !(element instanceof HTMLVideoElement)) {
    element.autoplay = true;
  }
  element.muted = mediaStream.getAudioTracks().length === 0;
  if (element instanceof HTMLVideoElement) {
    element.playsInline = true;
  }
  if (element.srcObject !== mediaStream) {
    element.srcObject = mediaStream;
    if ((isSafari() || isFireFox()) && element instanceof HTMLVideoElement) {
      setTimeout(() => {
        element.srcObject = mediaStream;
        element.play().catch(() => {
        });
      }, 0);
    }
  }
}
function detachTrack(track, element) {
  if (element.srcObject instanceof MediaStream) {
    const mediaStream = element.srcObject;
    mediaStream.removeTrack(track);
    if (mediaStream.getTracks().length > 0) {
      element.srcObject = mediaStream;
    } else {
      element.srcObject = null;
    }
  }
}
(function(Track2) {
  let Kind;
  (function(Kind2) {
    Kind2["Audio"] = "audio";
    Kind2["Video"] = "video";
    Kind2["Unknown"] = "unknown";
  })(Kind = Track2.Kind || (Track2.Kind = {}));
  let Source;
  (function(Source2) {
    Source2["Camera"] = "camera";
    Source2["Microphone"] = "microphone";
    Source2["ScreenShare"] = "screen_share";
    Source2["ScreenShareAudio"] = "screen_share_audio";
    Source2["Unknown"] = "unknown";
  })(Source = Track2.Source || (Track2.Source = {}));
  let StreamState$1;
  (function(StreamState2) {
    StreamState2["Active"] = "active";
    StreamState2["Paused"] = "paused";
    StreamState2["Unknown"] = "unknown";
  })(StreamState$1 = Track2.StreamState || (Track2.StreamState = {}));
  function kindToProto(k) {
    switch (k) {
      case Kind.Audio:
        return TrackType.AUDIO;
      case Kind.Video:
        return TrackType.VIDEO;
      default:
        return TrackType.DATA;
    }
  }
  Track2.kindToProto = kindToProto;
  function kindFromProto(t) {
    switch (t) {
      case TrackType.AUDIO:
        return Kind.Audio;
      case TrackType.VIDEO:
        return Kind.Video;
      default:
        return Kind.Unknown;
    }
  }
  Track2.kindFromProto = kindFromProto;
  function sourceToProto(s) {
    switch (s) {
      case Source.Camera:
        return TrackSource.CAMERA;
      case Source.Microphone:
        return TrackSource.MICROPHONE;
      case Source.ScreenShare:
        return TrackSource.SCREEN_SHARE;
      case Source.ScreenShareAudio:
        return TrackSource.SCREEN_SHARE_AUDIO;
      default:
        return TrackSource.UNKNOWN;
    }
  }
  Track2.sourceToProto = sourceToProto;
  function sourceFromProto(s) {
    switch (s) {
      case TrackSource.CAMERA:
        return Source.Camera;
      case TrackSource.MICROPHONE:
        return Source.Microphone;
      case TrackSource.SCREEN_SHARE:
        return Source.ScreenShare;
      case TrackSource.SCREEN_SHARE_AUDIO:
        return Source.ScreenShareAudio;
      default:
        return Source.Unknown;
    }
  }
  Track2.sourceFromProto = sourceFromProto;
  function streamStateFromProto(s) {
    switch (s) {
      case StreamState.ACTIVE:
        return StreamState$1.Active;
      case StreamState.PAUSED:
        return StreamState$1.Paused;
      default:
        return StreamState$1.Unknown;
    }
  }
  Track2.streamStateFromProto = streamStateFromProto;
})(Track || (Track = {}));
var separator = "|";
var ddExtensionURI = "https://aomediacodec.github.io/av1-rtp-spec/#dependency-descriptor-rtp-header-extension";
function unpackStreamId(packed) {
  const parts = packed.split(separator);
  if (parts.length > 1) {
    return [parts[0], packed.substr(parts[0].length + 1)];
  }
  return [packed, ""];
}
function sleep(duration) {
  return new TypedPromise((resolve) => CriticalTimers.setTimeout(resolve, duration));
}
function supportsTransceiver() {
  return "addTransceiver" in RTCPeerConnection.prototype;
}
function supportsAddTrack() {
  return "addTrack" in RTCPeerConnection.prototype;
}
function supportsAV1() {
  if (!("getCapabilities" in RTCRtpSender)) {
    return false;
  }
  if (isSafari() || isFireFox()) {
    return false;
  }
  const capabilities = RTCRtpSender.getCapabilities("video");
  let hasAV1 = false;
  if (capabilities) {
    for (const codec of capabilities.codecs) {
      if (codec.mimeType.toLowerCase() === "video/av1") {
        hasAV1 = true;
        break;
      }
    }
  }
  return hasAV1;
}
function supportsVP9() {
  if (!("getCapabilities" in RTCRtpSender)) {
    return false;
  }
  if (isFireFox()) {
    return false;
  }
  if (isSafari()) {
    const browser = getBrowser();
    if ((browser === null || browser === void 0 ? void 0 : browser.version) && compareVersions(browser.version, "16") < 0) {
      return false;
    }
    if ((browser === null || browser === void 0 ? void 0 : browser.os) === "iOS" && (browser === null || browser === void 0 ? void 0 : browser.osVersion) && compareVersions(browser.osVersion, "16") < 0) {
      return false;
    }
  }
  const capabilities = RTCRtpSender.getCapabilities("video");
  let hasVP9 = false;
  if (capabilities) {
    for (const codec of capabilities.codecs) {
      if (codec.mimeType.toLowerCase() === "video/vp9") {
        hasVP9 = true;
        break;
      }
    }
  }
  return hasVP9;
}
function isSVCCodec(codec) {
  return codec === "av1" || codec === "vp9";
}
function supportsSetSinkId(elm) {
  if (!document || isSafariBased()) {
    return false;
  }
  if (!elm) {
    elm = document.createElement("audio");
  }
  return "setSinkId" in elm;
}
function isBrowserSupported() {
  if (typeof RTCPeerConnection === "undefined") {
    return false;
  }
  return supportsTransceiver() || supportsAddTrack();
}
function isFireFox() {
  var _a2;
  return ((_a2 = getBrowser()) === null || _a2 === void 0 ? void 0 : _a2.name) === "Firefox";
}
function isChromiumBased() {
  const browser = getBrowser();
  return !!browser && browser.name === "Chrome" && browser.os !== "iOS";
}
function isScriptTransformSupportedForWorker() {
  return typeof window !== "undefined" && // @ts-ignore
  typeof window.RTCRtpScriptTransform !== "undefined" && !isChromiumBased();
}
function isSafari() {
  var _a2;
  return ((_a2 = getBrowser()) === null || _a2 === void 0 ? void 0 : _a2.name) === "Safari";
}
function isSafariBased() {
  const b = getBrowser();
  return (b === null || b === void 0 ? void 0 : b.name) === "Safari" || (b === null || b === void 0 ? void 0 : b.os) === "iOS";
}
function isSafari17Based() {
  const b = getBrowser();
  return (b === null || b === void 0 ? void 0 : b.name) === "Safari" && b.version.startsWith("17.") || (b === null || b === void 0 ? void 0 : b.os) === "iOS" && !!(b === null || b === void 0 ? void 0 : b.osVersion) && compareVersions(b.osVersion, "17") >= 0;
}
function isSafariSvcApi(browser) {
  if (!browser) {
    browser = getBrowser();
  }
  return (browser === null || browser === void 0 ? void 0 : browser.name) === "Safari" && compareVersions(browser.version, "18.3") > 0 || (browser === null || browser === void 0 ? void 0 : browser.os) === "iOS" && !!(browser === null || browser === void 0 ? void 0 : browser.osVersion) && compareVersions(browser.osVersion, "18.3") > 0;
}
function isMobile() {
  var _a2, _b2;
  if (!isWeb()) return false;
  return (
    // @ts-expect-error `userAgentData` is not yet part of typescript
    (_b2 = (_a2 = navigator.userAgentData) === null || _a2 === void 0 ? void 0 : _a2.mobile) !== null && _b2 !== void 0 ? _b2 : /Tablet|iPad|Mobile|Android|BlackBerry/.test(navigator.userAgent)
  );
}
function isE2EESimulcastSupported() {
  const browser = getBrowser();
  const supportedSafariVersion = "17.2";
  if (browser) {
    if (browser.name !== "Safari" && browser.os !== "iOS") {
      return true;
    } else if (browser.os === "iOS" && browser.osVersion && compareVersions(browser.osVersion, supportedSafariVersion) >= 0) {
      return true;
    } else if (browser.name === "Safari" && compareVersions(browser.version, supportedSafariVersion) >= 0) {
      return true;
    } else {
      return false;
    }
  }
}
function isWeb() {
  return typeof document !== "undefined";
}
function isReactNative() {
  return navigator.product == "ReactNative";
}
function isCloud(serverUrl) {
  return serverUrl.hostname.endsWith(".livekit.cloud") || serverUrl.hostname.endsWith(".livekit.run");
}
function extractProjectFromUrl(serverUrl) {
  if (!isCloud(serverUrl)) {
    return null;
  }
  return serverUrl.hostname.split(".")[0];
}
function getLKReactNativeInfo() {
  if (global && global.LiveKitReactNativeGlobal) {
    return global.LiveKitReactNativeGlobal;
  }
  return void 0;
}
function getReactNativeOs() {
  if (!isReactNative()) {
    return void 0;
  }
  let info = getLKReactNativeInfo();
  if (info) {
    return info.platform;
  }
  return void 0;
}
function getDevicePixelRatio() {
  if (isWeb()) {
    return window.devicePixelRatio;
  }
  if (isReactNative()) {
    let info = getLKReactNativeInfo();
    if (info) {
      return info.devicePixelRatio;
    }
  }
  return 1;
}
function compareVersions(v1, v2) {
  const parts1 = v1.split(".");
  const parts2 = v2.split(".");
  const k = Math.min(parts1.length, parts2.length);
  for (let i2 = 0; i2 < k; ++i2) {
    const p1 = parseInt(parts1[i2], 10);
    const p2 = parseInt(parts2[i2], 10);
    if (p1 > p2) return 1;
    if (p1 < p2) return -1;
    if (i2 === k - 1 && p1 === p2) return 0;
  }
  if (v1 === "" && v2 !== "") {
    return -1;
  } else if (v2 === "") {
    return 1;
  }
  return parts1.length == parts2.length ? 0 : parts1.length < parts2.length ? -1 : 1;
}
function roDispatchCallback(entries) {
  for (const entry of entries) {
    entry.target.handleResize(entry);
  }
}
function ioDispatchCallback(entries) {
  for (const entry of entries) {
    entry.target.handleVisibilityChanged(entry);
  }
}
var resizeObserver = null;
var getResizeObserver = () => {
  if (!resizeObserver) resizeObserver = new ResizeObserver(roDispatchCallback);
  return resizeObserver;
};
var intersectionObserver = null;
var getIntersectionObserver = () => {
  if (!intersectionObserver) {
    intersectionObserver = new IntersectionObserver(ioDispatchCallback, {
      root: null,
      rootMargin: "0px"
    });
  }
  return intersectionObserver;
};
function getClientInfo(capabilities) {
  var _a2;
  const info = new ClientInfo({
    capabilities,
    sdk: ClientInfo_SDK.JS,
    protocol: protocolVersion,
    clientProtocol,
    version
  });
  if (isReactNative()) {
    info.os = (_a2 = getReactNativeOs()) !== null && _a2 !== void 0 ? _a2 : "";
  }
  return info;
}
function createDummyVideoStreamTrack() {
  let width = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : 16;
  let height = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : 16;
  let enabled = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : false;
  let paintContent = arguments.length > 3 && arguments[3] !== void 0 ? arguments[3] : false;
  const canvas = document.createElement("canvas");
  canvas.width = width;
  canvas.height = height;
  const ctx = canvas.getContext("2d");
  ctx === null || ctx === void 0 ? void 0 : ctx.fillRect(0, 0, canvas.width, canvas.height);
  if (paintContent && ctx) {
    ctx.beginPath();
    ctx.arc(width / 2, height / 2, 50, 0, Math.PI * 2, true);
    ctx.closePath();
    ctx.fillStyle = "grey";
    ctx.fill();
  }
  const dummyStream = canvas.captureStream();
  const _dummyStream$getTrack = dummyStream.getTracks(), _dummyStream$getTrack2 = _slicedToArray(_dummyStream$getTrack, 1), dummyTrack = _dummyStream$getTrack2[0];
  if (!dummyTrack) {
    throw Error("Could not get empty media stream video track");
  }
  dummyTrack.enabled = enabled;
  return dummyTrack;
}
var emptyAudioStreamTrack;
function getEmptyAudioStreamTrack() {
  if (!emptyAudioStreamTrack) {
    const ctx = new AudioContext();
    const oscillator = ctx.createOscillator();
    const gain = ctx.createGain();
    gain.gain.setValueAtTime(0, 0);
    const dst = ctx.createMediaStreamDestination();
    oscillator.connect(gain);
    gain.connect(dst);
    oscillator.start();
    var _dst$stream$getAudioT = dst.stream.getAudioTracks();
    var _dst$stream$getAudioT2 = _slicedToArray(_dst$stream$getAudioT, 1);
    emptyAudioStreamTrack = _dst$stream$getAudioT2[0];
    if (!emptyAudioStreamTrack) {
      throw Error("Could not get empty media stream audio track");
    }
    emptyAudioStreamTrack.enabled = false;
  }
  return emptyAudioStreamTrack.clone();
}
var Future = class {
  get isResolved() {
    return this._isResolved;
  }
  constructor(futureBase, onFinally) {
    this._isResolved = false;
    this.onFinally = onFinally;
    this.promise = new Promise((resolve, reject) => __awaiter(this, void 0, void 0, function* () {
      this.resolve = resolve;
      this.reject = reject;
      if (futureBase) {
        yield futureBase(resolve, reject);
      }
    })).finally(() => {
      var _a2;
      this._isResolved = true;
      (_a2 = this.onFinally) === null || _a2 === void 0 ? void 0 : _a2.call(this);
    });
  }
};
function createAudioAnalyser(track, options) {
  const opts = Object.assign({
    cloneTrack: false,
    fftSize: 2048,
    smoothingTimeConstant: 0.8,
    minDecibels: -100,
    maxDecibels: -80
  }, options);
  const audioContext = getNewAudioContext();
  if (!audioContext) {
    throw new Error("Audio Context not supported on this browser");
  }
  const streamTrack = opts.cloneTrack ? track.mediaStreamTrack.clone() : track.mediaStreamTrack;
  const mediaStreamSource = audioContext.createMediaStreamSource(new MediaStream([streamTrack]));
  const analyser = audioContext.createAnalyser();
  analyser.minDecibels = opts.minDecibels;
  analyser.maxDecibels = opts.maxDecibels;
  analyser.fftSize = opts.fftSize;
  analyser.smoothingTimeConstant = opts.smoothingTimeConstant;
  mediaStreamSource.connect(analyser);
  const dataArray = new Uint8Array(analyser.frequencyBinCount);
  const calculateVolume = () => {
    analyser.getByteFrequencyData(dataArray);
    let sum = 0;
    for (const amplitude of dataArray) {
      sum += Math.pow(amplitude / 255, 2);
    }
    const volume = Math.sqrt(sum / dataArray.length);
    return volume;
  };
  const cleanup = () => __awaiter(this, void 0, void 0, function* () {
    yield audioContext.close();
    if (opts.cloneTrack) {
      streamTrack.stop();
    }
  });
  return {
    calculateVolume,
    analyser,
    cleanup
  };
}
function isVideoCodec(maybeCodec) {
  return videoCodecs.includes(maybeCodec);
}
function unwrapConstraint(constraint) {
  if (typeof constraint === "string" || typeof constraint === "number") {
    return constraint;
  }
  if (Array.isArray(constraint)) {
    return constraint[0];
  }
  if (constraint.exact !== void 0) {
    if (Array.isArray(constraint.exact)) {
      return constraint.exact[0];
    }
    return constraint.exact;
  }
  if (constraint.ideal !== void 0) {
    if (Array.isArray(constraint.ideal)) {
      return constraint.ideal[0];
    }
    return constraint.ideal;
  }
  throw Error("could not unwrap constraint");
}
function toWebsocketUrl(url) {
  if (url.startsWith("http")) {
    return url.replace(/^(http)/, "ws");
  }
  return url;
}
function toHttpUrl(url) {
  if (url.startsWith("ws")) {
    return url.replace(/^(ws)/, "http");
  }
  return url;
}
function extractTranscriptionSegments(transcription, firstReceivedTimesMap) {
  return transcription.segments.map((_ref) => {
    let id = _ref.id, text = _ref.text, language = _ref.language, startTime = _ref.startTime, endTime = _ref.endTime, final = _ref.final;
    var _a2;
    const firstReceivedTime = (_a2 = firstReceivedTimesMap.get(id)) !== null && _a2 !== void 0 ? _a2 : Date.now();
    const lastReceivedTime = Date.now();
    if (final) {
      firstReceivedTimesMap.delete(id);
    } else {
      firstReceivedTimesMap.set(id, firstReceivedTime);
    }
    return {
      id,
      text,
      startTime: Number.parseInt(startTime.toString()),
      endTime: Number.parseInt(endTime.toString()),
      final,
      language,
      firstReceivedTime,
      lastReceivedTime
    };
  });
}
function extractChatMessage(msg) {
  const id = msg.id, timestamp = msg.timestamp, message = msg.message, editTimestamp = msg.editTimestamp;
  return {
    id,
    timestamp: Number.parseInt(timestamp.toString()),
    editTimestamp: editTimestamp ? Number.parseInt(editTimestamp.toString()) : void 0,
    message
  };
}
function getDisconnectReasonFromConnectionError(e4) {
  switch (e4.reason) {
    case ConnectionErrorReason.LeaveRequest:
      return e4.context;
    case ConnectionErrorReason.Cancelled:
      return DisconnectReason.CLIENT_INITIATED;
    case ConnectionErrorReason.NotAllowed:
      return DisconnectReason.USER_REJECTED;
    case ConnectionErrorReason.ServerUnreachable:
      return DisconnectReason.JOIN_FAILURE;
    default:
      return DisconnectReason.UNKNOWN_REASON;
  }
}
function bigIntToNumber(value) {
  return value !== void 0 ? Number(value) : void 0;
}
function numberToBigInt(value) {
  return value !== void 0 ? BigInt(value) : void 0;
}
function isLocalTrack(track) {
  return !!track && !(track instanceof MediaStreamTrack) && track.isLocal;
}
function isAudioTrack(track) {
  return !!track && track.kind == Track.Kind.Audio;
}
function isVideoTrack(track) {
  return !!track && track.kind == Track.Kind.Video;
}
function isLocalVideoTrack(track) {
  return isLocalTrack(track) && isVideoTrack(track);
}
function isLocalAudioTrack(track) {
  return isLocalTrack(track) && isAudioTrack(track);
}
function isRemoteTrack(track) {
  return !!track && !track.isLocal;
}
function isRemotePub(pub) {
  return !!pub && !pub.isLocal;
}
function isRemoteVideoTrack(track) {
  return isRemoteTrack(track) && isVideoTrack(track);
}
function isLocalParticipant(p) {
  return p.isLocal;
}
function splitUtf8(s, n) {
  const result = [];
  let encoded = new TextEncoder().encode(s);
  while (encoded.length > n) {
    let k = n;
    while (k > 0) {
      const byte = encoded[k];
      if (byte !== void 0 && (byte & 192) !== 128) {
        break;
      }
      k--;
    }
    result.push(encoded.slice(0, k));
    encoded = encoded.slice(k);
  }
  if (encoded.length > 0) {
    result.push(encoded);
  }
  return result;
}
function readableFromBytes(bytes) {
  return new ReadableStream({
    start(controller) {
      controller.enqueue(bytes);
      controller.close();
    }
  });
}
function readBytesInChunks(source, chunkSize) {
  return __asyncGenerator(this, arguments, function* readBytesInChunks_1() {
    const reader = source.getReader();
    let buffer = new Uint8Array(0);
    try {
      while (true) {
        const _yield$__await = yield __await(reader.read()), done = _yield$__await.done, value = _yield$__await.value;
        if (done) {
          break;
        }
        if (value.byteLength === 0) {
          continue;
        }
        const merged = new Uint8Array(buffer.byteLength + value.byteLength);
        merged.set(buffer);
        merged.set(value, buffer.byteLength);
        buffer = merged;
        while (buffer.byteLength >= chunkSize) {
          yield yield __await(buffer.slice(0, chunkSize));
          buffer = buffer.slice(chunkSize);
        }
      }
      if (buffer.byteLength > 0) {
        yield yield __await(buffer);
      }
    } finally {
      reader.releaseLock();
    }
  });
}
function extractMaxAgeFromRequestHeaders(headers) {
  var _a2;
  const cacheControl = headers.get("Cache-Control");
  if (cacheControl) {
    const maxAge = (_a2 = cacheControl.match(/(?:^|[,\s])max-age=(\d+)/)) === null || _a2 === void 0 ? void 0 : _a2[1];
    if (maxAge) {
      return parseInt(maxAge, 10);
    }
  }
  return void 0;
}
function isCompressionStreamSupported() {
  return typeof CompressionStream !== "undefined";
}
function isPublisherOfferWithJoinSupported() {
  return isCompressionStreamSupported() && !isFireFox();
}
function createRtcUrl(url, searchParams) {
  let useV0Path = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : false;
  const v0Url = createV0RtcUrl(url, searchParams);
  if (useV0Path) {
    return v0Url;
  } else {
    return appendUrlPath(v0Url, "v1");
  }
}
function createV0RtcUrl(url, searchParams) {
  const urlObj = new URL(toWebsocketUrl(url));
  searchParams.forEach((value, key) => {
    urlObj.searchParams.set(key, value);
  });
  return appendUrlPath(urlObj, "rtc");
}
function createValidateUrl(rtcWsUrl) {
  const urlObj = new URL(toHttpUrl(rtcWsUrl));
  return appendUrlPath(urlObj, "validate");
}
function ensureTrailingSlash(path) {
  return path.endsWith("/") ? path : "".concat(path, "/");
}
function appendUrlPath(urlObj, path) {
  urlObj.pathname = "".concat(ensureTrailingSlash(urlObj.pathname)).concat(path);
  return urlObj;
}
function parseSignalResponse(value) {
  if (typeof value === "string") {
    return SignalResponse.fromJson(JSON.parse(value), {
      ignoreUnknownFields: true
    });
  } else if (value instanceof ArrayBuffer) {
    return SignalResponse.fromBinary(new Uint8Array(value));
  }
  throw new Error("could not decode websocket message: ".concat(typeof value));
}
function getAbortReasonAsString(signal) {
  let defaultMessage = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : "Unknown reason";
  if (!(signal instanceof AbortSignal)) {
    return defaultMessage;
  }
  const reason = signal.reason;
  switch (typeof reason) {
    case "string":
      return reason;
    case "object":
      return reason instanceof Error ? reason.message : defaultMessage;
    default:
      return "toString" in reason ? reason.toString() : defaultMessage;
  }
}
var DECRYPTION_FAILURE_TOLERANCE = 10;
var E2EE_FLAG = "lk_e2ee";
var SALT = "LKFrameEncryptionKey";
var KEY_PROVIDER_DEFAULTS = {
  sharedKey: false,
  ratchetSalt: SALT,
  ratchetWindowSize: 8,
  failureTolerance: DECRYPTION_FAILURE_TOLERANCE,
  keyringSize: 16,
  keySize: 128
};
var KeyProviderEvent;
(function(KeyProviderEvent2) {
  KeyProviderEvent2["SetKey"] = "setKey";
  KeyProviderEvent2["RatchetRequest"] = "ratchetRequest";
  KeyProviderEvent2["KeyRatcheted"] = "keyRatcheted";
})(KeyProviderEvent || (KeyProviderEvent = {}));
var KeyHandlerEvent;
(function(KeyHandlerEvent2) {
  KeyHandlerEvent2["KeyRatcheted"] = "keyRatcheted";
})(KeyHandlerEvent || (KeyHandlerEvent = {}));
var EncryptionEvent;
(function(EncryptionEvent2) {
  EncryptionEvent2["ParticipantEncryptionStatusChanged"] = "participantEncryptionStatusChanged";
  EncryptionEvent2["EncryptionError"] = "encryptionError";
})(EncryptionEvent || (EncryptionEvent = {}));
var CryptorEvent;
(function(CryptorEvent2) {
  CryptorEvent2["Error"] = "cryptorError";
})(CryptorEvent || (CryptorEvent = {}));
function isE2EESupported() {
  return isInsertableStreamSupported() || isScriptTransformSupported();
}
function isScriptTransformSupported() {
  return typeof window !== "undefined" && typeof window.RTCRtpScriptTransform !== "undefined";
}
function isInsertableStreamSupported() {
  return typeof window !== "undefined" && typeof window.RTCRtpSender !== "undefined" && // @ts-ignore
  typeof window.RTCRtpSender.prototype.createEncodedStreams !== "undefined";
}
function createKeyMaterialFromString(password) {
  return __awaiter(this, void 0, void 0, function* () {
    let enc = new TextEncoder();
    const keyMaterial = yield crypto.subtle.importKey("raw", enc.encode(password), {
      name: "PBKDF2"
    }, false, ["deriveBits", "deriveKey"]);
    return keyMaterial;
  });
}
function createKeyMaterialFromBuffer(cryptoBuffer) {
  return __awaiter(this, void 0, void 0, function* () {
    const keyMaterial = yield crypto.subtle.importKey("raw", cryptoBuffer, "HKDF", false, ["deriveBits", "deriveKey"]);
    return keyMaterial;
  });
}
function asEncryptablePacket(packet) {
  var _a2, _b2, _c, _d, _e2;
  if (((_a2 = packet.value) === null || _a2 === void 0 ? void 0 : _a2.case) !== "sipDtmf" && ((_b2 = packet.value) === null || _b2 === void 0 ? void 0 : _b2.case) !== "metrics" && ((_c = packet.value) === null || _c === void 0 ? void 0 : _c.case) !== "speaker" && ((_d = packet.value) === null || _d === void 0 ? void 0 : _d.case) !== "transcription" && ((_e2 = packet.value) === null || _e2 === void 0 ? void 0 : _e2.case) !== "encryptedPacket") {
    return new EncryptedPacketPayload({
      value: packet.value
    });
  }
  return void 0;
}
var BaseKeyProvider = class extends eventsExports.EventEmitter {
  constructor() {
    let options = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : {};
    super();
    this.latestManuallySetKeyIndex = 0;
    this.onKeyRatcheted = (ratchetResult, participantId, keyIndex) => {
      livekitLogger.debug("key ratcheted event received", {
        ratchetResult,
        participantId,
        keyIndex
      });
    };
    this.keyInfoMap = /* @__PURE__ */ new Map();
    this.options = Object.assign(Object.assign({}, KEY_PROVIDER_DEFAULTS), options);
    this.on(KeyProviderEvent.KeyRatcheted, this.onKeyRatcheted);
  }
  /**
   * callback to invoke once a key has been set for a participant
   * @param key
   * @param participantIdentity
   * @param keyIndex
   */
  onSetEncryptionKey(key, participantIdentity, keyIndex) {
    const keyInfo = {
      key,
      participantIdentity,
      keyIndex
    };
    if (!this.options.sharedKey && !participantIdentity) {
      throw new Error("participant identity needs to be passed for encryption key if sharedKey option is false");
    }
    this.keyInfoMap.set("".concat(participantIdentity !== null && participantIdentity !== void 0 ? participantIdentity : "shared", "-").concat(keyIndex !== null && keyIndex !== void 0 ? keyIndex : 0), keyInfo);
    if (keyIndex !== void 0) {
      this.latestManuallySetKeyIndex = keyIndex;
    }
    this.emit(KeyProviderEvent.SetKey, keyInfo, keyIndex !== void 0);
  }
  getKeys() {
    return Array.from(this.keyInfoMap.values());
  }
  getLatestManuallySetKeyIndex() {
    return this.latestManuallySetKeyIndex;
  }
  getOptions() {
    return this.options;
  }
  ratchetKey(participantIdentity, keyIndex) {
    this.emit(KeyProviderEvent.RatchetRequest, participantIdentity, keyIndex);
  }
};
var ExternalE2EEKeyProvider = class extends BaseKeyProvider {
  constructor() {
    let options = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : {};
    const opts = Object.assign(Object.assign({}, options), {
      sharedKey: true,
      // for a shared key provider failing to decrypt for a specific participant
      // should not mark the key as invalid, so we accept wrong keys forever
      // and won't try to auto-ratchet
      ratchetWindowSize: 0,
      failureTolerance: -1
    });
    super(opts);
  }
  /**
   * Accepts a passphrase that's used to create the crypto keys.
   * When passing in a string, PBKDF2 is used. (recommended for maximum compatibility across SDKs)
   * When passing in an ArrayBuffer of cryptographically random numbers, HKDF is used.
   *
   * Note: Not all client SDKS support HKDF.
   * @param key
   */
  setKey(key) {
    return __awaiter(this, void 0, void 0, function* () {
      const derivedKey = typeof key === "string" ? yield createKeyMaterialFromString(key) : yield createKeyMaterialFromBuffer(key);
      this.onSetEncryptionKey(derivedKey);
    });
  }
};
var CryptorErrorReason;
(function(CryptorErrorReason2) {
  CryptorErrorReason2[CryptorErrorReason2["InvalidKey"] = 0] = "InvalidKey";
  CryptorErrorReason2[CryptorErrorReason2["MissingKey"] = 1] = "MissingKey";
  CryptorErrorReason2[CryptorErrorReason2["InternalError"] = 2] = "InternalError";
})(CryptorErrorReason || (CryptorErrorReason = {}));
function shouldUseFrameMetadataScriptTransform() {
  return isScriptTransformSupportedForWorker();
}
function isFrameMetadataSupported(options) {
  return !!(options === null || options === void 0 ? void 0 : options.worker) && (isInsertableStreamSupported() || shouldUseFrameMetadataScriptTransform());
}
function hasFrameMetadataPublishOptions(options) {
  return !!((options === null || options === void 0 ? void 0 : options.timestamp) || (options === null || options === void 0 ? void 0 : options.frameId));
}
function getFrameMetadataFeatures(options) {
  const features = [];
  if (options === null || options === void 0 ? void 0 : options.timestamp) {
    features.push(PacketTrailerFeature.PTF_USER_TIMESTAMP);
  }
  if (options === null || options === void 0 ? void 0 : options.frameId) {
    features.push(PacketTrailerFeature.PTF_FRAME_ID);
  }
  return features;
}
function getFrameMetadataPublishOptions(features) {
  if (!features || features.length === 0) {
    return void 0;
  }
  const options = {};
  if (features.includes(PacketTrailerFeature.PTF_USER_TIMESTAMP)) {
    options.timestamp = true;
  }
  if (features.includes(PacketTrailerFeature.PTF_FRAME_ID)) {
    options.frameId = true;
  }
  return options.timestamp || options.frameId ? options : void 0;
}
function debounce(func) {
  let waitMilliseconds = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : 50;
  let options = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : {};
  var _a2, _b2;
  let timeoutId;
  const isImmediate = (_a2 = options.isImmediate) !== null && _a2 !== void 0 ? _a2 : false;
  const callback = (_b2 = options.callback) !== null && _b2 !== void 0 ? _b2 : false;
  const maxWait = options.maxWait;
  let lastInvokeTime = Date.now();
  let promises = [];
  function nextInvokeTimeout() {
    if (maxWait !== void 0) {
      const timeSinceLastInvocation = Date.now() - lastInvokeTime;
      if (timeSinceLastInvocation + waitMilliseconds >= maxWait) {
        return maxWait - timeSinceLastInvocation;
      }
    }
    return waitMilliseconds;
  }
  const debouncedFunction = function() {
    for (var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++) {
      args[_key] = arguments[_key];
    }
    const context = this;
    return new Promise((resolve, reject) => {
      const invokeFunction = function() {
        timeoutId = void 0;
        lastInvokeTime = Date.now();
        if (!isImmediate) {
          const result = func.apply(context, args);
          callback && callback(result);
          promises.forEach((_ref) => {
            let resolve2 = _ref.resolve;
            return resolve2(result);
          });
          promises = [];
        }
      };
      const shouldCallNow = isImmediate && timeoutId === void 0;
      if (timeoutId !== void 0) {
        CriticalTimers.clearTimeout(timeoutId);
      }
      timeoutId = CriticalTimers.setTimeout(invokeFunction, nextInvokeTimeout());
      if (shouldCallNow) {
        const result = func.apply(context, args);
        callback && callback(result);
        return resolve(result);
      }
      promises.push({
        resolve,
        reject
      });
    });
  };
  debouncedFunction.cancel = function(reason) {
    if (timeoutId !== void 0) {
      CriticalTimers.clearTimeout(timeoutId);
    }
    promises.forEach((_ref2) => {
      let reject = _ref2.reject;
      return reject(reason);
    });
    promises = [];
  };
  return debouncedFunction;
}
var monitorFrequency = 2e3;
function computeBitrate(currentStats, prevStats) {
  if (!prevStats) {
    return 0;
  }
  let bytesNow;
  let bytesPrev;
  if ("bytesReceived" in currentStats) {
    bytesNow = currentStats.bytesReceived;
    bytesPrev = prevStats.bytesReceived;
  } else if ("bytesSent" in currentStats) {
    bytesNow = currentStats.bytesSent;
    bytesPrev = prevStats.bytesSent;
  }
  if (bytesNow === void 0 || bytesPrev === void 0 || currentStats.timestamp === void 0 || prevStats.timestamp === void 0) {
    return 0;
  }
  return (bytesNow - bytesPrev) * 8 * 1e3 / (currentStats.timestamp - prevStats.timestamp);
}
var RemoteTrack = class extends Track {
  constructor(mediaTrack, sid, kind, receiver, loggerOptions) {
    super(mediaTrack, kind, loggerOptions);
    this.sid = sid;
    this.receiver = receiver;
  }
  get isLocal() {
    return false;
  }
  /** @internal */
  setMuted(muted) {
    if (this.isMuted !== muted) {
      this.isMuted = muted;
      this._mediaStreamTrack.enabled = !muted;
      this.emit(muted ? TrackEvent.Muted : TrackEvent.Unmuted, this);
    }
  }
  /** @internal */
  setMediaStream(stream) {
    this.mediaStream = stream;
    const onRemoveTrack = (event) => {
      if (event.track === this._mediaStreamTrack) {
        stream.removeEventListener("removetrack", onRemoveTrack);
        if (this.receiver && "playoutDelayHint" in this.receiver) {
          this.receiver.playoutDelayHint = void 0;
        }
        this.receiver = void 0;
        this._currentBitrate = 0;
        this.emit(TrackEvent.Ended, this);
      }
    };
    stream.addEventListener("removetrack", onRemoveTrack);
  }
  start() {
    this.startMonitor();
    super.enable();
  }
  stop() {
    this.stopMonitor();
    super.disable();
  }
  /**
   * Gets the RTCStatsReport for the RemoteTrack's underlying RTCRtpReceiver
   * See https://developer.mozilla.org/en-US/docs/Web/API/RTCStatsReport
   *
   * @returns Promise<RTCStatsReport> | undefined
   */
  getRTCStatsReport() {
    return __awaiter(this, void 0, void 0, function* () {
      var _a2;
      if (!((_a2 = this.receiver) === null || _a2 === void 0 ? void 0 : _a2.getStats)) {
        return;
      }
      const statsReport = yield this.receiver.getStats();
      return statsReport;
    });
  }
  /**
   * Allows to set a playout delay (in seconds) for this track.
   * A higher value allows for more buffering of the track in the browser
   * and will result in a delay of media being played back of `delayInSeconds`
   */
  setPlayoutDelay(delayInSeconds) {
    if (this.receiver) {
      if ("playoutDelayHint" in this.receiver) {
        this.receiver.playoutDelayHint = delayInSeconds;
      } else {
        this.log.warn("Playout delay not supported in this browser");
      }
    } else {
      this.log.warn("Cannot set playout delay, track already ended");
    }
  }
  /**
   * Returns the current playout delay (in seconds) of this track.
   */
  getPlayoutDelay() {
    if (this.receiver) {
      if ("playoutDelayHint" in this.receiver) {
        return this.receiver.playoutDelayHint;
      } else {
        this.log.warn("Playout delay not supported in this browser");
      }
    } else {
      this.log.warn("Cannot get playout delay, track already ended");
    }
    return 0;
  }
  /* @internal */
  startMonitor() {
    if (!this.monitorInterval) {
      this.monitorInterval = setInterval(() => this.monitorReceiver(), monitorFrequency);
    }
    if (supportsSynchronizationSources()) {
      this.registerTimeSyncUpdate();
    }
  }
  registerTimeSyncUpdate() {
    const loop = () => {
      var _a2;
      this.timeSyncHandle = requestAnimationFrame(() => loop());
      const sources = (_a2 = this.receiver) === null || _a2 === void 0 ? void 0 : _a2.getSynchronizationSources()[0];
      if (sources) {
        const timestamp = sources.timestamp, rtpTimestamp = sources.rtpTimestamp;
        if (rtpTimestamp && this.rtpTimestamp !== rtpTimestamp) {
          this.emit(TrackEvent.TimeSyncUpdate, {
            timestamp,
            rtpTimestamp
          });
          this.rtpTimestamp = rtpTimestamp;
        }
      }
    };
    loop();
  }
};
var REACTION_DELAY = 100;
var RemoteVideoTrack = class extends RemoteTrack {
  constructor(mediaTrack, sid, receiver, adaptiveStreamSettings, loggerOptions) {
    super(mediaTrack, sid, Track.Kind.Video, receiver, loggerOptions);
    this.elementInfos = [];
    this.monitorReceiver = () => __awaiter(this, void 0, void 0, function* () {
      if (!this.receiver) {
        this._currentBitrate = 0;
        return;
      }
      const stats = yield this.getReceiverStats();
      if (stats && this.prevStats && this.receiver) {
        this._currentBitrate = computeBitrate(stats, this.prevStats);
      }
      this.prevStats = stats;
    });
    this.debouncedHandleResize = debounce(() => {
      this.updateDimensions();
    }, REACTION_DELAY);
    this.adaptiveStreamSettings = adaptiveStreamSettings;
  }
  get isAdaptiveStream() {
    return this.adaptiveStreamSettings !== void 0;
  }
  /**
   * Look up frame-level metadata for a given RTP timestamp.
   * Use with the `TrackEvent.TimeSyncUpdate` event to correlate displayed frames
   * with their capture-time metadata.
   *
   * Requires the room to be configured with the `frameMetadata` worker option
   * and the publishing track to have frame metadata features enabled.
   *
   */
  lookupFrameMetadata(_ref) {
    let rtpTimestamp = _ref.rtpTimestamp;
    var _a2;
    return (_a2 = this.frameMetadataExtractor) === null || _a2 === void 0 ? void 0 : _a2.lookupMetadata(rtpTimestamp);
  }
  setStreamState(value) {
    super.setStreamState(value);
    this.log.debug("setStreamState", value);
    if (this.isAdaptiveStream && value === Track.StreamState.Active) {
      this.updateVisibility();
    }
  }
  /**
   * Note: When using adaptiveStream, you need to use remoteVideoTrack.attach() to add the track to a HTMLVideoElement, otherwise your video tracks might never start
   */
  get mediaStreamTrack() {
    return this._mediaStreamTrack;
  }
  /** @internal */
  setMuted(muted) {
    super.setMuted(muted);
    this.attachedElements.forEach((element) => {
      if (muted) {
        detachTrack(this._mediaStreamTrack, element);
      } else {
        attachToElement(this._mediaStreamTrack, element);
      }
    });
  }
  attach(element) {
    if (!element) {
      element = super.attach();
    } else {
      super.attach(element);
    }
    if (this.adaptiveStreamSettings && this.elementInfos.find((info) => info.element === element) === void 0) {
      const elementInfo = new HTMLElementInfo(element);
      this.observeElementInfo(elementInfo);
    }
    return element;
  }
  /**
   * Observe an ElementInfo for changes when adaptive streaming.
   * @param elementInfo
   * @internal
   */
  observeElementInfo(elementInfo) {
    if (this.adaptiveStreamSettings && this.elementInfos.find((info) => info === elementInfo) === void 0) {
      elementInfo.handleResize = () => {
        this.debouncedHandleResize();
      };
      elementInfo.handleVisibilityChanged = () => {
        this.updateVisibility();
      };
      this.elementInfos.push(elementInfo);
      elementInfo.observe();
      this.debouncedHandleResize();
      this.updateVisibility();
    } else {
      this.log.warn("visibility resize observer not triggered", this.logContext);
    }
  }
  /**
   * Stop observing an ElementInfo for changes.
   * @param elementInfo
   * @internal
   */
  stopObservingElementInfo(elementInfo) {
    if (!this.isAdaptiveStream) {
      this.log.warn("stopObservingElementInfo ignored", this.logContext);
      return;
    }
    const stopElementInfos = this.elementInfos.filter((info) => info === elementInfo);
    for (const info of stopElementInfos) {
      info.stopObserving();
    }
    this.elementInfos = this.elementInfos.filter((info) => info !== elementInfo);
    this.updateVisibility();
    this.debouncedHandleResize();
  }
  detach(element) {
    if (element) {
      this.stopObservingElement(element);
      return super.detach(element);
    }
    const detachedElements = super.detach();
    for (const e4 of detachedElements) {
      this.stopObservingElement(e4);
    }
    return detachedElements;
  }
  /** @internal */
  getDecoderImplementation() {
    var _a2;
    return (_a2 = this.prevStats) === null || _a2 === void 0 ? void 0 : _a2.decoderImplementation;
  }
  getReceiverStats() {
    return __awaiter(this, void 0, void 0, function* () {
      if (!this.receiver || !this.receiver.getStats) {
        return;
      }
      const stats = yield this.receiver.getStats();
      let receiverStats;
      let codecID = "";
      let codecs = /* @__PURE__ */ new Map();
      stats.forEach((v) => {
        if (v.type === "inbound-rtp") {
          codecID = v.codecId;
          receiverStats = {
            type: "video",
            streamId: v.id,
            framesDecoded: v.framesDecoded,
            framesDropped: v.framesDropped,
            framesReceived: v.framesReceived,
            packetsReceived: v.packetsReceived,
            packetsLost: v.packetsLost,
            frameWidth: v.frameWidth,
            frameHeight: v.frameHeight,
            pliCount: v.pliCount,
            firCount: v.firCount,
            nackCount: v.nackCount,
            jitter: v.jitter,
            timestamp: v.timestamp,
            bytesReceived: v.bytesReceived,
            decoderImplementation: v.decoderImplementation
          };
        } else if (v.type === "codec") {
          codecs.set(v.id, v);
        }
      });
      if (receiverStats && codecID !== "" && codecs.get(codecID)) {
        receiverStats.mimeType = codecs.get(codecID).mimeType;
      }
      return receiverStats;
    });
  }
  stopObservingElement(element) {
    const stopElementInfos = this.elementInfos.filter((info) => info.element === element);
    for (const info of stopElementInfos) {
      this.stopObservingElementInfo(info);
    }
  }
  handleAppVisibilityChanged() {
    const _super = Object.create(null, {
      handleAppVisibilityChanged: {
        get: () => super.handleAppVisibilityChanged
      }
    });
    return __awaiter(this, void 0, void 0, function* () {
      yield _super.handleAppVisibilityChanged.call(this);
      if (!this.isAdaptiveStream) return;
      this.updateVisibility();
    });
  }
  updateVisibility(forceEmit) {
    var _a2, _b2;
    const lastVisibilityChange = this.elementInfos.reduce((prev, info) => Math.max(prev, info.visibilityChangedAt || 0), 0);
    const backgroundPause = ((_b2 = (_a2 = this.adaptiveStreamSettings) === null || _a2 === void 0 ? void 0 : _a2.pauseVideoInBackground) !== null && _b2 !== void 0 ? _b2 : true) ? this.isInBackground : false;
    const isPiPMode = this.elementInfos.some((info) => info.pictureInPicture);
    const isVisible = this.elementInfos.some((info) => info.visible) && !backgroundPause || isPiPMode;
    if (this.lastVisible === isVisible && !forceEmit) {
      return;
    }
    if (!isVisible && Date.now() - lastVisibilityChange < REACTION_DELAY) {
      CriticalTimers.setTimeout(() => {
        this.updateVisibility();
      }, REACTION_DELAY);
      return;
    }
    this.lastVisible = isVisible;
    this.emit(TrackEvent.VisibilityChanged, isVisible, this);
  }
  updateDimensions() {
    var _a2, _b2;
    let maxWidth = 0;
    let maxHeight = 0;
    const pixelDensity = this.getPixelDensity();
    for (const info of this.elementInfos) {
      const currentElementWidth = info.width() * pixelDensity;
      const currentElementHeight = info.height() * pixelDensity;
      if (currentElementWidth + currentElementHeight > maxWidth + maxHeight) {
        maxWidth = currentElementWidth;
        maxHeight = currentElementHeight;
      }
    }
    if (((_a2 = this.lastDimensions) === null || _a2 === void 0 ? void 0 : _a2.width) === maxWidth && ((_b2 = this.lastDimensions) === null || _b2 === void 0 ? void 0 : _b2.height) === maxHeight) {
      return;
    }
    this.lastDimensions = {
      width: maxWidth,
      height: maxHeight
    };
    this.emit(TrackEvent.VideoDimensionsChanged, this.lastDimensions, this);
  }
  getPixelDensity() {
    var _a2;
    const pixelDensity = (_a2 = this.adaptiveStreamSettings) === null || _a2 === void 0 ? void 0 : _a2.pixelDensity;
    if (pixelDensity === "screen") {
      return getDevicePixelRatio();
    } else if (!pixelDensity) {
      const devicePixelRatio = getDevicePixelRatio();
      if (devicePixelRatio > 2) {
        return 2;
      } else {
        return 1;
      }
    }
    return pixelDensity;
  }
};
var HTMLElementInfo = class {
  get visible() {
    return this.isPiP || this.isIntersecting;
  }
  get pictureInPicture() {
    return this.isPiP;
  }
  constructor(element, visible) {
    this.onVisibilityChanged = (entry) => {
      var _a2;
      const target = entry.target, isIntersecting = entry.isIntersecting;
      if (target === this.element) {
        this.isIntersecting = isIntersecting;
        this.isPiP = isElementInPiP(this.element);
        this.visibilityChangedAt = Date.now();
        (_a2 = this.handleVisibilityChanged) === null || _a2 === void 0 ? void 0 : _a2.call(this);
      }
    };
    this.onEnterPiP = () => {
      var _a2, _b2;
      (_b2 = (_a2 = window.documentPictureInPicture) === null || _a2 === void 0 ? void 0 : _a2.window) === null || _b2 === void 0 ? void 0 : _b2.addEventListener("pagehide", this.onLeavePiP);
      queueMicrotask(() => {
        requestAnimationFrame(() => {
          var _a3;
          this.isPiP = isElementInPiP(this.element);
          (_a3 = this.handleVisibilityChanged) === null || _a3 === void 0 ? void 0 : _a3.call(this);
        });
      });
    };
    this.onLeavePiP = () => {
      var _a2;
      this.isPiP = isElementInPiP(this.element);
      (_a2 = this.handleVisibilityChanged) === null || _a2 === void 0 ? void 0 : _a2.call(this);
    };
    this.element = element;
    this.isIntersecting = visible !== null && visible !== void 0 ? visible : isElementInViewport(element);
    this.isPiP = isWeb() && isElementInPiP(element);
    this.visibilityChangedAt = 0;
  }
  width() {
    return this.element.clientWidth;
  }
  height() {
    return this.element.clientHeight;
  }
  observe() {
    var _a2, _b2, _c;
    this.isIntersecting = isElementInViewport(this.element);
    this.isPiP = isElementInPiP(this.element);
    this.element.handleResize = () => {
      var _a3;
      (_a3 = this.handleResize) === null || _a3 === void 0 ? void 0 : _a3.call(this);
    };
    this.element.handleVisibilityChanged = this.onVisibilityChanged;
    getIntersectionObserver().observe(this.element);
    getResizeObserver().observe(this.element);
    this.element.addEventListener("enterpictureinpicture", this.onEnterPiP);
    this.element.addEventListener("leavepictureinpicture", this.onLeavePiP);
    (_a2 = window.documentPictureInPicture) === null || _a2 === void 0 ? void 0 : _a2.addEventListener("enter", this.onEnterPiP);
    (_c = (_b2 = window.documentPictureInPicture) === null || _b2 === void 0 ? void 0 : _b2.window) === null || _c === void 0 ? void 0 : _c.addEventListener("pagehide", this.onLeavePiP);
  }
  stopObserving() {
    var _a2, _b2, _c, _d, _e2;
    (_a2 = getIntersectionObserver()) === null || _a2 === void 0 ? void 0 : _a2.unobserve(this.element);
    (_b2 = getResizeObserver()) === null || _b2 === void 0 ? void 0 : _b2.unobserve(this.element);
    this.element.removeEventListener("enterpictureinpicture", this.onEnterPiP);
    this.element.removeEventListener("leavepictureinpicture", this.onLeavePiP);
    (_c = window.documentPictureInPicture) === null || _c === void 0 ? void 0 : _c.removeEventListener("enter", this.onEnterPiP);
    (_e2 = (_d = window.documentPictureInPicture) === null || _d === void 0 ? void 0 : _d.window) === null || _e2 === void 0 ? void 0 : _e2.removeEventListener("pagehide", this.onLeavePiP);
  }
};
function isElementInPiP(el) {
  var _a2, _b2;
  if (document.pictureInPictureElement === el) return true;
  if ((_a2 = window.documentPictureInPicture) === null || _a2 === void 0 ? void 0 : _a2.window) return isElementInViewport(el, (_b2 = window.documentPictureInPicture) === null || _b2 === void 0 ? void 0 : _b2.window);
  return false;
}
function isElementInViewport(el, win) {
  const viewportWindow = win || window;
  let top = el.offsetTop;
  let left = el.offsetLeft;
  const width = el.offsetWidth;
  const height = el.offsetHeight;
  const _el = el, hidden = _el.hidden;
  const _getComputedStyle = getComputedStyle(el), display = _getComputedStyle.display;
  while (el.offsetParent) {
    el = el.offsetParent;
    top += el.offsetTop;
    left += el.offsetLeft;
  }
  return top < viewportWindow.pageYOffset + viewportWindow.innerHeight && left < viewportWindow.pageXOffset + viewportWindow.innerWidth && top + height > viewportWindow.pageYOffset && left + width > viewportWindow.pageXOffset && !hidden && display !== "none";
}
var E2EEManager = class extends eventsExports.EventEmitter {
  constructor(options, dcEncryptionEnabled) {
    super();
    this.decryptDataRequests = /* @__PURE__ */ new Map();
    this.encryptDataRequests = /* @__PURE__ */ new Map();
    this.onWorkerMessage = (ev) => {
      var _a2, _b2;
      const _ev$data = ev.data, kind = _ev$data.kind, data = _ev$data.data;
      switch (kind) {
        case "error":
          livekitLogger.error(data.error.message);
          if (data.uuid) {
            const decryptFuture2 = this.decryptDataRequests.get(data.uuid);
            if (decryptFuture2 === null || decryptFuture2 === void 0 ? void 0 : decryptFuture2.reject) {
              decryptFuture2.reject(data.error);
              break;
            }
            const encryptFuture2 = this.encryptDataRequests.get(data.uuid);
            if (encryptFuture2 === null || encryptFuture2 === void 0 ? void 0 : encryptFuture2.reject) {
              encryptFuture2.reject(data.error);
              break;
            }
          }
          this.emit(EncryptionEvent.EncryptionError, data.error, data.participantIdentity);
          break;
        case "initAck":
          if (data.enabled) {
            this.keyProvider.getKeys().forEach((keyInfo) => {
              this.postKey(keyInfo, false);
            });
          }
          break;
        case "enable":
          if (data.enabled) {
            this.keyProvider.getKeys().forEach((keyInfo) => {
              this.postKey(keyInfo, false);
            });
          }
          if (this.encryptionEnabled !== data.enabled && data.participantIdentity === ((_a2 = this.room) === null || _a2 === void 0 ? void 0 : _a2.localParticipant.identity)) {
            this.emit(EncryptionEvent.ParticipantEncryptionStatusChanged, data.enabled, this.room.localParticipant);
            this.encryptionEnabled = data.enabled;
          } else if (data.participantIdentity) {
            const participant = (_b2 = this.room) === null || _b2 === void 0 ? void 0 : _b2.getParticipantByIdentity(data.participantIdentity);
            if (!participant) {
              throw TypeError("couldn't set encryption status, participant not found".concat(data.participantIdentity));
            }
            this.emit(EncryptionEvent.ParticipantEncryptionStatusChanged, data.enabled, participant);
          }
          break;
        case "ratchetKey":
          this.keyProvider.emit(KeyProviderEvent.KeyRatcheted, data.ratchetResult, data.participantIdentity, data.keyIndex);
          break;
        case "decryptDataResponse":
          const decryptFuture = this.decryptDataRequests.get(data.uuid);
          if (decryptFuture === null || decryptFuture === void 0 ? void 0 : decryptFuture.resolve) {
            decryptFuture.resolve(data);
          }
          break;
        case "encryptDataResponse":
          const encryptFuture = this.encryptDataRequests.get(data.uuid);
          if (encryptFuture === null || encryptFuture === void 0 ? void 0 : encryptFuture.resolve) {
            encryptFuture.resolve(data);
          }
          break;
        case "packetTrailerMetadata":
          this.handleFrameMetadata(data.trackId, data.rtpTimestamp, data.ssrc, data.metadata);
          break;
      }
    };
    this.onWorkerError = (ev) => {
      livekitLogger.error("e2ee worker encountered an error:", {
        error: ev.error
      });
      this.emit(EncryptionEvent.EncryptionError, ev.error, void 0);
    };
    this.keyProvider = options.keyProvider;
    this.worker = options.worker;
    this.encryptionEnabled = false;
    this.dataChannelEncryptionEnabled = dcEncryptionEnabled;
  }
  get isEnabled() {
    return this.encryptionEnabled;
  }
  get isDataChannelEncryptionEnabled() {
    return this.isEnabled && this.dataChannelEncryptionEnabled;
  }
  /**
   * @internal
   */
  setup(room) {
    if (!isE2EESupported()) {
      throw new DeviceUnsupportedError("tried to setup end-to-end encryption on an unsupported browser");
    }
    livekitLogger.info("setting up e2ee");
    if (room !== this.room) {
      this.room = room;
      this.setupEventListeners(room, this.keyProvider);
      const msg = {
        kind: "init",
        data: {
          keyProviderOptions: this.keyProvider.getOptions(),
          loglevel: workerLogger.getLevel()
        }
      };
      if (this.worker) {
        livekitLogger.info("initializing worker", {
          worker: this.worker
        });
        this.worker.onmessage = this.onWorkerMessage;
        this.worker.onerror = this.onWorkerError;
        this.worker.postMessage(msg);
      }
    }
  }
  /**
   * @internal
   */
  setParticipantCryptorEnabled(enabled, participantIdentity) {
    livekitLogger.debug("set e2ee to ".concat(enabled, " for participant ").concat(participantIdentity));
    this.postEnable(enabled, participantIdentity);
  }
  /**
   * @internal
   */
  setSifTrailer(trailer) {
    if (!trailer || trailer.length === 0) {
      livekitLogger.warn("ignoring server sent trailer as it's empty");
    } else {
      this.postSifTrailer(trailer);
    }
  }
  handleFrameMetadata(trackId, rtpTimestamp, ssrc, metadata) {
    if (!this.room) {
      return;
    }
    for (const participant of [this.room.localParticipant, ...this.room.remoteParticipants.values()]) {
      for (const pub of participant.trackPublications.values()) {
        if (pub.track && pub.track.mediaStreamID === trackId && pub.track instanceof RemoteVideoTrack && pub.track.frameMetadataExtractor) {
          pub.track.frameMetadataExtractor.storeMetadata(rtpTimestamp, ssrc, metadata);
          return;
        }
      }
    }
  }
  setupEngine(engine) {
    engine.on(EngineEvent.RTPVideoMapUpdate, (rtpMap) => {
      this.postRTPMap(rtpMap);
    });
  }
  setupEventListeners(room, keyProvider) {
    room.on(RoomEvent.TrackPublished, (pub, participant) => this.setParticipantCryptorEnabled(pub.trackInfo.encryption !== Encryption_Type.NONE, participant.identity));
    room.on(RoomEvent.ConnectionStateChanged, (state) => {
      if (state === ConnectionState.Connected) {
        room.remoteParticipants.forEach((participant) => {
          participant.trackPublications.forEach((pub) => {
            this.setParticipantCryptorEnabled(pub.trackInfo.encryption !== Encryption_Type.NONE, participant.identity);
          });
        });
      }
    }).on(RoomEvent.TrackUnsubscribed, (track, _3, participant) => {
      var _a2;
      const msg = {
        kind: "removeTransform",
        data: {
          participantIdentity: participant.identity,
          trackId: track.mediaStreamID
        }
      };
      (_a2 = this.worker) === null || _a2 === void 0 ? void 0 : _a2.postMessage(msg);
    }).on(RoomEvent.TrackSubscribed, (track, pub, participant) => {
      this.setupE2EEReceiver(track, participant.identity, pub.trackInfo);
    }).on(RoomEvent.SignalConnected, () => {
      if (!this.room) {
        throw new TypeError("expected room to be present on signal connect");
      }
      const latestKeyIndex = keyProvider.getLatestManuallySetKeyIndex();
      keyProvider.getKeys().forEach((keyInfo) => {
        var _a2;
        this.postKey(keyInfo, latestKeyIndex === ((_a2 = keyInfo.keyIndex) !== null && _a2 !== void 0 ? _a2 : 0));
      });
      this.setParticipantCryptorEnabled(this.room.localParticipant.isE2EEEnabled, this.room.localParticipant.identity);
    });
    room.localParticipant.on(ParticipantEvent.LocalSenderCreated, (sender, track) => __awaiter(this, void 0, void 0, function* () {
      this.setupE2EESender(track, sender);
    }));
    room.localParticipant.on(ParticipantEvent.LocalTrackPublished, (publication) => {
      if (!isVideoTrack(publication.track) || !isSafariBased()) {
        return;
      }
      const msg = {
        kind: "updateCodec",
        data: {
          trackId: publication.track.mediaStreamID,
          codec: mimeTypeToVideoCodecString(publication.trackInfo.codecs[0].mimeType),
          participantIdentity: this.room.localParticipant.identity,
          hasPacketTrailer: false
        }
      };
      this.worker.postMessage(msg);
    });
    keyProvider.on(KeyProviderEvent.SetKey, (keyInfo, updateCurrentKeyIndex) => this.postKey(keyInfo, updateCurrentKeyIndex !== null && updateCurrentKeyIndex !== void 0 ? updateCurrentKeyIndex : true)).on(KeyProviderEvent.RatchetRequest, (participantId, keyIndex) => this.postRatchetRequest(participantId, keyIndex));
  }
  encryptData(data) {
    return __awaiter(this, void 0, void 0, function* () {
      if (!this.worker) {
        throw Error("could not encrypt data, worker is missing");
      }
      const uuid = crypto.randomUUID();
      const msg = {
        kind: "encryptDataRequest",
        data: {
          uuid,
          payload: data,
          participantIdentity: this.room.localParticipant.identity
        }
      };
      const future = new Future();
      future.onFinally = () => {
        this.encryptDataRequests.delete(uuid);
      };
      this.encryptDataRequests.set(uuid, future);
      this.worker.postMessage(msg);
      return future.promise;
    });
  }
  handleEncryptedData(payload, iv, participantIdentity, keyIndex) {
    if (!this.worker) {
      throw Error("could not handle encrypted data, worker is missing");
    }
    const uuid = crypto.randomUUID();
    const msg = {
      kind: "decryptDataRequest",
      data: {
        uuid,
        payload,
        iv,
        participantIdentity,
        keyIndex
      }
    };
    const future = new Future();
    future.onFinally = () => {
      this.decryptDataRequests.delete(uuid);
    };
    this.decryptDataRequests.set(uuid, future);
    this.worker.postMessage(msg);
    return future.promise;
  }
  postRatchetRequest(participantIdentity, keyIndex) {
    if (!this.worker) {
      throw Error("could not ratchet key, worker is missing");
    }
    const msg = {
      kind: "ratchetRequest",
      data: {
        participantIdentity,
        keyIndex
      }
    };
    this.worker.postMessage(msg);
  }
  postKey(_ref, updateCurrentKeyIndex) {
    let key = _ref.key, participantIdentity = _ref.participantIdentity, keyIndex = _ref.keyIndex;
    var _a2;
    if (!this.worker) {
      throw Error("could not set key, worker is missing");
    }
    const msg = {
      kind: "setKey",
      data: {
        participantIdentity,
        isPublisher: participantIdentity === ((_a2 = this.room) === null || _a2 === void 0 ? void 0 : _a2.localParticipant.identity),
        key,
        keyIndex,
        updateCurrentKeyIndex
      }
    };
    this.worker.postMessage(msg);
  }
  postEnable(enabled, participantIdentity) {
    if (this.worker) {
      const enableMsg = {
        kind: "enable",
        data: {
          enabled,
          participantIdentity
        }
      };
      this.worker.postMessage(enableMsg);
    } else {
      throw new ReferenceError("failed to enable e2ee, worker is not ready");
    }
  }
  postRTPMap(map) {
    var _a2;
    if (!this.worker) {
      throw TypeError("could not post rtp map, worker is missing");
    }
    if (!((_a2 = this.room) === null || _a2 === void 0 ? void 0 : _a2.localParticipant.identity)) {
      throw TypeError("could not post rtp map, local participant identity is missing");
    }
    const msg = {
      kind: "setRTPMap",
      data: {
        map,
        participantIdentity: this.room.localParticipant.identity
      }
    };
    this.worker.postMessage(msg);
  }
  postSifTrailer(trailer) {
    if (!this.worker) {
      throw Error("could not post SIF trailer, worker is missing");
    }
    const msg = {
      kind: "setSifTrailer",
      data: {
        trailer
      }
    };
    this.worker.postMessage(msg);
  }
  setupE2EEReceiver(track, remoteId, trackInfo) {
    if (!track.receiver) {
      return;
    }
    if (!(trackInfo === null || trackInfo === void 0 ? void 0 : trackInfo.mimeType) || trackInfo.mimeType === "") {
      throw new TypeError("MimeType missing from trackInfo, cannot set up E2EE cryptor");
    }
    const hasPacketTrailer = track.kind === "video" && !!trackInfo.packetTrailerFeatures && trackInfo.packetTrailerFeatures.length > 0;
    this.handleReceiver(track.receiver, track.mediaStreamID, remoteId, track.kind === "video" ? mimeTypeToVideoCodecString(trackInfo.mimeType) : void 0, hasPacketTrailer);
  }
  setupE2EESender(track, sender) {
    var _a2, _b2, _c;
    if (!isLocalTrack(track) || !sender) {
      if (!sender) livekitLogger.warn("early return because sender is not ready");
      return;
    }
    this.handleSender(sender, track.mediaStreamID, void 0, isVideoTrack(track) ? (_b2 = (_a2 = track.publishOptions) === null || _a2 === void 0 ? void 0 : _a2.frameMetadata) !== null && _b2 !== void 0 ? _b2 : (_c = track.publishOptions) === null || _c === void 0 ? void 0 : _c.packetTrailer : void 0);
  }
  /**
   * Handles the given {@code RTCRtpReceiver} by creating a {@code TransformStream} which will inject
   * a frame decoder.
   *
   */
  handleReceiver(receiver, trackId, participantIdentity, codec, hasPacketTrailer) {
    return __awaiter(this, void 0, void 0, function* () {
      if (!this.worker) {
        return;
      }
      if (isScriptTransformSupportedForWorker()) {
        const options = {
          kind: "decode",
          participantIdentity,
          trackId,
          codec,
          hasPacketTrailer
        };
        receiver.transform = new RTCRtpScriptTransform(this.worker, options);
      } else {
        if (E2EE_FLAG in receiver && codec) {
          const msg2 = {
            kind: "updateCodec",
            data: {
              trackId,
              codec,
              participantIdentity,
              hasPacketTrailer
            }
          };
          this.worker.postMessage(msg2);
          return;
        }
        let writable = receiver.writableStream;
        let readable = receiver.readableStream;
        if (!writable || !readable) {
          const receiverStreams = receiver.createEncodedStreams();
          receiver.writableStream = receiverStreams.writable;
          writable = receiverStreams.writable;
          receiver.readableStream = receiverStreams.readable;
          readable = receiverStreams.readable;
        }
        const msg = {
          kind: "decode",
          data: {
            readableStream: readable,
            writableStream: writable,
            trackId,
            codec,
            participantIdentity,
            isReuse: E2EE_FLAG in receiver,
            hasPacketTrailer
          }
        };
        this.worker.postMessage(msg, [readable, writable]);
      }
      receiver[E2EE_FLAG] = true;
    });
  }
  /**
   * Handles the given {@code RTCRtpSender} by creating a {@code TransformStream} which will inject
   * a frame encoder.
   *
   */
  handleSender(sender, trackId, codec, frameMetadata) {
    var _a2;
    if (E2EE_FLAG in sender || !this.worker) {
      return;
    }
    if (!((_a2 = this.room) === null || _a2 === void 0 ? void 0 : _a2.localParticipant.identity) || this.room.localParticipant.identity === "") {
      throw TypeError("local identity needs to be known in order to set up encrypted sender");
    }
    if (isScriptTransformSupportedForWorker()) {
      livekitLogger.info("initialize script transform");
      const options = {
        kind: "encode",
        participantIdentity: this.room.localParticipant.identity,
        trackId,
        codec,
        hasPacketTrailer: hasFrameMetadataPublishOptions(frameMetadata),
        packetTrailer: frameMetadata
      };
      sender.transform = new RTCRtpScriptTransform(this.worker, options);
    } else {
      livekitLogger.info("initialize encoded streams");
      const senderStreams = sender.createEncodedStreams();
      const msg = {
        kind: "encode",
        data: {
          readableStream: senderStreams.readable,
          writableStream: senderStreams.writable,
          codec,
          trackId,
          participantIdentity: this.room.localParticipant.identity,
          isReuse: false,
          hasPacketTrailer: hasFrameMetadataPublishOptions(frameMetadata),
          packetTrailer: frameMetadata
        }
      };
      this.worker.postMessage(msg, [senderStreams.readable, senderStreams.writable]);
    }
    sender[E2EE_FLAG] = true;
  }
};
var MAX_ENTRIES = 300;
var FrameMetadataExtractor = class {
  constructor() {
    this.metadataMap = /* @__PURE__ */ new Map();
    this.activeSsrc = 0;
  }
  storeMetadata(rtpTimestamp, ssrc, metadata) {
    if (this.activeSsrc !== 0 && this.activeSsrc !== ssrc) {
      this.metadataMap.clear();
    }
    this.activeSsrc = ssrc;
    while (this.metadataMap.size >= MAX_ENTRIES) {
      const evicted = this.metadataMap.keys().next().value;
      this.metadataMap.delete(evicted);
    }
    this.metadataMap.set(rtpTimestamp, metadata);
  }
  lookupMetadata(rtpTimestamp) {
    return this.metadataMap.get(rtpTimestamp);
  }
  dispose() {
    this.metadataMap.clear();
    this.activeSsrc = 0;
  }
};
var FrameMetadataManager = class {
  constructor(options) {
    this.extractors = /* @__PURE__ */ new Map();
    this.workerPipelines = /* @__PURE__ */ new Map();
    this.onWorkerMessage = (ev) => {
      const msg = ev.data;
      if (msg.kind === "metadata") {
        const extractor = this.extractors.get(msg.data.trackId);
        if (extractor) {
          extractor.storeMetadata(msg.data.rtpTimestamp, msg.data.ssrc, msg.data.metadata);
        }
      }
    };
    this.onWorkerError = (ev) => {
      livekitLogger.error("frame metadata worker encountered an error:", {
        error: ev.error
      });
    };
    this.worker = options === null || options === void 0 ? void 0 : options.worker;
  }
  /** @internal */
  setup(room) {
    if (room === this.room) {
      return;
    }
    this.room = room;
    if (this.worker) {
      this.worker.onmessage = this.onWorkerMessage;
      this.worker.onerror = this.onWorkerError;
      this.worker.postMessage({
        kind: "init"
      });
    }
    room.on(RoomEvent.TrackSubscribed, (track, pub, _participant) => {
      if (track.kind !== "video") {
        return;
      }
      this.setupReceiver(track, pub.trackInfo);
    }).on(RoomEvent.TrackUnsubscribed, (track) => {
      this.teardownTrack(track);
    }).on(RoomEvent.Disconnected, () => {
      this.cleanup();
    });
  }
  setupReceiver(track, trackInfo) {
    var _a2, _b2, _c;
    const receiver = track.receiver;
    if (!receiver) {
      return;
    }
    const hasFeatures = !!(trackInfo === null || trackInfo === void 0 ? void 0 : trackInfo.packetTrailerFeatures) && trackInfo.packetTrailerFeatures.length > 0;
    if (!hasFeatures) {
      if (!((_a2 = this.room) === null || _a2 === void 0 ? void 0 : _a2.hasE2EESetup)) {
        this.setupPassthroughReceiver(receiver, track.mediaStreamID);
      }
      return;
    }
    if (!isFrameMetadataSupported(this.worker ? {
      worker: this.worker
    } : void 0) && !((_b2 = this.room) === null || _b2 === void 0 ? void 0 : _b2.hasE2EESetup)) {
      livekitLogger.warn("frame metadata transform not supported; skipping extraction");
      return;
    }
    const extractor = new FrameMetadataExtractor();
    const trackId = track.mediaStreamID;
    this.extractors.set(trackId, extractor);
    track.frameMetadataExtractor = extractor;
    if ((_c = this.room) === null || _c === void 0 ? void 0 : _c.hasE2EESetup) {
      return;
    }
    this.setupWorkerReceiver(receiver, trackId, true);
  }
  setupPassthroughReceiver(receiver, trackId) {
    if (shouldUseFrameMetadataScriptTransform()) {
      if ("transform" in receiver) {
        receiver.transform = null;
      }
      return;
    }
    if (this.worker && isFrameMetadataSupported({
      worker: this.worker
    }) && !this.workerPipelines.has(receiver)) {
      this.setupWorkerReceiver(receiver, trackId, false);
      return;
    }
    if (this.worker && this.workerPipelines.has(receiver)) {
      this.setupWorkerReceiver(receiver, trackId, false);
    }
  }
  setupWorkerReceiver(receiver, newTrackId) {
    let hasPacketTrailer = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : true;
    const worker = this.worker;
    if (!worker) {
      return;
    }
    if (shouldUseFrameMetadataScriptTransform()) {
      receiver.transform = new RTCRtpScriptTransform(worker, {
        kind: "decode",
        trackId: newTrackId
      });
      return;
    }
    const existingTrackId = this.workerPipelines.get(receiver);
    if (existingTrackId) {
      const msg2 = {
        kind: "updateTrackId",
        data: {
          oldTrackId: existingTrackId,
          newTrackId,
          hasPacketTrailer
        }
      };
      worker.postMessage(msg2);
      this.workerPipelines.set(receiver, newTrackId);
      return;
    }
    if (!("createEncodedStreams" in receiver)) {
      livekitLogger.warn("createEncodedStreams not supported");
      return;
    }
    let streams;
    try {
      streams = receiver.createEncodedStreams();
    } catch (err) {
      livekitLogger.warn("failed to create encoded streams", {
        error: err
      });
      return;
    }
    const msg = {
      kind: "decode",
      data: {
        readableStream: streams.readable,
        writableStream: streams.writable,
        trackId: newTrackId,
        hasPacketTrailer
      }
    };
    worker.postMessage(msg, [streams.readable, streams.writable]);
    this.workerPipelines.set(receiver, newTrackId);
  }
  teardownTrack(track) {
    const trackId = track.mediaStreamID;
    const extractor = this.extractors.get(trackId);
    if (extractor) {
      extractor.dispose();
      this.extractors.delete(trackId);
    }
    if (track instanceof RemoteVideoTrack) {
      track.frameMetadataExtractor = void 0;
    }
  }
  cleanup() {
    var _a2;
    for (const extractor of this.extractors.values()) {
      extractor.dispose();
    }
    this.extractors.clear();
    this.workerPipelines.clear();
    (_a2 = this.worker) === null || _a2 === void 0 ? void 0 : _a2.terminate();
  }
};
var CONNECTION_BACKOFF_MIN_MS = 500;
var CONNECTION_BACKOFF_MAX_MS = 15e3;
var BackOffStrategy = class _BackOffStrategy {
  // eslint-disable-next-line @typescript-eslint/no-empty-function
  constructor() {
    this.failedConnectionAttempts = /* @__PURE__ */ new Map();
    this.backOffPromises = /* @__PURE__ */ new Map();
  }
  static getInstance() {
    if (!this._instance) {
      this._instance = new _BackOffStrategy();
    }
    return this._instance;
  }
  addFailedConnectionAttempt(urlString) {
    var _a2;
    const url = new URL(urlString);
    const projectName = extractProjectFromUrl(url);
    if (!projectName) {
      return;
    }
    let failureCount = (_a2 = this.failedConnectionAttempts.get(projectName)) !== null && _a2 !== void 0 ? _a2 : 0;
    this.failedConnectionAttempts.set(projectName, failureCount + 1);
    this.backOffPromises.set(projectName, sleep(Math.min(CONNECTION_BACKOFF_MIN_MS * Math.pow(2, failureCount), CONNECTION_BACKOFF_MAX_MS)));
  }
  getBackOffPromise(urlString) {
    const url = new URL(urlString);
    const projectName = url && extractProjectFromUrl(url);
    const backoffPromise = projectName && this.backOffPromises.get(projectName);
    return backoffPromise || Promise.resolve();
  }
  resetFailedConnectionAttempts(urlString) {
    const url = new URL(urlString);
    const projectName = url && extractProjectFromUrl(url);
    if (projectName) {
      this.failedConnectionAttempts.set(projectName, 0);
      this.backOffPromises.set(projectName, Promise.resolve());
    }
  }
  resetAll() {
    this.backOffPromises.clear();
    this.failedConnectionAttempts.clear();
  }
};
BackOffStrategy._instance = null;
var defaultId = "default";
var DeviceManager = class _DeviceManager {
  constructor() {
    this._previousDevices = [];
  }
  static getInstance() {
    if (this.instance === void 0) {
      this.instance = new _DeviceManager();
    }
    return this.instance;
  }
  get previousDevices() {
    return this._previousDevices;
  }
  getDevices(kind_1) {
    return __awaiter(this, arguments, void 0, function(kind) {
      var _this = this;
      let requestPermissions = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : true;
      return function* () {
        var _a2;
        if (((_a2 = _DeviceManager.userMediaPromiseMap) === null || _a2 === void 0 ? void 0 : _a2.size) > 0) {
          livekitLogger.debug("awaiting getUserMedia promise");
          try {
            if (kind) {
              yield _DeviceManager.userMediaPromiseMap.get(kind);
            } else {
              yield Promise.all(_DeviceManager.userMediaPromiseMap.values());
            }
          } catch (e4) {
            livekitLogger.warn("error waiting for media permissons");
          }
        }
        let devices = yield navigator.mediaDevices.enumerateDevices();
        if (requestPermissions && // for safari we need to skip this check, as otherwise it will re-acquire user media and fail on iOS https://bugs.webkit.org/show_bug.cgi?id=179363
        !(isSafari() && _this.hasDeviceInUse(kind))) {
          const isDummyDeviceOrEmpty = devices.filter((d) => d.kind === kind).length === 0 || devices.some((device) => {
            const noLabel = device.label === "";
            const isRelevant = kind ? device.kind === kind : true;
            return noLabel && isRelevant;
          });
          if (isDummyDeviceOrEmpty) {
            const permissionsToAcquire = {
              video: kind !== "audioinput" && kind !== "audiooutput",
              audio: kind !== "videoinput" && {
                deviceId: {
                  ideal: "default"
                }
              }
            };
            const stream = yield navigator.mediaDevices.getUserMedia(permissionsToAcquire);
            devices = yield navigator.mediaDevices.enumerateDevices();
            stream.getTracks().forEach((track) => {
              track.stop();
            });
          }
        }
        _this._previousDevices = devices;
        if (kind) {
          devices = devices.filter((device) => device.kind === kind);
        }
        return devices;
      }();
    });
  }
  normalizeDeviceId(kind, deviceId, groupId) {
    return __awaiter(this, void 0, void 0, function* () {
      if (deviceId !== defaultId) {
        return deviceId;
      }
      const devices = yield this.getDevices(kind);
      const defaultDevice = devices.find((d) => d.deviceId === defaultId);
      if (!defaultDevice) {
        livekitLogger.warn("could not reliably determine default device");
        return void 0;
      }
      const device = devices.find((d) => d.deviceId !== defaultId && d.groupId === (groupId !== null && groupId !== void 0 ? groupId : defaultDevice.groupId));
      if (!device) {
        livekitLogger.warn("could not reliably determine default device");
        return void 0;
      }
      return device === null || device === void 0 ? void 0 : device.deviceId;
    });
  }
  hasDeviceInUse(kind) {
    return kind ? _DeviceManager.userMediaPromiseMap.has(kind) : _DeviceManager.userMediaPromiseMap.size > 0;
  }
};
DeviceManager.mediaDeviceKinds = ["audioinput", "audiooutput", "videoinput"];
DeviceManager.userMediaPromiseMap = /* @__PURE__ */ new Map();
var U16_MAX_SIZE = 65535;
var U32_MAX_SIZE = 4294967295;
var WrapAroundUnsignedInt = class _WrapAroundUnsignedInt {
  static u16(raw2) {
    return new _WrapAroundUnsignedInt(raw2, U16_MAX_SIZE);
  }
  static u32(raw2) {
    return new _WrapAroundUnsignedInt(raw2, U32_MAX_SIZE);
  }
  constructor(raw2, maxSize) {
    this.value = raw2;
    if (raw2 < 0) {
      throw new Error("WrapAroundUnsignedInt: cannot faithfully represent an integer smaller than 0");
    }
    if (maxSize > Number.MAX_SAFE_INTEGER) {
      throw new Error("WrapAroundUnsignedInt: cannot faithfully represent an integer bigger than MAX_SAFE_INTEGER.");
    }
    this.maxSize = maxSize;
    this.clamp();
  }
  /** Manually clamp the given containing value according to the wrap around max size bounds. Use
   * this after out of bounds modification to the contained value by external code. */
  clamp() {
    while (this.value > this.maxSize) {
      this.value -= this.maxSize + 1;
    }
    while (this.value < 0) {
      this.value += this.maxSize + 1;
    }
  }
  clone() {
    return new _WrapAroundUnsignedInt(this.value, this.maxSize);
  }
  /** When called, maps the containing value to a new containing value. After mapping, the wrap
   * around external max size bounds are applied. Note that this is a mutative operation. */
  update(updateFn) {
    this.value = updateFn(this.value);
    this.clamp();
  }
  /** Increments the given `n` to the inner value. Note that this is a mutative operation. */
  increment() {
    let n = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : 1;
    this.update((value) => value + n);
  }
  /** Decrements the given `n` from the inner value. Note that this is a mutative operation. */
  decrement() {
    let n = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : 1;
    this.update((value) => value - n);
  }
  getThenIncrement() {
    const previousValue = this.value;
    this.increment();
    return new _WrapAroundUnsignedInt(previousValue, this.maxSize);
  }
  /** Returns true if {@link this} is before the passed other {@link WrapAroundUnsignedInt}. */
  isBefore(other) {
    const a = this.value >>> 0;
    const b = other.value >>> 0;
    const diff = b - a >>> 0;
    return diff !== 0 && diff < this.maxSize + 1;
  }
};
var DataTrackTimestamp = class _DataTrackTimestamp {
  static fromRtpTicks(rtpTicks) {
    return new _DataTrackTimestamp(rtpTicks, 9e4);
  }
  /** Generates a timestamp initialized to a non cryptographically secure random value, so that
   * different streams are more difficult to correlate in packet capture. */
  static rtpRandom() {
    const randomValue = Math.round(Math.random() * U32_MAX_SIZE);
    return _DataTrackTimestamp.fromRtpTicks(randomValue);
  }
  constructor(raw2, rateInHz) {
    this.timestamp = WrapAroundUnsignedInt.u32(raw2);
    this.rateInHz = rateInHz;
  }
  asTicks() {
    return this.timestamp.value;
  }
  clone() {
    return new _DataTrackTimestamp(this.timestamp.value, this.rateInHz);
  }
  wrappingAdd(n) {
    this.timestamp.increment(n);
  }
  /** Returns true if {@link this} is before the passed other {@link DataTrackTimestamp}. */
  isBefore(other) {
    return this.timestamp.isBefore(other.timestamp);
  }
};
var DataTrackClock = class _DataTrackClock {
  constructor(rateInHz, epoch, base2) {
    this.epoch = epoch;
    this.base = base2;
    this.previous = base2.clone();
    this.rateInHz = rateInHz;
  }
  static startingNow(base2, rateInHz) {
    return new _DataTrackClock(rateInHz, /* @__PURE__ */ new Date(), base2);
  }
  static startingAtTime(epoch, base2, rateInHz) {
    return new _DataTrackClock(rateInHz, epoch, base2);
  }
  static rtpStartingNow(base2) {
    return _DataTrackClock.startingNow(base2, 9e4);
  }
  static rtpStartingAtTime(epoch, base2) {
    return _DataTrackClock.startingAtTime(epoch, base2, 9e4);
  }
  now() {
    return this.at(/* @__PURE__ */ new Date());
  }
  at(timestamp) {
    let elapsedMs = timestamp.getTime() - this.epoch.getTime();
    let durationTicks = _DataTrackClock.durationInMsToTicks(elapsedMs, this.rateInHz);
    let result = this.base.clone();
    result.wrappingAdd(durationTicks);
    if (result.isBefore(this.previous)) {
      result = this.previous;
    }
    this.previous = result.clone();
    return result.clone();
  }
  /** Convert a duration since the epoch into clock ticks. */
  static durationInMsToTicks(durationMilliseconds, rateInHz) {
    let durationNanoseconds = durationMilliseconds * 1e6;
    let ticks = (durationNanoseconds * rateInHz + 5e8) / 1e9;
    return Math.round(ticks);
  }
};
function coerceToDataView(input) {
  if (input instanceof DataView) {
    return input;
  } else if (input instanceof ArrayBuffer) {
    return new DataView(input);
  } else if (input instanceof Uint8Array) {
    return new DataView(input.buffer, input.byteOffset, input.byteLength);
  } else {
    throw new Error("Error coercing ".concat(input, " to DataView - input was not DataView, ArrayBuffer, or Uint8Array."));
  }
}
var DataTrackHandleErrorReason;
(function(DataTrackHandleErrorReason2) {
  DataTrackHandleErrorReason2[DataTrackHandleErrorReason2["Reserved"] = 0] = "Reserved";
  DataTrackHandleErrorReason2[DataTrackHandleErrorReason2["TooLarge"] = 1] = "TooLarge";
})(DataTrackHandleErrorReason || (DataTrackHandleErrorReason = {}));
var DataTrackHandleError = class _DataTrackHandleError extends LivekitReasonedError {
  constructor(message, reason) {
    super(19, message);
    this.name = "DataTrackHandleError";
    this.reason = reason;
    this.reasonName = DataTrackHandleErrorReason[reason];
  }
  isReason(reason) {
    return this.reason === reason;
  }
  static tooLarge() {
    return new _DataTrackHandleError("Value too large to be a valid track handle", DataTrackHandleErrorReason.TooLarge);
  }
  static reserved(value) {
    return new _DataTrackHandleError("0x".concat(value.toString(16), " is a reserved value."), DataTrackHandleErrorReason.Reserved);
  }
};
var DataTrackHandle = {
  fromNumber(raw2) {
    if (raw2 === 0) {
      throw DataTrackHandleError.reserved(raw2);
    }
    if (raw2 > U16_MAX_SIZE) {
      throw DataTrackHandleError.tooLarge();
    }
    return raw2;
  }
};
var DataTrackHandleAllocator = class {
  constructor() {
    this.value = 0;
  }
  /** Returns a unique track handle for the next publication, if one can be obtained. */
  get() {
    this.value += 1;
    if (this.value > U16_MAX_SIZE) {
      return null;
    }
    return this.value;
  }
  reset() {
    this.value = 0;
  }
};
var DataTrackInfo = {
  from(protocolInfo) {
    return {
      sid: protocolInfo.sid,
      pubHandle: protocolInfo.pubHandle,
      name: protocolInfo.name,
      usesE2ee: protocolInfo.encryption !== Encryption_Type.NONE
    };
  },
  toProtobuf(info) {
    return new DataTrackInfo$1({
      sid: info.sid,
      pubHandle: info.pubHandle,
      name: info.name,
      encryption: info.usesE2ee ? Encryption_Type.GCM : Encryption_Type.NONE
    });
  }
};
var QueueTaskStatus;
(function(QueueTaskStatus2) {
  QueueTaskStatus2[QueueTaskStatus2["WAITING"] = 0] = "WAITING";
  QueueTaskStatus2[QueueTaskStatus2["RUNNING"] = 1] = "RUNNING";
  QueueTaskStatus2[QueueTaskStatus2["COMPLETED"] = 2] = "COMPLETED";
})(QueueTaskStatus || (QueueTaskStatus = {}));
var AsyncQueue = class {
  constructor() {
    this.pendingTasks = /* @__PURE__ */ new Map();
    this.taskMutex = new _();
    this.nextTaskIndex = 0;
  }
  run(task) {
    return __awaiter(this, void 0, void 0, function* () {
      const taskInfo = {
        id: this.nextTaskIndex++,
        enqueuedAt: Date.now(),
        status: QueueTaskStatus.WAITING
      };
      this.pendingTasks.set(taskInfo.id, taskInfo);
      const unlock = yield this.taskMutex.lock();
      try {
        taskInfo.executedAt = Date.now();
        taskInfo.status = QueueTaskStatus.RUNNING;
        return yield task();
      } finally {
        taskInfo.status = QueueTaskStatus.COMPLETED;
        this.pendingTasks.delete(taskInfo.id);
        unlock();
      }
    });
  }
  flush() {
    return __awaiter(this, void 0, void 0, function* () {
      return this.run(() => __awaiter(this, void 0, void 0, function* () {
      }));
    });
  }
  snapshot() {
    return Array.from(this.pendingTasks.values());
  }
};
var WebSocketStream = class {
  get readyState() {
    return this.ws.readyState;
  }
  constructor(url) {
    let options = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : {};
    var _a2, _b2;
    if ((_a2 = options.signal) === null || _a2 === void 0 ? void 0 : _a2.aborted) {
      throw new DOMException("This operation was aborted", "AbortError");
    }
    this.url = url;
    const ws2 = new WebSocket(url, (_b2 = options.protocols) !== null && _b2 !== void 0 ? _b2 : []);
    ws2.binaryType = "arraybuffer";
    this.ws = ws2;
    const closeWithInfo = function() {
      let _ref = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : {}, code = _ref.closeCode, reason = _ref.reason;
      return ws2.close(code, reason);
    };
    this.opened = new TypedPromise((resolve, reject) => {
      const rejectHandler = () => {
        reject(ConnectionError.websocket("Encountered websocket error during connection establishment"));
      };
      ws2.onopen = () => {
        resolve({
          readable: new ReadableStream({
            start(controller) {
              ws2.onmessage = (_ref2) => {
                let data = _ref2.data;
                return controller.enqueue(data);
              };
              ws2.onerror = (e4) => controller.error(e4);
            },
            cancel: closeWithInfo
          }),
          writable: new WritableStream({
            write(chunk) {
              ws2.send(chunk);
            },
            abort() {
              ws2.close();
            },
            close: closeWithInfo
          }),
          protocol: ws2.protocol,
          extensions: ws2.extensions
        });
        ws2.removeEventListener("error", rejectHandler);
      };
      ws2.addEventListener("error", rejectHandler);
    });
    this.closed = new TypedPromise((resolve, reject) => {
      const rejectHandler = () => __awaiter(this, void 0, void 0, function* () {
        const closePromise = new TypedPromise((res) => {
          if (ws2.readyState === WebSocket.CLOSED) return;
          else {
            ws2.addEventListener("close", (closeEv) => {
              res(closeEv);
            }, {
              once: true
            });
          }
        });
        const reason = yield TypedPromise.race([sleep(250), closePromise]);
        if (!reason) {
          reject(ConnectionError.websocket("Encountered unspecified websocket error without a timely close event"));
        } else {
          resolve(reason);
        }
      });
      ws2.onclose = (_ref3) => {
        let code = _ref3.code, reason = _ref3.reason;
        resolve({
          closeCode: code,
          reason
        });
        ws2.removeEventListener("error", rejectHandler);
      };
      ws2.addEventListener("error", rejectHandler);
    });
    if (options.signal) {
      options.signal.onabort = () => ws2.close();
    }
    this.close = closeWithInfo;
  }
};
var passThroughQueueSignals = ["syncState", "trickle", "offer", "answer", "simulate", "leave"];
function canPassThroughQueue(req) {
  const canPass = passThroughQueueSignals.indexOf(req.case) >= 0;
  livekitLogger.trace("request allowed to bypass queue:", {
    canPass,
    req
  });
  return canPass;
}
var SignalConnectionState;
(function(SignalConnectionState2) {
  SignalConnectionState2[SignalConnectionState2["CONNECTING"] = 0] = "CONNECTING";
  SignalConnectionState2[SignalConnectionState2["CONNECTED"] = 1] = "CONNECTED";
  SignalConnectionState2[SignalConnectionState2["RECONNECTING"] = 2] = "RECONNECTING";
  SignalConnectionState2[SignalConnectionState2["DISCONNECTING"] = 3] = "DISCONNECTING";
  SignalConnectionState2[SignalConnectionState2["DISCONNECTED"] = 4] = "DISCONNECTED";
})(SignalConnectionState || (SignalConnectionState = {}));
var MAX_WS_CLOSE_TIME = 250;
var JOIN_RESPONSE_TIMEOUT = 5e3;
var SignalClient = class {
  get currentState() {
    return this.state;
  }
  get isDisconnected() {
    return this.state === SignalConnectionState.DISCONNECTING || this.state === SignalConnectionState.DISCONNECTED;
  }
  get isEstablishingConnection() {
    return this.state === SignalConnectionState.CONNECTING || this.state === SignalConnectionState.RECONNECTING;
  }
  getNextRequestId() {
    this._requestId += 1;
    return this._requestId;
  }
  constructor() {
    let useJSON = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : false;
    let loggerOptions = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : {};
    var _a2;
    this.rtt = 0;
    this.state = SignalConnectionState.DISCONNECTED;
    this.log = livekitLogger;
    this._requestId = 0;
    this.useV0SignalPath = false;
    this.resetCallbacks = () => {
      this.onAnswer = void 0;
      this.onLeave = void 0;
      this.onLocalTrackPublished = void 0;
      this.onLocalTrackUnpublished = void 0;
      this.onNegotiateRequested = void 0;
      this.onOffer = void 0;
      this.onRemoteMuteChanged = void 0;
      this.onSubscribedQualityUpdate = void 0;
      this.onTokenRefresh = void 0;
      this.onTrickle = void 0;
      this.onClose = void 0;
      this.onMediaSectionsRequirement = void 0;
    };
    this.loggerContextCb = loggerOptions.loggerContextCb;
    this.log = getLogger((_a2 = loggerOptions.loggerName) !== null && _a2 !== void 0 ? _a2 : LoggerNames.Signal, () => this.logContext);
    this.useJSON = useJSON;
    this.requestQueue = new AsyncQueue();
    this.queuedRequests = [];
    this.closingLock = new _();
    this.connectionLock = new _();
    this.state = SignalConnectionState.DISCONNECTED;
  }
  get logContext() {
    var _a2, _b2;
    return (_b2 = (_a2 = this.loggerContextCb) === null || _a2 === void 0 ? void 0 : _a2.call(this)) !== null && _b2 !== void 0 ? _b2 : {};
  }
  join(url_1, token_1, opts_1, abortSignal_1) {
    return __awaiter(this, arguments, void 0, function(url, token, opts, abortSignal) {
      var _this = this;
      let useV0Path = arguments.length > 4 && arguments[4] !== void 0 ? arguments[4] : false;
      let publisherOffer = arguments.length > 5 ? arguments[5] : void 0;
      return function* () {
        _this.state = SignalConnectionState.CONNECTING;
        _this.options = opts;
        const res = yield _this.connect(url, token, opts, abortSignal, useV0Path, publisherOffer);
        return res;
      }();
    });
  }
  reconnect(url, token, sid, reason) {
    return __awaiter(this, void 0, void 0, function* () {
      if (!this.options) {
        this.log.warn("attempted to reconnect without signal options being set, ignoring");
        return;
      }
      this.state = SignalConnectionState.RECONNECTING;
      this.clearPingInterval();
      const res = yield this.connect(url, token, Object.assign(Object.assign({}, this.options), {
        reconnect: true,
        sid,
        reconnectReason: reason
      }), void 0, this.useV0SignalPath);
      return res;
    });
  }
  connect(url_1, token_1, opts_1, abortSignal_1) {
    return __awaiter(this, arguments, void 0, function(url, token, opts, abortSignal) {
      var _this2 = this;
      let useV0Path = arguments.length > 4 && arguments[4] !== void 0 ? arguments[4] : false;
      let publisherOffer = arguments.length > 5 ? arguments[5] : void 0;
      return function* () {
        const unlock = yield _this2.connectionLock.lock();
        _this2.connectOptions = opts;
        _this2.useV0SignalPath = useV0Path;
        const clientInfo = getClientInfo(opts.clientInfoCapabilities);
        const params = useV0Path ? createConnectionParams(token, clientInfo, opts) : yield createJoinRequestConnectionParams(token, clientInfo, opts, publisherOffer);
        const rtcUrl = createRtcUrl(url, params, useV0Path).toString();
        const validateUrl = createValidateUrl(rtcUrl).toString();
        return new Promise((resolve, reject) => __awaiter(_this2, void 0, void 0, function* () {
          var _a2, _b2;
          try {
            let alreadyAborted = false;
            const abortHandler = (eventOrError) => __awaiter(this, void 0, void 0, function* () {
              if (alreadyAborted) {
                return;
              }
              alreadyAborted = true;
              const target = eventOrError instanceof Event ? eventOrError.currentTarget : eventOrError;
              const reason = getAbortReasonAsString(target, "Abort handler called");
              if (this.streamWriter && !this.isDisconnected) {
                this.sendLeave().then(() => this.close(reason)).catch((e4) => {
                  this.log.error(e4);
                  this.close();
                });
              } else {
                this.close();
              }
              cleanupAbortHandlers();
              reject(ConnectionError.cancelled(reason));
            });
            abortSignal === null || abortSignal === void 0 ? void 0 : abortSignal.addEventListener("abort", abortHandler);
            const cleanupAbortHandlers = () => {
              clearTimeout(wsTimeout);
              abortSignal === null || abortSignal === void 0 ? void 0 : abortSignal.removeEventListener("abort", abortHandler);
            };
            const wsTimeout = setTimeout(() => {
              abortHandler(ConnectionError.timeout("room connection has timed out (signal)"));
            }, opts.websocketTimeout);
            const handleSignalConnected = (connection, firstMessage) => {
              this.handleSignalConnected(connection, wsTimeout, firstMessage);
            };
            const redactedUrl = new URL(rtcUrl);
            if (redactedUrl.searchParams.has("access_token")) {
              redactedUrl.searchParams.set("access_token", "<redacted>");
            }
            if (this.ws) {
              const startClose = performance.now();
              yield this.close(false);
              this.log.debug("closed previous ws connection in ".concat(performance.now() - startClose, "ms"));
            }
            this.log.info("signal connecting to ".concat(redactedUrl), {
              reconnect: opts.reconnect,
              reconnectReason: opts.reconnectReason
            });
            this.ws = new WebSocketStream(rtcUrl);
            try {
              this.ws.closed.then((closeInfo) => {
                if (this.isEstablishingConnection) {
                  reject(ConnectionError.internal("Websocket got closed during a (re)connection attempt: ".concat(closeInfo.reason)));
                }
                if (closeInfo.closeCode !== 1e3) {
                  this.log.warn("websocket closed", {
                    reason: closeInfo.reason,
                    code: closeInfo.closeCode,
                    wasClean: closeInfo.closeCode === 1e3,
                    state: this.state
                  });
                  if (this.state === SignalConnectionState.CONNECTED) {
                    this.handleOnClose(closeInfo.reason || "Unexpected WS error");
                  }
                }
                return;
              }).catch((reason) => {
                if (this.isEstablishingConnection) {
                  reject(ConnectionError.internal("Websocket error during a (re)connection attempt: ".concat(reason)));
                }
              });
              const connection = yield this.ws.opened.catch((reason) => __awaiter(this, void 0, void 0, function* () {
                if (this.state !== SignalConnectionState.CONNECTED) {
                  this.state = SignalConnectionState.DISCONNECTED;
                  clearTimeout(wsTimeout);
                  const error = yield this.handleConnectionError(reason, validateUrl);
                  reject(error);
                  return;
                }
                this.handleWSError(reason);
                reject(reason);
                return;
              }));
              clearTimeout(wsTimeout);
              if (!connection) {
                return;
              }
              const signalReader = connection.readable.getReader();
              this.streamWriter = connection.writable.getWriter();
              let firstMessage;
              let firstMessageTimeout;
              try {
                firstMessage = yield Promise.race([signalReader.read(), new Promise((_3, rejectRead) => {
                  firstMessageTimeout = setTimeout(() => {
                    rejectRead(ConnectionError.timeout("signal connection timed out while waiting for the first message"));
                  }, JOIN_RESPONSE_TIMEOUT);
                })]);
              } catch (e4) {
                signalReader.releaseLock();
                reject(e4);
                this.close();
                return;
              } finally {
                clearTimeout(firstMessageTimeout);
              }
              signalReader.releaseLock();
              if (!firstMessage.value) {
                throw ConnectionError.internal("no message received as first message");
              }
              const firstSignalResponse = parseSignalResponse(firstMessage.value);
              const validation = this.validateFirstMessage(firstSignalResponse, (_a2 = opts.reconnect) !== null && _a2 !== void 0 ? _a2 : false);
              if (!validation.isValid) {
                reject(validation.error);
                return;
              }
              if (((_b2 = firstSignalResponse.message) === null || _b2 === void 0 ? void 0 : _b2.case) === "join") {
                this.pingTimeoutDuration = firstSignalResponse.message.value.pingTimeout;
                this.pingIntervalDuration = firstSignalResponse.message.value.pingInterval;
                if (this.pingTimeoutDuration && this.pingTimeoutDuration > 0) {
                  this.log.debug("ping config", {
                    timeout: this.pingTimeoutDuration,
                    interval: this.pingIntervalDuration
                  });
                }
                if (this.onJoined) {
                  this.onJoined(firstSignalResponse.message.value);
                }
              }
              const firstMessageToProcess = validation.shouldProcessFirstMessage ? firstSignalResponse : void 0;
              handleSignalConnected(connection, firstMessageToProcess);
              resolve(validation.response);
            } catch (e4) {
              reject(e4);
            } finally {
              cleanupAbortHandlers();
            }
          } finally {
            unlock();
          }
        }));
      }();
    });
  }
  startReadingLoop(signalReader, firstMessage) {
    return __awaiter(this, void 0, void 0, function* () {
      if (firstMessage) {
        this.handleSignalResponse(firstMessage);
      }
      while (true) {
        if (this.signalLatency) {
          yield sleep(this.signalLatency);
        }
        const _yield$signalReader$r = yield signalReader.read(), done = _yield$signalReader$r.done, value = _yield$signalReader$r.value;
        if (done) {
          break;
        }
        const resp = parseSignalResponse(value);
        this.handleSignalResponse(resp);
      }
    });
  }
  close() {
    return __awaiter(this, arguments, void 0, function() {
      var _this3 = this;
      let updateState = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : true;
      let reason = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : "Close method called on signal client";
      return function* () {
        if ([SignalConnectionState.DISCONNECTING || SignalConnectionState.DISCONNECTED].includes(_this3.state)) {
          _this3.log.debug("ignoring signal close as it's already in disconnecting state");
          return;
        }
        const unlock = yield _this3.closingLock.lock();
        try {
          _this3.clearPingInterval();
          if (updateState) {
            _this3.state = SignalConnectionState.DISCONNECTING;
          }
          if (_this3.ws) {
            _this3.ws.close({
              closeCode: 1e3,
              reason
            });
            const closePromise = _this3.ws.closed;
            _this3.ws = void 0;
            _this3.streamWriter = void 0;
            yield Promise.race([closePromise, sleep(MAX_WS_CLOSE_TIME)]);
          }
        } catch (e4) {
          _this3.log.debug("websocket error while closing", {
            error: e4
          });
        } finally {
          if (updateState) {
            _this3.state = SignalConnectionState.DISCONNECTED;
          }
          unlock();
        }
      }();
    });
  }
  // initial offer after joining
  sendOffer(offer, offerId) {
    this.log.debug("sending offer", {
      offerSdp: offer.sdp
    });
    this.sendRequest({
      case: "offer",
      value: toProtoSessionDescription(offer, offerId)
    });
  }
  // answer a server-initiated offer
  sendAnswer(answer, offerId) {
    this.log.debug("sending answer", {
      answerSdp: answer.sdp
    });
    return this.sendRequest({
      case: "answer",
      value: toProtoSessionDescription(answer, offerId)
    });
  }
  sendIceCandidate(candidate, target) {
    this.log.debug("sending ice candidate", {
      candidate
    });
    return this.sendRequest({
      case: "trickle",
      value: new TrickleRequest({
        candidateInit: JSON.stringify(candidate),
        target
      })
    });
  }
  sendMuteTrack(trackSid, muted) {
    return this.sendRequest({
      case: "mute",
      value: new MuteTrackRequest({
        sid: trackSid,
        muted
      })
    });
  }
  sendAddTrack(req) {
    return this.sendRequest({
      case: "addTrack",
      value: req
    });
  }
  sendUpdateLocalMetadata(metadata_1, name_1) {
    return __awaiter(this, arguments, void 0, function(metadata, name) {
      var _this4 = this;
      let attributes = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : {};
      return function* () {
        const requestId = _this4.getNextRequestId();
        yield _this4.sendRequest({
          case: "updateMetadata",
          value: new UpdateParticipantMetadata({
            requestId,
            metadata,
            name,
            attributes
          })
        });
        return requestId;
      }();
    });
  }
  sendUpdateTrackSettings(settings) {
    this.sendRequest({
      case: "trackSetting",
      value: settings
    });
  }
  sendUpdateSubscription(sub) {
    return this.sendRequest({
      case: "subscription",
      value: sub
    });
  }
  sendSyncState(sync) {
    return this.sendRequest({
      case: "syncState",
      value: sync
    });
  }
  sendUpdateVideoLayers(trackSid, layers) {
    return this.sendRequest({
      case: "updateLayers",
      value: new UpdateVideoLayers({
        trackSid,
        layers
      })
    });
  }
  sendUpdateSubscriptionPermissions(allParticipants, trackPermissions) {
    return this.sendRequest({
      case: "subscriptionPermission",
      value: new SubscriptionPermission({
        allParticipants,
        trackPermissions
      })
    });
  }
  sendSimulateScenario(scenario) {
    return this.sendRequest({
      case: "simulate",
      value: scenario
    });
  }
  sendPing() {
    return Promise.all([this.sendRequest({
      case: "ping",
      value: protoInt64.parse(Date.now())
    }), this.sendRequest({
      case: "pingReq",
      value: new Ping({
        timestamp: protoInt64.parse(Date.now()),
        rtt: protoInt64.parse(this.rtt)
      })
    })]);
  }
  sendUpdateLocalAudioTrack(trackSid, features) {
    return this.sendRequest({
      case: "updateAudioTrack",
      value: new UpdateLocalAudioTrack({
        trackSid,
        features
      })
    });
  }
  sendLeave() {
    return this.sendRequest({
      case: "leave",
      value: new LeaveRequest({
        reason: DisconnectReason.CLIENT_INITIATED,
        // server doesn't process this field, keeping it here to indicate the intent of a full disconnect
        action: LeaveRequest_Action.DISCONNECT
      })
    });
  }
  sendPublishDataTrackRequest(handle, name, usesE2ee) {
    return this.sendRequest({
      case: "publishDataTrackRequest",
      value: new PublishDataTrackRequest({
        pubHandle: handle,
        name,
        encryption: usesE2ee ? Encryption_Type.GCM : Encryption_Type.NONE
      })
    });
  }
  sendUnPublishDataTrackRequest(handle) {
    return this.sendRequest({
      case: "unpublishDataTrackRequest",
      value: new UnpublishDataTrackRequest({
        pubHandle: handle
      })
    });
  }
  sendUpdateDataSubscription(sid, subscribe) {
    return this.sendRequest({
      case: "updateDataSubscription",
      value: new UpdateDataSubscription({
        // FIXME: consider refactoring to allow caller to pass an array of events through
        updates: [new UpdateDataSubscription_Update({
          trackSid: sid,
          subscribe
        })]
      })
    });
  }
  sendRequest(message_1) {
    return __awaiter(this, arguments, void 0, function(message) {
      var _this5 = this;
      let fromQueue = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : false;
      return function* () {
        const canQueue = !fromQueue && !canPassThroughQueue(message);
        if (canQueue && _this5.state === SignalConnectionState.RECONNECTING) {
          _this5.queuedRequests.push(() => __awaiter(_this5, void 0, void 0, function* () {
            yield this.sendRequest(message, true);
          }));
          return;
        }
        if (!fromQueue) {
          yield _this5.requestQueue.flush();
        }
        if (_this5.signalLatency) {
          yield sleep(_this5.signalLatency);
        }
        if (_this5.isDisconnected) {
          _this5.log.debug("skipping signal request (type: ".concat(message.case, ") - SignalClient disconnected"));
          return;
        }
        if (!_this5.streamWriter) {
          _this5.log.error("cannot send signal request before connected, type: ".concat(message === null || message === void 0 ? void 0 : message.case));
          return;
        }
        const req = new SignalRequest({
          message
        });
        try {
          if (_this5.useJSON) {
            yield _this5.streamWriter.write(req.toJsonString());
          } else {
            yield _this5.streamWriter.write(req.toBinary().buffer);
          }
        } catch (e4) {
          _this5.log.error("error sending signal message", {
            error: e4
          });
        }
      }();
    });
  }
  handleSignalResponse(res) {
    var _a2, _b2;
    const msg = res.message;
    if (msg == void 0) {
      this.log.debug("received unsupported message");
      return;
    }
    let pingHandled = false;
    if (msg.case === "answer") {
      const sd = fromProtoSessionDescription(msg.value);
      if (this.onAnswer) {
        this.onAnswer(sd, msg.value.id, msg.value.midToTrackId);
      }
    } else if (msg.case === "offer") {
      const sd = fromProtoSessionDescription(msg.value);
      if (this.onOffer) {
        this.onOffer(sd, msg.value.id, msg.value.midToTrackId);
      }
    } else if (msg.case === "trickle") {
      const candidate = JSON.parse(msg.value.candidateInit);
      if (this.onTrickle) {
        this.onTrickle(candidate, msg.value.target);
      }
    } else if (msg.case === "update") {
      if (this.onParticipantUpdate) {
        this.onParticipantUpdate((_a2 = msg.value.participants) !== null && _a2 !== void 0 ? _a2 : []);
      }
    } else if (msg.case === "trackPublished") {
      if (this.onLocalTrackPublished) {
        this.onLocalTrackPublished(msg.value);
      }
    } else if (msg.case === "speakersChanged") {
      if (this.onSpeakersChanged) {
        this.onSpeakersChanged((_b2 = msg.value.speakers) !== null && _b2 !== void 0 ? _b2 : []);
      }
    } else if (msg.case === "leave") {
      if (this.onLeave) {
        this.onLeave(msg.value);
      }
    } else if (msg.case === "mute") {
      if (this.onRemoteMuteChanged) {
        this.onRemoteMuteChanged(msg.value.sid, msg.value.muted);
      }
    } else if (msg.case === "roomUpdate") {
      if (this.onRoomUpdate && msg.value.room) {
        this.onRoomUpdate(msg.value.room);
      }
    } else if (msg.case === "connectionQuality") {
      if (this.onConnectionQuality) {
        this.onConnectionQuality(msg.value);
      }
    } else if (msg.case === "streamStateUpdate") {
      if (this.onStreamStateUpdate) {
        this.onStreamStateUpdate(msg.value);
      }
    } else if (msg.case === "subscribedQualityUpdate") {
      if (this.onSubscribedQualityUpdate) {
        this.onSubscribedQualityUpdate(msg.value);
      }
    } else if (msg.case === "subscriptionPermissionUpdate") {
      if (this.onSubscriptionPermissionUpdate) {
        this.onSubscriptionPermissionUpdate(msg.value);
      }
    } else if (msg.case === "refreshToken") {
      if (this.onTokenRefresh) {
        this.onTokenRefresh(msg.value);
      }
    } else if (msg.case === "trackUnpublished") {
      if (this.onLocalTrackUnpublished) {
        this.onLocalTrackUnpublished(msg.value);
      }
    } else if (msg.case === "subscriptionResponse") {
      if (this.onSubscriptionError) {
        this.onSubscriptionError(msg.value);
      }
    } else if (msg.case === "pong") ;
    else if (msg.case === "pongResp") {
      this.rtt = Date.now() - Number.parseInt(msg.value.lastPingTimestamp.toString());
      this.resetPingTimeout();
      pingHandled = true;
    } else if (msg.case === "requestResponse") {
      if (this.onRequestResponse) {
        this.onRequestResponse(msg.value);
      }
    } else if (msg.case === "trackSubscribed") {
      if (this.onLocalTrackSubscribed) {
        this.onLocalTrackSubscribed(msg.value.trackSid);
      }
    } else if (msg.case === "roomMoved") {
      if (this.onTokenRefresh) {
        this.onTokenRefresh(msg.value.token);
      }
      if (this.onRoomMoved) {
        this.onRoomMoved(msg.value);
      }
    } else if (msg.case === "mediaSectionsRequirement") {
      if (this.onMediaSectionsRequirement) {
        this.onMediaSectionsRequirement(msg.value);
      }
    } else if (msg.case === "publishDataTrackResponse") {
      if (this.onPublishDataTrackResponse) {
        this.onPublishDataTrackResponse(msg.value);
      }
    } else if (msg.case === "unpublishDataTrackResponse") {
      if (this.onUnPublishDataTrackResponse) {
        this.onUnPublishDataTrackResponse(msg.value);
      }
    } else if (msg.case === "dataTrackSubscriberHandles") {
      if (this.onDataTrackSubscriberHandles) {
        this.onDataTrackSubscriberHandles(msg.value);
      }
    } else {
      this.log.debug("unsupported message", {
        msgCase: msg.case
      });
    }
    if (!pingHandled) {
      this.resetPingTimeout();
    }
  }
  setReconnected() {
    while (this.queuedRequests.length > 0) {
      const req = this.queuedRequests.shift();
      if (req) {
        this.requestQueue.run(req);
      }
    }
  }
  handleOnClose(reason) {
    return __awaiter(this, void 0, void 0, function* () {
      if (this.state === SignalConnectionState.DISCONNECTED) return;
      const onCloseCallback = this.onClose;
      yield this.close(void 0, reason);
      this.log.info("websocket connection closed: ".concat(reason), {
        reason
      });
      if (onCloseCallback) {
        onCloseCallback(reason);
      }
    });
  }
  handleWSError(error) {
    this.log.error("websocket error", {
      error
    });
  }
  /**
   * Resets the ping timeout and starts a new timeout.
   * Call this after receiving a pong message
   */
  resetPingTimeout() {
    this.clearPingTimeout();
    if (!this.pingTimeoutDuration) {
      this.log.warn("ping timeout duration not set");
      return;
    }
    this.pingTimeout = CriticalTimers.setTimeout(() => {
      this.log.warn("ping timeout triggered. last pong received at: ".concat(new Date(Date.now() - this.pingTimeoutDuration * 1e3).toUTCString()));
      this.handleOnClose("ping timeout");
    }, this.pingTimeoutDuration * 1e3);
  }
  /**
   * Clears ping timeout (does not start a new timeout)
   */
  clearPingTimeout() {
    if (this.pingTimeout) {
      CriticalTimers.clearTimeout(this.pingTimeout);
    }
  }
  startPingInterval() {
    this.clearPingInterval();
    this.resetPingTimeout();
    if (!this.pingIntervalDuration) {
      this.log.warn("ping interval duration not set");
      return;
    }
    this.log.debug("start ping interval");
    this.pingInterval = CriticalTimers.setInterval(() => {
      this.sendPing();
    }, this.pingIntervalDuration * 1e3);
  }
  clearPingInterval() {
    this.log.debug("clearing ping interval");
    this.clearPingTimeout();
    if (this.pingInterval) {
      CriticalTimers.clearInterval(this.pingInterval);
    }
  }
  /**
   * Handles the successful connection to the signal server
   * @param connection The WebSocket connection
   * @param timeoutHandle The timeout handle to clear
   * @param firstMessage Optional first message to process
   * @internal
   */
  handleSignalConnected(connection, timeoutHandle, firstMessage) {
    this.state = SignalConnectionState.CONNECTED;
    this.log.info("signal connected");
    clearTimeout(timeoutHandle);
    this.startPingInterval();
    this.startReadingLoop(connection.readable.getReader(), firstMessage);
  }
  /**
   * Validates the first message received from the signal server
   * @param firstSignalResponse The first signal response received
   * @param isReconnect Whether this is a reconnection attempt
   * @returns Validation result with response or error
   * @internal
   */
  validateFirstMessage(firstSignalResponse, isReconnect) {
    var _a2, _b2, _c, _d, _e2;
    if (((_a2 = firstSignalResponse.message) === null || _a2 === void 0 ? void 0 : _a2.case) === "join") {
      return {
        isValid: true,
        response: firstSignalResponse.message.value
      };
    } else if (this.state === SignalConnectionState.RECONNECTING && ((_b2 = firstSignalResponse.message) === null || _b2 === void 0 ? void 0 : _b2.case) !== "leave") {
      if (((_c = firstSignalResponse.message) === null || _c === void 0 ? void 0 : _c.case) === "reconnect") {
        return {
          isValid: true,
          response: firstSignalResponse.message.value
        };
      } else {
        this.log.debug("declaring signal reconnected without reconnect response received");
        return {
          isValid: true,
          response: void 0,
          shouldProcessFirstMessage: true
        };
      }
    } else if (this.isEstablishingConnection && ((_d = firstSignalResponse.message) === null || _d === void 0 ? void 0 : _d.case) === "leave") {
      return {
        isValid: false,
        error: ConnectionError.leaveRequest("Received leave request while trying to (re)connect", firstSignalResponse.message.value.reason)
      };
    } else if (!isReconnect) {
      return {
        isValid: false,
        error: ConnectionError.internal("did not receive join response, got ".concat((_e2 = firstSignalResponse.message) === null || _e2 === void 0 ? void 0 : _e2.case, " instead"))
      };
    }
    return {
      isValid: false,
      error: ConnectionError.internal("Unexpected first message")
    };
  }
  /**
   * Handles WebSocket connection errors by validating with the server
   * @param reason The error that occurred
   * @param validateUrl The URL to validate the connection with
   * @returns A ConnectionError with appropriate reason and status
   * @internal
   */
  handleConnectionError(reason, validateUrl) {
    return __awaiter(this, void 0, void 0, function* () {
      try {
        const resp = yield fetch(validateUrl);
        switch (resp.status) {
          case 404:
            const errorMsg = yield resp.text();
            if (errorMsg.includes("requested room does not exist")) {
              return ConnectionError.notAllowed(errorMsg, resp.status);
            }
            return ConnectionError.serviceNotFound("v1 RTC path not found. Consider upgrading your LiveKit server version", "v0-rtc");
          case 401:
          case 403:
            const msg = yield resp.text();
            return ConnectionError.notAllowed(msg, resp.status);
          default:
            break;
        }
        if (reason instanceof ConnectionError) {
          return reason;
        } else {
          return ConnectionError.internal("Encountered unknown websocket error during connection: ".concat(reason), {
            status: resp.status,
            statusText: resp.statusText
          });
        }
      } catch (e4) {
        return e4 instanceof ConnectionError ? e4 : ConnectionError.serverUnreachable(e4 instanceof Error ? e4.message : "server was not reachable");
      }
    });
  }
};
function fromProtoSessionDescription(sd) {
  const rsd = {
    type: "offer",
    sdp: sd.sdp
  };
  switch (sd.type) {
    case "answer":
    case "offer":
    case "pranswer":
    case "rollback":
      rsd.type = sd.type;
      break;
  }
  return rsd;
}
function toProtoSessionDescription(rsd, id) {
  const sd = new SessionDescription({
    sdp: rsd.sdp,
    type: rsd.type,
    id
  });
  return sd;
}
function createConnectionParams(token, info, opts) {
  var _a2;
  const params = new URLSearchParams();
  params.set("access_token", token);
  if (opts.reconnect) {
    params.set("reconnect", "1");
    if (opts.sid) {
      params.set("sid", opts.sid);
    }
  }
  params.set("auto_subscribe", opts.autoSubscribe ? "1" : "0");
  params.set("sdk", isReactNative() ? "reactnative" : "js");
  params.set("version", info.version);
  params.set("protocol", info.protocol.toString());
  params.set("client_protocol", info.clientProtocol.toString());
  if (info.deviceModel) {
    params.set("device_model", info.deviceModel);
  }
  if (info.os) {
    params.set("os", info.os);
  }
  if (info.osVersion) {
    params.set("os_version", info.osVersion);
  }
  if (info.browser) {
    params.set("browser", info.browser);
  }
  if (info.browserVersion) {
    params.set("browser_version", info.browserVersion);
  }
  if (opts.adaptiveStream) {
    params.set("adaptive_stream", "1");
  }
  if (opts.reconnectReason) {
    params.set("reconnect_reason", opts.reconnectReason.toString());
  }
  if ((_a2 = navigator.connection) === null || _a2 === void 0 ? void 0 : _a2.type) {
    params.set("network", navigator.connection.type);
  }
  return params;
}
function createJoinRequestConnectionParams(token, info, opts, publisherOffer) {
  return __awaiter(this, void 0, void 0, function* () {
    const params = new URLSearchParams();
    params.set("access_token", token);
    const joinRequest = new JoinRequest({
      clientInfo: info,
      connectionSettings: new ConnectionSettings({
        autoSubscribe: !!opts.autoSubscribe,
        adaptiveStream: !!opts.adaptiveStream
      }),
      reconnect: !!opts.reconnect,
      participantSid: opts.sid ? opts.sid : void 0,
      publisherOffer
    });
    if (opts.reconnectReason) {
      joinRequest.reconnectReason = opts.reconnectReason;
    }
    const joinRequestBytes = joinRequest.toBinary();
    let requestBytes;
    let compression;
    if (isCompressionStreamSupported()) {
      const stream = new CompressionStream("gzip");
      const writer2 = stream.writable.getWriter();
      writer2.write(new Uint8Array(joinRequestBytes));
      writer2.close();
      const chunks = [];
      const reader = stream.readable.getReader();
      while (true) {
        const _yield$reader$read = yield reader.read(), done = _yield$reader$read.done, value = _yield$reader$read.value;
        if (done) break;
        chunks.push(value);
      }
      const totalLength = chunks.reduce((acc, chunk) => acc + chunk.length, 0);
      const result = new Uint8Array(totalLength);
      let offset = 0;
      for (const chunk of chunks) {
        result.set(chunk, offset);
        offset += chunk.length;
      }
      requestBytes = result;
      compression = WrappedJoinRequest_Compression.GZIP;
    } else {
      requestBytes = joinRequestBytes;
      compression = WrappedJoinRequest_Compression.NONE;
    }
    const wrappedJoinRequest = new WrappedJoinRequest({
      joinRequest: requestBytes,
      compression
    });
    const wrappedBytes = wrappedJoinRequest.toBinary();
    const bytesToBase64 = (bytes) => {
      const binString = Array.from(bytes, (byte) => String.fromCodePoint(byte)).join("");
      return btoa(binString);
    };
    params.set("join_request", bytesToBase64(wrappedBytes).replace(/\+/g, "-").replace(/\//g, "_"));
    return params;
  });
}
var TTLMap = class {
  /**
   * @param ttl ttl of the key (ms)
   */
  constructor(ttl) {
    this._map = /* @__PURE__ */ new Map();
    this._lastCleanup = 0;
    this.ttl = ttl;
  }
  set(key, value) {
    const now = Date.now();
    if (now - this._lastCleanup > this.ttl / 2) {
      this.cleanup();
    }
    const expiresAt = now + this.ttl;
    this._map.set(key, {
      value,
      expiresAt
    });
    return this;
  }
  get(key) {
    const entry = this._map.get(key);
    if (!entry) return void 0;
    if (entry.expiresAt < Date.now()) {
      this._map.delete(key);
      return void 0;
    }
    return entry.value;
  }
  has(key) {
    const entry = this._map.get(key);
    if (!entry) return false;
    if (entry.expiresAt < Date.now()) {
      this._map.delete(key);
      return false;
    }
    return true;
  }
  delete(key) {
    return this._map.delete(key);
  }
  clear() {
    this._map.clear();
  }
  cleanup() {
    const now = Date.now();
    for (const _ref of this._map.entries()) {
      var _ref2 = _slicedToArray(_ref, 2);
      const key = _ref2[0];
      const entry = _ref2[1];
      if (entry.expiresAt < now) {
        this._map.delete(key);
      }
    }
    this._lastCleanup = now;
  }
  get size() {
    this.cleanup();
    return this._map.size;
  }
  forEach(callback) {
    this.cleanup();
    for (const _ref3 of this._map.entries()) {
      var _ref4 = _slicedToArray(_ref3, 2);
      const key = _ref4[0];
      const entry = _ref4[1];
      if (entry.expiresAt >= Date.now()) {
        callback(entry.value, key, this.asValueMap());
      }
    }
  }
  map(callback) {
    this.cleanup();
    const result = [];
    const valueMap = this.asValueMap();
    for (const _ref5 of valueMap.entries()) {
      var _ref6 = _slicedToArray(_ref5, 2);
      const key = _ref6[0];
      const value = _ref6[1];
      result.push(callback(value, key, valueMap));
    }
    return result;
  }
  asValueMap() {
    const result = /* @__PURE__ */ new Map();
    for (const _ref7 of this._map.entries()) {
      var _ref8 = _slicedToArray(_ref7, 2);
      const key = _ref8[0];
      const entry = _ref8[1];
      if (entry.expiresAt >= Date.now()) {
        result.set(key, entry.value);
      }
    }
    return result;
  }
};
var lib = {};
var parser = {};
var grammar = { exports: {} };
var hasRequiredGrammar;
function requireGrammar() {
  if (hasRequiredGrammar) return grammar.exports;
  hasRequiredGrammar = 1;
  var grammar$1 = grammar.exports = {
    v: [{
      name: "version",
      reg: /^(\d*)$/
    }],
    o: [{
      // o=- 20518 0 IN IP4 203.0.113.1
      // NB: sessionId will be a String in most cases because it is huge
      name: "origin",
      reg: /^(\S*) (\d*) (\d*) (\S*) IP(\d) (\S*)/,
      names: ["username", "sessionId", "sessionVersion", "netType", "ipVer", "address"],
      format: "%s %s %d %s IP%d %s"
    }],
    // default parsing of these only (though some of these feel outdated)
    s: [{
      name: "name"
    }],
    i: [{
      name: "description"
    }],
    u: [{
      name: "uri"
    }],
    e: [{
      name: "email"
    }],
    p: [{
      name: "phone"
    }],
    z: [{
      name: "timezones"
    }],
    // TODO: this one can actually be parsed properly...
    r: [{
      name: "repeats"
    }],
    // TODO: this one can also be parsed properly
    // k: [{}], // outdated thing ignored
    t: [{
      // t=0 0
      name: "timing",
      reg: /^(\d*) (\d*)/,
      names: ["start", "stop"],
      format: "%d %d"
    }],
    c: [{
      // c=IN IP4 10.47.197.26
      name: "connection",
      reg: /^IN IP(\d) (\S*)/,
      names: ["version", "ip"],
      format: "IN IP%d %s"
    }],
    b: [{
      // b=AS:4000
      push: "bandwidth",
      reg: /^(TIAS|AS|CT|RR|RS):(\d*)/,
      names: ["type", "limit"],
      format: "%s:%s"
    }],
    m: [{
      // m=video 51744 RTP/AVP 126 97 98 34 31
      // NB: special - pushes to session
      // TODO: rtp/fmtp should be filtered by the payloads found here?
      reg: /^(\w*) (\d*) ([\w/]*)(?: (.*))?/,
      names: ["type", "port", "protocol", "payloads"],
      format: "%s %d %s %s"
    }],
    a: [
      {
        // a=rtpmap:110 opus/48000/2
        push: "rtp",
        reg: /^rtpmap:(\d*) ([\w\-.]*)(?:\s*\/(\d*)(?:\s*\/(\S*))?)?/,
        names: ["payload", "codec", "rate", "encoding"],
        format: function(o2) {
          return o2.encoding ? "rtpmap:%d %s/%s/%s" : o2.rate ? "rtpmap:%d %s/%s" : "rtpmap:%d %s";
        }
      },
      {
        // a=fmtp:108 profile-level-id=24;object=23;bitrate=64000
        // a=fmtp:111 minptime=10; useinbandfec=1
        push: "fmtp",
        reg: /^fmtp:(\d*) ([\S| ]*)/,
        names: ["payload", "config"],
        format: "fmtp:%d %s"
      },
      {
        // a=control:streamid=0
        name: "control",
        reg: /^control:(.*)/,
        format: "control:%s"
      },
      {
        // a=rtcp:65179 IN IP4 193.84.77.194
        name: "rtcp",
        reg: /^rtcp:(\d*)(?: (\S*) IP(\d) (\S*))?/,
        names: ["port", "netType", "ipVer", "address"],
        format: function(o2) {
          return o2.address != null ? "rtcp:%d %s IP%d %s" : "rtcp:%d";
        }
      },
      {
        // a=rtcp-fb:98 trr-int 100
        push: "rtcpFbTrrInt",
        reg: /^rtcp-fb:(\*|\d*) trr-int (\d*)/,
        names: ["payload", "value"],
        format: "rtcp-fb:%s trr-int %d"
      },
      {
        // a=rtcp-fb:98 nack rpsi
        push: "rtcpFb",
        reg: /^rtcp-fb:(\*|\d*) ([\w-_]*)(?: ([\w-_]*))?/,
        names: ["payload", "type", "subtype"],
        format: function(o2) {
          return o2.subtype != null ? "rtcp-fb:%s %s %s" : "rtcp-fb:%s %s";
        }
      },
      {
        // a=extmap:2 urn:ietf:params:rtp-hdrext:toffset
        // a=extmap:1/recvonly URI-gps-string
        // a=extmap:3 urn:ietf:params:rtp-hdrext:encrypt urn:ietf:params:rtp-hdrext:smpte-tc 25@600/24
        push: "ext",
        reg: /^extmap:(\d+)(?:\/(\w+))?(?: (urn:ietf:params:rtp-hdrext:encrypt))? (\S*)(?: (\S*))?/,
        names: ["value", "direction", "encrypt-uri", "uri", "config"],
        format: function(o2) {
          return "extmap:%d" + (o2.direction ? "/%s" : "%v") + (o2["encrypt-uri"] ? " %s" : "%v") + " %s" + (o2.config ? " %s" : "");
        }
      },
      {
        // a=extmap-allow-mixed
        name: "extmapAllowMixed",
        reg: /^(extmap-allow-mixed)/
      },
      {
        // a=crypto:1 AES_CM_128_HMAC_SHA1_80 inline:PS1uQCVeeCFCanVmcjkpPywjNWhcYD0mXXtxaVBR|2^20|1:32
        push: "crypto",
        reg: /^crypto:(\d*) ([\w_]*) (\S*)(?: (\S*))?/,
        names: ["id", "suite", "config", "sessionConfig"],
        format: function(o2) {
          return o2.sessionConfig != null ? "crypto:%d %s %s %s" : "crypto:%d %s %s";
        }
      },
      {
        // a=setup:actpass
        name: "setup",
        reg: /^setup:(\w*)/,
        format: "setup:%s"
      },
      {
        // a=connection:new
        name: "connectionType",
        reg: /^connection:(new|existing)/,
        format: "connection:%s"
      },
      {
        // a=mid:1
        name: "mid",
        reg: /^mid:([^\s]*)/,
        format: "mid:%s"
      },
      {
        // a=msid:0c8b064d-d807-43b4-b434-f92a889d8587 98178685-d409-46e0-8e16-7ef0db0db64a
        name: "msid",
        reg: /^msid:(.*)/,
        format: "msid:%s"
      },
      {
        // a=ptime:20
        name: "ptime",
        reg: /^ptime:(\d*(?:\.\d*)*)/,
        format: "ptime:%d"
      },
      {
        // a=maxptime:60
        name: "maxptime",
        reg: /^maxptime:(\d*(?:\.\d*)*)/,
        format: "maxptime:%d"
      },
      {
        // a=sendrecv
        name: "direction",
        reg: /^(sendrecv|recvonly|sendonly|inactive)/
      },
      {
        // a=ice-lite
        name: "icelite",
        reg: /^(ice-lite)/
      },
      {
        // a=ice-ufrag:F7gI
        name: "iceUfrag",
        reg: /^ice-ufrag:(\S*)/,
        format: "ice-ufrag:%s"
      },
      {
        // a=ice-pwd:x9cml/YzichV2+XlhiMu8g
        name: "icePwd",
        reg: /^ice-pwd:(\S*)/,
        format: "ice-pwd:%s"
      },
      {
        // a=fingerprint:SHA-1 00:11:22:33:44:55:66:77:88:99:AA:BB:CC:DD:EE:FF:00:11:22:33
        name: "fingerprint",
        reg: /^fingerprint:(\S*) (\S*)/,
        names: ["type", "hash"],
        format: "fingerprint:%s %s"
      },
      {
        // a=candidate:0 1 UDP 2113667327 203.0.113.1 54400 typ host
        // a=candidate:1162875081 1 udp 2113937151 192.168.34.75 60017 typ host generation 0 network-id 3 network-cost 10
        // a=candidate:3289912957 2 udp 1845501695 193.84.77.194 60017 typ srflx raddr 192.168.34.75 rport 60017 generation 0 network-id 3 network-cost 10
        // a=candidate:229815620 1 tcp 1518280447 192.168.150.19 60017 typ host tcptype active generation 0 network-id 3 network-cost 10
        // a=candidate:3289912957 2 tcp 1845501695 193.84.77.194 60017 typ srflx raddr 192.168.34.75 rport 60017 tcptype passive generation 0 network-id 3 network-cost 10
        push: "candidates",
        reg: /^candidate:(\S*) (\d*) (\S*) (\d*) (\S*) (\d*) typ (\S*)(?: raddr (\S*) rport (\d*))?(?: tcptype (\S*))?(?: generation (\d*))?(?: network-id (\d*))?(?: network-cost (\d*))?/,
        names: ["foundation", "component", "transport", "priority", "ip", "port", "type", "raddr", "rport", "tcptype", "generation", "network-id", "network-cost"],
        format: function(o2) {
          var str = "candidate:%s %d %s %d %s %d typ %s";
          str += o2.raddr != null ? " raddr %s rport %d" : "%v%v";
          str += o2.tcptype != null ? " tcptype %s" : "%v";
          if (o2.generation != null) {
            str += " generation %d";
          }
          str += o2["network-id"] != null ? " network-id %d" : "%v";
          str += o2["network-cost"] != null ? " network-cost %d" : "%v";
          return str;
        }
      },
      {
        // a=end-of-candidates (keep after the candidates line for readability)
        name: "endOfCandidates",
        reg: /^(end-of-candidates)/
      },
      {
        // a=remote-candidates:1 203.0.113.1 54400 2 203.0.113.1 54401 ...
        name: "remoteCandidates",
        reg: /^remote-candidates:(.*)/,
        format: "remote-candidates:%s"
      },
      {
        // a=ice-options:google-ice
        name: "iceOptions",
        reg: /^ice-options:(\S*)/,
        format: "ice-options:%s"
      },
      {
        // a=ssrc:2566107569 cname:t9YU8M1UxTF8Y1A1
        push: "ssrcs",
        reg: /^ssrc:(\d*) ([\w_-]*)(?::(.*))?/,
        names: ["id", "attribute", "value"],
        format: function(o2) {
          var str = "ssrc:%d";
          if (o2.attribute != null) {
            str += " %s";
            if (o2.value != null) {
              str += ":%s";
            }
          }
          return str;
        }
      },
      {
        // a=ssrc-group:FEC 1 2
        // a=ssrc-group:FEC-FR 3004364195 1080772241
        push: "ssrcGroups",
        // token-char = %x21 / %x23-27 / %x2A-2B / %x2D-2E / %x30-39 / %x41-5A / %x5E-7E
        reg: /^ssrc-group:([\x21\x23\x24\x25\x26\x27\x2A\x2B\x2D\x2E\w]*) (.*)/,
        names: ["semantics", "ssrcs"],
        format: "ssrc-group:%s %s"
      },
      {
        // a=msid-semantic: WMS Jvlam5X3SX1OP6pn20zWogvaKJz5Hjf9OnlV
        name: "msidSemantic",
        reg: /^msid-semantic:\s?(\w*) (\S*)/,
        names: ["semantic", "token"],
        format: "msid-semantic: %s %s"
        // space after ':' is not accidental
      },
      {
        // a=group:BUNDLE audio video
        push: "groups",
        reg: /^group:(\w*) (.*)/,
        names: ["type", "mids"],
        format: "group:%s %s"
      },
      {
        // a=rtcp-mux
        name: "rtcpMux",
        reg: /^(rtcp-mux)/
      },
      {
        // a=rtcp-rsize
        name: "rtcpRsize",
        reg: /^(rtcp-rsize)/
      },
      {
        // a=sctpmap:5000 webrtc-datachannel 1024
        name: "sctpmap",
        reg: /^sctpmap:([\w_/]*) (\S*)(?: (\S*))?/,
        names: ["sctpmapNumber", "app", "maxMessageSize"],
        format: function(o2) {
          return o2.maxMessageSize != null ? "sctpmap:%s %s %s" : "sctpmap:%s %s";
        }
      },
      {
        // a=x-google-flag:conference
        name: "xGoogleFlag",
        reg: /^x-google-flag:([^\s]*)/,
        format: "x-google-flag:%s"
      },
      {
        // a=rid:1 send max-width=1280;max-height=720;max-fps=30;depend=0
        push: "rids",
        reg: /^rid:([\d\w]+) (\w+)(?: ([\S| ]*))?/,
        names: ["id", "direction", "params"],
        format: function(o2) {
          return o2.params ? "rid:%s %s %s" : "rid:%s %s";
        }
      },
      {
        // a=imageattr:97 send [x=800,y=640,sar=1.1,q=0.6] [x=480,y=320] recv [x=330,y=250]
        // a=imageattr:* send [x=800,y=640] recv *
        // a=imageattr:100 recv [x=320,y=240]
        push: "imageattrs",
        reg: new RegExp(
          // a=imageattr:97
          "^imageattr:(\\d+|\\*)[\\s\\t]+(send|recv)[\\s\\t]+(\\*|\\[\\S+\\](?:[\\s\\t]+\\[\\S+\\])*)(?:[\\s\\t]+(recv|send)[\\s\\t]+(\\*|\\[\\S+\\](?:[\\s\\t]+\\[\\S+\\])*))?"
        ),
        names: ["pt", "dir1", "attrs1", "dir2", "attrs2"],
        format: function(o2) {
          return "imageattr:%s %s %s" + (o2.dir2 ? " %s %s" : "");
        }
      },
      {
        // a=simulcast:send 1,2,3;~4,~5 recv 6;~7,~8
        // a=simulcast:recv 1;4,5 send 6;7
        name: "simulcast",
        reg: new RegExp(
          // a=simulcast:
          "^simulcast:(send|recv) ([a-zA-Z0-9\\-_~;,]+)(?:\\s?(send|recv) ([a-zA-Z0-9\\-_~;,]+))?$"
        ),
        names: ["dir1", "list1", "dir2", "list2"],
        format: function(o2) {
          return "simulcast:%s %s" + (o2.dir2 ? " %s %s" : "");
        }
      },
      {
        // old simulcast draft 03 (implemented by Firefox)
        //   https://tools.ietf.org/html/draft-ietf-mmusic-sdp-simulcast-03
        // a=simulcast: recv pt=97;98 send pt=97
        // a=simulcast: send rid=5;6;7 paused=6,7
        name: "simulcast_03",
        reg: /^simulcast:[\s\t]+([\S+\s\t]+)$/,
        names: ["value"],
        format: "simulcast: %s"
      },
      {
        // a=framerate:25
        // a=framerate:29.97
        name: "framerate",
        reg: /^framerate:(\d+(?:$|\.\d+))/,
        format: "framerate:%s"
      },
      {
        // RFC4570
        // a=source-filter: incl IN IP4 239.5.2.31 10.1.15.5
        name: "sourceFilter",
        reg: /^source-filter: *(excl|incl) (\S*) (IP4|IP6|\*) (\S*) (.*)/,
        names: ["filterMode", "netType", "addressTypes", "destAddress", "srcList"],
        format: "source-filter: %s %s %s %s %s"
      },
      {
        // a=bundle-only
        name: "bundleOnly",
        reg: /^(bundle-only)/
      },
      {
        // a=label:1
        name: "label",
        reg: /^label:(.+)/,
        format: "label:%s"
      },
      {
        // RFC version 26 for SCTP over DTLS
        // https://tools.ietf.org/html/draft-ietf-mmusic-sctp-sdp-26#section-5
        name: "sctpPort",
        reg: /^sctp-port:(\d+)$/,
        format: "sctp-port:%s"
      },
      {
        // RFC version 26 for SCTP over DTLS
        // https://tools.ietf.org/html/draft-ietf-mmusic-sctp-sdp-26#section-6
        name: "maxMessageSize",
        reg: /^max-message-size:(\d+)$/,
        format: "max-message-size:%s"
      },
      {
        // RFC7273
        // a=ts-refclk:ptp=IEEE1588-2008:39-A7-94-FF-FE-07-CB-D0:37
        push: "tsRefClocks",
        reg: /^ts-refclk:([^\s=]*)(?:=(\S*))?/,
        names: ["clksrc", "clksrcExt"],
        format: function(o2) {
          return "ts-refclk:%s" + (o2.clksrcExt != null ? "=%s" : "");
        }
      },
      {
        // RFC7273
        // a=mediaclk:direct=963214424
        name: "mediaClk",
        reg: /^mediaclk:(?:id=(\S*))? *([^\s=]*)(?:=(\S*))?(?: *rate=(\d+)\/(\d+))?/,
        names: ["id", "mediaClockName", "mediaClockValue", "rateNumerator", "rateDenominator"],
        format: function(o2) {
          var str = "mediaclk:";
          str += o2.id != null ? "id=%s %s" : "%v%s";
          str += o2.mediaClockValue != null ? "=%s" : "";
          str += o2.rateNumerator != null ? " rate=%s" : "";
          str += o2.rateDenominator != null ? "/%s" : "";
          return str;
        }
      },
      {
        // a=keywds:keywords
        name: "keywords",
        reg: /^keywds:(.+)$/,
        format: "keywds:%s"
      },
      {
        // a=content:main
        name: "content",
        reg: /^content:(.+)/,
        format: "content:%s"
      },
      // BFCP https://tools.ietf.org/html/rfc4583
      {
        // a=floorctrl:c-s
        name: "bfcpFloorCtrl",
        reg: /^floorctrl:(c-only|s-only|c-s)/,
        format: "floorctrl:%s"
      },
      {
        // a=confid:1
        name: "bfcpConfId",
        reg: /^confid:(\d+)/,
        format: "confid:%s"
      },
      {
        // a=userid:1
        name: "bfcpUserId",
        reg: /^userid:(\d+)/,
        format: "userid:%s"
      },
      {
        // a=floorid:1
        name: "bfcpFloorId",
        reg: /^floorid:(.+) (?:m-stream|mstrm):(.+)/,
        names: ["id", "mStream"],
        format: "floorid:%s mstrm:%s"
      },
      {
        // any a= that we don't understand is kept verbatim on media.invalid
        push: "invalid",
        names: ["value"]
      }
    ]
  };
  Object.keys(grammar$1).forEach(function(key) {
    var objs = grammar$1[key];
    objs.forEach(function(obj) {
      if (!obj.reg) {
        obj.reg = /(.*)/;
      }
      if (!obj.format) {
        obj.format = "%s";
      }
    });
  });
  return grammar.exports;
}
var hasRequiredParser;
function requireParser() {
  if (hasRequiredParser) return parser;
  hasRequiredParser = 1;
  (function(exports) {
    var toIntIfInt = function(v) {
      return String(Number(v)) === v ? Number(v) : v;
    };
    var attachProperties = function(match, location, names, rawName) {
      if (rawName && !names) {
        location[rawName] = toIntIfInt(match[1]);
      } else {
        for (var i2 = 0; i2 < names.length; i2 += 1) {
          if (match[i2 + 1] != null) {
            location[names[i2]] = toIntIfInt(match[i2 + 1]);
          }
        }
      }
    };
    var parseReg = function(obj, location, content) {
      var needsBlank = obj.name && obj.names;
      if (obj.push && !location[obj.push]) {
        location[obj.push] = [];
      } else if (needsBlank && !location[obj.name]) {
        location[obj.name] = {};
      }
      var keyLocation = obj.push ? {} : (
        // blank object that will be pushed
        needsBlank ? location[obj.name] : location
      );
      attachProperties(content.match(obj.reg), keyLocation, obj.names, obj.name);
      if (obj.push) {
        location[obj.push].push(keyLocation);
      }
    };
    var grammar2 = requireGrammar();
    var validLine = RegExp.prototype.test.bind(/^([a-z])=(.*)/);
    exports.parse = function(sdp2) {
      var session = {}, media = [], location = session;
      sdp2.split(/(\r\n|\r|\n)/).filter(validLine).forEach(function(l) {
        var type = l[0];
        var content = l.slice(2);
        if (type === "m") {
          media.push({
            rtp: [],
            fmtp: []
          });
          location = media[media.length - 1];
        }
        for (var j2 = 0; j2 < (grammar2[type] || []).length; j2 += 1) {
          var obj = grammar2[type][j2];
          if (obj.reg.test(content)) {
            return parseReg(obj, location, content);
          }
        }
      });
      session.media = media;
      return session;
    };
    var paramReducer = function(acc, expr) {
      var s = expr.split(/=(.+)/, 2);
      if (s.length === 2) {
        acc[s[0]] = toIntIfInt(s[1]);
      } else if (s.length === 1 && expr.length > 1) {
        acc[s[0]] = void 0;
      }
      return acc;
    };
    exports.parseParams = function(str) {
      return str.split(/;\s?/).reduce(paramReducer, {});
    };
    exports.parseFmtpConfig = exports.parseParams;
    exports.parsePayloads = function(str) {
      return str.toString().split(" ").map(Number);
    };
    exports.parseRemoteCandidates = function(str) {
      var candidates = [];
      var parts = str.split(" ").map(toIntIfInt);
      for (var i2 = 0; i2 < parts.length; i2 += 3) {
        candidates.push({
          component: parts[i2],
          ip: parts[i2 + 1],
          port: parts[i2 + 2]
        });
      }
      return candidates;
    };
    exports.parseImageAttributes = function(str) {
      return str.split(" ").map(function(item) {
        return item.substring(1, item.length - 1).split(",").reduce(paramReducer, {});
      });
    };
    exports.parseSimulcastStreamList = function(str) {
      return str.split(";").map(function(stream) {
        return stream.split(",").map(function(format) {
          var scid, paused = false;
          if (format[0] !== "~") {
            scid = toIntIfInt(format);
          } else {
            scid = toIntIfInt(format.substring(1, format.length));
            paused = true;
          }
          return {
            scid,
            paused
          };
        });
      });
    };
  })(parser);
  return parser;
}
var writer;
var hasRequiredWriter;
function requireWriter() {
  if (hasRequiredWriter) return writer;
  hasRequiredWriter = 1;
  var grammar2 = requireGrammar();
  var formatRegExp = /%[sdv%]/g;
  var format = function(formatStr) {
    var i2 = 1;
    var args = arguments;
    var len = args.length;
    return formatStr.replace(formatRegExp, function(x) {
      if (i2 >= len) {
        return x;
      }
      var arg = args[i2];
      i2 += 1;
      switch (x) {
        case "%%":
          return "%";
        case "%s":
          return String(arg);
        case "%d":
          return Number(arg);
        case "%v":
          return "";
      }
    });
  };
  var makeLine = function(type, obj, location) {
    var str = obj.format instanceof Function ? obj.format(obj.push ? location : location[obj.name]) : obj.format;
    var args = [type + "=" + str];
    if (obj.names) {
      for (var i2 = 0; i2 < obj.names.length; i2 += 1) {
        var n = obj.names[i2];
        if (obj.name) {
          args.push(location[obj.name][n]);
        } else {
          args.push(location[obj.names[i2]]);
        }
      }
    } else {
      args.push(location[obj.name]);
    }
    return format.apply(null, args);
  };
  var defaultOuterOrder = ["v", "o", "s", "i", "u", "e", "p", "c", "b", "t", "r", "z", "a"];
  var defaultInnerOrder = ["i", "c", "b", "a"];
  writer = function(session, opts) {
    opts = opts || {};
    if (session.version == null) {
      session.version = 0;
    }
    if (session.name == null) {
      session.name = " ";
    }
    session.media.forEach(function(mLine) {
      if (mLine.payloads == null) {
        mLine.payloads = "";
      }
    });
    var outerOrder = opts.outerOrder || defaultOuterOrder;
    var innerOrder = opts.innerOrder || defaultInnerOrder;
    var sdp2 = [];
    outerOrder.forEach(function(type) {
      grammar2[type].forEach(function(obj) {
        if (obj.name in session && session[obj.name] != null) {
          sdp2.push(makeLine(type, obj, session));
        } else if (obj.push in session && session[obj.push] != null) {
          session[obj.push].forEach(function(el) {
            sdp2.push(makeLine(type, obj, el));
          });
        }
      });
    });
    session.media.forEach(function(mLine) {
      sdp2.push(makeLine("m", grammar2.m[0], mLine));
      innerOrder.forEach(function(type) {
        grammar2[type].forEach(function(obj) {
          if (obj.name in mLine && mLine[obj.name] != null) {
            sdp2.push(makeLine(type, obj, mLine));
          } else if (obj.push in mLine && mLine[obj.push] != null) {
            mLine[obj.push].forEach(function(el) {
              sdp2.push(makeLine(type, obj, el));
            });
          }
        });
      });
    });
    return sdp2.join("\r\n") + "\r\n";
  };
  return writer;
}
var hasRequiredLib;
function requireLib() {
  if (hasRequiredLib) return lib;
  hasRequiredLib = 1;
  var parser2 = requireParser();
  var writer2 = requireWriter();
  var grammar2 = requireGrammar();
  lib.grammar = grammar2;
  lib.write = writer2;
  lib.parse = parser2.parse;
  lib.parseParams = parser2.parseParams;
  lib.parseFmtpConfig = parser2.parseFmtpConfig;
  lib.parsePayloads = parser2.parsePayloads;
  lib.parseRemoteCandidates = parser2.parseRemoteCandidates;
  lib.parseImageAttributes = parser2.parseImageAttributes;
  lib.parseSimulcastStreamList = parser2.parseSimulcastStreamList;
  return lib;
}
var libExports = requireLib();
var startBitrateMultiplier = 0.9;
var maxStartBitrateKbps = 1e3;
var debounceInterval = 20;
var PCEvents = {
  NegotiationStarted: "negotiationStarted",
  NegotiationComplete: "negotiationComplete",
  // Fired with the offerId for every successful publisher answer application,
  // including answers that immediately recurse into another offer via
  // `renegotiate`. Use this rather than NegotiationComplete to know that a
  // specific offer has been negotiated end-to-end.
  OfferAnswered: "offerAnswered",
  RTPVideoPayloadTypes: "rtpVideoPayloadTypes"
};
var PCTransport = class extends eventsExports.EventEmitter {
  get pc() {
    if (!this._pc) {
      this._pc = this.createPC();
    }
    return this._pc;
  }
  constructor(config) {
    let loggerOptions = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : {};
    var _a2;
    super();
    this.log = livekitLogger;
    this.iceLog = livekitLogger;
    this.ddExtID = 0;
    this.latestOfferId = 0;
    this.latestAcknowledgedOfferId = 0;
    this.pendingCandidates = [];
    this.restartingIce = false;
    this.renegotiate = false;
    this.trackBitrates = [];
    this.remoteStereoMids = [];
    this.remoteNackMids = [];
    this.negotiate = debounce((onError) => __awaiter(this, void 0, void 0, function* () {
      this.emit(PCEvents.NegotiationStarted);
      try {
        yield this.createAndSendOffer();
      } catch (e4) {
        if (onError) {
          onError(e4);
        } else {
          throw e4;
        }
      }
    }), debounceInterval);
    this.close = () => {
      if (!this._pc) {
        return;
      }
      this.log.debug("closing peer connection");
      this.pendingInitialOffer = void 0;
      this._pc.close();
      this._pc.onconnectionstatechange = null;
      this._pc.oniceconnectionstatechange = null;
      this._pc.onicegatheringstatechange = null;
      this._pc.ondatachannel = null;
      this._pc.onnegotiationneeded = null;
      this._pc.onsignalingstatechange = null;
      this._pc.onicecandidate = null;
      this._pc.ondatachannel = null;
      this._pc.ontrack = null;
      this._pc.onconnectionstatechange = null;
      this._pc.oniceconnectionstatechange = null;
      this._pc = null;
    };
    this.loggerOptions = loggerOptions;
    this.log = getLogger((_a2 = loggerOptions.loggerName) !== null && _a2 !== void 0 ? _a2 : LoggerNames.PCTransport, () => this.logContext);
    this.iceLog = getLogger(LoggerNames.ICE, () => this.logContext);
    this.config = config;
    this._pc = this.createPC();
    this.offerLock = new _();
  }
  createPC() {
    const pc = new RTCPeerConnection(this.config);
    pc.onicecandidate = (ev) => {
      var _a2;
      if (!ev.candidate) return;
      this.iceLog.debug("local ICE candidate gathered", {
        candidate: ev.candidate.candidate
      });
      (_a2 = this.onIceCandidate) === null || _a2 === void 0 ? void 0 : _a2.call(this, ev.candidate);
    };
    pc.onicecandidateerror = (ev) => {
      var _a2;
      this.iceLog.debug("ICE candidate error", {
        event: ev
      });
      (_a2 = this.onIceCandidateError) === null || _a2 === void 0 ? void 0 : _a2.call(this, ev);
    };
    pc.oniceconnectionstatechange = () => {
      var _a2;
      this.iceLog.debug("ICE connection state: ".concat(pc.iceConnectionState));
      (_a2 = this.onIceConnectionStateChange) === null || _a2 === void 0 ? void 0 : _a2.call(this, pc.iceConnectionState);
    };
    pc.onsignalingstatechange = () => {
      var _a2;
      this.log.debug("signaling state: ".concat(pc.signalingState));
      (_a2 = this.onSignalingStatechange) === null || _a2 === void 0 ? void 0 : _a2.call(this, pc.signalingState);
    };
    pc.onconnectionstatechange = () => {
      var _a2;
      this.log.debug("connection state: ".concat(pc.connectionState));
      (_a2 = this.onConnectionStateChange) === null || _a2 === void 0 ? void 0 : _a2.call(this, pc.connectionState);
    };
    pc.ondatachannel = (ev) => {
      var _a2;
      this.log.debug("data channel opened by peer", {
        label: ev.channel.label,
        id: ev.channel.id
      });
      (_a2 = this.onDataChannel) === null || _a2 === void 0 ? void 0 : _a2.call(this, ev);
    };
    pc.ontrack = (ev) => {
      var _a2;
      (_a2 = this.onTrack) === null || _a2 === void 0 ? void 0 : _a2.call(this, ev);
    };
    return pc;
  }
  get logContext() {
    var _a2, _b2;
    return Object.assign({}, (_b2 = (_a2 = this.loggerOptions).loggerContextCb) === null || _b2 === void 0 ? void 0 : _b2.call(_a2));
  }
  get isICEConnected() {
    return this._pc !== null && (this.pc.iceConnectionState === "connected" || this.pc.iceConnectionState === "completed");
  }
  addIceCandidate(candidate) {
    return __awaiter(this, void 0, void 0, function* () {
      if (this.pc.remoteDescription && !this.restartingIce) {
        return this.pc.addIceCandidate(candidate);
      }
      this.iceLog.debug("queuing remote ICE candidate until remote description applied", {
        pendingCount: this.pendingCandidates.length + 1
      });
      this.pendingCandidates.push(candidate);
    });
  }
  setRemoteDescription(sd, offerId) {
    return __awaiter(this, void 0, void 0, function* () {
      var _a2, _b2;
      if (sd.type === "answer" && this.latestOfferId > 0 && offerId > 0 && offerId !== this.latestOfferId) {
        this.log.warn("ignoring answer for old offer", {
          offerId,
          latestOfferId: this.latestOfferId
        });
        return false;
      }
      let mungedSDP = void 0;
      if (sd.type === "offer") {
        let _extractStereoAndNack = extractStereoAndNackAudioFromOffer(sd), stereoMids = _extractStereoAndNack.stereoMids, nackMids = _extractStereoAndNack.nackMids;
        this.remoteStereoMids = stereoMids;
        this.remoteNackMids = nackMids;
      } else if (sd.type === "answer") {
        if (this.pendingInitialOffer && this._pc) {
          const initialOffer = this.pendingInitialOffer;
          this.pendingInitialOffer = void 0;
          const sdpParsed2 = libExports.parse((_a2 = initialOffer.sdp) !== null && _a2 !== void 0 ? _a2 : "");
          sdpParsed2.media.forEach((media) => {
            ensureIPAddrMatchVersion(media);
          });
          this.log.debug("setting pending initial offer before processing answer");
          yield this.setMungedSDP(initialOffer, libExports.write(sdpParsed2));
        }
        const sdpParsed = libExports.parse((_b2 = sd.sdp) !== null && _b2 !== void 0 ? _b2 : "");
        sdpParsed.media.forEach((media) => {
          const mid = getMidString(media.mid);
          if (media.type === "audio") {
            this.trackBitrates.some((trackbr) => {
              if (!trackbr.transceiver || mid != trackbr.transceiver.mid) {
                return false;
              }
              let codecPayload = 0;
              media.rtp.some((rtp) => {
                if (rtp.codec.toUpperCase() === trackbr.codec.toUpperCase()) {
                  codecPayload = rtp.payload;
                  return true;
                }
                return false;
              });
              if (codecPayload === 0) {
                return true;
              }
              let fmtpFound = false;
              for (const fmtp of media.fmtp) {
                if (fmtp.payload === codecPayload) {
                  fmtp.config = fmtp.config.split(";").filter((attr) => !attr.includes("maxaveragebitrate")).join(";");
                  if (trackbr.maxbr > 0) {
                    fmtp.config += ";maxaveragebitrate=".concat(trackbr.maxbr * 1e3);
                  }
                  fmtpFound = true;
                  break;
                }
              }
              if (!fmtpFound) {
                if (trackbr.maxbr > 0) {
                  media.fmtp.push({
                    payload: codecPayload,
                    config: "maxaveragebitrate=".concat(trackbr.maxbr * 1e3)
                  });
                }
              }
              return true;
            });
          }
        });
        const placeholderMids = this.getPlaceholderMids();
        if (placeholderMids.size > 0) {
          conformBundledCodecFmtp(sdpParsed.media, (media) => placeholderMids.has(getMidString(media.mid)));
        }
        mungedSDP = libExports.write(sdpParsed);
      }
      yield this.setMungedSDP(sd, mungedSDP, true);
      if (this.pendingCandidates.length > 0) {
        this.iceLog.debug("flushing queued ICE candidates", {
          count: this.pendingCandidates.length
        });
      }
      this.pendingCandidates.forEach((candidate) => {
        this.pc.addIceCandidate(candidate);
      });
      this.pendingCandidates = [];
      this.restartingIce = false;
      if (sd.type === "answer") {
        this.latestAcknowledgedOfferId = offerId;
        this.emit(PCEvents.OfferAnswered, offerId);
      }
      if (this.renegotiate) {
        this.renegotiate = false;
        yield this.createAndSendOffer();
      } else if (sd.type === "answer") {
        this.emit(PCEvents.NegotiationComplete);
        if (sd.sdp) {
          const sdpParsed = libExports.parse(sd.sdp);
          sdpParsed.media.forEach((media) => {
            if (media.type === "video") {
              this.emit(PCEvents.RTPVideoPayloadTypes, media.rtp);
            }
          });
        }
      }
      return true;
    });
  }
  createInitialOffer() {
    return __awaiter(this, void 0, void 0, function* () {
      var _a2;
      const unlock = yield this.offerLock.lock();
      try {
        if (this.pc.signalingState !== "stable") {
          this.log.warn("signaling state is not stable, cannot create initial offer");
          return;
        }
        const offerId = this.latestOfferId + 1;
        this.latestOfferId = offerId;
        const offer = yield this.pc.createOffer();
        this.pendingInitialOffer = {
          sdp: offer.sdp,
          type: offer.type
        };
        const sdpParsed = libExports.parse((_a2 = offer.sdp) !== null && _a2 !== void 0 ? _a2 : "");
        sdpParsed.media.forEach((media) => {
          ensureIPAddrMatchVersion(media);
        });
        offer.sdp = libExports.write(sdpParsed);
        return {
          offer,
          offerId
        };
      } finally {
        unlock();
      }
    });
  }
  createAndSendOffer(options) {
    return __awaiter(this, void 0, void 0, function* () {
      var _a2;
      const unlock = yield this.offerLock.lock();
      try {
        if (this.onOffer === void 0) {
          return;
        }
        if (options === null || options === void 0 ? void 0 : options.iceRestart) {
          this.iceLog.debug("restarting ICE");
          this.restartingIce = true;
        }
        if (this._pc && (this._pc.signalingState === "have-local-offer" || this.pendingInitialOffer)) {
          const currentSD = this._pc.remoteDescription;
          if ((options === null || options === void 0 ? void 0 : options.iceRestart) && currentSD) {
            yield this._pc.setRemoteDescription(currentSD);
          } else {
            this.renegotiate = true;
            this.log.debug("requesting renegotiation");
            return;
          }
        } else if (!this._pc || this._pc.signalingState === "closed") {
          this.log.warn("could not createOffer with closed peer connection");
          return;
        }
        this.log.debug("starting to negotiate");
        const offerId = this.latestOfferId + 1;
        this.latestOfferId = offerId;
        const offer = yield this.pc.createOffer(options);
        this.log.debug("original offer", {
          sdp: offer.sdp
        });
        const sdpParsed = libExports.parse((_a2 = offer.sdp) !== null && _a2 !== void 0 ? _a2 : "");
        sdpParsed.media.forEach((media) => {
          ensureIPAddrMatchVersion(media);
          if (media.type === "audio") {
            ensureAudioNackAndStereo(media, ["all"], []);
          } else if (media.type === "video") {
            this.trackBitrates.some((trackbr) => {
              if (!media.msid || !trackbr.cid || !media.msid.includes(trackbr.cid)) {
                return false;
              }
              let codecPayload = 0;
              media.rtp.some((rtp) => {
                if (rtp.codec.toUpperCase() === trackbr.codec.toUpperCase()) {
                  codecPayload = rtp.payload;
                  return true;
                }
                return false;
              });
              if (codecPayload === 0) {
                return true;
              }
              if (isSVCCodec(trackbr.codec) && !isSafari()) {
                this.ensureVideoDDExtensionForSVC(media, sdpParsed);
              }
              const calculatedStartBitrate = Math.round(trackbr.maxbr * startBitrateMultiplier);
              const startBitrate = trackbr.isScreenShare ? calculatedStartBitrate : Math.min(calculatedStartBitrate, maxStartBitrateKbps);
              let fmtpFound = false;
              for (const fmtp of media.fmtp) {
                if (fmtp.payload === codecPayload) {
                  fmtpFound = true;
                  if (!fmtp.config.includes("x-google-start-bitrate")) {
                    fmtp.config += ";x-google-start-bitrate=".concat(startBitrate);
                  }
                  break;
                }
              }
              if (!fmtpFound) {
                media.fmtp.push({
                  payload: codecPayload,
                  config: "x-google-start-bitrate=".concat(startBitrate)
                });
              }
              return true;
            });
          }
        });
        const placeholderMids = this.getPlaceholderMids();
        if (placeholderMids.size > 0) {
          conformBundledCodecFmtp(sdpParsed.media, (media) => placeholderMids.has(getMidString(media.mid)));
        }
        if (this.latestOfferId > offerId) {
          this.log.warn("latestOfferId mismatch", {
            latestOfferId: this.latestOfferId,
            offerId
          });
          return;
        }
        yield this.setMungedSDP(offer, libExports.write(sdpParsed));
        this.onOffer(offer, this.latestOfferId);
      } finally {
        unlock();
      }
    });
  }
  createAndSetAnswer() {
    return __awaiter(this, void 0, void 0, function* () {
      var _a2;
      const answer = yield this.pc.createAnswer();
      const sdpParsed = libExports.parse((_a2 = answer.sdp) !== null && _a2 !== void 0 ? _a2 : "");
      sdpParsed.media.forEach((media) => {
        ensureIPAddrMatchVersion(media);
        if (media.type === "audio") {
          ensureAudioNackAndStereo(media, this.remoteStereoMids, this.remoteNackMids);
        }
      });
      yield this.setMungedSDP(answer, libExports.write(sdpParsed));
      return answer;
    });
  }
  /**
   * Returns the mids of transceivers that carry no outgoing track on this
   * (publisher) connection: the pre-populated placeholders added by
   * `RTCEngine.applyInitialPublisherLayout`, plus any transceiver that was used
   * for a track and reverted on unpublish. Their codec fmtp is conformed to the
   * published tracks so a shared payload type stays consistent across the bundle.
   */
  getPlaceholderMids() {
    var _a2, _b2;
    return placeholderMidsFromTransceivers((_b2 = (_a2 = this._pc) === null || _a2 === void 0 ? void 0 : _a2.getTransceivers()) !== null && _b2 !== void 0 ? _b2 : []);
  }
  createDataChannel(label, dataChannelDict) {
    return this.pc.createDataChannel(label, dataChannelDict);
  }
  addTransceiver(mediaStreamTrack, transceiverInit) {
    return this.pc.addTransceiver(mediaStreamTrack, transceiverInit);
  }
  addTransceiverOfKind(kind, transceiverInit) {
    return this.pc.addTransceiver(kind, transceiverInit);
  }
  addTrack(track) {
    if (!this._pc) {
      throw new UnexpectedConnectionState("PC closed, cannot add track");
    }
    return this._pc.addTrack(track);
  }
  setTrackCodecBitrate(info) {
    this.trackBitrates.push(info);
  }
  setConfiguration(rtcConfig) {
    var _a2;
    if (!this._pc) {
      throw new UnexpectedConnectionState("PC closed, cannot configure");
    }
    return (_a2 = this._pc) === null || _a2 === void 0 ? void 0 : _a2.setConfiguration(rtcConfig);
  }
  canRemoveTrack() {
    var _a2;
    return !!((_a2 = this._pc) === null || _a2 === void 0 ? void 0 : _a2.removeTrack);
  }
  removeTrack(sender) {
    var _a2;
    return (_a2 = this._pc) === null || _a2 === void 0 ? void 0 : _a2.removeTrack(sender);
  }
  getConnectionState() {
    var _a2, _b2;
    return (_b2 = (_a2 = this._pc) === null || _a2 === void 0 ? void 0 : _a2.connectionState) !== null && _b2 !== void 0 ? _b2 : "closed";
  }
  getICEConnectionState() {
    var _a2, _b2;
    return (_b2 = (_a2 = this._pc) === null || _a2 === void 0 ? void 0 : _a2.iceConnectionState) !== null && _b2 !== void 0 ? _b2 : "closed";
  }
  getSignallingState() {
    var _a2, _b2;
    return (_b2 = (_a2 = this._pc) === null || _a2 === void 0 ? void 0 : _a2.signalingState) !== null && _b2 !== void 0 ? _b2 : "closed";
  }
  getTransceivers() {
    var _a2, _b2;
    return (_b2 = (_a2 = this._pc) === null || _a2 === void 0 ? void 0 : _a2.getTransceivers()) !== null && _b2 !== void 0 ? _b2 : [];
  }
  getSenders() {
    var _a2, _b2;
    return (_b2 = (_a2 = this._pc) === null || _a2 === void 0 ? void 0 : _a2.getSenders()) !== null && _b2 !== void 0 ? _b2 : [];
  }
  getLocalDescription() {
    var _a2;
    return (_a2 = this._pc) === null || _a2 === void 0 ? void 0 : _a2.localDescription;
  }
  getRemoteDescription() {
    var _a2;
    return (_a2 = this.pc) === null || _a2 === void 0 ? void 0 : _a2.remoteDescription;
  }
  getStats() {
    return this.pc.getStats();
  }
  getMaxMessageSize() {
    var _a2, _b2;
    return (_b2 = (_a2 = this._pc) === null || _a2 === void 0 ? void 0 : _a2.sctp) === null || _b2 === void 0 ? void 0 : _b2.maxMessageSize;
  }
  getConnectedAddress() {
    return __awaiter(this, void 0, void 0, function* () {
      var _a2;
      if (!this._pc) {
        return;
      }
      let selectedCandidatePairId = "";
      const candidatePairs = /* @__PURE__ */ new Map();
      const candidates = /* @__PURE__ */ new Map();
      const stats = yield this._pc.getStats();
      stats.forEach((v) => {
        switch (v.type) {
          case "transport":
            selectedCandidatePairId = v.selectedCandidatePairId;
            break;
          case "candidate-pair":
            if (selectedCandidatePairId === "" && v.selected) {
              selectedCandidatePairId = v.id;
            }
            candidatePairs.set(v.id, v);
            break;
          case "remote-candidate":
            candidates.set(v.id, "".concat(v.address, ":").concat(v.port));
            break;
        }
      });
      if (selectedCandidatePairId === "") {
        return void 0;
      }
      const selectedID = (_a2 = candidatePairs.get(selectedCandidatePairId)) === null || _a2 === void 0 ? void 0 : _a2.remoteCandidateId;
      if (selectedID === void 0) {
        return void 0;
      }
      return candidates.get(selectedID);
    });
  }
  setMungedSDP(sd, munged, remote) {
    return __awaiter(this, void 0, void 0, function* () {
      var _a2, _b2;
      const originalSdp = sd.sdp;
      if (munged) {
        sd.sdp = munged;
        try {
          this.log.debug("setting munged ".concat(remote ? "remote" : "local", " description"));
          if (remote) {
            yield this.pc.setRemoteDescription(sd);
          } else {
            yield this.pc.setLocalDescription(sd);
          }
          return;
        } catch (e4) {
          this.log.warn("not able to set ".concat(sd.type, ", falling back to unmodified sdp"), {
            error: e4,
            mungedSdp: munged,
            originalSdp
          });
          sd.sdp = originalSdp;
        }
      }
      try {
        if (remote) {
          yield (_a2 = this._pc) === null || _a2 === void 0 ? void 0 : _a2.setRemoteDescription(sd);
        } else {
          yield (_b2 = this._pc) === null || _b2 === void 0 ? void 0 : _b2.setLocalDescription(sd);
        }
      } catch (e4) {
        let msg = "unknown error";
        if (e4 instanceof Error) {
          msg = e4.message;
        } else if (typeof e4 === "string") {
          msg = e4;
        }
        const fields = {
          error: msg,
          sdp: sd.sdp
        };
        if (munged && munged !== originalSdp) {
          fields.mungedSdp = munged;
        }
        if (!remote && this.pc.remoteDescription) {
          fields.remoteSdp = this.pc.remoteDescription;
        }
        this.log.error("unable to set ".concat(sd.type), {
          fields
        });
        throw new NegotiationError(msg);
      }
    });
  }
  ensureVideoDDExtensionForSVC(media, sdp2) {
    var _a2, _b2;
    const ddFound = (_a2 = media.ext) === null || _a2 === void 0 ? void 0 : _a2.some((ext) => {
      if (ext.uri === ddExtensionURI) {
        return true;
      }
      return false;
    });
    if (!ddFound) {
      if (this.ddExtID === 0) {
        let maxID = 0;
        sdp2.media.forEach((m) => {
          var _a3;
          (_a3 = m.ext) === null || _a3 === void 0 ? void 0 : _a3.forEach((ext) => {
            if (ext.value > maxID) {
              maxID = ext.value;
            }
          });
        });
        this.ddExtID = maxID + 1;
      }
      (_b2 = media.ext) === null || _b2 === void 0 ? void 0 : _b2.push({
        value: this.ddExtID,
        uri: ddExtensionURI
      });
    }
  }
};
function ensureAudioNackAndStereo(media, stereoMids, nackMids) {
  const mid = getMidString(media.mid);
  let opusPayload = 0;
  media.rtp.some((rtp) => {
    if (rtp.codec === "opus") {
      opusPayload = rtp.payload;
      return true;
    }
    return false;
  });
  if (opusPayload > 0) {
    if (!media.rtcpFb) {
      media.rtcpFb = [];
    }
    if (nackMids.includes(mid) && !media.rtcpFb.some((fb) => fb.payload === opusPayload && fb.type === "nack")) {
      media.rtcpFb.push({
        payload: opusPayload,
        type: "nack"
      });
    }
    if (stereoMids.includes(mid) || stereoMids.length === 1 && stereoMids[0] === "all") {
      media.fmtp.some((fmtp) => {
        if (fmtp.payload === opusPayload) {
          if (!fmtp.config.includes("stereo=1")) {
            fmtp.config += ";stereo=1";
          }
          return true;
        }
        return false;
      });
    }
  }
}
function placeholderMidsFromTransceivers(transceivers) {
  const mids = /* @__PURE__ */ new Set();
  for (const transceiver of transceivers) {
    if (transceiver.mid && !transceiver.sender.track) {
      mids.add(transceiver.mid);
    }
  }
  return mids;
}
function conformBundledCodecFmtp(media, isPlaceholder) {
  var _a2, _b2;
  const canonicalByPayload = /* @__PURE__ */ new Map();
  const fromRealSection = /* @__PURE__ */ new Set();
  for (const m of media) {
    const placeholder = isPlaceholder(m);
    for (const fmtp of (_a2 = m.fmtp) !== null && _a2 !== void 0 ? _a2 : []) {
      if (!placeholder) {
        canonicalByPayload.set(fmtp.payload, fmtp.config);
        fromRealSection.add(fmtp.payload);
      } else if (!canonicalByPayload.has(fmtp.payload)) {
        canonicalByPayload.set(fmtp.payload, fmtp.config);
      }
    }
  }
  if (canonicalByPayload.size === 0) {
    return;
  }
  for (const m of media) {
    if (!isPlaceholder(m)) {
      continue;
    }
    for (const fmtp of (_b2 = m.fmtp) !== null && _b2 !== void 0 ? _b2 : []) {
      const config = canonicalByPayload.get(fmtp.payload);
      if (config !== void 0 && fmtp.config !== config) {
        fmtp.config = config;
      }
    }
  }
}
function extractStereoAndNackAudioFromOffer(offer) {
  var _a2;
  const stereoMids = [];
  const nackMids = [];
  const sdpParsed = libExports.parse((_a2 = offer.sdp) !== null && _a2 !== void 0 ? _a2 : "");
  let opusPayload = 0;
  sdpParsed.media.forEach((media) => {
    var _a3;
    const mid = getMidString(media.mid);
    if (media.type === "audio") {
      media.rtp.some((rtp) => {
        if (rtp.codec === "opus") {
          opusPayload = rtp.payload;
          return true;
        }
        return false;
      });
      if ((_a3 = media.rtcpFb) === null || _a3 === void 0 ? void 0 : _a3.some((fb) => fb.payload === opusPayload && fb.type === "nack")) {
        nackMids.push(mid);
      }
      media.fmtp.some((fmtp) => {
        if (fmtp.payload === opusPayload) {
          if (fmtp.config.includes("sprop-stereo=1")) {
            stereoMids.push(mid);
          }
          return true;
        }
        return false;
      });
    }
  });
  return {
    stereoMids,
    nackMids
  };
}
function ensureIPAddrMatchVersion(media) {
  if (media.connection) {
    const isV6 = media.connection.ip.indexOf(":") >= 0;
    if (media.connection.version === 4 && isV6 || media.connection.version === 6 && !isV6) {
      media.connection.ip = "0.0.0.0";
      media.connection.version = 4;
    }
  }
}
function getMidString(mid) {
  return typeof mid === "number" ? mid.toFixed(0) : mid;
}
var defaultVideoCodec = "vp8";
var publishDefaults = {
  audioPreset: AudioPresets.music,
  dtx: true,
  red: true,
  forceStereo: false,
  simulcast: true,
  screenShareEncoding: ScreenSharePresets.h1080fps15.encoding,
  stopMicTrackOnMute: false,
  videoCodec: defaultVideoCodec,
  backupCodec: true,
  preConnectBuffer: false
};
var audioDefaults = {
  deviceId: {
    ideal: "default"
  },
  autoGainControl: true,
  echoCancellation: true,
  noiseSuppression: true,
  voiceIsolation: true
};
var videoDefaults = {
  deviceId: {
    ideal: "default"
  },
  resolution: VideoPresets.h720.resolution
};
var roomOptionDefaults = {
  adaptiveStream: false,
  dynacast: false,
  stopLocalTrackOnUnpublish: true,
  reconnectPolicy: new DefaultReconnectPolicy(),
  disconnectOnPageLeave: true,
  webAudioMix: false,
  singlePeerConnection: true
};
var roomConnectOptionDefaults = {
  autoSubscribe: true,
  maxRetries: 1,
  peerConnectionTimeout: 15e3,
  websocketTimeout: 15e3
};
var PCTransportState;
(function(PCTransportState2) {
  PCTransportState2[PCTransportState2["NEW"] = 0] = "NEW";
  PCTransportState2[PCTransportState2["CONNECTING"] = 1] = "CONNECTING";
  PCTransportState2[PCTransportState2["CONNECTED"] = 2] = "CONNECTED";
  PCTransportState2[PCTransportState2["FAILED"] = 3] = "FAILED";
  PCTransportState2[PCTransportState2["CLOSING"] = 4] = "CLOSING";
  PCTransportState2[PCTransportState2["CLOSED"] = 5] = "CLOSED";
})(PCTransportState || (PCTransportState = {}));
var PCTransportManager = class {
  get needsPublisher() {
    return this.isPublisherConnectionRequired;
  }
  get needsSubscriber() {
    return this.isSubscriberConnectionRequired;
  }
  get currentState() {
    return this.state;
  }
  get mode() {
    return this._mode;
  }
  constructor(mode, loggerOptions, rtcConfig) {
    var _a2;
    this.peerConnectionTimeout = roomConnectOptionDefaults.peerConnectionTimeout;
    this.log = livekitLogger;
    this.iceLog = livekitLogger;
    this.updateState = () => {
      var _a3, _b2;
      const previousState = this.state;
      const connectionStates = this.requiredTransports.map((tr3) => tr3.getConnectionState());
      if (connectionStates.every((st2) => st2 === "connected")) {
        this.state = PCTransportState.CONNECTED;
      } else if (connectionStates.some((st2) => st2 === "failed")) {
        this.state = PCTransportState.FAILED;
      } else if (connectionStates.some((st2) => st2 === "connecting")) {
        this.state = PCTransportState.CONNECTING;
      } else if (connectionStates.every((st2) => st2 === "closed")) {
        this.state = PCTransportState.CLOSED;
      } else if (connectionStates.some((st2) => st2 === "closed")) {
        this.state = PCTransportState.CLOSING;
      } else if (connectionStates.every((st2) => st2 === "new")) {
        this.state = PCTransportState.NEW;
      }
      if (previousState !== this.state) {
        this.log.debug("pc state change: from ".concat(PCTransportState[previousState], " to ").concat(PCTransportState[this.state]));
        (_a3 = this.onStateChange) === null || _a3 === void 0 ? void 0 : _a3.call(this, this.state, this.publisher.getConnectionState(), (_b2 = this.subscriber) === null || _b2 === void 0 ? void 0 : _b2.getConnectionState());
      }
    };
    this.loggerOptions = loggerOptions;
    this.log = getLogger((_a2 = loggerOptions.loggerName) !== null && _a2 !== void 0 ? _a2 : LoggerNames.PCManager, () => this.logContext);
    this.iceLog = getLogger(LoggerNames.ICE, () => this.logContext);
    this.isPublisherConnectionRequired = mode !== "subscriber-primary";
    this.isSubscriberConnectionRequired = mode === "subscriber-primary";
    this.publisher = new PCTransport(rtcConfig, loggerOptions);
    this._mode = mode;
    if (mode !== "publisher-only") {
      this.subscriber = new PCTransport(rtcConfig, loggerOptions);
      this.subscriber.onConnectionStateChange = this.updateState;
      this.subscriber.onIceConnectionStateChange = this.updateState;
      this.subscriber.onSignalingStatechange = this.updateState;
      this.subscriber.onIceCandidate = (candidate) => {
        var _a3;
        (_a3 = this.onIceCandidate) === null || _a3 === void 0 ? void 0 : _a3.call(this, candidate, SignalTarget.SUBSCRIBER);
      };
      this.subscriber.onDataChannel = (ev) => {
        var _a3;
        (_a3 = this.onDataChannel) === null || _a3 === void 0 ? void 0 : _a3.call(this, ev);
      };
      this.subscriber.onTrack = (ev) => {
        var _a3;
        (_a3 = this.onTrack) === null || _a3 === void 0 ? void 0 : _a3.call(this, ev);
      };
    }
    this.publisher.onConnectionStateChange = this.updateState;
    this.publisher.onIceConnectionStateChange = this.updateState;
    this.publisher.onSignalingStatechange = this.updateState;
    this.publisher.onIceCandidate = (candidate) => {
      var _a3;
      (_a3 = this.onIceCandidate) === null || _a3 === void 0 ? void 0 : _a3.call(this, candidate, SignalTarget.PUBLISHER);
    };
    this.publisher.onTrack = (ev) => {
      var _a3;
      (_a3 = this.onTrack) === null || _a3 === void 0 ? void 0 : _a3.call(this, ev);
    };
    this.publisher.onOffer = (offer, offerId) => {
      var _a3;
      (_a3 = this.onPublisherOffer) === null || _a3 === void 0 ? void 0 : _a3.call(this, offer, offerId);
    };
    this.state = PCTransportState.NEW;
    this.connectionLock = new _();
    this.remoteOfferLock = new _();
  }
  get logContext() {
    var _a2, _b2;
    return Object.assign({}, (_b2 = (_a2 = this.loggerOptions).loggerContextCb) === null || _b2 === void 0 ? void 0 : _b2.call(_a2));
  }
  requirePublisher() {
    let require2 = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : true;
    this.isPublisherConnectionRequired = require2;
    this.updateState();
  }
  createAndSendPublisherOffer(options) {
    return this.publisher.createAndSendOffer(options);
  }
  setPublisherAnswer(sd, offerId) {
    return this.publisher.setRemoteDescription(sd, offerId);
  }
  removeTrack(sender) {
    return this.publisher.removeTrack(sender);
  }
  close() {
    return __awaiter(this, void 0, void 0, function* () {
      var _a2;
      if (this.publisher && this.publisher.getSignallingState() !== "closed") {
        const publisher = this.publisher;
        for (const sender of publisher.getSenders()) {
          try {
            if (publisher.canRemoveTrack()) {
              publisher.removeTrack(sender);
            }
          } catch (e4) {
            this.log.warn("could not removeTrack", {
              error: e4
            });
          }
        }
      }
      yield Promise.all([this.publisher.close(), (_a2 = this.subscriber) === null || _a2 === void 0 ? void 0 : _a2.close()]);
      this.updateState();
    });
  }
  triggerIceRestart() {
    return __awaiter(this, void 0, void 0, function* () {
      this.iceLog.warn("triggering ICE restart");
      if (this.subscriber) {
        this.subscriber.restartingIce = true;
      }
      if (this.needsPublisher) {
        yield this.createAndSendPublisherOffer({
          iceRestart: true
        });
      }
    });
  }
  addIceCandidate(candidate, target) {
    return __awaiter(this, void 0, void 0, function* () {
      var _a2;
      this.iceLog.debug("adding remote ICE candidate", {
        target,
        candidate
      });
      if (target === SignalTarget.PUBLISHER) {
        yield this.publisher.addIceCandidate(candidate);
      } else {
        yield (_a2 = this.subscriber) === null || _a2 === void 0 ? void 0 : _a2.addIceCandidate(candidate);
      }
    });
  }
  createSubscriberAnswerFromOffer(sd, offerId) {
    return __awaiter(this, void 0, void 0, function* () {
      var _a2, _b2, _c;
      this.log.debug("received server offer", {
        RTCSdpType: sd.type,
        sdp: sd.sdp,
        signalingState: (_a2 = this.subscriber) === null || _a2 === void 0 ? void 0 : _a2.getSignallingState().toString()
      });
      const unlock = yield this.remoteOfferLock.lock();
      try {
        const success = yield (_b2 = this.subscriber) === null || _b2 === void 0 ? void 0 : _b2.setRemoteDescription(sd, offerId);
        if (!success) {
          return void 0;
        }
        const answer = yield (_c = this.subscriber) === null || _c === void 0 ? void 0 : _c.createAndSetAnswer();
        return answer;
      } finally {
        unlock();
      }
    });
  }
  updateConfiguration(config, iceRestart) {
    var _a2;
    this.log.debug("updating rtc configuration", {
      iceRestart
    });
    this.publisher.setConfiguration(config);
    (_a2 = this.subscriber) === null || _a2 === void 0 ? void 0 : _a2.setConfiguration(config);
    if (iceRestart) {
      this.triggerIceRestart();
    }
  }
  ensurePCTransportConnection(abortController, timeout) {
    return __awaiter(this, void 0, void 0, function* () {
      var _a2;
      const unlock = yield this.connectionLock.lock();
      try {
        if (this.isPublisherConnectionRequired && this.publisher.getConnectionState() !== "connected" && this.publisher.getConnectionState() !== "connecting") {
          this.log.debug("negotiation required, start negotiating");
          this.publisher.negotiate();
        }
        yield Promise.all((_a2 = this.requiredTransports) === null || _a2 === void 0 ? void 0 : _a2.map((transport) => this.ensureTransportConnected(transport, abortController, timeout)));
      } finally {
        unlock();
      }
    });
  }
  negotiate(abortController) {
    return __awaiter(this, void 0, void 0, function* () {
      return new TypedPromise((resolve, reject) => {
        const checkpoint = this.publisher.latestOfferId;
        if (this.publisher.latestAcknowledgedOfferId > checkpoint) {
          this.log.debug("negotiation already handled in more recent acknowledged offer", this.logContext);
          resolve();
          return;
        }
        let cleanedUp = false;
        const cleanup = () => {
          if (cleanedUp) return;
          cleanedUp = true;
          clearTimeout(deadlineTimer);
          this.publisher.off(PCEvents.OfferAnswered, onAnswered);
          abortController.signal.removeEventListener("abort", onAbort);
        };
        const onAnswered = (offerId) => {
          if (offerId > checkpoint) {
            cleanup();
            resolve();
          }
        };
        const onAbort = () => {
          cleanup();
          reject(new NegotiationError("negotiation aborted"));
        };
        const deadlineTimer = setTimeout(() => {
          cleanup();
          reject(new NegotiationError("negotiation timed out"));
        }, this.peerConnectionTimeout);
        abortController.signal.addEventListener("abort", onAbort);
        this.publisher.on(PCEvents.OfferAnswered, onAnswered);
        this.publisher.negotiate((e4) => {
          cleanup();
          if (e4 instanceof Error) {
            reject(e4);
          } else {
            reject(new Error(String(e4)));
          }
        });
      });
    });
  }
  addPublisherTransceiver(track, transceiverInit) {
    return this.publisher.addTransceiver(track, transceiverInit);
  }
  addPublisherTransceiverOfKind(kind, transceiverInit) {
    return this.publisher.addTransceiverOfKind(kind, transceiverInit);
  }
  getMidForReceiver(receiver) {
    const transceivers = this.subscriber ? this.subscriber.getTransceivers() : this.publisher.getTransceivers();
    const matchingTransceiver = transceivers.find((transceiver) => transceiver.receiver === receiver);
    return matchingTransceiver === null || matchingTransceiver === void 0 ? void 0 : matchingTransceiver.mid;
  }
  getMaxPublisherMessageSize() {
    return this.publisher.getMaxMessageSize();
  }
  addPublisherTrack(track) {
    return this.publisher.addTrack(track);
  }
  createPublisherDataChannel(label, dataChannelDict) {
    return this.publisher.createDataChannel(label, dataChannelDict);
  }
  /**
   * Returns the first required transport's address if no explicit target is specified
   */
  getConnectedAddress(target) {
    if (target === SignalTarget.PUBLISHER) {
      return this.publisher.getConnectedAddress();
    } else if (target === SignalTarget.SUBSCRIBER) {
      return this.publisher.getConnectedAddress();
    }
    return this.requiredTransports[0].getConnectedAddress();
  }
  get requiredTransports() {
    const transports = [];
    if (this.isPublisherConnectionRequired) {
      transports.push(this.publisher);
    }
    if (this.isSubscriberConnectionRequired && this.subscriber) {
      transports.push(this.subscriber);
    }
    return transports;
  }
  ensureTransportConnected(pcTransport_1, abortController_1) {
    return __awaiter(this, arguments, void 0, function(pcTransport, abortController) {
      var _this = this;
      let timeout = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : this.peerConnectionTimeout;
      return function* () {
        const connectionState = pcTransport.getConnectionState();
        if (connectionState === "connected") {
          return;
        }
        return new Promise((resolve, reject) => __awaiter(_this, void 0, void 0, function* () {
          const abortHandler = () => {
            this.log.warn("abort transport connection");
            CriticalTimers.clearTimeout(connectTimeout);
            reject(ConnectionError.cancelled("room connection has been cancelled"));
          };
          if (abortController === null || abortController === void 0 ? void 0 : abortController.signal.aborted) {
            abortHandler();
          }
          abortController === null || abortController === void 0 ? void 0 : abortController.signal.addEventListener("abort", abortHandler);
          const connectTimeout = CriticalTimers.setTimeout(() => {
            abortController === null || abortController === void 0 ? void 0 : abortController.signal.removeEventListener("abort", abortHandler);
            reject(ConnectionError.internal("could not establish pc connection"));
          }, timeout);
          while (this.state !== PCTransportState.CONNECTED) {
            yield sleep(50);
            if (abortController === null || abortController === void 0 ? void 0 : abortController.signal.aborted) {
              reject(ConnectionError.cancelled("room connection has been cancelled"));
              return;
            }
          }
          CriticalTimers.clearTimeout(connectTimeout);
          abortController === null || abortController === void 0 ? void 0 : abortController.signal.removeEventListener("abort", abortHandler);
          resolve();
        }));
      }();
    });
  }
};
var FlowControlledDataChannel = class {
  constructor(opts) {
    this.bufferStatusLow = true;
    this.headroomLock = new _();
    this.waiterAbortController = new AbortController();
    this.kind = opts.kind;
    this.lowWaterMark = opts.lowWaterMark;
    this.highWaterMark = opts.highWaterMark;
    this.isEngineClosed = opts.isEngineClosed;
    this.onBufferStatusChanged = opts.onBufferStatusChanged;
  }
  /** The currently attached RTCDataChannel handle, if any. */
  get channelHandle() {
    return this.handle;
  }
  /**
   * Attaches the channel handle this wrapper controls. Replacing an existing handle rejects
   * parked waiters — their events would never fire again on the abandoned object — and installs a
   * fresh controller, so queued senders re-check against the new channel. Wrappers outlive their
   * handles: this is the one place handle turnover happens, which is what makes stranding a
   * waiter structurally impossible.
   */
  attach(dc) {
    if (this.handle && this.handle !== dc) {
      this.invalidateWaiters("data channel replaced");
    }
    this.handle = dc;
  }
  /** Detaches the handle on teardown, rejecting parked waiters. */
  detach() {
    let reason = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : "data channel torn down";
    if (this.handle) {
      this.invalidateWaiters(reason);
    }
    this.handle = void 0;
  }
  getChannel() {
    return this.handle;
  }
  /**
   * Whether the send buffer has room to accept more data (the send gate). Senders proceed while
   * this is true and block once it goes false. Callers resolve the handle (and decide what an
   * absent one means) before asking.
   */
  isBelowHighWaterMark(dc) {
    return dc.bufferedAmount <= this.highWaterMark;
  }
  /**
   * Whether the send buffer has drained to its low-water mark. Drives the engine's public
   * DCBufferStatusChanged event.
   */
  isBelowLowWaterMark(dc) {
    return dc.bufferedAmount <= dc.bufferedAmountLowThreshold;
  }
  /**
   * Acquires the headroom lock, resolving with the unlock function. Batch senders (the resume
   * replay) hold it across all of their sends so no other sender can interleave, calling
   * {@link waitForHeadroomWithoutLock} per message to respect flow control within the batch.
   */
  lockHeadroom() {
    return this.headroomLock.lock();
  }
  /**
   * Resolves once the caller may send on this channel: immediately while the send buffer is at or
   * below its high-water mark, otherwise once the buffer has drained to the low-water mark (the
   * `bufferedamountlow` event). Callers are serialized through the headroom lock so that, when
   * the buffer drains, they refill it one at a time (up to the high-water mark) rather than all
   * sending at once and overflowing the SCTP send buffer (see livekit/client-sdk-js#1995). The
   * closed/buffer checks run inside the lock so queued callers proceed in FIFO order.
   */
  waitForHeadroomWithLock() {
    return __awaiter(this, void 0, void 0, function* () {
      const unlock = yield this.lockHeadroom();
      try {
        yield this.waitForHeadroomWithoutLock();
      } finally {
        unlock();
      }
    });
  }
  /** Core wait of {@link waitForHeadroomWithLock}. The caller must hold the headroom lock. */
  waitForHeadroomWithoutLock() {
    return __awaiter(this, void 0, void 0, function* () {
      if (this.isEngineClosed()) {
        throw new UnexpectedConnectionState("engine closed");
      }
      const dc = this.getChannel();
      if (!dc) {
        throw new UnexpectedConnectionState("DataChannel not found, kind: ".concat(this.kind));
      }
      if (this.isBelowHighWaterMark(dc)) {
        return;
      }
      const abortSignal = this.waiterAbortController.signal;
      yield new TypedPromise((resolve, reject) => {
        const onBufferedAmountLow = () => {
          cleanup();
          resolve();
        };
        const onDCClose = () => {
          cleanup();
          reject(new UnexpectedConnectionState("DataChannel ".concat(this.kind, " closed while draining the buffer")));
        };
        const onAbort = () => {
          cleanup();
          reject(new UnexpectedConnectionState("DataChannel ".concat(this.kind, " was replaced or torn down while waiting for headroom")));
        };
        const cleanup = () => {
          dc.removeEventListener("bufferedamountlow", onBufferedAmountLow);
          dc.removeEventListener("close", onDCClose);
          abortSignal.removeEventListener("abort", onAbort);
        };
        if (abortSignal.aborted) {
          onAbort();
          return;
        }
        dc.addEventListener("bufferedamountlow", onBufferedAmountLow);
        dc.addEventListener("close", onDCClose);
        abortSignal.addEventListener("abort", onAbort);
      });
    });
  }
  /** Rejects all parked headroom waiters; the next waiter gets a fresh controller. */
  invalidateWaiters(reason) {
    this.waiterAbortController.abort(reason);
    this.waiterAbortController = new AbortController();
  }
  /**
   * Recomputes whether the buffer has drained to the low-water mark and, if that changed since the
   * last check, notifies the status listener. Two independent triggers land here: a send (which
   * raises the buffer) and the `bufferedamountlow` drain event (which lowers it) — the latter has
   * no send to hang the work off, which is why this is a shared entry point rather than a tail of
   * `send`.
   */
  refreshBufferStatus() {
    var _a2;
    const dc = this.getChannel();
    if (!dc) {
      return;
    }
    const isLow = this.isBelowLowWaterMark(dc);
    if (isLow !== this.bufferStatusLow) {
      this.bufferStatusLow = isLow;
      (_a2 = this.onBufferStatusChanged) === null || _a2 === void 0 ? void 0 : _a2.call(this, isLow);
    }
  }
};
var LossyDataChannel = class extends FlowControlledDataChannel {
  constructor(opts) {
    super(opts);
    this.statCurrentBytes = 0;
    this.statByterate = 0;
    this.dropCount = 0;
    this.bufferFullBehavior = opts.bufferFullBehavior;
    this.shouldSkipSends = opts.shouldSkipSends;
  }
  /** Sends prepared bytes with this channel's full-buffer policy (drop or wait). */
  send(msg) {
    return __awaiter(this, void 0, void 0, function* () {
      const dc = this.getChannel();
      if (!dc) {
        return;
      }
      switch (this.bufferFullBehavior) {
        case "wait":
          if (!this.isBelowHighWaterMark(dc)) {
            yield this.waitForHeadroomWithLock();
          }
          break;
        case "drop":
          if (!this.isBelowLowWaterMark(dc)) {
            this.dropCount += 1;
            if (this.dropCount % 100 === 0) {
              livekitLogger.warn("dropping lossy data channel messages, total dropped: ".concat(this.dropCount));
            }
            return;
          }
      }
      this.statCurrentBytes += msg.byteLength;
      if (this.shouldSkipSends()) {
        return;
      }
      try {
        dc.send(msg);
        this.refreshBufferStatus();
      } catch (error) {
        if (error instanceof TypeError) {
          livekitLogger.error(error);
        } else {
          throw error;
        }
      }
    });
  }
  /**
   * Starts the once-per-second adjustment of the channel's `bufferedAmountLowThreshold` to the
   * observed byterate, keeping the drop gate at roughly 100ms of buffered latency (clamped to
   * the watermarks). Restarts cleanly if already running.
   */
  startThresholdTuning() {
    this.stopThresholdTuning();
    this.statInterval = CriticalTimers.setInterval(() => {
      this.statByterate = this.statCurrentBytes;
      this.statCurrentBytes = 0;
      const dc = this.getChannel();
      if (dc) {
        const threshold = this.statByterate / 10;
        dc.bufferedAmountLowThreshold = Math.min(Math.max(threshold, this.lowWaterMark), this.highWaterMark);
      }
    }, 1e3);
  }
  /** Stops the threshold tuning and resets the stats and drop counter. */
  stopThresholdTuning() {
    this.statByterate = 0;
    this.statCurrentBytes = 0;
    if (this.statInterval) {
      CriticalTimers.clearInterval(this.statInterval);
      this.statInterval = void 0;
    }
    this.dropCount = 0;
  }
};
var DataPacketBuffer = class {
  constructor() {
    this.buffer = [];
    this._totalSize = 0;
    this._sentSize = 0;
  }
  push(item) {
    this.buffer.push(item);
    this._totalSize += item.data.byteLength;
    if (item.sent) {
      this._sentSize += item.data.byteLength;
    }
  }
  pop() {
    const item = this.buffer.shift();
    if (item) {
      this._totalSize -= item.data.byteLength;
      if (item.sent) {
        this._sentSize -= item.data.byteLength;
      }
    }
    return item;
  }
  getAll() {
    return this.buffer.slice();
  }
  /** Every queued packet not yet handed to the channel, in sequence order. */
  getUnsent() {
    return this.buffer.filter((item) => !item.sent);
  }
  /** Marks a single queued packet as handed to the channel. */
  markSent(item) {
    if (!item.sent) {
      item.sent = true;
      this._sentSize += item.data.byteLength;
    }
  }
  /**
   * Marks every queued packet as not-yet-sent. Used at the start of a resume replay: whatever is
   * still buffered was sent on the previous channel (or deferred) and must be re-handed to the
   * current one, so none of it counts as sent until the replay actually transmits it.
   */
  markAllUnsent() {
    for (const item of this.buffer) {
      item.sent = false;
    }
    this._sentSize = 0;
  }
  popToSequence(sequence) {
    while (this.buffer.length > 0) {
      const first = this.buffer[0];
      if (first.sequence <= sequence) {
        this.pop();
      } else {
        break;
      }
    }
  }
  alignBufferedAmount(bufferedAmount) {
    while (this.buffer.length > 0) {
      const first = this.buffer[0];
      if (!first.sent) {
        break;
      }
      if (this._sentSize - first.data.byteLength <= bufferedAmount) {
        break;
      }
      this.pop();
    }
  }
  get length() {
    return this.buffer.length;
  }
};
var ReliableDataChannel = class extends FlowControlledDataChannel {
  constructor(opts) {
    super(opts);
    this.messageBuffer = new DataPacketBuffer();
    this.sequence = 1;
    this.isDeferringSends = opts.isDeferringSends;
  }
  /**
   * Claims the next packet sequence. The caller stamps it into the packet before serialization,
   * then passes it back to {@link send} so the replay buffer stays keyed by wire sequence.
   */
  nextSequence() {
    const sequence = this.sequence;
    this.sequence += 1;
    return sequence;
  }
  /**
   * Sends prepared bytes with reliable semantics. Resolves once the packet has either been handed
   * to the channel or queued for the resume replay; throws only when the engine is closed.
   */
  send(msg, sequence) {
    return __awaiter(this, void 0, void 0, function* () {
      if (this.isDeferringSends()) {
        this.messageBuffer.push({
          data: msg,
          sequence,
          sent: false
        });
        return;
      }
      const dc = this.getChannel();
      if (!dc) {
        return;
      }
      try {
        yield this.waitForHeadroomWithLock();
      } catch (error) {
        if (this.isEngineClosed()) {
          throw error;
        }
        this.messageBuffer.push({
          data: msg,
          sequence,
          sent: false
        });
        return;
      }
      if (this.isDeferringSends()) {
        this.messageBuffer.push({
          data: msg,
          sequence,
          sent: false
        });
        return;
      }
      this.messageBuffer.push({
        data: msg,
        sequence,
        sent: true
      });
      dc.send(msg);
      this.refreshBufferStatus();
    });
  }
  /**
   * Replays the buffered backlog after a resume: drops everything the server acked
   * (`lastMessageSeq`), then re-sends the rest in order. The headroom lock is held across the
   * whole replay — releasing it between messages would let a concurrent send (whose newer
   * sequence was already assigned before it queued on the lock) hit the wire mid-replay, and
   * receivers would then discard the remaining lower-sequence resent messages as duplicates.
   */
  replay(lastMessageSeq) {
    return __awaiter(this, void 0, void 0, function* () {
      const dc = this.getChannel();
      if (!dc) {
        return;
      }
      this.messageBuffer.popToSequence(lastMessageSeq);
      const unlock = yield this.lockHeadroom();
      try {
        this.messageBuffer.markAllUnsent();
        for (let batch = this.messageBuffer.getUnsent(); batch.length > 0; batch = this.messageBuffer.getUnsent()) {
          for (const item of batch) {
            yield this.waitForHeadroomWithoutLock();
            dc.send(item.data);
            this.messageBuffer.markSent(item);
          }
        }
      } finally {
        unlock();
      }
      this.refreshBufferStatus();
    });
  }
  /**
   * Before recomputing status, trim packets the transport has now delivered — a send or a drain
   * may have acked buffered packets, and the replay buffer is keyed off the channel's buffered
   * bytes.
   */
  refreshBufferStatus() {
    const dc = this.channelHandle;
    if (dc) {
      this.messageBuffer.alignBufferedAmount(dc.bufferedAmount);
    }
    super.refreshBufferStatus();
  }
  /**
   * Drops all replay state and restarts sequencing. Only valid on a full reconnect, where the
   * session (and the receivers' sequence tracking) starts over.
   */
  reset() {
    this.messageBuffer = new DataPacketBuffer();
    this.sequence = 1;
  }
};
var DataChannelKind;
(function(DataChannelKind2) {
  DataChannelKind2[DataChannelKind2["RELIABLE"] = 0] = "RELIABLE";
  DataChannelKind2[DataChannelKind2["LOSSY"] = 1] = "LOSSY";
  DataChannelKind2[DataChannelKind2["DATA_TRACK_LOSSY"] = 2] = "DATA_TRACK_LOSSY";
})(DataChannelKind || (DataChannelKind = {}));
var reliableDataChannelWaterMarkLow = 64 * 1024;
var reliableDataChannelWaterMarkHigh = 1024 * 1024;
var lossyDataChannelWaterMarkLow = 8 * 1024;
var lossyDataChannelWaterMarkHigh = 256 * 1024;
function dataChannelLowWaterMark(kind) {
  return kind === DataChannelKind.RELIABLE ? reliableDataChannelWaterMarkLow : lossyDataChannelWaterMarkLow;
}
function dataChannelHighWaterMark(kind) {
  return kind === DataChannelKind.RELIABLE ? reliableDataChannelWaterMarkHigh : lossyDataChannelWaterMarkHigh;
}
var lossyDataChannelLabel = "_lossy";
var reliableDataChannelLabel = "_reliable";
var dataTrackDataChannelLabel = "_data_track";
var DataChannelManager = class {
  constructor(opts) {
    this.opts = opts;
    const flowControlOptions = (kind) => ({
      kind,
      lowWaterMark: dataChannelLowWaterMark(kind),
      highWaterMark: dataChannelHighWaterMark(kind),
      isEngineClosed: opts.isEngineClosed,
      onBufferStatusChanged: (isLow) => opts.onBufferStatusChanged(kind, isLow)
    });
    this.reliable = new ReliableDataChannel(Object.assign(Object.assign({}, flowControlOptions(DataChannelKind.RELIABLE)), {
      isDeferringSends: opts.isReconnecting
    }));
    this.lossy = new LossyDataChannel(Object.assign(Object.assign({}, flowControlOptions(DataChannelKind.LOSSY)), {
      // Classic lossy user data: a stale packet is worthless, so drop instead of queueing.
      bufferFullBehavior: "drop",
      shouldSkipSends: opts.isReconnecting
    }));
    this.dataTrack = new LossyDataChannel(Object.assign(Object.assign({}, flowControlOptions(DataChannelKind.DATA_TRACK_LOSSY)), {
      // Data tracks backpressure the producer instead — it decides what to skip at frame
      // granularity rather than the engine dropping arbitrary chunks out of frames.
      bufferFullBehavior: "wait",
      shouldSkipSends: opts.isReconnecting
    }));
  }
  /** The flow-control wrapper for `kind`. */
  channelFor(kind) {
    switch (kind) {
      case DataChannelKind.RELIABLE:
        return this.reliable;
      case DataChannelKind.LOSSY:
        return this.lossy;
      case DataChannelKind.DATA_TRACK_LOSSY:
        return this.dataTrack;
    }
  }
  /** The raw RTCDataChannel handle for `kind`, publisher side by default. */
  getHandle(kind) {
    let subscriber = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : false;
    if (!subscriber) {
      return this.channelFor(kind).channelHandle;
    }
    switch (kind) {
      case DataChannelKind.RELIABLE:
        return this.reliableSub;
      case DataChannelKind.LOSSY:
        return this.lossySub;
      case DataChannelKind.DATA_TRACK_LOSSY:
        return this.dataTrackSub;
    }
  }
  get hasPublisherChannels() {
    return Boolean(this.reliable.channelHandle || this.lossy.channelHandle || this.dataTrack.channelHandle);
  }
  /**
   * Creates the three publisher data channels on the given transport, wires their handlers, and
   * attaches them to the wrappers — attaching rejects any waiters still parked on replaced
   * channel objects.
   */
  createPublisherChannels(pcManager) {
    for (const channel of [this.lossy, this.reliable, this.dataTrack]) {
      const old = channel.channelHandle;
      if (old) {
        old.onmessage = null;
        old.onerror = null;
        old.onclose = null;
      }
    }
    const wire = (channel, dc, onMessage) => {
      dc.onmessage = onMessage;
      dc.onerror = this.opts.onDataError;
      dc.onclose = () => this.opts.onChannelClose(channel.kind);
      dc.bufferedAmountLowThreshold = channel.lowWaterMark;
      dc.onbufferedamountlow = () => channel.refreshBufferStatus();
      channel.attach(dc);
    };
    wire(this.lossy, pcManager.createPublisherDataChannel(lossyDataChannelLabel, {
      ordered: false,
      maxRetransmits: 0
    }), this.opts.onDataMessage);
    wire(this.reliable, pcManager.createPublisherDataChannel(reliableDataChannelLabel, {
      ordered: true
    }), this.opts.onDataMessage);
    wire(this.dataTrack, pcManager.createPublisherDataChannel(dataTrackDataChannelLabel, {
      ordered: false,
      maxRetransmits: 0
    }), this.opts.onDataTrackMessage);
    this.lossy.startThresholdTuning();
  }
  /**
   * Adopts a subscriber-side data channel by label, wiring the matching receive handler.
   * Returns false for labels this manager doesn't own.
   */
  adoptSubscriberChannel(channel) {
    let handler;
    if (channel.label === reliableDataChannelLabel) {
      this.reliableSub = channel;
      handler = this.opts.onDataMessage;
    } else if (channel.label === lossyDataChannelLabel) {
      this.lossySub = channel;
      handler = this.opts.onDataMessage;
    } else if (channel.label === dataTrackDataChannelLabel) {
      this.dataTrackSub = channel;
      handler = this.opts.onDataTrackMessage;
    } else {
      return false;
    }
    channel.onmessage = handler;
    return true;
  }
  /**
   * Tears down all channels for a peer-connection cleanup: rejects parked waiters (detach — the
   * spec allows `pc.close()` to transition channels to 'closed' without firing events, so waiting
   * for browser close events is not an option), strips handlers, closes the handles, and resets
   * the reliable session state.
   */
  teardown() {
    const dcCleanup = (dc) => {
      if (!dc) {
        return;
      }
      dc.onbufferedamountlow = null;
      dc.onclose = null;
      dc.onclosing = null;
      dc.onerror = null;
      dc.onmessage = null;
      dc.onopen = null;
      dc.close();
    };
    for (const channel of [this.lossy, this.reliable, this.dataTrack]) {
      const dc = channel.channelHandle;
      channel.detach("peer connections cleaned up");
      dcCleanup(dc);
    }
    dcCleanup(this.lossySub);
    dcCleanup(this.reliableSub);
    dcCleanup(this.dataTrackSub);
    this.lossySub = void 0;
    this.reliableSub = void 0;
    this.dataTrackSub = void 0;
    this.reliable.reset();
  }
};
var isMediaRecorderAvailable = typeof MediaRecorder !== "undefined";
var FallbackRecorder = class {
  constructor() {
    throw new Error("MediaRecorder is not available in this environment");
  }
};
var RecorderBase = isMediaRecorderAvailable ? MediaRecorder : FallbackRecorder;
var LocalTrackRecorder = class extends RecorderBase {
  constructor(track, options) {
    if (!isMediaRecorderAvailable) {
      throw new Error("MediaRecorder is not available in this environment");
    }
    super(new MediaStream([track.mediaStreamTrack]), options);
    let dataListener;
    let streamController;
    const isClosed = () => streamController === void 0;
    const onStop = () => {
      this.removeEventListener("dataavailable", dataListener);
      this.removeEventListener("stop", onStop);
      this.removeEventListener("error", onError);
      streamController === null || streamController === void 0 ? void 0 : streamController.close();
      streamController = void 0;
    };
    const onError = (event) => {
      streamController === null || streamController === void 0 ? void 0 : streamController.error(event);
      this.removeEventListener("dataavailable", dataListener);
      this.removeEventListener("stop", onStop);
      this.removeEventListener("error", onError);
      streamController = void 0;
    };
    this.byteStream = new ReadableStream({
      start: (controller) => {
        streamController = controller;
        dataListener = (event) => __awaiter(this, void 0, void 0, function* () {
          let data;
          if (event.data.arrayBuffer) {
            const arrayBuffer = yield event.data.arrayBuffer();
            data = new Uint8Array(arrayBuffer);
          } else if (event.data.byteArray) {
            data = event.data.byteArray;
          } else {
            throw new Error("no data available!");
          }
          if (isClosed()) {
            return;
          }
          controller.enqueue(data);
        });
        this.addEventListener("dataavailable", dataListener);
      },
      cancel: () => {
        onStop();
      }
    });
    this.addEventListener("stop", onStop);
    this.addEventListener("error", onError);
  }
};
function isRecordingSupported() {
  return isMediaRecorderAvailable;
}
var DEFAULT_DIMENSIONS_TIMEOUT = 1e3;
var PRE_CONNECT_BUFFER_TIMEOUT = 1e4;
var LocalTrack = class extends Track {
  /** @internal */
  get sender() {
    return this._sender;
  }
  /** @internal */
  set sender(sender) {
    this._sender = sender;
  }
  get constraints() {
    return this._constraints;
  }
  get hasPreConnectBuffer() {
    return !!this.localTrackRecorder;
  }
  /**
   *
   * @param mediaTrack
   * @param kind
   * @param constraints MediaTrackConstraints that are being used when restarting or reacquiring tracks
   * @param userProvidedTrack Signals to the SDK whether or not the mediaTrack should be managed (i.e. released and reacquired) internally by the SDK
   */
  constructor(mediaTrack, kind, constraints) {
    let userProvidedTrack = arguments.length > 3 && arguments[3] !== void 0 ? arguments[3] : false;
    let loggerOptions = arguments.length > 4 ? arguments[4] : void 0;
    super(mediaTrack, kind, loggerOptions);
    this.manuallyStopped = false;
    this.pendingDeviceChange = false;
    this._isUpstreamPaused = false;
    this.handleTrackMuteEvent = () => this.debouncedTrackMuteHandler().catch(() => this.log.debug("track mute bounce got cancelled by an unmute event", this.logContext));
    this.debouncedTrackMuteHandler = debounce(() => __awaiter(this, void 0, void 0, function* () {
      yield this.pauseUpstream();
    }), 5e3);
    this.handleTrackUnmuteEvent = () => __awaiter(this, void 0, void 0, function* () {
      this.debouncedTrackMuteHandler.cancel("unmute");
      yield this.resumeUpstream();
    });
    this.handleEnded = () => {
      if (this.isInBackground) {
        this.reacquireTrack = true;
      }
      this._mediaStreamTrack.removeEventListener("mute", this.handleTrackMuteEvent);
      this._mediaStreamTrack.removeEventListener("unmute", this.handleTrackUnmuteEvent);
      this.emit(TrackEvent.Ended, this);
    };
    this.reacquireTrack = false;
    this.providedByUser = userProvidedTrack;
    this.muteLock = new _();
    this.pauseUpstreamLock = new _();
    this.trackChangeLock = new _();
    this.trackChangeLock.lock().then((unlock) => __awaiter(this, void 0, void 0, function* () {
      try {
        yield this.setMediaStreamTrack(mediaTrack, true);
      } finally {
        unlock();
      }
    }));
    this._constraints = mediaTrack.getConstraints();
    if (constraints) {
      this._constraints = constraints;
    }
  }
  get id() {
    return this._mediaStreamTrack.id;
  }
  get dimensions() {
    if (this.kind !== Track.Kind.Video) {
      return void 0;
    }
    const _this$_mediaStreamTra = this._mediaStreamTrack.getSettings(), width = _this$_mediaStreamTra.width, height = _this$_mediaStreamTra.height;
    if (width && height) {
      return {
        width,
        height
      };
    }
    return void 0;
  }
  get isUpstreamPaused() {
    return this._isUpstreamPaused;
  }
  get isUserProvided() {
    return this.providedByUser;
  }
  get mediaStreamTrack() {
    var _a2, _b2;
    return (_b2 = (_a2 = this.processor) === null || _a2 === void 0 ? void 0 : _a2.processedTrack) !== null && _b2 !== void 0 ? _b2 : this._mediaStreamTrack;
  }
  get isLocal() {
    return true;
  }
  /**
   * @internal
   * returns mediaStreamTrack settings of the capturing mediastreamtrack source - ignoring processors
   */
  getSourceTrackSettings() {
    return this._mediaStreamTrack.getSettings();
  }
  setMediaStreamTrack(newTrack, force, isUnmuting) {
    return __awaiter(this, void 0, void 0, function* () {
      var _a2;
      if (newTrack === this._mediaStreamTrack && !force) {
        return;
      }
      if (this._mediaStreamTrack) {
        this.attachedElements.forEach((el) => {
          detachTrack(this._mediaStreamTrack, el);
        });
        this.debouncedTrackMuteHandler.cancel("new-track");
        this._mediaStreamTrack.removeEventListener("ended", this.handleEnded);
        this._mediaStreamTrack.removeEventListener("mute", this.handleTrackMuteEvent);
        this._mediaStreamTrack.removeEventListener("unmute", this.handleTrackUnmuteEvent);
      }
      this.mediaStream = new MediaStream([newTrack]);
      if (newTrack) {
        newTrack.addEventListener("ended", this.handleEnded);
        newTrack.addEventListener("mute", this.handleTrackMuteEvent);
        newTrack.addEventListener("unmute", this.handleTrackUnmuteEvent);
        this._constraints = newTrack.getConstraints();
      }
      let processedTrack;
      if (this.processor && newTrack) {
        this.log.debug("restarting processor", this.logContext);
        if (this.kind === "unknown") {
          throw TypeError("cannot set processor on track of unknown kind");
        }
        if (this.processorElement) {
          attachToElement(newTrack, this.processorElement);
          this.processorElement.muted = true;
        }
        yield this.processor.restart({
          track: newTrack,
          kind: this.kind,
          element: this.processorElement,
          localTrack: this
        });
        processedTrack = this.processor.processedTrack;
      }
      if (this.sender && ((_a2 = this.sender.transport) === null || _a2 === void 0 ? void 0 : _a2.state) !== "closed") {
        yield this.sender.replaceTrack(processedTrack !== null && processedTrack !== void 0 ? processedTrack : newTrack);
      }
      if (!this.providedByUser && this._mediaStreamTrack !== newTrack) {
        this._mediaStreamTrack.stop();
      }
      this._mediaStreamTrack = newTrack;
      if (newTrack) {
        this._mediaStreamTrack.enabled = isUnmuting ? true : !this.isMuted;
        yield this.resumeUpstream();
        this.attachedElements.forEach((el) => {
          attachToElement(processedTrack !== null && processedTrack !== void 0 ? processedTrack : newTrack, el);
        });
      }
    });
  }
  waitForDimensions() {
    return __awaiter(this, arguments, void 0, function() {
      var _this = this;
      let timeout = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : DEFAULT_DIMENSIONS_TIMEOUT;
      return function* () {
        var _a2;
        if (_this.kind === Track.Kind.Audio) {
          throw new Error("cannot get dimensions for audio tracks");
        }
        if (((_a2 = getBrowser()) === null || _a2 === void 0 ? void 0 : _a2.os) === "iOS") {
          yield sleep(10);
        }
        const started = Date.now();
        while (Date.now() - started < timeout) {
          const dims = _this.dimensions;
          if (dims) {
            return dims;
          }
          yield sleep(50);
        }
        throw new TrackInvalidError("unable to get track dimensions after timeout");
      }();
    });
  }
  setDeviceId(deviceId) {
    return __awaiter(this, void 0, void 0, function* () {
      if (this._constraints.deviceId === deviceId && this._mediaStreamTrack.getSettings().deviceId === unwrapConstraint(deviceId)) {
        return true;
      }
      this._constraints.deviceId = deviceId;
      if (this.isMuted) {
        this.pendingDeviceChange = true;
        return true;
      }
      yield this.restartTrack();
      return unwrapConstraint(deviceId) === this._mediaStreamTrack.getSettings().deviceId;
    });
  }
  /**
   * @returns DeviceID of the device that is currently being used for this track
   */
  getDeviceId() {
    return __awaiter(this, arguments, void 0, function() {
      var _this2 = this;
      let normalize = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : true;
      return function* () {
        if (_this2.source === Track.Source.ScreenShare) {
          return;
        }
        const _this2$_mediaStreamTr = _this2._mediaStreamTrack.getSettings(), deviceId = _this2$_mediaStreamTr.deviceId, groupId = _this2$_mediaStreamTr.groupId;
        const kind = _this2.kind === Track.Kind.Audio ? "audioinput" : "videoinput";
        return normalize ? DeviceManager.getInstance().normalizeDeviceId(kind, deviceId, groupId) : deviceId;
      }();
    });
  }
  mute() {
    return __awaiter(this, void 0, void 0, function* () {
      this.setTrackMuted(true);
      return this;
    });
  }
  unmute() {
    return __awaiter(this, void 0, void 0, function* () {
      this.setTrackMuted(false);
      return this;
    });
  }
  replaceTrack(track, userProvidedOrOptions) {
    return __awaiter(this, void 0, void 0, function* () {
      const unlock = yield this.trackChangeLock.lock();
      try {
        if (!this.sender) {
          throw new TrackInvalidError("unable to replace an unpublished track");
        }
        let userProvidedTrack;
        let stopProcessor;
        if (typeof userProvidedOrOptions === "boolean") {
          userProvidedTrack = userProvidedOrOptions;
        } else if (userProvidedOrOptions !== void 0) {
          userProvidedTrack = userProvidedOrOptions.userProvidedTrack;
          stopProcessor = userProvidedOrOptions.stopProcessor;
        }
        this.providedByUser = userProvidedTrack !== null && userProvidedTrack !== void 0 ? userProvidedTrack : true;
        this.log.debug("replace MediaStreamTrack", this.logContext);
        yield this.setMediaStreamTrack(track);
        if (stopProcessor && this.processor) {
          yield this.internalStopProcessor();
        }
      } finally {
        unlock();
      }
      yield this.onSenderTrackSwapped();
      return this;
    });
  }
  /**
   * Hook invoked after the MediaStreamTrack on the sender has been swapped
   * (via replaceTrack, setProcessor, or stopProcessor). Fires outside the
   * trackChangeLock so subclasses can do asynchronous work such as polling
   * for new dimensions without blocking other track operations.
   */
  onSenderTrackSwapped() {
    return __awaiter(this, void 0, void 0, function* () {
    });
  }
  restart(constraints, isUnmuting) {
    return __awaiter(this, void 0, void 0, function* () {
      this.manuallyStopped = false;
      const unlock = yield this.trackChangeLock.lock();
      try {
        if (!constraints) {
          constraints = this._constraints;
        }
        const _constraints = constraints, deviceId = _constraints.deviceId, facingMode = _constraints.facingMode, otherConstraints = __rest(constraints, ["deviceId", "facingMode"]);
        this.log.debug("restarting track with constraints", Object.assign(Object.assign({}, this.logContext), {
          constraints
        }));
        const streamConstraints = {
          audio: false,
          video: false
        };
        if (this.kind === Track.Kind.Video) {
          streamConstraints.video = deviceId || facingMode ? {
            deviceId,
            facingMode
          } : true;
        } else {
          streamConstraints.audio = deviceId ? Object.assign({
            deviceId
          }, otherConstraints) : true;
        }
        this.attachedElements.forEach((el) => {
          detachTrack(this.mediaStreamTrack, el);
        });
        this._mediaStreamTrack.removeEventListener("ended", this.handleEnded);
        this._mediaStreamTrack.stop();
        const mediaStream = yield navigator.mediaDevices.getUserMedia(streamConstraints);
        const newTrack = mediaStream.getTracks()[0];
        if (this.kind === Track.Kind.Video) {
          yield newTrack.applyConstraints(otherConstraints);
        }
        newTrack.addEventListener("ended", this.handleEnded);
        this.log.debug("re-acquired MediaStreamTrack", this.logContext);
        yield this.setMediaStreamTrack(newTrack, false, isUnmuting);
        this._constraints = constraints;
        this.pendingDeviceChange = false;
        this.emit(TrackEvent.Restarted, this);
        if (this.manuallyStopped) {
          this.log.warn("track was stopped during a restart, stopping restarted track", this.logContext);
          this.stop();
        }
        return this;
      } finally {
        unlock();
      }
    });
  }
  setTrackMuted(muted) {
    this.log.debug("setting ".concat(this.kind, " track ").concat(muted ? "muted" : "unmuted"), this.logContext);
    if (this.isMuted === muted && this._mediaStreamTrack.enabled !== muted) {
      return;
    }
    this.isMuted = muted;
    this._mediaStreamTrack.enabled = !muted;
    this.emit(muted ? TrackEvent.Muted : TrackEvent.Unmuted, this);
  }
  get needsReAcquisition() {
    return this._mediaStreamTrack.readyState !== "live" || this._mediaStreamTrack.muted || !this._mediaStreamTrack.enabled || this.reacquireTrack;
  }
  handleAppVisibilityChanged() {
    const _super = Object.create(null, {
      handleAppVisibilityChanged: {
        get: () => super.handleAppVisibilityChanged
      }
    });
    return __awaiter(this, void 0, void 0, function* () {
      yield _super.handleAppVisibilityChanged.call(this);
      if (!isMobile()) return;
      this.log.debug("visibility changed, is in Background: ".concat(this.isInBackground), this.logContext);
      if (!this.isInBackground && this.needsReAcquisition && !this.isUserProvided && !this.isMuted) {
        this.log.debug("track needs to be reacquired, restarting ".concat(this.source), this.logContext);
        yield this.restart();
        this.reacquireTrack = false;
      }
    });
  }
  stop() {
    var _a2;
    this.manuallyStopped = true;
    super.stop();
    this._mediaStreamTrack.removeEventListener("ended", this.handleEnded);
    this._mediaStreamTrack.removeEventListener("mute", this.handleTrackMuteEvent);
    this._mediaStreamTrack.removeEventListener("unmute", this.handleTrackUnmuteEvent);
    (_a2 = this.processor) === null || _a2 === void 0 ? void 0 : _a2.destroy();
    this.processor = void 0;
  }
  /**
   * pauses publishing to the server without disabling the local MediaStreamTrack
   * this is used to display a user's own video locally while pausing publishing to
   * the server.
   * this API is unsupported on Safari < 12 due to a bug
   **/
  pauseUpstream() {
    return __awaiter(this, void 0, void 0, function* () {
      var _a2;
      const unlock = yield this.pauseUpstreamLock.lock();
      try {
        if (this._isUpstreamPaused === true) {
          return;
        }
        if (!this.sender) {
          this.log.warn("unable to pause upstream for an unpublished track", this.logContext);
          return;
        }
        this._isUpstreamPaused = true;
        this.emit(TrackEvent.UpstreamPaused, this);
        const browser = getBrowser();
        if ((browser === null || browser === void 0 ? void 0 : browser.name) === "Safari" && compareVersions(browser.version, "12.0") < 0) {
          throw new DeviceUnsupportedError("pauseUpstream is not supported on Safari < 12.");
        }
        if (((_a2 = this.sender.transport) === null || _a2 === void 0 ? void 0 : _a2.state) !== "closed") {
          yield this.sender.replaceTrack(null);
        }
      } finally {
        unlock();
      }
    });
  }
  resumeUpstream() {
    return __awaiter(this, void 0, void 0, function* () {
      var _a2;
      const unlock = yield this.pauseUpstreamLock.lock();
      try {
        if (this._isUpstreamPaused === false) {
          return;
        }
        if (!this.sender) {
          this.log.warn("unable to resume upstream for an unpublished track", this.logContext);
          return;
        }
        this._isUpstreamPaused = false;
        this.emit(TrackEvent.UpstreamResumed, this);
        if (((_a2 = this.sender.transport) === null || _a2 === void 0 ? void 0 : _a2.state) !== "closed") {
          yield this.sender.replaceTrack(this.mediaStreamTrack);
        }
      } finally {
        unlock();
      }
    });
  }
  /**
   * Gets the RTCStatsReport for the LocalTrack's underlying RTCRtpSender
   * See https://developer.mozilla.org/en-US/docs/Web/API/RTCStatsReport
   *
   * @returns Promise<RTCStatsReport> | undefined
   */
  getRTCStatsReport() {
    return __awaiter(this, void 0, void 0, function* () {
      var _a2;
      if (!((_a2 = this.sender) === null || _a2 === void 0 ? void 0 : _a2.getStats)) {
        return;
      }
      const statsReport = yield this.sender.getStats();
      return statsReport;
    });
  }
  /**
   * Sets a processor on this track.
   * See https://github.com/livekit/track-processors-js for example usage
   *
   * @param processor
   * @param showProcessedStreamLocally
   * @returns
   */
  setProcessor(processor_1) {
    return __awaiter(this, arguments, void 0, function(processor) {
      var _this3 = this;
      let showProcessedStreamLocally = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : true;
      return function* () {
        var _a2;
        const unlock = yield _this3.trackChangeLock.lock();
        try {
          _this3.log.debug("setting up processor", _this3.logContext);
          const processorElement = document.createElement(_this3.kind);
          const processorOptions = {
            kind: _this3.kind,
            track: _this3._mediaStreamTrack,
            element: processorElement,
            audioContext: _this3.audioContext,
            localTrack: _this3
          };
          yield processor.init(processorOptions);
          _this3.log.debug("processor initialized", _this3.logContext);
          if (_this3.processor) {
            yield _this3.internalStopProcessor();
          }
          if (_this3.kind === "unknown") {
            throw TypeError("cannot set processor on track of unknown kind");
          }
          attachToElement(_this3._mediaStreamTrack, processorElement);
          processorElement.muted = true;
          processorElement.play().catch((error) => {
            if (error instanceof DOMException && error.name === "AbortError") {
              _this3.log.warn("failed to play processor element, retrying", Object.assign(Object.assign({}, _this3.logContext), {
                error
              }));
              setTimeout(() => {
                processorElement.play().catch((err) => {
                  _this3.log.error("failed to play processor element", Object.assign(Object.assign({}, _this3.logContext), {
                    err
                  }));
                });
              }, 100);
            } else {
              _this3.log.error("failed to play processor element", Object.assign(Object.assign({}, _this3.logContext), {
                error
              }));
            }
          });
          _this3.processor = processor;
          _this3.processorElement = processorElement;
          if (_this3.processor.processedTrack) {
            for (const el of _this3.attachedElements) {
              if (el !== _this3.processorElement && showProcessedStreamLocally) {
                detachTrack(_this3._mediaStreamTrack, el);
                attachToElement(_this3.processor.processedTrack, el);
              }
            }
            yield (_a2 = _this3.sender) === null || _a2 === void 0 ? void 0 : _a2.replaceTrack(_this3.processor.processedTrack);
          }
          _this3.emit(TrackEvent.TrackProcessorUpdate, _this3.processor);
        } finally {
          unlock();
        }
        yield _this3.onSenderTrackSwapped();
      }();
    });
  }
  getProcessor() {
    return this.processor;
  }
  /**
   * Stops the track processor
   * See https://github.com/livekit/track-processors-js for example usage
   *
   */
  stopProcessor() {
    return __awaiter(this, arguments, void 0, function() {
      var _this4 = this;
      let keepElement = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : true;
      return function* () {
        const unlock = yield _this4.trackChangeLock.lock();
        try {
          yield _this4.internalStopProcessor(keepElement);
        } finally {
          unlock();
        }
        yield _this4.onSenderTrackSwapped();
      }();
    });
  }
  /**
   * @internal
   * This method assumes the caller has acquired a trackChangeLock already.
   * The public facing method for stopping the processor is `stopProcessor` and it wraps this method in the trackChangeLock.
   */
  internalStopProcessor() {
    return __awaiter(this, arguments, void 0, function() {
      var _this5 = this;
      let keepElement = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : true;
      return function* () {
        var _a2, _b2;
        if (!_this5.processor) return;
        _this5.log.debug("stopping processor", _this5.logContext);
        (_a2 = _this5.processor.processedTrack) === null || _a2 === void 0 ? void 0 : _a2.stop();
        yield _this5.processor.destroy();
        _this5.processor = void 0;
        if (!keepElement) {
          (_b2 = _this5.processorElement) === null || _b2 === void 0 ? void 0 : _b2.remove();
          _this5.processorElement = void 0;
        }
        yield _this5._mediaStreamTrack.applyConstraints(_this5._constraints);
        yield _this5.setMediaStreamTrack(_this5._mediaStreamTrack, true);
        _this5.emit(TrackEvent.TrackProcessorUpdate);
      }();
    });
  }
  /** @internal */
  startPreConnectBuffer() {
    let timeslice = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : 100;
    if (!isRecordingSupported()) {
      this.log.warn("MediaRecorder is not available, cannot start preconnect buffer", this.logContext);
      return;
    }
    if (!this.localTrackRecorder) {
      let mimeType = "audio/webm;codecs=opus";
      if (!MediaRecorder.isTypeSupported(mimeType)) {
        mimeType = "video/mp4";
      }
      this.localTrackRecorder = new LocalTrackRecorder(this, {
        mimeType
      });
    } else {
      this.log.warn("preconnect buffer already started");
      return;
    }
    this.localTrackRecorder.start(timeslice);
    this.autoStopPreConnectBuffer = setTimeout(() => {
      this.log.warn("preconnect buffer timed out, stopping recording automatically", this.logContext);
      this.stopPreConnectBuffer();
    }, PRE_CONNECT_BUFFER_TIMEOUT);
  }
  /** @internal */
  stopPreConnectBuffer() {
    clearTimeout(this.autoStopPreConnectBuffer);
    if (this.localTrackRecorder) {
      this.localTrackRecorder.stop();
      this.localTrackRecorder = void 0;
    }
  }
  /** @internal */
  getPreConnectBuffer() {
    var _a2;
    return (_a2 = this.localTrackRecorder) === null || _a2 === void 0 ? void 0 : _a2.byteStream;
  }
  getPreConnectBufferMimeType() {
    var _a2;
    return (_a2 = this.localTrackRecorder) === null || _a2 === void 0 ? void 0 : _a2.mimeType;
  }
};
var LocalAudioTrack = class extends LocalTrack {
  /**
   * boolean indicating whether enhanced noise cancellation is currently being used on this track
   */
  get enhancedNoiseCancellation() {
    return this.isKrispNoiseFilterEnabled;
  }
  /**
   *
   * @param mediaTrack
   * @param constraints MediaTrackConstraints that are being used when restarting or reacquiring tracks
   * @param userProvidedTrack Signals to the SDK whether or not the mediaTrack should be managed (i.e. released and reacquired) internally by the SDK
   */
  constructor(mediaTrack, constraints) {
    let userProvidedTrack = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : true;
    let audioContext = arguments.length > 3 ? arguments[3] : void 0;
    let loggerOptions = arguments.length > 4 ? arguments[4] : void 0;
    super(mediaTrack, Track.Kind.Audio, constraints, userProvidedTrack, loggerOptions);
    this.stopOnMute = false;
    this.isKrispNoiseFilterEnabled = false;
    this.monitorSender = () => __awaiter(this, void 0, void 0, function* () {
      if (!this.sender) {
        this._currentBitrate = 0;
        return;
      }
      let stats;
      try {
        stats = yield this.getSenderStats();
      } catch (e4) {
        this.log.error("could not get audio sender stats", Object.assign(Object.assign({}, this.logContext), {
          error: e4
        }));
        return;
      }
      if (stats && this.prevStats) {
        this._currentBitrate = computeBitrate(stats, this.prevStats);
      }
      this.prevStats = stats;
    });
    this.handleKrispNoiseFilterEnable = () => {
      this.isKrispNoiseFilterEnabled = true;
      this.log.debug("Krisp noise filter enabled", this.logContext);
      this.emit(TrackEvent.AudioTrackFeatureUpdate, this, AudioTrackFeature.TF_ENHANCED_NOISE_CANCELLATION, true);
    };
    this.handleKrispNoiseFilterDisable = () => {
      this.isKrispNoiseFilterEnabled = false;
      this.log.debug("Krisp noise filter disabled", this.logContext);
      this.emit(TrackEvent.AudioTrackFeatureUpdate, this, AudioTrackFeature.TF_ENHANCED_NOISE_CANCELLATION, false);
    };
    this.audioContext = audioContext;
    this.checkForSilence();
  }
  mute() {
    const _super = Object.create(null, {
      mute: {
        get: () => super.mute
      }
    });
    return __awaiter(this, void 0, void 0, function* () {
      const unlock = yield this.muteLock.lock();
      try {
        if (this.isMuted) {
          this.log.debug("Track already muted", this.logContext);
          return this;
        }
        if (this.source === Track.Source.Microphone && this.stopOnMute && !this.isUserProvided) {
          this.log.debug("stopping mic track", this.logContext);
          this._mediaStreamTrack.stop();
        }
        yield _super.mute.call(this);
        return this;
      } finally {
        unlock();
      }
    });
  }
  unmute() {
    const _super = Object.create(null, {
      unmute: {
        get: () => super.unmute
      }
    });
    return __awaiter(this, void 0, void 0, function* () {
      const unlock = yield this.muteLock.lock();
      try {
        if (!this.isMuted) {
          this.log.debug("Track already unmuted", this.logContext);
          return this;
        }
        if (this.source === Track.Source.Microphone && (this.stopOnMute || this._mediaStreamTrack.readyState === "ended" || this.pendingDeviceChange) && !this.isUserProvided) {
          this.log.debug("reacquiring mic track", this.logContext);
          yield this.restart(void 0, true);
        }
        yield _super.unmute.call(this);
        return this;
      } finally {
        unlock();
      }
    });
  }
  restartTrack(options) {
    return __awaiter(this, void 0, void 0, function* () {
      let constraints;
      if (options) {
        const streamConstraints = constraintsForOptions({
          audio: options
        });
        if (typeof streamConstraints.audio !== "boolean") {
          constraints = streamConstraints.audio;
        }
      }
      yield this.restart(constraints);
    });
  }
  applyConstraints(constraints) {
    return __awaiter(this, void 0, void 0, function* () {
      const unlock = yield this.trackChangeLock.lock();
      try {
        const res = yield this._mediaStreamTrack.applyConstraints(constraints);
        this._constraints = Object.assign(Object.assign({}, this._constraints), constraints);
        return res;
      } finally {
        unlock();
      }
    });
  }
  restart(constraints, isUnmuting) {
    const _super = Object.create(null, {
      restart: {
        get: () => super.restart
      }
    });
    return __awaiter(this, void 0, void 0, function* () {
      const track = yield _super.restart.call(this, constraints, isUnmuting);
      this.checkForSilence();
      return track;
    });
  }
  /* @internal */
  startMonitor() {
    if (!isWeb()) {
      return;
    }
    if (this.monitorInterval) {
      return;
    }
    this.monitorInterval = setInterval(() => {
      this.monitorSender();
    }, monitorFrequency);
  }
  setProcessor(processor) {
    return __awaiter(this, void 0, void 0, function* () {
      var _a2;
      const unlock = yield this.trackChangeLock.lock();
      try {
        if (!isReactNative() && !this.audioContext) {
          throw Error("Audio context needs to be set on LocalAudioTrack in order to enable processors");
        }
        if (this.processor) {
          yield this.internalStopProcessor();
        }
        const processorOptions = {
          kind: this.kind,
          track: this._mediaStreamTrack,
          // RN won't have or use AudioContext
          audioContext: this.audioContext,
          localTrack: this
        };
        this.log.debug("setting up audio processor ".concat(processor.name), this.logContext);
        yield processor.init(processorOptions);
        this.processor = processor;
        if (this.processor.processedTrack) {
          yield (_a2 = this.sender) === null || _a2 === void 0 ? void 0 : _a2.replaceTrack(this.processor.processedTrack);
          this.processor.processedTrack.addEventListener("enable-lk-krisp-noise-filter", this.handleKrispNoiseFilterEnable);
          this.processor.processedTrack.addEventListener("disable-lk-krisp-noise-filter", this.handleKrispNoiseFilterDisable);
        }
        this.emit(TrackEvent.TrackProcessorUpdate, this.processor);
      } finally {
        unlock();
      }
    });
  }
  /**
   * @internal
   * @experimental
   */
  setAudioContext(audioContext) {
    this.audioContext = audioContext;
  }
  getSenderStats() {
    return __awaiter(this, void 0, void 0, function* () {
      var _a2;
      if (!((_a2 = this.sender) === null || _a2 === void 0 ? void 0 : _a2.getStats)) {
        return void 0;
      }
      const stats = yield this.sender.getStats();
      let audioStats;
      stats.forEach((v) => {
        if (v.type === "outbound-rtp") {
          audioStats = {
            type: "audio",
            streamId: v.id,
            packetsSent: v.packetsSent,
            packetsLost: v.packetsLost,
            bytesSent: v.bytesSent,
            timestamp: v.timestamp,
            roundTripTime: v.roundTripTime,
            jitter: v.jitter
          };
        }
      });
      return audioStats;
    });
  }
  checkForSilence() {
    return __awaiter(this, void 0, void 0, function* () {
      const trackIsSilent = yield detectSilence(this);
      if (trackIsSilent) {
        if (!this.isMuted) {
          this.log.debug("silence detected on local audio track", this.logContext);
        }
        this.emit(TrackEvent.AudioSilenceDetected);
      }
      return trackIsSilent;
    });
  }
};
function mediaTrackToLocalTrack(mediaStreamTrack, constraints, loggerOptions) {
  switch (mediaStreamTrack.kind) {
    case "audio":
      return new LocalAudioTrack(mediaStreamTrack, constraints, false, void 0, loggerOptions);
    case "video":
      return new LocalVideoTrack(mediaStreamTrack, constraints, false, loggerOptions);
    default:
      throw new TrackInvalidError("unsupported track type: ".concat(mediaStreamTrack.kind));
  }
}
var presets169 = Object.values(VideoPresets);
var presets43 = Object.values(VideoPresets43);
var presetsScreenShare = Object.values(ScreenSharePresets);
var defaultSimulcastPresets169 = [VideoPresets.h180, VideoPresets.h360];
var defaultSimulcastPresets43 = [VideoPresets43.h180, VideoPresets43.h360];
var computeDefaultScreenShareSimulcastPresets = (fromPreset) => {
  const layers = [{
    scaleResolutionDownBy: 2,
    fps: fromPreset.encoding.maxFramerate
  }];
  return layers.map((t) => {
    var _a2, _b2;
    return new VideoPreset(Math.floor(fromPreset.width / t.scaleResolutionDownBy), Math.floor(fromPreset.height / t.scaleResolutionDownBy), Math.max(15e4, Math.floor(fromPreset.encoding.maxBitrate / (Math.pow(t.scaleResolutionDownBy, 2) * (((_a2 = fromPreset.encoding.maxFramerate) !== null && _a2 !== void 0 ? _a2 : 30) / ((_b2 = t.fps) !== null && _b2 !== void 0 ? _b2 : 30))))), t.fps, fromPreset.encoding.priority);
  });
};
var videoRids = ["q", "h", "f"];
function computeVideoEncodings(isScreenShare, width, height, options) {
  var _a2, _b2;
  let videoEncoding = options === null || options === void 0 ? void 0 : options.videoEncoding;
  if (isScreenShare) {
    videoEncoding = options === null || options === void 0 ? void 0 : options.screenShareEncoding;
  }
  const useSimulcast = options === null || options === void 0 ? void 0 : options.simulcast;
  const scalabilityMode = options === null || options === void 0 ? void 0 : options.scalabilityMode;
  const videoCodec = options === null || options === void 0 ? void 0 : options.videoCodec;
  if (!videoEncoding && !useSimulcast && !scalabilityMode || !width || !height) {
    return [{}];
  }
  if (!videoEncoding) {
    videoEncoding = determineAppropriateEncoding(isScreenShare, width, height, videoCodec);
    livekitLogger.debug("using video encoding", videoEncoding);
  }
  const sourceFramerate = videoEncoding.maxFramerate;
  const original = new VideoPreset(width, height, videoEncoding.maxBitrate, videoEncoding.maxFramerate, videoEncoding.priority);
  if (scalabilityMode && isSVCCodec(videoCodec)) {
    const sm = new ScalabilityMode(scalabilityMode);
    const encodings = [];
    if (sm.spatial > 3) {
      throw new Error("unsupported scalabilityMode: ".concat(scalabilityMode));
    }
    const browser = getBrowser();
    if (isSafariBased() || // Even tho RN runs M114, it does not produce SVC layers when a single encoding
    // is provided. So we'll use the legacy SVC specification for now.
    // TODO: when we upstream libwebrtc, this will need additional verification
    isReactNative() || (browser === null || browser === void 0 ? void 0 : browser.name) === "Chrome" && compareVersions(browser === null || browser === void 0 ? void 0 : browser.version, "113") < 0) {
      const bitratesRatio = sm.suffix == "h" ? 2 : 3;
      const requireScale = isSafariSvcApi(browser);
      for (let i2 = 0; i2 < sm.spatial; i2 += 1) {
        encodings.push({
          rid: videoRids[2 - i2],
          maxBitrate: videoEncoding.maxBitrate / Math.pow(bitratesRatio, i2),
          maxFramerate: original.encoding.maxFramerate,
          scaleResolutionDownBy: requireScale ? Math.pow(2, i2) : void 0
        });
      }
      encodings[0].scalabilityMode = scalabilityMode;
    } else {
      encodings.push({
        maxBitrate: videoEncoding.maxBitrate,
        maxFramerate: original.encoding.maxFramerate,
        /* @ts-ignore */
        scalabilityMode
      });
    }
    if (original.encoding.priority) {
      encodings[0].priority = original.encoding.priority;
      encodings[0].networkPriority = original.encoding.priority;
    }
    livekitLogger.debug("using svc encoding", {
      encodings
    });
    return encodings;
  }
  if (!useSimulcast) {
    return [videoEncoding];
  }
  let presets;
  if (isScreenShare) {
    presets = (_a2 = sortPresets(options === null || options === void 0 ? void 0 : options.screenShareSimulcastLayers)) !== null && _a2 !== void 0 ? _a2 : defaultSimulcastLayers(isScreenShare, original);
  } else {
    presets = (_b2 = sortPresets(options === null || options === void 0 ? void 0 : options.videoSimulcastLayers)) !== null && _b2 !== void 0 ? _b2 : defaultSimulcastLayers(isScreenShare, original);
  }
  let midPreset;
  if (presets.length > 0) {
    const lowPreset = presets[0];
    if (presets.length > 1) {
      var _presets = presets;
      var _presets2 = _slicedToArray(_presets, 2);
      midPreset = _presets2[1];
    }
    const size = Math.max(width, height);
    if (size >= 960 && midPreset) {
      return encodingsFromPresets(width, height, [lowPreset, midPreset, original], sourceFramerate);
    }
    if (size >= 480) {
      return encodingsFromPresets(width, height, [lowPreset, original], sourceFramerate);
    }
  }
  return encodingsFromPresets(width, height, [original]);
}
function computeTrackBackupEncodings(track, videoCodec, opts) {
  var _a2, _b2, _c, _d;
  if (!opts.backupCodec || opts.backupCodec === true || opts.backupCodec.codec === opts.videoCodec) {
    return;
  }
  if (videoCodec !== opts.backupCodec.codec) {
    livekitLogger.warn("requested a different codec than specified as backup", {
      serverRequested: videoCodec,
      backup: opts.backupCodec.codec
    });
  }
  opts.videoCodec = videoCodec;
  opts.videoEncoding = opts.backupCodec.encoding;
  const settings = track.mediaStreamTrack.getSettings();
  const width = (_a2 = settings.width) !== null && _a2 !== void 0 ? _a2 : (_b2 = track.dimensions) === null || _b2 === void 0 ? void 0 : _b2.width;
  const height = (_c = settings.height) !== null && _c !== void 0 ? _c : (_d = track.dimensions) === null || _d === void 0 ? void 0 : _d.height;
  if (track.source === Track.Source.ScreenShare && opts.simulcast) {
    opts.simulcast = false;
  }
  const encodings = computeVideoEncodings(track.source === Track.Source.ScreenShare, width, height, opts);
  return encodings;
}
function determineAppropriateEncoding(isScreenShare, width, height, codec) {
  const presets = presetsForResolution(isScreenShare, width, height);
  let encoding = presets[0].encoding;
  const size = Math.max(width, height);
  for (let i2 = 0; i2 < presets.length; i2 += 1) {
    const preset = presets[i2];
    encoding = preset.encoding;
    if (preset.width >= size) {
      break;
    }
  }
  if (codec) {
    switch (codec) {
      case "av1":
      case "h265":
        encoding = Object.assign({}, encoding);
        encoding.maxBitrate = encoding.maxBitrate * 0.7;
        break;
      case "vp9":
        encoding = Object.assign({}, encoding);
        encoding.maxBitrate = encoding.maxBitrate * 0.85;
        break;
    }
  }
  return encoding;
}
function presetsForResolution(isScreenShare, width, height) {
  if (isScreenShare) {
    return presetsScreenShare;
  }
  const aspect = width > height ? width / height : height / width;
  if (Math.abs(aspect - 16 / 9) < Math.abs(aspect - 4 / 3)) {
    return presets169;
  }
  return presets43;
}
function defaultSimulcastLayers(isScreenShare, original) {
  if (isScreenShare) {
    return computeDefaultScreenShareSimulcastPresets(original);
  }
  const width = original.width, height = original.height;
  const aspect = width > height ? width / height : height / width;
  if (Math.abs(aspect - 16 / 9) < Math.abs(aspect - 4 / 3)) {
    return defaultSimulcastPresets169;
  }
  return defaultSimulcastPresets43;
}
function encodingsFromPresets(width, height, presets, sourceFramerate) {
  const encodings = [];
  presets.forEach((preset, idx) => {
    if (idx >= videoRids.length) {
      return;
    }
    const size = Math.min(width, height);
    const rid = videoRids[idx];
    const encoding = {
      rid,
      scaleResolutionDownBy: Math.max(1, size / Math.min(preset.width, preset.height)),
      maxBitrate: preset.encoding.maxBitrate
    };
    const maxFramerate = sourceFramerate && preset.encoding.maxFramerate ? Math.min(sourceFramerate, preset.encoding.maxFramerate) : preset.encoding.maxFramerate;
    if (maxFramerate) {
      encoding.maxFramerate = maxFramerate;
    }
    const browser = getBrowser();
    const canSetPriority = (browser === null || browser === void 0 ? void 0 : browser.name) === "Firefox" && browser.os !== "iOS" || idx === 0;
    if (preset.encoding.priority && canSetPriority) {
      encoding.priority = preset.encoding.priority;
      encoding.networkPriority = preset.encoding.priority;
    }
    encodings.push(encoding);
  });
  if (isReactNative() && getReactNativeOs() === "ios") {
    let topFramerate = void 0;
    encodings.forEach((encoding) => {
      if (!topFramerate) {
        topFramerate = encoding.maxFramerate;
      } else if (encoding.maxFramerate && encoding.maxFramerate > topFramerate) {
        topFramerate = encoding.maxFramerate;
      }
    });
    let notifyOnce = true;
    encodings.forEach((encoding) => {
      var _a2;
      if (encoding.maxFramerate != topFramerate) {
        if (notifyOnce) {
          notifyOnce = false;
          livekitLogger.info("Simulcast on iOS React-Native requires all encodings to share the same framerate.");
        }
        livekitLogger.info('Setting framerate of encoding "'.concat((_a2 = encoding.rid) !== null && _a2 !== void 0 ? _a2 : "", '" to ').concat(topFramerate));
        encoding.maxFramerate = topFramerate;
      }
    });
  }
  return encodings;
}
function sortPresets(presets) {
  if (!presets) return;
  return presets.slice().sort((a, b) => {
    const aEnc = a.encoding;
    const bEnc = b.encoding;
    if (aEnc.maxBitrate > bEnc.maxBitrate) {
      return 1;
    }
    if (aEnc.maxBitrate < bEnc.maxBitrate) return -1;
    if (aEnc.maxBitrate === bEnc.maxBitrate && aEnc.maxFramerate && bEnc.maxFramerate) {
      return aEnc.maxFramerate > bEnc.maxFramerate ? 1 : -1;
    }
    return 0;
  });
}
var ScalabilityMode = class {
  constructor(scalabilityMode) {
    const results = scalabilityMode.match(/^L(\d)T(\d)(h|_KEY|_KEY_SHIFT){0,1}$/);
    if (!results) {
      throw new Error("invalid scalability mode");
    }
    this.spatial = parseInt(results[1]);
    this.temporal = parseInt(results[2]);
    if (results.length > 3) {
      switch (results[3]) {
        case "h":
        case "_KEY":
        case "_KEY_SHIFT":
          this.suffix = results[3];
      }
    }
  }
  toString() {
    var _a2;
    return "L".concat(this.spatial, "T").concat(this.temporal).concat((_a2 = this.suffix) !== null && _a2 !== void 0 ? _a2 : "");
  }
};
function getDefaultDegradationPreference(track) {
  switch (track.source) {
    case Track.Source.Camera:
      return "maintain-framerate";
    case Track.Source.ScreenShare:
      return "maintain-resolution";
    default:
      return "balanced";
  }
}
var refreshSubscribedCodecAfterNewCodec = 5e3;
var LocalVideoTrack = class extends LocalTrack {
  get sender() {
    return this._sender;
  }
  set sender(sender) {
    this._sender = sender;
    if (this.degradationPreference) {
      this.setDegradationPreference(this.degradationPreference);
    }
  }
  /**
   *
   * @param mediaTrack
   * @param constraints MediaTrackConstraints that are being used when restarting or reacquiring tracks
   * @param userProvidedTrack Signals to the SDK whether or not the mediaTrack should be managed (i.e. released and reacquired) internally by the SDK
   */
  constructor(mediaTrack, constraints) {
    let userProvidedTrack = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : true;
    let loggerOptions = arguments.length > 3 ? arguments[3] : void 0;
    super(mediaTrack, Track.Kind.Video, constraints, userProvidedTrack, loggerOptions);
    this.simulcastCodecs = /* @__PURE__ */ new Map();
    this.degradationPreference = "balanced";
    this.isCpuConstrained = false;
    this.optimizeForPerformance = false;
    this.monitorSender = () => __awaiter(this, void 0, void 0, function* () {
      if (!this.sender) {
        this._currentBitrate = 0;
        return;
      }
      let stats;
      try {
        stats = yield this.getSenderStats();
      } catch (e4) {
        this.log.error("could not get video sender stats", Object.assign(Object.assign({}, this.logContext), {
          error: e4
        }));
        return;
      }
      const statsMap = new Map(stats.map((s) => [s.rid, s]));
      const isCpuConstrained = stats.some((s) => s.qualityLimitationReason === "cpu");
      if (isCpuConstrained !== this.isCpuConstrained) {
        this.isCpuConstrained = isCpuConstrained;
        if (this.isCpuConstrained) {
          this.emit(TrackEvent.CpuConstrained);
        }
      }
      if (this.prevStats) {
        let totalBitrate = 0;
        statsMap.forEach((s, key) => {
          var _a2;
          const prev = (_a2 = this.prevStats) === null || _a2 === void 0 ? void 0 : _a2.get(key);
          totalBitrate += computeBitrate(s, prev);
        });
        this._currentBitrate = totalBitrate;
      }
      this.prevStats = statsMap;
    });
    this.senderLock = new _();
  }
  get isSimulcast() {
    if (this.sender && this.sender.getParameters().encodings.length > 1) {
      return true;
    }
    return false;
  }
  /* @internal */
  startMonitor(signalClient) {
    var _a2;
    this.signalClient = signalClient;
    if (!isWeb()) {
      return;
    }
    const params = (_a2 = this.sender) === null || _a2 === void 0 ? void 0 : _a2.getParameters();
    if (params) {
      this.encodings = params.encodings;
    }
    if (this.monitorInterval) {
      return;
    }
    this.monitorInterval = setInterval(() => {
      this.monitorSender();
    }, monitorFrequency);
  }
  stop() {
    this._mediaStreamTrack.getConstraints();
    this.simulcastCodecs.forEach((trackInfo) => {
      trackInfo.mediaStreamTrack.stop();
    });
    super.stop();
  }
  pauseUpstream() {
    const _super = Object.create(null, {
      pauseUpstream: {
        get: () => super.pauseUpstream
      }
    });
    return __awaiter(this, void 0, void 0, function* () {
      var _a2, e_1, _b2, _c;
      var _d;
      yield _super.pauseUpstream.call(this);
      try {
        for (var _e2 = true, _f = __asyncValues(this.simulcastCodecs.values()), _g; _g = yield _f.next(), _a2 = _g.done, !_a2; _e2 = true) {
          _c = _g.value;
          _e2 = false;
          const sc = _c;
          yield (_d = sc.sender) === null || _d === void 0 ? void 0 : _d.replaceTrack(null);
        }
      } catch (e_1_1) {
        e_1 = {
          error: e_1_1
        };
      } finally {
        try {
          if (!_e2 && !_a2 && (_b2 = _f.return)) yield _b2.call(_f);
        } finally {
          if (e_1) throw e_1.error;
        }
      }
    });
  }
  resumeUpstream() {
    const _super = Object.create(null, {
      resumeUpstream: {
        get: () => super.resumeUpstream
      }
    });
    return __awaiter(this, void 0, void 0, function* () {
      var _a2, e_2, _b2, _c;
      var _d;
      yield _super.resumeUpstream.call(this);
      try {
        for (var _e2 = true, _f = __asyncValues(this.simulcastCodecs.values()), _g; _g = yield _f.next(), _a2 = _g.done, !_a2; _e2 = true) {
          _c = _g.value;
          _e2 = false;
          const sc = _c;
          yield (_d = sc.sender) === null || _d === void 0 ? void 0 : _d.replaceTrack(sc.mediaStreamTrack);
        }
      } catch (e_2_1) {
        e_2 = {
          error: e_2_1
        };
      } finally {
        try {
          if (!_e2 && !_a2 && (_b2 = _f.return)) yield _b2.call(_f);
        } finally {
          if (e_2) throw e_2.error;
        }
      }
    });
  }
  mute() {
    const _super = Object.create(null, {
      mute: {
        get: () => super.mute
      }
    });
    return __awaiter(this, void 0, void 0, function* () {
      const unlock = yield this.muteLock.lock();
      try {
        if (this.isMuted) {
          this.log.debug("Track already muted", this.logContext);
          return this;
        }
        if (this.source === Track.Source.Camera && !this.isUserProvided) {
          this.log.debug("stopping camera track", this.logContext);
          this._mediaStreamTrack.stop();
        }
        yield _super.mute.call(this);
        return this;
      } finally {
        unlock();
      }
    });
  }
  unmute() {
    const _super = Object.create(null, {
      unmute: {
        get: () => super.unmute
      }
    });
    return __awaiter(this, void 0, void 0, function* () {
      const unlock = yield this.muteLock.lock();
      try {
        if (!this.isMuted) {
          this.log.debug("Track already unmuted", this.logContext);
          return this;
        }
        if (this.source === Track.Source.Camera && !this.isUserProvided) {
          this.log.debug("reacquiring camera track", this.logContext);
          yield this.restart(void 0, true);
        }
        yield _super.unmute.call(this);
        return this;
      } finally {
        unlock();
      }
    });
  }
  setTrackMuted(muted) {
    super.setTrackMuted(muted);
    for (const sc of this.simulcastCodecs.values()) {
      sc.mediaStreamTrack.enabled = !muted;
    }
  }
  getSenderStats() {
    return __awaiter(this, void 0, void 0, function* () {
      var _a2;
      if (!((_a2 = this.sender) === null || _a2 === void 0 ? void 0 : _a2.getStats)) {
        return [];
      }
      const items = [];
      const stats = yield this.sender.getStats();
      stats.forEach((v) => {
        var _a3;
        if (v.type === "outbound-rtp") {
          const vs2 = {
            type: "video",
            streamId: v.id,
            frameHeight: v.frameHeight,
            frameWidth: v.frameWidth,
            framesPerSecond: v.framesPerSecond,
            framesSent: v.framesSent,
            firCount: v.firCount,
            pliCount: v.pliCount,
            nackCount: v.nackCount,
            packetsSent: v.packetsSent,
            bytesSent: v.bytesSent,
            qualityLimitationReason: v.qualityLimitationReason,
            qualityLimitationDurations: v.qualityLimitationDurations,
            qualityLimitationResolutionChanges: v.qualityLimitationResolutionChanges,
            rid: (_a3 = v.rid) !== null && _a3 !== void 0 ? _a3 : v.id,
            retransmittedPacketsSent: v.retransmittedPacketsSent,
            targetBitrate: v.targetBitrate,
            timestamp: v.timestamp
          };
          const r2 = stats.get(v.remoteId);
          if (r2) {
            vs2.jitter = r2.jitter;
            vs2.packetsLost = r2.packetsLost;
            vs2.roundTripTime = r2.roundTripTime;
          }
          items.push(vs2);
        }
      });
      items.sort((a, b) => {
        var _a3, _b2;
        return ((_a3 = b.frameWidth) !== null && _a3 !== void 0 ? _a3 : 0) - ((_b2 = a.frameWidth) !== null && _b2 !== void 0 ? _b2 : 0);
      });
      return items;
    });
  }
  setPublishingQuality(maxQuality) {
    const qualities = [];
    for (let q3 = VideoQuality.LOW; q3 <= VideoQuality.HIGH; q3 += 1) {
      qualities.push(new SubscribedQuality({
        quality: q3,
        enabled: q3 <= maxQuality
      }));
    }
    this.log.debug("setting publishing quality. max quality ".concat(maxQuality), this.logContext);
    this.setPublishingLayers(isSVCCodec(this.codec), qualities);
  }
  restartTrack(options) {
    return __awaiter(this, void 0, void 0, function* () {
      var _a2, e_3, _b2, _c;
      var _d;
      let constraints;
      if (options) {
        const streamConstraints = constraintsForOptions({
          video: options
        });
        if (typeof streamConstraints.video !== "boolean") {
          constraints = streamConstraints.video;
        }
      }
      yield this.restart(constraints);
      this.isCpuConstrained = false;
      try {
        for (var _e2 = true, _f = __asyncValues(this.simulcastCodecs.values()), _g; _g = yield _f.next(), _a2 = _g.done, !_a2; _e2 = true) {
          _c = _g.value;
          _e2 = false;
          const sc = _c;
          if (sc.sender && ((_d = sc.sender.transport) === null || _d === void 0 ? void 0 : _d.state) !== "closed") {
            sc.mediaStreamTrack = this.mediaStreamTrack.clone();
            yield sc.sender.replaceTrack(sc.mediaStreamTrack);
          }
        }
      } catch (e_3_1) {
        e_3 = {
          error: e_3_1
        };
      } finally {
        try {
          if (!_e2 && !_a2 && (_b2 = _f.return)) yield _b2.call(_f);
        } finally {
          if (e_3) throw e_3.error;
        }
      }
      yield this.onSenderTrackSwapped();
    });
  }
  onSenderTrackSwapped() {
    return __awaiter(this, void 0, void 0, function* () {
      yield this.refreshSenderEncodings();
    });
  }
  /**
   * Recomputes encoding parameters for this track's senders based on the current
   * MediaStreamTrack dimensions and reapplies them via setParameters. This is a no-op
   * if the track hasn't been published yet or if the track is in performance-optimized
   * mode (which manages its own encodings).
   */
  refreshSenderEncodings() {
    return __awaiter(this, void 0, void 0, function* () {
      var _a2;
      if (!this.sender || !this.publishOptions || this.optimizeForPerformance) {
        return;
      }
      const unlock = yield this.senderLock.lock();
      try {
        let dims;
        try {
          dims = yield this.waitForDimensions();
        } catch (e4) {
          this.log.warn("could not determine new track dimensions, skipping encoding recompute", Object.assign(Object.assign({}, this.logContext), {
            error: e4
          }));
          return;
        }
        if (this.lastEncodedDimensions && this.lastEncodedDimensions.width === dims.width && this.lastEncodedDimensions.height === dims.height) {
          return;
        }
        const isScreenShare = this.source === Track.Source.ScreenShare;
        const newEncodings = computeVideoEncodings(isScreenShare, dims.width, dims.height, Object.assign({}, this.publishOptions));
        yield this.applyEncodingsToSender(this.sender, newEncodings);
        this.encodings = newEncodings;
        this.lastEncodedDimensions = dims;
        for (const _ref of this.simulcastCodecs) {
          var _ref2 = _slicedToArray(_ref, 2);
          const codec = _ref2[0];
          const sc = _ref2[1];
          if (!sc.sender || ((_a2 = sc.sender.transport) === null || _a2 === void 0 ? void 0 : _a2.state) === "closed") {
            continue;
          }
          if (!isBackupVideoCodec(codec)) {
            continue;
          }
          const backupOpts = Object.assign({}, this.publishOptions);
          const backupEncodings = computeTrackBackupEncodings(this, codec, backupOpts);
          if (!backupEncodings) {
            continue;
          }
          yield this.applyEncodingsToSender(sc.sender, backupEncodings);
          sc.encodings = backupEncodings;
        }
      } catch (e4) {
        this.log.warn("failed to apply recomputed encodings", Object.assign(Object.assign({}, this.logContext), {
          error: e4
        }));
      } finally {
        unlock();
      }
    });
  }
  applyEncodingsToSender(sender, encodings) {
    return __awaiter(this, void 0, void 0, function* () {
      const params = sender.getParameters();
      if (!params.encodings || params.encodings.length !== encodings.length) {
        return;
      }
      params.encodings.forEach((existing, idx) => {
        if (existing.active === false) {
          return;
        }
        const next = encodings[idx];
        if (next.scaleResolutionDownBy !== void 0) {
          existing.scaleResolutionDownBy = next.scaleResolutionDownBy;
        }
        if (next.maxBitrate !== void 0) {
          existing.maxBitrate = next.maxBitrate;
        }
        if (next.maxFramerate !== void 0) {
          existing.maxFramerate = next.maxFramerate;
        }
        if (next.priority !== void 0) {
          existing.priority = next.priority;
          existing.networkPriority = next.priority;
        }
      });
      this.log.debug("updating sender encodings after track restart", Object.assign(Object.assign({}, this.logContext), {
        encodings: params.encodings
      }));
      yield sender.setParameters(params);
    });
  }
  setProcessor(processor_1) {
    const _super = Object.create(null, {
      setProcessor: {
        get: () => super.setProcessor
      }
    });
    return __awaiter(this, arguments, void 0, function(processor) {
      var _this = this;
      let showProcessedStreamLocally = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : true;
      return function* () {
        var _a2, e_4, _b2, _c;
        var _d, _e2;
        yield _super.setProcessor.call(_this, processor, showProcessedStreamLocally);
        if ((_d = _this.processor) === null || _d === void 0 ? void 0 : _d.processedTrack) {
          try {
            for (var _f = true, _g = __asyncValues(_this.simulcastCodecs.values()), _h; _h = yield _g.next(), _a2 = _h.done, !_a2; _f = true) {
              _c = _h.value;
              _f = false;
              const sc = _c;
              yield (_e2 = sc.sender) === null || _e2 === void 0 ? void 0 : _e2.replaceTrack(_this.processor.processedTrack);
            }
          } catch (e_4_1) {
            e_4 = {
              error: e_4_1
            };
          } finally {
            try {
              if (!_f && !_a2 && (_b2 = _g.return)) yield _b2.call(_g);
            } finally {
              if (e_4) throw e_4.error;
            }
          }
        }
      }();
    });
  }
  setDegradationPreference(preference) {
    return __awaiter(this, void 0, void 0, function* () {
      this.degradationPreference = preference;
      if (this.sender) {
        try {
          this.log.debug("setting degradationPreference to ".concat(preference), this.logContext);
          const params = this.sender.getParameters();
          params.degradationPreference = preference;
          this.sender.setParameters(params);
        } catch (e4) {
          this.log.warn("failed to set degradationPreference", Object.assign({
            error: e4
          }, this.logContext));
        }
      }
    });
  }
  addSimulcastTrack(codec, encodings) {
    if (this.simulcastCodecs.has(codec)) {
      this.log.error("".concat(codec, " already added, skipping adding simulcast codec"), this.logContext);
      return;
    }
    const simulcastCodecInfo = {
      codec,
      mediaStreamTrack: this.mediaStreamTrack.clone(),
      sender: void 0,
      encodings
    };
    this.simulcastCodecs.set(codec, simulcastCodecInfo);
    return simulcastCodecInfo;
  }
  setSimulcastTrackSender(codec, sender) {
    const simulcastCodecInfo = this.simulcastCodecs.get(codec);
    if (!simulcastCodecInfo) {
      return;
    }
    simulcastCodecInfo.sender = sender;
    setTimeout(() => {
      if (this.subscribedCodecs) {
        this.setPublishingCodecs(this.subscribedCodecs);
      }
    }, refreshSubscribedCodecAfterNewCodec);
  }
  /**
   * @internal
   * Sets codecs that should be publishing, returns new codecs that have not yet
   * been published
   */
  setPublishingCodecs(codecs) {
    return __awaiter(this, void 0, void 0, function* () {
      var _a2, codecs_1, codecs_1_1;
      var _b2, e_5, _c, _d;
      this.log.debug("setting publishing codecs", Object.assign(Object.assign({}, this.logContext), {
        codecs,
        currentCodec: this.codec
      }));
      if (!this.codec && codecs.length > 0) {
        yield this.setPublishingLayers(isSVCCodec(codecs[0].codec), codecs[0].qualities);
        return [];
      }
      this.subscribedCodecs = codecs;
      const newCodecs = [];
      try {
        for (_a2 = true, codecs_1 = __asyncValues(codecs); codecs_1_1 = yield codecs_1.next(), _b2 = codecs_1_1.done, !_b2; _a2 = true) {
          _d = codecs_1_1.value;
          _a2 = false;
          const codec = _d;
          if (!this.codec || this.codec === codec.codec) {
            yield this.setPublishingLayers(isSVCCodec(codec.codec), codec.qualities);
          } else {
            const simulcastCodecInfo = this.simulcastCodecs.get(codec.codec);
            this.log.debug("try setPublishingCodec for ".concat(codec.codec), Object.assign(Object.assign({}, this.logContext), {
              simulcastCodecInfo
            }));
            if (!simulcastCodecInfo || !simulcastCodecInfo.sender) {
              for (const q3 of codec.qualities) {
                if (q3.enabled) {
                  newCodecs.push(codec.codec);
                  break;
                }
              }
            } else if (simulcastCodecInfo.encodings) {
              this.log.debug("try setPublishingLayersForSender ".concat(codec.codec), this.logContext);
              yield setPublishingLayersForSender(simulcastCodecInfo.sender, simulcastCodecInfo.encodings, codec.qualities, this.senderLock, isSVCCodec(codec.codec), this.log, this.logContext);
            }
          }
        }
      } catch (e_5_1) {
        e_5 = {
          error: e_5_1
        };
      } finally {
        try {
          if (!_a2 && !_b2 && (_c = codecs_1.return)) yield _c.call(codecs_1);
        } finally {
          if (e_5) throw e_5.error;
        }
      }
      return newCodecs;
    });
  }
  /**
   * @internal
   * Sets layers that should be publishing
   */
  setPublishingLayers(isSvc, qualities) {
    return __awaiter(this, void 0, void 0, function* () {
      if (this.optimizeForPerformance) {
        this.log.info("skipping setPublishingLayers due to optimized publishing performance", Object.assign(Object.assign({}, this.logContext), {
          qualities
        }));
        return;
      }
      this.log.debug("setting publishing layers", Object.assign(Object.assign({}, this.logContext), {
        qualities
      }));
      if (!this.sender || !this.encodings) {
        return;
      }
      yield setPublishingLayersForSender(this.sender, this.encodings, qualities, this.senderLock, isSvc, this.log, this.logContext);
    });
  }
  /**
   * Designed for lower powered devices, reduces video publishing quality and disables simulcast.
   * @experimental
   */
  prioritizePerformance() {
    return __awaiter(this, void 0, void 0, function* () {
      if (!this.sender) {
        throw new Error("sender not found");
      }
      const unlock = yield this.senderLock.lock();
      try {
        this.optimizeForPerformance = true;
        const params = this.sender.getParameters();
        params.encodings = params.encodings.map((e4, idx) => {
          var _a2;
          return Object.assign(Object.assign({}, e4), {
            active: idx === 0,
            scaleResolutionDownBy: Math.max(1, Math.ceil(((_a2 = this.mediaStreamTrack.getSettings().height) !== null && _a2 !== void 0 ? _a2 : 360) / 360)),
            scalabilityMode: idx === 0 && isSVCCodec(this.codec) ? "L1T3" : void 0,
            maxFramerate: idx === 0 ? 15 : 0,
            maxBitrate: idx === 0 ? e4.maxBitrate : 0
          });
        });
        this.log.debug("setting performance optimised encodings", Object.assign(Object.assign({}, this.logContext), {
          encodings: params.encodings
        }));
        this.encodings = params.encodings;
        yield this.sender.setParameters(params);
      } catch (e4) {
        this.log.error("failed to set performance optimised encodings", Object.assign(Object.assign({}, this.logContext), {
          error: e4
        }));
        this.optimizeForPerformance = false;
      } finally {
        unlock();
      }
    });
  }
  handleAppVisibilityChanged() {
    const _super = Object.create(null, {
      handleAppVisibilityChanged: {
        get: () => super.handleAppVisibilityChanged
      }
    });
    return __awaiter(this, void 0, void 0, function* () {
      yield _super.handleAppVisibilityChanged.call(this);
      if (!isMobile()) return;
      if (this.isInBackground && this.source === Track.Source.Camera) {
        this._mediaStreamTrack.enabled = false;
      }
    });
  }
};
function setPublishingLayersForSender(sender, senderEncodings, qualities, senderLock, isSVC, log2, logContext) {
  return __awaiter(this, void 0, void 0, function* () {
    const unlock = yield senderLock.lock();
    log2.debug("setPublishingLayersForSender", Object.assign(Object.assign({}, logContext), {
      sender,
      qualities,
      senderEncodings
    }));
    try {
      const params = sender.getParameters();
      const encodings = params.encodings;
      if (!encodings) {
        return;
      }
      if (encodings.length !== senderEncodings.length) {
        log2.warn("cannot set publishing layers, encodings mismatch", Object.assign(Object.assign({}, logContext), {
          encodings,
          senderEncodings
        }));
        return;
      }
      let hasChanged = false;
      const closableSpatial = false;
      if (closableSpatial && encodings[0].scalabilityMode) ;
      else {
        if (isSVC) {
          const hasEnabledEncoding = qualities.some((q3) => q3.enabled);
          if (hasEnabledEncoding) {
            qualities.forEach((q3) => q3.enabled = true);
          }
        }
        encodings.forEach((encoding, idx) => {
          var _a2;
          let rid = (_a2 = encoding.rid) !== null && _a2 !== void 0 ? _a2 : "";
          if (rid === "") {
            rid = "q";
          }
          const quality = videoQualityForRid(rid);
          const subscribedQuality = qualities.find((q3) => q3.quality === quality);
          if (!subscribedQuality) {
            return;
          }
          if (encoding.active !== subscribedQuality.enabled) {
            hasChanged = true;
            encoding.active = subscribedQuality.enabled;
            log2.debug("setting layer ".concat(subscribedQuality.quality, " to ").concat(encoding.active ? "enabled" : "disabled"), logContext);
            if (isFireFox()) {
              if (subscribedQuality.enabled) {
                encoding.scaleResolutionDownBy = senderEncodings[idx].scaleResolutionDownBy;
                encoding.maxBitrate = senderEncodings[idx].maxBitrate;
                encoding.maxFrameRate = senderEncodings[idx].maxFrameRate;
              } else {
                encoding.scaleResolutionDownBy = 4;
                encoding.maxBitrate = 10;
                encoding.maxFrameRate = 2;
              }
            }
          }
        });
      }
      if (hasChanged) {
        params.encodings = encodings;
        log2.debug("setting encodings", Object.assign(Object.assign({}, logContext), {
          encodings: params.encodings
        }));
        yield sender.setParameters(params);
      }
    } finally {
      unlock();
    }
  });
}
function videoQualityForRid(rid) {
  switch (rid) {
    case "f":
      return VideoQuality.HIGH;
    case "h":
      return VideoQuality.MEDIUM;
    case "q":
      return VideoQuality.LOW;
    default:
      return VideoQuality.HIGH;
  }
}
function videoLayersFromEncodings(width, height, encodings, svc) {
  if (!encodings) {
    return [new VideoLayer({
      quality: VideoQuality.HIGH,
      width,
      height,
      bitrate: 0,
      ssrc: 0
    })];
  }
  if (svc) {
    const encodingSM = encodings[0].scalabilityMode;
    const sm = new ScalabilityMode(encodingSM);
    const layers = [];
    const resRatio = sm.suffix == "h" ? 1.5 : 2;
    const bitratesRatio = sm.suffix == "h" ? 2 : 3;
    for (let i2 = 0; i2 < sm.spatial; i2 += 1) {
      layers.push(new VideoLayer({
        quality: Math.min(VideoQuality.HIGH, sm.spatial - 1) - i2,
        width: Math.ceil(width / Math.pow(resRatio, i2)),
        height: Math.ceil(height / Math.pow(resRatio, i2)),
        bitrate: encodings[0].maxBitrate ? Math.ceil(encodings[0].maxBitrate / Math.pow(bitratesRatio, i2)) : 0,
        ssrc: 0
      }));
    }
    return layers;
  }
  return encodings.map((encoding) => {
    var _a2, _b2, _c;
    const scale = (_a2 = encoding.scaleResolutionDownBy) !== null && _a2 !== void 0 ? _a2 : 1;
    let quality = videoQualityForRid((_b2 = encoding.rid) !== null && _b2 !== void 0 ? _b2 : "");
    return new VideoLayer({
      quality,
      width: Math.ceil(width / scale),
      height: Math.ceil(height / scale),
      bitrate: (_c = encoding.maxBitrate) !== null && _c !== void 0 ? _c : 0,
      ssrc: 0
    });
  });
}
var minReconnectWait = 2 * 1e3;
var leaveReconnect = "leave-reconnect";
var reliabeReceiveStateTTL = 3e4;
var initialMediaSectionsAudio = 3;
var initialMediaSectionsVideo = 3;
var PCState;
(function(PCState2) {
  PCState2[PCState2["New"] = 0] = "New";
  PCState2[PCState2["Connected"] = 1] = "Connected";
  PCState2[PCState2["Disconnected"] = 2] = "Disconnected";
  PCState2[PCState2["Reconnecting"] = 3] = "Reconnecting";
  PCState2[PCState2["Closed"] = 4] = "Closed";
})(PCState || (PCState = {}));
var DEFAULT_MAX_MESSAGE_SIZE = 64e3;
var RTCEngine = class extends eventsExports.EventEmitter {
  get isClosed() {
    return this._isClosed;
  }
  get isNewlyCreated() {
    return this._isNewlyCreated;
  }
  get pendingReconnect() {
    return !!this.reconnectTimeout;
  }
  get reliableChannel() {
    return this.dataChannels.reliable;
  }
  get lossyChannel() {
    return this.dataChannels.lossy;
  }
  get dataTrackChannel() {
    return this.dataChannels.dataTrack;
  }
  constructor(options) {
    var _a2;
    super();
    this.options = options;
    this.rtcConfig = {};
    this.peerConnectionTimeout = roomConnectOptionDefaults.peerConnectionTimeout;
    this.fullReconnectOnNext = false;
    this.latestRemoteOfferId = 0;
    this.subscriberPrimary = false;
    this.pcState = PCState.New;
    this._isClosed = true;
    this._isNewlyCreated = true;
    this.pendingTrackResolvers = {};
    this.reconnectAttempts = 0;
    this.reconnectStart = 0;
    this.attemptingReconnect = false;
    this.joinAttempts = 0;
    this.maxJoinAttempts = 1;
    this.shouldFailNext = false;
    this.shouldFailOnV1Path = false;
    this.log = livekitLogger;
    this.reliableReceivedState = new TTLMap(reliabeReceiveStateTTL);
    this.midToTrackId = {};
    this.isWaitingForNetworkReconnect = false;
    this.handleDataChannel = (_a3) => __awaiter(this, [_a3], void 0, function(_ref) {
      var _this = this;
      let channel = _ref.channel;
      return function* () {
        if (!channel) {
          return;
        }
        if (_this.dataChannels.adoptSubscriberChannel(channel)) {
          _this.log.debug("on data channel ".concat(channel.id, ", ").concat(channel.label));
        }
      }();
    });
    this.handleDataMessage = (message) => __awaiter(this, void 0, void 0, function* () {
      var _a3, _b2, _c, _d, _e2;
      const unlock = yield this.dataProcessLock.lock();
      try {
        const bytes = yield this.decodeDataMessage(message);
        if (!bytes) {
          return;
        }
        const dp = DataPacket.fromBinary(bytes);
        if (dp.sequence > 0 && dp.participantSid !== "") {
          const lastSeq = this.reliableReceivedState.get(dp.participantSid);
          if (lastSeq && dp.sequence <= lastSeq) {
            return;
          }
          this.reliableReceivedState.set(dp.participantSid, dp.sequence);
        }
        if (((_a3 = dp.value) === null || _a3 === void 0 ? void 0 : _a3.case) === "speaker") {
          this.emit(EngineEvent.ActiveSpeakersUpdate, dp.value.value.speakers);
        } else if (((_b2 = dp.value) === null || _b2 === void 0 ? void 0 : _b2.case) === "encryptedPacket") {
          if (!this.e2eeManager) {
            this.log.error("Received encrypted packet but E2EE not set up");
            return;
          }
          const decryptedData = yield (_c = this.e2eeManager) === null || _c === void 0 ? void 0 : _c.handleEncryptedData(dp.value.value.encryptedValue, dp.value.value.iv, dp.participantIdentity, dp.value.value.keyIndex);
          const decryptedPacket = EncryptedPacketPayload.fromBinary(decryptedData.payload);
          const newDp = new DataPacket({
            value: decryptedPacket.value,
            participantIdentity: dp.participantIdentity,
            participantSid: dp.participantSid
          });
          if (((_d = newDp.value) === null || _d === void 0 ? void 0 : _d.case) === "user") {
            applyUserDataCompat(newDp, newDp.value.value);
          }
          this.emit(EngineEvent.DataPacketReceived, newDp, dp.value.value.encryptionType);
        } else {
          if (((_e2 = dp.value) === null || _e2 === void 0 ? void 0 : _e2.case) === "user") {
            applyUserDataCompat(dp, dp.value.value);
          }
          this.emit(EngineEvent.DataPacketReceived, dp, Encryption_Type.NONE);
        }
      } finally {
        unlock();
      }
    });
    this.handleDataTrackMessage = (message) => __awaiter(this, void 0, void 0, function* () {
      const bytes = yield this.decodeDataMessage(message);
      if (!bytes) {
        return;
      }
      this.emit("dataTrackPacketReceived", bytes);
    });
    this.handleDataError = (event) => {
      if (this._isClosed) {
        return;
      }
      const channel = event.currentTarget;
      const channelKind = channel.maxRetransmits === 0 ? "lossy" : "reliable";
      if (typeof RTCErrorEvent !== "undefined" && event instanceof RTCErrorEvent && event.error) {
        const error = event.error;
        this.log.error("DataChannel error on ".concat(channelKind, ": ").concat(error.message), {
          error,
          errorDetail: error.errorDetail,
          sctpCauseCode: error.sctpCauseCode
        });
      } else {
        this.log.error("Unknown DataChannel error on ".concat(channelKind), {
          event
        });
      }
    };
    this.handleDataChannelClose = (kind) => () => {
      var _a3;
      if (!this._isClosed && ((_a3 = this.pcManager) === null || _a3 === void 0 ? void 0 : _a3.publisher.getConnectionState()) === "connected") {
        this.log.error("publisher data channel '".concat(DataChannelKind[kind], "' closed unexpectedly"), this.logContext);
      }
    };
    this.handleDisconnect = (connection, disconnectReason) => {
      if (this._isClosed) {
        return;
      }
      this.log.warn("".concat(connection, " disconnected"));
      if (this.reconnectAttempts === 0) {
        this.reconnectStart = Date.now();
      }
      const disconnect = (duration2) => {
        this.log.warn("could not recover connection after ".concat(this.reconnectAttempts, " attempts, ").concat(duration2, "ms. giving up"));
        this.emit(EngineEvent.Disconnected);
        this.close();
      };
      const duration = Date.now() - this.reconnectStart;
      let delay = this.getNextRetryDelay({
        elapsedMs: duration,
        retryCount: this.reconnectAttempts
      });
      if (delay === null) {
        disconnect(duration);
        return;
      }
      if (connection === leaveReconnect) {
        delay = 0;
      }
      this.log.debug("reconnecting in ".concat(delay, "ms"));
      this.clearReconnectTimeout();
      if (this.token) {
        this.emit(EngineEvent.TokenRefreshed, this.token);
      }
      this.reconnectTimeout = CriticalTimers.setTimeout(() => this.attemptReconnect(disconnectReason).finally(() => this.reconnectTimeout = void 0), delay);
    };
    this.waitForRestarted = () => {
      return new Promise((resolve, reject) => {
        if (this.pcState === PCState.Connected) {
          resolve();
        }
        const onRestarted = () => {
          this.off(EngineEvent.Disconnected, onDisconnected);
          resolve();
        };
        const onDisconnected = () => {
          this.off(EngineEvent.Restarted, onRestarted);
          reject();
        };
        this.once(EngineEvent.Restarted, onRestarted);
        this.once(EngineEvent.Disconnected, onDisconnected);
      });
    };
    this.onRtpMapAvailable = (rtpTypes) => {
      const rtpMap = /* @__PURE__ */ new Map();
      rtpTypes.forEach((rtp) => {
        const codec = rtp.codec.toLowerCase();
        if (isVideoCodec(codec)) {
          rtpMap.set(rtp.payload, codec);
        }
      });
      this.emit(EngineEvent.RTPVideoMapUpdate, rtpMap);
    };
    this.handleBrowserOnLine = () => __awaiter(this, void 0, void 0, function* () {
      if (!this.url) {
        return;
      }
      const hasNetworkConnection = yield fetch(toHttpUrl(this.url), {
        method: "HEAD"
      }).then((resp) => resp.ok).catch(() => false);
      if (!hasNetworkConnection) {
        return;
      }
      this.log.info("detected network reconnected");
      if (
        // in case the engine is currently reconnecting, attempt a reconnect immediately after the browser state has changed to 'onLine'
        this.client.currentState === SignalConnectionState.RECONNECTING || // also if the browser went offline before and the engine still thinks it's in a connected state, treat it as a network interruption that we haven't noticed yet
        this.isWaitingForNetworkReconnect && this.client.currentState === SignalConnectionState.CONNECTED
      ) {
        this.clearReconnectTimeout();
        this.attemptReconnect(ReconnectReason.RR_SIGNAL_DISCONNECTED);
        this.isWaitingForNetworkReconnect = false;
      }
    });
    this.handleBrowserOffline = () => __awaiter(this, void 0, void 0, function* () {
      if (!this.url) {
        return;
      }
      try {
        yield Promise.race([
          fetch(toHttpUrl(this.url), {
            method: "HEAD"
          }),
          // if there's no internet connection the fetch rejects immediately, so we only use a short timeout here
          sleep(4e3).then(() => Promise.reject())
        ]);
      } catch (e4) {
        if (window.navigator.onLine === false) {
          this.log.info("detected network interruption");
          this.isWaitingForNetworkReconnect = true;
        }
      }
    });
    this.log = getLogger((_a2 = options.loggerName) !== null && _a2 !== void 0 ? _a2 : LoggerNames.Engine, () => this.logContext);
    this.loggerOptions = {
      loggerName: options.loggerName,
      loggerContextCb: () => this.logContext
    };
    this.client = new SignalClient(void 0, this.loggerOptions);
    this.client.signalLatency = this.options.expSignalLatency;
    this.reconnectPolicy = this.options.reconnectPolicy;
    this.closingLock = new _();
    this.dataProcessLock = new _();
    this.dataChannels = new DataChannelManager({
      isEngineClosed: () => this.isClosed,
      isReconnecting: () => this.attemptingReconnect,
      onDataMessage: (message) => this.handleDataMessage(message),
      onDataTrackMessage: (message) => this.handleDataTrackMessage(message),
      onDataError: (event) => this.handleDataError(event),
      onChannelClose: (kind) => this.handleDataChannelClose(kind)(),
      onBufferStatusChanged: (kind, isLow) => this.emit(EngineEvent.DCBufferStatusChanged, isLow, kind)
    });
    this.client.onParticipantUpdate = (updates) => this.emit(EngineEvent.ParticipantUpdate, updates);
    this.client.onConnectionQuality = (update) => this.emit(EngineEvent.ConnectionQualityUpdate, update);
    this.client.onRoomUpdate = (update) => this.emit(EngineEvent.RoomUpdate, update);
    this.client.onSubscriptionError = (resp) => this.emit(EngineEvent.SubscriptionError, resp);
    this.client.onSubscriptionPermissionUpdate = (update) => this.emit(EngineEvent.SubscriptionPermissionUpdate, update);
    this.client.onSpeakersChanged = (update) => this.emit(EngineEvent.SpeakersChanged, update);
    this.client.onStreamStateUpdate = (update) => this.emit(EngineEvent.StreamStateChanged, update);
    this.client.onRequestResponse = (response) => this.emit(EngineEvent.SignalRequestResponse, response);
    this.client.onParticipantUpdate = (updates) => this.emit(EngineEvent.ParticipantUpdate, updates);
    this.client.onJoined = (joinResponse) => this.emit(EngineEvent.Joined, joinResponse);
  }
  /** @internal */
  get logContext() {
    var _a2, _b2, _c, _d, _e2, _f;
    return {
      room: (_b2 = (_a2 = this.latestJoinResponse) === null || _a2 === void 0 ? void 0 : _a2.room) === null || _b2 === void 0 ? void 0 : _b2.name,
      roomID: (_d = (_c = this.latestJoinResponse) === null || _c === void 0 ? void 0 : _c.room) === null || _d === void 0 ? void 0 : _d.sid,
      participant: (_f = (_e2 = this.latestJoinResponse) === null || _e2 === void 0 ? void 0 : _e2.participant) === null || _f === void 0 ? void 0 : _f.identity,
      participantID: this.participantSid
    };
  }
  join(url_1, token_1, opts_1, abortSignal_1) {
    return __awaiter(this, arguments, void 0, function(url, token, opts, abortSignal) {
      var _this2 = this;
      let useV0Path = arguments.length > 4 && arguments[4] !== void 0 ? arguments[4] : false;
      return function* () {
        var _a2, _b2, _c;
        _this2._isNewlyCreated = false;
        _this2.url = url;
        _this2.token = token;
        _this2.signalOpts = opts;
        _this2.maxJoinAttempts = opts.maxRetries;
        try {
          _this2.joinAttempts += 1;
          _this2.setupSignalClientCallbacks();
          const sendOfferWithJoin = !useV0Path && isPublisherOfferWithJoinSupported();
          let offerProto;
          if (sendOfferWithJoin) {
            if (!_this2.pcManager) {
              yield _this2.configure();
              _this2.applyInitialPublisherLayout();
            }
            const offer = yield (_a2 = _this2.pcManager) === null || _a2 === void 0 ? void 0 : _a2.publisher.createInitialOffer();
            if (offer) {
              offerProto = toProtoSessionDescription(offer.offer, offer.offerId);
            }
          }
          if (abortSignal === null || abortSignal === void 0 ? void 0 : abortSignal.aborted) {
            throw ConnectionError.cancelled("Connection aborted");
          }
          if (!useV0Path && _this2.shouldFailOnV1Path) {
            _this2.shouldFailOnV1Path = false;
            throw ConnectionError.serviceNotFound("Simulated v1 path failure", "v0-rtc");
          }
          const joinResponse = yield _this2.client.join(url, token, opts, abortSignal, useV0Path, offerProto);
          _this2._isClosed = false;
          _this2.latestJoinResponse = joinResponse;
          _this2.participantSid = (_b2 = joinResponse.participant) === null || _b2 === void 0 ? void 0 : _b2.sid;
          _this2.subscriberPrimary = joinResponse.subscriberPrimary;
          if (sendOfferWithJoin) {
            (_c = _this2.pcManager) === null || _c === void 0 ? void 0 : _c.updateConfiguration(_this2.makeRTCConfiguration(joinResponse));
          } else {
            if (!_this2.pcManager) {
              yield _this2.configure(joinResponse, !useV0Path);
              if (!useV0Path) {
                _this2.applyInitialPublisherLayout();
              }
            }
            if (!_this2.subscriberPrimary || joinResponse.fastPublish) {
              _this2.negotiate().catch((err) => {
                _this2.log.error(err);
              });
            }
          }
          _this2.registerOnLineListener();
          _this2.clientConfiguration = joinResponse.clientConfiguration;
          _this2.emit(EngineEvent.SignalConnected, joinResponse);
          let serverInfo = joinResponse.serverInfo;
          if (!serverInfo) {
            serverInfo = {
              version: joinResponse.serverVersion,
              region: joinResponse.serverRegion
            };
          }
          _this2.log.info("connected to Livekit Server ".concat(Object.entries(serverInfo).map((_ref2) => {
            let _ref3 = _slicedToArray(_ref2, 2), key = _ref3[0], value = _ref3[1];
            return "".concat(key, ": ").concat(value);
          }).join(", ")));
          return {
            joinResponse,
            serverInfo
          };
        } catch (e4) {
          if (e4 instanceof ConnectionError) {
            if (e4.reason === ConnectionErrorReason.ServerUnreachable) {
              _this2.log.warn("Couldn't connect to server, attempt ".concat(_this2.joinAttempts, " of ").concat(_this2.maxJoinAttempts));
              if (_this2.joinAttempts < _this2.maxJoinAttempts) {
                return _this2.join(url, token, opts, abortSignal, useV0Path);
              }
            } else if (e4.reason === ConnectionErrorReason.ServiceNotFound) {
              _this2.log.warn("Initial connection failed: ".concat(e4.message, " – Retrying"));
              if (_this2.pcManager) {
                _this2.pcManager.onStateChange = void 0;
                yield _this2.cleanupPeerConnections();
              }
              return _this2.join(url, token, opts, abortSignal, true);
            }
          }
          throw e4;
        }
      }();
    });
  }
  close() {
    return __awaiter(this, void 0, void 0, function* () {
      const unlock = yield this.closingLock.lock();
      if (this.isClosed) {
        unlock();
        return;
      }
      try {
        this._isClosed = true;
        this.joinAttempts = 0;
        this.emit(EngineEvent.Closing);
        this.removeAllListeners();
        this.deregisterOnLineListener();
        this.clearPendingReconnect();
        this.cleanupLossyDataStats();
        yield this.cleanupPeerConnections();
        yield this.cleanupClient();
      } finally {
        unlock();
      }
    });
  }
  cleanupPeerConnections() {
    return __awaiter(this, void 0, void 0, function* () {
      var _a2;
      this.dataChannels.teardown();
      yield (_a2 = this.pcManager) === null || _a2 === void 0 ? void 0 : _a2.close();
      this.pcManager = void 0;
      this.reliableReceivedState.clear();
    });
  }
  cleanupLossyDataStats() {
    this.lossyChannel.stopThresholdTuning();
  }
  cleanupClient() {
    return __awaiter(this, void 0, void 0, function* () {
      yield this.client.close();
      this.client.resetCallbacks();
      for (const cid of Object.keys(this.pendingTrackResolvers)) {
        this.pendingTrackResolvers[cid].reject();
      }
      this.pendingTrackResolvers = {};
    });
  }
  addTrack(req) {
    if (this.pendingTrackResolvers[req.cid]) {
      throw new TrackInvalidError("a track with the same ID has already been published");
    }
    return new Promise((resolve, reject) => {
      const publicationTimeout = setTimeout(() => {
        delete this.pendingTrackResolvers[req.cid];
        reject(ConnectionError.timeout("publication of local track timed out, no response from server"));
      }, 1e4);
      this.pendingTrackResolvers[req.cid] = {
        resolve: (info) => {
          clearTimeout(publicationTimeout);
          resolve(info);
        },
        reject: () => {
          clearTimeout(publicationTimeout);
          reject(new Error("Cancelled publication by calling unpublish"));
        }
      };
      this.client.sendAddTrack(req);
    });
  }
  /**
   * Removes sender from PeerConnection, returning true if it was removed successfully
   * and a negotiation is necessary
   * @param sender
   * @returns
   */
  removeTrack(sender) {
    if (sender.track && this.pendingTrackResolvers[sender.track.id]) {
      const reject = this.pendingTrackResolvers[sender.track.id].reject;
      if (reject) {
        reject();
      }
      delete this.pendingTrackResolvers[sender.track.id];
    }
    try {
      this.pcManager.removeTrack(sender);
      return true;
    } catch (e4) {
      this.log.warn("failed to remove track", {
        error: e4
      });
    }
    return false;
  }
  updateMuteStatus(trackSid, muted) {
    this.client.sendMuteTrack(trackSid, muted);
  }
  get dataSubscriberReadyState() {
    var _a2;
    return (_a2 = this.dataChannelForKind(DataChannelKind.RELIABLE, true)) === null || _a2 === void 0 ? void 0 : _a2.readyState;
  }
  getConnectedServerAddress() {
    return __awaiter(this, void 0, void 0, function* () {
      var _a2;
      return (_a2 = this.pcManager) === null || _a2 === void 0 ? void 0 : _a2.getConnectedAddress();
    });
  }
  /* @internal */
  setRegionStrategy(strategy) {
    this.regionStrategy = strategy;
  }
  configure(joinResponse, useSinglePeerConnection) {
    return __awaiter(this, void 0, void 0, function* () {
      var _a2;
      if (this.pcManager && this.pcManager.currentState !== PCTransportState.NEW) {
        return;
      }
      if (!joinResponse) {
        const rtcConfig = this.makeRTCConfiguration();
        this.pcManager = new PCTransportManager("publisher-only", this.loggerOptions, rtcConfig);
      } else {
        this.participantSid = (_a2 = joinResponse.participant) === null || _a2 === void 0 ? void 0 : _a2.sid;
        const rtcConfig = this.makeRTCConfiguration(joinResponse);
        this.pcManager = new PCTransportManager(useSinglePeerConnection ? "publisher-only" : joinResponse.subscriberPrimary ? "subscriber-primary" : "publisher-primary", this.loggerOptions, rtcConfig);
      }
      this.emit(EngineEvent.TransportsCreated, this.pcManager.publisher, this.pcManager.subscriber);
      this.pcManager.onIceCandidate = (candidate, target) => {
        this.client.sendIceCandidate(candidate, target);
      };
      this.pcManager.onPublisherOffer = (offer, offerId) => {
        this.client.sendOffer(offer, offerId);
      };
      this.pcManager.onDataChannel = this.handleDataChannel;
      this.pcManager.onStateChange = (connectionState, publisherState, subscriberState) => __awaiter(this, void 0, void 0, function* () {
        this.log.debug("primary PC state changed ".concat(connectionState));
        if (["closed", "disconnected", "failed"].includes(publisherState)) {
          this.publisherConnectionPromise = void 0;
        }
        if (connectionState === PCTransportState.CONNECTED) {
          const shouldEmit = this.pcState === PCState.New;
          this.pcState = PCState.Connected;
          if (shouldEmit) {
            this.emit(EngineEvent.Connected, this.latestJoinResponse);
          }
        } else if (connectionState === PCTransportState.FAILED) {
          if (this.pcState === PCState.Connected || this.pcState === PCState.Reconnecting) {
            this.pcState = PCState.Disconnected;
            this.handleDisconnect("peerconnection failed", subscriberState === "failed" ? ReconnectReason.RR_SUBSCRIBER_FAILED : ReconnectReason.RR_PUBLISHER_FAILED);
          }
        }
        const isSignalSevered = this.client.isDisconnected || this.client.currentState === SignalConnectionState.RECONNECTING;
        const isPCSevered = [PCTransportState.FAILED, PCTransportState.CLOSING, PCTransportState.CLOSED].includes(connectionState);
        if (isSignalSevered && isPCSevered && !this._isClosed) {
          this.emit(EngineEvent.Offline);
        }
      });
      this.pcManager.onTrack = (ev) => {
        if (ev.streams.length === 0) return;
        this.emit(EngineEvent.MediaTrackAdded, ev.track, ev.streams[0], ev.receiver);
      };
    });
  }
  setupSignalClientCallbacks() {
    this.client.onAnswer = (sd, offerId, midToTrackId) => __awaiter(this, void 0, void 0, function* () {
      if (!this.pcManager) {
        return;
      }
      this.log.debug("received server answer", {
        RTCSdpType: sd.type,
        sdp: sd.sdp,
        midToTrackId
      });
      this.midToTrackId = midToTrackId;
      yield this.pcManager.setPublisherAnswer(sd, offerId);
    });
    this.client.onTrickle = (candidate, target) => {
      if (!this.pcManager) {
        return;
      }
      this.log.debug("got ICE candidate from peer", {
        candidate,
        target
      });
      this.pcManager.addIceCandidate(candidate, target);
    };
    this.client.onOffer = (sd, offerId, midToTrackId) => __awaiter(this, void 0, void 0, function* () {
      this.latestRemoteOfferId = offerId;
      if (!this.pcManager) {
        return;
      }
      this.midToTrackId = midToTrackId;
      const answer = yield this.pcManager.createSubscriberAnswerFromOffer(sd, offerId);
      if (answer) {
        this.client.sendAnswer(answer, offerId);
      }
    });
    this.client.onLocalTrackPublished = (res) => {
      var _a2;
      this.log.debug("received trackPublishedResponse", {
        cid: res.cid,
        track: (_a2 = res.track) === null || _a2 === void 0 ? void 0 : _a2.sid
      });
      if (!this.pendingTrackResolvers[res.cid]) {
        this.log.error("missing track resolver for ".concat(res.cid), {
          cid: res.cid
        });
        return;
      }
      const resolve = this.pendingTrackResolvers[res.cid].resolve;
      delete this.pendingTrackResolvers[res.cid];
      resolve(res.track);
    };
    this.client.onLocalTrackUnpublished = (response) => {
      this.emit(EngineEvent.LocalTrackUnpublished, response);
    };
    this.client.onLocalTrackSubscribed = (trackSid) => {
      this.emit(EngineEvent.LocalTrackSubscribed, trackSid);
    };
    this.client.onTokenRefresh = (token) => {
      this.token = token;
      this.emit(EngineEvent.TokenRefreshed, token);
    };
    this.client.onRemoteMuteChanged = (trackSid, muted) => {
      this.emit(EngineEvent.RemoteMute, trackSid, muted);
    };
    this.client.onSubscribedQualityUpdate = (update) => {
      this.emit(EngineEvent.SubscribedQualityUpdate, update);
    };
    this.client.onRoomMoved = (res) => {
      var _a2;
      this.participantSid = (_a2 = res.participant) === null || _a2 === void 0 ? void 0 : _a2.sid;
      if (this.latestJoinResponse) {
        this.latestJoinResponse.room = res.room;
      }
      this.emit(EngineEvent.RoomMoved, res);
    };
    this.client.onMediaSectionsRequirement = (requirement) => {
      this.addMediaSections(requirement.numAudios, requirement.numVideos);
      this.negotiate();
    };
    this.client.onPublishDataTrackResponse = (event) => {
      this.emit(EngineEvent.PublishDataTrackResponse, event);
    };
    this.client.onUnPublishDataTrackResponse = (event) => {
      this.emit(EngineEvent.UnPublishDataTrackResponse, event);
    };
    this.client.onDataTrackSubscriberHandles = (event) => {
      this.emit(EngineEvent.DataTrackSubscriberHandles, event);
    };
    this.client.onClose = () => {
      this.handleDisconnect("signal", ReconnectReason.RR_SIGNAL_DISCONNECTED);
    };
    this.client.onLeave = (leave) => {
      this.log.info("client leave request received (action=".concat(leave === null || leave === void 0 ? void 0 : leave.action, ")"), {
        reason: leave === null || leave === void 0 ? void 0 : leave.reason
      });
      if (leave.regions) {
        this.log.debug("updating regions");
        this.emit(EngineEvent.ServerRegionsReported, leave.regions);
      }
      switch (leave.action) {
        case LeaveRequest_Action.DISCONNECT:
          this.emit(EngineEvent.Disconnected, leave === null || leave === void 0 ? void 0 : leave.reason);
          this.close();
          break;
        case LeaveRequest_Action.RECONNECT:
          this.fullReconnectOnNext = true;
          this.handleDisconnect(leaveReconnect);
          break;
        case LeaveRequest_Action.RESUME:
          this.handleDisconnect(leaveReconnect);
      }
    };
  }
  makeRTCConfiguration(serverResponse) {
    var _a2;
    const rtcConfig = Object.assign({}, this.rtcConfig);
    const needsInsertableStreams = ((_a2 = this.signalOpts) === null || _a2 === void 0 ? void 0 : _a2.e2eeEnabled) || this.frameMetadataWorker && !shouldUseFrameMetadataScriptTransform();
    if (needsInsertableStreams && isInsertableStreamSupported()) {
      this.log.debug("E2EE - setting up transports with insertable streams");
      rtcConfig.encodedInsertableStreams = true;
    }
    rtcConfig.sdpSemantics = "unified-plan";
    rtcConfig.continualGatheringPolicy = "gather_continually";
    if (!serverResponse) {
      return rtcConfig;
    }
    if (serverResponse.iceServers && !rtcConfig.iceServers) {
      const rtcIceServers = [];
      serverResponse.iceServers.forEach((iceServer) => {
        const rtcIceServer = {
          urls: iceServer.urls
        };
        if (iceServer.username) rtcIceServer.username = iceServer.username;
        if (iceServer.credential) {
          rtcIceServer.credential = iceServer.credential;
        }
        rtcIceServers.push(rtcIceServer);
      });
      rtcConfig.iceServers = rtcIceServers;
    }
    if (serverResponse.clientConfiguration && serverResponse.clientConfiguration.forceRelay === ClientConfigSetting.ENABLED) {
      rtcConfig.iceTransportPolicy = "relay";
    }
    return rtcConfig;
  }
  /**
   * Populate the publisher PC so its first offer carries the data channels + recvonly media
   * sections. Required for every V1 connection: Firefox only binds receive decoders for media
   * present in that first offer, and the offer-with-join path needs the sections to
   * build a meaningful initial offer. Must be called on a configured pcManager.
   */
  applyInitialPublisherLayout() {
    this.createDataChannels();
    if (!isReactNative()) {
      this.addMediaSections(initialMediaSectionsAudio, initialMediaSectionsVideo);
    }
  }
  addMediaSections(numAudios, numVideos) {
    var _a2, _b2;
    const transceiverInit = {
      direction: "recvonly"
    };
    for (let i2 = 0; i2 < numAudios; i2++) {
      (_a2 = this.pcManager) === null || _a2 === void 0 ? void 0 : _a2.addPublisherTransceiverOfKind("audio", transceiverInit);
    }
    for (let i2 = 0; i2 < numVideos; i2++) {
      (_b2 = this.pcManager) === null || _b2 === void 0 ? void 0 : _b2.addPublisherTransceiverOfKind("video", transceiverInit);
    }
  }
  createDataChannels() {
    if (!this.pcManager) {
      return;
    }
    this.dataChannels.createPublisherChannels(this.pcManager);
  }
  /** Normalizes an incoming data-channel message into bytes, or logs and returns undefined. */
  decodeDataMessage(message) {
    return __awaiter(this, void 0, void 0, function* () {
      if (message.data instanceof ArrayBuffer) {
        return new Uint8Array(message.data);
      }
      if (message.data instanceof Blob) {
        return new Uint8Array(yield message.data.arrayBuffer());
      }
      this.log.error("unsupported data type", {
        data: message.data
      });
      return void 0;
    });
  }
  createSender(track, opts, encodings) {
    return __awaiter(this, void 0, void 0, function* () {
      let sender;
      if (supportsTransceiver()) {
        sender = yield this.createTransceiverRTCRtpSender(track, opts, encodings);
      } else if (supportsAddTrack()) {
        this.log.warn("using add-track fallback");
        sender = yield this.createRTCRtpSender(track.mediaStreamTrack);
      } else {
        throw new UnexpectedConnectionState("Required webRTC APIs not supported on this device");
      }
      this.setupFrameMetadataSender(sender, opts);
      return sender;
    });
  }
  createSimulcastSender(track, simulcastTrack, opts, encodings) {
    return __awaiter(this, void 0, void 0, function* () {
      let sender;
      if (supportsTransceiver()) {
        sender = yield this.createSimulcastTransceiverSender(track, simulcastTrack, opts, encodings);
      } else if (supportsAddTrack()) {
        this.log.debug("using add-track fallback");
        sender = yield this.createRTCRtpSender(track.mediaStreamTrack);
      } else {
        throw new UnexpectedConnectionState("Cannot stream on this device");
      }
      if (sender) {
        this.setupFrameMetadataSender(sender, opts);
      }
      return sender;
    });
  }
  get frameMetadataWorker() {
    var _a2, _b2;
    return (_b2 = (_a2 = this.options.frameMetadata) !== null && _a2 !== void 0 ? _a2 : this.options.packetTrailer) === null || _b2 === void 0 ? void 0 : _b2.worker;
  }
  setupFrameMetadataSender(sender) {
    let opts = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : {};
    var _a2, _b2, _c;
    const worker = this.frameMetadataWorker;
    if (!worker || ((_a2 = this.signalOpts) === null || _a2 === void 0 ? void 0 : _a2.e2eeEnabled)) {
      return;
    }
    const frameMetadata = (_b2 = opts.frameMetadata) !== null && _b2 !== void 0 ? _b2 : opts.packetTrailer;
    const hasMetadata = hasFrameMetadataPublishOptions(frameMetadata);
    if (shouldUseFrameMetadataScriptTransform()) {
      if (hasMetadata) {
        sender.transform = new RTCRtpScriptTransform(worker, {
          kind: "encode",
          packetTrailer: frameMetadata
        });
      }
      return;
    }
    if (!isFrameMetadataSupported((_c = this.options.frameMetadata) !== null && _c !== void 0 ? _c : this.options.packetTrailer) || !("createEncodedStreams" in sender)) {
      if (hasMetadata) {
        this.log.warn("frame metadata transform not supported; skipping write", this.logContext);
      }
      return;
    }
    const _sender$createEncoded = sender.createEncodedStreams(), readable = _sender$createEncoded.readable, writable = _sender$createEncoded.writable;
    if (hasMetadata) {
      worker.postMessage({
        kind: "encode",
        data: {
          readableStream: readable,
          writableStream: writable,
          packetTrailer: frameMetadata
        }
      }, [readable, writable]);
    } else {
      readable.pipeTo(writable);
    }
  }
  createTransceiverRTCRtpSender(track, opts, encodings) {
    return __awaiter(this, void 0, void 0, function* () {
      if (!this.pcManager) {
        throw new UnexpectedConnectionState("publisher is closed");
      }
      const streams = [];
      if (track.mediaStream) {
        streams.push(track.mediaStream);
      }
      if (isVideoTrack(track)) {
        track.codec = opts.videoCodec;
      }
      const transceiverInit = {
        direction: "sendonly",
        streams
      };
      if (encodings) {
        transceiverInit.sendEncodings = encodings;
      }
      const transceiver = yield this.pcManager.addPublisherTransceiver(track.mediaStreamTrack, transceiverInit);
      return transceiver.sender;
    });
  }
  createSimulcastTransceiverSender(track, simulcastTrack, opts, encodings) {
    return __awaiter(this, void 0, void 0, function* () {
      if (!this.pcManager) {
        throw new UnexpectedConnectionState("publisher is closed");
      }
      const transceiverInit = {
        direction: "sendonly"
      };
      if (encodings) {
        transceiverInit.sendEncodings = encodings;
      }
      const transceiver = yield this.pcManager.addPublisherTransceiver(simulcastTrack.mediaStreamTrack, transceiverInit);
      if (!opts.videoCodec) {
        return;
      }
      track.setSimulcastTrackSender(opts.videoCodec, transceiver.sender);
      return transceiver.sender;
    });
  }
  createRTCRtpSender(track) {
    return __awaiter(this, void 0, void 0, function* () {
      if (!this.pcManager) {
        throw new UnexpectedConnectionState("publisher is closed");
      }
      return this.pcManager.addPublisherTrack(track);
    });
  }
  attemptReconnect(reason) {
    return __awaiter(this, void 0, void 0, function* () {
      var _a2, _b2, _c;
      if (this._isClosed) {
        return;
      }
      if (this.attemptingReconnect) {
        this.log.warn("already attempting reconnect, returning early");
        return;
      }
      if (((_a2 = this.clientConfiguration) === null || _a2 === void 0 ? void 0 : _a2.resumeConnection) === ClientConfigSetting.DISABLED || // signaling state could change to closed due to hardware sleep
      // those connections cannot be resumed
      ((_c = (_b2 = this.pcManager) === null || _b2 === void 0 ? void 0 : _b2.currentState) !== null && _c !== void 0 ? _c : PCTransportState.NEW) === PCTransportState.NEW) {
        this.fullReconnectOnNext = true;
      }
      try {
        this.attemptingReconnect = true;
        if (this.fullReconnectOnNext) {
          yield this.restartConnection();
        } else {
          yield this.resumeConnection(reason);
        }
        this.clearPendingReconnect();
        this.fullReconnectOnNext = false;
      } catch (e4) {
        this.reconnectAttempts += 1;
        let recoverable = true;
        if (e4 instanceof UnexpectedConnectionState) {
          this.log.debug("received unrecoverable error", {
            error: e4
          });
          recoverable = false;
        } else if (!(e4 instanceof SignalReconnectError)) {
          this.fullReconnectOnNext = true;
        }
        if (recoverable) {
          this.handleDisconnect("reconnect", ReconnectReason.RR_UNKNOWN);
        } else {
          this.log.info("could not recover connection after ".concat(this.reconnectAttempts, " attempts, ").concat(Date.now() - this.reconnectStart, "ms. giving up"));
          this.emit(EngineEvent.Disconnected);
          yield this.close();
        }
      } finally {
        this.attemptingReconnect = false;
      }
    });
  }
  getNextRetryDelay(context) {
    try {
      return this.reconnectPolicy.nextRetryDelayInMs(context);
    } catch (e4) {
      this.log.warn("encountered error in reconnect policy", {
        error: e4
      });
    }
    return null;
  }
  restartConnection(regionUrl) {
    return __awaiter(this, void 0, void 0, function* () {
      var _a2, _b2, _c;
      try {
        if (!this.url || !this.token) {
          throw new UnexpectedConnectionState("could not reconnect, url or token not saved");
        }
        this.log.info("reconnecting, attempt: ".concat(this.reconnectAttempts));
        this.emit(EngineEvent.Restarting);
        if (!this.client.isDisconnected) {
          yield this.client.sendLeave();
        }
        yield this.cleanupPeerConnections();
        yield this.cleanupClient();
        let joinResponse;
        try {
          if (!this.signalOpts) {
            this.log.warn("attempted connection restart, without signal options present");
            throw new SignalReconnectError();
          }
          joinResponse = (yield this.join(regionUrl !== null && regionUrl !== void 0 ? regionUrl : this.url, this.token, this.signalOpts, void 0, !this.options.singlePeerConnection)).joinResponse;
        } catch (e4) {
          if (e4 instanceof ConnectionError && e4.reason === ConnectionErrorReason.NotAllowed) {
            throw new UnexpectedConnectionState("could not reconnect, token might be expired");
          }
          throw new SignalReconnectError();
        }
        if (this.shouldFailNext) {
          this.shouldFailNext = false;
          throw new Error("simulated failure");
        }
        this.client.setReconnected();
        this.emit(EngineEvent.SignalRestarted, joinResponse);
        yield this.waitForPCReconnected();
        if (this.client.currentState !== SignalConnectionState.CONNECTED) {
          throw new SignalReconnectError("Signal connection got severed during reconnect");
        }
        (_a2 = this.regionStrategy) === null || _a2 === void 0 ? void 0 : _a2.resetAttempts();
        this.emit(EngineEvent.Restarted);
      } catch (error) {
        const nextRegionUrl = yield (_b2 = this.regionStrategy) === null || _b2 === void 0 ? void 0 : _b2.getNextUrl();
        if (nextRegionUrl) {
          yield this.restartConnection(nextRegionUrl);
          return;
        } else {
          (_c = this.regionStrategy) === null || _c === void 0 ? void 0 : _c.resetAttempts();
          throw error;
        }
      }
    });
  }
  resumeConnection(reason) {
    return __awaiter(this, void 0, void 0, function* () {
      if (!this.url || !this.token) {
        throw new UnexpectedConnectionState("could not reconnect, url or token not saved");
      }
      if (!this.pcManager) {
        throw new UnexpectedConnectionState("publisher and subscriber connections unset");
      }
      this.log.info("resuming signal connection, attempt ".concat(this.reconnectAttempts));
      this.emit(EngineEvent.Resuming);
      let res;
      try {
        this.setupSignalClientCallbacks();
        res = yield this.client.reconnect(this.url, this.token, this.participantSid, reason);
      } catch (error) {
        let message = "";
        if (error instanceof Error) {
          message = error.message;
          this.log.error(error.message, {
            error
          });
        }
        if (error instanceof ConnectionError && error.reason === ConnectionErrorReason.NotAllowed) {
          throw new UnexpectedConnectionState("could not reconnect, token might be expired");
        }
        if (error instanceof ConnectionError && error.reason === ConnectionErrorReason.LeaveRequest) {
          throw error;
        }
        throw new SignalReconnectError(message);
      }
      this.emit(EngineEvent.SignalResumed);
      if (res) {
        const rtcConfig = this.makeRTCConfiguration(res);
        this.pcManager.updateConfiguration(rtcConfig);
        if (this.latestJoinResponse) {
          this.latestJoinResponse.serverInfo = res.serverInfo;
        }
      } else {
        this.log.warn("Did not receive reconnect response");
      }
      if (this.shouldFailNext) {
        this.shouldFailNext = false;
        throw new Error("simulated failure");
      }
      yield this.pcManager.triggerIceRestart();
      yield this.waitForPCReconnected();
      if (this.client.currentState !== SignalConnectionState.CONNECTED) {
        throw new SignalReconnectError("Signal connection got severed during reconnect");
      }
      this.client.setReconnected();
      const reliableDC = this.dataChannelForKind(DataChannelKind.RELIABLE);
      if ((reliableDC === null || reliableDC === void 0 ? void 0 : reliableDC.readyState) === "open" && reliableDC.id === null) {
        this.createDataChannels();
      }
      if (res === null || res === void 0 ? void 0 : res.lastMessageSeq) {
        this.resendReliableMessagesForResume(res.lastMessageSeq).catch((error) => {
          this.log.warn("failed to resend reliable messages after resume", Object.assign(Object.assign({}, this.logContext), {
            error
          }));
        });
      }
      this.emit(EngineEvent.Resumed);
    });
  }
  waitForPCInitialConnection(timeout, abortController) {
    return __awaiter(this, void 0, void 0, function* () {
      if (!this.pcManager) {
        throw new UnexpectedConnectionState("PC manager is closed");
      }
      yield this.pcManager.ensurePCTransportConnection(abortController, timeout);
    });
  }
  waitForPCReconnected() {
    return __awaiter(this, void 0, void 0, function* () {
      this.pcState = PCState.Reconnecting;
      this.log.debug("waiting for peer connection to reconnect");
      try {
        yield sleep(minReconnectWait);
        if (!this.pcManager) {
          throw new UnexpectedConnectionState("PC manager is closed");
        }
        yield this.pcManager.ensurePCTransportConnection(void 0, this.peerConnectionTimeout);
        this.pcState = PCState.Connected;
      } catch (e4) {
        this.pcState = PCState.Disconnected;
        throw ConnectionError.internal("could not establish PC connection, ".concat(e4.message));
      }
    });
  }
  /** @internal */
  publishRpcAck(destinationIdentity, requestId) {
    return __awaiter(this, void 0, void 0, function* () {
      const packet = new DataPacket({
        destinationIdentities: [destinationIdentity],
        kind: DataPacket_Kind.RELIABLE,
        value: {
          case: "rpcAck",
          value: new RpcAck({
            requestId
          })
        }
      });
      yield this.sendDataPacket(packet, DataChannelKind.RELIABLE);
    });
  }
  /* @internal */
  sendDataPacket(packet, kind) {
    return __awaiter(this, void 0, void 0, function* () {
      var _a2, _b2;
      yield this.ensurePublisherConnected(kind);
      if (this.e2eeManager && this.e2eeManager.isDataChannelEncryptionEnabled) {
        const encryptablePacket = asEncryptablePacket(packet);
        if (encryptablePacket) {
          const encryptedData = yield this.e2eeManager.encryptData(encryptablePacket.toBinary());
          packet.value = {
            case: "encryptedPacket",
            value: new EncryptedPacket({
              encryptedValue: encryptedData.payload,
              iv: encryptedData.iv,
              keyIndex: encryptedData.keyIndex
            })
          };
        }
      }
      if (kind === DataChannelKind.RELIABLE) {
        packet.sequence = this.reliableChannel.nextSequence();
      }
      const msg = packet.toBinary();
      const maxPublisherMessageSizeBytes = Math.min((_b2 = (_a2 = this.pcManager) === null || _a2 === void 0 ? void 0 : _a2.getMaxPublisherMessageSize()) !== null && _b2 !== void 0 ? _b2 : DEFAULT_MAX_MESSAGE_SIZE, DEFAULT_MAX_MESSAGE_SIZE);
      if (typeof maxPublisherMessageSizeBytes !== "undefined" && maxPublisherMessageSizeBytes !== 0 && msg.byteLength > maxPublisherMessageSizeBytes) {
        throw new PublishDataError("cannot publish data packet larger than ".concat(maxPublisherMessageSizeBytes, " bytes (got ").concat(msg.byteLength, ")"));
      }
      if (kind === DataChannelKind.RELIABLE) {
        yield this.reliableChannel.send(msg, packet.sequence);
      } else {
        yield this.lossyChannel.send(msg);
      }
    });
  }
  /**
   * Sends pre-serialized bytes on the data-track channel. This is the one send path that doesn't
   * go through {@link sendDataPacket} — Room's `packetAvailable` handler calls it directly with
   * bytes the data-track pipeline already serialized.
   *
   * @internal
   */
  sendDataTrackFrame(bytes) {
    return __awaiter(this, void 0, void 0, function* () {
      yield this.ensurePublisherConnected(DataChannelKind.DATA_TRACK_LOSSY);
      yield this.dataTrackChannel.send(bytes);
    });
  }
  resendReliableMessagesForResume(lastMessageSeq) {
    return __awaiter(this, void 0, void 0, function* () {
      yield this.ensurePublisherConnected(DataChannelKind.RELIABLE);
      yield this.reliableChannel.replay(lastMessageSeq);
    });
  }
  /** The flow-control gate for `kind` — see {@link FlowControlledDataChannel}. */
  flowControlFor(kind) {
    return this.dataChannels.channelFor(kind);
  }
  /**
   * Resolves once the caller may send on the `kind` channel — see
   * {@link FlowControlledDataChannel.waitForHeadroomWithLock}.
   */
  waitForBufferHeadroom(kind) {
    return __awaiter(this, void 0, void 0, function* () {
      return this.flowControlFor(kind).waitForHeadroomWithLock();
    });
  }
  /**
   * @internal
   */
  ensureDataTransportConnected(kind_1) {
    return __awaiter(this, arguments, void 0, function(kind) {
      var _this3 = this;
      let subscriber = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : this.subscriberPrimary;
      return function* () {
        var _a2;
        if (!_this3.pcManager) {
          throw new UnexpectedConnectionState("PC manager is closed");
        }
        const transport = subscriber ? _this3.pcManager.subscriber : _this3.pcManager.publisher;
        const transportName = subscriber ? "Subscriber" : "Publisher";
        if (!transport) {
          throw ConnectionError.internal("".concat(transportName, " connection not set"));
        }
        let needNegotiation = false;
        if (!subscriber && !_this3.dataChannelForKind(kind, subscriber)) {
          _this3.createDataChannels();
          needNegotiation = true;
        }
        if (!needNegotiation && !subscriber && !_this3.pcManager.publisher.isICEConnected && _this3.pcManager.publisher.getICEConnectionState() !== "checking") {
          needNegotiation = true;
        }
        if (needNegotiation) {
          _this3.negotiate().catch((err) => {
            _this3.log.error(err);
          });
        }
        const targetChannel = _this3.dataChannelForKind(kind, subscriber);
        if ((targetChannel === null || targetChannel === void 0 ? void 0 : targetChannel.readyState) === "open") {
          return;
        }
        const endTime = (/* @__PURE__ */ new Date()).getTime() + _this3.peerConnectionTimeout;
        while ((/* @__PURE__ */ new Date()).getTime() < endTime) {
          if (transport.isICEConnected && ((_a2 = _this3.dataChannelForKind(kind, subscriber)) === null || _a2 === void 0 ? void 0 : _a2.readyState) === "open") {
            return;
          }
          yield sleep(50);
        }
        throw ConnectionError.internal("could not establish ".concat(transportName, " connection, state: ").concat(transport.getICEConnectionState()));
      }();
    });
  }
  ensurePublisherConnected(kind) {
    return __awaiter(this, void 0, void 0, function* () {
      if (!this.publisherConnectionPromise) {
        this.publisherConnectionPromise = this.ensureDataTransportConnected(kind, false);
      }
      yield this.publisherConnectionPromise;
    });
  }
  /* @internal */
  verifyTransport() {
    if (!this.pcManager) {
      return false;
    }
    const allowedConnectionStates = [PCTransportState.CONNECTING, PCTransportState.CONNECTED];
    if (!allowedConnectionStates.includes(this.pcManager.currentState)) {
      return false;
    }
    if (!this.client.ws || this.client.ws.readyState === WebSocket.CLOSED) {
      return false;
    }
    return true;
  }
  /** @internal */
  negotiate() {
    return __awaiter(this, void 0, void 0, function* () {
      return new TypedPromise((resolve, reject) => __awaiter(this, void 0, void 0, function* () {
        if (!this.pcManager) {
          reject(new NegotiationError("PC manager is closed"));
          return;
        }
        this.pcManager.requirePublisher();
        if (this.pcManager.publisher.getTransceivers().length == 0 && !this.dataChannels.hasPublisherChannels) {
          this.createDataChannels();
        }
        const abortController = new AbortController();
        const handleClosed = () => {
          abortController.abort();
          this.log.debug("engine disconnected while negotiation was ongoing");
          resolve();
          return;
        };
        if (this.isClosed) {
          reject(new NegotiationError("cannot negotiate on closed engine"));
        }
        this.on(EngineEvent.Closing, handleClosed);
        this.on(EngineEvent.Restarting, handleClosed);
        this.pcManager.publisher.off(PCEvents.RTPVideoPayloadTypes, this.onRtpMapAvailable);
        this.pcManager.publisher.once(PCEvents.RTPVideoPayloadTypes, this.onRtpMapAvailable);
        try {
          yield this.pcManager.negotiate(abortController);
          resolve();
        } catch (e4) {
          if (abortController.signal.aborted) {
            resolve();
            return;
          }
          if (e4 instanceof NegotiationError) {
            this.fullReconnectOnNext = true;
          }
          this.handleDisconnect("negotiation", ReconnectReason.RR_UNKNOWN);
          if (e4 instanceof Error) {
            reject(e4);
          } else {
            reject(new Error(String(e4)));
          }
        } finally {
          this.off(EngineEvent.Closing, handleClosed);
          this.off(EngineEvent.Restarting, handleClosed);
        }
      }));
    });
  }
  dataChannelForKind(kind, sub) {
    return this.dataChannels.getHandle(kind, sub);
  }
  /** @internal */
  sendSyncState(remoteTracks, localTracks, localDataTrackInfos) {
    var _a2, _b2, _c, _d;
    if (!this.pcManager) {
      this.log.warn("sync state cannot be sent without peer connection setup");
      return;
    }
    const previousPublisherOffer = this.pcManager.publisher.getLocalDescription();
    const previousPublisherAnswer = this.pcManager.publisher.getRemoteDescription();
    const previousSubscriberOffer = (_a2 = this.pcManager.subscriber) === null || _a2 === void 0 ? void 0 : _a2.getRemoteDescription();
    const previousSubscriberAnswer = (_b2 = this.pcManager.subscriber) === null || _b2 === void 0 ? void 0 : _b2.getLocalDescription();
    const autoSubscribe = (_d = (_c = this.signalOpts) === null || _c === void 0 ? void 0 : _c.autoSubscribe) !== null && _d !== void 0 ? _d : true;
    const trackSids = new Array();
    const trackSidsDisabled = new Array();
    remoteTracks.forEach((track) => {
      if (track.isDesired !== autoSubscribe) {
        trackSids.push(track.trackSid);
      }
      if (!track.isEnabled) {
        trackSidsDisabled.push(track.trackSid);
      }
    });
    this.client.sendSyncState(new SyncState({
      answer: this.pcManager.mode === "publisher-only" ? previousPublisherAnswer ? toProtoSessionDescription({
        sdp: previousPublisherAnswer.sdp,
        type: previousPublisherAnswer.type
      }) : void 0 : previousSubscriberAnswer ? toProtoSessionDescription({
        sdp: previousSubscriberAnswer.sdp,
        type: previousSubscriberAnswer.type
      }) : void 0,
      offer: this.pcManager.mode === "publisher-only" ? previousPublisherOffer ? toProtoSessionDescription({
        sdp: previousPublisherOffer.sdp,
        type: previousPublisherOffer.type
      }) : void 0 : previousSubscriberOffer ? toProtoSessionDescription({
        sdp: previousSubscriberOffer.sdp,
        type: previousSubscriberOffer.type
      }) : void 0,
      subscription: new UpdateSubscription({
        trackSids,
        subscribe: !autoSubscribe,
        participantTracks: []
      }),
      publishTracks: getTrackPublicationInfo(localTracks),
      dataChannels: this.dataChannelsInfo(),
      trackSidsDisabled,
      datachannelReceiveStates: this.reliableReceivedState.map((seq, sid) => {
        return new DataChannelReceiveState({
          publisherSid: sid,
          lastSeq: seq
        });
      }),
      publishDataTracks: localDataTrackInfos.map((info) => {
        return new PublishDataTrackResponse({
          info: DataTrackInfo.toProtobuf(info)
        });
      })
    }));
  }
  /* @internal */
  failNext() {
    this.shouldFailNext = true;
  }
  /* @internal */
  failNextV1Path() {
    this.shouldFailOnV1Path = true;
  }
  dataChannelsInfo() {
    const infos = [];
    const getInfo = (dc, target) => {
      if ((dc === null || dc === void 0 ? void 0 : dc.id) !== void 0 && dc.id !== null) {
        infos.push(new DataChannelInfo({
          label: dc.label,
          id: dc.id,
          target
        }));
      }
    };
    getInfo(this.dataChannelForKind(DataChannelKind.LOSSY), SignalTarget.PUBLISHER);
    getInfo(this.dataChannelForKind(DataChannelKind.RELIABLE), SignalTarget.PUBLISHER);
    getInfo(this.dataChannelForKind(DataChannelKind.LOSSY, true), SignalTarget.SUBSCRIBER);
    getInfo(this.dataChannelForKind(DataChannelKind.RELIABLE, true), SignalTarget.SUBSCRIBER);
    return infos;
  }
  clearReconnectTimeout() {
    if (this.reconnectTimeout) {
      CriticalTimers.clearTimeout(this.reconnectTimeout);
    }
  }
  clearPendingReconnect() {
    this.clearReconnectTimeout();
    this.reconnectAttempts = 0;
  }
  registerOnLineListener() {
    if (isWeb()) {
      window.addEventListener("online", this.handleBrowserOnLine);
      window.addEventListener("offline", this.handleBrowserOffline);
    }
  }
  deregisterOnLineListener() {
    if (isWeb()) {
      window.removeEventListener("online", this.handleBrowserOnLine);
      window.removeEventListener("offline", this.handleBrowserOffline);
    }
  }
  getTrackIdForReceiver(receiver) {
    var _a2;
    const mid = (_a2 = this.pcManager) === null || _a2 === void 0 ? void 0 : _a2.getMidForReceiver(receiver);
    if (mid) {
      const match = Object.entries(this.midToTrackId).find((_ref4) => {
        let _ref5 = _slicedToArray(_ref4, 1), key = _ref5[0];
        return key === mid;
      });
      if (match) {
        return match[1];
      }
    }
  }
};
function applyUserDataCompat(newObj, oldObj) {
  const participantIdentity = newObj.participantIdentity ? newObj.participantIdentity : oldObj.participantIdentity;
  newObj.participantIdentity = participantIdentity;
  oldObj.participantIdentity = participantIdentity;
  const destinationIdentities = newObj.destinationIdentities.length !== 0 ? newObj.destinationIdentities : oldObj.destinationIdentities;
  newObj.destinationIdentities = destinationIdentities;
  oldObj.destinationIdentities = destinationIdentities;
}
var log$4 = getLogger(LoggerNames.Region);
var DEFAULT_MAX_AGE_MS = 5e3;
var STOP_REFETCH_DELAY_MS = 3e4;
var RegionUrlProvider = class _RegionUrlProvider {
  static fetchRegionSettings(serverUrl, token, signal) {
    return __awaiter(this, void 0, void 0, function* () {
      const unlock = yield _RegionUrlProvider.fetchLock.lock();
      try {
        const regionSettingsResponse = yield fetch("".concat(getCloudConfigUrl(serverUrl), "/regions"), {
          headers: {
            authorization: "Bearer ".concat(token)
          },
          signal
        });
        if (regionSettingsResponse.ok) {
          const maxAge = extractMaxAgeFromRequestHeaders(regionSettingsResponse.headers);
          const maxAgeInMs = maxAge ? maxAge * 1e3 : DEFAULT_MAX_AGE_MS;
          const regionSettings = yield regionSettingsResponse.json();
          return {
            regionSettings,
            updatedAtInMs: Date.now(),
            maxAgeInMs
          };
        } else {
          if (regionSettingsResponse.status === 401) {
            throw ConnectionError.notAllowed("Could not fetch region settings: ".concat(regionSettingsResponse.statusText), regionSettingsResponse.status);
          } else {
            throw ConnectionError.internal("Could not fetch region settings: ".concat(regionSettingsResponse.statusText));
          }
        }
      } catch (e4) {
        if (e4 instanceof ConnectionError) {
          throw e4;
        } else if (signal === null || signal === void 0 ? void 0 : signal.aborted) {
          throw ConnectionError.cancelled("Region fetching was aborted");
        } else {
          throw ConnectionError.serverUnreachable("Could not fetch region settings, ".concat(e4 instanceof Error ? "".concat(e4.name, ": ").concat(e4.message) : e4));
        }
      } finally {
        unlock();
      }
    });
  }
  static scheduleRefetch(url, token, maxAgeInMs) {
    return __awaiter(this, void 0, void 0, function* () {
      const timeout = _RegionUrlProvider.settingsTimeouts.get(url.hostname);
      clearTimeout(timeout);
      _RegionUrlProvider.settingsTimeouts.set(url.hostname, setTimeout(() => __awaiter(this, void 0, void 0, function* () {
        try {
          const newSettings = yield _RegionUrlProvider.fetchRegionSettings(url, token);
          _RegionUrlProvider.updateCachedRegionSettings(url, token, newSettings);
        } catch (error) {
          if (error instanceof ConnectionError && error.reason === ConnectionErrorReason.NotAllowed) {
            log$4.debug("token is not valid, cancelling auto region refresh");
            return;
          }
          log$4.debug("auto refetching of region settings failed", {
            error
          });
          _RegionUrlProvider.scheduleRefetch(url, token, maxAgeInMs);
        }
      }), maxAgeInMs));
    });
  }
  static updateCachedRegionSettings(url, token, settings) {
    _RegionUrlProvider.cache.set(url.hostname, settings);
    _RegionUrlProvider.scheduleRefetch(url, token, settings.maxAgeInMs);
  }
  static stopRefetch(hostname) {
    const timeout = _RegionUrlProvider.settingsTimeouts.get(hostname);
    if (timeout) {
      clearTimeout(timeout);
      _RegionUrlProvider.settingsTimeouts.delete(hostname);
    }
  }
  static scheduleCleanup(hostname) {
    let tracker = _RegionUrlProvider.connectionTrackers.get(hostname);
    if (!tracker) {
      return;
    }
    if (tracker.cleanupTimeout) {
      clearTimeout(tracker.cleanupTimeout);
    }
    tracker.cleanupTimeout = setTimeout(() => {
      const currentTracker = _RegionUrlProvider.connectionTrackers.get(hostname);
      if (currentTracker && currentTracker.connectionCount === 0) {
        log$4.debug("stopping region refetch after disconnect delay", {
          hostname
        });
        _RegionUrlProvider.stopRefetch(hostname);
      }
      if (currentTracker) {
        currentTracker.cleanupTimeout = void 0;
      }
    }, STOP_REFETCH_DELAY_MS);
  }
  static cancelCleanup(hostname) {
    const tracker = _RegionUrlProvider.connectionTrackers.get(hostname);
    if (tracker === null || tracker === void 0 ? void 0 : tracker.cleanupTimeout) {
      clearTimeout(tracker.cleanupTimeout);
      tracker.cleanupTimeout = void 0;
    }
  }
  notifyConnected() {
    const hostname = this.serverUrl.hostname;
    let tracker = _RegionUrlProvider.connectionTrackers.get(hostname);
    if (!tracker) {
      tracker = {
        connectionCount: 0
      };
      _RegionUrlProvider.connectionTrackers.set(hostname, tracker);
    }
    tracker.connectionCount++;
    _RegionUrlProvider.cancelCleanup(hostname);
  }
  notifyDisconnected() {
    const hostname = this.serverUrl.hostname;
    const tracker = _RegionUrlProvider.connectionTrackers.get(hostname);
    if (!tracker) {
      return;
    }
    tracker.connectionCount = Math.max(0, tracker.connectionCount - 1);
    if (tracker.connectionCount === 0) {
      _RegionUrlProvider.scheduleCleanup(hostname);
    }
  }
  constructor(url, token) {
    this.attemptedRegions = [];
    this.serverUrl = new URL(url);
    this.token = token;
  }
  updateToken(token) {
    var _a2;
    this.token = token;
    const url = this.getServerUrl();
    const settings = _RegionUrlProvider.cache.get(url.hostname);
    _RegionUrlProvider.scheduleRefetch(this.serverUrl, this.token, (_a2 = settings === null || settings === void 0 ? void 0 : settings.maxAgeInMs) !== null && _a2 !== void 0 ? _a2 : DEFAULT_MAX_AGE_MS);
  }
  isCloud() {
    return isCloud(this.serverUrl);
  }
  getServerUrl() {
    return this.serverUrl;
  }
  /** @internal */
  fetchRegionSettings(abortSignal) {
    return __awaiter(this, void 0, void 0, function* () {
      return _RegionUrlProvider.fetchRegionSettings(this.serverUrl, this.token, abortSignal);
    });
  }
  getNextBestRegionUrl(abortSignal) {
    return __awaiter(this, void 0, void 0, function* () {
      if (!this.isCloud()) {
        throw Error("region availability is only supported for LiveKit Cloud domains");
      }
      let cachedSettings = _RegionUrlProvider.cache.get(this.serverUrl.hostname);
      if (!cachedSettings || Date.now() - cachedSettings.updatedAtInMs > cachedSettings.maxAgeInMs) {
        cachedSettings = yield this.fetchRegionSettings(abortSignal);
        _RegionUrlProvider.updateCachedRegionSettings(this.serverUrl, this.token, cachedSettings);
      }
      const regionsLeft = cachedSettings.regionSettings.regions.filter((region) => !this.attemptedRegions.find((attempted) => attempted.url === region.url));
      if (regionsLeft.length > 0) {
        const nextRegion = regionsLeft[0];
        this.attemptedRegions.push(nextRegion);
        log$4.info("switching to region: ".concat(nextRegion.region), {
          region: nextRegion.region
        });
        return nextRegion.url;
      } else {
        return null;
      }
    });
  }
  resetAttempts() {
    this.attemptedRegions = [];
  }
  setServerReportedRegions(settings) {
    _RegionUrlProvider.updateCachedRegionSettings(this.serverUrl, this.token, settings);
  }
};
RegionUrlProvider.cache = /* @__PURE__ */ new Map();
RegionUrlProvider.settingsTimeouts = /* @__PURE__ */ new Map();
RegionUrlProvider.connectionTrackers = /* @__PURE__ */ new Map();
RegionUrlProvider.fetchLock = new _();
function getCloudConfigUrl(serverUrl) {
  return "".concat(serverUrl.protocol.replace("ws", "http"), "//").concat(serverUrl.host, "/settings");
}
function deflateRawTransform() {
  return new CompressionStream("deflate-raw");
}
function inflateRawTransform() {
  return new DecompressionStream("deflate-raw");
}
function deflateRawDecompress(data, maxByteLength) {
  return __awaiter(this, void 0, void 0, function* () {
    const ds2 = new DecompressionStream("deflate-raw");
    const writer2 = ds2.writable.getWriter();
    writer2.write(data).catch(() => {
    });
    writer2.close().catch(() => {
    });
    return collect(ds2.readable, maxByteLength);
  });
}
function collect(stream, maxByteLength) {
  return __awaiter(this, void 0, void 0, function* () {
    const reader = stream.getReader();
    const chunks = [];
    let total = 0;
    while (true) {
      const _yield$reader$read = yield reader.read(), done = _yield$reader$read.done, value = _yield$reader$read.value;
      if (done) {
        break;
      }
      chunks.push(value);
      total += value.byteLength;
      if (typeof maxByteLength === "number" && total > maxByteLength) {
        yield reader.cancel();
        throw new DataStreamError("Decompressed payload exceeds the maximum payload size of ".concat(maxByteLength, " bytes"), DataStreamErrorReason.PayloadTooLarge);
      }
    }
    const result = new Uint8Array(total);
    let offset = 0;
    for (const chunk of chunks) {
      result.set(chunk, offset);
      offset += chunk.byteLength;
    }
    return result;
  });
}
var STREAM_CHUNK_SIZE_BYTES = 15e3;
var DEFAULT_MAX_PAYLOAD_BYTE_LENGTH = 5e9;
var BaseStreamReader = class {
  get info() {
    return this._info;
  }
  /** @internal */
  validateBytesReceived() {
    let doneReceiving = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : false;
    if (typeof this.totalByteSize !== "number" || this.totalByteSize === 0) {
      return;
    }
    if (doneReceiving && this.bytesReceived < this.totalByteSize) {
      throw new DataStreamError("Not enough chunk(s) received - expected ".concat(this.totalByteSize, " bytes of data total, only received ").concat(this.bytesReceived, " bytes"), DataStreamErrorReason.Incomplete);
    } else if (this.bytesReceived > this.totalByteSize) {
      throw new DataStreamError("Extra chunk(s) received - expected ".concat(this.totalByteSize, " bytes of data total, received ").concat(this.bytesReceived, " bytes"), DataStreamErrorReason.LengthExceeded);
    }
  }
  constructor(info, stream, totalByteSize) {
    this.reader = stream;
    this.totalByteSize = totalByteSize;
    this._info = info;
    this.bytesReceived = 0;
  }
};
var ByteStreamReader = class extends BaseStreamReader {
  handleChunkReceived(chunk) {
    var _a2;
    this.bytesReceived += chunk.content.byteLength;
    this.validateBytesReceived();
    const currentProgress = this.totalByteSize ? this.bytesReceived / this.totalByteSize : void 0;
    (_a2 = this.onProgress) === null || _a2 === void 0 ? void 0 : _a2.call(this, currentProgress);
  }
  [Symbol.asyncIterator]() {
    const reader = this.reader.getReader();
    reader.closed.catch(() => {
    });
    const cleanup = () => {
      reader.releaseLock();
      this.signal = void 0;
    };
    return {
      next: () => __awaiter(this, void 0, void 0, function* () {
        var _a2;
        try {
          const signal = this.signal;
          if (signal === null || signal === void 0 ? void 0 : signal.aborted) {
            throw signal.reason;
          }
          const result = yield new Promise((resolve, reject) => {
            if (signal) {
              const onAbort = () => reject(signal.reason);
              signal.addEventListener("abort", onAbort, {
                once: true
              });
              reader.read().then(resolve, reject).finally(() => {
                signal.removeEventListener("abort", onAbort);
              });
            } else {
              reader.read().then(resolve, reject);
            }
          });
          if (result.done) {
            this.validateBytesReceived(true);
            if (typeof this.totalByteSize === "number") {
              (_a2 = this.onProgress) === null || _a2 === void 0 ? void 0 : _a2.call(this, 1);
            }
            return {
              done: true,
              value: void 0
            };
          } else {
            this.handleChunkReceived(result.value);
            return {
              done: false,
              value: result.value.content
            };
          }
        } catch (err) {
          cleanup();
          throw err;
        }
      }),
      // note: `return` runs only for premature exits, see:
      // https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Iteration_protocols#errors_during_iteration
      return() {
        return __awaiter(this, void 0, void 0, function* () {
          cleanup();
          return {
            done: true,
            value: void 0
          };
        });
      }
    };
  }
  /**
   * Injects an AbortSignal, which if aborted, will terminate the currently active
   * stream iteration operation.
   *
   * Note that when using AbortSignal.timeout(...), the timeout applies across
   * the whole iteration operation, not just one individual chunk read.
   */
  withAbortSignal(signal) {
    this.signal = signal;
    return this;
  }
  readAll() {
    return __awaiter(this, arguments, void 0, function() {
      var _this = this;
      let opts = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : {};
      return function* () {
        var _a2, e_1, _b2, _c;
        let chunks = /* @__PURE__ */ new Set();
        const iterator = opts.signal ? _this.withAbortSignal(opts.signal) : _this;
        try {
          for (var _d = true, iterator_1 = __asyncValues(iterator), iterator_1_1; iterator_1_1 = yield iterator_1.next(), _a2 = iterator_1_1.done, !_a2; _d = true) {
            _c = iterator_1_1.value;
            _d = false;
            const chunk = _c;
            chunks.add(chunk);
          }
        } catch (e_1_1) {
          e_1 = {
            error: e_1_1
          };
        } finally {
          try {
            if (!_d && !_a2 && (_b2 = iterator_1.return)) yield _b2.call(iterator_1);
          } finally {
            if (e_1) throw e_1.error;
          }
        }
        return Array.from(chunks);
      }();
    });
  }
};
var TextStreamReader = class extends BaseStreamReader {
  /**
   * A TextStreamReader instance can be used as an AsyncIterator that returns the entire string
   * that has been received up to the current point in time.
   */
  constructor(info, stream, totalChunkCount) {
    super(info, stream, totalChunkCount);
    this.receivedChunks = /* @__PURE__ */ new Map();
  }
  handleChunkReceived(chunk) {
    var _a2;
    const index = bigIntToNumber(chunk.chunkIndex);
    const previousChunkAtIndex = this.receivedChunks.get(index);
    if (previousChunkAtIndex && previousChunkAtIndex.version > chunk.version) {
      return;
    }
    this.receivedChunks.set(index, chunk);
    this.bytesReceived += chunk.content.byteLength;
    this.validateBytesReceived();
    const currentProgress = this.totalByteSize ? this.bytesReceived / this.totalByteSize : void 0;
    (_a2 = this.onProgress) === null || _a2 === void 0 ? void 0 : _a2.call(this, currentProgress);
  }
  /**
   * Async iterator implementation to allow usage of `for await...of` syntax.
   * Yields structured chunks from the stream.
   *
   */
  [Symbol.asyncIterator]() {
    const reader = this.reader.getReader();
    reader.closed.catch(() => {
    });
    const decoder2 = new TextDecoder("utf-8", {
      fatal: true
    });
    const signal = this.signal;
    const cleanup = () => {
      reader.releaseLock();
      this.signal = void 0;
    };
    return {
      next: () => __awaiter(this, void 0, void 0, function* () {
        var _a2;
        try {
          if (signal === null || signal === void 0 ? void 0 : signal.aborted) {
            throw signal.reason;
          }
          const result = yield new Promise((resolve, reject) => {
            if (signal) {
              const onAbort = () => reject(signal.reason);
              signal.addEventListener("abort", onAbort, {
                once: true
              });
              reader.read().then(resolve, reject).finally(() => {
                signal.removeEventListener("abort", onAbort);
              });
            } else {
              reader.read().then(resolve, reject);
            }
          });
          if (result.done) {
            this.validateBytesReceived(true);
            if (typeof this.totalByteSize === "number") {
              (_a2 = this.onProgress) === null || _a2 === void 0 ? void 0 : _a2.call(this, 1);
            }
            return {
              done: true,
              value: void 0
            };
          } else {
            this.handleChunkReceived(result.value);
            let decodedResult;
            try {
              decodedResult = decoder2.decode(result.value.content);
            } catch (err) {
              throw new DataStreamError("Cannot decode datastream chunk ".concat(result.value.chunkIndex, " as text: ").concat(err), DataStreamErrorReason.DecodeFailed);
            }
            return {
              done: false,
              value: decodedResult
            };
          }
        } catch (err) {
          cleanup();
          throw err;
        }
      }),
      // note: `return` runs only for premature exits, see:
      // https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Iteration_protocols#errors_during_iteration
      return() {
        return __awaiter(this, void 0, void 0, function* () {
          cleanup();
          return {
            done: true,
            value: void 0
          };
        });
      }
    };
  }
  /**
   * Injects an AbortSignal, which if aborted, will terminate the currently active
   * stream iteration operation.
   *
   * Note that when using AbortSignal.timeout(...), the timeout applies across
   * the whole iteration operation, not just one individual chunk read.
   */
  withAbortSignal(signal) {
    this.signal = signal;
    return this;
  }
  readAll() {
    return __awaiter(this, arguments, void 0, function() {
      var _this2 = this;
      let opts = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : {};
      return function* () {
        var _a2, e_2, _b2, _c;
        let finalString = "";
        const iterator = opts.signal ? _this2.withAbortSignal(opts.signal) : _this2;
        try {
          for (var _d = true, iterator_2 = __asyncValues(iterator), iterator_2_1; iterator_2_1 = yield iterator_2.next(), _a2 = iterator_2_1.done, !_a2; _d = true) {
            _c = iterator_2_1.value;
            _d = false;
            const chunk = _c;
            finalString += chunk;
          }
        } catch (e_2_1) {
          e_2 = {
            error: e_2_1
          };
        } finally {
          try {
            if (!_d && !_a2 && (_b2 = iterator_2.return)) yield _b2.call(iterator_2);
          } finally {
            if (e_2) throw e_2.error;
          }
        }
        return finalString;
      }();
    });
  }
};
var IncomingDataStreamManager = class {
  constructor() {
    let maxPayloadByteLength = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : DEFAULT_MAX_PAYLOAD_BYTE_LENGTH;
    this.log = livekitLogger;
    this.byteStreamControllers = /* @__PURE__ */ new Map();
    this.textStreamControllers = /* @__PURE__ */ new Map();
    this.byteStreamHandlers = /* @__PURE__ */ new Map();
    this.textStreamHandlers = /* @__PURE__ */ new Map();
    this.isConnected = false;
    this.bufferedPackets = [];
    this.maxPayloadByteLength = maxPayloadByteLength;
  }
  setConnected(connected) {
    this.isConnected = connected;
    if (connected) {
      this.flushBufferedPackets();
    }
  }
  flushBufferedPackets() {
    const packets = this.bufferedPackets;
    this.bufferedPackets = [];
    for (const _ref of packets) {
      const packet = _ref.packet;
      const encryptionType = _ref.encryptionType;
      this.handleDataStreamPacket(packet, encryptionType);
    }
  }
  registerTextStreamHandler(topic, callback) {
    if (this.textStreamHandlers.has(topic)) {
      throw new DataStreamError('A text stream handler for topic "'.concat(topic, '" has already been set.'), DataStreamErrorReason.HandlerAlreadyRegistered);
    }
    this.textStreamHandlers.set(topic, callback);
  }
  unregisterTextStreamHandler(topic) {
    this.textStreamHandlers.delete(topic);
  }
  registerByteStreamHandler(topic, callback) {
    if (this.byteStreamHandlers.has(topic)) {
      throw new DataStreamError('A byte stream handler for topic "'.concat(topic, '" has already been set.'), DataStreamErrorReason.HandlerAlreadyRegistered);
    }
    this.byteStreamHandlers.set(topic, callback);
  }
  unregisterByteStreamHandler(topic) {
    this.byteStreamHandlers.delete(topic);
  }
  clearControllers() {
    this.byteStreamControllers.clear();
    this.textStreamControllers.clear();
    this.bufferedPackets = [];
  }
  validateParticipantHasNoActiveDataStreams(participantIdentity) {
    const textStreamsBeingSentByDisconnectingParticipant = Array.from(this.textStreamControllers.entries()).filter((entry) => entry[1].sendingParticipantIdentity === participantIdentity);
    const byteStreamsBeingSentByDisconnectingParticipant = Array.from(this.byteStreamControllers.entries()).filter((entry) => entry[1].sendingParticipantIdentity === participantIdentity);
    if (textStreamsBeingSentByDisconnectingParticipant.length > 0 || byteStreamsBeingSentByDisconnectingParticipant.length > 0) {
      const abnormalEndError = new DataStreamError("Participant ".concat(participantIdentity, " unexpectedly disconnected in the middle of sending data"), DataStreamErrorReason.AbnormalEnd);
      for (const _ref2 of byteStreamsBeingSentByDisconnectingParticipant) {
        var _ref3 = _slicedToArray(_ref2, 2);
        const id = _ref3[0];
        const controller = _ref3[1];
        controller.controller.error(abnormalEndError);
        this.byteStreamControllers.delete(id);
      }
      for (const _ref4 of textStreamsBeingSentByDisconnectingParticipant) {
        var _ref5 = _slicedToArray(_ref4, 2);
        const id = _ref5[0];
        const controller = _ref5[1];
        controller.controller.error(abnormalEndError);
        this.textStreamControllers.delete(id);
      }
    }
  }
  handleDataStreamPacket(packet, encryptionType) {
    if (!this.isConnected) {
      this.bufferedPackets.push({
        packet,
        encryptionType
      });
      return;
    }
    switch (packet.value.case) {
      case "streamHeader":
        return this.handleStreamHeader(packet.value.value, packet.participantIdentity, encryptionType);
      case "streamChunk":
        return this.handleStreamChunk(packet.value.value, encryptionType);
      case "streamTrailer":
        return this.handleStreamTrailer(packet.value.value, encryptionType);
      default:
        throw new Error('DataPacket of value "'.concat(packet.value.case, '" is not data stream related!'));
    }
  }
  handleStreamHeader(streamHeader, participantIdentity, encryptionType) {
    var _a2;
    switch (streamHeader.contentHeader.case) {
      case "byteHeader": {
        const streamHandlerCallback = this.byteStreamHandlers.get(streamHeader.topic);
        if (!streamHandlerCallback) {
          this.log.debug("ignoring incoming byte stream due to no handler for topic", streamHeader.topic);
          return;
        }
        let streamController;
        const info = {
          id: streamHeader.streamId,
          name: (_a2 = streamHeader.contentHeader.value.name) !== null && _a2 !== void 0 ? _a2 : "unknown",
          mimeType: streamHeader.mimeType,
          size: streamHeader.totalLength ? Number(streamHeader.totalLength) : void 0,
          topic: streamHeader.topic,
          timestamp: bigIntToNumber(streamHeader.timestamp),
          attributes: streamHeader.attributes,
          encryptionType
        };
        let compressed;
        switch (streamHeader.compression) {
          case DataStream_CompressionType.DEFLATE_RAW:
            if (!isCompressionStreamSupported()) {
              livekitLogger.warn("Data stream ".concat(streamHeader.streamId, " received with deflate-raw compression, but this browser does not have support for DecompressionStream. Dropping..."));
              return;
            }
            compressed = true;
            break;
          case DataStream_CompressionType.NONE:
            compressed = false;
            break;
          default:
            livekitLogger.warn("Data stream ".concat(streamHeader.streamId, " received with unknown compression type ").concat(streamHeader.compression, ", dropping..."));
            return;
        }
        const inlineContent = streamHeader.inlineContent;
        if (typeof inlineContent !== "undefined") {
          streamHandlerCallback(new ByteStreamReader(info, createInlineStream(streamHeader.streamId, compressed ? deflateRawDecompress(inlineContent, this.maxPayloadByteLength) : inlineContent), bigIntToNumber(streamHeader.totalLength)), {
            identity: participantIdentity
          });
          return;
        }
        const stream = new ReadableStream({
          start: (controller) => {
            streamController = controller;
            if (this.byteStreamControllers.has(streamHeader.streamId)) {
              throw new DataStreamError("A data stream read is already in progress for a stream with id ".concat(streamHeader.streamId, "."), DataStreamErrorReason.AlreadyOpened);
            }
            this.byteStreamControllers.set(streamHeader.streamId, {
              info,
              controller: streamController,
              startTime: Date.now(),
              sendingParticipantIdentity: participantIdentity
            });
          }
        });
        streamHandlerCallback(new ByteStreamReader(
          info,
          compressed ? inflateRawByteChunkStream(stream, streamHeader.streamId, this.maxPayloadByteLength) : stream,
          // `totalLength` is the pre-compression size, and the reader counts decompressed bytes,
          // so it applies to both paths (mirrors text).
          bigIntToNumber(streamHeader.totalLength)
        ), {
          identity: participantIdentity
        });
        return;
      }
      case "textHeader": {
        const streamHandlerCallback = this.textStreamHandlers.get(streamHeader.topic);
        if (!streamHandlerCallback) {
          this.log.debug("ignoring incoming text stream due to no handler for topic", streamHeader.topic);
          return;
        }
        let streamController;
        const info = {
          id: streamHeader.streamId,
          mimeType: streamHeader.mimeType,
          size: streamHeader.totalLength ? Number(streamHeader.totalLength) : void 0,
          topic: streamHeader.topic,
          timestamp: Number(streamHeader.timestamp),
          attributes: streamHeader.attributes,
          encryptionType,
          attachedStreamIds: streamHeader.contentHeader.value.attachedStreamIds
        };
        let compressed;
        switch (streamHeader.compression) {
          case DataStream_CompressionType.DEFLATE_RAW:
            if (!isCompressionStreamSupported()) {
              livekitLogger.warn("Data stream ".concat(streamHeader.streamId, " received with deflate-raw compression, but this browser does not have support for DecompressionStream. Dropping..."));
              return;
            }
            compressed = true;
            break;
          case DataStream_CompressionType.NONE:
            compressed = false;
            break;
          default:
            livekitLogger.warn("Data stream ".concat(streamHeader.streamId, " received with unknown compression type ").concat(streamHeader.compression, ", dropping..."));
            return;
        }
        const inlineContent = streamHeader.inlineContent;
        if (typeof inlineContent !== "undefined") {
          const content = compressed ? deflateRawDecompress(inlineContent, this.maxPayloadByteLength) : inlineContent;
          streamHandlerCallback(new TextStreamReader(info, createInlineStream(streamHeader.streamId, content), bigIntToNumber(streamHeader.totalLength)), {
            identity: participantIdentity
          });
          return;
        }
        const stream = new ReadableStream({
          start: (controller) => {
            streamController = controller;
            if (this.textStreamControllers.has(streamHeader.streamId)) {
              throw new DataStreamError("A data stream read is already in progress for a stream with id ".concat(streamHeader.streamId, "."), DataStreamErrorReason.AlreadyOpened);
            }
            this.textStreamControllers.set(streamHeader.streamId, {
              info,
              controller: streamController,
              startTime: Date.now(),
              sendingParticipantIdentity: participantIdentity
            });
          }
        });
        streamHandlerCallback(new TextStreamReader(
          info,
          compressed ? inflateRawChunkStream(stream, streamHeader.streamId, this.maxPayloadByteLength) : stream,
          // `totalLength` is the pre-compression size, and the reader sees decompressed bytes, so
          // it applies to both paths.
          bigIntToNumber(streamHeader.totalLength)
        ), {
          identity: participantIdentity
        });
        return;
      }
    }
  }
  handleStreamChunk(chunk, encryptionType) {
    const fileBuffer = this.byteStreamControllers.get(chunk.streamId);
    if (fileBuffer) {
      if (fileBuffer.info.encryptionType !== encryptionType) {
        fileBuffer.controller.error(new DataStreamError("Encryption type mismatch for stream ".concat(chunk.streamId, ". Expected ").concat(encryptionType, ", got ").concat(fileBuffer.info.encryptionType), DataStreamErrorReason.EncryptionTypeMismatch));
        this.byteStreamControllers.delete(chunk.streamId);
      } else if (chunk.content.length > 0) {
        fileBuffer.controller.enqueue(chunk);
      }
    }
    const textBuffer = this.textStreamControllers.get(chunk.streamId);
    if (textBuffer) {
      if (textBuffer.info.encryptionType !== encryptionType) {
        textBuffer.controller.error(new DataStreamError("Encryption type mismatch for stream ".concat(chunk.streamId, ". Expected ").concat(encryptionType, ", got ").concat(textBuffer.info.encryptionType), DataStreamErrorReason.EncryptionTypeMismatch));
        this.textStreamControllers.delete(chunk.streamId);
      } else if (chunk.content.length > 0) {
        textBuffer.controller.enqueue(chunk);
      }
    }
  }
  handleStreamTrailer(trailer, encryptionType) {
    const textBuffer = this.textStreamControllers.get(trailer.streamId);
    if (textBuffer) {
      if (textBuffer.info.encryptionType !== encryptionType) {
        textBuffer.controller.error(new DataStreamError("Encryption type mismatch for stream ".concat(trailer.streamId, ". Expected ").concat(encryptionType, ", got ").concat(textBuffer.info.encryptionType), DataStreamErrorReason.EncryptionTypeMismatch));
      } else {
        textBuffer.info.attributes = Object.assign(Object.assign({}, textBuffer.info.attributes), trailer.attributes);
        if (trailer.reason) {
          textBuffer.controller.error(new DataStreamError("Data stream ".concat(trailer.streamId, " closed abnormally: ").concat(trailer.reason), DataStreamErrorReason.AbnormalEnd));
        } else {
          textBuffer.controller.close();
        }
      }
      this.textStreamControllers.delete(trailer.streamId);
    }
    const fileBuffer = this.byteStreamControllers.get(trailer.streamId);
    if (fileBuffer) {
      if (fileBuffer.info.encryptionType !== encryptionType) {
        fileBuffer.controller.error(new DataStreamError("Encryption type mismatch for stream ".concat(trailer.streamId, ". Expected ").concat(encryptionType, ", got ").concat(fileBuffer.info.encryptionType), DataStreamErrorReason.EncryptionTypeMismatch));
      } else {
        fileBuffer.info.attributes = Object.assign(Object.assign({}, fileBuffer.info.attributes), trailer.attributes);
        if (trailer.reason) {
          fileBuffer.controller.error(new DataStreamError("Data stream ".concat(trailer.streamId, " closed abnormally: ").concat(trailer.reason), DataStreamErrorReason.AbnormalEnd));
        } else {
          fileBuffer.controller.close();
        }
      }
      this.byteStreamControllers.delete(trailer.streamId);
    }
  }
};
function createInlineStream(streamId, content) {
  return new ReadableStream({
    start: (controller) => __awaiter(this, void 0, void 0, function* () {
      try {
        const bytes = yield content;
        controller.enqueue(new DataStream_Chunk({
          streamId,
          chunkIndex: BigInt(0),
          content: bytes
        }));
        controller.close();
      } catch (err) {
        controller.error(err);
      }
    })
  });
}
function ensureOrderedChunks(streamId) {
  let lastChunkIndex = -1;
  return new TransformStream({
    transform: (value, controller) => {
      const index = bigIntToNumber(value.chunkIndex);
      if (index <= lastChunkIndex) {
        livekitLogger.warn("ignoring duplicate chunk ".concat(index, " for compressed data stream ").concat(streamId, " (last processed: ").concat(lastChunkIndex, ")"));
        return;
      }
      if (index > lastChunkIndex + 1) {
        throw new DataStreamError("Missing chunk(s) ".concat(lastChunkIndex + 1, "..").concat(index - 1, " for compressed data stream ").concat(streamId, " - cannot continue decompressing"), DataStreamErrorReason.Incomplete);
      }
      lastChunkIndex = index;
      controller.enqueue(value);
    }
  });
}
function chunksToBytes() {
  return new TransformStream({
    transform: (value, controller) => {
      controller.enqueue(value.content);
    }
  });
}
function bytesToChunks(streamId) {
  let outIndex = 0;
  return new TransformStream({
    transform: (value, controller) => {
      if (value.byteLength > 0) {
        controller.enqueue(new DataStream_Chunk({
          streamId,
          chunkIndex: numberToBigInt(outIndex),
          content: value
        }));
        outIndex += 1;
      }
    }
  });
}
function bytesToDecodedUtf8(streamId) {
  const decoder2 = new TextDecoder("utf-8", {
    fatal: true
  });
  const encoder = new TextEncoder();
  let outIndex = 0;
  const decodeOrThrow = (bytes) => {
    try {
      return bytes ? decoder2.decode(bytes, {
        stream: true
      }) : decoder2.decode();
    } catch (err) {
      throw new DataStreamError("Cannot decode compressed data stream ".concat(streamId, " as text: ").concat(err), DataStreamErrorReason.DecodeFailed);
    }
  };
  return new TransformStream({
    transform: (value, controller) => {
      const text = decodeOrThrow(value);
      if (text.length > 0) {
        controller.enqueue(new DataStream_Chunk({
          streamId,
          chunkIndex: numberToBigInt(outIndex),
          content: encoder.encode(text)
        }));
        outIndex += 1;
      }
    },
    flush: (controller) => {
      const tail = decodeOrThrow();
      if (tail.length > 0) {
        controller.enqueue(new DataStream_Chunk({
          streamId,
          chunkIndex: numberToBigInt(outIndex),
          content: encoder.encode(tail)
        }));
        outIndex += 1;
      }
    }
  });
}
function inflateRawByteChunkStream(raw2, streamId, maxPayloadByteLength) {
  return raw2.pipeThrough(ensureOrderedChunks(streamId)).pipeThrough(chunksToBytes()).pipeThrough(inflateRawTransform()).pipeThrough(maxDecompressedLengthGuard(streamId, maxPayloadByteLength)).pipeThrough(bytesToChunks(streamId));
}
function inflateRawChunkStream(raw2, streamId, maxPayloadByteLength) {
  return raw2.pipeThrough(ensureOrderedChunks(streamId)).pipeThrough(chunksToBytes()).pipeThrough(inflateRawTransform()).pipeThrough(maxDecompressedLengthGuard(streamId, maxPayloadByteLength)).pipeThrough(bytesToDecodedUtf8(streamId));
}
function maxDecompressedLengthGuard(streamId, maxByteLength) {
  let total = 0;
  return new TransformStream({
    transform: (value, controller) => {
      total += value.byteLength;
      if (total > maxByteLength) {
        throw new DataStreamError("Data stream ".concat(streamId, " exceeds the maximum payload size of ").concat(maxByteLength, " bytes"), DataStreamErrorReason.PayloadTooLarge);
      }
      controller.enqueue(value);
    }
  });
}
var BaseStreamWriter = class {
  constructor(writableStream, info, onClose) {
    this.writableStream = writableStream;
    this.defaultWriter = writableStream.getWriter();
    this.onClose = onClose;
    this.info = info;
  }
  write(chunk) {
    return this.defaultWriter.write(chunk);
  }
  close() {
    return __awaiter(this, void 0, void 0, function* () {
      var _a2;
      yield this.defaultWriter.close();
      this.defaultWriter.releaseLock();
      (_a2 = this.onClose) === null || _a2 === void 0 ? void 0 : _a2.call(this);
    });
  }
};
var TextStreamWriter = class extends BaseStreamWriter {
};
var ByteStreamWriter = class extends BaseStreamWriter {
};
function buildTextStreamHeader(info, options, v2) {
  var _a2;
  return new DataStream_Header({
    streamId: info.id,
    mimeType: info.mimeType,
    topic: info.topic,
    timestamp: numberToBigInt(info.timestamp),
    totalLength: numberToBigInt(info.size),
    attributes: info.attributes,
    compression: (_a2 = v2 === null || v2 === void 0 ? void 0 : v2.compression) !== null && _a2 !== void 0 ? _a2 : DataStream_CompressionType.NONE,
    inlineContent: v2 === null || v2 === void 0 ? void 0 : v2.inlineContent,
    contentHeader: {
      case: "textHeader",
      value: new DataStream_TextHeader({
        version: options === null || options === void 0 ? void 0 : options.version,
        attachedStreamIds: info.attachedStreamIds,
        replyToStreamId: options === null || options === void 0 ? void 0 : options.replyToStreamId,
        operationType: (options === null || options === void 0 ? void 0 : options.type) === "update" ? DataStream_OperationType.UPDATE : DataStream_OperationType.CREATE
      })
    }
  });
}
function buildByteStreamHeader(info, v2) {
  var _a2;
  return new DataStream_Header({
    streamId: info.id,
    mimeType: info.mimeType,
    topic: info.topic,
    timestamp: numberToBigInt(info.timestamp),
    totalLength: numberToBigInt(info.size),
    attributes: info.attributes,
    compression: (_a2 = v2 === null || v2 === void 0 ? void 0 : v2.compression) !== null && _a2 !== void 0 ? _a2 : DataStream_CompressionType.NONE,
    inlineContent: v2 === null || v2 === void 0 ? void 0 : v2.inlineContent,
    contentHeader: {
      case: "byteHeader",
      value: new DataStream_ByteHeader({
        name: info.name
      })
    }
  });
}
function createStreamHeaderPacket(header, destinationIdentities) {
  return new DataPacket({
    destinationIdentities,
    value: {
      case: "streamHeader",
      value: header
    }
  });
}
var textEncoder = new TextEncoder();
var OutgoingDataStreamManager = class {
  constructor(engine, log2, getRemoteParticipantClientProtocol, getRemoteParticipantCapabilities, getAllRemoteParticipantIdentities) {
    this.engine = engine;
    this.log = log2;
    this.getRemoteParticipantClientProtocol = getRemoteParticipantClientProtocol;
    this.getRemoteParticipantCapabilities = getRemoteParticipantCapabilities;
    this.getAllRemoteParticipantIdentities = getAllRemoteParticipantIdentities;
  }
  setupEngine(engine) {
    this.engine = engine;
  }
  /** {@inheritDoc LocalParticipant.sendText} */
  sendText(text, options) {
    return __awaiter(this, void 0, void 0, function* () {
      var _a2, _b2, _c, _d, _e2;
      const streamId = crypto.randomUUID();
      const textInBytes = textEncoder.encode(text);
      const totalTextLength = textInBytes.byteLength;
      const compress = (_a2 = options === null || options === void 0 ? void 0 : options.compress) !== null && _a2 !== void 0 ? _a2 : true;
      let info = {
        id: streamId,
        mimeType: "text/plain",
        timestamp: Date.now(),
        topic: (_b2 = options === null || options === void 0 ? void 0 : options.topic) !== null && _b2 !== void 0 ? _b2 : "",
        size: totalTextLength,
        // NOTE: size is always the pre-compression byte length
        attributes: options === null || options === void 0 ? void 0 : options.attributes,
        encryptionType: ((_c = this.engine.e2eeManager) === null || _c === void 0 ? void 0 : _c.isDataChannelEncryptionEnabled) ? Encryption_Type.GCM : Encryption_Type.NONE
      };
      const compressEligible = compress && isCompressionStreamSupported() && this.allRecipientsSupportV2(options === null || options === void 0 ? void 0 : options.destinationIdentities) && this.allRecipientsSupportCompression(options === null || options === void 0 ? void 0 : options.destinationIdentities);
      let compressedStream = compressEligible ? MaybeCollectedStream.fromStream(readableFromBytes(textInBytes).pipeThrough(deflateRawTransform())) : null;
      const noAttachments = !(options === null || options === void 0 ? void 0 : options.attachments) || options.attachments.length === 0;
      if (noAttachments && this.allRecipientsSupportV2(options === null || options === void 0 ? void 0 : options.destinationIdentities)) {
        let inlineContent = textInBytes;
        let compression = DataStream_CompressionType.NONE;
        if (compressedStream) {
          const collectedBytes = yield compressedStream.collect();
          if (collectedBytes.byteLength < textInBytes.byteLength) {
            inlineContent = collectedBytes;
            compression = DataStream_CompressionType.DEFLATE_RAW;
          }
        }
        const header = buildTextStreamHeader(info, void 0, {
          compression,
          inlineContent
        });
        const packet = createStreamHeaderPacket(header, options === null || options === void 0 ? void 0 : options.destinationIdentities);
        if (packet.toBinary().byteLength <= STREAM_CHUNK_SIZE_BYTES) {
          yield this.engine.sendDataPacket(packet, DataChannelKind.RELIABLE);
          (_d = options === null || options === void 0 ? void 0 : options.onProgress) === null || _d === void 0 ? void 0 : _d.call(options, 1);
          return info;
        }
      }
      const fileIds = (_e2 = options === null || options === void 0 ? void 0 : options.attachments) === null || _e2 === void 0 ? void 0 : _e2.map(() => crypto.randomUUID());
      const parts = fileIds ? fileIds.length + 1 : 1;
      const progresses = new Array(parts).fill(0);
      const handleProgress = (progress, idx) => {
        var _a3;
        progresses[idx] = progress;
        (_a3 = options === null || options === void 0 ? void 0 : options.onProgress) === null || _a3 === void 0 ? void 0 : _a3.call(options, progresses.reduce((acc, val) => acc + val, 0) / parts);
      };
      if (compressedStream) {
        info.attachedStreamIds = fileIds;
        const header = buildTextStreamHeader(info, void 0, {
          compression: DataStream_CompressionType.DEFLATE_RAW
        });
        const packet = createStreamHeaderPacket(header, options === null || options === void 0 ? void 0 : options.destinationIdentities);
        yield this.sendChunkedByteStream(packet, streamId, options === null || options === void 0 ? void 0 : options.destinationIdentities, compressedStream.stream().pipeThrough(progressReportingStream(textInBytes.length, (progress) => handleProgress(progress, 0))));
        if (textInBytes.length === 0) {
          handleProgress(1, 0);
        }
      } else {
        const writer2 = yield this.streamText({
          streamId,
          totalSize: totalTextLength,
          destinationIdentities: options === null || options === void 0 ? void 0 : options.destinationIdentities,
          topic: options === null || options === void 0 ? void 0 : options.topic,
          attachedStreamIds: fileIds,
          attributes: options === null || options === void 0 ? void 0 : options.attributes
        });
        yield writer2.write(text);
        handleProgress(1, 0);
        yield writer2.close();
        info = writer2.info;
      }
      if ((options === null || options === void 0 ? void 0 : options.attachments) && fileIds) {
        yield Promise.all(options.attachments.map((file, idx) => __awaiter(this, void 0, void 0, function* () {
          return this._sendFile(fileIds[idx], file, {
            topic: options.topic,
            mimeType: file.type,
            destinationIdentities: options.destinationIdentities,
            compress: options.compress,
            onProgress: (progress) => {
              handleProgress(progress, idx + 1);
            }
          });
        })));
      }
      return info;
    });
  }
  /**
   * Sends a complete in-memory byte payload. Mirrors {@link sendText}'s semantics: when every
   * recipient supports data streams v2 the payload rides inline in a single header packet
   * (optionally deflate-raw compressed), otherwise it is sent as a (optionally compressed)
   * chunked byte stream. Unlike {@link sendFile}, the whole payload is already in memory, so the
   * inline single-packet fast path applies.
   */
  sendBytes(bytes, options) {
    return __awaiter(this, void 0, void 0, function* () {
      var _a2, _b2, _c, _d, _e2, _f, _g;
      const streamId = crypto.randomUUID();
      const destinationIdentities = options === null || options === void 0 ? void 0 : options.destinationIdentities;
      const compress = (_a2 = options === null || options === void 0 ? void 0 : options.compress) !== null && _a2 !== void 0 ? _a2 : true;
      const info = {
        id: streamId,
        name: (_b2 = options === null || options === void 0 ? void 0 : options.name) !== null && _b2 !== void 0 ? _b2 : "unknown",
        mimeType: (_c = options === null || options === void 0 ? void 0 : options.mimeType) !== null && _c !== void 0 ? _c : "application/octet-stream",
        timestamp: Date.now(),
        topic: (_d = options === null || options === void 0 ? void 0 : options.topic) !== null && _d !== void 0 ? _d : "",
        size: bytes.byteLength,
        // NOTE: size is always the pre-compression byte length
        attributes: options === null || options === void 0 ? void 0 : options.attributes,
        encryptionType: ((_e2 = this.engine.e2eeManager) === null || _e2 === void 0 ? void 0 : _e2.isDataChannelEncryptionEnabled) ? Encryption_Type.GCM : Encryption_Type.NONE
      };
      const progressMonitorTap = progressReportingStream(bytes.length, options === null || options === void 0 ? void 0 : options.onProgress);
      const compressEligible = compress && isCompressionStreamSupported() && this.allRecipientsSupportV2(destinationIdentities) && this.allRecipientsSupportCompression(destinationIdentities);
      let compressedStream = compressEligible ? MaybeCollectedStream.fromStream(readableFromBytes(bytes).pipeThrough(progressMonitorTap).pipeThrough(deflateRawTransform())) : null;
      if (this.allRecipientsSupportV2(destinationIdentities)) {
        let inlineContent = bytes;
        let compression = DataStream_CompressionType.NONE;
        if (compressedStream) {
          const collectedBytes = yield compressedStream.collect();
          if (collectedBytes.byteLength < bytes.byteLength) {
            inlineContent = collectedBytes;
            compression = DataStream_CompressionType.DEFLATE_RAW;
          }
        }
        const header2 = buildByteStreamHeader(info, {
          compression,
          inlineContent
        });
        const packet2 = createStreamHeaderPacket(header2, destinationIdentities);
        if (packet2.toBinary().byteLength <= STREAM_CHUNK_SIZE_BYTES) {
          yield this.engine.sendDataPacket(packet2, DataChannelKind.RELIABLE);
          (_f = options === null || options === void 0 ? void 0 : options.onProgress) === null || _f === void 0 ? void 0 : _f.call(options, 1);
          return info;
        }
      }
      const header = buildByteStreamHeader(info, {
        compression: compressedStream ? DataStream_CompressionType.DEFLATE_RAW : DataStream_CompressionType.NONE
      });
      const packet = createStreamHeaderPacket(header, destinationIdentities);
      const source = compressedStream ? compressedStream.stream() : readableFromBytes(bytes).pipeThrough(progressMonitorTap);
      yield this.sendChunkedByteStream(packet, streamId, destinationIdentities, source);
      if (bytes.length === 0) {
        (_g = options === null || options === void 0 ? void 0 : options.onProgress) === null || _g === void 0 ? void 0 : _g.call(options, 1);
      }
      return info;
    });
  }
  /**
   * Returns true only if every recipient is known to support data streams v2 (single-packet inline
   * streams and compression). For a targeted send this checks the named destination identities; for
   * a broadcast (no explicit destinations) it checks every remote participant currently in the room.
   * An empty room (nobody to receive) is considered eligible.
   */
  allRecipientsSupportV2(destinationIdentities) {
    const identities = destinationIdentities && destinationIdentities.length > 0 ? destinationIdentities : this.getAllRemoteParticipantIdentities();
    return identities.every((identity) => this.getRemoteParticipantClientProtocol(identity) >= CLIENT_PROTOCOL_DATA_STREAM_V2);
  }
  /**
   * Returns true only if every recipient advertises the deflate-raw compression capability (so it
   * can decompress a compressed stream). Resolved the same way as {@link allRecipientsSupportV2}:
   * named destinations, or every remote participant for a broadcast; an empty room is eligible.
   */
  allRecipientsSupportCompression(destinationIdentities) {
    const identities = destinationIdentities && destinationIdentities.length > 0 ? destinationIdentities : this.getAllRemoteParticipantIdentities();
    return identities.every((identity) => this.getRemoteParticipantCapabilities(identity).includes(ClientInfo_Capability.CAP_COMPRESSION_DEFLATE_RAW));
  }
  /**
   * Shared chunked-stream send for `sendText`/`sendFile`: sends the prebuilt header packet, then
   * forwards `source` (optionally deflate-raw compressed) as `streamChunk` packets re-chunked to
   * the MTU budget with contiguous indices, then sends the trailer. The source is consumed
   * incrementally, so a `file.stream()` is never buffered in full. The platform compressor can't
   * flush mid-stream, so compression is only used when the whole payload is available as a stream
   * up front (not for incremental writers like `streamText`/`streamBytes`).
   */
  sendChunkedByteStream(headerPacket, streamId, destinationIdentities, source) {
    return __awaiter(this, void 0, void 0, function* () {
      var _a2, e_1, _b2, _c;
      const engine = this.engine;
      yield sendHeaderPacket(engine, headerPacket);
      let chunkId = 0;
      try {
        for (var _d = true, _e2 = __asyncValues(readBytesInChunks(source, STREAM_CHUNK_SIZE_BYTES)), _f; _f = yield _e2.next(), _a2 = _f.done, !_a2; _d = true) {
          _c = _f.value;
          _d = false;
          const chunk = _c;
          const chunkPacket = new DataPacket({
            destinationIdentities,
            value: {
              case: "streamChunk",
              value: new DataStream_Chunk({
                content: chunk,
                streamId,
                chunkIndex: numberToBigInt(chunkId)
              })
            }
          });
          yield engine.sendDataPacket(chunkPacket, DataChannelKind.RELIABLE);
          chunkId += 1;
        }
      } catch (e_1_1) {
        e_1 = {
          error: e_1_1
        };
      } finally {
        try {
          if (!_d && !_a2 && (_b2 = _e2.return)) yield _b2.call(_e2);
        } finally {
          if (e_1) throw e_1.error;
        }
      }
      yield sendStreamTrailer(streamId, destinationIdentities, engine);
    });
  }
  /**
   * @internal
   */
  streamText(options) {
    return __awaiter(this, void 0, void 0, function* () {
      var _a2, _b2, _c;
      const streamId = (_a2 = options === null || options === void 0 ? void 0 : options.streamId) !== null && _a2 !== void 0 ? _a2 : crypto.randomUUID();
      const destinationIdentities = options === null || options === void 0 ? void 0 : options.destinationIdentities;
      const info = {
        id: streamId,
        mimeType: "text/plain",
        timestamp: Date.now(),
        topic: (_b2 = options === null || options === void 0 ? void 0 : options.topic) !== null && _b2 !== void 0 ? _b2 : "",
        size: options === null || options === void 0 ? void 0 : options.totalSize,
        attributes: options === null || options === void 0 ? void 0 : options.attributes,
        encryptionType: ((_c = this.engine.e2eeManager) === null || _c === void 0 ? void 0 : _c.isDataChannelEncryptionEnabled) ? Encryption_Type.GCM : Encryption_Type.NONE,
        attachedStreamIds: options === null || options === void 0 ? void 0 : options.attachedStreamIds
      };
      const header = buildTextStreamHeader(info, options);
      const packet = createStreamHeaderPacket(header, destinationIdentities);
      yield sendHeaderPacket(this.engine, packet);
      let chunkId = 0;
      const engine = this.engine;
      const writableStream = new WritableStream({
        write(text) {
          return __awaiter(this, void 0, void 0, function* () {
            for (const textByteChunk of splitUtf8(text, STREAM_CHUNK_SIZE_BYTES)) {
              const chunk = new DataStream_Chunk({
                content: textByteChunk,
                streamId,
                chunkIndex: numberToBigInt(chunkId)
              });
              const chunkPacket = new DataPacket({
                destinationIdentities,
                value: {
                  case: "streamChunk",
                  value: chunk
                }
              });
              yield engine.sendDataPacket(chunkPacket, DataChannelKind.RELIABLE);
              chunkId += 1;
            }
          });
        },
        close() {
          return __awaiter(this, void 0, void 0, function* () {
            yield sendStreamTrailer(streamId, destinationIdentities, engine);
          });
        },
        abort(err) {
          console.log("Sink error:", err);
        }
      });
      let onEngineClose = () => __awaiter(this, void 0, void 0, function* () {
        yield writer2.close();
      });
      engine.once(EngineEvent.Closing, onEngineClose);
      const writer2 = new TextStreamWriter(writableStream, info, () => this.engine.off(EngineEvent.Closing, onEngineClose));
      return writer2;
    });
  }
  sendFile(file, options) {
    return __awaiter(this, void 0, void 0, function* () {
      const streamId = crypto.randomUUID();
      yield this._sendFile(streamId, file, options);
      return {
        id: streamId
      };
    });
  }
  /**
   * Streams a file as a chunked byte stream, compressed (deflate-raw) when the runtime supports it
   * and every recipient is on data streams v2. The file is piped `file.stream()` →
   * (`CompressionStream`) → chunk packets via {@link sendChunkedByteStream}, so it is never fully
   * buffered in memory — unlike {@link sendBytes}, there is no inline single-packet fast path for
   * files (the compressed size can't be known up front without buffering the whole file).
   */
  _sendFile(streamId, file, options) {
    return __awaiter(this, void 0, void 0, function* () {
      var _a2, _b2, _c, _d, _e2;
      const destinationIdentities = options === null || options === void 0 ? void 0 : options.destinationIdentities;
      const compress = ((_a2 = options === null || options === void 0 ? void 0 : options.compress) !== null && _a2 !== void 0 ? _a2 : true) && isCompressionStreamSupported() && this.allRecipientsSupportV2(destinationIdentities) && this.allRecipientsSupportCompression(destinationIdentities);
      const info = {
        id: streamId,
        name: file.name,
        mimeType: (_b2 = options === null || options === void 0 ? void 0 : options.mimeType) !== null && _b2 !== void 0 ? _b2 : file.type,
        topic: (_c = options === null || options === void 0 ? void 0 : options.topic) !== null && _c !== void 0 ? _c : "",
        timestamp: Date.now(),
        size: file.size,
        encryptionType: ((_d = this.engine.e2eeManager) === null || _d === void 0 ? void 0 : _d.isDataChannelEncryptionEnabled) ? Encryption_Type.GCM : Encryption_Type.NONE
      };
      const header = buildByteStreamHeader(info, {
        compression: compress ? DataStream_CompressionType.DEFLATE_RAW : DataStream_CompressionType.NONE
      });
      const packet = createStreamHeaderPacket(header, destinationIdentities);
      const tapped = file.stream().pipeThrough(progressReportingStream(file.size, options === null || options === void 0 ? void 0 : options.onProgress));
      const source = compress ? tapped.pipeThrough(deflateRawTransform()) : tapped;
      yield this.sendChunkedByteStream(packet, streamId, destinationIdentities, source);
      if (file.size === 0) {
        (_e2 = options === null || options === void 0 ? void 0 : options.onProgress) === null || _e2 === void 0 ? void 0 : _e2.call(options, 1);
      }
      return info;
    });
  }
  streamBytes(options) {
    return __awaiter(this, void 0, void 0, function* () {
      var _a2, _b2, _c, _d, _e2;
      const streamId = (_a2 = options === null || options === void 0 ? void 0 : options.streamId) !== null && _a2 !== void 0 ? _a2 : crypto.randomUUID();
      const destinationIdentities = options === null || options === void 0 ? void 0 : options.destinationIdentities;
      const info = {
        id: streamId,
        mimeType: (_b2 = options === null || options === void 0 ? void 0 : options.mimeType) !== null && _b2 !== void 0 ? _b2 : "application/octet-stream",
        topic: (_c = options === null || options === void 0 ? void 0 : options.topic) !== null && _c !== void 0 ? _c : "",
        timestamp: Date.now(),
        attributes: options === null || options === void 0 ? void 0 : options.attributes,
        size: options === null || options === void 0 ? void 0 : options.totalSize,
        name: (_d = options === null || options === void 0 ? void 0 : options.name) !== null && _d !== void 0 ? _d : "unknown",
        encryptionType: ((_e2 = this.engine.e2eeManager) === null || _e2 === void 0 ? void 0 : _e2.isDataChannelEncryptionEnabled) ? Encryption_Type.GCM : Encryption_Type.NONE
      };
      const header = buildByteStreamHeader(info);
      const packet = createStreamHeaderPacket(header, destinationIdentities);
      yield sendHeaderPacket(this.engine, packet);
      let chunkId = 0;
      const writeMutex = new _();
      const engine = this.engine;
      const logLocal = this.log;
      const writableStream = new WritableStream({
        write(chunk) {
          return __awaiter(this, void 0, void 0, function* () {
            const unlock = yield writeMutex.lock();
            let byteOffset = 0;
            try {
              while (byteOffset < chunk.byteLength) {
                const subChunk = chunk.slice(byteOffset, byteOffset + STREAM_CHUNK_SIZE_BYTES);
                const chunkPacket = new DataPacket({
                  destinationIdentities,
                  value: {
                    case: "streamChunk",
                    value: new DataStream_Chunk({
                      content: subChunk,
                      streamId,
                      chunkIndex: numberToBigInt(chunkId)
                    })
                  }
                });
                yield engine.sendDataPacket(chunkPacket, DataChannelKind.RELIABLE);
                chunkId += 1;
                byteOffset += subChunk.byteLength;
              }
            } finally {
              unlock();
            }
          });
        },
        close() {
          return __awaiter(this, void 0, void 0, function* () {
            yield sendStreamTrailer(streamId, destinationIdentities, engine);
          });
        },
        abort(err) {
          logLocal.error("Sink error:", err);
        }
      });
      const byteWriter = new ByteStreamWriter(writableStream, info);
      return byteWriter;
    });
  }
};
var MaybeCollectedStream = class _MaybeCollectedStream {
  constructor(state) {
    this.state = state;
  }
  static fromStream(stream) {
    return new _MaybeCollectedStream({
      type: "stream",
      stream
    });
  }
  /** Collect data from the stream into memory and return as a Uint8Array. */
  collect() {
    return __awaiter(this, void 0, void 0, function* () {
      switch (this.state.type) {
        case "stream":
          const bytes = yield collect(this.state.stream);
          this.state = {
            type: "collected",
            bytes
          };
          return bytes;
        case "collected":
          return this.state.bytes;
      }
    });
  }
  /** Pass wrapped stream through to downstream consumer. */
  stream() {
    switch (this.state.type) {
      case "stream":
        return this.state.stream;
      case "collected":
        return readableFromBytes(this.state.bytes);
    }
  }
};
function progressReportingStream(totalPreCompressionLength, onProgress) {
  let sent = 0;
  return new TransformStream({
    transform(chunk, controller) {
      sent += chunk.byteLength;
      if (onProgress && typeof totalPreCompressionLength === "number" && totalPreCompressionLength > 0) {
        onProgress(Math.min(sent / totalPreCompressionLength, 1));
      }
      controller.enqueue(chunk);
    }
  });
}
function sendHeaderPacket(engine, packet) {
  return __awaiter(this, void 0, void 0, function* () {
    if (packet.toBinary().byteLength > STREAM_CHUNK_SIZE_BYTES) {
      throw new DataStreamError("data stream header exceeds the ".concat(STREAM_CHUNK_SIZE_BYTES, "-byte limit; reduce attribute size"), DataStreamErrorReason.HeaderTooLarge);
    }
    yield engine.sendDataPacket(packet, DataChannelKind.RELIABLE);
  });
}
function sendStreamTrailer(streamId, destinationIdentities, engine) {
  return __awaiter(this, void 0, void 0, function* () {
    const trailerPacket = new DataPacket({
      destinationIdentities,
      value: {
        case: "streamTrailer",
        value: new DataStream_Trailer({
          streamId
        })
      }
    });
    yield engine.sendDataPacket(trailerPacket, DataChannelKind.RELIABLE);
  });
}
function abortSignalAny(signals) {
  if (signals.length === 0) {
    const controller2 = new AbortController();
    return controller2.signal;
  }
  if (signals.length === 1) {
    return signals[0];
  }
  for (const signal of signals) {
    if (signal.aborted) {
      return signal;
    }
  }
  const controller = new AbortController();
  const unlisteners = Array(signals.length);
  const cleanup = () => {
    for (const unsubscribe of unlisteners) {
      unsubscribe();
    }
  };
  signals.forEach((signal, index) => {
    const handler = () => {
      controller.abort(signal.reason);
      cleanup();
    };
    signal.addEventListener("abort", handler);
    unlisteners[index] = () => signal.removeEventListener("abort", handler);
  });
  return controller.signal;
}
function abortSignalTimeout(ms2) {
  const controller = new AbortController();
  setTimeout(() => {
    controller.abort(new DOMException("signal timed out after ".concat(ms2, " ms"), "TimeoutError"));
  }, ms2);
  return controller.signal;
}
var U8_LENGTH_BYTES = 1;
var U16_LENGTH_BYTES = 2;
var U32_LENGTH_BYTES = 4;
var U64_LENGTH_BYTES = 8;
var SUPPORTED_VERSION = 0;
var BASE_HEADER_LEN = 12;
var VERSION_SHIFT = 5;
var VERSION_MASK = 7;
var FRAME_MARKER_SHIFT = 3;
var FRAME_MARKER_MASK = 3;
var FRAME_MARKER_START = 2;
var FRAME_MARKER_FINAL = 1;
var FRAME_MARKER_INTER = 0;
var FRAME_MARKER_SINGLE = 3;
var EXT_WORDS_INDICATOR_SIZE = 2;
var EXT_FLAG_SHIFT = 2;
var EXT_FLAG_MASK = 1;
var EXT_TAG_PADDING = 0;
var DataTrackDeserializeErrorReason;
(function(DataTrackDeserializeErrorReason2) {
  DataTrackDeserializeErrorReason2[DataTrackDeserializeErrorReason2["TooShort"] = 0] = "TooShort";
  DataTrackDeserializeErrorReason2[DataTrackDeserializeErrorReason2["HeaderOverrun"] = 1] = "HeaderOverrun";
  DataTrackDeserializeErrorReason2[DataTrackDeserializeErrorReason2["MissingExtWords"] = 2] = "MissingExtWords";
  DataTrackDeserializeErrorReason2[DataTrackDeserializeErrorReason2["UnsupportedVersion"] = 3] = "UnsupportedVersion";
  DataTrackDeserializeErrorReason2[DataTrackDeserializeErrorReason2["InvalidHandle"] = 4] = "InvalidHandle";
  DataTrackDeserializeErrorReason2[DataTrackDeserializeErrorReason2["MalformedExt"] = 5] = "MalformedExt";
})(DataTrackDeserializeErrorReason || (DataTrackDeserializeErrorReason = {}));
var DataTrackDeserializeError = class _DataTrackDeserializeError extends LivekitReasonedError {
  constructor(message, reason, options) {
    super(19, message, options);
    this.name = "DataTrackDeserializeError";
    this.reason = reason;
    this.reasonName = DataTrackDeserializeErrorReason[reason];
  }
  static tooShort() {
    return new _DataTrackDeserializeError("Too short to contain a valid header", DataTrackDeserializeErrorReason.TooShort);
  }
  static headerOverrun() {
    return new _DataTrackDeserializeError("Header exceeds total packet length", DataTrackDeserializeErrorReason.HeaderOverrun);
  }
  static missingExtWords() {
    return new _DataTrackDeserializeError("Extension word indicator is missing", DataTrackDeserializeErrorReason.MissingExtWords);
  }
  static unsupportedVersion(version2) {
    return new _DataTrackDeserializeError("Unsupported version ".concat(version2), DataTrackDeserializeErrorReason.UnsupportedVersion);
  }
  static invalidHandle(cause) {
    return new _DataTrackDeserializeError("invalid track handle: ".concat(cause.message), DataTrackDeserializeErrorReason.InvalidHandle, {
      cause
    });
  }
  static malformedExt(tag) {
    return new _DataTrackDeserializeError("Extension with tag ".concat(tag, " is malformed"), DataTrackDeserializeErrorReason.MalformedExt);
  }
};
var DataTrackSerializeErrorReason;
(function(DataTrackSerializeErrorReason2) {
  DataTrackSerializeErrorReason2[DataTrackSerializeErrorReason2["TooSmallForHeader"] = 0] = "TooSmallForHeader";
  DataTrackSerializeErrorReason2[DataTrackSerializeErrorReason2["TooSmallForPayload"] = 1] = "TooSmallForPayload";
})(DataTrackSerializeErrorReason || (DataTrackSerializeErrorReason = {}));
var DataTrackSerializeError = class _DataTrackSerializeError extends LivekitReasonedError {
  constructor(message, reason, options) {
    super(19, message, options);
    this.name = "DataTrackSerializeError";
    this.reason = reason;
    this.reasonName = DataTrackSerializeErrorReason[reason];
  }
  static tooSmallForHeader() {
    return new _DataTrackSerializeError("Buffer cannot fit header", DataTrackSerializeErrorReason.TooSmallForHeader);
  }
  static tooSmallForPayload() {
    return new _DataTrackSerializeError("Buffer cannot fit payload", DataTrackSerializeErrorReason.TooSmallForPayload);
  }
};
var Serializable = class {
  /** Encodes the instance as binary and returns the data as a Uint8Array. */
  toBinary() {
    const lengthBytes = this.toBinaryLengthBytes();
    const output = new ArrayBuffer(lengthBytes);
    const view = new DataView(output);
    const writtenBytes = this.toBinaryInto(view);
    if (lengthBytes !== writtenBytes) {
      throw new Error("".concat(this.constructor.name, ".toBinary: written bytes (").concat(writtenBytes, " bytes) not equal to allocated array buffer length (").concat(lengthBytes, " bytes)."));
    }
    return new Uint8Array(output);
  }
};
var DataTrackExtensionTag;
(function(DataTrackExtensionTag2) {
  DataTrackExtensionTag2[DataTrackExtensionTag2["UserTimestamp"] = 2] = "UserTimestamp";
  DataTrackExtensionTag2[DataTrackExtensionTag2["E2ee"] = 1] = "E2ee";
})(DataTrackExtensionTag || (DataTrackExtensionTag = {}));
var DataTrackExtension = class extends Serializable {
};
var DataTrackUserTimestampExtension = class _DataTrackUserTimestampExtension extends DataTrackExtension {
  constructor(timestamp) {
    super();
    this.timestamp = timestamp;
  }
  toBinaryLengthBytes() {
    return U8_LENGTH_BYTES + U8_LENGTH_BYTES + _DataTrackUserTimestampExtension.lengthBytes;
  }
  toBinaryInto(dataView) {
    let byteIndex = 0;
    dataView.setUint8(byteIndex, _DataTrackUserTimestampExtension.tag);
    byteIndex += U8_LENGTH_BYTES;
    dataView.setUint8(byteIndex, _DataTrackUserTimestampExtension.lengthBytes);
    byteIndex += U8_LENGTH_BYTES;
    dataView.setBigUint64(byteIndex, this.timestamp);
    byteIndex += U64_LENGTH_BYTES;
    const totalLengthBytes = this.toBinaryLengthBytes();
    if (byteIndex !== totalLengthBytes) {
      throw new Error("DataTrackUserTimestampExtension.toBinaryInto: Wrote ".concat(byteIndex, " bytes but expected length was ").concat(totalLengthBytes, " bytes"));
    }
    return byteIndex;
  }
  toJSON() {
    return {
      tag: _DataTrackUserTimestampExtension.tag,
      lengthBytes: _DataTrackUserTimestampExtension.lengthBytes,
      timestamp: this.timestamp
    };
  }
};
DataTrackUserTimestampExtension.tag = DataTrackExtensionTag.UserTimestamp;
DataTrackUserTimestampExtension.lengthBytes = 8;
var DataTrackE2eeExtension = class _DataTrackE2eeExtension extends DataTrackExtension {
  constructor(keyIndex, iv) {
    super();
    this.keyIndex = keyIndex;
    this.iv = iv;
  }
  toBinaryLengthBytes() {
    return U8_LENGTH_BYTES + U8_LENGTH_BYTES + _DataTrackE2eeExtension.lengthBytes;
  }
  toBinaryInto(dataView) {
    let byteIndex = 0;
    dataView.setUint8(byteIndex, _DataTrackE2eeExtension.tag);
    byteIndex += U8_LENGTH_BYTES;
    dataView.setUint8(byteIndex, _DataTrackE2eeExtension.lengthBytes);
    byteIndex += U8_LENGTH_BYTES;
    dataView.setUint8(byteIndex, this.keyIndex);
    byteIndex += U8_LENGTH_BYTES;
    for (let i2 = 0; i2 < this.iv.length; i2 += 1) {
      dataView.setUint8(byteIndex, this.iv[i2]);
      byteIndex += U8_LENGTH_BYTES;
    }
    const totalLengthBytes = this.toBinaryLengthBytes();
    if (byteIndex !== totalLengthBytes) {
      throw new Error("DataTrackE2eeExtension.toBinaryInto: Wrote ".concat(byteIndex, " bytes but expected length was ").concat(totalLengthBytes, " bytes"));
    }
    return byteIndex;
  }
  toJSON() {
    return {
      tag: _DataTrackE2eeExtension.tag,
      lengthBytes: _DataTrackE2eeExtension.lengthBytes,
      keyIndex: this.keyIndex,
      iv: this.iv
    };
  }
};
DataTrackE2eeExtension.tag = DataTrackExtensionTag.E2ee;
DataTrackE2eeExtension.lengthBytes = 13;
var DataTrackExtensions = class _DataTrackExtensions extends Serializable {
  constructor() {
    let opts = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : {};
    super();
    this.userTimestamp = opts.userTimestamp;
    this.e2ee = opts.e2ee;
  }
  toBinaryLengthBytes() {
    let lengthBytes = 0;
    if (this.userTimestamp) {
      lengthBytes += this.userTimestamp.toBinaryLengthBytes();
    }
    if (this.e2ee) {
      lengthBytes += this.e2ee.toBinaryLengthBytes();
    }
    return lengthBytes;
  }
  toBinaryInto(dataView) {
    let byteIndex = 0;
    if (this.e2ee) {
      const e2eeBytes = this.e2ee.toBinaryInto(dataView);
      byteIndex += e2eeBytes;
    }
    if (this.userTimestamp) {
      const userTimestampBytes = this.userTimestamp.toBinaryInto(new DataView(dataView.buffer, dataView.byteOffset + byteIndex));
      byteIndex += userTimestampBytes;
    }
    const totalLengthBytes = this.toBinaryLengthBytes();
    if (byteIndex !== totalLengthBytes) {
      throw new Error("DataTrackExtensions.toBinaryInto: Wrote ".concat(byteIndex, " bytes but expected length was ").concat(totalLengthBytes, " bytes"));
    }
    return byteIndex;
  }
  static fromBinary(input) {
    const dataView = coerceToDataView(input);
    let userTimestamp;
    let e2ee;
    let byteIndex = 0;
    while (dataView.byteLength - byteIndex >= U8_LENGTH_BYTES + U8_LENGTH_BYTES) {
      const tag = dataView.getUint8(byteIndex);
      byteIndex += U8_LENGTH_BYTES;
      const lengthBytes = dataView.getUint8(byteIndex);
      byteIndex += U8_LENGTH_BYTES;
      if (tag === EXT_TAG_PADDING) {
        continue;
      }
      switch (tag) {
        case DataTrackExtensionTag.UserTimestamp:
          if (dataView.byteLength - byteIndex < DataTrackUserTimestampExtension.lengthBytes) {
            throw DataTrackDeserializeError.malformedExt(tag);
          }
          userTimestamp = new DataTrackUserTimestampExtension(dataView.getBigUint64(byteIndex));
          byteIndex += lengthBytes;
          break;
        case DataTrackExtensionTag.E2ee:
          if (dataView.byteLength - byteIndex < DataTrackE2eeExtension.lengthBytes) {
            throw DataTrackDeserializeError.malformedExt(tag);
          }
          const keyIndex = dataView.getUint8(byteIndex);
          const iv = new Uint8Array(12);
          for (let i2 = 0; i2 < iv.length; i2 += 1) {
            let byteIndexForIv = byteIndex;
            byteIndexForIv += U8_LENGTH_BYTES;
            byteIndexForIv += i2 * U8_LENGTH_BYTES;
            iv[i2] = dataView.getUint8(byteIndexForIv);
          }
          e2ee = new DataTrackE2eeExtension(keyIndex, iv);
          byteIndex += lengthBytes;
          break;
        default:
          if (dataView.byteLength - byteIndex < lengthBytes) {
            throw DataTrackDeserializeError.malformedExt(tag);
          }
          byteIndex += lengthBytes;
          break;
      }
    }
    return [new _DataTrackExtensions({
      userTimestamp,
      e2ee
    }), dataView.byteLength];
  }
  toJSON() {
    var _a2, _b2, _c, _d;
    return {
      userTimestamp: (_b2 = (_a2 = this.userTimestamp) === null || _a2 === void 0 ? void 0 : _a2.toJSON()) !== null && _b2 !== void 0 ? _b2 : null,
      e2ee: (_d = (_c = this.e2ee) === null || _c === void 0 ? void 0 : _c.toJSON()) !== null && _d !== void 0 ? _d : null
    };
  }
};
var DataTrackFrameInternal = {
  from(frame) {
    return {
      payload: frame.payload,
      extensions: new DataTrackExtensions({
        userTimestamp: frame.userTimestamp ? new DataTrackUserTimestampExtension(frame.userTimestamp) : void 0
      })
    };
  },
  /** Converts from a DataTrackFrameInternal -> DataTrackFrame. Some internal information is
   * discarded like e2ee encrption extension data. */
  lossyIntoFrame(frame) {
    var _a2;
    return {
      payload: frame.payload,
      userTimestamp: (_a2 = frame.extensions.userTimestamp) === null || _a2 === void 0 ? void 0 : _a2.timestamp
    };
  }
};
var TrackSymbol = Symbol.for("lk.track");
var DataTrackSymbol = Symbol.for("lk.data-track");
var RemoteDataTrack = class {
  /** @internal */
  constructor(info, manager, options) {
    this.trackSymbol = TrackSymbol;
    this.isLocal = false;
    this.typeSymbol = DataTrackSymbol;
    this.info = info;
    this.manager = manager;
    this.publisherIdentity = options.publisherIdentity;
  }
  /** Subscribes to the data track to receive frames.
   *
   * # Returns
   *
   * A stream that yields {@link DataTrackFrame}s as they arrive.
   *
   * # Multiple Subscriptions
   *
   * An application may call `subscribe` more than once to process frames in
   * multiple places. For example, one async task might plot values on a graph
   * while another writes them to a file.
   *
   * Internally, only the first call to `subscribe` communicates with the SFU and
   * allocates the resources required to receive frames. Additional subscriptions
   * reuse the same underlying pipeline and do not trigger additional signaling.
   *
   * Note that newly created subscriptions only receive frames published after
   * the initial subscription is established.
   */
  subscribe(options) {
    try {
      const _this$manager$openSub = this.manager.openSubscriptionStream(this.info.sid, options === null || options === void 0 ? void 0 : options.signal, options === null || options === void 0 ? void 0 : options.bufferSize), _this$manager$openSub2 = _slicedToArray(_this$manager$openSub, 2), stream = _this$manager$openSub2[0], sfuSubscriptionComplete = _this$manager$openSub2[1];
      sfuSubscriptionComplete.catch(() => {
      });
      return stream;
    } catch (err) {
      throw err;
    }
  }
  /** Configure how incoming frames for this track are processed before they are handed out to
   * subscribers (the "pipeline"). These options apply to all current and future subscriptions
   * of this track, and may be set at any time. */
  setPipelineOptions(options) {
    this.manager.setPipelineOptions(this.info.sid, options);
  }
};
var DataTrackPacketHeader = class _DataTrackPacketHeader extends Serializable {
  constructor(opts) {
    var _a2;
    super();
    this.marker = opts.marker;
    this.trackHandle = opts.trackHandle;
    this.sequence = opts.sequence;
    this.frameNumber = opts.frameNumber;
    this.timestamp = opts.timestamp;
    this.extensions = (_a2 = opts.extensions) !== null && _a2 !== void 0 ? _a2 : new DataTrackExtensions();
  }
  extensionsMetrics() {
    const lengthBytes = this.extensions.toBinaryLengthBytes();
    const lengthWords = Math.ceil((EXT_WORDS_INDICATOR_SIZE + lengthBytes) / 4);
    const paddingLengthBytes = lengthWords * 4 - EXT_WORDS_INDICATOR_SIZE - lengthBytes;
    return {
      lengthBytes,
      lengthWords,
      paddingLengthBytes
    };
  }
  toBinaryLengthBytes() {
    const _this$extensionsMetri = this.extensionsMetrics(), extLengthBytes = _this$extensionsMetri.lengthBytes, extPaddingLengthBytes = _this$extensionsMetri.paddingLengthBytes;
    let totalLengthBytes = BASE_HEADER_LEN;
    if (extLengthBytes > 0) {
      totalLengthBytes += EXT_WORDS_INDICATOR_SIZE + extLengthBytes + extPaddingLengthBytes;
    }
    return totalLengthBytes;
  }
  toBinaryInto(dataView) {
    if (dataView.byteLength < this.toBinaryLengthBytes()) {
      throw DataTrackSerializeError.tooSmallForHeader();
    }
    let initial = SUPPORTED_VERSION << VERSION_SHIFT;
    let marker;
    switch (this.marker) {
      case FrameMarker.Inter:
        marker = FRAME_MARKER_INTER;
        break;
      case FrameMarker.Final:
        marker = FRAME_MARKER_FINAL;
        break;
      case FrameMarker.Start:
        marker = FRAME_MARKER_START;
        break;
      case FrameMarker.Single:
        marker = FRAME_MARKER_SINGLE;
        break;
    }
    initial |= marker << FRAME_MARKER_SHIFT;
    const _this$extensionsMetri2 = this.extensionsMetrics(), extensionsLengthBytes = _this$extensionsMetri2.lengthBytes, extensionsLengthWords = _this$extensionsMetri2.lengthWords, extensionsPaddingLengthBytes = _this$extensionsMetri2.paddingLengthBytes;
    if (extensionsLengthBytes > 0) {
      initial |= 1 << EXT_FLAG_SHIFT;
    }
    let byteIndex = 0;
    dataView.setUint8(byteIndex, initial);
    byteIndex += U8_LENGTH_BYTES;
    dataView.setUint8(byteIndex, 0);
    byteIndex += U8_LENGTH_BYTES;
    dataView.setUint16(byteIndex, this.trackHandle);
    byteIndex += U16_LENGTH_BYTES;
    dataView.setUint16(byteIndex, this.sequence.value);
    byteIndex += U16_LENGTH_BYTES;
    dataView.setUint16(byteIndex, this.frameNumber.value);
    byteIndex += U16_LENGTH_BYTES;
    dataView.setUint32(byteIndex, this.timestamp.asTicks());
    byteIndex += U32_LENGTH_BYTES;
    if (extensionsLengthBytes > 0) {
      const rtpOrientedExtensionLengthWords = extensionsLengthWords - 1;
      dataView.setUint16(byteIndex, rtpOrientedExtensionLengthWords);
      byteIndex += U16_LENGTH_BYTES;
      const extensionBytes = this.extensions.toBinaryInto(new DataView(dataView.buffer, dataView.byteOffset + byteIndex));
      byteIndex += extensionBytes;
      for (let i2 = 0; i2 < extensionsPaddingLengthBytes; i2 += 1) {
        dataView.setUint8(byteIndex, 0);
        byteIndex += U8_LENGTH_BYTES;
      }
    }
    const totalLengthBytes = this.toBinaryLengthBytes();
    if (byteIndex !== totalLengthBytes) {
      throw new Error("DataTrackPacketHeader.toBinaryInto: Wrote ".concat(byteIndex, " bytes but expected length was ").concat(totalLengthBytes, " bytes"));
    }
    return totalLengthBytes;
  }
  static fromBinary(input) {
    const dataView = coerceToDataView(input);
    if (dataView.byteLength < BASE_HEADER_LEN) {
      throw DataTrackDeserializeError.tooShort();
    }
    let byteIndex = 0;
    const initial = dataView.getUint8(byteIndex);
    byteIndex += U8_LENGTH_BYTES;
    const version2 = initial >> VERSION_SHIFT & VERSION_MASK;
    if (version2 > SUPPORTED_VERSION) {
      throw DataTrackDeserializeError.unsupportedVersion(version2);
    }
    let marker;
    switch (initial >> FRAME_MARKER_SHIFT & FRAME_MARKER_MASK) {
      case FRAME_MARKER_START:
        marker = FrameMarker.Start;
        break;
      case FRAME_MARKER_FINAL:
        marker = FrameMarker.Final;
        break;
      case FRAME_MARKER_SINGLE:
        marker = FrameMarker.Single;
        break;
      case FRAME_MARKER_INTER:
      default:
        marker = FrameMarker.Inter;
        break;
    }
    const extensionsFlag = (initial >> EXT_FLAG_SHIFT & EXT_FLAG_MASK) > 0;
    byteIndex += U8_LENGTH_BYTES;
    let trackHandle;
    try {
      trackHandle = DataTrackHandle.fromNumber(dataView.getUint16(byteIndex));
    } catch (e4) {
      if (e4 instanceof DataTrackHandleError && (e4.isReason(DataTrackHandleErrorReason.Reserved) || e4.isReason(DataTrackHandleErrorReason.TooLarge))) {
        throw DataTrackDeserializeError.invalidHandle(e4);
      } else {
        throw e4;
      }
    }
    byteIndex += U16_LENGTH_BYTES;
    const sequence = WrapAroundUnsignedInt.u16(dataView.getUint16(byteIndex));
    byteIndex += U16_LENGTH_BYTES;
    const frameNumber = WrapAroundUnsignedInt.u16(dataView.getUint16(byteIndex));
    byteIndex += U16_LENGTH_BYTES;
    const timestamp = DataTrackTimestamp.fromRtpTicks(dataView.getUint32(byteIndex));
    byteIndex += U32_LENGTH_BYTES;
    let extensions = new DataTrackExtensions();
    if (extensionsFlag) {
      if (dataView.byteLength - byteIndex < U16_LENGTH_BYTES) {
        throw DataTrackDeserializeError.missingExtWords();
      }
      let rtpOrientedExtensionWords = dataView.getUint16(byteIndex);
      byteIndex += U16_LENGTH_BYTES;
      const extensionWords = rtpOrientedExtensionWords + 1;
      let extensionLengthBytes = 4 * extensionWords - EXT_WORDS_INDICATOR_SIZE;
      if (byteIndex + extensionLengthBytes > dataView.byteLength) {
        throw DataTrackDeserializeError.headerOverrun();
      }
      let extensionDataView = new DataView(dataView.buffer, dataView.byteOffset + byteIndex, extensionLengthBytes);
      const _DataTrackExtensions$ = DataTrackExtensions.fromBinary(extensionDataView), _DataTrackExtensions$2 = _slicedToArray(_DataTrackExtensions$, 2), result = _DataTrackExtensions$2[0], readBytes = _DataTrackExtensions$2[1];
      extensions = result;
      byteIndex += readBytes;
    }
    return [new _DataTrackPacketHeader({
      marker,
      trackHandle,
      sequence,
      frameNumber,
      timestamp,
      extensions
    }), byteIndex];
  }
  toJSON() {
    return {
      marker: this.marker,
      trackHandle: this.trackHandle,
      sequence: this.sequence.value,
      frameNumber: this.frameNumber.value,
      timestamp: this.timestamp.asTicks(),
      extensions: this.extensions.toJSON()
    };
  }
};
var FrameMarker;
(function(FrameMarker2) {
  FrameMarker2[FrameMarker2["Start"] = 0] = "Start";
  FrameMarker2[FrameMarker2["Inter"] = 1] = "Inter";
  FrameMarker2[FrameMarker2["Final"] = 2] = "Final";
  FrameMarker2[FrameMarker2["Single"] = 3] = "Single";
})(FrameMarker || (FrameMarker = {}));
var DataTrackPacket = class _DataTrackPacket extends Serializable {
  constructor(header, payload) {
    super();
    this.header = header;
    this.payload = payload;
  }
  toBinaryLengthBytes() {
    return this.header.toBinaryLengthBytes() + this.payload.byteLength;
  }
  toBinaryInto(dataView) {
    let byteIndex = 0;
    const headerLengthBytes = this.header.toBinaryInto(dataView);
    byteIndex += headerLengthBytes;
    if (dataView.byteLength - byteIndex < this.payload.byteLength) {
      throw DataTrackSerializeError.tooSmallForPayload();
    }
    for (let index = 0; index < this.payload.length; index += 1) {
      dataView.setUint8(byteIndex, this.payload[index]);
      byteIndex += U8_LENGTH_BYTES;
    }
    const totalLengthBytes = this.toBinaryLengthBytes();
    if (byteIndex !== totalLengthBytes) {
      throw new Error("DataTrackPacket.toBinaryInto: Wrote ".concat(byteIndex, " bytes but expected length was ").concat(totalLengthBytes, " bytes"));
    }
    return totalLengthBytes;
  }
  static fromBinary(input) {
    const dataView = coerceToDataView(input);
    const _DataTrackPacketHeade = DataTrackPacketHeader.fromBinary(dataView), _DataTrackPacketHeade2 = _slicedToArray(_DataTrackPacketHeade, 2), header = _DataTrackPacketHeade2[0], headerByteLength = _DataTrackPacketHeade2[1];
    const payload = dataView.buffer.slice(dataView.byteOffset + headerByteLength, dataView.byteOffset + dataView.byteLength);
    return [new _DataTrackPacket(header, new Uint8Array(payload)), dataView.byteLength];
  }
  toJSON() {
    return {
      header: this.header.toJSON(),
      payload: this.payload
    };
  }
};
var log$3 = getLogger(LoggerNames.DataTracks);
var DataTrackDepacketizerDropError = class _DataTrackDepacketizerDropError extends LivekitReasonedError {
  constructor(message, reason, frameNumber, options) {
    super(19, "Frame ".concat(frameNumber, " dropped: ").concat(message), options);
    this.name = "DataTrackDepacketizerDropError";
    this.reason = reason;
    this.reasonName = DataTrackDepacketizerDropReason[reason];
    this.frameNumber = frameNumber;
  }
  static interrupted(frameNumber, newFrameNumber) {
    return new _DataTrackDepacketizerDropError("Interrupted by the start of a new frame ".concat(newFrameNumber), DataTrackDepacketizerDropReason.Interrupted, frameNumber);
  }
  static unknownFrame(frameNumber) {
    return new _DataTrackDepacketizerDropError("Initial packet was never received.", DataTrackDepacketizerDropReason.UnknownFrame, frameNumber);
  }
  static bufferFull(frameNumber) {
    return new _DataTrackDepacketizerDropError("Reorder buffer is full.", DataTrackDepacketizerDropReason.BufferFull, frameNumber);
  }
  static incomplete(frameNumber, receivedPackets, expectedPackets) {
    return new _DataTrackDepacketizerDropError("Not all packets received before final packet. Received ".concat(receivedPackets, " packets, expected ").concat(expectedPackets, " packets."), DataTrackDepacketizerDropReason.Incomplete, frameNumber);
  }
};
var DataTrackDepacketizerDropReason;
(function(DataTrackDepacketizerDropReason2) {
  DataTrackDepacketizerDropReason2[DataTrackDepacketizerDropReason2["Interrupted"] = 0] = "Interrupted";
  DataTrackDepacketizerDropReason2[DataTrackDepacketizerDropReason2["UnknownFrame"] = 1] = "UnknownFrame";
  DataTrackDepacketizerDropReason2[DataTrackDepacketizerDropReason2["BufferFull"] = 2] = "BufferFull";
  DataTrackDepacketizerDropReason2[DataTrackDepacketizerDropReason2["Incomplete"] = 3] = "Incomplete";
})(DataTrackDepacketizerDropReason || (DataTrackDepacketizerDropReason = {}));
var DataTrackDepacketizer = class _DataTrackDepacketizer {
  constructor() {
    this.partials = /* @__PURE__ */ new Map();
  }
  /** Should be repeatedly called with received {@link DataTrackPacket}s - intermediate calls
   * aggregate the packet's state internally, and return null.
   *
   * Once this method is called with the final packet to form a frame, a new {@link DataTrackFrameInternal}
   * is returned.*/
  push(packet, options) {
    switch (packet.header.marker) {
      case FrameMarker.Single:
        return this.frameFromSingle(packet, options);
      case FrameMarker.Start:
        return this.beginPartial(packet, options);
      case FrameMarker.Inter:
      case FrameMarker.Final:
        return this.pushToPartial(packet);
    }
  }
  reset() {
    this.partials.clear();
  }
  peekOldestPartialFrameNumber() {
    const first = this.partials.keys().next();
    return first.done ? null : first.value;
  }
  frameFromSingle(packet, options) {
    var _a2;
    if (packet.header.marker !== FrameMarker.Single) {
      throw new Error("Depacketizer.frameFromSingle: packet.header.marker was not FrameMarker.Single, found ".concat(packet.header.marker, "."));
    }
    const maxPartialFrames = (_a2 = options === null || options === void 0 ? void 0 : options.maxPartialFrames) !== null && _a2 !== void 0 ? _a2 : 1;
    if (this.partials.size >= maxPartialFrames) {
      const oldestPartialFrameNumber = this.peekOldestPartialFrameNumber();
      if (typeof oldestPartialFrameNumber !== "number") {
        throw new Error("Depacketizer.frameFromSingle: no oldest frame number found, but partials.size is ".concat(this.partials.size, "."));
      }
      this.partials.delete(oldestPartialFrameNumber);
      if (options === null || options === void 0 ? void 0 : options.throwOnInterruption) {
        throw DataTrackDepacketizerDropError.interrupted(oldestPartialFrameNumber, packet.header.frameNumber.value);
      }
      log$3.warn("Data track frame ".concat(oldestPartialFrameNumber, " was interrupted by single-packet frame ").concat(packet.header.frameNumber.value, ", dropping."));
    }
    return {
      payload: packet.payload,
      extensions: packet.header.extensions
    };
  }
  /** Begin assembling a new packet. */
  beginPartial(packet, options) {
    var _a2;
    if (packet.header.marker !== FrameMarker.Start) {
      throw new Error("Depacketizer.beginPartial: packet.header.marker was not FrameMarker.Start, found ".concat(packet.header.marker, "."));
    }
    const startSequence = packet.header.sequence;
    const frameNumber = packet.header.frameNumber.value;
    const partial = {
      startSequence,
      extensions: packet.header.extensions,
      payloads: /* @__PURE__ */ new Map([[startSequence.value, packet.payload]])
    };
    const maxPartialFrames = (_a2 = options === null || options === void 0 ? void 0 : options.maxPartialFrames) !== null && _a2 !== void 0 ? _a2 : 1;
    while (this.partials.size >= maxPartialFrames) {
      const oldestPartialFrameNumber = this.peekOldestPartialFrameNumber();
      if (typeof oldestPartialFrameNumber !== "number") {
        break;
      }
      this.partials.delete(oldestPartialFrameNumber);
      if (options === null || options === void 0 ? void 0 : options.throwOnInterruption) {
        throw DataTrackDepacketizerDropError.interrupted(oldestPartialFrameNumber, frameNumber);
      }
      log$3.warn("Data track partials full (max ".concat(maxPartialFrames, "), evicted oldest frame ").concat(oldestPartialFrameNumber, " to make room for new frame ").concat(frameNumber, "."));
    }
    this.partials.set(frameNumber, partial);
    return null;
  }
  /** Push to the existing partial frame. */
  pushToPartial(packet) {
    if (packet.header.marker !== FrameMarker.Inter && packet.header.marker !== FrameMarker.Final) {
      throw new Error("Depacketizer.pushToPartial: packet.header.marker was not FrameMarker.Inter or FrameMarker.Final, found ".concat(packet.header.marker, "."));
    }
    const packetFrameNumber = packet.header.frameNumber.value;
    const matchingPartial = this.partials.get(packetFrameNumber);
    if (!matchingPartial) {
      this.partials.delete(packetFrameNumber);
      throw DataTrackDepacketizerDropError.unknownFrame(packetFrameNumber);
    }
    if (matchingPartial.payloads.size >= _DataTrackDepacketizer.MAX_BUFFER_PACKETS) {
      this.partials.delete(packetFrameNumber);
      throw DataTrackDepacketizerDropError.bufferFull(packetFrameNumber);
    }
    if (matchingPartial.payloads.has(packet.header.sequence.value)) {
      log$3.warn("Data track frame ".concat(packetFrameNumber, " received duplicate packet for sequence ").concat(packet.header.sequence.value, ", so replacing with newly received packet."));
    }
    matchingPartial.payloads.set(packet.header.sequence.value, packet.payload);
    if (packet.header.marker === FrameMarker.Final) {
      return this.finalize(packetFrameNumber, matchingPartial, packet.header.sequence.value);
    }
    return null;
  }
  /** Try to reassemble the complete frame. */
  finalize(partialFrameNumber, partial, endSequence) {
    const received = partial.payloads.size;
    let payloadLengthBytes = 0;
    for (const p of partial.payloads.values()) {
      payloadLengthBytes += p.length;
    }
    const payload = new Uint8Array(payloadLengthBytes);
    let sequencePointer = partial.startSequence.clone();
    let payloadOffsetPointerBytes = 0;
    while (true) {
      const partialPayload = partial.payloads.get(sequencePointer.value);
      if (!partialPayload) {
        break;
      }
      partial.payloads.delete(sequencePointer.value);
      const payloadRemainingBytes = payload.length - payloadOffsetPointerBytes;
      if (partialPayload.length > payloadRemainingBytes) {
        throw new Error("Depacketizer.finalize: Expected at least ".concat(partialPayload.length, " more bytes left in the payload buffer, only got ").concat(payloadRemainingBytes, " bytes."));
      }
      payload.set(partialPayload, payloadOffsetPointerBytes);
      payloadOffsetPointerBytes += partialPayload.length;
      if (sequencePointer.value != endSequence) {
        sequencePointer.increment();
        continue;
      }
      this.partials.delete(partialFrameNumber);
      return {
        payload,
        extensions: partial.extensions
      };
    }
    this.partials.delete(partialFrameNumber);
    throw DataTrackDepacketizerDropError.incomplete(partialFrameNumber, received, endSequence - partial.startSequence.value + 1);
  }
};
DataTrackDepacketizer.MAX_BUFFER_PACKETS = 128;
var DataTrackSubscribeErrorReason;
(function(DataTrackSubscribeErrorReason2) {
  DataTrackSubscribeErrorReason2[DataTrackSubscribeErrorReason2["Unpublished"] = 0] = "Unpublished";
  DataTrackSubscribeErrorReason2[DataTrackSubscribeErrorReason2["Timeout"] = 1] = "Timeout";
  DataTrackSubscribeErrorReason2[DataTrackSubscribeErrorReason2["Disconnected"] = 2] = "Disconnected";
  DataTrackSubscribeErrorReason2[DataTrackSubscribeErrorReason2["Cancelled"] = 4] = "Cancelled";
})(DataTrackSubscribeErrorReason || (DataTrackSubscribeErrorReason = {}));
var DataTrackSubscribeError = class _DataTrackSubscribeError extends LivekitReasonedError {
  constructor(message, reason, options) {
    super(22, message, options);
    this.name = "DataTrackSubscribeError";
    this.reason = reason;
    this.reasonName = DataTrackSubscribeErrorReason[reason];
  }
  static unpublished() {
    return new _DataTrackSubscribeError("The track has been unpublished and is no longer available", DataTrackSubscribeErrorReason.Unpublished);
  }
  static timeout() {
    return new _DataTrackSubscribeError("Request to subscribe to data track timed-out", DataTrackSubscribeErrorReason.Timeout);
  }
  static disconnected() {
    return new _DataTrackSubscribeError("Cannot subscribe to data track when disconnected", DataTrackSubscribeErrorReason.Disconnected);
  }
  // NOTE: this was introduced by web / there isn't a corresponding case in the rust version.
  static cancelled() {
    return new _DataTrackSubscribeError("Subscription to data track cancelled by caller", DataTrackSubscribeErrorReason.Cancelled);
  }
};
var log$2 = getLogger(LoggerNames.DataTracks);
var IncomingDataTrackPipeline = class {
  /**
   * Creates a new pipeline with the given options.
   */
  constructor(options) {
    var _a2, _b2;
    const hasProvider = options.e2eeManager !== null;
    if (options.info.usesE2ee !== hasProvider) {
      throw new Error("IncomingDataTrackPipeline: DataTrackInfo.usesE2ee must match presence of decryptionProvider");
    }
    const depacketizer = new DataTrackDepacketizer();
    this.publisherIdentity = options.publisherIdentity;
    this.e2eeManager = (_a2 = options.e2eeManager) !== null && _a2 !== void 0 ? _a2 : null;
    this.depacketizer = depacketizer;
    this.options = (_b2 = options.pipelineOptions) !== null && _b2 !== void 0 ? _b2 : {};
  }
  updateE2eeManager(e2eeManager) {
    this.e2eeManager = e2eeManager;
  }
  setOptions(options) {
    this.options = options;
  }
  processPacket(packet) {
    return __awaiter(this, void 0, void 0, function* () {
      const frame = this.depacketize(packet);
      if (!frame) {
        return null;
      }
      const decrypted = yield this.decryptIfNeeded(frame);
      if (!decrypted) {
        return null;
      }
      return decrypted;
    });
  }
  /**
   * Depacketize the given frame, log if a drop occurs.
   */
  depacketize(packet) {
    let frame;
    try {
      frame = this.depacketizer.push(packet, {
        throwOnInterruption: false,
        maxPartialFrames: this.options.maxPartialFrames
      });
    } catch (err) {
      log$2.warn("Data frame depacketize error: ".concat(err));
      return null;
    }
    return frame;
  }
  /**
   * Decrypt the frame's payload if E2EE is enabled for this track.
   */
  decryptIfNeeded(frame) {
    return __awaiter(this, void 0, void 0, function* () {
      var _a2, _b2;
      const e2eeManager = this.e2eeManager;
      if (!e2eeManager) {
        return frame;
      }
      const e2ee = (_b2 = (_a2 = frame.extensions) === null || _a2 === void 0 ? void 0 : _a2.e2ee) !== null && _b2 !== void 0 ? _b2 : null;
      if (!e2ee) {
        log$2.error("Missing E2EE meta");
        return null;
      }
      let result;
      try {
        result = yield e2eeManager.handleEncryptedData(frame.payload, e2ee.iv, this.publisherIdentity, e2ee.keyIndex);
      } catch (err) {
        log$2.error("Error decrypting packet: ".concat(err));
        return null;
      }
      frame.payload = result.payload;
      return frame;
    });
  }
};
var log$1 = getLogger(LoggerNames.DataTracks);
var SUBSCRIBE_TIMEOUT_MILLISECONDS = 1e4;
var READABLE_STREAM_DEFAULT_BUFFER_SIZE = 16;
var IncomingDataTrackManager = class extends eventsExports.EventEmitter {
  constructor(options) {
    var _a2;
    super();
    this.descriptors = /* @__PURE__ */ new Map();
    this.subscriptionHandles = /* @__PURE__ */ new Map();
    this.e2eeManager = (_a2 = options === null || options === void 0 ? void 0 : options.e2eeManager) !== null && _a2 !== void 0 ? _a2 : null;
  }
  /** @internal */
  updateE2eeManager(e2eeManager) {
    this.e2eeManager = e2eeManager;
    for (const descriptor of this.descriptors.values()) {
      if (descriptor.subscription.type === "active") {
        descriptor.subscription.pipeline.updateE2eeManager(e2eeManager);
      }
    }
  }
  /** @internal */
  setPipelineOptions(sid, options) {
    const descriptor = this.descriptors.get(sid);
    if (!descriptor) {
      log$1.warn("Unknown track ".concat(sid, ", cannot set pipeline options."));
      return;
    }
    descriptor.pipelineOptions = options;
    if (descriptor.subscription.type === "active") {
      descriptor.subscription.pipeline.setOptions(options);
    }
  }
  /** Allocates a ReadableStream which emits when a new {@link DataTrackFrame} is received from the
   * SFU. The SFU subscription is initiated lazily when the stream is created.
   *
   * @returns A tuple of the ReadableStream and a Promise that resolves once the SFU subscription
   * is fully established / the stream is ready to receive frames.
   *
   * @internal
   **/
  openSubscriptionStream(sid, signal) {
    let bufferSize = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : READABLE_STREAM_DEFAULT_BUFFER_SIZE;
    let streamController = null;
    const sfuSubscriptionComplete = new Future();
    const descriptor = this.descriptors.get(sid);
    const detachSignal = () => {
      signal === null || signal === void 0 ? void 0 : signal.removeEventListener("abort", onAbort);
    };
    const cleanup = () => {
      detachSignal();
      if (!streamController) {
        log$1.warn("ReadableStream subscribed to ".concat(sid, " was not started."));
        return;
      }
      if (!descriptor || this.descriptors.get(descriptor.info.sid) !== descriptor) {
        log$1.warn("Unknown track ".concat(sid, ", skipping cancel..."));
        return;
      }
      if (descriptor.subscription.type !== "active") {
        log$1.warn("Subscription for track ".concat(sid, " is not active, skipping cancel..."));
        return;
      }
      descriptor.subscription.streamControllers.delete(streamController);
      if (descriptor.subscription.streamControllers.size === 0) {
        this.unSubscribeRequest(descriptor.info.sid);
      }
    };
    const onAbort = () => {
      var _a2;
      if (!streamController) {
        return;
      }
      if ((descriptor === null || descriptor === void 0 ? void 0 : descriptor.subscription.type) === "active") {
        descriptor.subscription.streamControllers.delete(streamController);
      }
      streamController.error(DataTrackSubscribeError.cancelled());
      (_a2 = sfuSubscriptionComplete.reject) === null || _a2 === void 0 ? void 0 : _a2.call(sfuSubscriptionComplete, DataTrackSubscribeError.cancelled());
      cleanup();
    };
    const stream = new ReadableStream({
      start: (controller) => {
        streamController = controller;
        this.subscribeRequest(sid, signal).then(() => __awaiter(this, void 0, void 0, function* () {
          var _a2, _b2, _c;
          if (!descriptor || this.descriptors.get(descriptor.info.sid) !== descriptor) {
            log$1.error("Unknown track ".concat(sid));
            const err = DataTrackSubscribeError.disconnected();
            controller.error(err);
            (_a2 = sfuSubscriptionComplete.reject) === null || _a2 === void 0 ? void 0 : _a2.call(sfuSubscriptionComplete, err);
            return;
          }
          if (descriptor.subscription.type !== "active") {
            log$1.error("Subscription for track ".concat(sid, " is not active"));
            const err = DataTrackSubscribeError.disconnected();
            controller.error(err);
            (_b2 = sfuSubscriptionComplete.reject) === null || _b2 === void 0 ? void 0 : _b2.call(sfuSubscriptionComplete, err);
            return;
          }
          if (signal === null || signal === void 0 ? void 0 : signal.aborted) {
            onAbort();
            return;
          }
          signal === null || signal === void 0 ? void 0 : signal.addEventListener("abort", onAbort);
          descriptor.subscription.streamControllers.set(controller, detachSignal);
          (_c = sfuSubscriptionComplete.resolve) === null || _c === void 0 ? void 0 : _c.call(sfuSubscriptionComplete);
        })).catch((err) => {
          var _a2;
          controller.error(err);
          (_a2 = sfuSubscriptionComplete.reject) === null || _a2 === void 0 ? void 0 : _a2.call(sfuSubscriptionComplete, err);
        });
      },
      cancel: () => {
        cleanup();
      }
    }, new CountQueuingStrategy({
      highWaterMark: bufferSize
    }));
    return [stream, sfuSubscriptionComplete.promise];
  }
  /** Client requested to subscribe to a data track.
   *
   * This is sent when the user calls {@link RemoteDataTrack.subscribe}.
   *
   * Only the first request to subscribe to a given track incurs meaningful overhead; subsequent
   * requests simply attach an additional receiver to the broadcast channel, allowing them to consume
   * frames from the existing subscription pipeline.
   */
  subscribeRequest(sid, signal) {
    return __awaiter(this, void 0, void 0, function* () {
      const descriptor = this.descriptors.get(sid);
      if (!descriptor) {
        throw new Error("Cannot subscribe to unknown track");
      }
      const waitForCompletionFuture = (currentDescriptor, userProvidedSignal, timeoutSignal) => __awaiter(this, void 0, void 0, function* () {
        if (currentDescriptor.subscription.type === "active") {
          return;
        }
        if (currentDescriptor.subscription.type !== "pending") {
          throw new Error("Descriptor for track ".concat(sid, " is not pending, found ").concat(currentDescriptor.subscription.type));
        }
        const combinedSignal = abortSignalAny([userProvidedSignal, timeoutSignal].filter((s) => typeof s !== "undefined"));
        const proxiedCompletionFuture = new Future();
        currentDescriptor.subscription.completionFuture.promise.then(() => {
          var _a2;
          return (_a2 = proxiedCompletionFuture.resolve) === null || _a2 === void 0 ? void 0 : _a2.call(proxiedCompletionFuture);
        }).catch((err) => {
          var _a2;
          return (_a2 = proxiedCompletionFuture.reject) === null || _a2 === void 0 ? void 0 : _a2.call(proxiedCompletionFuture, err);
        });
        const onAbort = () => {
          var _a2;
          if (currentDescriptor.subscription.type !== "pending") {
            return;
          }
          currentDescriptor.subscription.pendingRequestCount -= 1;
          if (timeoutSignal === null || timeoutSignal === void 0 ? void 0 : timeoutSignal.aborted) {
            currentDescriptor.subscription.cancel();
            return;
          }
          if (currentDescriptor.subscription.pendingRequestCount <= 0) {
            currentDescriptor.subscription.cancel();
            return;
          }
          (_a2 = proxiedCompletionFuture.reject) === null || _a2 === void 0 ? void 0 : _a2.call(proxiedCompletionFuture, DataTrackSubscribeError.cancelled());
        };
        if (combinedSignal.aborted) {
          onAbort();
        }
        combinedSignal.addEventListener("abort", onAbort);
        yield proxiedCompletionFuture.promise;
        combinedSignal.removeEventListener("abort", onAbort);
      });
      switch (descriptor.subscription.type) {
        case "none": {
          descriptor.subscription = {
            type: "pending",
            completionFuture: new Future(),
            pendingRequestCount: 1,
            cancel: () => {
              var _a2, _b2;
              const previousDescriptorSubscription = descriptor.subscription;
              descriptor.subscription = {
                type: "none"
              };
              this.emit("sfuUpdateSubscription", {
                sid: descriptor.info.sid,
                subscribe: false
              });
              if (previousDescriptorSubscription.type === "pending") {
                (_b2 = (_a2 = previousDescriptorSubscription.completionFuture).reject) === null || _b2 === void 0 ? void 0 : _b2.call(_a2, timeoutSignal.aborted ? DataTrackSubscribeError.timeout() : (
                  // NOTE: the below cancelled case was introduced by web / there isn't a corresponding case in the rust version.
                  DataTrackSubscribeError.cancelled()
                ));
              }
            }
          };
          this.emit("sfuUpdateSubscription", {
            sid,
            subscribe: true
          });
          const timeoutSignal = abortSignalTimeout(SUBSCRIBE_TIMEOUT_MILLISECONDS);
          yield waitForCompletionFuture(descriptor, signal, timeoutSignal);
          return;
        }
        case "pending": {
          descriptor.subscription.pendingRequestCount += 1;
          yield waitForCompletionFuture(descriptor, signal);
          return;
        }
        case "active": {
          return;
        }
      }
    });
  }
  /**
   * Get information about all currently subscribed tracks.
   * @internal */
  querySubscribed() {
    return __awaiter(this, void 0, void 0, function* () {
      const descriptorInfos = Array.from(this.descriptors.values()).filter((descriptor) => descriptor.subscription.type === "active").map((descriptor) => [descriptor.info, descriptor.publisherIdentity]);
      return descriptorInfos;
    });
  }
  /** Client requested to unsubscribe from a data track. */
  unSubscribeRequest(sid) {
    var _a2;
    const descriptor = this.descriptors.get(sid);
    if (!descriptor) {
      throw new Error("Cannot subscribe to unknown track");
    }
    if (descriptor.subscription.type !== "active") {
      log$1.warn("Unexpected descriptor state in unSubscribeRequest, expected active, found ".concat((_a2 = descriptor.subscription) === null || _a2 === void 0 ? void 0 : _a2.type));
      return;
    }
    this.closeStreamControllers(descriptor.subscription.streamControllers, sid);
    const previousDescriptorSubscription = descriptor.subscription;
    descriptor.subscription = {
      type: "none"
    };
    this.subscriptionHandles.delete(previousDescriptorSubscription.subcriptionHandle);
    this.emit("sfuUpdateSubscription", {
      sid,
      subscribe: false
    });
  }
  /** Detach abort-signal listeners and close all downstream stream controllers for an active
   * subscription. Used when the subscription is being torn down by the manager (unsubscribe,
   * unpublish, or shutdown). */
  closeStreamControllers(streamControllers, sid) {
    for (const _ref of streamControllers) {
      var _ref2 = _slicedToArray(_ref, 2);
      const controller = _ref2[0];
      const detachSignal = _ref2[1];
      detachSignal();
      try {
        controller.close();
      } catch (err) {
        log$1.warn("Failed to close readable stream for track ".concat(sid, ": ").concat(err));
      }
    }
  }
  /** SFU notification that track publications have changed.
   *
   * This event is produced from both {@link JoinResponse} and {@link ParticipantUpdate}
   * to provide a complete view of remote participants' track publications:
   *
   * - From a `JoinResponse`, it captures the initial set of tracks published when a participant joins.
   * - From a `ParticipantUpdate`, it captures subsequent changes (i.e., new tracks being
   *   published and existing tracks unpublished).
   */
  receiveSfuPublicationUpdates(updates) {
    return __awaiter(this, void 0, void 0, function* () {
      if (updates.size === 0) {
        return;
      }
      const publisherParticipantToSidsInUpdate = /* @__PURE__ */ new Map();
      for (const _ref3 of updates.entries()) {
        var _ref4 = _slicedToArray(_ref3, 2);
        const publisherIdentity = _ref4[0];
        const infos = _ref4[1];
        const sidsInUpdate = /* @__PURE__ */ new Set();
        for (const info of infos) {
          sidsInUpdate.add(info.sid);
          if (this.descriptors.has(info.sid)) {
            continue;
          }
          if (this.handleSidReassigned(publisherIdentity, info)) {
            continue;
          }
          yield this.handleTrackPublished(publisherIdentity, info);
        }
        publisherParticipantToSidsInUpdate.set(publisherIdentity, sidsInUpdate);
      }
      for (const _ref5 of publisherParticipantToSidsInUpdate.entries()) {
        var _ref6 = _slicedToArray(_ref5, 2);
        const publisherIdentity = _ref6[0];
        const sidsInUpdate = _ref6[1];
        const descriptorsForPublisher = Array.from(this.descriptors.entries()).filter((_ref7) => {
          let _ref8 = _slicedToArray(_ref7, 2);
          _ref8[0];
          let descriptor = _ref8[1];
          return descriptor.publisherIdentity === publisherIdentity;
        }).map((_ref9) => {
          let _ref0 = _slicedToArray(_ref9, 1), sid = _ref0[0];
          return sid;
        });
        let unpublishedSids = descriptorsForPublisher.filter((sid) => !sidsInUpdate.has(sid));
        for (const sid of unpublishedSids) {
          this.handleTrackUnpublished(sid);
        }
      }
    });
  }
  /**
   * Get information about all currently remotely published tracks which could be subscribed to.
   * @internal */
  queryPublications() {
    return __awaiter(this, void 0, void 0, function* () {
      return Array.from(this.descriptors.values()).map((descriptor) => descriptor.info);
    });
  }
  handleTrackPublished(publisherIdentity, info) {
    return __awaiter(this, void 0, void 0, function* () {
      if (this.descriptors.has(info.sid)) {
        log$1.error("Existing descriptor for track ".concat(info.sid));
        return;
      }
      let descriptor = {
        info,
        publisherIdentity,
        subscription: {
          type: "none"
        },
        pipelineOptions: {}
      };
      this.descriptors.set(descriptor.info.sid, descriptor);
      const track = new RemoteDataTrack(descriptor.info, this, {
        publisherIdentity
      });
      this.emit("trackPublished", {
        track
      });
    });
  }
  /**
   * Detects and handles SID reassignment, which occurs when the publisher
   * republishes its tracks after a full reconnect.
   *
   * Returns `true` if an SID reassignment occurred, `false` otherwise.
   */
  handleSidReassigned(publisherIdentity, info) {
    const existingEntry = Array.from(this.descriptors.entries()).find((_ref1) => {
      let _ref10 = _slicedToArray(_ref1, 2);
      _ref10[0];
      let descriptor2 = _ref10[1];
      return descriptor2.publisherIdentity === publisherIdentity && descriptor2.info.pubHandle === info.pubHandle;
    });
    if (!existingEntry) {
      return false;
    }
    const _existingEntry = _slicedToArray(existingEntry, 2), oldSid = _existingEntry[0], descriptor = _existingEntry[1];
    const _descriptor$info = descriptor.info, name = _descriptor$info.name, usesE2ee = _descriptor$info.usesE2ee;
    if (name !== info.name || usesE2ee !== info.usesE2ee) {
      log$1.warn("Info mismatch for ".concat(oldSid, ", treating as new publication"));
      return false;
    }
    const newSid = info.sid;
    log$1.debug("SID reassigned: ".concat(oldSid, " -> ").concat(newSid));
    if (!this.descriptors.delete(oldSid)) {
      return false;
    }
    descriptor.info.sid = newSid;
    switch (descriptor.subscription.type) {
      case "none":
        break;
      case "pending":
      case "active":
        this.emit("sfuUpdateSubscription", {
          sid: newSid,
          subscribe: true
        });
        break;
    }
    if (descriptor.subscription.type === "active") {
      this.subscriptionHandles.set(descriptor.subscription.subcriptionHandle, newSid);
    }
    this.descriptors.set(newSid, descriptor);
    return true;
  }
  handleTrackUnpublished(sid) {
    const descriptor = this.descriptors.get(sid);
    if (!descriptor) {
      log$1.error("Unknown track ".concat(sid));
      return;
    }
    this.descriptors.delete(sid);
    if (descriptor.subscription.type === "active") {
      this.closeStreamControllers(descriptor.subscription.streamControllers, sid);
      this.subscriptionHandles.delete(descriptor.subscription.subcriptionHandle);
    }
    this.emit("trackUnpublished", {
      sid,
      publisherIdentity: descriptor.publisherIdentity
    });
  }
  /** SFU notification that handles have been assigned for requested subscriptions. */
  receivedSfuSubscriberHandles(mapping) {
    for (const _ref11 of mapping.entries()) {
      var _ref12 = _slicedToArray(_ref11, 2);
      const handle = _ref12[0];
      const sid = _ref12[1];
      this.registerSubscriberHandle(handle, sid);
    }
  }
  registerSubscriberHandle(assignedHandle, sid) {
    var _a2, _b2;
    const descriptor = this.descriptors.get(sid);
    if (!descriptor) {
      log$1.error("Unknown track ".concat(sid));
      return;
    }
    switch (descriptor.subscription.type) {
      case "none": {
        log$1.warn("No subscription for ".concat(sid));
        return;
      }
      case "active": {
        this.subscriptionHandles.delete(descriptor.subscription.subcriptionHandle);
        descriptor.subscription.subcriptionHandle = assignedHandle;
        this.subscriptionHandles.set(assignedHandle, sid);
        return;
      }
      case "pending": {
        log$1.debug("data track subscription activated", {
          sid,
          handle: assignedHandle
        });
        const pipeline = new IncomingDataTrackPipeline({
          info: descriptor.info,
          publisherIdentity: descriptor.publisherIdentity,
          e2eeManager: this.e2eeManager,
          pipelineOptions: descriptor.pipelineOptions
        });
        const previousDescriptorSubscription = descriptor.subscription;
        descriptor.subscription = {
          type: "active",
          subcriptionHandle: assignedHandle,
          pipeline,
          streamControllers: /* @__PURE__ */ new Map()
        };
        this.subscriptionHandles.set(assignedHandle, sid);
        (_b2 = (_a2 = previousDescriptorSubscription.completionFuture).resolve) === null || _b2 === void 0 ? void 0 : _b2.call(_a2);
      }
    }
  }
  /** Packet has been received over the transport. */
  packetReceived(bytes) {
    return __awaiter(this, void 0, void 0, function* () {
      let packet;
      try {
        var _DataTrackPacket$from = DataTrackPacket.fromBinary(bytes);
        var _DataTrackPacket$from2 = _slicedToArray(_DataTrackPacket$from, 1);
        packet = _DataTrackPacket$from2[0];
      } catch (err) {
        log$1.error("Failed to deserialize packet: ".concat(err));
        return;
      }
      const sid = this.subscriptionHandles.get(packet.header.trackHandle);
      if (!sid) {
        log$1.warn("Unknown subscriber handle ".concat(packet.header.trackHandle));
        return;
      }
      const descriptor = this.descriptors.get(sid);
      if (!descriptor) {
        log$1.error("Missing descriptor for track ".concat(sid));
        return;
      }
      if (descriptor.subscription.type !== "active") {
        log$1.warn("Received packet for track ".concat(sid, " without active subscription"));
        return;
      }
      const internalFrame = yield descriptor.subscription.pipeline.processPacket(packet);
      if (!internalFrame) {
        return;
      }
      for (const controller of descriptor.subscription.streamControllers.keys()) {
        if (controller.desiredSize !== null && controller.desiredSize <= 0) {
          log$1.warn("Cannot send frame to subscribers: readable stream is full (desiredSize is ".concat(controller.desiredSize, "). To increase this threshold, set a higher 'options.highWaterMark' when calling .subscribe()."));
          continue;
        }
        const frame = DataTrackFrameInternal.lossyIntoFrame(internalFrame);
        controller.enqueue(frame);
      }
    });
  }
  /** Resend all subscription updates.
   *
   * This must be sent after a full reconnect to ensure the SFU knows which
   * tracks are subscribed to locally.
   */
  resendSubscriptionUpdates() {
    for (const _ref13 of this.descriptors) {
      var _ref14 = _slicedToArray(_ref13, 2);
      const sid = _ref14[0];
      const descriptor = _ref14[1];
      if (descriptor.subscription.type === "none") {
        continue;
      }
      this.emit("sfuUpdateSubscription", {
        sid,
        subscribe: true
      });
    }
  }
  /** Called when a remote participant is disconnected so that any pending data tracks can be
   * cancelled. */
  handleRemoteParticipantDisconnected(remoteParticipantIdentity) {
    var _a2, _b2;
    for (const descriptor of this.descriptors.values()) {
      if (descriptor.publisherIdentity !== remoteParticipantIdentity) {
        continue;
      }
      switch (descriptor.subscription.type) {
        case "none":
          break;
        case "pending":
          (_b2 = (_a2 = descriptor.subscription.completionFuture).reject) === null || _b2 === void 0 ? void 0 : _b2.call(_a2, DataTrackSubscribeError.disconnected());
          break;
        case "active":
          this.unSubscribeRequest(descriptor.info.sid);
          break;
      }
    }
  }
  /** Resets the manager, ending any subscriptions, and getting it ready for the next room
   * connection.  */
  reset() {
    var _a2, _b2;
    for (const descriptor of this.descriptors.values()) {
      this.emit("trackUnpublished", {
        sid: descriptor.info.sid,
        publisherIdentity: descriptor.publisherIdentity
      });
      if (descriptor.subscription.type === "pending") {
        (_b2 = (_a2 = descriptor.subscription.completionFuture).reject) === null || _b2 === void 0 ? void 0 : _b2.call(_a2, DataTrackSubscribeError.disconnected());
      }
      if (descriptor.subscription.type === "active") {
        this.closeStreamControllers(descriptor.subscription.streamControllers, descriptor.info.sid);
      }
    }
    this.descriptors.clear();
    this.subscriptionHandles.clear();
  }
};
var DataTrackPacketizerError = class _DataTrackPacketizerError extends LivekitReasonedError {
  constructor(message, reason, options) {
    super(19, message, options);
    this.name = "DataTrackPacketizerError";
    this.reason = reason;
    this.reasonName = DataTrackPacketizerReason[reason];
  }
  static mtuTooShort() {
    return new _DataTrackPacketizerError("MTU is too short to send frame", DataTrackPacketizerReason.MtuTooShort);
  }
};
var DataTrackPacketizerReason;
(function(DataTrackPacketizerReason2) {
  DataTrackPacketizerReason2[DataTrackPacketizerReason2["MtuTooShort"] = 0] = "MtuTooShort";
})(DataTrackPacketizerReason || (DataTrackPacketizerReason = {}));
var DataTrackPacketizer = class _DataTrackPacketizer {
  constructor(trackHandle, mtuSizeBytes) {
    this.sequence = WrapAroundUnsignedInt.u16(0);
    this.frameNumber = WrapAroundUnsignedInt.u16(0);
    this.clock = DataTrackClock.rtpStartingNow(DataTrackTimestamp.rtpRandom());
    this.handle = trackHandle;
    this.mtuSizeBytes = mtuSizeBytes;
  }
  /** @internal */
  static computeFrameMarker(index, packetCount) {
    if (packetCount <= 1) {
      return FrameMarker.Single;
    }
    if (index === 0) {
      return FrameMarker.Start;
    } else if (index === packetCount - 1) {
      return FrameMarker.Final;
    } else {
      return FrameMarker.Inter;
    }
  }
  /** Generates a series of packets for the specified {@link DataTrackFrameInternal}.
   *
   * NOTE: The return value of this function is a generator, so it can be lazily ran if desired,
   * or converted to an array with {@link Array.from}.
   */
  *packetize(frame, options) {
    var _a2;
    const frameNumber = this.frameNumber.getThenIncrement();
    const headerParams = {
      marker: FrameMarker.Inter,
      trackHandle: this.handle,
      sequence: WrapAroundUnsignedInt.u16(0),
      frameNumber,
      timestamp: (_a2 = options === null || options === void 0 ? void 0 : options.now) !== null && _a2 !== void 0 ? _a2 : this.clock.now(),
      extensions: frame.extensions
    };
    const headerSerializedLengthBytes = new DataTrackPacketHeader(headerParams).toBinaryLengthBytes();
    if (headerSerializedLengthBytes >= this.mtuSizeBytes) {
      throw DataTrackPacketizerError.mtuTooShort();
    }
    const maxPayloadSizeBytes = this.mtuSizeBytes - headerSerializedLengthBytes;
    const packetCount = Math.ceil(frame.payload.byteLength / maxPayloadSizeBytes);
    for (let index = 0, indexBytes = 0; indexBytes < frame.payload.byteLength; _ref = [index + 1, indexBytes + maxPayloadSizeBytes], index = _ref[0], indexBytes = _ref[1], _ref) {
      var _ref;
      const sequence = this.sequence.getThenIncrement();
      const packetHeader = new DataTrackPacketHeader(Object.assign(Object.assign({}, headerParams), {
        marker: _DataTrackPacketizer.computeFrameMarker(index, packetCount),
        sequence
      }));
      const packetPayloadLengthBytes = Math.min(
        // All but the last packet will be max length ...
        maxPayloadSizeBytes,
        // ... and the last packet will be as long as it needs to be to finish out the buffer.
        frame.payload.byteLength - indexBytes
      );
      const packetPayload = new Uint8Array(frame.payload.buffer, frame.payload.byteOffset + indexBytes, packetPayloadLengthBytes);
      yield new DataTrackPacket(packetHeader, packetPayload);
    }
  }
};
var DataTrackPublishErrorReason;
(function(DataTrackPublishErrorReason2) {
  DataTrackPublishErrorReason2[DataTrackPublishErrorReason2["NotAllowed"] = 0] = "NotAllowed";
  DataTrackPublishErrorReason2[DataTrackPublishErrorReason2["DuplicateName"] = 1] = "DuplicateName";
  DataTrackPublishErrorReason2[DataTrackPublishErrorReason2["Timeout"] = 2] = "Timeout";
  DataTrackPublishErrorReason2[DataTrackPublishErrorReason2["LimitReached"] = 3] = "LimitReached";
  DataTrackPublishErrorReason2[DataTrackPublishErrorReason2["Disconnected"] = 4] = "Disconnected";
  DataTrackPublishErrorReason2[DataTrackPublishErrorReason2["Cancelled"] = 5] = "Cancelled";
  DataTrackPublishErrorReason2[DataTrackPublishErrorReason2["InvalidName"] = 6] = "InvalidName";
  DataTrackPublishErrorReason2[DataTrackPublishErrorReason2["Unknown"] = 7] = "Unknown";
})(DataTrackPublishErrorReason || (DataTrackPublishErrorReason = {}));
var DataTrackPublishError = class _DataTrackPublishError extends LivekitReasonedError {
  constructor(message, reason, options) {
    super(21, message, options);
    this.name = "DataTrackPublishError";
    this.reason = reason;
    this.reasonName = DataTrackPublishErrorReason[reason];
    this.rawMessage = options === null || options === void 0 ? void 0 : options.rawMessage;
  }
  static notAllowed(rawMessage) {
    return new _DataTrackPublishError("Data track publishing unauthorized", DataTrackPublishErrorReason.NotAllowed, {
      rawMessage
    });
  }
  static duplicateName(rawMessage) {
    return new _DataTrackPublishError("Track name already taken", DataTrackPublishErrorReason.DuplicateName, {
      rawMessage
    });
  }
  static invalidName(rawMessage) {
    return new _DataTrackPublishError("Track name is invalid", DataTrackPublishErrorReason.InvalidName, {
      rawMessage
    });
  }
  static timeout() {
    return new _DataTrackPublishError("Publish data track timed-out. Does the LiveKit server support data tracks?", DataTrackPublishErrorReason.Timeout);
  }
  static limitReached(rawMessage) {
    return new _DataTrackPublishError("Data track publication limit reached", DataTrackPublishErrorReason.LimitReached, {
      rawMessage
    });
  }
  static unknown(reason, message) {
    return new _DataTrackPublishError("Received RequestResponse for publishDataTrack, but reason was unrecognised (".concat(reason, ", ").concat(message, ")"), DataTrackPublishErrorReason.Unknown);
  }
  static disconnected() {
    return new _DataTrackPublishError("Room disconnected", DataTrackPublishErrorReason.Disconnected);
  }
  // NOTE: this was introduced by web / there isn't a corresponding case in the rust version.
  static cancelled() {
    return new _DataTrackPublishError("Publish data track cancelled by caller", DataTrackPublishErrorReason.Cancelled);
  }
};
var DataTrackPushFrameErrorReason;
(function(DataTrackPushFrameErrorReason2) {
  DataTrackPushFrameErrorReason2[DataTrackPushFrameErrorReason2["TrackUnpublished"] = 0] = "TrackUnpublished";
  DataTrackPushFrameErrorReason2[DataTrackPushFrameErrorReason2["Dropped"] = 1] = "Dropped";
})(DataTrackPushFrameErrorReason || (DataTrackPushFrameErrorReason = {}));
var DataTrackPushFrameError = class _DataTrackPushFrameError extends LivekitReasonedError {
  constructor(message, reason, options) {
    super(22, message, options);
    this.name = "DataTrackPushFrameError";
    this.reason = reason;
    this.reasonName = DataTrackPushFrameErrorReason[reason];
  }
  static trackUnpublished() {
    return new _DataTrackPushFrameError("Track is no longer published", DataTrackPushFrameErrorReason.TrackUnpublished);
  }
  static dropped(cause) {
    return new _DataTrackPushFrameError("Frame was dropped", DataTrackPushFrameErrorReason.Dropped, {
      cause
    });
  }
};
var DataTrackOutgoingPipelineErrorReason;
(function(DataTrackOutgoingPipelineErrorReason2) {
  DataTrackOutgoingPipelineErrorReason2[DataTrackOutgoingPipelineErrorReason2["Packetizer"] = 0] = "Packetizer";
  DataTrackOutgoingPipelineErrorReason2[DataTrackOutgoingPipelineErrorReason2["Encryption"] = 1] = "Encryption";
})(DataTrackOutgoingPipelineErrorReason || (DataTrackOutgoingPipelineErrorReason = {}));
var DataTrackOutgoingPipelineError = class _DataTrackOutgoingPipelineError extends LivekitReasonedError {
  constructor(message, reason, options) {
    super(21, message, options);
    this.name = "DataTrackOutgoingPipelineError";
    this.reason = reason;
    this.reasonName = DataTrackOutgoingPipelineErrorReason[reason];
  }
  static packetizer(cause) {
    return new _DataTrackOutgoingPipelineError("Error packetizing frame", DataTrackOutgoingPipelineErrorReason.Packetizer, {
      cause
    });
  }
  static encryption(cause) {
    return new _DataTrackOutgoingPipelineError("Error encrypting frame", DataTrackOutgoingPipelineErrorReason.Encryption, {
      cause
    });
  }
};
var LocalDataTrack = class _LocalDataTrack {
  /** @internal */
  constructor(options, manager) {
    this.trackSymbol = TrackSymbol;
    this.isLocal = true;
    this.typeSymbol = DataTrackSymbol;
    this.handle = null;
    this.log = livekitLogger;
    this.flushedFuture = new Future();
    this.isFlushed = true;
    this.handleManagerReset = () => {
      var _a2, _b2;
      (_b2 = (_a2 = this.flushedFuture).resolve) === null || _b2 === void 0 ? void 0 : _b2.call(_a2);
      this.manager.off("packetsFlushedChange", this.handleManagerPacketsFlushedChange);
      this.manager.off("reset", this.handleManagerReset);
    };
    this.handleManagerPacketsFlushedChange = (event) => {
      var _a2, _b2;
      this.isFlushed = event.isFlushed;
      if (event.isFlushed) {
        (_b2 = (_a2 = this.flushedFuture).resolve) === null || _b2 === void 0 ? void 0 : _b2.call(_a2);
        this.flushedFuture = new Future();
      }
    };
    this.options = options;
    this.manager = manager;
    this.log = getLogger(LoggerNames.DataTracks);
    this.manager.on("packetsFlushedChange", this.handleManagerPacketsFlushedChange);
    this.manager.on("reset", this.handleManagerReset);
  }
  /** @internal */
  static withExplicitHandle(options, manager, handle) {
    const track = new _LocalDataTrack(options, manager);
    track.handle = handle;
    return track;
  }
  /** Metrics about the data track publication. */
  get info() {
    const descriptor = this.descriptor;
    if ((descriptor === null || descriptor === void 0 ? void 0 : descriptor.type) === "active") {
      return descriptor.info;
    } else {
      return void 0;
    }
  }
  /** The raw descriptor from the manager containing the internal state for this local track. */
  get descriptor() {
    return this.handle ? this.manager.getDescriptor(this.handle) : null;
  }
  /**
   * Publish the track to the SFU. This must be done before calling {@link tryPush} for the first time.
   * @internal
   * */
  publish(signal) {
    return __awaiter(this, void 0, void 0, function* () {
      try {
        this.handle = yield this.manager.publishRequest(this.options, signal);
      } catch (err) {
        throw err;
      }
    });
  }
  isPublished() {
    var _a2;
    return ((_a2 = this.descriptor) === null || _a2 === void 0 ? void 0 : _a2.type) === "active" && this.descriptor.publishState !== "unpublished";
  }
  /** Try pushing a frame to subscribers of the track.
   *
   * Pushing a frame can fail for several reasons:
   *
   * - The track has been unpublished by the local participant or SFU
   * - The room is no longer connected
   */
  tryPush(frame) {
    if (!this.handle) {
      throw DataTrackPushFrameError.trackUnpublished();
    }
    const internalFrame = DataTrackFrameInternal.from(frame);
    try {
      return this.manager.tryProcessAndSend(this.handle, internalFrame);
    } catch (err) {
      throw err;
    }
  }
  /**
   * When called, waits for all in flight packets to be sent before resolving.
   *
   * Use this to:
   *
   * 1. Send frames exactly in order:
   * ```ts
   * await track.tryPush(/* ... *\/);
   * await track.flush();
   * await track.tryPush(/* ... *\/);
   * await track.flush();
   * // ... etc ...
   * ```
   *
   * 2. Wait for frames to all be delivered before unpublishing a local data track:
   *
   * ```ts
   * await track.tryPush(/* ... *\/);
   * await track.tryPush(/* ... *\/);
   * await track.tryPush(/* ... *\/);
   * // ... etc ...
   * await track.flush();
   * await track.unpublish();
   * ```
   **/
  flush() {
    return __awaiter(this, void 0, void 0, function* () {
      if (this.isFlushed) {
        return;
      }
      return this.flushedFuture.promise;
    });
  }
  /**
   * Unpublish the track from the SFU. Once this is called, any further calls to {@link tryPush}
   * will fail.
   * */
  unpublish() {
    return __awaiter(this, void 0, void 0, function* () {
      if (!this.handle) {
        livekitLogger.warn('Data track "'.concat(this.options.name, '" is not published, so unpublishing has no effect.'));
        return;
      }
      try {
        yield this.manager.unpublishRequest(this.handle);
      } catch (err) {
        throw err;
      }
    });
  }
};
var DataTrackOutgoingPipeline = class _DataTrackOutgoingPipeline {
  constructor(options) {
    this.e2eeManager = options.e2eeManager;
    this.packetizer = new DataTrackPacketizer(options.info.pubHandle, _DataTrackOutgoingPipeline.TRANSPORT_MTU_BYTES);
  }
  updateE2eeManager(e2eeManager) {
    this.e2eeManager = e2eeManager;
  }
  processFrame(frame) {
    return __asyncGenerator(this, arguments, function* processFrame_1() {
      const encryptedFrame = yield __await(this.encryptIfNeeded(frame));
      try {
        yield __await(yield* __asyncDelegator(__asyncValues(this.packetizer.packetize(encryptedFrame))));
      } catch (error) {
        if (error instanceof DataTrackPacketizerError) {
          throw DataTrackOutgoingPipelineError.packetizer(error);
        }
        throw error;
      }
    });
  }
  encryptIfNeeded(frame) {
    return __awaiter(this, void 0, void 0, function* () {
      if (!this.e2eeManager) {
        return frame;
      }
      let encryptedResult;
      try {
        encryptedResult = yield this.e2eeManager.encryptData(frame.payload);
      } catch (err) {
        throw DataTrackOutgoingPipelineError.encryption(err);
      }
      frame.payload = encryptedResult.payload;
      frame.extensions.e2ee = new DataTrackE2eeExtension(encryptedResult.keyIndex, encryptedResult.iv);
      return frame;
    });
  }
};
DataTrackOutgoingPipeline.TRANSPORT_MTU_BYTES = 16e3;
var log = getLogger(LoggerNames.DataTracks);
var Descriptor = {
  pending() {
    return {
      type: "pending",
      completionFuture: new Future()
    };
  },
  active(info, e2eeManager) {
    return {
      type: "active",
      info,
      publishState: "published",
      pipeline: new DataTrackOutgoingPipeline({
        info,
        e2eeManager
      }),
      unpublishingFuture: new Future()
    };
  }
};
var PUBLISH_TIMEOUT_MILLISECONDS = 1e4;
var OutgoingDataTrackManager = class _OutgoingDataTrackManager extends eventsExports.EventEmitter {
  constructor(options) {
    var _a2;
    super();
    this.handleAllocator = new DataTrackHandleAllocator();
    this.descriptors = /* @__PURE__ */ new Map();
    this.inFlightPacketCounter = /* @__PURE__ */ new Map();
    this.e2eeManager = (_a2 = options === null || options === void 0 ? void 0 : options.e2eeManager) !== null && _a2 !== void 0 ? _a2 : null;
  }
  static withDescriptors(descriptors) {
    const manager = new _OutgoingDataTrackManager();
    manager.descriptors = descriptors;
    return manager;
  }
  /** @internal */
  updateE2eeManager(e2eeManager) {
    this.e2eeManager = e2eeManager;
    for (const descriptor of this.descriptors.values()) {
      if (descriptor.type === "active") {
        descriptor.pipeline.updateE2eeManager(e2eeManager);
      }
    }
  }
  /**
   * Used by attached {@link LocalDataTrack} instances to query their associated descriptor info.
   * @internal
   */
  getDescriptor(handle) {
    var _a2;
    return (_a2 = this.descriptors.get(handle)) !== null && _a2 !== void 0 ? _a2 : null;
  }
  /** Used by attached {@link LocalDataTrack} instances to broadcast data track packets to other
   * subscribers.
   * @internal
   */
  tryProcessAndSend(handle, frame) {
    return __awaiter(this, void 0, void 0, function* () {
      var _a2, e_1, _b2, _c;
      var _d;
      const descriptor = this.getDescriptor(handle);
      if ((descriptor === null || descriptor === void 0 ? void 0 : descriptor.type) !== "active") {
        throw DataTrackPushFrameError.trackUnpublished();
      }
      if (descriptor.publishState === "unpublished") {
        throw DataTrackPushFrameError.trackUnpublished();
      }
      if (descriptor.publishState === "republishing") {
        throw DataTrackPushFrameError.dropped("Data track republishing");
      }
      try {
        try {
          for (var _e2 = true, _f = __asyncValues(descriptor.pipeline.processFrame(frame)), _g; _g = yield _f.next(), _a2 = _g.done, !_a2; _e2 = true) {
            _c = _g.value;
            _e2 = false;
            const packet = _c;
            const prev = (_d = this.inFlightPacketCounter.get(handle)) !== null && _d !== void 0 ? _d : 0;
            this.inFlightPacketCounter.set(handle, prev + 1);
            if (prev === 0) {
              this.emit("packetsFlushedChange", {
                handle,
                isFlushed: false
              });
            }
            this.emit("packetAvailable", {
              handle,
              bytes: packet.toBinary()
            });
          }
        } catch (e_1_1) {
          e_1 = {
            error: e_1_1
          };
        } finally {
          try {
            if (!_e2 && !_a2 && (_b2 = _f.return)) yield _b2.call(_f);
          } finally {
            if (e_1) throw e_1.error;
          }
        }
      } catch (err) {
        throw DataTrackPushFrameError.dropped(err);
      }
    });
  }
  /** The client has sent a packet over the rtc data channel. This signal is used for determining
   * once all packets are sent and a data track has been "flushed".
   *
   * @internal */
  handlePacketSendComplete(handle) {
    var _a2;
    const prev = (_a2 = this.inFlightPacketCounter.get(handle)) !== null && _a2 !== void 0 ? _a2 : 0;
    let counter = prev - 1;
    if (counter < 0) {
      log.warn("OutgoingDataTrackManager.handlePacketSendComplete: inFlightPacketCounter was decremented below 0 (got ".concat(this.inFlightPacketCounter, " - resetting to 0. Were more packets send than were emitted?"));
      counter = 0;
    }
    this.inFlightPacketCounter.set(handle, counter);
    if (counter === 0) {
      this.emit("packetsFlushedChange", {
        handle,
        isFlushed: true
      });
    }
  }
  /**
   * Client requested to publish a track.
   *
   * If the LiveKit server is too old and doesn't support data tracks, a
   * {@link DataTrackPublishError#timeout} will be thrown.
   *
   * @internal
   **/
  publishRequest(options, signal) {
    return __awaiter(this, void 0, void 0, function* () {
      const handle = this.handleAllocator.get();
      if (!handle) {
        throw DataTrackPublishError.limitReached();
      }
      const timeoutSignal = abortSignalTimeout(PUBLISH_TIMEOUT_MILLISECONDS);
      const combinedSignal = signal ? abortSignalAny([signal, timeoutSignal]) : timeoutSignal;
      if (this.descriptors.has(handle)) {
        throw new Error("Descriptor for handle already exists");
      }
      const descriptor = Descriptor.pending();
      this.descriptors.set(handle, descriptor);
      const onAbort = () => {
        var _a2, _b2;
        const existingDescriptor = this.descriptors.get(handle);
        if (!existingDescriptor) {
          log.warn("No descriptor for ".concat(handle));
          return;
        }
        this.descriptors.delete(handle);
        this.emit("sfuUnpublishRequest", {
          handle
        });
        if (existingDescriptor.type === "pending") {
          (_b2 = (_a2 = existingDescriptor.completionFuture).reject) === null || _b2 === void 0 ? void 0 : _b2.call(_a2, timeoutSignal.aborted ? DataTrackPublishError.timeout() : (
            // NOTE: the below cancelled case was introduced by web / there isn't a corresponding case in the rust version.
            DataTrackPublishError.cancelled()
          ));
        }
      };
      if (combinedSignal.aborted) {
        onAbort();
        return descriptor.completionFuture.promise.then(
          () => handle
          /* no-op, makes typescript happy */
        );
      }
      combinedSignal.addEventListener("abort", onAbort);
      this.emit("sfuPublishRequest", {
        handle,
        name: options.name,
        usesE2ee: this.e2eeManager !== null
      });
      yield descriptor.completionFuture.promise;
      combinedSignal.removeEventListener("abort", onAbort);
      this.emit("trackPublished", {
        track: LocalDataTrack.withExplicitHandle(options, this, handle)
      });
      return handle;
    });
  }
  /**
   * Get information about all currently published tracks.
   * @internal
   **/
  queryPublished() {
    const descriptorInfos = Array.from(this.descriptors.values()).filter((descriptor) => descriptor.type === "active").map((descriptor) => descriptor.info);
    return descriptorInfos;
  }
  /**
   * Client request to unpublish a track.
   * @internal
   **/
  unpublishRequest(handle) {
    return __awaiter(this, void 0, void 0, function* () {
      const descriptor = this.descriptors.get(handle);
      if (!descriptor) {
        log.warn("No descriptor for ".concat(handle));
        return;
      }
      if (descriptor.type !== "active") {
        log.warn("Track ".concat(handle, " not active"));
        return;
      }
      this.emit("sfuUnpublishRequest", {
        handle
      });
      yield descriptor.unpublishingFuture.promise;
      this.inFlightPacketCounter.delete(handle);
      this.emit("trackUnpublished", {
        sid: descriptor.info.sid
      });
    });
  }
  /**
   * SFU responded to a request to publish a data track.
   * @internal
   **/
  receivedSfuPublishResponse(handle, result) {
    var _a2, _b2, _c, _d;
    const descriptor = this.descriptors.get(handle);
    if (!descriptor) {
      log.warn("No descriptor for ".concat(handle));
      return;
    }
    this.descriptors.delete(handle);
    switch (descriptor.type) {
      case "pending": {
        if (result.type === "ok") {
          const info = result.data;
          log.debug("SFU accepted publish request for handle ".concat(handle), {
            sid: info.sid
          });
          const e2eeManager = info.usesE2ee ? this.e2eeManager : null;
          this.descriptors.set(info.pubHandle, Descriptor.active(info, e2eeManager));
          (_b2 = (_a2 = descriptor.completionFuture).resolve) === null || _b2 === void 0 ? void 0 : _b2.call(_a2);
        } else {
          log.debug("SFU rejected publish request for handle ".concat(handle), {
            error: result.error
          });
          (_d = (_c = descriptor.completionFuture).reject) === null || _d === void 0 ? void 0 : _d.call(_c, result.error);
        }
        return;
      }
      case "active": {
        if (descriptor.publishState !== "republishing") {
          log.warn("Track ".concat(handle, " already active"));
          return;
        }
        if (result.type === "error") {
          log.warn("Republish failed for track ".concat(handle));
          return;
        }
        log.debug("Track ".concat(handle, " republished"));
        descriptor.info.sid = result.data.sid;
        descriptor.publishState = "published";
        this.descriptors.set(descriptor.info.pubHandle, descriptor);
      }
    }
  }
  /**
   * SFU notification that a track has been unpublished.
   * @internal
   **/
  receivedSfuUnpublishResponse(handle) {
    var _a2, _b2;
    const descriptor = this.descriptors.get(handle);
    if (!descriptor) {
      log.warn("No descriptor for ".concat(handle));
      return;
    }
    this.descriptors.delete(handle);
    if (descriptor.type !== "active") {
      log.warn("Track ".concat(handle, " not active"));
      return;
    }
    descriptor.publishState = "unpublished";
    (_b2 = (_a2 = descriptor.unpublishingFuture).resolve) === null || _b2 === void 0 ? void 0 : _b2.call(_a2);
  }
  /** Republish all tracks.
   *
   * This must be sent after a full reconnect in order for existing publications
   * to be recognized by the SFU. Each republished track will be assigned a new SID.
   * @internal
   */
  sfuWillRepublishTracks() {
    var _a2, _b2;
    for (const _ref of this.descriptors.entries()) {
      var _ref2 = _slicedToArray(_ref, 2);
      const handle = _ref2[0];
      const descriptor = _ref2[1];
      switch (descriptor.type) {
        case "pending":
          this.descriptors.delete(handle);
          (_b2 = (_a2 = descriptor.completionFuture).reject) === null || _b2 === void 0 ? void 0 : _b2.call(_a2, DataTrackPublishError.disconnected());
          break;
        case "active":
          descriptor.publishState = "republishing";
          this.emit("sfuPublishRequest", {
            handle: descriptor.info.pubHandle,
            name: descriptor.info.name,
            usesE2ee: descriptor.info.usesE2ee
          });
      }
    }
  }
  /**
   * Reset's the state of the manager and all associated tracks. Run on room disconnect to get
   * the manager ready for the next room connection.
   * @internal
   **/
  reset() {
    return __awaiter(this, void 0, void 0, function* () {
      var _a2, _b2, _c, _d;
      this.handleAllocator.reset();
      for (const descriptor of this.descriptors.values()) {
        switch (descriptor.type) {
          case "pending":
            (_b2 = (_a2 = descriptor.completionFuture).reject) === null || _b2 === void 0 ? void 0 : _b2.call(_a2, DataTrackPublishError.disconnected());
            break;
          case "active":
            (_d = (_c = descriptor.unpublishingFuture).resolve) === null || _d === void 0 ? void 0 : _d.call(_c);
            yield this.unpublishRequest(descriptor.info.pubHandle);
            break;
        }
      }
      this.descriptors.clear();
      this.inFlightPacketCounter.clear();
      this.emit("reset");
    });
  }
};
var RpcError = class _RpcError extends Error {
  /**
   * Creates an error object with the given code and message, plus an optional data payload.
   *
   * If thrown in an RPC method handler, the error will be sent back to the caller.
   *
   * Error codes 1001-1999 are reserved for built-in errors (see RpcError.ErrorCode for their meanings).
   */
  constructor(code, message, data, options) {
    super(message);
    this.code = code;
    this.message = truncateBytes(message, _RpcError.MAX_MESSAGE_BYTES);
    this.data = data ? truncateBytes(data, _RpcError.MAX_DATA_BYTES) : void 0;
    if (typeof (options === null || options === void 0 ? void 0 : options.cause) !== "undefined") {
      this.cause = options === null || options === void 0 ? void 0 : options.cause;
    }
  }
  /**
   * @internal
   */
  static fromProto(proto) {
    return new _RpcError(proto.code, proto.message, proto.data);
  }
  /**
   * @internal
   */
  toProto() {
    return new RpcError$1({
      code: this.code,
      message: this.message,
      data: this.data
    });
  }
  /**
   * Creates an error object from the code, with an auto-populated message.
   *
   * @internal
   */
  static builtIn(key, data, options) {
    return new _RpcError(_RpcError.ErrorCode[key], _RpcError.ErrorMessage[key], data, options);
  }
};
RpcError.MAX_MESSAGE_BYTES = 256;
RpcError.MAX_DATA_BYTES = 15360;
RpcError.ErrorCode = {
  APPLICATION_ERROR: 1500,
  CONNECTION_TIMEOUT: 1501,
  RESPONSE_TIMEOUT: 1502,
  RECIPIENT_DISCONNECTED: 1503,
  RESPONSE_PAYLOAD_TOO_LARGE: 1504,
  SEND_FAILED: 1505,
  UNSUPPORTED_METHOD: 1400,
  RECIPIENT_NOT_FOUND: 1401,
  REQUEST_PAYLOAD_TOO_LARGE: 1402,
  UNSUPPORTED_SERVER: 1403,
  UNSUPPORTED_VERSION: 1404
};
RpcError.ErrorMessage = {
  APPLICATION_ERROR: "Application error in method handler",
  CONNECTION_TIMEOUT: "Connection timeout",
  RESPONSE_TIMEOUT: "Response timeout",
  RECIPIENT_DISCONNECTED: "Recipient disconnected",
  RESPONSE_PAYLOAD_TOO_LARGE: "Response payload too large",
  SEND_FAILED: "Failed to send",
  UNSUPPORTED_METHOD: "Method not supported at destination",
  RECIPIENT_NOT_FOUND: "Recipient not found",
  REQUEST_PAYLOAD_TOO_LARGE: "Request payload too large",
  UNSUPPORTED_SERVER: "RPC not supported by server",
  UNSUPPORTED_VERSION: "Unsupported RPC version"
};
var MAX_V1_PAYLOAD_BYTES = 15360;
var RPC_REQUEST_DATA_STREAM_TOPIC = "lk.rpc_request";
var RPC_RESPONSE_DATA_STREAM_TOPIC = "lk.rpc_response";
var RpcRequestAttrs;
(function(RpcRequestAttrs2) {
  RpcRequestAttrs2["RPC_REQUEST_ID"] = "lk.rpc_request_id";
  RpcRequestAttrs2["RPC_REQUEST_METHOD"] = "lk.rpc_request_method";
  RpcRequestAttrs2["RPC_REQUEST_RESPONSE_TIMEOUT_MS"] = "lk.rpc_request_response_timeout_ms";
  RpcRequestAttrs2["RPC_REQUEST_VERSION"] = "lk.rpc_request_version";
})(RpcRequestAttrs || (RpcRequestAttrs = {}));
var RPC_VERSION_V1 = 1;
var RPC_VERSION_V2 = 2;
function byteLength(str) {
  const encoder = new TextEncoder();
  return encoder.encode(str).length;
}
function truncateBytes(str, maxBytes) {
  if (byteLength(str) <= maxBytes) {
    return str;
  }
  let low = 0;
  let high = str.length;
  const encoder = new TextEncoder();
  while (low < high) {
    const mid = Math.floor((low + high + 1) / 2);
    if (encoder.encode(str.slice(0, mid)).length <= maxBytes) {
      low = mid;
    } else {
      high = mid - 1;
    }
  }
  return str.slice(0, low);
}
var RpcClientManager = class extends eventsExports.EventEmitter {
  constructor(log2, outgoingDataStreamManager, getRemoteParticipantClientProtocol, getServerVersion) {
    super();
    this.pendingAcks = /* @__PURE__ */ new Map();
    this.pendingResponses = /* @__PURE__ */ new Map();
    this.log = log2;
    this.outgoingDataStreamManager = outgoingDataStreamManager;
    this.getRemoteParticipantClientProtocol = getRemoteParticipantClientProtocol;
    this.getServerVersion = getServerVersion;
  }
  performRpc(_a2) {
    return __awaiter(this, arguments, void 0, function(_ref) {
      var _this = this;
      let destinationIdentity = _ref.destinationIdentity, method = _ref.method, payload = _ref.payload, _ref$responseTimeout = _ref.responseTimeout, responseTimeoutMs = _ref$responseTimeout === void 0 ? 15e3 : _ref$responseTimeout;
      return function* () {
        const maxRoundTripLatencyMs = 7e3;
        const minEffectiveTimeoutMs = maxRoundTripLatencyMs + 1e3;
        const remoteClientProtocol = _this.getRemoteParticipantClientProtocol(destinationIdentity);
        const payloadBytes = byteLength(payload);
        if (payloadBytes > MAX_V1_PAYLOAD_BYTES && remoteClientProtocol < CLIENT_PROTOCOL_DATA_STREAM_RPC) {
          throw RpcError.builtIn("REQUEST_PAYLOAD_TOO_LARGE");
        }
        const serverVersion = _this.getServerVersion();
        if (serverVersion && compareVersions(serverVersion, "1.8.0") < 0) {
          throw RpcError.builtIn("UNSUPPORTED_SERVER");
        }
        const effectiveTimeoutMs = Math.max(responseTimeoutMs, minEffectiveTimeoutMs);
        const id = crypto.randomUUID();
        const completionFuture = new Future();
        let responseTimeoutId = null;
        const ackTimeoutId = setTimeout(() => {
          var _a3;
          _this.pendingAcks.delete(id);
          (_a3 = completionFuture.reject) === null || _a3 === void 0 ? void 0 : _a3.call(completionFuture, RpcError.builtIn("CONNECTION_TIMEOUT"));
          _this.pendingResponses.delete(id);
          if (responseTimeoutId !== null) {
            clearTimeout(responseTimeoutId);
          }
        }, maxRoundTripLatencyMs);
        _this.pendingAcks.set(id, {
          resolve: () => {
            clearTimeout(ackTimeoutId);
          },
          participantIdentity: destinationIdentity
        });
        _this.pendingResponses.set(id, {
          completionFuture,
          participantIdentity: destinationIdentity
        });
        yield _this.publishRpcRequest(destinationIdentity, id, method, payload, effectiveTimeoutMs, remoteClientProtocol);
        responseTimeoutId = setTimeout(() => {
          var _a3;
          _this.pendingResponses.delete(id);
          (_a3 = completionFuture.reject) === null || _a3 === void 0 ? void 0 : _a3.call(completionFuture, RpcError.builtIn("RESPONSE_TIMEOUT"));
        }, responseTimeoutMs);
        const completionPromise = completionFuture.promise.finally(() => {
          clearTimeout(responseTimeoutId);
          if (_this.pendingAcks.has(id)) {
            _this.log.warn("RPC response received before ack", id);
            _this.pendingAcks.delete(id);
            clearTimeout(ackTimeoutId);
          }
        });
        return [id, completionPromise];
      }();
    });
  }
  publishRpcRequest(destinationIdentity, requestId, method, payload, responseTimeout, remoteClientProtocol) {
    return __awaiter(this, void 0, void 0, function* () {
      if (remoteClientProtocol >= CLIENT_PROTOCOL_DATA_STREAM_RPC) {
        yield this.outgoingDataStreamManager.sendText(payload, {
          topic: RPC_REQUEST_DATA_STREAM_TOPIC,
          destinationIdentities: [destinationIdentity],
          attributes: {
            [RpcRequestAttrs.RPC_REQUEST_ID]: requestId,
            [RpcRequestAttrs.RPC_REQUEST_METHOD]: method,
            [RpcRequestAttrs.RPC_REQUEST_RESPONSE_TIMEOUT_MS]: "".concat(responseTimeout),
            [RpcRequestAttrs.RPC_REQUEST_VERSION]: "".concat(RPC_VERSION_V2)
          }
        });
        return;
      }
      this.emit("sendDataPacket", {
        packet: new DataPacket({
          destinationIdentities: [destinationIdentity],
          kind: DataPacket_Kind.RELIABLE,
          value: {
            case: "rpcRequest",
            value: new RpcRequest({
              id: requestId,
              method,
              payload,
              responseTimeoutMs: responseTimeout,
              version: RPC_VERSION_V1
            })
          }
        })
      });
    });
  }
  /**
   * Handle an incoming data stream containing an RPC response payload.
   * @internal
   */
  handleIncomingDataStream(reader, senderIdentity, attributes) {
    return __awaiter(this, void 0, void 0, function* () {
      const associatedRequestId = attributes[RpcRequestAttrs.RPC_REQUEST_ID];
      if (!associatedRequestId) {
        this.log.warn("RPC data stream malformed: ".concat(RpcRequestAttrs.RPC_REQUEST_ID, " not set."));
        return;
      }
      const pending = this.pendingResponses.get(associatedRequestId);
      if (pending && pending.participantIdentity !== senderIdentity) {
        this.log.warn("RPC response stream for ".concat(associatedRequestId, " arrived from unexpected sender ").concat(senderIdentity, ", expected ").concat(pending.participantIdentity, ". Ignoring."));
        return;
      }
      let payload;
      try {
        payload = yield reader.readAll();
      } catch (e4) {
        this.log.warn("Error reading RPC response payload: ".concat(e4));
        this.handleIncomingRpcResponseFailure(associatedRequestId, RpcError.builtIn("APPLICATION_ERROR", "Error reading RPC response payload", {
          cause: e4
        }));
        return;
      }
      this.handleIncomingRpcResponseSuccess(associatedRequestId, payload);
    });
  }
  /** @internal */
  handleIncomingRpcResponseSuccess(requestId, payload) {
    var _a2, _b2;
    const handler = this.pendingResponses.get(requestId);
    if (handler) {
      (_b2 = (_a2 = handler.completionFuture).resolve) === null || _b2 === void 0 ? void 0 : _b2.call(_a2, payload);
      this.pendingResponses.delete(requestId);
    } else {
      this.log.error("Response received for unexpected RPC request", requestId);
    }
  }
  /** @internal */
  handleIncomingRpcResponseFailure(requestId, error) {
    var _a2, _b2;
    const handler = this.pendingResponses.get(requestId);
    if (handler) {
      (_b2 = (_a2 = handler.completionFuture).reject) === null || _b2 === void 0 ? void 0 : _b2.call(_a2, error);
      this.pendingResponses.delete(requestId);
    } else {
      this.log.error("Response received for unexpected RPC request", requestId);
    }
  }
  /** @internal */
  handleIncomingRpcAck(requestId) {
    const handler = this.pendingAcks.get(requestId);
    if (handler) {
      handler.resolve();
      this.pendingAcks.delete(requestId);
    } else {
      this.log.error("Ack received for unexpected RPC request: ".concat(requestId));
    }
  }
  /** @internal */
  handleParticipantDisconnected(participantIdentity) {
    var _a2;
    for (const _ref2 of this.pendingAcks) {
      var _ref3 = _slicedToArray(_ref2, 2);
      const id = _ref3[0];
      const pendingIdentity = _ref3[1].participantIdentity;
      if (pendingIdentity === participantIdentity) {
        this.pendingAcks.delete(id);
      }
    }
    for (const _ref4 of this.pendingResponses) {
      var _ref5 = _slicedToArray(_ref4, 2);
      const id = _ref5[0];
      var _ref5$ = _ref5[1];
      const pendingIdentity = _ref5$.participantIdentity;
      const completionFuture = _ref5$.completionFuture;
      if (pendingIdentity === participantIdentity) {
        (_a2 = completionFuture.reject) === null || _a2 === void 0 ? void 0 : _a2.call(completionFuture, RpcError.builtIn("RECIPIENT_DISCONNECTED"));
        this.pendingResponses.delete(id);
      }
    }
  }
};
var RpcServerManager = class extends eventsExports.EventEmitter {
  constructor(log2, outgoingDataStreamManager, getRemoteParticipantClientProtocol) {
    super();
    this.rpcHandlers = /* @__PURE__ */ new Map();
    this.log = log2;
    this.outgoingDataStreamManager = outgoingDataStreamManager;
    this.getRemoteParticipantClientProtocol = getRemoteParticipantClientProtocol;
  }
  registerRpcMethod(method, handler) {
    if (this.rpcHandlers.has(method)) {
      throw Error("RPC handler already registered for method ".concat(method, ", unregisterRpcMethod before trying to register again"));
    }
    this.rpcHandlers.set(method, handler);
  }
  unregisterRpcMethod(method) {
    this.rpcHandlers.delete(method);
  }
  /**
   * Handle an incoming RPCRequest message containing a payload.
   * This handles "version 1" of rpc requests.
   * @internal
   */
  handleIncomingRpcRequest(callerIdentity, rpcRequest) {
    return __awaiter(this, void 0, void 0, function* () {
      var _a2;
      this.publishRpcAck(callerIdentity, rpcRequest.id);
      if (rpcRequest.version !== 1) {
        this.publishRpcResponsePacket(callerIdentity, rpcRequest.id, null, RpcError.builtIn("UNSUPPORTED_VERSION"));
        return;
      }
      const handler = this.rpcHandlers.get(rpcRequest.method);
      if (!handler) {
        this.publishRpcResponsePacket(callerIdentity, rpcRequest.id, null, RpcError.builtIn("UNSUPPORTED_METHOD"));
        return;
      }
      let response;
      try {
        response = yield handler({
          requestId: rpcRequest.id,
          callerIdentity,
          payload: rpcRequest.payload,
          responseTimeout: rpcRequest.responseTimeoutMs
        });
      } catch (error) {
        let responseError;
        if (error instanceof RpcError) {
          responseError = error;
        } else {
          this.log.warn("Uncaught error returned by RPC handler for ".concat(rpcRequest.method, ". Returning APPLICATION_ERROR instead."), error);
          responseError = RpcError.builtIn("APPLICATION_ERROR", "Uncaught error: ".concat((_a2 = error === null || error === void 0 ? void 0 : error.message) !== null && _a2 !== void 0 ? _a2 : error), {
            cause: error
          });
        }
        this.publishRpcResponsePacket(callerIdentity, rpcRequest.id, null, responseError);
        return;
      }
      yield this.publishRpcResponse(callerIdentity, rpcRequest.id, response !== null && response !== void 0 ? response : "");
    });
  }
  /**
   * Handle an incoming data stream containing a RPC request payload.
   * This handles "version 2" of rpc requests.
   * @internal
   */
  handleIncomingDataStream(reader, callerIdentity, dataStreamAttrs) {
    return __awaiter(this, void 0, void 0, function* () {
      const requestId = dataStreamAttrs[RpcRequestAttrs.RPC_REQUEST_ID];
      const method = dataStreamAttrs[RpcRequestAttrs.RPC_REQUEST_METHOD];
      const responseTimeout = parseInt(dataStreamAttrs[RpcRequestAttrs.RPC_REQUEST_RESPONSE_TIMEOUT_MS], 10);
      const version2 = parseInt(dataStreamAttrs[RpcRequestAttrs.RPC_REQUEST_VERSION], 10);
      if (!requestId || !method || Number.isNaN(responseTimeout) || Number.isNaN(version2)) {
        this.log.warn("RPC data stream malformed: ".concat(RpcRequestAttrs.RPC_REQUEST_ID, " / ").concat(RpcRequestAttrs.RPC_REQUEST_METHOD, " / ").concat(RpcRequestAttrs.RPC_REQUEST_RESPONSE_TIMEOUT_MS, " / ").concat(RpcRequestAttrs.RPC_REQUEST_VERSION, " not set."));
        this.publishRpcResponsePacket(callerIdentity, requestId, null, RpcError.builtIn("APPLICATION_ERROR", "RPC data stream malformed"));
        return;
      }
      this.publishRpcAck(callerIdentity, requestId);
      if (version2 !== RPC_VERSION_V2) {
        this.publishRpcResponsePacket(callerIdentity, requestId, null, RpcError.builtIn("UNSUPPORTED_VERSION"));
        return;
      }
      let payload;
      try {
        payload = yield reader.readAll();
      } catch (e4) {
        this.log.warn("Error reading RPC request payload: ".concat(e4));
        this.publishRpcResponsePacket(callerIdentity, requestId, null, RpcError.builtIn("APPLICATION_ERROR", "Error reading RPC request payload", {
          cause: e4
        }));
        return;
      }
      const handler = this.rpcHandlers.get(method);
      if (!handler) {
        this.publishRpcResponsePacket(callerIdentity, requestId, null, RpcError.builtIn("UNSUPPORTED_METHOD"));
        return;
      }
      let response;
      try {
        response = yield handler({
          requestId,
          callerIdentity,
          payload,
          responseTimeout
        });
      } catch (error) {
        let responseError;
        if (error instanceof RpcError) {
          responseError = error;
        } else {
          this.log.warn("Uncaught error returned by RPC handler for ".concat(method, ". Returning APPLICATION_ERROR instead."), error);
          responseError = RpcError.builtIn("APPLICATION_ERROR");
        }
        this.publishRpcResponsePacket(callerIdentity, requestId, null, responseError);
        return;
      }
      yield this.publishRpcResponse(callerIdentity, requestId, response !== null && response !== void 0 ? response : "");
    });
  }
  publishRpcAck(destinationIdentity, requestId) {
    this.emit("sendDataPacket", {
      packet: new DataPacket({
        destinationIdentities: [destinationIdentity],
        kind: DataPacket_Kind.RELIABLE,
        value: {
          case: "rpcAck",
          value: new RpcAck({
            requestId
          })
        }
      })
    });
  }
  publishRpcResponsePacket(destinationIdentity, requestId, payload, error) {
    this.emit("sendDataPacket", {
      packet: new DataPacket({
        destinationIdentities: [destinationIdentity],
        kind: DataPacket_Kind.RELIABLE,
        value: {
          case: "rpcResponse",
          value: new RpcResponse({
            requestId,
            value: error ? {
              case: "error",
              value: error.toProto()
            } : {
              case: "payload",
              value: payload !== null && payload !== void 0 ? payload : ""
            }
          })
        }
      })
    });
  }
  /**
   * Send a successful RPC response payload, choosing the transport based on
   * the caller's client protocol version.
   */
  publishRpcResponse(destinationIdentity, requestId, payload) {
    return __awaiter(this, void 0, void 0, function* () {
      const callerClientProtocol = this.getRemoteParticipantClientProtocol(destinationIdentity);
      if (callerClientProtocol >= CLIENT_PROTOCOL_DATA_STREAM_RPC) {
        yield this.outgoingDataStreamManager.sendText(payload, {
          topic: RPC_RESPONSE_DATA_STREAM_TOPIC,
          destinationIdentities: [destinationIdentity],
          attributes: {
            [RpcRequestAttrs.RPC_REQUEST_ID]: requestId
          }
        });
        return;
      }
      const responseBytes = byteLength(payload);
      if (responseBytes > MAX_V1_PAYLOAD_BYTES) {
        this.log.warn("RPC Response payload too large for request ".concat(requestId, ". To send larger responses, consider updating the sending client."));
        this.publishRpcResponsePacket(destinationIdentity, requestId, null, RpcError.builtIn("RESPONSE_PAYLOAD_TOO_LARGE"));
        return;
      }
      this.publishRpcResponsePacket(destinationIdentity, requestId, payload, null);
    });
  }
};
var RemoteAudioTrack = class extends RemoteTrack {
  constructor(mediaTrack, sid, receiver, audioContext, audioOutput, loggerOptions) {
    super(mediaTrack, sid, Track.Kind.Audio, receiver, loggerOptions);
    this.monitorReceiver = () => __awaiter(this, void 0, void 0, function* () {
      if (!this.receiver) {
        this._currentBitrate = 0;
        return;
      }
      const stats = yield this.getReceiverStats();
      if (stats && this.prevStats && this.receiver) {
        this._currentBitrate = computeBitrate(stats, this.prevStats);
      }
      this.prevStats = stats;
    });
    this.audioContext = audioContext;
    this.webAudioPluginNodes = [];
    if (audioOutput) {
      this.sinkId = audioOutput.deviceId;
    }
  }
  /**
   * sets the volume for all attached audio elements
   */
  setVolume(volume) {
    var _a2;
    for (const el of this.attachedElements) {
      if (this.audioContext) {
        (_a2 = this.gainNode) === null || _a2 === void 0 ? void 0 : _a2.gain.setTargetAtTime(volume, 0, 0.1);
      } else {
        el.volume = volume;
      }
    }
    if (isReactNative()) {
      this._mediaStreamTrack._setVolume(volume);
    }
    this.elementVolume = volume;
  }
  /**
   * gets the volume of attached audio elements (loudest)
   */
  getVolume() {
    if (this.elementVolume) {
      return this.elementVolume;
    }
    if (isReactNative()) {
      return 1;
    }
    let highestVolume = 0;
    this.attachedElements.forEach((element) => {
      if (element.volume > highestVolume) {
        highestVolume = element.volume;
      }
    });
    return highestVolume;
  }
  /**
   * calls setSinkId on all attached elements, if supported
   * @param deviceId audio output device
   */
  setSinkId(deviceId) {
    return __awaiter(this, void 0, void 0, function* () {
      this.sinkId = deviceId;
      yield Promise.all(this.attachedElements.map((elm) => {
        if (!supportsSetSinkId(elm)) {
          return;
        }
        return elm.setSinkId(deviceId);
      }));
    });
  }
  attach(element) {
    const needsNewWebAudioConnection = this.attachedElements.length === 0;
    if (!element) {
      element = super.attach();
    } else {
      super.attach(element);
    }
    if (this.sinkId && supportsSetSinkId(element)) {
      element.setSinkId(this.sinkId).catch((e4) => {
        this.log.error("Failed to set sink id on remote audio track", e4, this.logContext);
      });
    }
    if (this.audioContext && needsNewWebAudioConnection) {
      this.log.debug("using audio context mapping", this.logContext);
      this.connectWebAudio(this.audioContext, element);
      element.volume = 0;
      element.muted = true;
    }
    if (this.elementVolume) {
      this.setVolume(this.elementVolume);
    }
    return element;
  }
  detach(element) {
    let detached;
    if (!element) {
      detached = super.detach();
      this.disconnectWebAudio();
    } else {
      detached = super.detach(element);
      if (this.audioContext) {
        if (this.attachedElements.length > 0) {
          this.connectWebAudio(this.audioContext, this.attachedElements[0]);
        } else {
          this.disconnectWebAudio();
        }
      }
    }
    return detached;
  }
  /**
   * @internal
   * @experimental
   */
  setAudioContext(audioContext) {
    this.audioContext = audioContext;
    if (audioContext && this.attachedElements.length > 0) {
      this.connectWebAudio(audioContext, this.attachedElements[0]);
    } else if (!audioContext) {
      this.disconnectWebAudio();
    }
  }
  /**
   * @internal
   * @experimental
   * @param {AudioNode[]} nodes - An array of WebAudio nodes. These nodes should not be connected to each other when passed, as the sdk will take care of connecting them in the order of the array.
   */
  setWebAudioPlugins(nodes) {
    this.webAudioPluginNodes = nodes;
    if (this.attachedElements.length > 0 && this.audioContext) {
      this.connectWebAudio(this.audioContext, this.attachedElements[0]);
    }
  }
  connectWebAudio(context, element) {
    this.disconnectWebAudio();
    this.sourceNode = context.createMediaStreamSource(element.srcObject);
    let lastNode = this.sourceNode;
    this.webAudioPluginNodes.forEach((node) => {
      lastNode.connect(node);
      lastNode = node;
    });
    this.gainNode = context.createGain();
    lastNode.connect(this.gainNode);
    this.gainNode.connect(context.destination);
    if (this.elementVolume) {
      this.gainNode.gain.setTargetAtTime(this.elementVolume, 0, 0.1);
    }
    if (context.state !== "running") {
      context.resume().then(() => {
        if (context.state !== "running") {
          this.emit(TrackEvent.AudioPlaybackFailed, new Error("Audio Context couldn't be started automatically"));
        }
      }).catch((e4) => {
        this.emit(TrackEvent.AudioPlaybackFailed, e4);
      });
    }
  }
  disconnectWebAudio() {
    var _a2, _b2;
    (_a2 = this.gainNode) === null || _a2 === void 0 ? void 0 : _a2.disconnect();
    (_b2 = this.sourceNode) === null || _b2 === void 0 ? void 0 : _b2.disconnect();
    this.gainNode = void 0;
    this.sourceNode = void 0;
  }
  getReceiverStats() {
    return __awaiter(this, void 0, void 0, function* () {
      if (!this.receiver || !this.receiver.getStats) {
        return;
      }
      const stats = yield this.receiver.getStats();
      let receiverStats;
      stats.forEach((v) => {
        if (v.type === "inbound-rtp") {
          receiverStats = {
            type: "audio",
            streamId: v.id,
            timestamp: v.timestamp,
            jitter: v.jitter,
            bytesReceived: v.bytesReceived,
            concealedSamples: v.concealedSamples,
            concealmentEvents: v.concealmentEvents,
            silentConcealedSamples: v.silentConcealedSamples,
            silentConcealmentEvents: v.silentConcealmentEvents,
            totalAudioEnergy: v.totalAudioEnergy,
            totalSamplesDuration: v.totalSamplesDuration
          };
        }
      });
      return receiverStats;
    });
  }
};
var TrackPublication = class extends eventsExports.EventEmitter {
  constructor(kind, id, name, loggerOptions) {
    var _a2;
    super();
    this.metadataMuted = false;
    this.encryption = Encryption_Type.NONE;
    this.log = livekitLogger;
    this.handleMuted = () => {
      this.emit(TrackEvent.Muted);
    };
    this.handleUnmuted = () => {
      this.emit(TrackEvent.Unmuted);
    };
    this.log = getLogger((_a2 = loggerOptions === null || loggerOptions === void 0 ? void 0 : loggerOptions.loggerName) !== null && _a2 !== void 0 ? _a2 : LoggerNames.Publication);
    this.loggerContextCb = this.loggerContextCb;
    this.setMaxListeners(100);
    this.kind = kind;
    this.trackSid = id;
    this.trackName = name;
    this.source = Track.Source.Unknown;
  }
  /** @internal */
  setTrack(track) {
    if (this.track) {
      this.track.off(TrackEvent.Muted, this.handleMuted);
      this.track.off(TrackEvent.Unmuted, this.handleUnmuted);
    }
    this.track = track;
    if (track) {
      track.on(TrackEvent.Muted, this.handleMuted);
      track.on(TrackEvent.Unmuted, this.handleUnmuted);
    }
  }
  get logContext() {
    var _a2;
    return Object.assign(Object.assign({}, (_a2 = this.loggerContextCb) === null || _a2 === void 0 ? void 0 : _a2.call(this)), getLogContextFromTrack(this));
  }
  get isMuted() {
    return this.metadataMuted;
  }
  get isEnabled() {
    return true;
  }
  get isSubscribed() {
    return this.track !== void 0;
  }
  get isEncrypted() {
    return this.encryption !== Encryption_Type.NONE;
  }
  /**
   * an [AudioTrack] if this publication holds an audio track
   */
  get audioTrack() {
    if (isAudioTrack(this.track)) {
      return this.track;
    }
  }
  /**
   * an [VideoTrack] if this publication holds a video track
   */
  get videoTrack() {
    if (isVideoTrack(this.track)) {
      return this.track;
    }
  }
  /** @internal */
  updateInfo(info) {
    this.trackSid = info.sid;
    this.trackName = info.name;
    this.source = Track.sourceFromProto(info.source);
    this.mimeType = info.mimeType;
    if (this.kind === Track.Kind.Video && info.width > 0) {
      this.dimensions = {
        width: info.width,
        height: info.height
      };
      this.simulcasted = info.simulcast;
    }
    this.encryption = info.encryption;
    this.trackInfo = info;
    this.log.debug("update publication info", Object.assign(Object.assign({}, this.logContext), {
      info
    }));
  }
};
(function(TrackPublication2) {
  (function(SubscriptionStatus) {
    SubscriptionStatus["Desired"] = "desired";
    SubscriptionStatus["Subscribed"] = "subscribed";
    SubscriptionStatus["Unsubscribed"] = "unsubscribed";
  })(TrackPublication2.SubscriptionStatus || (TrackPublication2.SubscriptionStatus = {}));
  (function(PermissionStatus) {
    PermissionStatus["Allowed"] = "allowed";
    PermissionStatus["NotAllowed"] = "not_allowed";
  })(TrackPublication2.PermissionStatus || (TrackPublication2.PermissionStatus = {}));
})(TrackPublication || (TrackPublication = {}));
var LocalTrackPublication = class extends TrackPublication {
  get isUpstreamPaused() {
    var _a2;
    return (_a2 = this.track) === null || _a2 === void 0 ? void 0 : _a2.isUpstreamPaused;
  }
  constructor(kind, ti2, track, loggerOptions) {
    super(kind, ti2.sid, ti2.name, loggerOptions);
    this.track = void 0;
    this.handleTrackEnded = () => {
      this.emit(TrackEvent.Ended);
    };
    this.handleCpuConstrained = () => {
      if (this.track && isVideoTrack(this.track)) {
        this.emit(TrackEvent.CpuConstrained, this.track);
      }
    };
    this.updateInfo(ti2);
    this.setTrack(track);
  }
  setTrack(track) {
    if (this.track) {
      this.track.off(TrackEvent.Ended, this.handleTrackEnded);
      this.track.off(TrackEvent.CpuConstrained, this.handleCpuConstrained);
    }
    super.setTrack(track);
    if (track) {
      track.on(TrackEvent.Ended, this.handleTrackEnded);
      track.on(TrackEvent.CpuConstrained, this.handleCpuConstrained);
    }
  }
  get isMuted() {
    if (this.track) {
      return this.track.isMuted;
    }
    return super.isMuted;
  }
  get audioTrack() {
    return super.audioTrack;
  }
  get videoTrack() {
    return super.videoTrack;
  }
  get isLocal() {
    return true;
  }
  /**
   * Mute the track associated with this publication
   */
  mute() {
    return __awaiter(this, void 0, void 0, function* () {
      var _a2;
      return (_a2 = this.track) === null || _a2 === void 0 ? void 0 : _a2.mute();
    });
  }
  /**
   * Unmute track associated with this publication
   */
  unmute() {
    return __awaiter(this, void 0, void 0, function* () {
      var _a2;
      return (_a2 = this.track) === null || _a2 === void 0 ? void 0 : _a2.unmute();
    });
  }
  /**
   * Pauses the media stream track associated with this publication from being sent to the server
   * and signals "muted" event to other participants
   * Useful if you want to pause the stream without pausing the local media stream track
   */
  pauseUpstream() {
    return __awaiter(this, void 0, void 0, function* () {
      var _a2;
      yield (_a2 = this.track) === null || _a2 === void 0 ? void 0 : _a2.pauseUpstream();
    });
  }
  /**
   * Resumes sending the media stream track associated with this publication to the server after a call to [[pauseUpstream()]]
   * and signals "unmuted" event to other participants (unless the track is explicitly muted)
   */
  resumeUpstream() {
    return __awaiter(this, void 0, void 0, function* () {
      var _a2;
      yield (_a2 = this.track) === null || _a2 === void 0 ? void 0 : _a2.resumeUpstream();
    });
  }
  getTrackFeatures() {
    var _a2;
    if (isAudioTrack(this.track)) {
      const settings = this.track.getSourceTrackSettings();
      const features = /* @__PURE__ */ new Set();
      if (settings.autoGainControl) {
        features.add(AudioTrackFeature.TF_AUTO_GAIN_CONTROL);
      }
      if (settings.echoCancellation) {
        features.add(AudioTrackFeature.TF_ECHO_CANCELLATION);
      }
      if (settings.noiseSuppression) {
        features.add(AudioTrackFeature.TF_NOISE_SUPPRESSION);
      }
      if (settings.channelCount && settings.channelCount > 1) {
        features.add(AudioTrackFeature.TF_STEREO);
      }
      if (!((_a2 = this.options) === null || _a2 === void 0 ? void 0 : _a2.dtx)) {
        features.add(AudioTrackFeature.TF_NO_DTX);
      }
      if (this.track.enhancedNoiseCancellation) {
        features.add(AudioTrackFeature.TF_ENHANCED_NOISE_CANCELLATION);
      }
      return Array.from(features.values());
    } else return [];
  }
};
function createLocalTracks(options, loggerOptions) {
  return __awaiter(this, void 0, void 0, function* () {
    options !== null && options !== void 0 ? options : options = {};
    let attemptExactMatch = false;
    const _extractProcessorsFro = extractProcessorsFromOptions(options), audioProcessor = _extractProcessorsFro.audioProcessor, videoProcessor = _extractProcessorsFro.videoProcessor, internalOptions = _extractProcessorsFro.optionsWithoutProcessor;
    let retryAudioOptions = internalOptions.audio;
    let retryVideoOptions = internalOptions.video;
    if (audioProcessor && typeof internalOptions.audio === "object") {
      internalOptions.audio.processor = audioProcessor;
    }
    if (videoProcessor && typeof internalOptions.video === "object") {
      internalOptions.video.processor = videoProcessor;
    }
    if (options.audio && typeof internalOptions.audio === "object" && typeof internalOptions.audio.deviceId === "string") {
      const deviceId = internalOptions.audio.deviceId;
      internalOptions.audio.deviceId = {
        exact: deviceId
      };
      attemptExactMatch = true;
      retryAudioOptions = Object.assign(Object.assign({}, internalOptions.audio), {
        deviceId: {
          ideal: deviceId
        }
      });
    }
    if (internalOptions.video && typeof internalOptions.video === "object" && typeof internalOptions.video.deviceId === "string") {
      const deviceId = internalOptions.video.deviceId;
      internalOptions.video.deviceId = {
        exact: deviceId
      };
      attemptExactMatch = true;
      retryVideoOptions = Object.assign(Object.assign({}, internalOptions.video), {
        deviceId: {
          ideal: deviceId
        }
      });
    }
    if (internalOptions.audio === true) {
      internalOptions.audio = {
        deviceId: "default"
      };
    } else if (typeof internalOptions.audio === "object" && internalOptions.audio !== null) {
      internalOptions.audio = Object.assign(Object.assign({}, internalOptions.audio), {
        deviceId: internalOptions.audio.deviceId || "default"
      });
    }
    if (internalOptions.video === true) {
      internalOptions.video = {
        deviceId: "default"
      };
    } else if (typeof internalOptions.video === "object" && !internalOptions.video.deviceId) {
      internalOptions.video.deviceId = "default";
    }
    const opts = mergeDefaultOptions(internalOptions, audioDefaults, videoDefaults);
    const constraints = constraintsForOptions(opts);
    const mediaPromise = navigator.mediaDevices.getUserMedia(constraints);
    if (internalOptions.audio) {
      DeviceManager.userMediaPromiseMap.set("audioinput", mediaPromise);
      mediaPromise.catch(() => DeviceManager.userMediaPromiseMap.delete("audioinput"));
    }
    if (internalOptions.video) {
      DeviceManager.userMediaPromiseMap.set("videoinput", mediaPromise);
      mediaPromise.catch(() => DeviceManager.userMediaPromiseMap.delete("videoinput"));
    }
    try {
      const stream = yield mediaPromise;
      return yield Promise.all(stream.getTracks().map((mediaStreamTrack) => __awaiter(this, void 0, void 0, function* () {
        const isAudio = mediaStreamTrack.kind === "audio";
        let trackConstraints;
        const conOrBool = isAudio ? constraints.audio : constraints.video;
        if (typeof conOrBool !== "boolean") {
          trackConstraints = conOrBool;
        }
        const newDeviceId = mediaStreamTrack.getSettings().deviceId;
        if ((trackConstraints === null || trackConstraints === void 0 ? void 0 : trackConstraints.deviceId) && unwrapConstraint(trackConstraints.deviceId) !== newDeviceId) {
          trackConstraints.deviceId = newDeviceId;
        } else if (!trackConstraints) {
          trackConstraints = {
            deviceId: newDeviceId
          };
        }
        const track = mediaTrackToLocalTrack(mediaStreamTrack, trackConstraints, loggerOptions);
        if (track.kind === Track.Kind.Video) {
          track.source = Track.Source.Camera;
        } else if (track.kind === Track.Kind.Audio) {
          track.source = Track.Source.Microphone;
        }
        track.mediaStream = stream;
        if (isAudioTrack(track) && audioProcessor) {
          yield track.setProcessor(audioProcessor);
        } else if (isVideoTrack(track) && videoProcessor) {
          yield track.setProcessor(videoProcessor);
        }
        return track;
      })));
    } catch (e4) {
      if (!attemptExactMatch) {
        throw e4;
      }
      return createLocalTracks(Object.assign(Object.assign({}, options), {
        audio: retryAudioOptions,
        video: retryVideoOptions
      }), loggerOptions);
    }
  });
}
function createLocalVideoTrack(options) {
  return __awaiter(this, void 0, void 0, function* () {
    const tracks = yield createLocalTracks({
      audio: false,
      video: options !== null && options !== void 0 ? options : true
    });
    return tracks[0];
  });
}
function createLocalAudioTrack(options) {
  return __awaiter(this, void 0, void 0, function* () {
    const tracks = yield createLocalTracks({
      audio: options !== null && options !== void 0 ? options : true,
      video: false
    });
    return tracks[0];
  });
}
var ConnectionQuality;
(function(ConnectionQuality2) {
  ConnectionQuality2["Excellent"] = "excellent";
  ConnectionQuality2["Good"] = "good";
  ConnectionQuality2["Poor"] = "poor";
  ConnectionQuality2["Lost"] = "lost";
  ConnectionQuality2["Unknown"] = "unknown";
})(ConnectionQuality || (ConnectionQuality = {}));
function qualityFromProto(q3) {
  switch (q3) {
    case ConnectionQuality$1.EXCELLENT:
      return ConnectionQuality.Excellent;
    case ConnectionQuality$1.GOOD:
      return ConnectionQuality.Good;
    case ConnectionQuality$1.POOR:
      return ConnectionQuality.Poor;
    case ConnectionQuality$1.LOST:
      return ConnectionQuality.Lost;
    default:
      return ConnectionQuality.Unknown;
  }
}
var Participant = class extends eventsExports.EventEmitter {
  get logContext() {
    var _a2, _b2;
    return Object.assign({}, (_b2 = (_a2 = this.loggerOptions) === null || _a2 === void 0 ? void 0 : _a2.loggerContextCb) === null || _b2 === void 0 ? void 0 : _b2.call(_a2));
  }
  get isEncrypted() {
    return this.trackPublications.size > 0 && Array.from(this.trackPublications.values()).every((tr3) => tr3.isEncrypted);
  }
  get isAgent() {
    var _a2;
    return ((_a2 = this.permissions) === null || _a2 === void 0 ? void 0 : _a2.agent) || this.kind === ParticipantInfo_Kind.AGENT;
  }
  get isActive() {
    var _a2;
    return ((_a2 = this.participantInfo) === null || _a2 === void 0 ? void 0 : _a2.state) === ParticipantInfo_State.ACTIVE;
  }
  get kind() {
    return this._kind;
  }
  /** participant attributes, similar to metadata, but as a key/value map */
  get attributes() {
    return Object.freeze(Object.assign({}, this._attributes));
  }
  /** @internal */
  constructor(sid, identity, name, metadata, attributes, loggerOptions) {
    let kind = arguments.length > 6 && arguments[6] !== void 0 ? arguments[6] : ParticipantInfo_Kind.STANDARD;
    var _a2;
    super();
    this.audioLevel = 0;
    this.isSpeaking = false;
    this._connectionQuality = ConnectionQuality.Unknown;
    this.log = livekitLogger;
    this.loggerOptions = loggerOptions;
    this.log = getLogger((_a2 = loggerOptions === null || loggerOptions === void 0 ? void 0 : loggerOptions.loggerName) !== null && _a2 !== void 0 ? _a2 : LoggerNames.Participant, () => this.logContext);
    this.setMaxListeners(100);
    this.sid = sid;
    this.identity = identity;
    this.name = name;
    this.metadata = metadata;
    this.audioTrackPublications = /* @__PURE__ */ new Map();
    this.videoTrackPublications = /* @__PURE__ */ new Map();
    this.trackPublications = /* @__PURE__ */ new Map();
    this._kind = kind;
    this._attributes = attributes !== null && attributes !== void 0 ? attributes : {};
  }
  getTrackPublications() {
    return Array.from(this.trackPublications.values());
  }
  /**
   * Finds the first track that matches the source filter, for example, getting
   * the user's camera track with getTrackBySource(Track.Source.Camera).
   */
  getTrackPublication(source) {
    for (const _ref of this.trackPublications) {
      var _ref2 = _slicedToArray(_ref, 2);
      const pub = _ref2[1];
      if (pub.source === source) {
        return pub;
      }
    }
  }
  /**
   * Finds the first track that matches the track's name.
   */
  getTrackPublicationByName(name) {
    for (const _ref3 of this.trackPublications) {
      var _ref4 = _slicedToArray(_ref3, 2);
      const pub = _ref4[1];
      if (pub.trackName === name) {
        return pub;
      }
    }
  }
  /**
   * Waits until the participant is active and ready to receive data messages
   * @returns a promise that resolves when the participant is active
   */
  waitUntilActive() {
    if (this.isActive) {
      return Promise.resolve();
    }
    if (this.activeFuture) {
      return this.activeFuture.promise;
    }
    this.activeFuture = new Future();
    this.once(ParticipantEvent.Active, () => {
      var _a2, _b2;
      (_b2 = (_a2 = this.activeFuture) === null || _a2 === void 0 ? void 0 : _a2.resolve) === null || _b2 === void 0 ? void 0 : _b2.call(_a2);
      this.activeFuture = void 0;
    });
    return this.activeFuture.promise;
  }
  get connectionQuality() {
    return this._connectionQuality;
  }
  get isCameraEnabled() {
    var _a2;
    const track = this.getTrackPublication(Track.Source.Camera);
    return !((_a2 = track === null || track === void 0 ? void 0 : track.isMuted) !== null && _a2 !== void 0 ? _a2 : true);
  }
  get isMicrophoneEnabled() {
    var _a2;
    const track = this.getTrackPublication(Track.Source.Microphone);
    return !((_a2 = track === null || track === void 0 ? void 0 : track.isMuted) !== null && _a2 !== void 0 ? _a2 : true);
  }
  get isScreenShareEnabled() {
    const track = this.getTrackPublication(Track.Source.ScreenShare);
    return !!track;
  }
  get isLocal() {
    return false;
  }
  /** when participant joined the room */
  get joinedAt() {
    if (this.participantInfo) {
      return new Date(Number.parseInt(this.participantInfo.joinedAt.toString()) * 1e3);
    }
    return /* @__PURE__ */ new Date();
  }
  /** @internal */
  updateInfo(info) {
    var _a2;
    if (this.participantInfo && this.participantInfo.sid === info.sid && this.participantInfo.version > info.version) {
      return false;
    }
    this.identity = info.identity;
    this.sid = info.sid;
    this._setName(info.name);
    this._setMetadata(info.metadata);
    this._setAttributes(info.attributes);
    if (info.state === ParticipantInfo_State.ACTIVE && ((_a2 = this.participantInfo) === null || _a2 === void 0 ? void 0 : _a2.state) !== ParticipantInfo_State.ACTIVE) {
      this.emit(ParticipantEvent.Active);
    }
    if (info.permission) {
      this.setPermissions(info.permission);
    }
    this.participantInfo = info;
    return true;
  }
  /**
   * Updates metadata from server
   **/
  _setMetadata(md) {
    const changed = this.metadata !== md;
    const prevMetadata = this.metadata;
    this.metadata = md;
    if (changed) {
      this.emit(ParticipantEvent.ParticipantMetadataChanged, prevMetadata);
    }
  }
  _setName(name) {
    const changed = this.name !== name;
    this.name = name;
    if (changed) {
      this.emit(ParticipantEvent.ParticipantNameChanged, name);
    }
  }
  /**
   * Updates metadata from server
   **/
  _setAttributes(attributes) {
    const diff = diffAttributes(this.attributes, attributes);
    this._attributes = attributes;
    if (Object.keys(diff).length > 0) {
      this.emit(ParticipantEvent.AttributesChanged, diff);
    }
  }
  /** @internal */
  setPermissions(permissions) {
    var _a2, _b2, _c, _d, _e2, _f;
    const prevPermissions = this.permissions;
    const changed = permissions.canPublish !== ((_a2 = this.permissions) === null || _a2 === void 0 ? void 0 : _a2.canPublish) || permissions.canSubscribe !== ((_b2 = this.permissions) === null || _b2 === void 0 ? void 0 : _b2.canSubscribe) || permissions.canPublishData !== ((_c = this.permissions) === null || _c === void 0 ? void 0 : _c.canPublishData) || permissions.hidden !== ((_d = this.permissions) === null || _d === void 0 ? void 0 : _d.hidden) || permissions.recorder !== ((_e2 = this.permissions) === null || _e2 === void 0 ? void 0 : _e2.recorder) || permissions.canPublishSources.length !== this.permissions.canPublishSources.length || permissions.canPublishSources.some((value, index) => {
      var _a3;
      return value !== ((_a3 = this.permissions) === null || _a3 === void 0 ? void 0 : _a3.canPublishSources[index]);
    }) || permissions.canSubscribeMetrics !== ((_f = this.permissions) === null || _f === void 0 ? void 0 : _f.canSubscribeMetrics);
    this.permissions = permissions;
    if (changed) {
      this.emit(ParticipantEvent.ParticipantPermissionsChanged, prevPermissions);
    }
    return changed;
  }
  /** @internal */
  setIsSpeaking(speaking) {
    if (speaking === this.isSpeaking) {
      return;
    }
    this.isSpeaking = speaking;
    if (speaking) {
      this.lastSpokeAt = /* @__PURE__ */ new Date();
    }
    this.emit(ParticipantEvent.IsSpeakingChanged, speaking);
  }
  /** @internal */
  setConnectionQuality(q3) {
    const prevQuality = this._connectionQuality;
    this._connectionQuality = qualityFromProto(q3);
    if (prevQuality !== this._connectionQuality) {
      this.emit(ParticipantEvent.ConnectionQualityChanged, this._connectionQuality);
    }
  }
  /**
   * @internal
   */
  setDisconnected() {
    var _a2, _b2;
    if (this.activeFuture) {
      (_b2 = (_a2 = this.activeFuture).reject) === null || _b2 === void 0 ? void 0 : _b2.call(_a2, new Error("Participant disconnected"));
      this.activeFuture = void 0;
    }
  }
  /**
   * @internal
   */
  setAudioContext(ctx) {
    this.audioContext = ctx;
    this.audioTrackPublications.forEach((track) => isAudioTrack(track.track) && track.track.setAudioContext(ctx));
  }
  addTrackPublication(publication) {
    this.log.debug("adding track publication", {
      trackSid: publication.trackSid,
      source: publication.source,
      kind: publication.kind
    });
    publication.on(TrackEvent.Muted, () => {
      this.emit(ParticipantEvent.TrackMuted, publication);
    });
    publication.on(TrackEvent.Unmuted, () => {
      this.emit(ParticipantEvent.TrackUnmuted, publication);
    });
    const pub = publication;
    if (pub.track) {
      pub.track.sid = publication.trackSid;
    }
    this.trackPublications.set(publication.trackSid, publication);
    switch (publication.kind) {
      case Track.Kind.Audio:
        this.audioTrackPublications.set(publication.trackSid, publication);
        break;
      case Track.Kind.Video:
        this.videoTrackPublications.set(publication.trackSid, publication);
        break;
    }
  }
};
function trackPermissionToProto(perms) {
  var _a2, _b2, _c;
  if (!perms.participantSid && !perms.participantIdentity) {
    throw new Error("Invalid track permission, must provide at least one of participantIdentity and participantSid");
  }
  return new TrackPermission({
    participantIdentity: (_a2 = perms.participantIdentity) !== null && _a2 !== void 0 ? _a2 : "",
    participantSid: (_b2 = perms.participantSid) !== null && _b2 !== void 0 ? _b2 : "",
    allTracks: (_c = perms.allowAll) !== null && _c !== void 0 ? _c : false,
    trackSids: perms.allowedTrackSids || []
  });
}
var LocalParticipant = class extends Participant {
  /** @internal */
  constructor(sid, identity, engine, options, roomOutgoingDataStreamManager, roomOutgoingDataTrackManager, rpcClientManager, rpcServerManager) {
    super(sid, identity, void 0, void 0, void 0, {
      loggerName: options.loggerName,
      loggerContextCb: () => this.engine.logContext
    });
    this.pendingPublishing = /* @__PURE__ */ new Set();
    this.pendingPublishPromises = /* @__PURE__ */ new Map();
    this.participantTrackPermissions = [];
    this.allParticipantsAllowedToSubscribe = true;
    this.encryptionType = Encryption_Type.NONE;
    this.e2eeStateMutex = new _();
    this.enabledPublishVideoCodecs = [];
    this.handleReconnecting = () => {
      if (!this.reconnectFuture) {
        this.reconnectFuture = new Future();
      }
    };
    this.handleReconnected = () => {
      var _a2, _b2;
      (_b2 = (_a2 = this.reconnectFuture) === null || _a2 === void 0 ? void 0 : _a2.resolve) === null || _b2 === void 0 ? void 0 : _b2.call(_a2);
      this.reconnectFuture = void 0;
      this.updateTrackSubscriptionPermissions();
    };
    this.handleClosing = () => {
      var _a2, _b2, _c, _d, _e2, _f;
      if (this.reconnectFuture) {
        this.reconnectFuture.promise.catch((e4) => this.log.warn(e4.message));
        (_b2 = (_a2 = this.reconnectFuture) === null || _a2 === void 0 ? void 0 : _a2.reject) === null || _b2 === void 0 ? void 0 : _b2.call(_a2, new Error("Got disconnected during reconnection attempt"));
        this.reconnectFuture = void 0;
      }
      if (this.signalConnectedFuture) {
        (_d = (_c = this.signalConnectedFuture).reject) === null || _d === void 0 ? void 0 : _d.call(_c, new Error("Got disconnected without signal connected"));
        this.signalConnectedFuture = void 0;
      }
      (_f = (_e2 = this.activeAgentFuture) === null || _e2 === void 0 ? void 0 : _e2.reject) === null || _f === void 0 ? void 0 : _f.call(_e2, new Error("Got disconnected without active agent present"));
      this.activeAgentFuture = void 0;
      this.firstActiveAgent = void 0;
    };
    this.handleSignalConnected = (joinResponse) => {
      var _a2, _b2;
      if (joinResponse.participant) {
        this.updateInfo(joinResponse.participant);
      }
      if (!this.signalConnectedFuture) {
        this.signalConnectedFuture = new Future();
      }
      (_b2 = (_a2 = this.signalConnectedFuture).resolve) === null || _b2 === void 0 ? void 0 : _b2.call(_a2);
    };
    this.handleSignalRequestResponse = (response) => {
      const requestId = response.requestId, reason = response.reason, message = response.message;
      const targetRequest = this.pendingSignalRequests.get(requestId);
      if (targetRequest) {
        if (reason !== RequestResponse_Reason.OK) {
          targetRequest.reject(new SignalRequestError(message, reason));
        }
        this.pendingSignalRequests.delete(requestId);
      }
      switch (response.request.case) {
        case "publishDataTrack": {
          let error;
          switch (response.reason) {
            case RequestResponse_Reason.NOT_ALLOWED:
              error = DataTrackPublishError.notAllowed(response.message);
              break;
            case RequestResponse_Reason.DUPLICATE_NAME:
              error = DataTrackPublishError.duplicateName(response.message);
              break;
            case RequestResponse_Reason.INVALID_NAME:
              error = DataTrackPublishError.invalidName(response.message);
              break;
            case RequestResponse_Reason.LIMIT_EXCEEDED:
              error = DataTrackPublishError.limitReached(response.message);
              break;
            default:
              error = DataTrackPublishError.unknown(response.reason, response.message);
              break;
          }
          this.roomOutgoingDataTrackManager.receivedSfuPublishResponse(response.request.value.pubHandle, {
            type: "error",
            error
          });
          break;
        }
      }
    };
    this.updateTrackSubscriptionPermissions = () => {
      this.log.debug("updating track subscription permissions", {
        allParticipantsAllowed: this.allParticipantsAllowedToSubscribe,
        participantTrackPermissions: this.participantTrackPermissions
      });
      this.engine.client.sendUpdateSubscriptionPermissions(this.allParticipantsAllowedToSubscribe, this.participantTrackPermissions.map((p) => trackPermissionToProto(p)));
    };
    this.onTrackUnmuted = (track) => {
      this.onTrackMuted(track, track.isUpstreamPaused);
    };
    this.onTrackMuted = (track, muted) => {
      if (muted === void 0) {
        muted = true;
      }
      if (!track.sid) {
        this.log.error("could not update mute status for unpublished track", getLogContextFromTrack(track));
        return;
      }
      this.engine.updateMuteStatus(track.sid, muted);
    };
    this.onTrackUpstreamPaused = (track) => {
      this.log.debug("upstream paused", getLogContextFromTrack(track));
      this.onTrackMuted(track, true);
    };
    this.onTrackUpstreamResumed = (track) => {
      this.log.debug("upstream resumed", getLogContextFromTrack(track));
      this.onTrackMuted(track, track.isMuted);
    };
    this.onTrackFeatureUpdate = (track) => {
      const pub = this.audioTrackPublications.get(track.sid);
      if (!pub) {
        this.log.warn("Could not update local audio track settings, missing publication for track ".concat(track.sid));
        return;
      }
      this.engine.client.sendUpdateLocalAudioTrack(pub.trackSid, pub.getTrackFeatures());
    };
    this.onTrackCpuConstrained = (track, publication) => {
      this.log.debug("track cpu constrained", getLogContextFromTrack(publication));
      this.emit(ParticipantEvent.LocalTrackCpuConstrained, track, publication);
    };
    this.handleSubscribedQualityUpdate = (update) => __awaiter(this, void 0, void 0, function* () {
      var _a2, e_1, _b2, _c;
      var _d;
      if (!((_d = this.roomOptions) === null || _d === void 0 ? void 0 : _d.dynacast)) {
        return;
      }
      const pub = this.videoTrackPublications.get(update.trackSid);
      if (!pub) {
        this.log.warn("received subscribed quality update for unknown track", {
          trackSid: update.trackSid
        });
        return;
      }
      if (!pub.videoTrack) {
        return;
      }
      const newCodecs = yield pub.videoTrack.setPublishingCodecs(update.subscribedCodecs);
      try {
        for (var _e2 = true, newCodecs_1 = __asyncValues(newCodecs), newCodecs_1_1; newCodecs_1_1 = yield newCodecs_1.next(), _a2 = newCodecs_1_1.done, !_a2; _e2 = true) {
          _c = newCodecs_1_1.value;
          _e2 = false;
          const codec = _c;
          if (isBackupCodec(codec)) {
            this.log.debug("publish ".concat(codec, " for ").concat(pub.videoTrack.sid), getLogContextFromTrack(pub));
            yield this.publishAdditionalCodecForTrack(pub.videoTrack, codec, pub.options);
          }
        }
      } catch (e_1_1) {
        e_1 = {
          error: e_1_1
        };
      } finally {
        try {
          if (!_e2 && !_a2 && (_b2 = newCodecs_1.return)) yield _b2.call(newCodecs_1);
        } finally {
          if (e_1) throw e_1.error;
        }
      }
    });
    this.handleLocalTrackUnpublished = (unpublished) => {
      const track = this.trackPublications.get(unpublished.trackSid);
      if (!track) {
        this.log.warn("received unpublished event for unknown track", {
          trackSid: unpublished.trackSid
        });
        return;
      }
      this.unpublishTrack(track.track);
    };
    this.handleTrackEnded = (track) => __awaiter(this, void 0, void 0, function* () {
      if (track.source === Track.Source.ScreenShare || track.source === Track.Source.ScreenShareAudio) {
        this.log.debug("unpublishing local track due to TrackEnded", getLogContextFromTrack(track));
        this.unpublishTrack(track);
      } else if (track.isUserProvided) {
        yield track.mute();
      } else if (isLocalAudioTrack(track) || isLocalVideoTrack(track)) {
        try {
          if (isWeb()) {
            try {
              const currentPermissions = yield navigator === null || navigator === void 0 ? void 0 : navigator.permissions.query({
                // the permission query for camera and microphone currently not supported in Safari and Firefox
                // @ts-ignore
                name: track.source === Track.Source.Camera ? "camera" : "microphone"
              });
              if (currentPermissions && currentPermissions.state === "denied") {
                this.log.warn("user has revoked access to ".concat(track.source), getLogContextFromTrack(track));
                currentPermissions.onchange = () => {
                  if (currentPermissions.state !== "denied") {
                    if (!track.isMuted) {
                      track.restartTrack();
                    }
                    currentPermissions.onchange = null;
                  }
                };
                throw new Error("GetUserMedia Permission denied");
              }
            } catch (e4) {
            }
          }
          if (!track.isMuted) {
            this.log.debug("track ended, attempting to use a different device", getLogContextFromTrack(track));
            if (isLocalAudioTrack(track)) {
              yield track.restartTrack({
                deviceId: "default"
              });
            } else {
              yield track.restartTrack();
            }
          }
        } catch (e4) {
          this.log.warn("could not restart track, muting instead", getLogContextFromTrack(track));
          yield track.mute();
        }
      }
    });
    this.audioTrackPublications = /* @__PURE__ */ new Map();
    this.videoTrackPublications = /* @__PURE__ */ new Map();
    this.trackPublications = /* @__PURE__ */ new Map();
    this.engine = engine;
    this.roomOptions = options;
    this.setupEngine(engine);
    this.activeDeviceMap = /* @__PURE__ */ new Map([["audioinput", "default"], ["videoinput", "default"], ["audiooutput", "default"]]);
    this.pendingSignalRequests = /* @__PURE__ */ new Map();
    this.roomOutgoingDataStreamManager = roomOutgoingDataStreamManager;
    this.roomOutgoingDataTrackManager = roomOutgoingDataTrackManager;
    this.rpcClientManager = rpcClientManager;
    this.rpcServerManager = rpcServerManager;
  }
  get lastCameraError() {
    return this.cameraError;
  }
  get lastMicrophoneError() {
    return this.microphoneError;
  }
  get isE2EEEnabled() {
    return this.encryptionType !== Encryption_Type.NONE;
  }
  getTrackPublication(source) {
    const track = super.getTrackPublication(source);
    if (track) {
      return track;
    }
  }
  getTrackPublicationByName(name) {
    const track = super.getTrackPublicationByName(name);
    if (track) {
      return track;
    }
  }
  /**
   * @internal
   */
  setupEngine(engine) {
    var _a2;
    this.engine = engine;
    this.engine.on(EngineEvent.RemoteMute, (trackSid, muted) => {
      const pub = this.trackPublications.get(trackSid);
      if (!pub || !pub.track) {
        return;
      }
      if (muted) {
        pub.mute();
      } else {
        pub.unmute();
      }
    });
    if ((_a2 = this.signalConnectedFuture) === null || _a2 === void 0 ? void 0 : _a2.isResolved) {
      this.signalConnectedFuture = void 0;
    }
    this.engine.on(EngineEvent.Connected, this.handleReconnected).on(EngineEvent.SignalConnected, this.handleSignalConnected).on(EngineEvent.SignalRestarted, this.handleReconnected).on(EngineEvent.SignalResumed, this.handleReconnected).on(EngineEvent.Restarting, this.handleReconnecting).on(EngineEvent.Resuming, this.handleReconnecting).on(EngineEvent.LocalTrackUnpublished, this.handleLocalTrackUnpublished).on(EngineEvent.SubscribedQualityUpdate, this.handleSubscribedQualityUpdate).on(EngineEvent.Closing, this.handleClosing).on(EngineEvent.SignalRequestResponse, this.handleSignalRequestResponse);
  }
  /**
   * Sets and updates the metadata of the local participant.
   * Note: this requires `canUpdateOwnMetadata` permission.
   * method will throw if the user doesn't have the required permissions
   * @param metadata
   */
  setMetadata(metadata) {
    return __awaiter(this, void 0, void 0, function* () {
      yield this.requestMetadataUpdate({
        metadata
      });
    });
  }
  /**
   * Sets and updates the name of the local participant.
   * Note: this requires `canUpdateOwnMetadata` permission.
   * method will throw if the user doesn't have the required permissions
   * @param metadata
   */
  setName(name) {
    return __awaiter(this, void 0, void 0, function* () {
      yield this.requestMetadataUpdate({
        name
      });
    });
  }
  /**
   * Set or update participant attributes. It will make updates only to keys that
   * are present in `attributes`, and will not override others.
   * Note: this requires `canUpdateOwnMetadata` permission.
   * @param attributes attributes to update
   */
  setAttributes(attributes) {
    return __awaiter(this, void 0, void 0, function* () {
      yield this.requestMetadataUpdate({
        attributes
      });
    });
  }
  requestMetadataUpdate(_a2) {
    return __awaiter(this, arguments, void 0, function(_ref) {
      var _this = this;
      let metadata = _ref.metadata, name = _ref.name, attributes = _ref.attributes;
      return function* () {
        return new TypedPromise((resolve, reject) => __awaiter(_this, void 0, void 0, function* () {
          var _a3, _b2;
          try {
            let isRejected = false;
            const requestId = yield this.engine.client.sendUpdateLocalMetadata((_a3 = metadata !== null && metadata !== void 0 ? metadata : this.metadata) !== null && _a3 !== void 0 ? _a3 : "", (_b2 = name !== null && name !== void 0 ? name : this.name) !== null && _b2 !== void 0 ? _b2 : "", attributes);
            const startTime = performance.now();
            this.pendingSignalRequests.set(requestId, {
              resolve,
              reject: (error) => {
                reject(error);
                isRejected = true;
              },
              values: {
                name,
                metadata,
                attributes
              }
            });
            while (performance.now() - startTime < 5e3 && !isRejected) {
              if ((!name || this.name === name) && (!metadata || this.metadata === metadata) && (!attributes || Object.entries(attributes).every((_ref2) => {
                let _ref3 = _slicedToArray(_ref2, 2), key = _ref3[0], value = _ref3[1];
                return this.attributes[key] === value || value === "" && !this.attributes[key];
              }))) {
                this.pendingSignalRequests.delete(requestId);
                resolve();
                return;
              }
              yield sleep(50);
            }
            reject(new SignalRequestError("Request to update local metadata timed out", "TimeoutError"));
          } catch (e4) {
            if (e4 instanceof Error) {
              reject(e4);
            } else {
              reject(new Error(String(e4)));
            }
          }
        }));
      }();
    });
  }
  /**
   * Enable or disable a participant's camera track.
   *
   * If a track has already published, it'll mute or unmute the track.
   * Resolves with a `LocalTrackPublication` instance if successful and `undefined` otherwise
   */
  setCameraEnabled(enabled, options, publishOptions) {
    return this.setTrackEnabled(Track.Source.Camera, enabled, options, publishOptions);
  }
  /**
   * Enable or disable a participant's microphone track.
   *
   * If a track has already published, it'll mute or unmute the track.
   * Resolves with a `LocalTrackPublication` instance if successful and `undefined` otherwise
   */
  setMicrophoneEnabled(enabled, options, publishOptions) {
    return this.setTrackEnabled(Track.Source.Microphone, enabled, options, publishOptions);
  }
  /**
   * Start or stop sharing a participant's screen
   * Resolves with a `LocalTrackPublication` instance if successful and `undefined` otherwise
   */
  setScreenShareEnabled(enabled, options, publishOptions) {
    return this.setTrackEnabled(Track.Source.ScreenShare, enabled, options, publishOptions);
  }
  /** @internal */
  setE2EEEnabled(enabled) {
    return __awaiter(this, void 0, void 0, function* () {
      const unlock = yield this.e2eeStateMutex.lock();
      try {
        this.encryptionType = enabled ? Encryption_Type.GCM : Encryption_Type.NONE;
        yield Promise.all(this.pendingPublishPromises.values());
        if (this.trackPublications.size === 0 || Array.from(this.trackPublications.values()).every((pub) => pub.isEncrypted === enabled)) {
          return;
        }
        yield this.republishAllTracks(void 0, false);
      } finally {
        unlock();
      }
    });
  }
  setTrackEnabled(source, enabled, options, publishOptions) {
    return __awaiter(this, void 0, void 0, function* () {
      var _a2, _b2;
      this.log.debug("setTrackEnabled", {
        source,
        enabled
      });
      if (this.republishPromise) {
        yield this.republishPromise;
      }
      let track = this.getTrackPublication(source);
      if (enabled) {
        if (track) {
          yield track.unmute();
        } else {
          let localTracks;
          if (this.pendingPublishing.has(source)) {
            const pendingTrack = yield this.waitForPendingPublicationOfSource(source);
            if (!pendingTrack) {
              this.log.info("waiting for pending publication promise timed out", {
                source
              });
            }
            yield pendingTrack === null || pendingTrack === void 0 ? void 0 : pendingTrack.unmute();
            return pendingTrack;
          }
          this.pendingPublishing.add(source);
          try {
            switch (source) {
              case Track.Source.Camera:
                localTracks = yield this.createTracks({
                  video: (_a2 = options) !== null && _a2 !== void 0 ? _a2 : true
                });
                break;
              case Track.Source.Microphone:
                localTracks = yield this.createTracks({
                  audio: (_b2 = options) !== null && _b2 !== void 0 ? _b2 : true
                });
                break;
              case Track.Source.ScreenShare:
                localTracks = yield this.createScreenTracks(Object.assign({}, options));
                break;
              default:
                throw new TrackInvalidError(source);
            }
          } catch (e4) {
            localTracks === null || localTracks === void 0 ? void 0 : localTracks.forEach((tr3) => {
              tr3.stop();
            });
            if (e4 instanceof Error) {
              this.emit(ParticipantEvent.MediaDevicesError, e4, sourceToKind(source));
            }
            this.pendingPublishing.delete(source);
            throw e4;
          }
          for (const localTrack of localTracks) {
            const opts = Object.assign(Object.assign({}, this.roomOptions.publishDefaults), options);
            if (source === Track.Source.Microphone && isAudioTrack(localTrack) && opts.preConnectBuffer) {
              this.log.info("starting preconnect buffer for microphone");
              localTrack.startPreConnectBuffer();
            }
          }
          try {
            const publishPromises = [];
            for (const localTrack of localTracks) {
              this.log.info("publishing track", getLogContextFromTrack(localTrack));
              publishPromises.push(this.publishTrack(localTrack, publishOptions));
            }
            const publishedTracks = yield Promise.all(publishPromises);
            var _publishedTracks = _slicedToArray(publishedTracks, 1);
            track = _publishedTracks[0];
          } catch (e4) {
            localTracks === null || localTracks === void 0 ? void 0 : localTracks.forEach((tr3) => {
              tr3.stop();
            });
            throw e4;
          } finally {
            this.pendingPublishing.delete(source);
          }
        }
      } else {
        if (!(track === null || track === void 0 ? void 0 : track.track) && this.pendingPublishing.has(source)) {
          track = yield this.waitForPendingPublicationOfSource(source);
          if (!track) {
            this.log.info("waiting for pending publication promise timed out", {
              source
            });
          }
        }
        if (track && track.track) {
          if (source === Track.Source.ScreenShare) {
            const unpublishPromises = [this.unpublishTrack(track.track)];
            const screenAudioTrack = this.getTrackPublication(Track.Source.ScreenShareAudio);
            if (screenAudioTrack && screenAudioTrack.track) {
              unpublishPromises.push(this.unpublishTrack(screenAudioTrack.track));
            }
            var _yield$Promise$all = yield Promise.all(unpublishPromises);
            var _yield$Promise$all2 = _slicedToArray(_yield$Promise$all, 1);
            track = _yield$Promise$all2[0];
          } else {
            yield track.mute();
          }
        }
      }
      return track;
    });
  }
  /**
   * Publish both camera and microphone at the same time. This is useful for
   * displaying a single Permission Dialog box to the end user.
   */
  enableCameraAndMicrophone() {
    return __awaiter(this, void 0, void 0, function* () {
      if (this.pendingPublishing.has(Track.Source.Camera) || this.pendingPublishing.has(Track.Source.Microphone)) {
        return;
      }
      this.pendingPublishing.add(Track.Source.Camera);
      this.pendingPublishing.add(Track.Source.Microphone);
      try {
        const tracks = yield this.createTracks({
          audio: true,
          video: true
        });
        yield Promise.all(tracks.map((track) => this.publishTrack(track)));
      } finally {
        this.pendingPublishing.delete(Track.Source.Camera);
        this.pendingPublishing.delete(Track.Source.Microphone);
      }
    });
  }
  /**
   * Create local camera and/or microphone tracks
   * @param options
   * @returns
   */
  createTracks(options) {
    return __awaiter(this, void 0, void 0, function* () {
      var _a2, _b2;
      options !== null && options !== void 0 ? options : options = {};
      const mergedOptionsWithProcessors = mergeDefaultOptions(options, (_a2 = this.roomOptions) === null || _a2 === void 0 ? void 0 : _a2.audioCaptureDefaults, (_b2 = this.roomOptions) === null || _b2 === void 0 ? void 0 : _b2.videoCaptureDefaults);
      try {
        const tracks = yield createLocalTracks(mergedOptionsWithProcessors, {
          loggerName: this.roomOptions.loggerName,
          loggerContextCb: () => this.logContext
        });
        const localTracks = tracks.map((track) => {
          if (isAudioTrack(track)) {
            this.microphoneError = void 0;
            track.setAudioContext(this.audioContext);
            track.source = Track.Source.Microphone;
            this.emit(ParticipantEvent.AudioStreamAcquired);
          }
          if (isVideoTrack(track)) {
            this.cameraError = void 0;
            track.source = Track.Source.Camera;
          }
          return track;
        });
        return localTracks;
      } catch (err) {
        if (err instanceof Error) {
          if (options.audio) {
            this.microphoneError = err;
          }
          if (options.video) {
            this.cameraError = err;
          }
        }
        throw err;
      }
    });
  }
  /**
   * Creates a screen capture tracks with getDisplayMedia().
   * A LocalVideoTrack is always created and returned.
   * If { audio: true }, and the browser supports audio capture, a LocalAudioTrack is also created.
   */
  createScreenTracks(options) {
    return __awaiter(this, void 0, void 0, function* () {
      if (options === void 0) {
        options = {};
      }
      if (navigator.mediaDevices.getDisplayMedia === void 0) {
        throw new DeviceUnsupportedError("getDisplayMedia not supported");
      }
      if (options.resolution === void 0 && !isSafari17Based()) {
        options.resolution = ScreenSharePresets.h1080fps30.resolution;
      }
      const constraints = screenCaptureToDisplayMediaStreamOptions(options);
      const stream = yield navigator.mediaDevices.getDisplayMedia(constraints);
      const tracks = stream.getVideoTracks();
      if (tracks.length === 0) {
        throw new TrackInvalidError("no video track found");
      }
      const screenVideo = new LocalVideoTrack(tracks[0], void 0, false, {
        loggerName: this.roomOptions.loggerName,
        loggerContextCb: () => this.logContext
      });
      screenVideo.source = Track.Source.ScreenShare;
      if (options.contentHint) {
        screenVideo.mediaStreamTrack.contentHint = options.contentHint;
      }
      const localTracks = [screenVideo];
      if (stream.getAudioTracks().length > 0) {
        this.emit(ParticipantEvent.AudioStreamAcquired);
        const screenAudio = new LocalAudioTrack(stream.getAudioTracks()[0], void 0, false, this.audioContext, {
          loggerName: this.roomOptions.loggerName,
          loggerContextCb: () => this.logContext
        });
        screenAudio.source = Track.Source.ScreenShareAudio;
        localTracks.push(screenAudio);
      }
      return localTracks;
    });
  }
  /**
   * Publish a new track to the room
   * @param track
   * @param options
   */
  publishTrack(track, options) {
    return __awaiter(this, void 0, void 0, function* () {
      return this.publishOrRepublishTrack(track, options);
    });
  }
  /**
   * Waits for the engine's next `Restarted` event. Unlike `engine.waitForRestarted`, this does
   * not short-circuit when `pcState === Connected` — at the point this is called (right after a
   * `NegotiationError`) the PC transport is still connected, but `fullReconnectOnNext` has been
   * set and `attemptReconnect` is queued via setTimeout. We need to wait for that restart to
   * actually complete (which clears `pendingTrackResolvers` via `cleanupClient`) before retrying.
   */
  waitForNextEngineRestart() {
    let timeoutMs = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : 15e3;
    return new Promise((resolve, reject) => {
      const cleanup = () => {
        clearTimeout(timeout);
        this.engine.off(EngineEvent.Restarted, onRestarted);
        this.engine.off(EngineEvent.Closing, onClosing);
      };
      const onRestarted = () => {
        cleanup();
        resolve();
      };
      const onClosing = () => {
        cleanup();
        reject(new Error("engine closed before restart completed"));
      };
      const timeout = setTimeout(() => {
        cleanup();
        reject(new Error("timed out waiting for engine restart"));
      }, timeoutMs);
      this.engine.once(EngineEvent.Restarted, onRestarted);
      this.engine.once(EngineEvent.Closing, onClosing);
    });
  }
  publishOrRepublishTrack(track_1, options_1) {
    return __awaiter(this, arguments, void 0, function(track, options) {
      var _this2 = this;
      let isRepublish = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : false;
      let hasRetriedAfterNegotiationError = arguments.length > 3 && arguments[3] !== void 0 ? arguments[3] : false;
      return function* () {
        var _a2, _b2, _c, _d;
        if (isLocalAudioTrack(track)) {
          track.setAudioContext(_this2.audioContext);
        }
        yield (_a2 = _this2.reconnectFuture) === null || _a2 === void 0 ? void 0 : _a2.promise;
        if (_this2.republishPromise && !isRepublish) {
          yield _this2.republishPromise;
        }
        if (isLocalTrack(track) && _this2.pendingPublishPromises.has(track)) {
          yield _this2.pendingPublishPromises.get(track);
        }
        let defaultConstraints;
        if (track instanceof MediaStreamTrack) {
          defaultConstraints = track.getConstraints();
        } else {
          defaultConstraints = track.constraints;
          let deviceKind = void 0;
          switch (track.source) {
            case Track.Source.Microphone:
              deviceKind = "audioinput";
              break;
            case Track.Source.Camera:
              deviceKind = "videoinput";
          }
          if (deviceKind && _this2.activeDeviceMap.has(deviceKind)) {
            defaultConstraints = Object.assign(Object.assign({}, defaultConstraints), {
              deviceId: _this2.activeDeviceMap.get(deviceKind)
            });
          }
        }
        if (track instanceof MediaStreamTrack) {
          switch (track.kind) {
            case "audio":
              track = new LocalAudioTrack(track, defaultConstraints, true, _this2.audioContext, {
                loggerName: _this2.roomOptions.loggerName,
                loggerContextCb: () => _this2.logContext
              });
              break;
            case "video":
              track = new LocalVideoTrack(track, defaultConstraints, true, {
                loggerName: _this2.roomOptions.loggerName,
                loggerContextCb: () => _this2.logContext
              });
              break;
            default:
              throw new TrackInvalidError("unsupported MediaStreamTrack kind ".concat(track.kind));
          }
        } else {
          track.updateLoggerOptions({
            loggerName: _this2.roomOptions.loggerName,
            loggerContextCb: () => _this2.logContext
          });
        }
        let existingPublication;
        _this2.trackPublications.forEach((publication) => {
          if (!publication.track) {
            return;
          }
          if (publication.track === track) {
            existingPublication = publication;
          }
        });
        if (existingPublication) {
          _this2.log.warn("track has already been published, skipping", getLogContextFromTrack(existingPublication));
          return existingPublication;
        }
        const opts = Object.assign(Object.assign({}, _this2.roomOptions.publishDefaults), options);
        const isStereoInput = "channelCount" in track.mediaStreamTrack.getSettings() && // @ts-ignore `channelCount` on getSettings() is currently only available for Safari, but is generally the best way to determine a stereo track https://developer.mozilla.org/en-US/docs/Web/API/MediaTrackSettings/channelCount
        track.mediaStreamTrack.getSettings().channelCount === 2 || track.mediaStreamTrack.getConstraints().channelCount === 2;
        const isStereo = (_b2 = opts.forceStereo) !== null && _b2 !== void 0 ? _b2 : isStereoInput;
        if (isStereo) {
          if (opts.dtx === void 0) {
            _this2.log.debug("Opus DTX will be disabled for stereo tracks by default. Enable them explicitly to make it work.", getLogContextFromTrack(track));
          }
          if (opts.red === void 0) {
            _this2.log.debug("Opus RED will be disabled for stereo tracks by default. Enable them explicitly to make it work.");
          }
          (_c = opts.dtx) !== null && _c !== void 0 ? _c : opts.dtx = false;
          (_d = opts.red) !== null && _d !== void 0 ? _d : opts.red = false;
        }
        if (!isE2EESimulcastSupported() && _this2.roomOptions.e2ee) {
          _this2.log.info("End-to-end encryption is set up, simulcast publishing will be disabled on Safari versions and iOS browsers running iOS < v17.2");
          opts.simulcast = false;
        }
        if (opts.source) {
          track.source = opts.source;
        }
        const publishPromise = new Promise((resolve, reject) => __awaiter(_this2, void 0, void 0, function* () {
          try {
            if (this.engine.client.currentState !== SignalConnectionState.CONNECTED) {
              this.log.debug("deferring track publication until signal is connected", {
                track: getLogContextFromTrack(track)
              });
              let publicationTimedOut = false;
              const timeout = setTimeout(() => {
                publicationTimedOut = true;
                track.stop();
                reject(new PublishTrackError("publishing rejected as engine not connected within timeout", 408));
              }, 15e3);
              yield this.waitUntilEngineConnected();
              clearTimeout(timeout);
              if (publicationTimedOut) {
                return;
              }
              const publication = yield this.publish(track, opts, isStereo);
              resolve(publication);
            } else {
              try {
                const publication = yield this.publish(track, opts, isStereo);
                resolve(publication);
              } catch (e4) {
                reject(e4);
              }
            }
          } catch (e4) {
            reject(e4);
          }
        }));
        _this2.pendingPublishPromises.set(track, publishPromise);
        try {
          const publication = yield publishPromise;
          return publication;
        } catch (e4) {
          if (!hasRetriedAfterNegotiationError && e4 instanceof NegotiationError) {
            _this2.log.warn("negotiation due to track publish failed, retrying after reconnect", {
              error: e4
            });
            _this2.pendingPublishPromises.delete(track);
            yield _this2.waitForNextEngineRestart();
            return yield _this2.publishOrRepublishTrack(track, options, isRepublish, true);
          }
          throw e4;
        } finally {
          _this2.pendingPublishPromises.delete(track);
        }
      }();
    });
  }
  waitUntilEngineConnected() {
    if (!this.signalConnectedFuture) {
      this.signalConnectedFuture = new Future();
    }
    return this.signalConnectedFuture.promise;
  }
  hasPermissionsToPublish(track) {
    if (!this.permissions) {
      this.log.warn("no permissions present for publishing track", getLogContextFromTrack(track));
      return false;
    }
    const _this$permissions = this.permissions, canPublish = _this$permissions.canPublish, canPublishSources = _this$permissions.canPublishSources;
    if (canPublish && (canPublishSources.length === 0 || canPublishSources.map((source) => getTrackSourceFromProto(source)).includes(track.source))) {
      return true;
    }
    this.log.warn("insufficient permissions to publish", getLogContextFromTrack(track));
    return false;
  }
  publish(track, opts, isStereo) {
    return __awaiter(this, void 0, void 0, function* () {
      var _a2, _b2, _c, _d, _e2, _f, _g, _h, _j, _k;
      if (!this.hasPermissionsToPublish(track)) {
        throw new PublishTrackError("failed to publish track, insufficient permissions", 403);
      }
      const existingTrackOfSource = Array.from(this.trackPublications.values()).find((publishedTrack) => isLocalTrack(track) && publishedTrack.source === track.source);
      if (existingTrackOfSource && track.source !== Track.Source.Unknown) {
        this.log.info("publishing a second track with the same source: ".concat(track.source), getLogContextFromTrack(track));
      }
      if (opts.stopMicTrackOnMute && isAudioTrack(track)) {
        track.stopOnMute = true;
      }
      if (track.source === Track.Source.ScreenShare && isFireFox()) {
        opts.simulcast = false;
      }
      if (opts.videoCodec === "av1" && !supportsAV1()) {
        opts.videoCodec = void 0;
      }
      if (opts.videoCodec === "vp9" && !supportsVP9()) {
        opts.videoCodec = void 0;
      }
      if (opts.videoCodec === void 0) {
        opts.videoCodec = defaultVideoCodec;
      }
      if (this.enabledPublishVideoCodecs.length > 0) {
        if (!this.enabledPublishVideoCodecs.some((c) => opts.videoCodec === mimeTypeToVideoCodecString(c.mime))) {
          opts.videoCodec = mimeTypeToVideoCodecString(this.enabledPublishVideoCodecs[0].mime);
        }
      }
      const videoCodec = opts.videoCodec;
      track.on(TrackEvent.Muted, this.onTrackMuted);
      track.on(TrackEvent.Unmuted, this.onTrackUnmuted);
      track.on(TrackEvent.Ended, this.handleTrackEnded);
      track.on(TrackEvent.UpstreamPaused, this.onTrackUpstreamPaused);
      track.on(TrackEvent.UpstreamResumed, this.onTrackUpstreamResumed);
      track.on(TrackEvent.AudioTrackFeatureUpdate, this.onTrackFeatureUpdate);
      const audioFeatures = [];
      const disableDtx = !((_a2 = opts.dtx) !== null && _a2 !== void 0 ? _a2 : true);
      const settings = track.getSourceTrackSettings();
      if (settings.autoGainControl) {
        audioFeatures.push(AudioTrackFeature.TF_AUTO_GAIN_CONTROL);
      }
      if (settings.echoCancellation) {
        audioFeatures.push(AudioTrackFeature.TF_ECHO_CANCELLATION);
      }
      if (settings.noiseSuppression) {
        audioFeatures.push(AudioTrackFeature.TF_NOISE_SUPPRESSION);
      }
      if (settings.channelCount && settings.channelCount > 1) {
        audioFeatures.push(AudioTrackFeature.TF_STEREO);
      }
      if (disableDtx) {
        audioFeatures.push(AudioTrackFeature.TF_NO_DTX);
      }
      if (isLocalAudioTrack(track) && track.hasPreConnectBuffer) {
        audioFeatures.push(AudioTrackFeature.TF_PRECONNECT_BUFFER);
      }
      const packetTrailerFeatures = this.normalizeRequestedFrameMetadataOptions(track, opts);
      const req = new AddTrackRequest({
        // get local track id for use during publishing
        cid: track.mediaStreamTrack.id,
        name: opts.name,
        type: Track.kindToProto(track.kind),
        muted: track.isMuted,
        source: Track.sourceToProto(track.source),
        disableDtx,
        encryption: this.encryptionType,
        stereo: isStereo,
        disableRed: this.isE2EEEnabled || !((_b2 = opts.red) !== null && _b2 !== void 0 ? _b2 : true),
        stream: opts === null || opts === void 0 ? void 0 : opts.stream,
        backupCodecPolicy: opts === null || opts === void 0 ? void 0 : opts.backupCodecPolicy,
        audioFeatures,
        packetTrailerFeatures
      });
      let encodings;
      if (track.kind === Track.Kind.Video) {
        let dims;
        try {
          dims = yield track.waitForDimensions();
        } catch (e4) {
          const defaultRes = (_d = (_c = this.roomOptions.videoCaptureDefaults) === null || _c === void 0 ? void 0 : _c.resolution) !== null && _d !== void 0 ? _d : VideoPresets.h720.resolution;
          dims = {
            width: defaultRes.width,
            height: defaultRes.height
          };
          this.log.error("could not determine track dimensions, using defaults", Object.assign(Object.assign({}, getLogContextFromTrack(track)), {
            dims
          }));
        }
        req.width = dims.width;
        req.height = dims.height;
        if (isLocalVideoTrack(track)) {
          if (isSVCCodec(videoCodec)) {
            if (track.source === Track.Source.ScreenShare) {
              opts.scalabilityMode = "L1T3";
              if ("contentHint" in track.mediaStreamTrack) {
                track.mediaStreamTrack.contentHint = "motion";
                this.log.debug("forcing contentHint to motion for screenshare with SVC codecs", getLogContextFromTrack(track));
              }
            }
            opts.scalabilityMode = (_e2 = opts.scalabilityMode) !== null && _e2 !== void 0 ? _e2 : "L3T3_KEY";
          }
          req.simulcastCodecs = [new SimulcastCodec({
            codec: videoCodec,
            cid: track.mediaStreamTrack.id
          })];
          if (opts.backupCodec === true) {
            opts.backupCodec = {
              codec: defaultVideoCodec
            };
          }
          if (opts.backupCodec && videoCodec !== opts.backupCodec.codec && // TODO remove this once e2ee is supported for backup codecs
          req.encryption === Encryption_Type.NONE) {
            if (!this.roomOptions.dynacast) {
              this.roomOptions.dynacast = true;
            }
            req.simulcastCodecs.push(new SimulcastCodec({
              codec: opts.backupCodec.codec,
              cid: ""
            }));
          }
        }
        encodings = computeVideoEncodings(track.source === Track.Source.ScreenShare, req.width, req.height, opts);
        req.layers = videoLayersFromEncodings(req.width, req.height, encodings, isSVCCodec(opts.videoCodec));
      } else if (track.kind === Track.Kind.Audio) {
        encodings = [{
          maxBitrate: (_f = opts.audioPreset) === null || _f === void 0 ? void 0 : _f.maxBitrate,
          priority: (_h = (_g = opts.audioPreset) === null || _g === void 0 ? void 0 : _g.priority) !== null && _h !== void 0 ? _h : "high",
          networkPriority: (_k = (_j = opts.audioPreset) === null || _j === void 0 ? void 0 : _j.priority) !== null && _k !== void 0 ? _k : "high"
        }];
      }
      if (!this.engine || this.engine.isClosed) {
        throw new UnexpectedConnectionState("cannot publish track when not connected");
      }
      const negotiate = () => __awaiter(this, void 0, void 0, function* () {
        var _a3, _b3, _c2, _d2;
        if (!this.engine.pcManager) {
          throw new UnexpectedConnectionState("pcManager is not ready");
        }
        track.sender = yield this.engine.createSender(track, opts, encodings);
        if (isLocalVideoTrack(track)) {
          track.publishOptions = opts;
        }
        this.emit(ParticipantEvent.LocalSenderCreated, track.sender, track);
        if (isLocalVideoTrack(track)) {
          (_a3 = opts.degradationPreference) !== null && _a3 !== void 0 ? _a3 : opts.degradationPreference = getDefaultDegradationPreference(track);
          track.setDegradationPreference(opts.degradationPreference);
        }
        if (encodings) {
          if (isFireFox() && track.kind === Track.Kind.Audio) {
            let trackTransceiver = void 0;
            for (const transceiver of this.engine.pcManager.publisher.getTransceivers()) {
              if (transceiver.sender === track.sender) {
                trackTransceiver = transceiver;
                break;
              }
            }
            if (trackTransceiver) {
              this.engine.pcManager.publisher.setTrackCodecBitrate({
                transceiver: trackTransceiver,
                codec: "opus",
                maxbr: ((_b3 = encodings[0]) === null || _b3 === void 0 ? void 0 : _b3.maxBitrate) ? encodings[0].maxBitrate / 1e3 : 0
              });
            }
          } else if (track.codec && isVideoCodec(track.codec)) {
            const targetBitrate = isSVCCodec(track.codec) ? (_d2 = (_c2 = encodings[0]) === null || _c2 === void 0 ? void 0 : _c2.maxBitrate) !== null && _d2 !== void 0 ? _d2 : 0 : encodings.reduce((sum, enc) => {
              var _a4;
              return sum + ((_a4 = enc.maxBitrate) !== null && _a4 !== void 0 ? _a4 : 0);
            }, 0);
            if (targetBitrate > 0) {
              this.engine.pcManager.publisher.setTrackCodecBitrate({
                cid: req.cid,
                codec: track.codec,
                maxbr: targetBitrate / 1e3,
                isScreenShare: track.source === Track.Source.ScreenShare
              });
            }
          }
        }
        yield this.engine.negotiate();
      });
      let ti2;
      const addTrackPromise = new Promise((resolve, reject) => __awaiter(this, void 0, void 0, function* () {
        var _a3;
        try {
          ti2 = yield this.engine.addTrack(req);
          resolve(ti2);
        } catch (err) {
          if (track.sender && ((_a3 = this.engine.pcManager) === null || _a3 === void 0 ? void 0 : _a3.publisher)) {
            try {
              this.engine.pcManager.publisher.removeTrack(track.sender);
            } catch (e4) {
              this.log.error(e4);
            }
            yield this.engine.negotiate().catch((negotiateErr) => {
              this.log.error("failed to negotiate after removing track due to failed add track request", Object.assign(Object.assign({}, getLogContextFromTrack(track)), {
                error: negotiateErr
              }));
            });
          }
          reject(err);
        }
      }));
      if (this.enabledPublishVideoCodecs.length > 0 && packetTrailerFeatures.length === 0) {
        const rets = yield Promise.all([addTrackPromise, negotiate()]);
        ti2 = rets[0];
      } else {
        ti2 = yield addTrackPromise;
        let primaryCodecMime;
        ti2.codecs.forEach((codec) => {
          if (primaryCodecMime === void 0) {
            primaryCodecMime = codec.mimeType;
          }
        });
        if (primaryCodecMime && track.kind === Track.Kind.Video) {
          const updatedCodec = mimeTypeToVideoCodecString(primaryCodecMime);
          if (updatedCodec !== videoCodec) {
            this.log.debug("falling back to server selected codec", Object.assign(Object.assign({}, getLogContextFromTrack(track)), {
              codec: updatedCodec
            }));
            opts.videoCodec = updatedCodec;
            encodings = computeVideoEncodings(track.source === Track.Source.ScreenShare, req.width, req.height, opts);
          }
        }
        yield negotiate();
      }
      const publication = new LocalTrackPublication(track.kind, ti2, track, {
        loggerName: this.roomOptions.loggerName,
        loggerContextCb: () => this.logContext
      });
      publication.on(TrackEvent.CpuConstrained, (constrainedTrack) => this.onTrackCpuConstrained(constrainedTrack, publication));
      publication.options = opts;
      track.sid = ti2.sid;
      if (isLocalVideoTrack(track)) {
        track.publishOptions = opts;
        if (req.width && req.height) {
          track.lastEncodedDimensions = {
            width: req.width,
            height: req.height
          };
        }
      }
      this.log.debug("publishing ".concat(track.kind, " with encodings"), {
        encodings,
        trackInfo: ti2
      });
      if (isLocalVideoTrack(track)) {
        track.startMonitor(this.engine.client);
      } else if (isLocalAudioTrack(track)) {
        track.startMonitor();
      }
      this.addTrackPublication(publication);
      this.emit(ParticipantEvent.LocalTrackPublished, publication);
      if (isLocalAudioTrack(track) && ti2.audioFeatures.includes(AudioTrackFeature.TF_PRECONNECT_BUFFER)) {
        const stream = track.getPreConnectBuffer();
        const mimeType = track.getPreConnectBufferMimeType();
        this.on(ParticipantEvent.LocalTrackSubscribed, (pub) => {
          if (pub.trackSid === ti2.sid) {
            if (!track.hasPreConnectBuffer) {
              this.log.warn("subscribe event came to late, buffer already closed");
              return;
            }
            this.log.debug("finished recording preconnect buffer", getLogContextFromTrack(track));
            track.stopPreConnectBuffer();
          }
        });
        if (stream) {
          const bufferStreamPromise = new Promise((resolve, reject) => __awaiter(this, void 0, void 0, function* () {
            var _a3, e_2, _b3, _c2;
            var _d2, _e3;
            try {
              this.log.debug("waiting for agent", getLogContextFromTrack(track));
              const agentActiveTimeout = setTimeout(() => {
                reject(new Error("agent not active within 10 seconds"));
              }, 1e4);
              const agent = yield this.waitUntilActiveAgentPresent();
              clearTimeout(agentActiveTimeout);
              this.log.debug("sending preconnect buffer", getLogContextFromTrack(track));
              const writer2 = yield this.streamBytes({
                name: "preconnect-buffer",
                mimeType,
                topic: "lk.agent.pre-connect-audio-buffer",
                destinationIdentities: [agent.identity],
                attributes: {
                  trackId: publication.trackSid,
                  sampleRate: String((_d2 = settings.sampleRate) !== null && _d2 !== void 0 ? _d2 : "48000"),
                  channels: String((_e3 = settings.channelCount) !== null && _e3 !== void 0 ? _e3 : "1")
                }
              });
              try {
                for (var _f2 = true, stream_1 = __asyncValues(stream), stream_1_1; stream_1_1 = yield stream_1.next(), _a3 = stream_1_1.done, !_a3; _f2 = true) {
                  _c2 = stream_1_1.value;
                  _f2 = false;
                  const chunk = _c2;
                  yield writer2.write(chunk);
                }
              } catch (e_2_1) {
                e_2 = {
                  error: e_2_1
                };
              } finally {
                try {
                  if (!_f2 && !_a3 && (_b3 = stream_1.return)) yield _b3.call(stream_1);
                } finally {
                  if (e_2) throw e_2.error;
                }
              }
              yield writer2.close();
              resolve();
            } catch (e4) {
              reject(e4);
            }
          }));
          bufferStreamPromise.then(() => {
            this.log.debug("preconnect buffer sent successfully", getLogContextFromTrack(track));
          }).catch((e4) => {
            this.log.error("error sending preconnect buffer", Object.assign(Object.assign({}, getLogContextFromTrack(track)), {
              error: e4
            }));
          });
        }
      }
      return publication;
    });
  }
  canPublishFrameMetadata() {
    var _a2;
    return !!(this.roomOptions.e2ee || this.roomOptions.encryption || isFrameMetadataSupported((_a2 = this.roomOptions.frameMetadata) !== null && _a2 !== void 0 ? _a2 : this.roomOptions.packetTrailer));
  }
  normalizeRequestedFrameMetadataOptions(track, opts) {
    var _a2;
    const fmOpts = (_a2 = opts.frameMetadata) !== null && _a2 !== void 0 ? _a2 : opts.packetTrailer;
    if (track.kind !== Track.Kind.Video || !hasFrameMetadataPublishOptions(fmOpts)) {
      opts.frameMetadata = void 0;
      opts.packetTrailer = void 0;
      return [];
    }
    if (!this.canPublishFrameMetadata()) {
      this.log.warn("frame metadata transform not supported; not advertising features", Object.assign(Object.assign({}, this.logContext), getLogContextFromTrack(track)));
      opts.frameMetadata = void 0;
      opts.packetTrailer = void 0;
      return [];
    }
    const features = getFrameMetadataFeatures(fmOpts);
    const normalized = getFrameMetadataPublishOptions(features);
    opts.frameMetadata = normalized;
    opts.packetTrailer = normalized;
    return features;
  }
  get isLocal() {
    return true;
  }
  /** @internal
   * publish additional codec to existing track
   */
  publishAdditionalCodecForTrack(track, videoCodec, options) {
    return __awaiter(this, void 0, void 0, function* () {
      var _a2;
      if (this.encryptionType !== Encryption_Type.NONE) {
        return;
      }
      let existingPublication;
      this.trackPublications.forEach((publication) => {
        if (!publication.track) {
          return;
        }
        if (publication.track === track) {
          existingPublication = publication;
        }
      });
      if (!existingPublication) {
        throw new TrackInvalidError("track is not published");
      }
      if (!isLocalVideoTrack(track)) {
        throw new TrackInvalidError("track is not a video track");
      }
      const opts = Object.assign(Object.assign({}, (_a2 = this.roomOptions) === null || _a2 === void 0 ? void 0 : _a2.publishDefaults), options);
      const encodings = computeTrackBackupEncodings(track, videoCodec, opts);
      if (!encodings) {
        this.log.info("backup codec has been disabled, ignoring request to add additional codec for track", getLogContextFromTrack(track));
        return;
      }
      const simulcastTrack = track.addSimulcastTrack(videoCodec, encodings);
      if (!simulcastTrack) {
        return;
      }
      const packetTrailerFeatures = this.normalizeRequestedFrameMetadataOptions(track, opts);
      const req = new AddTrackRequest({
        cid: simulcastTrack.mediaStreamTrack.id,
        type: Track.kindToProto(track.kind),
        muted: track.isMuted,
        source: Track.sourceToProto(track.source),
        sid: track.sid,
        packetTrailerFeatures,
        simulcastCodecs: [{
          codec: opts.videoCodec,
          cid: simulcastTrack.mediaStreamTrack.id
        }]
      });
      req.layers = videoLayersFromEncodings(req.width, req.height, encodings);
      if (!this.engine || this.engine.isClosed) {
        throw new UnexpectedConnectionState("cannot publish track when not connected");
      }
      const negotiate = () => __awaiter(this, void 0, void 0, function* () {
        yield this.engine.createSimulcastSender(track, simulcastTrack, opts, encodings);
        yield this.engine.negotiate();
      });
      const rets = yield Promise.all([this.engine.addTrack(req), negotiate()]);
      const ti2 = rets[0];
      this.log.debug("published ".concat(videoCodec, " for track ").concat(track.sid), {
        encodings,
        trackInfo: ti2
      });
    });
  }
  unpublishTrack(track, stopOnUnpublish) {
    return __awaiter(this, void 0, void 0, function* () {
      var _a2, _b2;
      if (isLocalTrack(track)) {
        const publishPromise = this.pendingPublishPromises.get(track);
        if (publishPromise) {
          this.log.debug("awaiting publish promise before attempting to unpublish", getLogContextFromTrack(track));
          yield publishPromise;
        }
      }
      const publication = this.getPublicationForTrack(track);
      const pubLogContext = publication ? getLogContextFromTrack(publication) : void 0;
      this.log.info("unpublishing track", pubLogContext);
      if (!publication || !publication.track) {
        this.log.warn("track was not unpublished because no publication was found", pubLogContext);
        return void 0;
      }
      track = publication.track;
      track.off(TrackEvent.Muted, this.onTrackMuted);
      track.off(TrackEvent.Unmuted, this.onTrackUnmuted);
      track.off(TrackEvent.Ended, this.handleTrackEnded);
      track.off(TrackEvent.UpstreamPaused, this.onTrackUpstreamPaused);
      track.off(TrackEvent.UpstreamResumed, this.onTrackUpstreamResumed);
      track.off(TrackEvent.AudioTrackFeatureUpdate, this.onTrackFeatureUpdate);
      if (stopOnUnpublish === void 0) {
        stopOnUnpublish = (_b2 = (_a2 = this.roomOptions) === null || _a2 === void 0 ? void 0 : _a2.stopLocalTrackOnUnpublish) !== null && _b2 !== void 0 ? _b2 : true;
      }
      if (stopOnUnpublish) {
        track.stop();
      } else {
        track.stopMonitor();
      }
      let negotiationNeeded = false;
      const trackSender = track.sender;
      track.sender = void 0;
      if (this.engine.pcManager && this.engine.pcManager.currentState < PCTransportState.FAILED && trackSender) {
        try {
          for (const transceiver of this.engine.pcManager.publisher.getTransceivers()) {
            if (transceiver.sender === trackSender) {
              transceiver.direction = "inactive";
              negotiationNeeded = true;
            }
          }
          try {
            negotiationNeeded = this.engine.removeTrack(trackSender);
          } catch (e4) {
            this.log.warn(e4);
            negotiationNeeded = true;
          }
          if (isLocalVideoTrack(track)) {
            for (const _ref4 of track.simulcastCodecs) {
              var _ref5 = _slicedToArray(_ref4, 2);
              const trackInfo = _ref5[1];
              if (trackInfo.sender) {
                try {
                  negotiationNeeded = this.engine.removeTrack(trackInfo.sender);
                } catch (e4) {
                  this.log.warn(e4);
                  negotiationNeeded = true;
                }
                trackInfo.sender = void 0;
              }
            }
            track.simulcastCodecs.clear();
          }
        } catch (e4) {
          this.log.warn("failed to unpublish track", Object.assign(Object.assign({}, pubLogContext), {
            error: e4
          }));
        }
      }
      this.trackPublications.delete(publication.trackSid);
      switch (publication.kind) {
        case Track.Kind.Audio:
          this.audioTrackPublications.delete(publication.trackSid);
          break;
        case Track.Kind.Video:
          this.videoTrackPublications.delete(publication.trackSid);
          break;
      }
      this.emit(ParticipantEvent.LocalTrackUnpublished, publication);
      publication.setTrack(void 0);
      if (negotiationNeeded) {
        yield this.engine.negotiate();
      }
      return publication;
    });
  }
  unpublishTracks(tracks) {
    return __awaiter(this, void 0, void 0, function* () {
      const results = yield Promise.all(tracks.map((track) => this.unpublishTrack(track)));
      return results.filter((track) => !!track);
    });
  }
  republishAllTracks(options_1) {
    return __awaiter(this, arguments, void 0, function(options) {
      var _this3 = this;
      let restartTracks = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : true;
      return function* () {
        if (_this3.republishPromise) {
          yield _this3.republishPromise;
        }
        _this3.republishPromise = new TypedPromise((resolve, reject) => __awaiter(_this3, void 0, void 0, function* () {
          try {
            const localPubs = [];
            this.trackPublications.forEach((pub) => {
              if (pub.track) {
                if (options) {
                  pub.options = Object.assign(Object.assign({}, pub.options), options);
                }
                localPubs.push(pub);
              }
            });
            yield Promise.all(localPubs.map((pub) => __awaiter(this, void 0, void 0, function* () {
              const track = pub.track;
              yield this.unpublishTrack(track, false);
              if (restartTracks && !track.isMuted && track.source !== Track.Source.ScreenShare && track.source !== Track.Source.ScreenShareAudio && (isLocalAudioTrack(track) || isLocalVideoTrack(track)) && !track.isUserProvided) {
                this.log.debug("restarting existing track", {
                  track: pub.trackSid
                });
                yield track.restartTrack();
              }
              yield this.publishOrRepublishTrack(track, pub.options, true);
            })));
            resolve();
          } catch (error) {
            if (error instanceof Error) {
              reject(error);
            } else {
              reject(new Error(String(error)));
            }
          } finally {
            this.republishPromise = void 0;
          }
        }));
        yield _this3.republishPromise;
      }();
    });
  }
  /**
   * Publish a new data payload to the room. Data will be forwarded to each
   * participant in the room if the destination field in publishOptions is empty
   *
   * @param data Uint8Array of the payload. To send string data, use TextEncoder.encode
   * @param options optionally specify a `reliable`, `topic` and `destination`
   */
  publishData(data_1) {
    return __awaiter(this, arguments, void 0, function(data) {
      var _this4 = this;
      let options = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : {};
      return function* () {
        const kind = options.reliable ? DataChannelKind.RELIABLE : DataChannelKind.LOSSY;
        const dataPacketKind = options.reliable ? DataPacket_Kind.RELIABLE : DataPacket_Kind.LOSSY;
        const destinationIdentities = options.destinationIdentities;
        const topic = options.topic;
        let userPacket = new UserPacket({
          participantIdentity: _this4.identity,
          payload: data,
          destinationIdentities,
          topic
        });
        const packet = new DataPacket({
          kind: dataPacketKind,
          value: {
            case: "user",
            value: userPacket
          }
        });
        yield _this4.engine.sendDataPacket(packet, kind);
      }();
    });
  }
  /**
   * Publish SIP DTMF message to the room.
   *
   * @param code DTMF code
   * @param digit DTMF digit
   */
  publishDtmf(code, digit) {
    return __awaiter(this, void 0, void 0, function* () {
      const packet = new DataPacket({
        kind: DataPacket_Kind.RELIABLE,
        value: {
          case: "sipDtmf",
          value: new SipDTMF({
            code,
            digit
          })
        }
      });
      yield this.engine.sendDataPacket(packet, DataChannelKind.RELIABLE);
    });
  }
  /** @deprecated Consider migrating to {@link sendText} */
  sendChatMessage(text, options) {
    return __awaiter(this, void 0, void 0, function* () {
      const msg = {
        id: crypto.randomUUID(),
        message: text,
        timestamp: Date.now(),
        attachedFiles: options === null || options === void 0 ? void 0 : options.attachments
      };
      const packet = new DataPacket({
        value: {
          case: "chatMessage",
          value: new ChatMessage(Object.assign(Object.assign({}, msg), {
            timestamp: protoInt64.parse(msg.timestamp)
          }))
        }
      });
      yield this.engine.sendDataPacket(packet, DataChannelKind.RELIABLE);
      this.emit(ParticipantEvent.ChatMessage, msg);
      return msg;
    });
  }
  /** @deprecated Consider migrating to {@link sendText} */
  editChatMessage(editText, originalMessage) {
    return __awaiter(this, void 0, void 0, function* () {
      const msg = Object.assign(Object.assign({}, originalMessage), {
        message: editText,
        editTimestamp: Date.now()
      });
      const packet = new DataPacket({
        value: {
          case: "chatMessage",
          value: new ChatMessage(Object.assign(Object.assign({}, msg), {
            timestamp: protoInt64.parse(msg.timestamp),
            editTimestamp: protoInt64.parse(msg.editTimestamp)
          }))
        }
      });
      yield this.engine.sendDataPacket(packet, DataChannelKind.RELIABLE);
      this.emit(ParticipantEvent.ChatMessage, msg);
      return msg;
    });
  }
  /**
   * Sends the given string to participants in the room via the data channel.
   * For longer messages, consider using {@link streamText} instead.
   *
   * @param text The text payload
   * @param options.topic Topic identifier used to route the stream to appropriate handlers.
   */
  sendText(text, options) {
    return __awaiter(this, void 0, void 0, function* () {
      return this.roomOutgoingDataStreamManager.sendText(text, options);
    });
  }
  /**
   * Creates a new TextStreamWriter which can be used to stream text incrementally
   * to participants in the room via the data channel.
   *
   * @param options.topic Topic identifier used to route the stream to appropriate handlers.
   *
   * @internal
   * @experimental CAUTION, might get removed in a minor release
   */
  streamText(options) {
    return __awaiter(this, void 0, void 0, function* () {
      return this.roomOutgoingDataStreamManager.streamText(options);
    });
  }
  /** Send a File to all participants in the room via the data channel.
   * @param file The File object payload
   * @param options.topic Topic identifier used to route the stream to appropriate handlers.
   * @param options.onProgress A callback function used to monitor the upload progress percentage.
   */
  sendFile(file, options) {
    return __awaiter(this, void 0, void 0, function* () {
      return this.roomOutgoingDataStreamManager.sendFile(file, options);
    });
  }
  /**
   * Sends the given bytes to participants in the room via the data channel.
   * For files, consider using {@link sendFile}; for longer/incremental payloads, {@link streamBytes}.
   *
   * @param bytes The byte payload
   * @param options.topic Topic identifier used to route the stream to appropriate handlers.
   */
  sendBytes(bytes, options) {
    return __awaiter(this, void 0, void 0, function* () {
      return this.roomOutgoingDataStreamManager.sendBytes(bytes, options);
    });
  }
  /**
   * Stream bytes incrementally to participants in the room via the data channel.
   * For sending files, consider using {@link sendFile} instead.
   *
   * @param options.topic Topic identifier used to route the stream to appropriate handlers.
   */
  streamBytes(options) {
    return __awaiter(this, void 0, void 0, function* () {
      return this.roomOutgoingDataStreamManager.streamBytes(options);
    });
  }
  /**
   * Initiate an RPC call to a remote participant
   * @param params - Parameters for initiating the RPC call, see {@link PerformRpcParams}
   * @returns A promise that resolves with the response payload or rejects with an error.
   * @throws Error on failure. Details in `message`.
   */
  performRpc(params) {
    return this.rpcClientManager.performRpc(params).then((_ref6) => {
      let _ref7 = _slicedToArray(_ref6, 2);
      _ref7[0];
      let completionPromise = _ref7[1];
      return completionPromise;
    });
  }
  /**
   * @deprecated use `room.registerRpcMethod` instead
   */
  registerRpcMethod(method, handler) {
    this.rpcServerManager.registerRpcMethod(method, handler);
  }
  /**
   * @deprecated use `room.unregisterRpcMethod` instead
   */
  unregisterRpcMethod(method) {
    this.rpcServerManager.unregisterRpcMethod(method);
  }
  /**
   * Control who can subscribe to LocalParticipant's published tracks.
   *
   * By default, all participants can subscribe. This allows fine-grained control over
   * who is able to subscribe at a participant and track level.
   *
   * Note: if access is given at a track-level (i.e. both [allParticipantsAllowed] and
   * [ParticipantTrackPermission.allTracksAllowed] are false), any newer published tracks
   * will not grant permissions to any participants and will require a subsequent
   * permissions update to allow subscription.
   *
   * @param allParticipantsAllowed Allows all participants to subscribe all tracks.
   *  Takes precedence over [[participantTrackPermissions]] if set to true.
   *  By default this is set to true.
   * @param participantTrackPermissions Full list of individual permissions per
   *  participant/track. Any omitted participants will not receive any permissions.
   */
  setTrackSubscriptionPermissions(allParticipantsAllowed) {
    let participantTrackPermissions = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : [];
    this.participantTrackPermissions = participantTrackPermissions;
    this.allParticipantsAllowedToSubscribe = allParticipantsAllowed;
    if (!this.engine.client.isDisconnected) {
      this.updateTrackSubscriptionPermissions();
    }
  }
  /** @internal */
  setEnabledPublishCodecs(codecs) {
    this.enabledPublishVideoCodecs = codecs.filter((c) => c.mime.split("/")[0].toLowerCase() === "video");
  }
  /** @internal */
  updateInfo(info) {
    if (!super.updateInfo(info)) {
      return false;
    }
    info.tracks.forEach((ti2) => {
      var _a2, _b2;
      const pub = this.trackPublications.get(ti2.sid);
      if (pub) {
        const mutedOnServer = pub.isMuted || ((_b2 = (_a2 = pub.track) === null || _a2 === void 0 ? void 0 : _a2.isUpstreamPaused) !== null && _b2 !== void 0 ? _b2 : false);
        if (mutedOnServer !== ti2.muted) {
          this.log.debug("updating server mute state after reconcile", Object.assign(Object.assign({}, getLogContextFromTrack(pub)), {
            mutedOnServer
          }));
          this.engine.client.sendMuteTrack(ti2.sid, mutedOnServer);
        }
      }
    });
    return true;
  }
  /** @internal */
  setActiveAgent(agent) {
    var _a2, _b2, _c, _d;
    this.firstActiveAgent = agent;
    if (agent && !this.firstActiveAgent) {
      this.firstActiveAgent = agent;
    }
    if (agent) {
      (_b2 = (_a2 = this.activeAgentFuture) === null || _a2 === void 0 ? void 0 : _a2.resolve) === null || _b2 === void 0 ? void 0 : _b2.call(_a2, agent);
    } else {
      (_d = (_c = this.activeAgentFuture) === null || _c === void 0 ? void 0 : _c.reject) === null || _d === void 0 ? void 0 : _d.call(_c, new Error("Agent disconnected"));
    }
    this.activeAgentFuture = void 0;
  }
  waitUntilActiveAgentPresent() {
    if (this.firstActiveAgent) {
      return Promise.resolve(this.firstActiveAgent);
    }
    if (!this.activeAgentFuture) {
      this.activeAgentFuture = new Future();
    }
    return this.activeAgentFuture.promise;
  }
  getPublicationForTrack(track) {
    let publication;
    this.trackPublications.forEach((pub) => {
      const localTrack = pub.track;
      if (!localTrack) {
        return;
      }
      if (track instanceof MediaStreamTrack) {
        if (isLocalAudioTrack(localTrack) || isLocalVideoTrack(localTrack)) {
          if (localTrack.mediaStreamTrack === track) {
            publication = pub;
          }
        }
      } else if (track === localTrack) {
        publication = pub;
      }
    });
    return publication;
  }
  waitForPendingPublicationOfSource(source) {
    return __awaiter(this, void 0, void 0, function* () {
      const waitForPendingTimeout = 1e4;
      const startTime = Date.now();
      while (Date.now() < startTime + waitForPendingTimeout) {
        const publishPromiseEntry = Array.from(this.pendingPublishPromises.entries()).find((_ref8) => {
          let _ref9 = _slicedToArray(_ref8, 1), pendingTrack = _ref9[0];
          return pendingTrack.source === source;
        });
        if (publishPromiseEntry) {
          return publishPromiseEntry[1];
        }
        yield sleep(20);
      }
    });
  }
  /** Publishes a data track.
   *
   * Returns the published data track if successful. Use {@link LocalDataTrack#tryPush}
   * to send data frames on the track.
   */
  publishDataTrack(options) {
    return __awaiter(this, void 0, void 0, function* () {
      const track = new LocalDataTrack(options, this.roomOutgoingDataTrackManager);
      yield track.publish();
      return track;
    });
  }
};
var DeferrableMapAbortError = class extends DOMException {
  constructor(message, reason) {
    super(message, "AbortError");
    this.reason = reason;
  }
};
var DeferrableMap = class extends Map {
  constructor() {
    super(...arguments);
    this.pending = /* @__PURE__ */ new Map();
  }
  set(key, value) {
    var _a2, _b2;
    super.set(key, value);
    const futures = (_a2 = this.pending) === null || _a2 === void 0 ? void 0 : _a2.get(key);
    if (futures) {
      for (const future of futures) {
        if (!future.isResolved) {
          (_b2 = future.resolve) === null || _b2 === void 0 ? void 0 : _b2.call(future, value);
        }
      }
      this.pending.delete(key);
    }
    return this;
  }
  get [Symbol.toStringTag]() {
    return "DeferrableMap";
  }
  getDeferred(key, signal) {
    return __awaiter(this, void 0, void 0, function* () {
      const existing = this.get(key);
      if (typeof existing !== "undefined") {
        return existing;
      }
      if (signal === null || signal === void 0 ? void 0 : signal.aborted) {
        throw new DeferrableMapAbortError("The operation was aborted.", signal.reason);
      }
      const future = new Future(void 0, () => {
        const futures = this.pending.get(key);
        if (!futures) {
          return;
        }
        const idx = futures.indexOf(future);
        if (idx !== -1) {
          futures.splice(idx, 1);
        }
        if (futures.length === 0) {
          this.pending.delete(key);
        }
      });
      const existingFutures = this.pending.get(key);
      if (existingFutures) {
        existingFutures.push(future);
      } else {
        this.pending.set(key, [future]);
      }
      if (signal) {
        const onAbort = () => {
          var _a2;
          if (!future.isResolved) {
            (_a2 = future.reject) === null || _a2 === void 0 ? void 0 : _a2.call(future, new DeferrableMapAbortError("The operation was aborted.", signal.reason));
          }
        };
        signal.addEventListener("abort", onAbort, {
          once: true
        });
        future.promise.finally(() => {
          signal.removeEventListener("abort", onAbort);
        });
      }
      return future.promise;
    });
  }
};
var RemoteTrackPublication = class extends TrackPublication {
  constructor(kind, ti2, autoSubscribe, loggerOptions) {
    super(kind, ti2.sid, ti2.name, loggerOptions);
    this.track = void 0;
    this.allowed = true;
    this.requestedDisabled = void 0;
    this.visible = true;
    this.handleEnded = (track) => {
      this.setTrack(void 0);
      this.emit(TrackEvent.Ended, track);
    };
    this.handleVisibilityChange = (visible) => {
      this.log.debug("adaptivestream video visibility ".concat(this.trackSid, ", visible=").concat(visible), this.logContext);
      this.visible = visible;
      this.emitTrackUpdate();
    };
    this.handleVideoDimensionsChange = (dimensions) => {
      this.log.debug("adaptivestream video dimensions ".concat(dimensions.width, "x").concat(dimensions.height), this.logContext);
      this.videoDimensionsAdaptiveStream = dimensions;
      this.emitTrackUpdate();
    };
    this.subscribed = autoSubscribe;
    this.updateInfo(ti2);
  }
  /**
   * Subscribe or unsubscribe to this remote track
   * @param subscribed true to subscribe to a track, false to unsubscribe
   */
  setSubscribed(subscribed) {
    const prevStatus = this.subscriptionStatus;
    const prevPermission = this.permissionStatus;
    this.subscribed = subscribed;
    if (subscribed) {
      this.allowed = true;
    }
    const sub = new UpdateSubscription({
      trackSids: [this.trackSid],
      subscribe: this.subscribed,
      participantTracks: [new ParticipantTracks({
        // sending an empty participant id since TrackPublication doesn't keep it
        // this is filled in by the participant that receives this message
        participantSid: "",
        trackSids: [this.trackSid]
      })]
    });
    this.emit(TrackEvent.UpdateSubscription, sub);
    this.emitSubscriptionUpdateIfChanged(prevStatus);
    this.emitPermissionUpdateIfChanged(prevPermission);
  }
  get subscriptionStatus() {
    if (this.subscribed === false) {
      return TrackPublication.SubscriptionStatus.Unsubscribed;
    }
    if (!super.isSubscribed) {
      return TrackPublication.SubscriptionStatus.Desired;
    }
    return TrackPublication.SubscriptionStatus.Subscribed;
  }
  get permissionStatus() {
    return this.allowed ? TrackPublication.PermissionStatus.Allowed : TrackPublication.PermissionStatus.NotAllowed;
  }
  /**
   * Returns true if track is subscribed, and ready for playback
   */
  get isSubscribed() {
    if (this.subscribed === false) {
      return false;
    }
    return super.isSubscribed;
  }
  // returns client's desire to subscribe to a track, also true if autoSubscribe is enabled
  get isDesired() {
    return this.subscribed !== false;
  }
  get isEnabled() {
    return this.requestedDisabled !== void 0 ? !this.requestedDisabled : this.isAdaptiveStream ? this.visible : true;
  }
  get isLocal() {
    return false;
  }
  /**
   * disable server from sending down data for this track. this is useful when
   * the participant is off screen, you may disable streaming down their video
   * to reduce bandwidth requirements
   * @param enabled
   */
  setEnabled(enabled) {
    if (!this.isManualOperationAllowed() || this.requestedDisabled === !enabled) {
      return;
    }
    this.requestedDisabled = !enabled;
    this.emitTrackUpdate();
  }
  /**
   * for tracks that support simulcasting, adjust subscribed quality
   *
   * This indicates the highest quality the client can accept. if network
   * bandwidth does not allow, server will automatically reduce quality to
   * optimize for uninterrupted video
   */
  setVideoQuality(quality) {
    if (!this.isManualOperationAllowed() || this.requestedMaxQuality === quality) {
      return;
    }
    this.requestedMaxQuality = quality;
    this.requestedVideoDimensions = void 0;
    this.emitTrackUpdate();
  }
  /**
   * Explicitly set the video dimensions for this track.
   *
   * This will take precedence over adaptive stream dimensions.
   *
   * @param dimensions The video dimensions to set.
   */
  setVideoDimensions(dimensions) {
    var _a2, _b2;
    if (!this.isManualOperationAllowed()) {
      return;
    }
    if (((_a2 = this.requestedVideoDimensions) === null || _a2 === void 0 ? void 0 : _a2.width) === dimensions.width && ((_b2 = this.requestedVideoDimensions) === null || _b2 === void 0 ? void 0 : _b2.height) === dimensions.height) {
      return;
    }
    if (isRemoteVideoTrack(this.track)) {
      this.requestedVideoDimensions = dimensions;
    }
    this.requestedMaxQuality = void 0;
    this.emitTrackUpdate();
  }
  setVideoFPS(fps) {
    if (!this.isManualOperationAllowed()) {
      return;
    }
    if (!isRemoteVideoTrack(this.track)) {
      return;
    }
    if (this.fps === fps) {
      return;
    }
    this.fps = fps;
    this.emitTrackUpdate();
  }
  get videoQuality() {
    var _a2;
    return (_a2 = this.requestedMaxQuality) !== null && _a2 !== void 0 ? _a2 : VideoQuality.HIGH;
  }
  /** @internal */
  setTrack(track) {
    const prevStatus = this.subscriptionStatus;
    const prevPermission = this.permissionStatus;
    const prevTrack = this.track;
    if (prevTrack === track) {
      return;
    }
    if (prevTrack) {
      prevTrack.off(TrackEvent.VideoDimensionsChanged, this.handleVideoDimensionsChange);
      prevTrack.off(TrackEvent.VisibilityChanged, this.handleVisibilityChange);
      prevTrack.off(TrackEvent.Ended, this.handleEnded);
      prevTrack.detach();
      prevTrack.stopMonitor();
      this.emit(TrackEvent.Unsubscribed, prevTrack);
    }
    super.setTrack(track);
    if (track) {
      track.sid = this.trackSid;
      track.on(TrackEvent.VideoDimensionsChanged, this.handleVideoDimensionsChange);
      track.on(TrackEvent.VisibilityChanged, this.handleVisibilityChange);
      track.on(TrackEvent.Ended, this.handleEnded);
      this.emit(TrackEvent.Subscribed, track);
    }
    this.emitPermissionUpdateIfChanged(prevPermission);
    this.emitSubscriptionUpdateIfChanged(prevStatus);
  }
  /** @internal */
  setAllowed(allowed) {
    const prevStatus = this.subscriptionStatus;
    const prevPermission = this.permissionStatus;
    this.allowed = allowed;
    this.emitPermissionUpdateIfChanged(prevPermission);
    this.emitSubscriptionUpdateIfChanged(prevStatus);
  }
  /** @internal */
  setSubscriptionError(error) {
    this.emit(TrackEvent.SubscriptionFailed, error);
  }
  /** @internal */
  updateInfo(info) {
    super.updateInfo(info);
    const prevMetadataMuted = this.metadataMuted;
    this.metadataMuted = info.muted;
    if (this.track) {
      this.track.setMuted(info.muted);
    } else if (prevMetadataMuted !== info.muted) {
      this.emit(info.muted ? TrackEvent.Muted : TrackEvent.Unmuted);
    }
  }
  emitSubscriptionUpdateIfChanged(previousStatus) {
    const currentStatus = this.subscriptionStatus;
    if (previousStatus === currentStatus) {
      return;
    }
    this.emit(TrackEvent.SubscriptionStatusChanged, currentStatus, previousStatus);
  }
  emitPermissionUpdateIfChanged(previousPermissionStatus) {
    const currentPermissionStatus = this.permissionStatus;
    if (currentPermissionStatus !== previousPermissionStatus) {
      this.emit(TrackEvent.SubscriptionPermissionChanged, this.permissionStatus, previousPermissionStatus);
    }
  }
  isManualOperationAllowed() {
    if (!this.isDesired) {
      this.log.warn("cannot update track settings when not subscribed", this.logContext);
      return false;
    }
    return true;
  }
  get isAdaptiveStream() {
    return isRemoteVideoTrack(this.track) && this.track.isAdaptiveStream;
  }
  /* @internal */
  emitTrackUpdate() {
    const settings = new UpdateTrackSettings({
      trackSids: [this.trackSid],
      disabled: !this.isEnabled,
      fps: this.fps
    });
    if (this.kind === Track.Kind.Video) {
      let minDimensions = this.requestedVideoDimensions;
      if (this.videoDimensionsAdaptiveStream !== void 0) {
        if (minDimensions) {
          const smallerAdaptive = areDimensionsSmaller(this.videoDimensionsAdaptiveStream, minDimensions);
          if (smallerAdaptive) {
            this.log.debug("using adaptive stream dimensions instead of requested", Object.assign(Object.assign({}, this.logContext), this.videoDimensionsAdaptiveStream));
            minDimensions = this.videoDimensionsAdaptiveStream;
          }
        } else if (this.requestedMaxQuality !== void 0 && this.trackInfo) {
          const maxQualityLayer = layerDimensionsFor(this.trackInfo, this.requestedMaxQuality);
          if (maxQualityLayer && areDimensionsSmaller(this.videoDimensionsAdaptiveStream, maxQualityLayer)) {
            this.log.debug("using adaptive stream dimensions instead of max quality layer", Object.assign(Object.assign({}, this.logContext), this.videoDimensionsAdaptiveStream));
            minDimensions = this.videoDimensionsAdaptiveStream;
          }
        } else {
          this.log.debug("using adaptive stream dimensions", Object.assign(Object.assign({}, this.logContext), this.videoDimensionsAdaptiveStream));
          minDimensions = this.videoDimensionsAdaptiveStream;
        }
      }
      if (minDimensions) {
        settings.width = Math.ceil(minDimensions.width);
        settings.height = Math.ceil(minDimensions.height);
      } else if (this.requestedMaxQuality !== void 0) {
        this.log.debug("using requested max quality", Object.assign(Object.assign({}, this.logContext), {
          quality: this.requestedMaxQuality
        }));
        settings.quality = this.requestedMaxQuality;
      } else {
        this.log.debug("using default quality", Object.assign(Object.assign({}, this.logContext), {
          quality: VideoQuality.HIGH
        }));
        settings.quality = VideoQuality.HIGH;
      }
    }
    this.emit(TrackEvent.UpdateSettings, settings);
  }
};
var RemoteParticipant = class _RemoteParticipant extends Participant {
  /** @internal */
  static fromParticipantInfo(signalClient, pi2, loggerOptions, manager) {
    return new _RemoteParticipant(signalClient, pi2.sid, pi2.identity, pi2.name, pi2.metadata, pi2.attributes, loggerOptions, pi2.kind, pi2.dataTracks.map((dti) => {
      const info = DataTrackInfo.from(dti);
      return new RemoteDataTrack(info, manager, {
        publisherIdentity: pi2.identity
      });
    }), pi2.clientProtocol, pi2.capabilities);
  }
  get logContext() {
    return Object.assign(Object.assign({}, super.logContext), {
      remoteParticipantID: this.sid,
      remoteParticipant: this.identity
    });
  }
  /** @internal */
  constructor(signalClient, sid, identity, name, metadata, attributes, loggerOptions) {
    let kind = arguments.length > 7 && arguments[7] !== void 0 ? arguments[7] : ParticipantInfo_Kind.STANDARD;
    let remoteDataTracks = arguments.length > 8 && arguments[8] !== void 0 ? arguments[8] : [];
    let clientProtocol2 = arguments.length > 9 && arguments[9] !== void 0 ? arguments[9] : CLIENT_PROTOCOL_DEFAULT;
    let capabilities = arguments.length > 10 && arguments[10] !== void 0 ? arguments[10] : [];
    super(sid, identity || "", name, metadata, attributes, loggerOptions, kind);
    this.signalClient = signalClient;
    this.trackPublications = /* @__PURE__ */ new Map();
    this.audioTrackPublications = /* @__PURE__ */ new Map();
    this.videoTrackPublications = /* @__PURE__ */ new Map();
    this.dataTracks = new DeferrableMap(remoteDataTracks.map((remoteDataTrack) => {
      return [remoteDataTrack.info.name, remoteDataTrack];
    }));
    this.volumeMap = /* @__PURE__ */ new Map();
    this.clientProtocol = clientProtocol2;
    this.capabilities = capabilities;
  }
  addTrackPublication(publication) {
    super.addTrackPublication(publication);
    publication.on(TrackEvent.UpdateSettings, (settings) => {
      this.log.debug("send update settings", Object.assign(Object.assign(Object.assign({}, this.logContext), getLogContextFromTrack(publication)), {
        settings
      }));
      this.signalClient.sendUpdateTrackSettings(settings);
    });
    publication.on(TrackEvent.UpdateSubscription, (sub) => {
      sub.participantTracks.forEach((pt2) => {
        pt2.participantSid = this.sid;
      });
      this.signalClient.sendUpdateSubscription(sub);
    });
    publication.on(TrackEvent.SubscriptionPermissionChanged, (status) => {
      this.emit(ParticipantEvent.TrackSubscriptionPermissionChanged, publication, status);
    });
    publication.on(TrackEvent.SubscriptionStatusChanged, (status) => {
      this.emit(ParticipantEvent.TrackSubscriptionStatusChanged, publication, status);
    });
    publication.on(TrackEvent.Subscribed, (track) => {
      this.emit(ParticipantEvent.TrackSubscribed, track, publication);
    });
    publication.on(TrackEvent.Unsubscribed, (previousTrack) => {
      this.emit(ParticipantEvent.TrackUnsubscribed, previousTrack, publication);
    });
    publication.on(TrackEvent.SubscriptionFailed, (error) => {
      this.emit(ParticipantEvent.TrackSubscriptionFailed, publication.trackSid, error);
    });
  }
  getTrackPublication(source) {
    const track = super.getTrackPublication(source);
    if (track) {
      return track;
    }
  }
  getTrackPublicationByName(name) {
    const track = super.getTrackPublicationByName(name);
    if (track) {
      return track;
    }
  }
  /**
   * sets the volume on the participant's audio track
   * by default, this affects the microphone publication
   * a different source can be passed in as a second argument
   * if no track exists the volume will be applied when the microphone track is added
   */
  setVolume(volume) {
    let source = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : Track.Source.Microphone;
    this.volumeMap.set(source, volume);
    const audioPublication = this.getTrackPublication(source);
    if (audioPublication && audioPublication.track) {
      audioPublication.track.setVolume(volume);
    }
  }
  /**
   * gets the volume on the participant's microphone track
   */
  getVolume() {
    let source = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : Track.Source.Microphone;
    const audioPublication = this.getTrackPublication(source);
    if (audioPublication && audioPublication.track) {
      return audioPublication.track.getVolume();
    }
    return this.volumeMap.get(source);
  }
  /** @internal */
  addSubscribedMediaTrack(mediaTrack, sid, mediaStream, receiver, adaptiveStreamSettings, triesLeft) {
    let publication = this.getTrackPublicationBySid(sid);
    if (!publication) {
      if (!sid.startsWith("TR")) {
        this.trackPublications.forEach((p) => {
          if (!publication && mediaTrack.kind === p.kind.toString()) {
            publication = p;
          }
        });
      }
    }
    if (!publication) {
      if (triesLeft === 0) {
        this.log.error("could not find published track", Object.assign(Object.assign({}, this.logContext), {
          trackSid: sid
        }));
        this.emit(ParticipantEvent.TrackSubscriptionFailed, sid);
        return;
      }
      if (triesLeft === void 0) triesLeft = 20;
      setTimeout(() => {
        this.addSubscribedMediaTrack(mediaTrack, sid, mediaStream, receiver, adaptiveStreamSettings, triesLeft - 1);
      }, 150);
      return;
    }
    if (mediaTrack.readyState === "ended") {
      this.log.error("unable to subscribe because MediaStreamTrack is ended. Do not call MediaStreamTrack.stop()", Object.assign(Object.assign({}, this.logContext), getLogContextFromTrack(publication)));
      this.emit(ParticipantEvent.TrackSubscriptionFailed, sid);
      return;
    }
    const isVideo = mediaTrack.kind === "video";
    let track;
    if (isVideo) {
      track = new RemoteVideoTrack(mediaTrack, sid, receiver, adaptiveStreamSettings);
    } else {
      track = new RemoteAudioTrack(mediaTrack, sid, receiver, this.audioContext, this.audioOutput);
    }
    track.source = publication.source;
    track.isMuted = publication.isMuted;
    track.setMediaStream(mediaStream);
    track.start();
    publication.setTrack(track);
    if (this.volumeMap.has(publication.source) && isRemoteTrack(track) && isAudioTrack(track)) {
      track.setVolume(this.volumeMap.get(publication.source));
    }
    return publication;
  }
  /** @internal */
  get hasMetadata() {
    return !!this.participantInfo;
  }
  /**
   * @internal
   */
  getTrackPublicationBySid(sid) {
    return this.trackPublications.get(sid);
  }
  /** @internal */
  updateInfo(info) {
    if (!super.updateInfo(info)) {
      return false;
    }
    const validTracks = /* @__PURE__ */ new Map();
    const newTracks = /* @__PURE__ */ new Map();
    info.tracks.forEach((ti2) => {
      var _a2, _b2;
      let publication = this.getTrackPublicationBySid(ti2.sid);
      if (!publication) {
        const kind = Track.kindFromProto(ti2.type);
        if (!kind) {
          return;
        }
        publication = new RemoteTrackPublication(kind, ti2, (_a2 = this.signalClient.connectOptions) === null || _a2 === void 0 ? void 0 : _a2.autoSubscribe, {
          loggerContextCb: () => this.logContext,
          loggerName: (_b2 = this.loggerOptions) === null || _b2 === void 0 ? void 0 : _b2.loggerName
        });
        publication.updateInfo(ti2);
        newTracks.set(ti2.sid, publication);
        const existingTrackOfSource = Array.from(this.trackPublications.values()).find((publishedTrack) => publishedTrack.source === (publication === null || publication === void 0 ? void 0 : publication.source));
        if (existingTrackOfSource && publication.source !== Track.Source.Unknown) {
          this.log.debug("received a second track publication for ".concat(this.identity, " with the same source: ").concat(publication.source), Object.assign(Object.assign({}, this.logContext), {
            oldTrack: getLogContextFromTrack(existingTrackOfSource),
            newTrack: getLogContextFromTrack(publication)
          }));
        }
        this.addTrackPublication(publication);
      } else {
        publication.updateInfo(ti2);
      }
      validTracks.set(ti2.sid, publication);
    });
    this.trackPublications.forEach((publication) => {
      if (!validTracks.has(publication.trackSid)) {
        this.log.trace("detected removed track on remote participant, unpublishing", Object.assign(Object.assign({}, this.logContext), getLogContextFromTrack(publication)));
        this.unpublishTrack(publication.trackSid, true);
      }
    });
    newTracks.forEach((publication) => {
      this.emit(ParticipantEvent.TrackPublished, publication);
    });
    return true;
  }
  /** @internal */
  unpublishTrack(sid, sendUnpublish) {
    const publication = this.trackPublications.get(sid);
    if (!publication) {
      return;
    }
    const track = publication.track;
    if (track) {
      track.stop();
      publication.setTrack(void 0);
    }
    this.trackPublications.delete(sid);
    switch (publication.kind) {
      case Track.Kind.Audio:
        this.audioTrackPublications.delete(sid);
        break;
      case Track.Kind.Video:
        this.videoTrackPublications.delete(sid);
        break;
    }
    if (sendUnpublish) {
      this.emit(ParticipantEvent.TrackUnpublished, publication);
    }
  }
  /**
   * @internal
   */
  setAudioOutput(output) {
    return __awaiter(this, void 0, void 0, function* () {
      this.audioOutput = output;
      const promises = [];
      this.audioTrackPublications.forEach((pub) => {
        var _a2;
        if (isAudioTrack(pub.track) && isRemoteTrack(pub.track)) {
          promises.push(pub.track.setSinkId((_a2 = output.deviceId) !== null && _a2 !== void 0 ? _a2 : "default"));
        }
      });
      yield Promise.all(promises);
    });
  }
  /** @internal */
  addRemoteDataTrack(remoteDataTrack) {
    this.dataTracks.set(remoteDataTrack.info.name, remoteDataTrack);
  }
  /** @internal */
  removeRemoteDataTrack(remoteDataTrackSid) {
    for (const _ref of this.dataTracks.entries()) {
      var _ref2 = _slicedToArray(_ref, 2);
      const name = _ref2[0];
      const dataTrack = _ref2[1];
      if (remoteDataTrackSid === dataTrack.info.sid) {
        this.dataTracks.delete(name);
      }
    }
  }
  /** @internal */
  emit(event) {
    for (var _len = arguments.length, args = new Array(_len > 1 ? _len - 1 : 0), _key = 1; _key < _len; _key++) {
      args[_key - 1] = arguments[_key];
    }
    this.log.trace("participant event", Object.assign(Object.assign({}, this.logContext), {
      event,
      args
    }));
    return super.emit(event, ...args);
  }
};
var ConnectionState;
(function(ConnectionState2) {
  ConnectionState2["Disconnected"] = "disconnected";
  ConnectionState2["Connecting"] = "connecting";
  ConnectionState2["Connected"] = "connected";
  ConnectionState2["Reconnecting"] = "reconnecting";
  ConnectionState2["SignalReconnecting"] = "signalReconnecting";
})(ConnectionState || (ConnectionState = {}));
var CONNECTION_RECONCILE_FREQUENCY_MS = 4 * 1e3;
var Room = class _Room extends eventsExports.EventEmitter {
  get hasE2EESetup() {
    return this.e2eeManager !== void 0;
  }
  /**
   * Creates a new Room, the primary construct for a LiveKit session.
   * @param options
   */
  constructor(options) {
    var _this;
    var _a2, _b2, _c, _d, _e2, _f;
    super();
    _this = this;
    this.state = ConnectionState.Disconnected;
    this.activeSpeakers = [];
    this.isE2EEEnabled = false;
    this.audioEnabled = true;
    this.e2eeStateMutex = new _();
    this.isVideoPlaybackBlocked = false;
    this.log = livekitLogger;
    this.bufferedEvents = [];
    this.isResuming = false;
    this.connect = (url, token, opts) => __awaiter(this, void 0, void 0, function* () {
      var _a3;
      if (!isBrowserSupported()) {
        if (isReactNative()) {
          throw Error("WebRTC isn't detected, have you called registerGlobals?");
        } else {
          throw Error("LiveKit doesn't seem to be supported on this browser. Try to update your browser and make sure no browser extensions are disabling webRTC.");
        }
      }
      const unlockDisconnect = yield this.disconnectLock.lock();
      if (this.state === ConnectionState.Connected) {
        this.log.info("already connected to room ".concat(this.name));
        unlockDisconnect();
        return Promise.resolve();
      }
      if (this.connectFuture) {
        unlockDisconnect();
        return this.connectFuture.promise;
      }
      this.setAndEmitConnectionState(ConnectionState.Connecting);
      if (((_a3 = this.regionUrlProvider) === null || _a3 === void 0 ? void 0 : _a3.getServerUrl().toString()) !== ensureTrailingSlash(url)) {
        this.regionUrl = void 0;
        this.regionUrlProvider = void 0;
      }
      if (isCloud(new URL(url))) {
        if (this.regionUrlProvider === void 0) {
          this.regionUrlProvider = new RegionUrlProvider(url, token);
        } else {
          this.regionUrlProvider.updateToken(token);
        }
        this.regionUrlProvider.fetchRegionSettings().then((settings) => {
          var _a4;
          (_a4 = this.regionUrlProvider) === null || _a4 === void 0 ? void 0 : _a4.setServerReportedRegions(settings);
        }).catch((e4) => {
          this.log.warn("could not fetch region settings", {
            error: e4
          });
        });
      }
      const connectFn = (resolve, reject, regionUrl2) => __awaiter(this, void 0, void 0, function* () {
        var _a4, _b3;
        if (this.abortController) {
          this.abortController.abort();
        }
        const abortController = new AbortController();
        this.abortController = abortController;
        unlockDisconnect === null || unlockDisconnect === void 0 ? void 0 : unlockDisconnect();
        try {
          yield BackOffStrategy.getInstance().getBackOffPromise(url);
          if (abortController.signal.aborted) {
            throw ConnectionError.cancelled("Connection attempt aborted");
          }
          yield this.attemptConnection(regionUrl2 !== null && regionUrl2 !== void 0 ? regionUrl2 : url, token, opts, abortController);
          this.abortController = void 0;
          resolve();
        } catch (error) {
          if (this.regionUrlProvider && error instanceof ConnectionError && error.reason !== ConnectionErrorReason.Cancelled && error.reason !== ConnectionErrorReason.NotAllowed) {
            let nextUrl = null;
            try {
              this.log.debug("Fetching next region");
              nextUrl = yield this.regionUrlProvider.getNextBestRegionUrl((_a4 = this.abortController) === null || _a4 === void 0 ? void 0 : _a4.signal);
            } catch (regionFetchError) {
              if (regionFetchError instanceof ConnectionError && (regionFetchError.status === 401 || regionFetchError.reason === ConnectionErrorReason.Cancelled)) {
                this.handleDisconnect(this.options.stopLocalTrackOnUnpublish);
                reject(regionFetchError);
                return;
              }
            }
            if (
              // making sure we only register failed attempts on things we actually care about
              [ConnectionErrorReason.InternalError, ConnectionErrorReason.ServerUnreachable, ConnectionErrorReason.Timeout].includes(error.reason)
            ) {
              this.log.debug("Adding failed connection attempt to back off");
              BackOffStrategy.getInstance().addFailedConnectionAttempt(url);
            }
            if (nextUrl && !((_b3 = this.abortController) === null || _b3 === void 0 ? void 0 : _b3.signal.aborted)) {
              this.log.info("Initial connection failed with ConnectionError: ".concat(error.message, ". Retrying with another region: ").concat(nextUrl));
              this.recreateEngine(true);
              yield connectFn(resolve, reject, nextUrl);
            } else {
              this.handleDisconnect(this.options.stopLocalTrackOnUnpublish, getDisconnectReasonFromConnectionError(error));
              reject(error);
            }
          } else {
            let disconnectReason = DisconnectReason.UNKNOWN_REASON;
            if (error instanceof ConnectionError) {
              disconnectReason = getDisconnectReasonFromConnectionError(error);
            }
            this.handleDisconnect(this.options.stopLocalTrackOnUnpublish, disconnectReason);
            reject(error);
          }
        }
      });
      const regionUrl = this.regionUrl;
      this.regionUrl = void 0;
      this.connectFuture = new Future((resolve, reject) => {
        connectFn(resolve, reject, regionUrl);
      }, () => {
        this.clearConnectionFutures();
      });
      return this.connectFuture.promise;
    });
    this.connectSignal = (url, token, engine, connectOptions, roomOptions, abortController) => __awaiter(this, void 0, void 0, function* () {
      const _yield$engine$join = yield engine.join(url, token, {
        autoSubscribe: connectOptions.autoSubscribe,
        adaptiveStream: typeof roomOptions.adaptiveStream === "object" ? true : roomOptions.adaptiveStream,
        clientInfoCapabilities: this.getClientInfoCapabilities(roomOptions),
        maxRetries: connectOptions.maxRetries,
        e2eeEnabled: !!this.e2eeManager,
        websocketTimeout: connectOptions.websocketTimeout
      }, abortController.signal, !roomOptions.singlePeerConnection), joinResponse = _yield$engine$join.joinResponse, serverInfo = _yield$engine$join.serverInfo;
      this.serverInfo = serverInfo;
      if (!serverInfo.version) {
        throw new UnsupportedServer("unknown server version");
      }
      if (serverInfo.version === "0.15.1" && this.options.dynacast) {
        this.log.debug("disabling dynacast due to server version");
        roomOptions.dynacast = false;
      }
      return joinResponse;
    });
    this.applyJoinResponse = (joinResponse) => {
      const pi2 = joinResponse.participant;
      this.localParticipant.sid = pi2.sid;
      this.localParticipant.identity = pi2.identity;
      this.localParticipant.setEnabledPublishCodecs(joinResponse.enabledPublishCodecs);
      if (this.e2eeManager) {
        try {
          this.e2eeManager.setSifTrailer(joinResponse.sifTrailer);
        } catch (e4) {
          this.log.error(e4 instanceof Error ? e4.message : "Could not set SifTrailer", {
            error: e4
          });
        }
      }
      this.handleParticipantUpdates([pi2, ...joinResponse.otherParticipants]);
      if (joinResponse.room) {
        this.handleRoomUpdate(joinResponse.room);
      }
    };
    this.attemptConnection = (url, token, opts, abortController) => __awaiter(this, void 0, void 0, function* () {
      var _a3, _b3;
      if (this.state === ConnectionState.Reconnecting || this.isResuming || ((_a3 = this.engine) === null || _a3 === void 0 ? void 0 : _a3.pendingReconnect)) {
        this.log.info("Reconnection attempt replaced by new connection attempt");
        this.recreateEngine(true);
      } else {
        this.maybeCreateEngine();
      }
      if ((_b3 = this.regionUrlProvider) === null || _b3 === void 0 ? void 0 : _b3.isCloud()) {
        this.engine.setRegionStrategy(this.createRegionStrategy());
      }
      this.acquireAudioContext();
      this.connOptions = Object.assign(Object.assign({}, roomConnectOptionDefaults), opts);
      if (this.connOptions.rtcConfig) {
        this.engine.rtcConfig = this.connOptions.rtcConfig;
      }
      if (this.connOptions.peerConnectionTimeout) {
        this.engine.peerConnectionTimeout = this.connOptions.peerConnectionTimeout;
      }
      try {
        const joinResponse = yield this.connectSignal(url, token, this.engine, this.connOptions, this.options, abortController);
        this.applyJoinResponse(joinResponse);
        this.setupLocalParticipantEvents();
        this.emit(RoomEvent.SignalConnected);
      } catch (err) {
        yield this.engine.close();
        this.recreateEngine();
        const resultingError = abortController.signal.aborted ? ConnectionError.cancelled("Signal connection aborted") : ConnectionError.serverUnreachable("could not establish signal connection");
        if (err instanceof Error) {
          resultingError.message = "".concat(resultingError.message, ": ").concat(err.message);
        }
        if (err instanceof ConnectionError) {
          resultingError.reason = err.reason;
          resultingError.status = err.status;
        }
        this.log.debug("error trying to establish signal connection", {
          error: err
        });
        throw resultingError;
      }
      if (abortController.signal.aborted) {
        yield this.engine.close();
        this.recreateEngine();
        throw ConnectionError.cancelled("Connection attempt aborted");
      }
      try {
        yield this.engine.waitForPCInitialConnection(this.connOptions.peerConnectionTimeout, abortController);
      } catch (e4) {
        yield this.engine.close();
        this.recreateEngine();
        throw e4;
      }
      if (isWeb() && this.options.disconnectOnPageLeave) {
        window.addEventListener("pagehide", this.onPageLeave);
        window.addEventListener("beforeunload", this.onPageLeave);
      }
      if (isWeb()) {
        window.addEventListener("freeze", this.onPageLeave);
      }
      this.setAndEmitConnectionState(ConnectionState.Connected);
      this.emit(RoomEvent.Connected);
      BackOffStrategy.getInstance().resetFailedConnectionAttempts(url);
      this.registerConnectionReconcile();
      if (this.regionUrlProvider) {
        this.regionUrlProvider.notifyConnected();
      }
    });
    this.disconnect = function() {
      for (var _len = arguments.length, args_1 = new Array(_len), _key = 0; _key < _len; _key++) {
        args_1[_key] = arguments[_key];
      }
      return __awaiter(_this, [...args_1], void 0, function() {
        var _this2 = this;
        let stopTracks = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : true;
        return function* () {
          var _a3, _b3, _c2;
          const unlock = yield _this2.disconnectLock.lock();
          try {
            if (_this2.state === ConnectionState.Disconnected) {
              _this2.log.debug("already disconnected");
              return;
            }
            _this2.log.info("disconnect from room");
            if (_this2.state === ConnectionState.Connecting || _this2.state === ConnectionState.Reconnecting || _this2.isResuming) {
              const msg = "Abort connection attempt due to user initiated disconnect";
              _this2.log.warn(msg);
              (_a3 = _this2.abortController) === null || _a3 === void 0 ? void 0 : _a3.abort(msg);
              (_c2 = (_b3 = _this2.connectFuture) === null || _b3 === void 0 ? void 0 : _b3.reject) === null || _c2 === void 0 ? void 0 : _c2.call(_b3, ConnectionError.cancelled("Client initiated disconnect"));
              _this2.connectFuture = void 0;
            }
            if (_this2.engine) {
              if (!_this2.engine.client.isDisconnected) {
                yield _this2.engine.client.sendLeave();
              }
              yield _this2.engine.close();
            }
            _this2.handleDisconnect(stopTracks, DisconnectReason.CLIENT_INITIATED);
            _this2.engine = void 0;
          } finally {
            unlock();
          }
        }();
      });
    };
    this.onPageLeave = () => __awaiter(this, void 0, void 0, function* () {
      this.log.info("Page leave detected, disconnecting");
      yield this.disconnect();
    });
    this.startAudio = () => __awaiter(this, void 0, void 0, function* () {
      const elements = [];
      const browser = getBrowser();
      if (browser && browser.os === "iOS") {
        const audioId = "livekit-dummy-audio-el";
        let dummyAudioEl = document.getElementById(audioId);
        if (!dummyAudioEl) {
          dummyAudioEl = document.createElement("audio");
          dummyAudioEl.id = audioId;
          dummyAudioEl.autoplay = true;
          dummyAudioEl.hidden = true;
          const track = getEmptyAudioStreamTrack();
          track.enabled = true;
          const stream = new MediaStream([track]);
          dummyAudioEl.srcObject = stream;
          document.addEventListener("visibilitychange", () => {
            if (!dummyAudioEl) {
              return;
            }
            dummyAudioEl.srcObject = document.hidden ? null : stream;
            if (!document.hidden) {
              this.log.debug("page visible again, triggering startAudio to resume playback and update playback status");
              this.startAudio();
            }
          });
          document.body.append(dummyAudioEl);
          this.once(RoomEvent.Disconnected, () => {
            dummyAudioEl === null || dummyAudioEl === void 0 ? void 0 : dummyAudioEl.remove();
            dummyAudioEl = null;
          });
        }
        elements.push(dummyAudioEl);
      }
      this.remoteParticipants.forEach((p) => {
        p.audioTrackPublications.forEach((t) => {
          if (t.track) {
            t.track.attachedElements.forEach((e4) => {
              elements.push(e4);
            });
          }
        });
      });
      try {
        yield Promise.all([this.acquireAudioContext(), ...elements.map((e4) => {
          if (!this.options.webAudioMix) {
            e4.muted = false;
          }
          return e4.play();
        })]);
        this.handleAudioPlaybackStarted();
      } catch (err) {
        this.handleAudioPlaybackFailed(err);
        throw err;
      }
    });
    this.startVideo = () => __awaiter(this, void 0, void 0, function* () {
      const elements = [];
      for (const p of this.remoteParticipants.values()) {
        p.videoTrackPublications.forEach((tr3) => {
          var _a3;
          (_a3 = tr3.track) === null || _a3 === void 0 ? void 0 : _a3.attachedElements.forEach((el) => {
            if (!elements.includes(el)) {
              elements.push(el);
            }
          });
        });
      }
      yield Promise.all(elements.map((el) => el.play())).then(() => {
        this.handleVideoPlaybackStarted();
      }).catch((e4) => {
        if (e4.name === "NotAllowedError") {
          this.handleVideoPlaybackFailed();
        } else {
          this.log.warn("Resuming video playback failed, make sure you call `startVideo` directly in a user gesture handler");
        }
      });
    });
    this.handleRestarting = () => {
      this.clearConnectionReconcile();
      this.isResuming = false;
      for (const p of this.remoteParticipants.values()) {
        this.handleParticipantDisconnected(p.identity, p);
      }
      if (this.setAndEmitConnectionState(ConnectionState.Reconnecting)) {
        this.emit(RoomEvent.Reconnecting);
      }
    };
    this.handleRestarted = () => {
      this.outgoingDataTrackManager.sfuWillRepublishTracks();
      this.incomingDataTrackManager.resendSubscriptionUpdates();
    };
    this.handleSignalRestarted = (joinResponse) => __awaiter(this, void 0, void 0, function* () {
      this.log.debug("signal reconnected to server, region ".concat(joinResponse.serverRegion), {
        region: joinResponse.serverRegion
      });
      this.bufferedEvents = [];
      this.applyJoinResponse(joinResponse);
      try {
        yield this.localParticipant.republishAllTracks(void 0, true);
      } catch (error) {
        this.log.error("error trying to re-publish tracks after reconnection", {
          error
        });
      }
      try {
        yield this.engine.waitForRestarted();
        this.log.debug("fully reconnected to server", {
          region: joinResponse.serverRegion
        });
      } catch (_a3) {
        return;
      }
      this.setAndEmitConnectionState(ConnectionState.Connected);
      this.emit(RoomEvent.Reconnected);
      this.registerConnectionReconcile();
      this.emitBufferedEvents();
    });
    this.handleParticipantUpdates = (participantInfos) => {
      var _a3;
      for (const info of participantInfos) {
        if (info.identity === this.localParticipant.identity) {
          this.localParticipant.updateInfo(info);
          continue;
        }
        if (info.identity === "") {
          info.identity = (_a3 = this.sidToIdentity.get(info.sid)) !== null && _a3 !== void 0 ? _a3 : "";
        }
        let remoteParticipant = this.remoteParticipants.get(info.identity);
        if (info.state === ParticipantInfo_State.DISCONNECTED) {
          this.handleParticipantDisconnected(info.identity, remoteParticipant);
        } else {
          this.getOrCreateParticipant(info.identity, info);
        }
      }
      const mapped = new Map(participantInfos.filter((p) => p.identity !== this.localParticipant.identity).map((info) => {
        return [info.identity, info.dataTracks.map((dataTrack) => DataTrackInfo.from(dataTrack))];
      }));
      this.incomingDataTrackManager.receiveSfuPublicationUpdates(mapped);
    };
    this.handleActiveSpeakersUpdate = (speakers) => {
      const activeSpeakers = [];
      const seenSids = {};
      speakers.forEach((speaker) => {
        seenSids[speaker.sid] = true;
        if (speaker.sid === this.localParticipant.sid) {
          this.localParticipant.audioLevel = speaker.level;
          this.localParticipant.setIsSpeaking(true);
          activeSpeakers.push(this.localParticipant);
        } else {
          const p = this.getRemoteParticipantBySid(speaker.sid);
          if (p) {
            p.audioLevel = speaker.level;
            p.setIsSpeaking(true);
            activeSpeakers.push(p);
          }
        }
      });
      if (!seenSids[this.localParticipant.sid]) {
        this.localParticipant.audioLevel = 0;
        this.localParticipant.setIsSpeaking(false);
      }
      this.remoteParticipants.forEach((p) => {
        if (!seenSids[p.sid]) {
          p.audioLevel = 0;
          p.setIsSpeaking(false);
        }
      });
      this.activeSpeakers = activeSpeakers;
      this.emitWhenConnected(RoomEvent.ActiveSpeakersChanged, activeSpeakers);
    };
    this.handleSpeakersChanged = (speakerUpdates) => {
      const lastSpeakers = /* @__PURE__ */ new Map();
      this.activeSpeakers.forEach((p) => {
        const remoteParticipant = this.remoteParticipants.get(p.identity);
        if (remoteParticipant && remoteParticipant.sid !== p.sid) {
          return;
        }
        lastSpeakers.set(p.sid, p);
      });
      speakerUpdates.forEach((speaker) => {
        let p = this.getRemoteParticipantBySid(speaker.sid);
        if (speaker.sid === this.localParticipant.sid) {
          p = this.localParticipant;
        }
        if (!p) {
          return;
        }
        p.audioLevel = speaker.level;
        p.setIsSpeaking(speaker.active);
        if (speaker.active) {
          lastSpeakers.set(speaker.sid, p);
        } else {
          lastSpeakers.delete(speaker.sid);
        }
      });
      const activeSpeakers = Array.from(lastSpeakers.values());
      activeSpeakers.sort((a, b) => b.audioLevel - a.audioLevel);
      this.activeSpeakers = activeSpeakers;
      this.emitWhenConnected(RoomEvent.ActiveSpeakersChanged, activeSpeakers);
    };
    this.handleStreamStateUpdate = (streamStateUpdate) => {
      streamStateUpdate.streamStates.forEach((streamState) => {
        const participant = this.getRemoteParticipantBySid(streamState.participantSid);
        if (!participant) {
          return;
        }
        const pub = participant.getTrackPublicationBySid(streamState.trackSid);
        if (!pub || !pub.track) {
          return;
        }
        const newStreamState = Track.streamStateFromProto(streamState.state);
        pub.track.setStreamState(newStreamState);
        if (newStreamState !== pub.track.streamState) {
          participant.emit(ParticipantEvent.TrackStreamStateChanged, pub, pub.track.streamState);
          this.emitWhenConnected(RoomEvent.TrackStreamStateChanged, pub, pub.track.streamState, participant);
        }
      });
    };
    this.handleSubscriptionPermissionUpdate = (update) => {
      const participant = this.getRemoteParticipantBySid(update.participantSid);
      if (!participant) {
        return;
      }
      const pub = participant.getTrackPublicationBySid(update.trackSid);
      if (!pub) {
        return;
      }
      pub.setAllowed(update.allowed);
    };
    this.handleSubscriptionError = (update) => {
      const participant = Array.from(this.remoteParticipants.values()).find((p) => p.trackPublications.has(update.trackSid));
      if (!participant) {
        return;
      }
      const pub = participant.getTrackPublicationBySid(update.trackSid);
      if (!pub) {
        return;
      }
      pub.setSubscriptionError(update.err);
    };
    this.handleDataPacket = (packet, encryptionType) => {
      const participant = this.remoteParticipants.get(packet.participantIdentity);
      if (packet.value.case === "user") {
        this.handleUserPacket(participant, packet.value.value, packet.kind, encryptionType);
      } else if (packet.value.case === "transcription") {
        this.handleTranscription(participant, packet.value.value);
      } else if (packet.value.case === "sipDtmf") {
        this.handleSipDtmf(participant, packet.value.value);
      } else if (packet.value.case === "chatMessage") {
        this.handleChatMessage(participant, packet.value.value);
      } else if (packet.value.case === "metrics") {
        this.handleMetrics(packet.value.value, participant);
      } else if (packet.value.case === "streamHeader" || packet.value.case === "streamChunk" || packet.value.case === "streamTrailer") {
        this.handleDataStream(packet, encryptionType);
      } else if (packet.value.case === "rpcRequest") {
        const rpc = packet.value.value;
        this.rpcServerManager.handleIncomingRpcRequest(packet.participantIdentity, rpc);
      } else if (packet.value.case === "rpcResponse") {
        const rpcResponse = packet.value.value;
        switch (rpcResponse.value.case) {
          case "payload":
            this.rpcClientManager.handleIncomingRpcResponseSuccess(rpcResponse.requestId, rpcResponse.value.value);
            break;
          case "error":
            this.rpcClientManager.handleIncomingRpcResponseFailure(rpcResponse.requestId, RpcError.fromProto(rpcResponse.value.value));
            break;
          default:
            this.log.warn("Unknown rpcResponse.value.case: ".concat(rpcResponse.value.case), this.logContext);
            break;
        }
      } else if (packet.value.case === "rpcAck") {
        this.rpcClientManager.handleIncomingRpcAck(packet.value.value.requestId);
      }
    };
    this.handleUserPacket = (participant, userPacket, kind, encryptionType) => {
      this.emit(RoomEvent.DataReceived, userPacket.payload, participant, kind, userPacket.topic, encryptionType);
      participant === null || participant === void 0 ? void 0 : participant.emit(ParticipantEvent.DataReceived, userPacket.payload, kind, encryptionType);
    };
    this.handleSipDtmf = (participant, dtmf) => {
      this.emit(RoomEvent.SipDTMFReceived, dtmf, participant);
      participant === null || participant === void 0 ? void 0 : participant.emit(ParticipantEvent.SipDTMFReceived, dtmf);
    };
    this.handleTranscription = (_remoteParticipant, transcription) => {
      const participant = transcription.transcribedParticipantIdentity === this.localParticipant.identity ? this.localParticipant : this.getParticipantByIdentity(transcription.transcribedParticipantIdentity);
      const publication = participant === null || participant === void 0 ? void 0 : participant.trackPublications.get(transcription.trackId);
      const segments = extractTranscriptionSegments(transcription, this.transcriptionReceivedTimes);
      publication === null || publication === void 0 ? void 0 : publication.emit(TrackEvent.TranscriptionReceived, segments);
      participant === null || participant === void 0 ? void 0 : participant.emit(ParticipantEvent.TranscriptionReceived, segments, publication);
      this.emit(RoomEvent.TranscriptionReceived, segments, participant, publication);
    };
    this.handleChatMessage = (participant, chatMessage) => {
      const msg = extractChatMessage(chatMessage);
      this.emit(RoomEvent.ChatMessage, msg, participant);
    };
    this.handleMetrics = (metrics, participant) => {
      this.emit(RoomEvent.MetricsReceived, metrics, participant);
    };
    this.handleDataStream = (packet, encryptionType) => {
      this.incomingDataStreamManager.handleDataStreamPacket(packet, encryptionType);
    };
    this.bufferedSegments = /* @__PURE__ */ new Map();
    this.handleAudioPlaybackStarted = () => {
      if (this.canPlaybackAudio) {
        return;
      }
      this.audioEnabled = true;
      this.emit(RoomEvent.AudioPlaybackStatusChanged, true);
    };
    this.handleAudioPlaybackFailed = (e4) => {
      this.log.warn("could not playback audio", {
        error: e4
      });
      if (!this.canPlaybackAudio) {
        return;
      }
      this.audioEnabled = false;
      this.emit(RoomEvent.AudioPlaybackStatusChanged, false);
    };
    this.handleVideoPlaybackStarted = () => {
      if (this.isVideoPlaybackBlocked) {
        this.isVideoPlaybackBlocked = false;
        this.emit(RoomEvent.VideoPlaybackStatusChanged, true);
      }
    };
    this.handleVideoPlaybackFailed = () => {
      if (!this.isVideoPlaybackBlocked) {
        this.isVideoPlaybackBlocked = true;
        this.emit(RoomEvent.VideoPlaybackStatusChanged, false);
      }
    };
    this.handleDeviceChange = () => __awaiter(this, void 0, void 0, function* () {
      var _a3;
      if (((_a3 = getBrowser()) === null || _a3 === void 0 ? void 0 : _a3.os) !== "iOS") {
        yield this.selectDefaultDevices();
      }
      this.emit(RoomEvent.MediaDevicesChanged);
    });
    this.handleRoomUpdate = (room) => {
      const oldRoom = this.roomInfo;
      this.roomInfo = room;
      if (oldRoom && oldRoom.metadata !== room.metadata) {
        this.emitWhenConnected(RoomEvent.RoomMetadataChanged, room.metadata);
      }
      if ((oldRoom === null || oldRoom === void 0 ? void 0 : oldRoom.activeRecording) !== room.activeRecording) {
        this.emitWhenConnected(RoomEvent.RecordingStatusChanged, room.activeRecording);
      }
    };
    this.handleConnectionQualityUpdate = (update) => {
      update.updates.forEach((info) => {
        if (info.participantSid === this.localParticipant.sid) {
          this.localParticipant.setConnectionQuality(info.quality);
          return;
        }
        const participant = this.getRemoteParticipantBySid(info.participantSid);
        if (participant) {
          participant.setConnectionQuality(info.quality);
        }
      });
    };
    this.getRemoteParticipantClientProtocol = (identity) => {
      var _a3, _b3;
      return (_b3 = (_a3 = this.remoteParticipants.get(identity)) === null || _a3 === void 0 ? void 0 : _a3.clientProtocol) !== null && _b3 !== void 0 ? _b3 : CLIENT_PROTOCOL_DEFAULT;
    };
    this.getRemoteParticipantCapabilities = (identity) => {
      var _a3, _b3;
      return (_b3 = (_a3 = this.remoteParticipants.get(identity)) === null || _a3 === void 0 ? void 0 : _a3.capabilities) !== null && _b3 !== void 0 ? _b3 : [];
    };
    this.getAllRemoteParticipantIdentities = () => {
      return Array.from(this.remoteParticipants.keys());
    };
    this.onLocalParticipantMetadataChanged = (metadata) => {
      this.emit(RoomEvent.ParticipantMetadataChanged, metadata, this.localParticipant);
    };
    this.onLocalParticipantNameChanged = (name) => {
      this.emit(RoomEvent.ParticipantNameChanged, name, this.localParticipant);
    };
    this.onLocalAttributesChanged = (changedAttributes) => {
      this.emit(RoomEvent.ParticipantAttributesChanged, changedAttributes, this.localParticipant);
    };
    this.onLocalTrackMuted = (pub) => {
      this.emit(RoomEvent.TrackMuted, pub, this.localParticipant);
    };
    this.onLocalTrackUnmuted = (pub) => {
      this.emit(RoomEvent.TrackUnmuted, pub, this.localParticipant);
    };
    this.onTrackProcessorUpdate = (processor) => {
      var _a3;
      (_a3 = processor === null || processor === void 0 ? void 0 : processor.onPublish) === null || _a3 === void 0 ? void 0 : _a3.call(processor, this);
    };
    this.onLocalTrackPublished = (pub) => __awaiter(this, void 0, void 0, function* () {
      var _a3, _b3, _c2, _d2, _e3, _f2;
      (_a3 = pub.track) === null || _a3 === void 0 ? void 0 : _a3.on(TrackEvent.TrackProcessorUpdate, this.onTrackProcessorUpdate);
      (_b3 = pub.track) === null || _b3 === void 0 ? void 0 : _b3.on(TrackEvent.Restarted, this.onLocalTrackRestarted);
      (_e3 = (_d2 = (_c2 = pub.track) === null || _c2 === void 0 ? void 0 : _c2.getProcessor()) === null || _d2 === void 0 ? void 0 : _d2.onPublish) === null || _e3 === void 0 ? void 0 : _e3.call(_d2, this);
      this.emit(RoomEvent.LocalTrackPublished, pub, this.localParticipant);
      if (isLocalAudioTrack(pub.track)) {
        const trackIsSilent = yield pub.track.checkForSilence();
        if (trackIsSilent) {
          this.emit(RoomEvent.LocalAudioSilenceDetected, pub);
        }
      }
      const deviceId = yield (_f2 = pub.track) === null || _f2 === void 0 ? void 0 : _f2.getDeviceId(false);
      const deviceKind = sourceToKind(pub.source);
      if (deviceKind && deviceId && deviceId !== this.localParticipant.activeDeviceMap.get(deviceKind)) {
        this.localParticipant.activeDeviceMap.set(deviceKind, deviceId);
        this.emit(RoomEvent.ActiveDeviceChanged, deviceKind, deviceId);
      }
    });
    this.onLocalTrackUnpublished = (pub) => {
      var _a3, _b3;
      (_a3 = pub.track) === null || _a3 === void 0 ? void 0 : _a3.off(TrackEvent.TrackProcessorUpdate, this.onTrackProcessorUpdate);
      (_b3 = pub.track) === null || _b3 === void 0 ? void 0 : _b3.off(TrackEvent.Restarted, this.onLocalTrackRestarted);
      this.emit(RoomEvent.LocalTrackUnpublished, pub, this.localParticipant);
    };
    this.onLocalTrackRestarted = (track) => __awaiter(this, void 0, void 0, function* () {
      const deviceId = yield track.getDeviceId(false);
      const deviceKind = sourceToKind(track.source);
      if (deviceKind && deviceId && deviceId !== this.localParticipant.activeDeviceMap.get(deviceKind)) {
        this.log.debug("local track restarted, setting ".concat(deviceKind, " ").concat(deviceId, " active"));
        this.localParticipant.activeDeviceMap.set(deviceKind, deviceId);
        this.emit(RoomEvent.ActiveDeviceChanged, deviceKind, deviceId);
      }
    });
    this.onLocalConnectionQualityChanged = (quality) => {
      this.emit(RoomEvent.ConnectionQualityChanged, quality, this.localParticipant);
    };
    this.onMediaDevicesError = (e4, kind) => {
      this.emit(RoomEvent.MediaDevicesError, e4, kind);
    };
    this.onLocalParticipantPermissionsChanged = (prevPermissions) => {
      this.emit(RoomEvent.ParticipantPermissionsChanged, prevPermissions, this.localParticipant);
    };
    this.onLocalChatMessageSent = (msg) => {
      this.emit(RoomEvent.ChatMessage, msg, this.localParticipant);
    };
    this.setMaxListeners(100);
    this.remoteParticipants = /* @__PURE__ */ new Map();
    this.sidToIdentity = /* @__PURE__ */ new Map();
    this.options = Object.assign(Object.assign({}, roomOptionDefaults), options);
    this.log = getLogger((_a2 = this.options.loggerName) !== null && _a2 !== void 0 ? _a2 : LoggerNames.Room, () => this.logContext);
    this.transcriptionReceivedTimes = /* @__PURE__ */ new Map();
    this.options.audioCaptureDefaults = Object.assign(Object.assign({}, audioDefaults), options === null || options === void 0 ? void 0 : options.audioCaptureDefaults);
    this.options.videoCaptureDefaults = Object.assign(Object.assign({}, videoDefaults), options === null || options === void 0 ? void 0 : options.videoCaptureDefaults);
    this.options.publishDefaults = Object.assign(Object.assign({}, publishDefaults), options === null || options === void 0 ? void 0 : options.publishDefaults);
    this.maybeCreateEngine();
    this.incomingDataStreamManager = new IncomingDataStreamManager();
    this.outgoingDataStreamManager = new OutgoingDataStreamManager(this.engine, this.log, this.getRemoteParticipantClientProtocol, this.getRemoteParticipantCapabilities, this.getAllRemoteParticipantIdentities);
    this.incomingDataTrackManager = new IncomingDataTrackManager({
      e2eeManager: this.e2eeManager
    });
    this.incomingDataTrackManager.on("sfuUpdateSubscription", (event) => {
      this.engine.client.sendUpdateDataSubscription(event.sid, event.subscribe);
    }).on("trackPublished", (event) => {
      var _a3;
      if (event.track.publisherIdentity === this.localParticipant.identity) {
        return;
      }
      this.emit(RoomEvent.DataTrackPublished, event.track);
      (_a3 = this.remoteParticipants.get(event.track.publisherIdentity)) === null || _a3 === void 0 ? void 0 : _a3.addRemoteDataTrack(event.track);
    }).on("trackUnpublished", (event) => {
      var _a3;
      if (event.publisherIdentity === this.localParticipant.identity) {
        return;
      }
      this.emit(RoomEvent.DataTrackUnpublished, event.sid);
      (_a3 = this.remoteParticipants.get(event.publisherIdentity)) === null || _a3 === void 0 ? void 0 : _a3.removeRemoteDataTrack(event.sid);
    });
    this.outgoingDataTrackManager = new OutgoingDataTrackManager({
      e2eeManager: this.e2eeManager
    });
    this.outgoingDataTrackManager.on("sfuPublishRequest", (event) => {
      this.engine.client.sendPublishDataTrackRequest(event.handle, event.name, event.usesE2ee);
    }).on("sfuUnpublishRequest", (event) => {
      this.engine.client.sendUnPublishDataTrackRequest(event.handle);
    }).on("trackPublished", (event) => {
      this.emit(RoomEvent.LocalDataTrackPublished, event.track);
    }).on("trackUnpublished", (event) => {
      this.emit(RoomEvent.LocalDataTrackUnpublished, event.sid);
    }).on("packetAvailable", (_ref) => {
      let handle = _ref.handle, bytes = _ref.bytes;
      this.engine.sendDataTrackFrame(bytes).finally(() => this.outgoingDataTrackManager.handlePacketSendComplete(handle));
    });
    this.registerRpcDataStreamHandler();
    this.rpcClientManager = new RpcClientManager(this.log, this.outgoingDataStreamManager, this.getRemoteParticipantClientProtocol, () => {
      var _a3, _b3;
      return (_b3 = (_a3 = this.engine.latestJoinResponse) === null || _a3 === void 0 ? void 0 : _a3.serverInfo) === null || _b3 === void 0 ? void 0 : _b3.version;
    });
    this.rpcClientManager.on("sendDataPacket", (_ref2) => {
      let packet = _ref2.packet;
      var _a3;
      (_a3 = this.engine) === null || _a3 === void 0 ? void 0 : _a3.sendDataPacket(packet, DataChannelKind.RELIABLE);
    });
    this.rpcServerManager = new RpcServerManager(this.log, this.outgoingDataStreamManager, this.getRemoteParticipantClientProtocol);
    this.rpcServerManager.on("sendDataPacket", (_ref3) => {
      let packet = _ref3.packet;
      var _a3;
      (_a3 = this.engine) === null || _a3 === void 0 ? void 0 : _a3.sendDataPacket(packet, DataChannelKind.RELIABLE);
    });
    this.disconnectLock = new _();
    this.localParticipant = new LocalParticipant("", "", this.engine, this.options, this.outgoingDataStreamManager, this.outgoingDataTrackManager, this.rpcClientManager, this.rpcServerManager);
    this.setupFrameMetadata();
    if (this.options.e2ee || this.options.encryption) {
      this.setupE2EE();
    }
    this.engine.e2eeManager = this.e2eeManager;
    this.incomingDataTrackManager.updateE2eeManager((_b2 = this.e2eeManager) !== null && _b2 !== void 0 ? _b2 : null);
    this.outgoingDataTrackManager.updateE2eeManager((_c = this.e2eeManager) !== null && _c !== void 0 ? _c : null);
    if (this.options.videoCaptureDefaults.deviceId) {
      this.localParticipant.activeDeviceMap.set("videoinput", unwrapConstraint(this.options.videoCaptureDefaults.deviceId));
    }
    if (this.options.audioCaptureDefaults.deviceId) {
      this.localParticipant.activeDeviceMap.set("audioinput", unwrapConstraint(this.options.audioCaptureDefaults.deviceId));
    }
    if ((_d = this.options.audioOutput) === null || _d === void 0 ? void 0 : _d.deviceId) {
      this.switchActiveDevice("audiooutput", unwrapConstraint(this.options.audioOutput.deviceId)).catch((e4) => this.log.warn("Could not set audio output: ".concat(e4.message)));
    }
    if (isWeb()) {
      const cleanupController = new AbortController();
      let onDeviceChange;
      if (_Room.cleanupRegistry) {
        const roomRef = new WeakRef(this);
        onDeviceChange = () => {
          const self2 = roomRef.deref();
          if (!self2) {
            return;
          }
          self2.handleDeviceChange();
        };
        _Room.cleanupRegistry.register(this, () => {
          cleanupController.abort();
        });
      } else {
        onDeviceChange = this.handleDeviceChange;
      }
      (_f = (_e2 = navigator.mediaDevices) === null || _e2 === void 0 ? void 0 : _e2.addEventListener) === null || _f === void 0 ? void 0 : _f.call(_e2, "devicechange", onDeviceChange, {
        signal: cleanupController.signal
      });
    }
  }
  registerTextStreamHandler(topic, callback) {
    return this.incomingDataStreamManager.registerTextStreamHandler(topic, callback);
  }
  unregisterTextStreamHandler(topic) {
    return this.incomingDataStreamManager.unregisterTextStreamHandler(topic);
  }
  registerByteStreamHandler(topic, callback) {
    return this.incomingDataStreamManager.registerByteStreamHandler(topic, callback);
  }
  unregisterByteStreamHandler(topic) {
    return this.incomingDataStreamManager.unregisterByteStreamHandler(topic);
  }
  /**
   * Establishes the participant as a receiver for calls of the specified RPC method.
   *
   * @param method - The name of the indicated RPC method
   * @param handler - Will be invoked when an RPC request for this method is received
   * @returns A promise that resolves when the method is successfully registered
   * @throws {Error} If a handler for this method is already registered (must call unregisterRpcMethod first)
   *
   * @example
   * ```typescript
   * room.localParticipant?.registerRpcMethod(
   *   'greet',
   *   async (data: RpcInvocationData) => {
   *     console.log(`Received greeting from ${data.callerIdentity}: ${data.payload}`);
   *     return `Hello, ${data.callerIdentity}!`;
   *   }
   * );
   * ```
   *
   * The handler should return a Promise that resolves to a string.
   * If unable to respond within `responseTimeout`, the request will result in an error on the caller's side.
   *
   * You may throw errors of type `RpcError` with a string `message` in the handler,
   * and they will be received on the caller's side with the message intact.
   * Other errors thrown in your handler will not be transmitted as-is, and will instead arrive to the caller as `1500` ("Application Error").
   */
  registerRpcMethod(method, handler) {
    this.rpcServerManager.registerRpcMethod(method, handler);
  }
  /**
   * Unregisters a previously registered RPC method.
   *
   * @param method - The name of the RPC method to unregister
   */
  unregisterRpcMethod(method) {
    this.rpcServerManager.unregisterRpcMethod(method);
  }
  /**
   * @experimental
   */
  setE2EEEnabled(enabled) {
    return __awaiter(this, void 0, void 0, function* () {
      const unlock = yield this.e2eeStateMutex.lock();
      try {
        if (this.e2eeManager) {
          if (this.isE2EEEnabled !== enabled) {
            yield this.localParticipant.setE2EEEnabled(enabled);
            if (this.localParticipant.identity !== "") {
              this.e2eeManager.setParticipantCryptorEnabled(enabled, this.localParticipant.identity);
            }
          }
        } else {
          throw Error("e2ee not configured, please set e2ee settings within the room options");
        }
      } finally {
        unlock();
      }
    });
  }
  setupE2EE() {
    var _a2, _b2;
    const dcEncryptionEnabled = !!this.options.encryption;
    const e2eeOptions = this.options.encryption || this.options.e2ee;
    if (e2eeOptions) {
      if ("e2eeManager" in e2eeOptions) {
        this.e2eeManager = e2eeOptions.e2eeManager;
        this.e2eeManager.isDataChannelEncryptionEnabled = dcEncryptionEnabled;
      } else {
        this.e2eeManager = new E2EEManager(e2eeOptions, dcEncryptionEnabled);
      }
      this.e2eeManager.on(EncryptionEvent.ParticipantEncryptionStatusChanged, (enabled, participant) => {
        if (isLocalParticipant(participant)) {
          this.isE2EEEnabled = enabled;
        }
        this.emit(RoomEvent.ParticipantEncryptionStatusChanged, enabled, participant);
      });
      this.e2eeManager.on(EncryptionEvent.EncryptionError, (error, participantIdentity) => {
        const participant = participantIdentity ? this.getParticipantByIdentity(participantIdentity) : void 0;
        this.emit(RoomEvent.EncryptionError, error, participant);
      });
      (_a2 = this.e2eeManager) === null || _a2 === void 0 ? void 0 : _a2.setup(this);
      (_b2 = this.e2eeManager) === null || _b2 === void 0 ? void 0 : _b2.setupEngine(this.engine);
    }
  }
  setupFrameMetadata() {
    var _a2;
    const opts = (_a2 = this.options.frameMetadata) !== null && _a2 !== void 0 ? _a2 : this.options.packetTrailer;
    this.frameMetadataManager = new FrameMetadataManager(opts);
    this.frameMetadataManager.setup(this);
  }
  get logContext() {
    var _a2, _b2, _c;
    return {
      room: this.name,
      roomID: (_a2 = this.roomInfo) === null || _a2 === void 0 ? void 0 : _a2.sid,
      participant: (_b2 = this.localParticipant) === null || _b2 === void 0 ? void 0 : _b2.identity,
      participantID: (_c = this.localParticipant) === null || _c === void 0 ? void 0 : _c.sid
    };
  }
  /**
   * if the current room has a participant with `recorder: true` in its JWT grant
   **/
  get isRecording() {
    var _a2, _b2;
    return (_b2 = (_a2 = this.roomInfo) === null || _a2 === void 0 ? void 0 : _a2.activeRecording) !== null && _b2 !== void 0 ? _b2 : false;
  }
  /**
   * server assigned unique room id.
   * returns once a sid has been issued by the server.
   */
  getSid() {
    if (this.state === ConnectionState.Disconnected) {
      return TypedPromise.resolve("");
    }
    if (this.roomInfo && this.roomInfo.sid !== "") {
      return TypedPromise.resolve(this.roomInfo.sid);
    }
    return new TypedPromise((resolve, reject) => {
      const handleRoomUpdate = (roomInfo) => {
        if (roomInfo.sid !== "") {
          this.engine.off(EngineEvent.RoomUpdate, handleRoomUpdate);
          resolve(roomInfo.sid);
        }
      };
      this.engine.on(EngineEvent.RoomUpdate, handleRoomUpdate);
      this.once(RoomEvent.Disconnected, () => {
        this.engine.off(EngineEvent.RoomUpdate, handleRoomUpdate);
        reject(new UnexpectedConnectionState("Room disconnected before room server id was available"));
      });
    });
  }
  /** user assigned name, derived from JWT token */
  get name() {
    var _a2, _b2;
    return (_b2 = (_a2 = this.roomInfo) === null || _a2 === void 0 ? void 0 : _a2.name) !== null && _b2 !== void 0 ? _b2 : "";
  }
  /** room metadata */
  get metadata() {
    var _a2;
    return (_a2 = this.roomInfo) === null || _a2 === void 0 ? void 0 : _a2.metadata;
  }
  get numParticipants() {
    var _a2, _b2;
    return (_b2 = (_a2 = this.roomInfo) === null || _a2 === void 0 ? void 0 : _a2.numParticipants) !== null && _b2 !== void 0 ? _b2 : 0;
  }
  get numPublishers() {
    var _a2, _b2;
    return (_b2 = (_a2 = this.roomInfo) === null || _a2 === void 0 ? void 0 : _a2.numPublishers) !== null && _b2 !== void 0 ? _b2 : 0;
  }
  maybeCreateEngine() {
    if (this.engine && (this.engine.isNewlyCreated || !this.engine.isClosed)) {
      return;
    }
    this.engine = new RTCEngine(this.options);
    this.engine.e2eeManager = this.e2eeManager;
    this.engine.on(EngineEvent.ParticipantUpdate, this.handleParticipantUpdates).on(EngineEvent.RoomUpdate, this.handleRoomUpdate).on(EngineEvent.SpeakersChanged, this.handleSpeakersChanged).on(EngineEvent.StreamStateChanged, this.handleStreamStateUpdate).on(EngineEvent.ConnectionQualityUpdate, this.handleConnectionQualityUpdate).on(EngineEvent.SubscriptionError, this.handleSubscriptionError).on(EngineEvent.SubscriptionPermissionUpdate, this.handleSubscriptionPermissionUpdate).on(EngineEvent.MediaTrackAdded, (mediaTrack, stream, receiver) => {
      this.onTrackAdded(mediaTrack, stream, receiver);
    }).on(EngineEvent.Disconnected, (reason) => {
      this.handleDisconnect(this.options.stopLocalTrackOnUnpublish, reason);
    }).on(EngineEvent.ActiveSpeakersUpdate, this.handleActiveSpeakersUpdate).on(EngineEvent.DataPacketReceived, this.handleDataPacket).on(EngineEvent.Resuming, () => {
      this.clearConnectionReconcile();
      this.isResuming = true;
      this.log.debug("Resuming signal connection");
      if (this.setAndEmitConnectionState(ConnectionState.SignalReconnecting)) {
        this.emit(RoomEvent.SignalReconnecting);
      }
    }).on(EngineEvent.Resumed, () => {
      this.registerConnectionReconcile();
      this.isResuming = false;
      this.log.debug("Resumed signal connection");
      this.updateSubscriptions();
      if (this.setAndEmitConnectionState(ConnectionState.Connected)) {
        this.emit(RoomEvent.Reconnected);
      }
      this.emitBufferedEvents();
    }).on(EngineEvent.SignalResumed, () => {
      this.bufferedEvents = [];
      if (this.state === ConnectionState.Reconnecting || this.isResuming) {
        this.sendSyncState();
      }
    }).on(EngineEvent.Restarting, this.handleRestarting).on(EngineEvent.Restarted, this.handleRestarted).on(EngineEvent.SignalRestarted, this.handleSignalRestarted).on(EngineEvent.Offline, () => {
      if (this.setAndEmitConnectionState(ConnectionState.Reconnecting)) {
        this.emit(RoomEvent.Reconnecting);
      }
    }).on(EngineEvent.DCBufferStatusChanged, (status, kind) => {
      this.emit(RoomEvent.DCBufferStatusChanged, status, kind);
    }).on(EngineEvent.LocalTrackSubscribed, (subscribedSid) => {
      this.handleLocalTrackSubscribed(subscribedSid);
    }).on(EngineEvent.RoomMoved, (roomMoved) => {
      this.log.debug("room moved", roomMoved);
      if (roomMoved.room) {
        this.handleRoomUpdate(roomMoved.room);
      }
      this.remoteParticipants.forEach((participant, identity) => {
        this.handleParticipantDisconnected(identity, participant);
      });
      this.emit(RoomEvent.Moved, roomMoved.room.name);
      if (roomMoved.participant) {
        this.handleParticipantUpdates([roomMoved.participant, ...roomMoved.otherParticipants]);
      } else {
        this.handleParticipantUpdates(roomMoved.otherParticipants);
      }
    }).on(EngineEvent.PublishDataTrackResponse, (event) => {
      if (!event.info) {
        this.log.warn("received PublishDataTrackResponse, but event.info was ".concat(event.info, ", so skipping."));
        return;
      }
      this.outgoingDataTrackManager.receivedSfuPublishResponse(event.info.pubHandle, {
        type: "ok",
        data: {
          sid: event.info.sid,
          pubHandle: event.info.pubHandle,
          name: event.info.name,
          usesE2ee: event.info.encryption !== Encryption_Type.NONE
        }
      });
    }).on(EngineEvent.UnPublishDataTrackResponse, (event) => {
      if (!event.info) {
        this.log.warn("received UnPublishDataTrackResponse, but event.info was ".concat(event.info, ", so skipping."));
        return;
      }
      this.outgoingDataTrackManager.receivedSfuUnpublishResponse(event.info.pubHandle);
    }).on(EngineEvent.DataTrackSubscriberHandles, (event) => {
      const handleToSidMapping = new Map(Object.entries(event.subHandles).map((_ref4) => {
        let _ref5 = _slicedToArray(_ref4, 2), key = _ref5[0], value = _ref5[1];
        return [parseInt(key, 10), value.trackSid];
      }));
      this.incomingDataTrackManager.receivedSfuSubscriberHandles(handleToSidMapping);
    }).on(EngineEvent.DataTrackPacketReceived, (packetBytes) => {
      try {
        this.incomingDataTrackManager.packetReceived(packetBytes);
      } catch (err) {
        throw err;
      }
    }).on(EngineEvent.Joined, (joinResponse) => {
      const mapped = new Map(joinResponse.otherParticipants.map((participant) => {
        return [participant.identity, participant.dataTracks.map((info) => DataTrackInfo.from(info))];
      }));
      this.incomingDataTrackManager.receiveSfuPublicationUpdates(mapped);
    }).on(EngineEvent.TokenRefreshed, (token) => {
      var _a2;
      (_a2 = this.regionUrlProvider) === null || _a2 === void 0 ? void 0 : _a2.updateToken(token);
    }).on(EngineEvent.ServerRegionsReported, (regions) => {
      var _a2;
      (_a2 = this.regionUrlProvider) === null || _a2 === void 0 ? void 0 : _a2.setServerReportedRegions({
        regionSettings: regions,
        updatedAtInMs: Date.now(),
        maxAgeInMs: DEFAULT_MAX_AGE_MS
      });
    });
    if (this.localParticipant) {
      this.localParticipant.setupEngine(this.engine);
    }
    if (this.e2eeManager) {
      this.e2eeManager.setupEngine(this.engine);
    }
    if (this.outgoingDataStreamManager) {
      this.outgoingDataStreamManager.setupEngine(this.engine);
    }
  }
  createRegionStrategy() {
    return {
      getNextUrl: (signal) => __awaiter(this, void 0, void 0, function* () {
        return this.regionUrlProvider ? this.regionUrlProvider.getNextBestRegionUrl(signal) : null;
      }),
      resetAttempts: () => {
        var _a2;
        return (_a2 = this.regionUrlProvider) === null || _a2 === void 0 ? void 0 : _a2.resetAttempts();
      }
    };
  }
  /**
   * getLocalDevices abstracts navigator.mediaDevices.enumerateDevices.
   * In particular, it requests device permissions by default if needed
   * and makes sure the returned device does not consist of dummy devices
   * @param kind
   * @returns a list of available local devices
   */
  static getLocalDevices(kind) {
    let requestPermissions = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : true;
    return DeviceManager.getInstance().getDevices(kind, requestPermissions);
  }
  /**
   * prepareConnection should be called as soon as the page is loaded, in order
   * to speed up the connection attempt. This function will
   * - perform DNS resolution and pre-warm the DNS cache
   * - establish TLS connection and cache TLS keys
   *
   * With LiveKit Cloud, it will also determine the best edge data center for
   * the current client to connect to if a token is provided.
   */
  prepareConnection(url, token) {
    return __awaiter(this, void 0, void 0, function* () {
      if (this.state !== ConnectionState.Disconnected) {
        return;
      }
      this.log.debug("prepareConnection to ".concat(url));
      try {
        if (isCloud(new URL(url)) && token) {
          this.regionUrlProvider = new RegionUrlProvider(url, token);
          const regionUrl = yield this.regionUrlProvider.getNextBestRegionUrl();
          if (regionUrl && this.state === ConnectionState.Disconnected) {
            this.regionUrl = regionUrl;
            yield fetch(toHttpUrl(regionUrl), {
              method: "HEAD"
            });
            this.log.debug("prepared connection to ".concat(regionUrl));
          }
        } else {
          yield fetch(toHttpUrl(url), {
            method: "HEAD"
          });
        }
      } catch (e4) {
        this.log.warn("could not prepare connection", {
          error: e4
        });
      }
    });
  }
  /**
   * retrieves a participant by identity
   * @param identity
   * @returns
   */
  getParticipantByIdentity(identity) {
    if (this.localParticipant.identity === identity) {
      return this.localParticipant;
    }
    return this.remoteParticipants.get(identity);
  }
  clearConnectionFutures() {
    this.connectFuture = void 0;
  }
  /**
   * @internal for testing
   */
  simulateScenario(scenario, arg) {
    return __awaiter(this, void 0, void 0, function* () {
      let postAction = () => __awaiter(this, void 0, void 0, function* () {
      });
      let req;
      switch (scenario) {
        case "signal-reconnect":
          yield this.engine.client.handleOnClose("simulate disconnect");
          break;
        case "fail-on-v1-path":
          this.engine.failNextV1Path();
          break;
        case "speaker":
          req = new SimulateScenario({
            scenario: {
              case: "speakerUpdate",
              value: 3
            }
          });
          break;
        case "node-failure":
          req = new SimulateScenario({
            scenario: {
              case: "nodeFailure",
              value: true
            }
          });
          break;
        case "server-leave":
          req = new SimulateScenario({
            scenario: {
              case: "serverLeave",
              value: true
            }
          });
          break;
        case "migration":
          req = new SimulateScenario({
            scenario: {
              case: "migration",
              value: true
            }
          });
          break;
        case "resume-reconnect":
          this.engine.failNext();
          yield this.engine.client.handleOnClose("simulate resume-disconnect");
          break;
        case "disconnect-signal-on-resume":
          postAction = () => __awaiter(this, void 0, void 0, function* () {
            yield this.engine.client.handleOnClose("simulate resume-disconnect");
          });
          req = new SimulateScenario({
            scenario: {
              case: "disconnectSignalOnResume",
              value: true
            }
          });
          break;
        case "disconnect-signal-on-resume-no-messages":
          postAction = () => __awaiter(this, void 0, void 0, function* () {
            yield this.engine.client.handleOnClose("simulate resume-disconnect");
          });
          req = new SimulateScenario({
            scenario: {
              case: "disconnectSignalOnResumeNoMessages",
              value: true
            }
          });
          break;
        case "full-reconnect":
          this.engine.fullReconnectOnNext = true;
          yield this.engine.client.handleOnClose("simulate full-reconnect");
          break;
        case "force-tcp":
        case "force-tls":
          req = new SimulateScenario({
            scenario: {
              case: "switchCandidateProtocol",
              value: scenario === "force-tls" ? 2 : 1
            }
          });
          postAction = () => __awaiter(this, void 0, void 0, function* () {
            const onLeave = this.engine.client.onLeave;
            if (onLeave) {
              onLeave(new LeaveRequest({
                reason: DisconnectReason.CLIENT_INITIATED,
                action: LeaveRequest_Action.RECONNECT
              }));
            }
          });
          break;
        case "subscriber-bandwidth":
          if (arg === void 0 || typeof arg !== "number") {
            throw new Error("subscriber-bandwidth requires a number as argument");
          }
          req = new SimulateScenario({
            scenario: {
              case: "subscriberBandwidth",
              value: numberToBigInt(arg)
            }
          });
          break;
        case "leave-full-reconnect":
          req = new SimulateScenario({
            scenario: {
              case: "leaveRequestFullReconnect",
              value: true
            }
          });
      }
      if (req) {
        yield this.engine.client.sendSimulateScenario(req);
        yield postAction();
      }
    });
  }
  /**
   * Returns true if audio playback is enabled
   */
  get canPlaybackAudio() {
    return this.audioEnabled;
  }
  /**
   * Returns true if video playback is enabled
   */
  get canPlaybackVideo() {
    return !this.isVideoPlaybackBlocked;
  }
  getActiveDevice(kind) {
    return this.localParticipant.activeDeviceMap.get(kind);
  }
  /**
   * Switches all active devices used in this room to the given device.
   *
   * Note: setting AudioOutput is not supported on some browsers. See [setSinkId](https://developer.mozilla.org/en-US/docs/Web/API/HTMLMediaElement/setSinkId#browser_compatibility)
   *
   * @param kind use `videoinput` for camera track,
   *  `audioinput` for microphone track,
   *  `audiooutput` to set speaker for all incoming audio tracks
   * @param deviceId
   */
  switchActiveDevice(kind_1, deviceId_1) {
    return __awaiter(this, arguments, void 0, function(kind, deviceId) {
      var _this3 = this;
      let exact = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : true;
      return function* () {
        var _a2, _b2, _c, _d, _e2, _f;
        var _g;
        let success = true;
        let shouldTriggerImmediateDeviceChange = false;
        const deviceConstraint = exact ? {
          exact: deviceId
        } : deviceId;
        if (kind === "audioinput") {
          shouldTriggerImmediateDeviceChange = _this3.localParticipant.audioTrackPublications.size === 0;
          const prevDeviceId = (_a2 = _this3.getActiveDevice(kind)) !== null && _a2 !== void 0 ? _a2 : _this3.options.audioCaptureDefaults.deviceId;
          _this3.options.audioCaptureDefaults.deviceId = deviceConstraint;
          const tracks = Array.from(_this3.localParticipant.audioTrackPublications.values()).filter((track) => track.source === Track.Source.Microphone);
          try {
            success = (yield Promise.all(tracks.map((t) => {
              var _a3;
              return (_a3 = t.audioTrack) === null || _a3 === void 0 ? void 0 : _a3.setDeviceId(deviceConstraint);
            }))).every((val) => val === true);
          } catch (e4) {
            _this3.options.audioCaptureDefaults.deviceId = prevDeviceId;
            throw e4;
          }
          const isMuted = tracks.some((t) => {
            var _a3, _b3;
            return (_b3 = (_a3 = t.track) === null || _a3 === void 0 ? void 0 : _a3.isMuted) !== null && _b3 !== void 0 ? _b3 : false;
          });
          if (success && isMuted) shouldTriggerImmediateDeviceChange = true;
        } else if (kind === "videoinput") {
          shouldTriggerImmediateDeviceChange = _this3.localParticipant.videoTrackPublications.size === 0;
          const prevDeviceId = (_b2 = _this3.getActiveDevice(kind)) !== null && _b2 !== void 0 ? _b2 : _this3.options.videoCaptureDefaults.deviceId;
          _this3.options.videoCaptureDefaults.deviceId = deviceConstraint;
          const tracks = Array.from(_this3.localParticipant.videoTrackPublications.values()).filter((track) => track.source === Track.Source.Camera);
          try {
            success = (yield Promise.all(tracks.map((t) => {
              var _a3;
              return (_a3 = t.videoTrack) === null || _a3 === void 0 ? void 0 : _a3.setDeviceId(deviceConstraint);
            }))).every((val) => val === true);
          } catch (e4) {
            _this3.options.videoCaptureDefaults.deviceId = prevDeviceId;
            throw e4;
          }
          const isMuted = tracks.some((t) => {
            var _a3, _b3;
            return (_b3 = (_a3 = t.track) === null || _a3 === void 0 ? void 0 : _a3.isMuted) !== null && _b3 !== void 0 ? _b3 : false;
          });
          if (success && isMuted) shouldTriggerImmediateDeviceChange = true;
        } else if (kind === "audiooutput") {
          shouldTriggerImmediateDeviceChange = true;
          if (!supportsSetSinkId() && !_this3.options.webAudioMix || _this3.options.webAudioMix && _this3.audioContext && !("setSinkId" in _this3.audioContext)) {
            throw new Error("cannot switch audio output, the current browser does not support it");
          }
          if (_this3.options.webAudioMix) {
            deviceId = (_c = yield DeviceManager.getInstance().normalizeDeviceId("audiooutput", deviceId)) !== null && _c !== void 0 ? _c : "";
          }
          (_d = (_g = _this3.options).audioOutput) !== null && _d !== void 0 ? _d : _g.audioOutput = {};
          const prevDeviceId = (_e2 = _this3.getActiveDevice(kind)) !== null && _e2 !== void 0 ? _e2 : _this3.options.audioOutput.deviceId;
          _this3.options.audioOutput.deviceId = deviceId;
          try {
            if (_this3.options.webAudioMix) {
              (_f = _this3.audioContext) === null || _f === void 0 ? void 0 : _f.setSinkId(deviceId);
            }
            yield Promise.all(Array.from(_this3.remoteParticipants.values()).map((p) => p.setAudioOutput({
              deviceId
            })));
          } catch (e4) {
            _this3.options.audioOutput.deviceId = prevDeviceId;
            throw e4;
          }
        }
        if (shouldTriggerImmediateDeviceChange) {
          _this3.localParticipant.activeDeviceMap.set(kind, deviceId);
          _this3.emit(RoomEvent.ActiveDeviceChanged, kind, deviceId);
        }
        return success;
      }();
    });
  }
  setupLocalParticipantEvents() {
    this.localParticipant.on(ParticipantEvent.ParticipantMetadataChanged, this.onLocalParticipantMetadataChanged).on(ParticipantEvent.ParticipantNameChanged, this.onLocalParticipantNameChanged).on(ParticipantEvent.AttributesChanged, this.onLocalAttributesChanged).on(ParticipantEvent.TrackMuted, this.onLocalTrackMuted).on(ParticipantEvent.TrackUnmuted, this.onLocalTrackUnmuted).on(ParticipantEvent.LocalTrackPublished, this.onLocalTrackPublished).on(ParticipantEvent.LocalTrackUnpublished, this.onLocalTrackUnpublished).on(ParticipantEvent.ConnectionQualityChanged, this.onLocalConnectionQualityChanged).on(ParticipantEvent.MediaDevicesError, this.onMediaDevicesError).on(ParticipantEvent.AudioStreamAcquired, this.startAudio).on(ParticipantEvent.ChatMessage, this.onLocalChatMessageSent).on(ParticipantEvent.ParticipantPermissionsChanged, this.onLocalParticipantPermissionsChanged);
  }
  recreateEngine(sendLeave) {
    const oldEngine = this.engine;
    if (sendLeave && oldEngine && !oldEngine.client.isDisconnected) {
      oldEngine.client.sendLeave().finally(() => oldEngine.close());
    } else {
      oldEngine === null || oldEngine === void 0 ? void 0 : oldEngine.close();
    }
    this.engine = void 0;
    this.isResuming = false;
    this.remoteParticipants.clear();
    this.sidToIdentity.clear();
    this.bufferedEvents = [];
    this.maybeCreateEngine();
  }
  onTrackAdded(mediaTrack, stream, receiver) {
    if (this.state === ConnectionState.Connecting || this.state === ConnectionState.Reconnecting) {
      const reconnectedHandler = () => {
        this.log.debug("deferring on track for later", {
          mediaTrackId: mediaTrack.id,
          mediaStreamId: stream.id,
          tracksInStream: stream.getTracks().map((track) => track.id)
        });
        this.onTrackAdded(mediaTrack, stream, receiver);
        cleanup();
      };
      const cleanup = () => {
        this.off(RoomEvent.Reconnected, reconnectedHandler);
        this.off(RoomEvent.Connected, reconnectedHandler);
        this.off(RoomEvent.Disconnected, cleanup);
      };
      this.once(RoomEvent.Reconnected, reconnectedHandler);
      this.once(RoomEvent.Connected, reconnectedHandler);
      this.once(RoomEvent.Disconnected, cleanup);
      return;
    }
    if (this.state === ConnectionState.Disconnected) {
      this.log.warn("skipping incoming track after Room disconnected");
      return;
    }
    if (mediaTrack.readyState === "ended") {
      this.log.debug("skipping incoming track as it already ended");
      return;
    }
    const parts = unpackStreamId(stream.id);
    const participantSid = parts[0];
    let streamId = parts[1];
    let trackId = mediaTrack.id;
    if (streamId && streamId.startsWith("TR")) trackId = streamId;
    if (participantSid === this.localParticipant.sid) {
      this.log.warn("tried to create RemoteParticipant for local participant");
      return;
    }
    const participant = Array.from(this.remoteParticipants.values()).find((p) => p.sid === participantSid);
    if (!participant) {
      if (participantSid.startsWith("PA")) {
        this.log.error("Tried to add a track for a participant, that's not present. Sid: ".concat(participantSid));
      }
      return;
    }
    if (!trackId.startsWith("TR")) {
      const id = this.engine.getTrackIdForReceiver(receiver);
      if (!id) {
        this.log.error("Tried to add a track whose 'sid' could not be found for a participant, that's not present. Sid: ".concat(participantSid));
        return;
      }
      trackId = id;
    }
    if (!trackId.startsWith("TR")) {
      this.log.warn("Tried to add a track whose 'sid' could not be determined for a participant, that's not present. Sid: ".concat(participantSid, ", streamId: ").concat(streamId, ", trackId: ").concat(trackId), {
        remoteParticipantID: participantSid,
        streamId,
        trackId
      });
    }
    let adaptiveStreamSettings;
    if (this.options.adaptiveStream) {
      if (typeof this.options.adaptiveStream === "object") {
        adaptiveStreamSettings = this.options.adaptiveStream;
      } else {
        adaptiveStreamSettings = {};
      }
    }
    const publication = participant.addSubscribedMediaTrack(mediaTrack, trackId, stream, receiver, adaptiveStreamSettings);
    if ((publication === null || publication === void 0 ? void 0 : publication.isEncrypted) && !this.e2eeManager) {
      this.emit(RoomEvent.EncryptionError, new Error("Encrypted ".concat(publication.source, " track received from participant ").concat(participant.sid, ", but room does not have encryption enabled!")));
    }
  }
  handleLocalTrackSubscribed(subscribedSid) {
    const findPublication = () => this.localParticipant.getTrackPublications().find((_ref6) => {
      let trackSid = _ref6.trackSid;
      return trackSid === subscribedSid;
    });
    const trackPublication = findPublication();
    if (trackPublication) {
      this.emitLocalTrackSubscribed(trackPublication);
      return;
    }
    this.log.debug("deferring LocalTrackSubscribed, publication not yet available", {
      subscribedSid
    });
    const TIMEOUT_MS = 1e4;
    let timer;
    const onPublished = (pub) => {
      if (pub.trackSid === subscribedSid) {
        cleanup();
        this.emitLocalTrackSubscribed(pub);
      }
    };
    const cleanup = () => {
      clearTimeout(timer);
      this.localParticipant.off(ParticipantEvent.LocalTrackPublished, onPublished);
      this.off(RoomEvent.Disconnected, cleanup);
    };
    this.localParticipant.on(ParticipantEvent.LocalTrackPublished, onPublished);
    this.once(RoomEvent.Disconnected, cleanup);
    timer = setTimeout(() => {
      cleanup();
      const pub = findPublication();
      if (pub) {
        this.emitLocalTrackSubscribed(pub);
      } else {
        this.log.warn("could not find local track publication for LocalTrackSubscribed event after timeout", {
          subscribedSid
        });
      }
    }, TIMEOUT_MS);
  }
  emitLocalTrackSubscribed(trackPublication) {
    this.localParticipant.emit(ParticipantEvent.LocalTrackSubscribed, trackPublication);
    this.emitWhenConnected(RoomEvent.LocalTrackSubscribed, trackPublication, this.localParticipant);
  }
  handleDisconnect() {
    let shouldStopTracks = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : true;
    let reason = arguments.length > 1 ? arguments[1] : void 0;
    var _a2, _b2;
    this.clearConnectionReconcile();
    this.isResuming = false;
    this.bufferedEvents = [];
    this.transcriptionReceivedTimes.clear();
    this.incomingDataStreamManager.clearControllers();
    this.incomingDataTrackManager.reset();
    this.outgoingDataTrackManager.reset();
    if (this.state === ConnectionState.Disconnected) {
      return;
    }
    this.regionUrl = void 0;
    if (this.regionUrlProvider) {
      this.regionUrlProvider.notifyDisconnected();
    }
    try {
      this.remoteParticipants.forEach((p) => {
        p.trackPublications.forEach((pub) => {
          p.unpublishTrack(pub.trackSid);
        });
      });
      this.localParticipant.trackPublications.forEach((pub) => {
        var _a3, _b3, _c;
        if (pub.track) {
          this.localParticipant.unpublishTrack(pub.track, shouldStopTracks);
        }
        if (shouldStopTracks) {
          (_a3 = pub.track) === null || _a3 === void 0 ? void 0 : _a3.detach();
          (_b3 = pub.track) === null || _b3 === void 0 ? void 0 : _b3.stop();
        } else {
          (_c = pub.track) === null || _c === void 0 ? void 0 : _c.stopMonitor();
        }
      });
      this.localParticipant.off(ParticipantEvent.ParticipantMetadataChanged, this.onLocalParticipantMetadataChanged).off(ParticipantEvent.ParticipantNameChanged, this.onLocalParticipantNameChanged).off(ParticipantEvent.AttributesChanged, this.onLocalAttributesChanged).off(ParticipantEvent.TrackMuted, this.onLocalTrackMuted).off(ParticipantEvent.TrackUnmuted, this.onLocalTrackUnmuted).off(ParticipantEvent.LocalTrackPublished, this.onLocalTrackPublished).off(ParticipantEvent.LocalTrackUnpublished, this.onLocalTrackUnpublished).off(ParticipantEvent.ConnectionQualityChanged, this.onLocalConnectionQualityChanged).off(ParticipantEvent.MediaDevicesError, this.onMediaDevicesError).off(ParticipantEvent.AudioStreamAcquired, this.startAudio).off(ParticipantEvent.ChatMessage, this.onLocalChatMessageSent).off(ParticipantEvent.ParticipantPermissionsChanged, this.onLocalParticipantPermissionsChanged);
      this.localParticipant.trackPublications.clear();
      this.localParticipant.videoTrackPublications.clear();
      this.localParticipant.audioTrackPublications.clear();
      this.remoteParticipants.clear();
      this.sidToIdentity.clear();
      this.activeSpeakers = [];
      if (this.audioContext && typeof this.options.webAudioMix === "boolean") {
        this.audioContext.close();
        this.audioContext = void 0;
      }
      if (isWeb()) {
        window.removeEventListener("beforeunload", this.onPageLeave);
        window.removeEventListener("pagehide", this.onPageLeave);
        window.removeEventListener("freeze", this.onPageLeave);
        (_b2 = (_a2 = navigator.mediaDevices) === null || _a2 === void 0 ? void 0 : _a2.removeEventListener) === null || _b2 === void 0 ? void 0 : _b2.call(_a2, "devicechange", this.handleDeviceChange);
      }
    } finally {
      this.setAndEmitConnectionState(ConnectionState.Disconnected);
      this.emit(RoomEvent.Disconnected, reason);
    }
  }
  handleParticipantDisconnected(identity, participant) {
    this.remoteParticipants.delete(identity);
    if (!participant) {
      return;
    }
    this.incomingDataStreamManager.validateParticipantHasNoActiveDataStreams(identity);
    this.incomingDataTrackManager.handleRemoteParticipantDisconnected(identity);
    participant.trackPublications.forEach((publication) => {
      participant.unpublishTrack(publication.trackSid, true);
    });
    this.emit(RoomEvent.ParticipantDisconnected, participant);
    participant.setDisconnected();
    this.rpcClientManager.handleParticipantDisconnected(participant.identity);
  }
  /**
   * attempt to select the default devices if the previously selected devices are no longer available after a device change event
   */
  selectDefaultDevices() {
    return __awaiter(this, void 0, void 0, function* () {
      var _a2, _b2, _c;
      const previousDevices = DeviceManager.getInstance().previousDevices;
      const availableDevices = yield DeviceManager.getInstance().getDevices(void 0, false);
      const browser = getBrowser();
      if ((browser === null || browser === void 0 ? void 0 : browser.name) === "Chrome" && browser.os !== "iOS") {
        for (let availableDevice of availableDevices) {
          const previousDevice = previousDevices.find((info) => info.deviceId === availableDevice.deviceId);
          if (previousDevice && previousDevice.label !== "" && previousDevice.kind === availableDevice.kind && previousDevice.label !== availableDevice.label) {
            if (this.getActiveDevice(availableDevice.kind) === "default") {
              this.emit(RoomEvent.ActiveDeviceChanged, availableDevice.kind, availableDevice.deviceId);
            }
          }
        }
      }
      const kinds = ["audiooutput", "audioinput", "videoinput"];
      for (let kind of kinds) {
        const targetSource = kindToSource(kind);
        const targetPublication = this.localParticipant.getTrackPublication(targetSource);
        if (targetPublication && ((_a2 = targetPublication.track) === null || _a2 === void 0 ? void 0 : _a2.isUserProvided)) {
          continue;
        }
        const devicesOfKind = availableDevices.filter((d) => d.kind === kind);
        const activeDevice = this.getActiveDevice(kind);
        if (activeDevice === ((_b2 = previousDevices.filter((info) => info.kind === kind)[0]) === null || _b2 === void 0 ? void 0 : _b2.deviceId)) {
          if (devicesOfKind.length > 0 && ((_c = devicesOfKind[0]) === null || _c === void 0 ? void 0 : _c.deviceId) !== activeDevice) {
            yield this.switchActiveDevice(kind, devicesOfKind[0].deviceId);
            continue;
          }
        }
        if (kind === "audioinput" && !isSafariBased() || kind === "videoinput") {
          continue;
        }
        if (devicesOfKind.length > 0 && !devicesOfKind.find((deviceInfo) => deviceInfo.deviceId === this.getActiveDevice(kind)) && // avoid switching audio output on safari without explicit user action as it leads to slowed down audio playback
        (kind !== "audiooutput" || !isSafariBased())) {
          yield this.switchActiveDevice(kind, devicesOfKind[0].deviceId);
        }
      }
    });
  }
  acquireAudioContext() {
    return __awaiter(this, void 0, void 0, function* () {
      var _a2, _b2;
      if (typeof this.options.webAudioMix !== "boolean" && this.options.webAudioMix.audioContext) {
        this.audioContext = this.options.webAudioMix.audioContext;
      } else if (!this.audioContext || this.audioContext.state === "closed") {
        this.audioContext = (_a2 = getNewAudioContext()) !== null && _a2 !== void 0 ? _a2 : void 0;
      }
      if (this.options.webAudioMix) {
        this.remoteParticipants.forEach((participant) => participant.setAudioContext(this.audioContext));
      }
      this.localParticipant.setAudioContext(this.audioContext);
      if (this.audioContext && this.audioContext.state === "suspended") {
        try {
          yield Promise.race([this.audioContext.resume(), sleep(200)]);
        } catch (e4) {
          this.log.warn("Could not resume audio context", {
            error: e4
          });
        }
      }
      const newContextIsRunning = ((_b2 = this.audioContext) === null || _b2 === void 0 ? void 0 : _b2.state) === "running";
      if (newContextIsRunning !== this.canPlaybackAudio) {
        this.audioEnabled = newContextIsRunning;
        this.emit(RoomEvent.AudioPlaybackStatusChanged, newContextIsRunning);
      }
    });
  }
  createParticipant(identity, info) {
    var _a2;
    let participant;
    if (info) {
      participant = RemoteParticipant.fromParticipantInfo(this.engine.client, info, {
        loggerContextCb: () => this.logContext,
        loggerName: this.options.loggerName
      }, this.incomingDataTrackManager);
    } else {
      participant = new RemoteParticipant(this.engine.client, "", identity, void 0, void 0, void 0, {
        loggerContextCb: () => this.logContext,
        loggerName: this.options.loggerName
      });
    }
    if (this.options.webAudioMix) {
      participant.setAudioContext(this.audioContext);
    }
    if ((_a2 = this.options.audioOutput) === null || _a2 === void 0 ? void 0 : _a2.deviceId) {
      participant.setAudioOutput(this.options.audioOutput).catch((e4) => this.log.warn("Could not set audio output: ".concat(e4.message)));
    }
    return participant;
  }
  getOrCreateParticipant(identity, info) {
    if (this.remoteParticipants.has(identity)) {
      const existingParticipant = this.remoteParticipants.get(identity);
      if (info) {
        const wasUpdated = existingParticipant.updateInfo(info);
        if (wasUpdated) {
          this.sidToIdentity.set(info.sid, info.identity);
        }
      }
      return existingParticipant;
    }
    const participant = this.createParticipant(identity, info);
    this.remoteParticipants.set(identity, participant);
    this.sidToIdentity.set(info.sid, info.identity);
    this.emitWhenConnected(RoomEvent.ParticipantConnected, participant);
    participant.on(ParticipantEvent.TrackPublished, (trackPublication) => {
      this.emitWhenConnected(RoomEvent.TrackPublished, trackPublication, participant);
    }).on(ParticipantEvent.TrackSubscribed, (track, publication) => {
      if (track.kind === Track.Kind.Audio) {
        track.on(TrackEvent.AudioPlaybackStarted, this.handleAudioPlaybackStarted);
        track.on(TrackEvent.AudioPlaybackFailed, this.handleAudioPlaybackFailed);
      } else if (track.kind === Track.Kind.Video) {
        track.on(TrackEvent.VideoPlaybackFailed, this.handleVideoPlaybackFailed);
        track.on(TrackEvent.VideoPlaybackStarted, this.handleVideoPlaybackStarted);
      }
      this.emitWhenConnected(RoomEvent.TrackSubscribed, track, publication, participant);
    }).on(ParticipantEvent.TrackUnpublished, (publication) => {
      this.emit(RoomEvent.TrackUnpublished, publication, participant);
    }).on(ParticipantEvent.TrackUnsubscribed, (track, publication) => {
      this.emit(RoomEvent.TrackUnsubscribed, track, publication, participant);
    }).on(ParticipantEvent.TrackMuted, (pub) => {
      this.emitWhenConnected(RoomEvent.TrackMuted, pub, participant);
    }).on(ParticipantEvent.TrackUnmuted, (pub) => {
      this.emitWhenConnected(RoomEvent.TrackUnmuted, pub, participant);
    }).on(ParticipantEvent.ParticipantMetadataChanged, (metadata) => {
      this.emitWhenConnected(RoomEvent.ParticipantMetadataChanged, metadata, participant);
    }).on(ParticipantEvent.ParticipantNameChanged, (name) => {
      this.emitWhenConnected(RoomEvent.ParticipantNameChanged, name, participant);
    }).on(ParticipantEvent.AttributesChanged, (changedAttributes) => {
      this.emitWhenConnected(RoomEvent.ParticipantAttributesChanged, changedAttributes, participant);
    }).on(ParticipantEvent.ConnectionQualityChanged, (quality) => {
      this.emitWhenConnected(RoomEvent.ConnectionQualityChanged, quality, participant);
    }).on(ParticipantEvent.ParticipantPermissionsChanged, (prevPermissions) => {
      this.emitWhenConnected(RoomEvent.ParticipantPermissionsChanged, prevPermissions, participant);
    }).on(ParticipantEvent.TrackSubscriptionStatusChanged, (pub, status) => {
      this.emitWhenConnected(RoomEvent.TrackSubscriptionStatusChanged, pub, status, participant);
    }).on(ParticipantEvent.TrackSubscriptionFailed, (trackSid, error) => {
      this.emit(RoomEvent.TrackSubscriptionFailed, trackSid, participant, error);
    }).on(ParticipantEvent.TrackSubscriptionPermissionChanged, (pub, status) => {
      this.emitWhenConnected(RoomEvent.TrackSubscriptionPermissionChanged, pub, status, participant);
    }).on(ParticipantEvent.Active, () => {
      this.emitWhenConnected(RoomEvent.ParticipantActive, participant);
      if (participant.kind === ParticipantInfo_Kind.AGENT) {
        this.localParticipant.setActiveAgent(participant);
      }
    });
    if (info) {
      participant.updateInfo(info);
    }
    return participant;
  }
  sendSyncState() {
    const remoteTracks = Array.from(this.remoteParticipants.values()).reduce((acc, participant) => {
      acc.push(...participant.getTrackPublications());
      return acc;
    }, []);
    const localTracks = this.localParticipant.getTrackPublications();
    const localDataTrackInfos = this.outgoingDataTrackManager.queryPublished();
    this.engine.sendSyncState(remoteTracks, localTracks, localDataTrackInfos);
  }
  /**
   * After resuming, we'll need to notify the server of the current
   * subscription settings.
   */
  updateSubscriptions() {
    for (const p of this.remoteParticipants.values()) {
      for (const pub of p.videoTrackPublications.values()) {
        if (pub.isSubscribed && isRemotePub(pub)) {
          pub.emitTrackUpdate();
        }
      }
    }
  }
  getRemoteParticipantBySid(sid) {
    const identity = this.sidToIdentity.get(sid);
    if (identity) {
      return this.remoteParticipants.get(identity);
    }
  }
  /** The client capabilities this SDK advertises to other participants in its `ClientInfo`. */
  getClientInfoCapabilities(roomOptions) {
    var _a2;
    const capabilities = [];
    if (isFrameMetadataSupported((_a2 = roomOptions.frameMetadata) !== null && _a2 !== void 0 ? _a2 : roomOptions.packetTrailer) || !!this.e2eeManager) {
      capabilities.push(ClientInfo_Capability.CAP_PACKET_TRAILER);
    }
    if (isCompressionStreamSupported()) {
      capabilities.push(ClientInfo_Capability.CAP_COMPRESSION_DEFLATE_RAW);
    }
    return capabilities;
  }
  registerRpcDataStreamHandler() {
    this.incomingDataStreamManager.registerTextStreamHandler(RPC_REQUEST_DATA_STREAM_TOPIC, (reader_1, _a2) => __awaiter(this, [reader_1, _a2], void 0, function(reader, _ref7) {
      var _this4 = this;
      let identity = _ref7.identity;
      return function* () {
        var _b2;
        const attributes = (_b2 = reader.info.attributes) !== null && _b2 !== void 0 ? _b2 : {};
        yield _this4.rpcServerManager.handleIncomingDataStream(reader, identity, attributes);
      }();
    }));
    this.incomingDataStreamManager.registerTextStreamHandler(RPC_RESPONSE_DATA_STREAM_TOPIC, (reader_1, _a2) => __awaiter(this, [reader_1, _a2], void 0, function(reader, _ref8) {
      var _this5 = this;
      let identity = _ref8.identity;
      return function* () {
        var _b2;
        const attributes = (_b2 = reader.info.attributes) !== null && _b2 !== void 0 ? _b2 : {};
        yield _this5.rpcClientManager.handleIncomingDataStream(reader, identity, attributes);
      }();
    }));
  }
  registerConnectionReconcile() {
    this.clearConnectionReconcile();
    let consecutiveFailures = 0;
    this.connectionReconcileInterval = CriticalTimers.setInterval(() => {
      if (
        // ensure we didn't tear it down
        !this.engine || // engine detected close, but Room missed it
        this.engine.isClosed || // transports failed without notifying engine
        !this.engine.verifyTransport()
      ) {
        consecutiveFailures++;
        this.log.warn("detected connection state mismatch", {
          numFailures: consecutiveFailures,
          engine: this.engine ? {
            closed: this.engine.isClosed,
            transportsConnectedOrConnecting: this.engine.verifyTransport()
          } : void 0
        });
        if (consecutiveFailures >= 3) {
          this.recreateEngine();
          this.handleDisconnect(this.options.stopLocalTrackOnUnpublish, DisconnectReason.STATE_MISMATCH);
        }
      } else {
        consecutiveFailures = 0;
      }
    }, CONNECTION_RECONCILE_FREQUENCY_MS);
  }
  clearConnectionReconcile() {
    if (this.connectionReconcileInterval) {
      CriticalTimers.clearInterval(this.connectionReconcileInterval);
    }
  }
  setAndEmitConnectionState(state) {
    if (state === this.state) {
      return false;
    }
    this.log.info("connection state changed: ".concat(this.state, " -> ").concat(state));
    this.state = state;
    this.incomingDataStreamManager.setConnected(state === ConnectionState.Connected);
    this.emit(RoomEvent.ConnectionStateChanged, this.state);
    return true;
  }
  emitBufferedEvents() {
    this.bufferedEvents.forEach((_ref9) => {
      let _ref0 = _slicedToArray(_ref9, 2), ev = _ref0[0], args = _ref0[1];
      this.emit(ev, ...args);
    });
    this.bufferedEvents = [];
  }
  emitWhenConnected(event) {
    for (var _len2 = arguments.length, args = new Array(_len2 > 1 ? _len2 - 1 : 0), _key2 = 1; _key2 < _len2; _key2++) {
      args[_key2 - 1] = arguments[_key2];
    }
    if (this.state === ConnectionState.Reconnecting || this.isResuming || !this.engine || this.engine.pendingReconnect) {
      this.bufferedEvents.push([event, args]);
    } else if (this.state === ConnectionState.Connected) {
      return this.emit(event, ...args);
    }
    return false;
  }
  /**
   * Allows to populate a room with simulated participants.
   * No actual connection to a server will be established, all state is
   * @experimental
   */
  simulateParticipants(options) {
    return __awaiter(this, void 0, void 0, function* () {
      var _a2, _b2, _c, _d;
      const publishOptions = Object.assign({
        audio: true,
        video: true,
        useRealTracks: false
      }, options.publish);
      const participantOptions = Object.assign({
        count: 9,
        audio: false,
        video: true,
        aspectRatios: [1.66, 1.7, 1.3]
      }, options.participants);
      this.handleDisconnect();
      this.roomInfo = new Room$1({
        sid: "RM_SIMULATED",
        name: "simulated-room",
        emptyTimeout: 0,
        maxParticipants: 0,
        creationTime: protoInt64.parse((/* @__PURE__ */ new Date()).getTime()),
        metadata: "",
        numParticipants: 1,
        numPublishers: 1,
        turnPassword: "",
        enabledCodecs: [],
        activeRecording: false
      });
      this.localParticipant.updateInfo(new ParticipantInfo({
        identity: "simulated-local",
        name: "local-name"
      }));
      this.setupLocalParticipantEvents();
      this.emit(RoomEvent.SignalConnected);
      this.emit(RoomEvent.Connected);
      this.setAndEmitConnectionState(ConnectionState.Connected);
      if (publishOptions.video) {
        const camPub = new LocalTrackPublication(Track.Kind.Video, new TrackInfo({
          source: TrackSource.CAMERA,
          sid: Math.floor(Math.random() * 1e4).toString(),
          type: TrackType.AUDIO,
          name: "video-dummy"
        }), new LocalVideoTrack(publishOptions.useRealTracks && ((_a2 = window.navigator.mediaDevices) === null || _a2 === void 0 ? void 0 : _a2.getUserMedia) ? (yield window.navigator.mediaDevices.getUserMedia({
          video: true
        })).getVideoTracks()[0] : createDummyVideoStreamTrack(160 * ((_b2 = participantOptions.aspectRatios[0]) !== null && _b2 !== void 0 ? _b2 : 1), 160, true, true), void 0, false, {
          loggerName: this.options.loggerName,
          loggerContextCb: () => this.logContext
        }), {
          loggerName: this.options.loggerName,
          loggerContextCb: () => this.logContext
        });
        this.localParticipant.addTrackPublication(camPub);
        this.localParticipant.emit(ParticipantEvent.LocalTrackPublished, camPub);
      }
      if (publishOptions.audio) {
        const audioPub = new LocalTrackPublication(Track.Kind.Audio, new TrackInfo({
          source: TrackSource.MICROPHONE,
          sid: Math.floor(Math.random() * 1e4).toString(),
          type: TrackType.AUDIO
        }), new LocalAudioTrack(publishOptions.useRealTracks && ((_c = navigator.mediaDevices) === null || _c === void 0 ? void 0 : _c.getUserMedia) ? (yield navigator.mediaDevices.getUserMedia({
          audio: true
        })).getAudioTracks()[0] : getEmptyAudioStreamTrack(), void 0, false, this.audioContext, {
          loggerName: this.options.loggerName,
          loggerContextCb: () => this.logContext
        }), {
          loggerName: this.options.loggerName,
          loggerContextCb: () => this.logContext
        });
        this.localParticipant.addTrackPublication(audioPub);
        this.localParticipant.emit(ParticipantEvent.LocalTrackPublished, audioPub);
      }
      for (let i2 = 0; i2 < participantOptions.count - 1; i2 += 1) {
        let info = new ParticipantInfo({
          sid: Math.floor(Math.random() * 1e4).toString(),
          identity: "simulated-".concat(i2),
          state: ParticipantInfo_State.ACTIVE,
          tracks: [],
          joinedAt: protoInt64.parse(Date.now())
        });
        const p = this.getOrCreateParticipant(info.identity, info);
        if (participantOptions.video) {
          const dummyVideo = createDummyVideoStreamTrack(160 * ((_d = participantOptions.aspectRatios[i2 % participantOptions.aspectRatios.length]) !== null && _d !== void 0 ? _d : 1), 160, false, true);
          const videoTrack = new TrackInfo({
            source: TrackSource.CAMERA,
            sid: Math.floor(Math.random() * 1e4).toString(),
            type: TrackType.AUDIO
          });
          p.addSubscribedMediaTrack(dummyVideo, videoTrack.sid, new MediaStream([dummyVideo]), new RTCRtpReceiver());
          info.tracks = [...info.tracks, videoTrack];
        }
        if (participantOptions.audio) {
          const dummyTrack = getEmptyAudioStreamTrack();
          const audioTrack = new TrackInfo({
            source: TrackSource.MICROPHONE,
            sid: Math.floor(Math.random() * 1e4).toString(),
            type: TrackType.AUDIO
          });
          p.addSubscribedMediaTrack(dummyTrack, audioTrack.sid, new MediaStream([dummyTrack]), new RTCRtpReceiver());
          info.tracks = [...info.tracks, audioTrack];
        }
        p.updateInfo(info);
      }
    });
  }
  // /** @internal */
  emit(event) {
    for (var _len3 = arguments.length, args = new Array(_len3 > 1 ? _len3 - 1 : 0), _key3 = 1; _key3 < _len3; _key3++) {
      args[_key3 - 1] = arguments[_key3];
    }
    if (event !== RoomEvent.ActiveSpeakersChanged && event !== RoomEvent.TranscriptionReceived) {
      const minimizedArgs = mapArgs(args).filter((arg) => arg !== void 0);
      if (event === RoomEvent.TrackSubscribed || event === RoomEvent.TrackUnsubscribed) {
        this.log.trace("subscribe trace: ".concat(event), {
          event,
          args: minimizedArgs
        });
      }
      this.log.debug("room event ".concat(event), {
        event,
        args: minimizedArgs
      });
    }
    return super.emit(event, ...args);
  }
};
Room.cleanupRegistry = typeof FinalizationRegistry !== "undefined" && typeof WeakRef !== "undefined" && new FinalizationRegistry((cleanup) => {
  cleanup();
});
function mapArgs(args) {
  return args.map((arg) => {
    if (!arg) {
      return;
    }
    if (Array.isArray(arg)) {
      return mapArgs(arg);
    }
    if (typeof arg === "object") {
      return "logContext" in arg ? arg.logContext : void 0;
    }
    return arg;
  });
}
var Convert = class {
  static toAgentAttributes(json2) {
    return JSON.parse(json2);
  }
  static agentAttributesToJson(value) {
    return JSON.stringify(value);
  }
  static toTranscriptionAttributes(json2) {
    return JSON.parse(json2);
  }
  static transcriptionAttributesToJson(value) {
    return JSON.stringify(value);
  }
};
var attributeTypings = Object.freeze({ __proto__: null, Convert });
var CheckStatus;
(function(CheckStatus2) {
  CheckStatus2[CheckStatus2["IDLE"] = 0] = "IDLE";
  CheckStatus2[CheckStatus2["RUNNING"] = 1] = "RUNNING";
  CheckStatus2[CheckStatus2["SKIPPED"] = 2] = "SKIPPED";
  CheckStatus2[CheckStatus2["SUCCESS"] = 3] = "SUCCESS";
  CheckStatus2[CheckStatus2["FAILED"] = 4] = "FAILED";
})(CheckStatus || (CheckStatus = {}));
var Checker = class extends eventsExports.EventEmitter {
  constructor(url, token) {
    let options = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : {};
    super();
    this.status = CheckStatus.IDLE;
    this.logs = [];
    this.options = {};
    this.url = url;
    this.token = token;
    this.name = this.constructor.name;
    this.room = new Room(options.roomOptions);
    this.connectOptions = options.connectOptions;
    this.options = options;
  }
  run(onComplete) {
    return __awaiter(this, void 0, void 0, function* () {
      if (this.status !== CheckStatus.IDLE) {
        throw Error("check is running already");
      }
      this.setStatus(CheckStatus.RUNNING);
      try {
        yield this.perform();
      } catch (err) {
        if (err instanceof Error) {
          if (this.options.errorsAsWarnings) {
            this.appendWarning(err.message);
          } else {
            this.appendError(err.message);
          }
        }
      }
      yield this.disconnect();
      yield new Promise((resolve) => setTimeout(resolve, 500));
      if (this.status !== CheckStatus.SKIPPED) {
        this.setStatus(this.isSuccess() ? CheckStatus.SUCCESS : CheckStatus.FAILED);
      }
      if (onComplete) {
        onComplete();
      }
      return this.getInfo();
    });
  }
  isSuccess() {
    return !this.logs.some((l) => l.level === "error");
  }
  connect(url) {
    return __awaiter(this, void 0, void 0, function* () {
      if (this.room.state === ConnectionState.Connected) {
        return this.room;
      }
      if (!url) {
        url = this.url;
      }
      yield this.room.connect(url, this.token, this.connectOptions);
      return this.room;
    });
  }
  disconnect() {
    return __awaiter(this, void 0, void 0, function* () {
      if (this.room && this.room.state !== ConnectionState.Disconnected) {
        yield this.room.disconnect();
        yield new Promise((resolve) => setTimeout(resolve, 500));
      }
    });
  }
  skip() {
    this.setStatus(CheckStatus.SKIPPED);
  }
  switchProtocol(protocol) {
    return __awaiter(this, void 0, void 0, function* () {
      let hasReconnecting = false;
      let hasReconnected = false;
      this.room.on(RoomEvent.Reconnecting, () => {
        hasReconnecting = true;
      });
      this.room.once(RoomEvent.Reconnected, () => {
        hasReconnected = true;
      });
      this.room.simulateScenario("force-".concat(protocol));
      yield new Promise((resolve) => setTimeout(resolve, 1e3));
      if (!hasReconnecting) {
        return;
      }
      const timeout = Date.now() + 1e4;
      while (Date.now() < timeout) {
        if (hasReconnected) {
          return;
        }
        yield sleep(100);
      }
      throw new Error("Could not reconnect using ".concat(protocol, " protocol after 10 seconds"));
    });
  }
  appendMessage(message) {
    this.logs.push({
      level: "info",
      message
    });
    this.emit("update", this.getInfo());
  }
  appendWarning(message) {
    this.logs.push({
      level: "warning",
      message
    });
    this.emit("update", this.getInfo());
  }
  appendError(message) {
    this.logs.push({
      level: "error",
      message
    });
    this.emit("update", this.getInfo());
  }
  setStatus(status) {
    this.status = status;
    this.emit("update", this.getInfo());
  }
  get engine() {
    var _a2;
    return (_a2 = this.room) === null || _a2 === void 0 ? void 0 : _a2.engine;
  }
  getInfo() {
    return {
      logs: this.logs,
      name: this.name,
      status: this.status,
      description: this.description
    };
  }
};
var CloudRegionCheck = class extends Checker {
  get description() {
    return "Cloud regions";
  }
  perform() {
    return __awaiter(this, void 0, void 0, function* () {
      const regionProvider = new RegionUrlProvider(this.url, this.token);
      if (!regionProvider.isCloud()) {
        this.skip();
        return;
      }
      const regionStats = [];
      const seenUrls = /* @__PURE__ */ new Set();
      for (let i2 = 0; i2 < 3; i2++) {
        const regionUrl = yield regionProvider.getNextBestRegionUrl();
        if (!regionUrl) {
          break;
        }
        if (seenUrls.has(regionUrl)) {
          continue;
        }
        seenUrls.add(regionUrl);
        const stats = yield this.checkCloudRegion(regionUrl);
        this.appendMessage("".concat(stats.region, " RTT: ").concat(stats.rtt, "ms, duration: ").concat(stats.duration, "ms"));
        regionStats.push(stats);
      }
      regionStats.sort((a, b) => {
        return (a.duration - b.duration) * 0.5 + (a.rtt - b.rtt) * 0.5;
      });
      const bestRegion = regionStats[0];
      this.bestStats = bestRegion;
      this.appendMessage("best Cloud region: ".concat(bestRegion.region));
    });
  }
  getInfo() {
    const info = super.getInfo();
    info.data = this.bestStats;
    return info;
  }
  checkCloudRegion(url) {
    return __awaiter(this, void 0, void 0, function* () {
      var _a2, _b2;
      yield this.connect(url);
      if (this.options.protocol === "tcp") {
        yield this.switchProtocol("tcp");
      }
      const region = (_a2 = this.room.serverInfo) === null || _a2 === void 0 ? void 0 : _a2.region;
      if (!region) {
        throw new Error("Region not found");
      }
      const writer2 = yield this.room.localParticipant.streamText({
        topic: "test"
      });
      const chunkSize = 1e3;
      const totalSize = 1e6;
      const numChunks = totalSize / chunkSize;
      const chunkData = "A".repeat(chunkSize);
      const startTime = Date.now();
      for (let i2 = 0; i2 < numChunks; i2++) {
        yield writer2.write(chunkData);
      }
      yield writer2.close();
      const endTime = Date.now();
      const stats = yield (_b2 = this.room.engine.pcManager) === null || _b2 === void 0 ? void 0 : _b2.publisher.getStats();
      const regionStats = {
        region,
        rtt: 1e4,
        duration: endTime - startTime
      };
      stats === null || stats === void 0 ? void 0 : stats.forEach((stat) => {
        if (stat.type === "candidate-pair" && stat.nominated) {
          regionStats.rtt = stat.currentRoundTripTime * 1e3;
        }
      });
      yield this.disconnect();
      return regionStats;
    });
  }
};
var TEST_DURATION = 1e4;
var ConnectionProtocolCheck = class extends Checker {
  get description() {
    return "Connection via UDP vs TCP";
  }
  perform() {
    return __awaiter(this, void 0, void 0, function* () {
      const udpStats = yield this.checkConnectionProtocol("udp");
      const tcpStats = yield this.checkConnectionProtocol("tcp");
      this.bestStats = udpStats;
      if (udpStats.qualityLimitationDurations.bandwidth - tcpStats.qualityLimitationDurations.bandwidth > 0.5 || (udpStats.packetsLost - tcpStats.packetsLost) / udpStats.packetsSent > 0.01) {
        this.appendMessage("best connection quality via tcp");
        this.bestStats = tcpStats;
      } else {
        this.appendMessage("best connection quality via udp");
      }
      const stats = this.bestStats;
      this.appendMessage("upstream bitrate: ".concat((stats.bitrateTotal / stats.count / 1e3 / 1e3).toFixed(2), " mbps"));
      this.appendMessage("RTT: ".concat((stats.rttTotal / stats.count * 1e3).toFixed(2), " ms"));
      this.appendMessage("jitter: ".concat((stats.jitterTotal / stats.count * 1e3).toFixed(2), " ms"));
      if (stats.packetsLost > 0) {
        this.appendWarning("packets lost: ".concat((stats.packetsLost / stats.packetsSent * 100).toFixed(2), "%"));
      }
      if (stats.qualityLimitationDurations.bandwidth > 1) {
        this.appendWarning("bandwidth limited ".concat((stats.qualityLimitationDurations.bandwidth / (TEST_DURATION / 1e3) * 100).toFixed(2), "%"));
      }
      if (stats.qualityLimitationDurations.cpu > 0) {
        this.appendWarning("cpu limited ".concat((stats.qualityLimitationDurations.cpu / (TEST_DURATION / 1e3) * 100).toFixed(2), "%"));
      }
    });
  }
  getInfo() {
    const info = super.getInfo();
    info.data = this.bestStats;
    return info;
  }
  checkConnectionProtocol(protocol) {
    return __awaiter(this, void 0, void 0, function* () {
      yield this.connect();
      if (protocol === "tcp") {
        yield this.switchProtocol("tcp");
      } else {
        yield this.switchProtocol("udp");
      }
      const canvas = document.createElement("canvas");
      canvas.width = 1280;
      canvas.height = 720;
      const ctx = canvas.getContext("2d");
      if (!ctx) {
        throw new Error("Could not get canvas context");
      }
      let hue = 0;
      const animate = () => {
        hue = (hue + 1) % 360;
        ctx.fillStyle = "hsl(".concat(hue, ", 100%, 50%)");
        ctx.fillRect(0, 0, canvas.width, canvas.height);
        requestAnimationFrame(animate);
      };
      animate();
      const stream = canvas.captureStream(30);
      const videoTrack = stream.getVideoTracks()[0];
      const pub = yield this.room.localParticipant.publishTrack(videoTrack, {
        simulcast: false,
        degradationPreference: "maintain-resolution",
        videoEncoding: {
          maxBitrate: 2e6
        }
      });
      const track = pub.track;
      const protocolStats = {
        protocol,
        packetsLost: 0,
        packetsSent: 0,
        qualityLimitationDurations: {},
        rttTotal: 0,
        jitterTotal: 0,
        bitrateTotal: 0,
        count: 0
      };
      const interval = setInterval(() => __awaiter(this, void 0, void 0, function* () {
        const stats = yield track.getRTCStatsReport();
        stats === null || stats === void 0 ? void 0 : stats.forEach((stat) => {
          if (stat.type === "outbound-rtp") {
            protocolStats.packetsSent = stat.packetsSent;
            protocolStats.qualityLimitationDurations = stat.qualityLimitationDurations;
            protocolStats.bitrateTotal += stat.targetBitrate;
            protocolStats.count++;
          } else if (stat.type === "remote-inbound-rtp") {
            protocolStats.packetsLost = stat.packetsLost;
            protocolStats.rttTotal += stat.roundTripTime;
            protocolStats.jitterTotal += stat.jitter;
          }
        });
      }), 1e3);
      yield new Promise((resolve) => setTimeout(resolve, TEST_DURATION));
      clearInterval(interval);
      videoTrack.stop();
      canvas.remove();
      yield this.disconnect();
      return protocolStats;
    });
  }
};
var PublishAudioCheck = class extends Checker {
  get description() {
    return "Can publish audio";
  }
  perform() {
    return __awaiter(this, void 0, void 0, function* () {
      var _a2;
      const room = yield this.connect();
      const track = yield createLocalAudioTrack();
      const trackIsSilent = yield detectSilence(track, 1e3);
      if (trackIsSilent) {
        throw new Error("unable to detect audio from microphone");
      }
      this.appendMessage("detected audio from microphone");
      room.localParticipant.publishTrack(track);
      yield new Promise((resolve) => setTimeout(resolve, 3e3));
      const stats = yield (_a2 = track.sender) === null || _a2 === void 0 ? void 0 : _a2.getStats();
      if (!stats) {
        throw new Error("Could not get RTCStats");
      }
      let numPackets = 0;
      stats.forEach((stat) => {
        if (stat.type === "outbound-rtp" && (stat.kind === "audio" || !stat.kind && stat.mediaType === "audio")) {
          numPackets = stat.packetsSent;
        }
      });
      if (numPackets === 0) {
        throw new Error("Could not determine packets are sent");
      }
      this.appendMessage("published ".concat(numPackets, " audio packets"));
    });
  }
};
var PublishVideoCheck = class extends Checker {
  get description() {
    return "Can publish video";
  }
  perform() {
    return __awaiter(this, void 0, void 0, function* () {
      var _a2;
      const room = yield this.connect();
      const track = yield createLocalVideoTrack();
      yield this.checkForVideo(track.mediaStreamTrack);
      room.localParticipant.publishTrack(track);
      yield new Promise((resolve) => setTimeout(resolve, 5e3));
      const stats = yield (_a2 = track.sender) === null || _a2 === void 0 ? void 0 : _a2.getStats();
      if (!stats) {
        throw new Error("Could not get RTCStats");
      }
      let numPackets = 0;
      stats.forEach((stat) => {
        if (stat.type === "outbound-rtp" && (stat.kind === "video" || !stat.kind && stat.mediaType === "video")) {
          numPackets += stat.packetsSent;
        }
      });
      if (numPackets === 0) {
        throw new Error("Could not determine packets are sent");
      }
      this.appendMessage("published ".concat(numPackets, " video packets"));
    });
  }
  checkForVideo(track) {
    return __awaiter(this, void 0, void 0, function* () {
      const stream = new MediaStream();
      stream.addTrack(track.clone());
      const video = document.createElement("video");
      video.srcObject = stream;
      video.muted = true;
      video.autoplay = true;
      video.playsInline = true;
      video.setAttribute("playsinline", "true");
      document.body.appendChild(video);
      yield new Promise((resolve) => {
        video.onplay = () => {
          setTimeout(() => {
            var _a2, _b2, _c, _d;
            const canvas = document.createElement("canvas");
            const settings = track.getSettings();
            const width = (_b2 = (_a2 = settings.width) !== null && _a2 !== void 0 ? _a2 : video.videoWidth) !== null && _b2 !== void 0 ? _b2 : 1280;
            const height = (_d = (_c = settings.height) !== null && _c !== void 0 ? _c : video.videoHeight) !== null && _d !== void 0 ? _d : 720;
            canvas.width = width;
            canvas.height = height;
            const ctx = canvas.getContext("2d");
            ctx.drawImage(video, 0, 0);
            const imageData = ctx.getImageData(0, 0, canvas.width, canvas.height);
            const data = imageData.data;
            let isAllBlack = true;
            for (let i2 = 0; i2 < data.length; i2 += 4) {
              if (data[i2] !== 0 || data[i2 + 1] !== 0 || data[i2 + 2] !== 0) {
                isAllBlack = false;
                break;
              }
            }
            if (isAllBlack) {
              this.appendError("camera appears to be producing only black frames");
            } else {
              this.appendMessage("received video frames");
            }
            resolve();
          }, 1e3);
        };
        video.play();
      });
      stream.getTracks().forEach((t) => t.stop());
      video.remove();
    });
  }
};
var ReconnectCheck = class extends Checker {
  get description() {
    return "Resuming connection after interruption";
  }
  perform() {
    return __awaiter(this, void 0, void 0, function* () {
      var _a2;
      const room = yield this.connect();
      let reconnectingTriggered = false;
      let reconnected = false;
      let reconnectResolver;
      const reconnectTimeout = new Promise((resolve) => {
        setTimeout(resolve, 5e3);
        reconnectResolver = resolve;
      });
      const handleReconnecting = () => {
        reconnectingTriggered = true;
      };
      room.on(RoomEvent.SignalReconnecting, handleReconnecting).on(RoomEvent.Reconnecting, handleReconnecting).on(RoomEvent.Reconnected, () => {
        reconnected = true;
        reconnectResolver(true);
      });
      (_a2 = room.engine.client.ws) === null || _a2 === void 0 ? void 0 : _a2.close();
      const onClose = room.engine.client.onClose;
      if (onClose) {
        onClose("");
      }
      yield reconnectTimeout;
      if (!reconnectingTriggered) {
        throw new Error("Did not attempt to reconnect");
      } else if (!reconnected || room.state !== ConnectionState.Connected) {
        this.appendWarning("reconnection is only possible in Redis-based configurations");
        throw new Error("Not able to reconnect");
      }
    });
  }
};
var TURNCheck = class extends Checker {
  get description() {
    return "Can connect via TURN";
  }
  perform() {
    return __awaiter(this, void 0, void 0, function* () {
      var _a2, _b2, _c;
      if (isCloud(new URL(this.url))) {
        this.appendMessage("Using region specific url");
        this.url = (_a2 = yield new RegionUrlProvider(this.url, this.token).getNextBestRegionUrl()) !== null && _a2 !== void 0 ? _a2 : this.url;
      }
      const signalClient = new SignalClient();
      const joinRes = yield signalClient.join(this.url, this.token, {
        autoSubscribe: true,
        maxRetries: 0,
        e2eeEnabled: false,
        websocketTimeout: 15e3
      }, void 0, true);
      let hasTLS = false;
      let hasTURN = false;
      let hasSTUN = false;
      for (let iceServer of joinRes.iceServers) {
        for (let url of iceServer.urls) {
          if (url.startsWith("turn:")) {
            hasTURN = true;
            hasSTUN = true;
          } else if (url.startsWith("turns:")) {
            hasTURN = true;
            hasSTUN = true;
            hasTLS = true;
          }
          if (url.startsWith("stun:")) {
            hasSTUN = true;
          }
        }
      }
      if (!hasSTUN) {
        this.appendWarning("No STUN servers configured on server side.");
      } else if (hasTURN && !hasTLS) {
        this.appendWarning("TURN is configured server side, but TURN/TLS is unavailable.");
      }
      yield signalClient.close();
      if (((_c = (_b2 = this.connectOptions) === null || _b2 === void 0 ? void 0 : _b2.rtcConfig) === null || _c === void 0 ? void 0 : _c.iceServers) || hasTURN) {
        yield this.room.connect(this.url, this.token, {
          rtcConfig: {
            iceTransportPolicy: "relay"
          }
        });
      } else {
        this.appendWarning("No TURN servers configured.");
        this.skip();
        yield new Promise((resolve) => setTimeout(resolve, 0));
      }
    });
  }
};
var WebRTCCheck = class extends Checker {
  get description() {
    return "Establishing WebRTC connection";
  }
  perform() {
    return __awaiter(this, void 0, void 0, function* () {
      let hasTcp = false;
      let hasIpv4Udp = false;
      this.room.on(RoomEvent.SignalConnected, () => {
        var _a2;
        const prevTrickle = this.room.engine.client.onTrickle;
        this.room.engine.client.onTrickle = (sd, target) => {
          if (sd.candidate) {
            const candidate = new RTCIceCandidate(sd);
            let str = "".concat(candidate.protocol, " ").concat(candidate.address, ":").concat(candidate.port, " ").concat(candidate.type);
            if (candidate.address) {
              if (isIPPrivate(candidate.address)) {
                str += " (private)";
              } else {
                if (candidate.protocol === "tcp" && candidate.tcpType === "passive") {
                  hasTcp = true;
                  str += " (passive)";
                } else if (candidate.protocol === "udp") {
                  hasIpv4Udp = true;
                }
              }
            }
            this.appendMessage(str);
          }
          if (prevTrickle) {
            prevTrickle(sd, target);
          }
        };
        if ((_a2 = this.room.engine.pcManager) === null || _a2 === void 0 ? void 0 : _a2.subscriber) {
          this.room.engine.pcManager.subscriber.onIceCandidateError = (ev) => {
            if (ev instanceof RTCPeerConnectionIceErrorEvent) {
              this.appendWarning("error with ICE candidate: ".concat(ev.errorCode, " ").concat(ev.errorText, " ").concat(ev.url));
            }
          };
        }
      });
      try {
        yield this.connect();
        livekitLogger.info("now the room is connected");
      } catch (err) {
        this.appendWarning("ports need to be open on firewall in order to connect.");
        throw err;
      }
      if (!hasTcp) {
        this.appendWarning("Server is not configured for ICE/TCP");
      }
      if (!hasIpv4Udp) {
        this.appendWarning("No public IPv4 UDP candidates were found. Your server is likely not configured correctly");
      }
    });
  }
};
function isIPPrivate(address) {
  const parts = address.split(".");
  if (parts.length === 4) {
    if (parts[0] === "10") {
      return true;
    } else if (parts[0] === "192" && parts[1] === "168") {
      return true;
    } else if (parts[0] === "172") {
      const second = parseInt(parts[1], 10);
      if (second >= 16 && second <= 31) {
        return true;
      }
    }
  }
  return false;
}
var WebSocketCheck = class extends Checker {
  get description() {
    return "Connecting to signal connection via WebSocket";
  }
  perform() {
    return __awaiter(this, void 0, void 0, function* () {
      var _a2, _b2, _c;
      if (this.url.startsWith("ws:") || this.url.startsWith("http:")) {
        this.appendWarning("Server is insecure, clients may block connections to it");
      }
      let signalClient = new SignalClient();
      let joinRes;
      try {
        joinRes = yield signalClient.join(this.url, this.token, {
          autoSubscribe: true,
          maxRetries: 0,
          e2eeEnabled: false,
          websocketTimeout: 15e3
        }, void 0, true);
      } catch (e4) {
        if (isCloud(new URL(this.url))) {
          this.appendMessage("Initial connection failed with error ".concat(e4.message, ". Retrying with region fallback"));
          const regionProvider = new RegionUrlProvider(this.url, this.token);
          const regionUrl = yield regionProvider.getNextBestRegionUrl();
          if (regionUrl) {
            joinRes = yield signalClient.join(regionUrl, this.token, {
              autoSubscribe: true,
              maxRetries: 0,
              e2eeEnabled: false,
              websocketTimeout: 15e3
            }, void 0, true);
            this.appendMessage("Fallback to region worked. To avoid initial connections failing, ensure you're calling room.prepareConnection() ahead of time");
          }
        }
      }
      if (joinRes) {
        this.appendMessage("Connected to server, version ".concat(joinRes.serverVersion, "."));
        if (((_a2 = joinRes.serverInfo) === null || _a2 === void 0 ? void 0 : _a2.edition) === ServerInfo_Edition.Cloud && ((_b2 = joinRes.serverInfo) === null || _b2 === void 0 ? void 0 : _b2.region)) {
          this.appendMessage("LiveKit Cloud: ".concat((_c = joinRes.serverInfo) === null || _c === void 0 ? void 0 : _c.region));
        }
      } else {
        this.appendError("Websocket connection could not be established");
      }
      yield signalClient.close();
    });
  }
};
var ConnectionCheck = class extends eventsExports.EventEmitter {
  constructor(url, token) {
    let options = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : {};
    super();
    this.options = {};
    this.checkResults = /* @__PURE__ */ new Map();
    this.url = url;
    this.token = token;
    this.options = options;
  }
  getNextCheckId() {
    const nextId = this.checkResults.size;
    this.checkResults.set(nextId, {
      logs: [],
      status: CheckStatus.IDLE,
      name: "",
      description: ""
    });
    return nextId;
  }
  updateCheck(checkId, info) {
    this.checkResults.set(checkId, info);
    this.emit("checkUpdate", checkId, info);
  }
  isSuccess() {
    return Array.from(this.checkResults.values()).every((r2) => r2.status !== CheckStatus.FAILED);
  }
  getResults() {
    return Array.from(this.checkResults.values());
  }
  createAndRunCheck(check) {
    return __awaiter(this, void 0, void 0, function* () {
      const checkId = this.getNextCheckId();
      const test = new check(this.url, this.token, this.options);
      const handleUpdate = (info) => {
        this.updateCheck(checkId, info);
      };
      test.on("update", handleUpdate);
      const result = yield test.run();
      test.off("update", handleUpdate);
      return result;
    });
  }
  checkWebsocket() {
    return __awaiter(this, void 0, void 0, function* () {
      return this.createAndRunCheck(WebSocketCheck);
    });
  }
  checkWebRTC() {
    return __awaiter(this, void 0, void 0, function* () {
      return this.createAndRunCheck(WebRTCCheck);
    });
  }
  checkTURN() {
    return __awaiter(this, void 0, void 0, function* () {
      return this.createAndRunCheck(TURNCheck);
    });
  }
  checkReconnect() {
    return __awaiter(this, void 0, void 0, function* () {
      return this.createAndRunCheck(ReconnectCheck);
    });
  }
  checkPublishAudio() {
    return __awaiter(this, void 0, void 0, function* () {
      return this.createAndRunCheck(PublishAudioCheck);
    });
  }
  checkPublishVideo() {
    return __awaiter(this, void 0, void 0, function* () {
      return this.createAndRunCheck(PublishVideoCheck);
    });
  }
  checkConnectionProtocol() {
    return __awaiter(this, void 0, void 0, function* () {
      const info = yield this.createAndRunCheck(ConnectionProtocolCheck);
      if (info.data && "protocol" in info.data) {
        const stats = info.data;
        this.options.protocol = stats.protocol;
      }
      return info;
    });
  }
  checkCloudRegion() {
    return __awaiter(this, void 0, void 0, function* () {
      return this.createAndRunCheck(CloudRegionCheck);
    });
  }
};
var TokenSourceConfigurable = class {
};
new TextEncoder();
var decoder = new TextDecoder();
function decodeBase64(encoded) {
  if (Uint8Array.fromBase64) {
    return Uint8Array.fromBase64(encoded);
  }
  const binary = atob(encoded);
  const bytes = new Uint8Array(binary.length);
  for (let i2 = 0; i2 < binary.length; i2++) {
    bytes[i2] = binary.charCodeAt(i2);
  }
  return bytes;
}
function decode(input) {
  if (Uint8Array.fromBase64) {
    return Uint8Array.fromBase64(typeof input === "string" ? input : decoder.decode(input), {
      alphabet: "base64url"
    });
  }
  let encoded = input;
  if (encoded instanceof Uint8Array) {
    encoded = decoder.decode(encoded);
  }
  encoded = encoded.replace(/-/g, "+").replace(/_/g, "/");
  try {
    return decodeBase64(encoded);
  } catch (_unused) {
    throw new TypeError("The input to be decoded is not correctly encoded.");
  }
}
var JOSEError = class extends Error {
  constructor(message, options) {
    var _Error$captureStackTr;
    super(message, options);
    _defineProperty(this, "code", "ERR_JOSE_GENERIC");
    this.name = this.constructor.name;
    (_Error$captureStackTr = Error.captureStackTrace) === null || _Error$captureStackTr === void 0 || _Error$captureStackTr.call(Error, this, this.constructor);
  }
};
_defineProperty(JOSEError, "code", "ERR_JOSE_GENERIC");
var JWTClaimValidationFailed = class extends JOSEError {
  constructor(message, payload) {
    let claim = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : "unspecified";
    let reason = arguments.length > 3 && arguments[3] !== void 0 ? arguments[3] : "unspecified";
    super(message, {
      cause: {
        claim,
        reason,
        payload
      }
    });
    _defineProperty(this, "code", "ERR_JWT_CLAIM_VALIDATION_FAILED");
    _defineProperty(this, "claim", void 0);
    _defineProperty(this, "reason", void 0);
    _defineProperty(this, "payload", void 0);
    this.claim = claim;
    this.reason = reason;
    this.payload = payload;
  }
};
_defineProperty(JWTClaimValidationFailed, "code", "ERR_JWT_CLAIM_VALIDATION_FAILED");
var JWTExpired = class extends JOSEError {
  constructor(message, payload) {
    let claim = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : "unspecified";
    let reason = arguments.length > 3 && arguments[3] !== void 0 ? arguments[3] : "unspecified";
    super(message, {
      cause: {
        claim,
        reason,
        payload
      }
    });
    _defineProperty(this, "code", "ERR_JWT_EXPIRED");
    _defineProperty(this, "claim", void 0);
    _defineProperty(this, "reason", void 0);
    _defineProperty(this, "payload", void 0);
    this.claim = claim;
    this.reason = reason;
    this.payload = payload;
  }
};
_defineProperty(JWTExpired, "code", "ERR_JWT_EXPIRED");
var JOSEAlgNotAllowed = class extends JOSEError {
  constructor() {
    super(...arguments);
    _defineProperty(this, "code", "ERR_JOSE_ALG_NOT_ALLOWED");
  }
};
_defineProperty(JOSEAlgNotAllowed, "code", "ERR_JOSE_ALG_NOT_ALLOWED");
var JOSENotSupported = class extends JOSEError {
  constructor() {
    super(...arguments);
    _defineProperty(this, "code", "ERR_JOSE_NOT_SUPPORTED");
  }
};
_defineProperty(JOSENotSupported, "code", "ERR_JOSE_NOT_SUPPORTED");
var JWEDecryptionFailed = class extends JOSEError {
  constructor() {
    let message = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : "decryption operation failed";
    let options = arguments.length > 1 ? arguments[1] : void 0;
    super(message, options);
    _defineProperty(this, "code", "ERR_JWE_DECRYPTION_FAILED");
  }
};
_defineProperty(JWEDecryptionFailed, "code", "ERR_JWE_DECRYPTION_FAILED");
var JWEInvalid = class extends JOSEError {
  constructor() {
    super(...arguments);
    _defineProperty(this, "code", "ERR_JWE_INVALID");
  }
};
_defineProperty(JWEInvalid, "code", "ERR_JWE_INVALID");
var JWSInvalid = class extends JOSEError {
  constructor() {
    super(...arguments);
    _defineProperty(this, "code", "ERR_JWS_INVALID");
  }
};
_defineProperty(JWSInvalid, "code", "ERR_JWS_INVALID");
var JWTInvalid = class extends JOSEError {
  constructor() {
    super(...arguments);
    _defineProperty(this, "code", "ERR_JWT_INVALID");
  }
};
_defineProperty(JWTInvalid, "code", "ERR_JWT_INVALID");
var JWKInvalid = class extends JOSEError {
  constructor() {
    super(...arguments);
    _defineProperty(this, "code", "ERR_JWK_INVALID");
  }
};
_defineProperty(JWKInvalid, "code", "ERR_JWK_INVALID");
var JWKSInvalid = class extends JOSEError {
  constructor() {
    super(...arguments);
    _defineProperty(this, "code", "ERR_JWKS_INVALID");
  }
};
_defineProperty(JWKSInvalid, "code", "ERR_JWKS_INVALID");
var JWKSNoMatchingKey = class extends JOSEError {
  constructor() {
    let message = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : "no applicable key found in the JSON Web Key Set";
    let options = arguments.length > 1 ? arguments[1] : void 0;
    super(message, options);
    _defineProperty(this, "code", "ERR_JWKS_NO_MATCHING_KEY");
  }
};
_defineProperty(JWKSNoMatchingKey, "code", "ERR_JWKS_NO_MATCHING_KEY");
var JWKSMultipleMatchingKeys = class extends JOSEError {
  constructor() {
    let message = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : "multiple matching keys found in the JSON Web Key Set";
    let options = arguments.length > 1 ? arguments[1] : void 0;
    super(message, options);
    _defineProperty(this, Symbol.asyncIterator, void 0);
    _defineProperty(this, "code", "ERR_JWKS_MULTIPLE_MATCHING_KEYS");
  }
};
_defineProperty(JWKSMultipleMatchingKeys, "code", "ERR_JWKS_MULTIPLE_MATCHING_KEYS");
var JWKSTimeout = class extends JOSEError {
  constructor() {
    let message = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : "request timed out";
    let options = arguments.length > 1 ? arguments[1] : void 0;
    super(message, options);
    _defineProperty(this, "code", "ERR_JWKS_TIMEOUT");
  }
};
_defineProperty(JWKSTimeout, "code", "ERR_JWKS_TIMEOUT");
var JWSSignatureVerificationFailed = class extends JOSEError {
  constructor() {
    let message = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : "signature verification failed";
    let options = arguments.length > 1 ? arguments[1] : void 0;
    super(message, options);
    _defineProperty(this, "code", "ERR_JWS_SIGNATURE_VERIFICATION_FAILED");
  }
};
_defineProperty(JWSSignatureVerificationFailed, "code", "ERR_JWS_SIGNATURE_VERIFICATION_FAILED");
var isObjectLike = (value) => typeof value === "object" && value !== null;
function isObject(input) {
  if (!isObjectLike(input) || Object.prototype.toString.call(input) !== "[object Object]") {
    return false;
  }
  if (Object.getPrototypeOf(input) === null) {
    return true;
  }
  let proto = input;
  while (Object.getPrototypeOf(proto) !== null) {
    proto = Object.getPrototypeOf(proto);
  }
  return Object.getPrototypeOf(input) === proto;
}
function decodeJwt(jwt) {
  if (typeof jwt !== "string") throw new JWTInvalid("JWTs must use Compact JWS serialization, JWT must be a string");
  const _jwt$split = jwt.split("."), payload = _jwt$split[1], length = _jwt$split.length;
  if (length === 5) throw new JWTInvalid("Only JWTs using Compact JWS serialization can be decoded");
  if (length !== 3) throw new JWTInvalid("Invalid JWT");
  if (!payload) throw new JWTInvalid("JWTs must contain a payload");
  let decoded;
  try {
    decoded = decode(payload);
  } catch (_unused) {
    throw new JWTInvalid("Failed to base64url decode the payload");
  }
  let result;
  try {
    result = JSON.parse(decoder.decode(decoded));
  } catch (_unused2) {
    throw new JWTInvalid("Failed to parse the decoded payload as JSON");
  }
  if (!isObject(result)) throw new JWTInvalid("Invalid JWT Claims Set");
  return result;
}
var ONE_SECOND_IN_MILLISECONDS = 1e3;
var ONE_MINUTE_IN_MILLISECONDS = 60 * ONE_SECOND_IN_MILLISECONDS;
function decodeTokenPayload(token) {
  const payload = decodeJwt(token);
  payload.roomConfig;
  const rest = __rest(payload, ["roomConfig"]);
  const mappedPayload = Object.assign(Object.assign({}, rest), {
    roomConfig: payload.roomConfig ? RoomConfiguration.fromJson(payload.roomConfig, {
      ignoreUnknownFields: true
    }) : void 0
  });
  return mappedPayload;
}
function facingModeFromLocalTrack(localTrack) {
  let options = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : {};
  var _a2;
  const track = isLocalTrack(localTrack) ? localTrack.mediaStreamTrack : localTrack;
  const trackSettings = track.getSettings();
  let result = {
    facingMode: (_a2 = options.defaultFacingMode) !== null && _a2 !== void 0 ? _a2 : "user",
    confidence: "low"
  };
  if ("facingMode" in trackSettings) {
    const rawFacingMode = trackSettings.facingMode;
    livekitLogger.trace("rawFacingMode", {
      rawFacingMode
    });
    if (rawFacingMode && typeof rawFacingMode === "string" && isFacingModeValue(rawFacingMode)) {
      result = {
        facingMode: rawFacingMode,
        confidence: "high"
      };
    }
  }
  if (["low", "medium"].includes(result.confidence)) {
    livekitLogger.trace("Try to get facing mode from device label: (".concat(track.label, ")"));
    const labelAnalysisResult = facingModeFromDeviceLabel(track.label);
    if (labelAnalysisResult !== void 0) {
      result = labelAnalysisResult;
    }
  }
  return result;
}
var knownDeviceLabels = /* @__PURE__ */ new Map([["obs virtual camera", {
  facingMode: "environment",
  confidence: "medium"
}]]);
var knownDeviceLabelSections = /* @__PURE__ */ new Map([["iphone", {
  facingMode: "environment",
  confidence: "medium"
}], ["ipad", {
  facingMode: "environment",
  confidence: "medium"
}]]);
function facingModeFromDeviceLabel(deviceLabel) {
  var _a2;
  const label = deviceLabel.trim().toLowerCase();
  if (label === "") {
    return void 0;
  }
  if (knownDeviceLabels.has(label)) {
    return knownDeviceLabels.get(label);
  }
  return (_a2 = Array.from(knownDeviceLabelSections.entries()).find((_ref) => {
    let _ref2 = _slicedToArray(_ref, 1), section = _ref2[0];
    return label.includes(section);
  })) === null || _a2 === void 0 ? void 0 : _a2[1];
}
function isFacingModeValue(item) {
  const allowedValues = ["user", "environment", "left", "right"];
  return item === void 0 || allowedValues.includes(item);
}
var SerializerSymbol = Symbol.for("lk.serializer");
function isSerializer(v) {
  return typeof v === "object" && v !== null && "symbol" in v && v.symbol === SerializerSymbol;
}
function base(params) {
  return Object.assign(Object.assign({}, params), {
    symbol: SerializerSymbol
  });
}
function json() {
  return base({
    parse: (rawString) => JSON.parse(rawString),
    serialize: (val) => JSON.stringify(val)
  });
}
function raw() {
  return base({
    parse: (rawString) => rawString,
    serialize: (val) => val
  });
}
function custom(params) {
  return base(params);
}
var serializers = {
  json,
  raw,
  custom
};

// node_modules/@livekit/components-react/dist/contexts-BtSGUJjc.mjs
var D = __toESM(require_react(), 1);
var We = Math.min;
var de = Math.max;
var Be = Math.round;
var Re = Math.floor;
var te = (e4) => ({
  x: e4,
  y: e4
});
var er = {
  left: "right",
  right: "left",
  bottom: "top",
  top: "bottom"
};
function At(e4, t, n) {
  return de(e4, We(t, n));
}
function Ge(e4, t) {
  return typeof e4 == "function" ? e4(t) : e4;
}
function pe(e4) {
  return e4.split("-")[0];
}
function Qe(e4) {
  return e4.split("-")[1];
}
function en(e4) {
  return e4 === "x" ? "y" : "x";
}
function tn(e4) {
  return e4 === "y" ? "height" : "width";
}
function ae(e4) {
  const t = e4[0];
  return t === "t" || t === "b" ? "y" : "x";
}
function nn(e4) {
  return en(ae(e4));
}
function tr(e4, t, n) {
  n === void 0 && (n = false);
  const r2 = Qe(e4), i2 = nn(e4), o2 = tn(i2);
  let s = i2 === "x" ? r2 === (n ? "end" : "start") ? "right" : "left" : r2 === "start" ? "bottom" : "top";
  return t.reference[o2] > t.floating[o2] && (s = je(s)), [s, je(s)];
}
function nr(e4) {
  const t = je(e4);
  return [ft(e4), t, ft(t)];
}
function ft(e4) {
  return e4.includes("start") ? e4.replace("start", "end") : e4.replace("end", "start");
}
var _t = ["left", "right"];
var kt = ["right", "left"];
var rr = ["top", "bottom"];
var ir = ["bottom", "top"];
function or(e4, t, n) {
  switch (e4) {
    case "top":
    case "bottom":
      return n ? t ? kt : _t : t ? _t : kt;
    case "left":
    case "right":
      return t ? rr : ir;
    default:
      return [];
  }
}
function sr(e4, t, n, r2) {
  const i2 = Qe(e4);
  let o2 = or(pe(e4), n === "start", r2);
  return i2 && (o2 = o2.map((s) => s + "-" + i2), t && (o2 = o2.concat(o2.map(ft)))), o2;
}
function je(e4) {
  const t = pe(e4);
  return er[t] + e4.slice(t.length);
}
function ar(e4) {
  return {
    top: 0,
    right: 0,
    bottom: 0,
    left: 0,
    ...e4
  };
}
function cr(e4) {
  return typeof e4 != "number" ? ar(e4) : {
    top: e4,
    right: e4,
    bottom: e4,
    left: e4
  };
}
function Ve(e4) {
  const {
    x: t,
    y: n,
    width: r2,
    height: i2
  } = e4;
  return {
    width: r2,
    height: i2,
    top: n,
    left: t,
    right: t + r2,
    bottom: n + i2,
    x: t,
    y: n
  };
}
function Lt(e4, t, n) {
  let {
    reference: r2,
    floating: i2
  } = e4;
  const o2 = ae(t), s = nn(t), a = tn(s), c = pe(t), u = o2 === "y", l = r2.x + r2.width / 2 - i2.width / 2, f = r2.y + r2.height / 2 - i2.height / 2, h2 = r2[a] / 2 - i2[a] / 2;
  let d;
  switch (c) {
    case "top":
      d = {
        x: l,
        y: r2.y - i2.height
      };
      break;
    case "bottom":
      d = {
        x: l,
        y: r2.y + r2.height
      };
      break;
    case "right":
      d = {
        x: r2.x + r2.width,
        y: f
      };
      break;
    case "left":
      d = {
        x: r2.x - i2.width,
        y: f
      };
      break;
    default:
      d = {
        x: r2.x,
        y: r2.y
      };
  }
  switch (Qe(t)) {
    case "start":
      d[s] -= h2 * (n && u ? -1 : 1);
      break;
    case "end":
      d[s] += h2 * (n && u ? -1 : 1);
      break;
  }
  return d;
}
async function ur(e4, t) {
  var n;
  t === void 0 && (t = {});
  const {
    x: r2,
    y: i2,
    platform: o2,
    rects: s,
    elements: a,
    strategy: c
  } = e4, {
    boundary: u = "clippingAncestors",
    rootBoundary: l = "viewport",
    elementContext: f = "floating",
    altBoundary: h2 = false,
    padding: d = 0
  } = Ge(t, e4), m = cr(d), g = a[h2 ? f === "floating" ? "reference" : "floating" : f], v = Ve(await o2.getClippingRect({
    element: (n = await (o2.isElement == null ? void 0 : o2.isElement(g))) == null || n ? g : g.contextElement || await (o2.getDocumentElement == null ? void 0 : o2.getDocumentElement(a.floating)),
    boundary: u,
    rootBoundary: l,
    strategy: c
  })), x = f === "floating" ? {
    x: r2,
    y: i2,
    width: s.floating.width,
    height: s.floating.height
  } : s.reference, C = await (o2.getOffsetParent == null ? void 0 : o2.getOffsetParent(a.floating)), T2 = await (o2.isElement == null ? void 0 : o2.isElement(C)) ? await (o2.getScale == null ? void 0 : o2.getScale(C)) || {
    x: 1,
    y: 1
  } : {
    x: 1,
    y: 1
  }, S = Ve(o2.convertOffsetParentRelativeRectToViewportRelativeRect ? await o2.convertOffsetParentRelativeRectToViewportRelativeRect({
    elements: a,
    rect: x,
    offsetParent: C,
    strategy: c
  }) : x);
  return {
    top: (v.top - S.top + m.top) / T2.y,
    bottom: (S.bottom - v.bottom + m.bottom) / T2.y,
    left: (v.left - S.left + m.left) / T2.x,
    right: (S.right - v.right + m.right) / T2.x
  };
}
var lr = 50;
var fr = async (e4, t, n) => {
  const {
    placement: r2 = "bottom",
    strategy: i2 = "absolute",
    middleware: o2 = [],
    platform: s
  } = n, a = s.detectOverflow ? s : {
    ...s,
    detectOverflow: ur
  }, c = await (s.isRTL == null ? void 0 : s.isRTL(t));
  let u = await s.getElementRects({
    reference: e4,
    floating: t,
    strategy: i2
  }), {
    x: l,
    y: f
  } = Lt(u, r2, c), h2 = r2, d = 0;
  const m = {};
  for (let p = 0; p < o2.length; p++) {
    const g = o2[p];
    if (!g)
      continue;
    const {
      name: v,
      fn: x
    } = g, {
      x: C,
      y: T2,
      data: S,
      reset: b
    } = await x({
      x: l,
      y: f,
      initialPlacement: r2,
      placement: h2,
      strategy: i2,
      middlewareData: m,
      rects: u,
      platform: a,
      elements: {
        reference: e4,
        floating: t
      }
    });
    l = C ?? l, f = T2 ?? f, m[v] = {
      ...m[v],
      ...S
    }, b && d < lr && (d++, typeof b == "object" && (b.placement && (h2 = b.placement), b.rects && (u = b.rects === true ? await s.getElementRects({
      reference: e4,
      floating: t,
      strategy: i2
    }) : b.rects), {
      x: l,
      y: f
    } = Lt(u, h2, c)), p = -1);
  }
  return {
    x: l,
    y: f,
    placement: h2,
    strategy: i2,
    middlewareData: m
  };
};
var dr = function(e4) {
  return e4 === void 0 && (e4 = {}), {
    name: "flip",
    options: e4,
    async fn(t) {
      var n, r2;
      const {
        placement: i2,
        middlewareData: o2,
        rects: s,
        initialPlacement: a,
        platform: c,
        elements: u
      } = t, {
        mainAxis: l = true,
        crossAxis: f = true,
        fallbackPlacements: h2,
        fallbackStrategy: d = "bestFit",
        fallbackAxisSideDirection: m = "none",
        flipAlignment: p = true,
        ...g
      } = Ge(e4, t);
      if ((n = o2.arrow) != null && n.alignmentOffset)
        return {};
      const v = pe(i2), x = ae(a), C = pe(a) === a, T2 = await (c.isRTL == null ? void 0 : c.isRTL(u.floating)), S = h2 || (C || !p ? [je(a)] : nr(a)), b = m !== "none";
      !h2 && b && S.push(...sr(a, p, m, T2));
      const P = [a, ...S], R = await c.detectOverflow(t, g), M2 = [];
      let W3 = ((r2 = o2.flip) == null ? void 0 : r2.overflows) || [];
      if (l && M2.push(R[v]), f) {
        const k = tr(i2, s, T2);
        M2.push(R[k[0]], R[k[1]]);
      }
      if (W3 = [...W3, {
        placement: i2,
        overflows: M2
      }], !M2.every((k) => k <= 0)) {
        var E, O;
        const k = (((E = o2.flip) == null ? void 0 : E.index) || 0) + 1, $2 = P[k];
        if ($2 && (!(f === "alignment" ? x !== ae($2) : false) || // We leave the current main axis only if every placement on that axis
        // overflows the main axis.
        W3.every((N) => ae(N.placement) === x ? N.overflows[0] > 0 : true)))
          return {
            data: {
              index: k,
              overflows: W3
            },
            reset: {
              placement: $2
            }
          };
        let G2 = (O = W3.filter((X) => X.overflows[0] <= 0).sort((X, N) => X.overflows[1] - N.overflows[1])[0]) == null ? void 0 : O.placement;
        if (!G2)
          switch (d) {
            case "bestFit": {
              var j2;
              const X = (j2 = W3.filter((N) => {
                if (b) {
                  const se3 = ae(N.placement);
                  return se3 === x || // Create a bias to the `y` side axis due to horizontal
                  // reading directions favoring greater width.
                  se3 === "y";
                }
                return true;
              }).map((N) => [N.placement, N.overflows.filter((se3) => se3 > 0).reduce((se3, Gn2) => se3 + Gn2, 0)]).sort((N, se3) => N[1] - se3[1])[0]) == null ? void 0 : j2[0];
              X && (G2 = X);
              break;
            }
            case "initialPlacement":
              G2 = a;
              break;
          }
        if (i2 !== G2)
          return {
            reset: {
              placement: G2
            }
          };
      }
      return {};
    }
  };
};
var pr = /* @__PURE__ */ new Set(["left", "top"]);
async function hr(e4, t) {
  const {
    placement: n,
    platform: r2,
    elements: i2
  } = e4, o2 = await (r2.isRTL == null ? void 0 : r2.isRTL(i2.floating)), s = pe(n), a = Qe(n), c = ae(n) === "y", u = pr.has(s) ? -1 : 1, l = o2 && c ? -1 : 1, f = Ge(t, e4);
  let {
    mainAxis: h2,
    crossAxis: d,
    alignmentAxis: m
  } = typeof f == "number" ? {
    mainAxis: f,
    crossAxis: 0,
    alignmentAxis: null
  } : {
    mainAxis: f.mainAxis || 0,
    crossAxis: f.crossAxis || 0,
    alignmentAxis: f.alignmentAxis
  };
  return a && typeof m == "number" && (d = a === "end" ? m * -1 : m), c ? {
    x: d * l,
    y: h2 * u
  } : {
    x: h2 * u,
    y: d * l
  };
}
var vr = function(e4) {
  return e4 === void 0 && (e4 = 0), {
    name: "offset",
    options: e4,
    async fn(t) {
      var n, r2;
      const {
        x: i2,
        y: o2,
        placement: s,
        middlewareData: a
      } = t, c = await hr(t, e4);
      return s === ((n = a.offset) == null ? void 0 : n.placement) && (r2 = a.arrow) != null && r2.alignmentOffset ? {} : {
        x: i2 + c.x,
        y: o2 + c.y,
        data: {
          ...c,
          placement: s
        }
      };
    }
  };
};
var mr = function(e4) {
  return e4 === void 0 && (e4 = {}), {
    name: "shift",
    options: e4,
    async fn(t) {
      const {
        x: n,
        y: r2,
        placement: i2,
        platform: o2
      } = t, {
        mainAxis: s = true,
        crossAxis: a = false,
        limiter: c = {
          fn: (v) => {
            let {
              x,
              y: C
            } = v;
            return {
              x,
              y: C
            };
          }
        },
        ...u
      } = Ge(e4, t), l = {
        x: n,
        y: r2
      }, f = await o2.detectOverflow(t, u), h2 = ae(pe(i2)), d = en(h2);
      let m = l[d], p = l[h2];
      if (s) {
        const v = d === "y" ? "top" : "left", x = d === "y" ? "bottom" : "right", C = m + f[v], T2 = m - f[x];
        m = At(C, m, T2);
      }
      if (a) {
        const v = h2 === "y" ? "top" : "left", x = h2 === "y" ? "bottom" : "right", C = p + f[v], T2 = p - f[x];
        p = At(C, p, T2);
      }
      const g = c.fn({
        ...t,
        [d]: m,
        [h2]: p
      });
      return {
        ...g,
        data: {
          x: g.x - n,
          y: g.y - r2,
          enabled: {
            [d]: s,
            [h2]: a
          }
        }
      };
    }
  };
};
function Xe() {
  return typeof window < "u";
}
function Ce(e4) {
  return rn(e4) ? (e4.nodeName || "").toLowerCase() : "#document";
}
function K(e4) {
  var t;
  return (e4 == null || (t = e4.ownerDocument) == null ? void 0 : t.defaultView) || window;
}
function ne(e4) {
  var t;
  return (t = (rn(e4) ? e4.ownerDocument : e4.document) || window.document) == null ? void 0 : t.documentElement;
}
function rn(e4) {
  return Xe() ? e4 instanceof Node || e4 instanceof K(e4).Node : false;
}
function Z(e4) {
  return Xe() ? e4 instanceof Element || e4 instanceof K(e4).Element : false;
}
function ie(e4) {
  return Xe() ? e4 instanceof HTMLElement || e4 instanceof K(e4).HTMLElement : false;
}
function It(e4) {
  return !Xe() || typeof ShadowRoot > "u" ? false : e4 instanceof ShadowRoot || e4 instanceof K(e4).ShadowRoot;
}
function Le(e4) {
  const {
    overflow: t,
    overflowX: n,
    overflowY: r2,
    display: i2
  } = ee(e4);
  return /auto|scroll|overlay|hidden|clip/.test(t + r2 + n) && i2 !== "inline" && i2 !== "contents";
}
function gr(e4) {
  return /^(table|td|th)$/.test(Ce(e4));
}
function Je(e4) {
  try {
    if (e4.matches(":popover-open"))
      return true;
  } catch {
  }
  try {
    return e4.matches(":modal");
  } catch {
    return false;
  }
}
var br = /transform|translate|scale|rotate|perspective|filter/;
var yr = /paint|layout|strict|content/;
var le = (e4) => !!e4 && e4 !== "none";
var it;
function bt(e4) {
  const t = Z(e4) ? ee(e4) : e4;
  return le(t.transform) || le(t.translate) || le(t.scale) || le(t.rotate) || le(t.perspective) || !yt() && (le(t.backdropFilter) || le(t.filter)) || br.test(t.willChange || "") || yr.test(t.contain || "");
}
function wr(e4) {
  let t = ue(e4);
  for (; ie(t) && !we(t); ) {
    if (bt(t))
      return t;
    if (Je(t))
      return null;
    t = ue(t);
  }
  return null;
}
function yt() {
  return it == null && (it = typeof CSS < "u" && CSS.supports && CSS.supports("-webkit-backdrop-filter", "none")), it;
}
function we(e4) {
  return /^(html|body|#document)$/.test(Ce(e4));
}
function ee(e4) {
  return K(e4).getComputedStyle(e4);
}
function Ze(e4) {
  return Z(e4) ? {
    scrollLeft: e4.scrollLeft,
    scrollTop: e4.scrollTop
  } : {
    scrollLeft: e4.scrollX,
    scrollTop: e4.scrollY
  };
}
function ue(e4) {
  if (Ce(e4) === "html")
    return e4;
  const t = (
    // Step into the shadow DOM of the parent of a slotted node.
    e4.assignedSlot || // DOM Element detected.
    e4.parentNode || // ShadowRoot detected.
    It(e4) && e4.host || // Fallback.
    ne(e4)
  );
  return It(t) ? t.host : t;
}
function on(e4) {
  const t = ue(e4);
  return we(t) ? e4.ownerDocument ? e4.ownerDocument.body : e4.body : ie(t) && Le(t) ? t : on(t);
}
function Oe(e4, t, n) {
  var r2;
  t === void 0 && (t = []), n === void 0 && (n = true);
  const i2 = on(e4), o2 = i2 === ((r2 = e4.ownerDocument) == null ? void 0 : r2.body), s = K(i2);
  if (o2) {
    const a = dt(s);
    return t.concat(s, s.visualViewport || [], Le(i2) ? i2 : [], a && n ? Oe(a) : []);
  } else
    return t.concat(i2, Oe(i2, [], n));
}
function dt(e4) {
  return e4.parent && Object.getPrototypeOf(e4.parent) ? e4.frameElement : null;
}
function sn(e4) {
  const t = ee(e4);
  let n = parseFloat(t.width) || 0, r2 = parseFloat(t.height) || 0;
  const i2 = ie(e4), o2 = i2 ? e4.offsetWidth : n, s = i2 ? e4.offsetHeight : r2, a = Be(n) !== o2 || Be(r2) !== s;
  return a && (n = o2, r2 = s), {
    width: n,
    height: r2,
    $: a
  };
}
function wt(e4) {
  return Z(e4) ? e4 : e4.contextElement;
}
function ge(e4) {
  const t = wt(e4);
  if (!ie(t))
    return te(1);
  const n = t.getBoundingClientRect(), {
    width: r2,
    height: i2,
    $: o2
  } = sn(t);
  let s = (o2 ? Be(n.width) : n.width) / r2, a = (o2 ? Be(n.height) : n.height) / i2;
  return (!s || !Number.isFinite(s)) && (s = 1), (!a || !Number.isFinite(a)) && (a = 1), {
    x: s,
    y: a
  };
}
var xr = te(0);
function an(e4) {
  const t = K(e4);
  return !yt() || !t.visualViewport ? xr : {
    x: t.visualViewport.offsetLeft,
    y: t.visualViewport.offsetTop
  };
}
function Sr(e4, t, n) {
  return t === void 0 && (t = false), !n || t && n !== K(e4) ? false : t;
}
function he(e4, t, n, r2) {
  t === void 0 && (t = false), n === void 0 && (n = false);
  const i2 = e4.getBoundingClientRect(), o2 = wt(e4);
  let s = te(1);
  t && (r2 ? Z(r2) && (s = ge(r2)) : s = ge(e4));
  const a = Sr(o2, n, r2) ? an(o2) : te(0);
  let c = (i2.left + a.x) / s.x, u = (i2.top + a.y) / s.y, l = i2.width / s.x, f = i2.height / s.y;
  if (o2) {
    const h2 = K(o2), d = r2 && Z(r2) ? K(r2) : r2;
    let m = h2, p = dt(m);
    for (; p && r2 && d !== m; ) {
      const g = ge(p), v = p.getBoundingClientRect(), x = ee(p), C = v.left + (p.clientLeft + parseFloat(x.paddingLeft)) * g.x, T2 = v.top + (p.clientTop + parseFloat(x.paddingTop)) * g.y;
      c *= g.x, u *= g.y, l *= g.x, f *= g.y, c += C, u += T2, m = K(p), p = dt(m);
    }
  }
  return Ve({
    width: l,
    height: f,
    x: c,
    y: u
  });
}
function et(e4, t) {
  const n = Ze(e4).scrollLeft;
  return t ? t.left + n : he(ne(e4)).left + n;
}
function cn(e4, t) {
  const n = e4.getBoundingClientRect(), r2 = n.left + t.scrollLeft - et(e4, n), i2 = n.top + t.scrollTop;
  return {
    x: r2,
    y: i2
  };
}
function Cr(e4) {
  let {
    elements: t,
    rect: n,
    offsetParent: r2,
    strategy: i2
  } = e4;
  const o2 = i2 === "fixed", s = ne(r2), a = t ? Je(t.floating) : false;
  if (r2 === s || a && o2)
    return n;
  let c = {
    scrollLeft: 0,
    scrollTop: 0
  }, u = te(1);
  const l = te(0), f = ie(r2);
  if ((f || !f && !o2) && ((Ce(r2) !== "body" || Le(s)) && (c = Ze(r2)), f)) {
    const d = he(r2);
    u = ge(r2), l.x = d.x + r2.clientLeft, l.y = d.y + r2.clientTop;
  }
  const h2 = s && !f && !o2 ? cn(s, c) : te(0);
  return {
    width: n.width * u.x,
    height: n.height * u.y,
    x: n.x * u.x - c.scrollLeft * u.x + l.x + h2.x,
    y: n.y * u.y - c.scrollTop * u.y + l.y + h2.y
  };
}
function Er(e4) {
  return Array.from(e4.getClientRects());
}
function Tr(e4) {
  const t = ne(e4), n = Ze(e4), r2 = e4.ownerDocument.body, i2 = de(t.scrollWidth, t.clientWidth, r2.scrollWidth, r2.clientWidth), o2 = de(t.scrollHeight, t.clientHeight, r2.scrollHeight, r2.clientHeight);
  let s = -n.scrollLeft + et(e4);
  const a = -n.scrollTop;
  return ee(r2).direction === "rtl" && (s += de(t.clientWidth, r2.clientWidth) - i2), {
    width: i2,
    height: o2,
    x: s,
    y: a
  };
}
var Rt = 25;
function Pr(e4, t) {
  const n = K(e4), r2 = ne(e4), i2 = n.visualViewport;
  let o2 = r2.clientWidth, s = r2.clientHeight, a = 0, c = 0;
  if (i2) {
    o2 = i2.width, s = i2.height;
    const l = yt();
    (!l || l && t === "fixed") && (a = i2.offsetLeft, c = i2.offsetTop);
  }
  const u = et(r2);
  if (u <= 0) {
    const l = r2.ownerDocument, f = l.body, h2 = getComputedStyle(f), d = l.compatMode === "CSS1Compat" && parseFloat(h2.marginLeft) + parseFloat(h2.marginRight) || 0, m = Math.abs(r2.clientWidth - f.clientWidth - d);
    m <= Rt && (o2 -= m);
  } else u <= Rt && (o2 += u);
  return {
    width: o2,
    height: s,
    x: a,
    y: c
  };
}
function Or(e4, t) {
  const n = he(e4, true, t === "fixed"), r2 = n.top + e4.clientTop, i2 = n.left + e4.clientLeft, o2 = ie(e4) ? ge(e4) : te(1), s = e4.clientWidth * o2.x, a = e4.clientHeight * o2.y, c = i2 * o2.x, u = r2 * o2.y;
  return {
    width: s,
    height: a,
    x: c,
    y: u
  };
}
function Mt(e4, t, n) {
  let r2;
  if (t === "viewport")
    r2 = Pr(e4, n);
  else if (t === "document")
    r2 = Tr(ne(e4));
  else if (Z(t))
    r2 = Or(t, n);
  else {
    const i2 = an(e4);
    r2 = {
      x: t.x - i2.x,
      y: t.y - i2.y,
      width: t.width,
      height: t.height
    };
  }
  return Ve(r2);
}
function un(e4, t) {
  const n = ue(e4);
  return n === t || !Z(n) || we(n) ? false : ee(n).position === "fixed" || un(n, t);
}
function Ar(e4, t) {
  const n = t.get(e4);
  if (n)
    return n;
  let r2 = Oe(e4, [], false).filter((a) => Z(a) && Ce(a) !== "body"), i2 = null;
  const o2 = ee(e4).position === "fixed";
  let s = o2 ? ue(e4) : e4;
  for (; Z(s) && !we(s); ) {
    const a = ee(s), c = bt(s);
    !c && a.position === "fixed" && (i2 = null), (o2 ? !c && !i2 : !c && a.position === "static" && !!i2 && (i2.position === "absolute" || i2.position === "fixed") || Le(s) && !c && un(e4, s)) ? r2 = r2.filter((l) => l !== s) : i2 = a, s = ue(s);
  }
  return t.set(e4, r2), r2;
}
function _r(e4) {
  let {
    element: t,
    boundary: n,
    rootBoundary: r2,
    strategy: i2
  } = e4;
  const s = [...n === "clippingAncestors" ? Je(t) ? [] : Ar(t, this._c) : [].concat(n), r2], a = Mt(t, s[0], i2);
  let c = a.top, u = a.right, l = a.bottom, f = a.left;
  for (let h2 = 1; h2 < s.length; h2++) {
    const d = Mt(t, s[h2], i2);
    c = de(d.top, c), u = We(d.right, u), l = We(d.bottom, l), f = de(d.left, f);
  }
  return {
    width: u - f,
    height: l - c,
    x: f,
    y: c
  };
}
function kr(e4) {
  const {
    width: t,
    height: n
  } = sn(e4);
  return {
    width: t,
    height: n
  };
}
function Lr(e4, t, n) {
  const r2 = ie(t), i2 = ne(t), o2 = n === "fixed", s = he(e4, true, o2, t);
  let a = {
    scrollLeft: 0,
    scrollTop: 0
  };
  const c = te(0);
  function u() {
    c.x = et(i2);
  }
  if (r2 || !r2 && !o2)
    if ((Ce(t) !== "body" || Le(i2)) && (a = Ze(t)), r2) {
      const d = he(t, true, o2, t);
      c.x = d.x + t.clientLeft, c.y = d.y + t.clientTop;
    } else i2 && u();
  o2 && !r2 && i2 && u();
  const l = i2 && !r2 && !o2 ? cn(i2, a) : te(0), f = s.left + a.scrollLeft - c.x - l.x, h2 = s.top + a.scrollTop - c.y - l.y;
  return {
    x: f,
    y: h2,
    width: s.width,
    height: s.height
  };
}
function ot(e4) {
  return ee(e4).position === "static";
}
function Dt(e4, t) {
  if (!ie(e4) || ee(e4).position === "fixed")
    return null;
  if (t)
    return t(e4);
  let n = e4.offsetParent;
  return ne(e4) === n && (n = n.ownerDocument.body), n;
}
function ln(e4, t) {
  const n = K(e4);
  if (Je(e4))
    return n;
  if (!ie(e4)) {
    let i2 = ue(e4);
    for (; i2 && !we(i2); ) {
      if (Z(i2) && !ot(i2))
        return i2;
      i2 = ue(i2);
    }
    return n;
  }
  let r2 = Dt(e4, t);
  for (; r2 && gr(r2) && ot(r2); )
    r2 = Dt(r2, t);
  return r2 && we(r2) && ot(r2) && !bt(r2) ? n : r2 || wr(e4) || n;
}
var Ir = async function(e4) {
  const t = this.getOffsetParent || ln, n = this.getDimensions, r2 = await n(e4.floating);
  return {
    reference: Lr(e4.reference, await t(e4.floating), e4.strategy),
    floating: {
      x: 0,
      y: 0,
      width: r2.width,
      height: r2.height
    }
  };
};
function Rr(e4) {
  return ee(e4).direction === "rtl";
}
var Mr = {
  convertOffsetParentRelativeRectToViewportRelativeRect: Cr,
  getDocumentElement: ne,
  getClippingRect: _r,
  getOffsetParent: ln,
  getElementRects: Ir,
  getClientRects: Er,
  getDimensions: kr,
  getScale: ge,
  isElement: Z,
  isRTL: Rr
};
function fn(e4, t) {
  return e4.x === t.x && e4.y === t.y && e4.width === t.width && e4.height === t.height;
}
function Dr(e4, t) {
  let n = null, r2;
  const i2 = ne(e4);
  function o2() {
    var a;
    clearTimeout(r2), (a = n) == null || a.disconnect(), n = null;
  }
  function s(a, c) {
    a === void 0 && (a = false), c === void 0 && (c = 1), o2();
    const u = e4.getBoundingClientRect(), {
      left: l,
      top: f,
      width: h2,
      height: d
    } = u;
    if (a || t(), !h2 || !d)
      return;
    const m = Re(f), p = Re(i2.clientWidth - (l + h2)), g = Re(i2.clientHeight - (f + d)), v = Re(l), C = {
      rootMargin: -m + "px " + -p + "px " + -g + "px " + -v + "px",
      threshold: de(0, We(1, c)) || 1
    };
    let T2 = true;
    function S(b) {
      const P = b[0].intersectionRatio;
      if (P !== c) {
        if (!T2)
          return s();
        P ? s(false, P) : r2 = setTimeout(() => {
          s(false, 1e-7);
        }, 1e3);
      }
      P === 1 && !fn(u, e4.getBoundingClientRect()) && s(), T2 = false;
    }
    try {
      n = new IntersectionObserver(S, {
        ...C,
        // Handle <iframe>s
        root: i2.ownerDocument
      });
    } catch {
      n = new IntersectionObserver(S, C);
    }
    n.observe(e4);
  }
  return s(true), o2;
}
function $r(e4, t, n, r2) {
  r2 === void 0 && (r2 = {});
  const {
    ancestorScroll: i2 = true,
    ancestorResize: o2 = true,
    elementResize: s = typeof ResizeObserver == "function",
    layoutShift: a = typeof IntersectionObserver == "function",
    animationFrame: c = false
  } = r2, u = wt(e4), l = i2 || o2 ? [...u ? Oe(u) : [], ...t ? Oe(t) : []] : [];
  l.forEach((v) => {
    i2 && v.addEventListener("scroll", n, {
      passive: true
    }), o2 && v.addEventListener("resize", n);
  });
  const f = u && a ? Dr(u, n) : null;
  let h2 = -1, d = null;
  s && (d = new ResizeObserver((v) => {
    let [x] = v;
    x && x.target === u && d && t && (d.unobserve(t), cancelAnimationFrame(h2), h2 = requestAnimationFrame(() => {
      var C;
      (C = d) == null || C.observe(t);
    })), n();
  }), u && !c && d.observe(u), t && d.observe(t));
  let m, p = c ? he(e4) : null;
  c && g();
  function g() {
    const v = he(e4);
    p && !fn(p, v) && n(), p = v, m = requestAnimationFrame(g);
  }
  return n(), () => {
    var v;
    l.forEach((x) => {
      i2 && x.removeEventListener("scroll", n), o2 && x.removeEventListener("resize", n);
    }), f == null ? void 0 : f(), (v = d) == null || v.disconnect(), d = null, c && cancelAnimationFrame(m);
  };
}
var Nr = vr;
var Fr = mr;
var Ur = dr;
var Wr = (e4, t, n) => {
  const r2 = /* @__PURE__ */ new Map(), i2 = {
    platform: Mr,
    ...n
  }, o2 = {
    ...i2.platform,
    _c: r2
  };
  return fr(e4, t, {
    ...i2,
    platform: o2
  });
};
var es = typeof globalThis < "u" ? globalThis : typeof window < "u" ? window : typeof global < "u" ? global : typeof self < "u" ? self : {};
function Br(e4) {
  return e4 && e4.__esModule && Object.prototype.hasOwnProperty.call(e4, "default") ? e4.default : e4;
}
var Ne = { exports: {} };
var jr = Ne.exports;
var $t;
function Vr() {
  return $t || ($t = 1, function(e4) {
    (function(t, n) {
      e4.exports ? e4.exports = n() : t.log = n();
    })(jr, function() {
      var t = function() {
      }, n = "undefined", r2 = typeof window !== n && typeof window.navigator !== n && /Trident\/|MSIE /.test(window.navigator.userAgent), i2 = [
        "trace",
        "debug",
        "info",
        "warn",
        "error"
      ], o2 = {}, s = null;
      function a(p, g) {
        var v = p[g];
        if (typeof v.bind == "function")
          return v.bind(p);
        try {
          return Function.prototype.bind.call(v, p);
        } catch {
          return function() {
            return Function.prototype.apply.apply(v, [p, arguments]);
          };
        }
      }
      function c() {
        console.log && (console.log.apply ? console.log.apply(console, arguments) : Function.prototype.apply.apply(console.log, [console, arguments])), console.trace && console.trace();
      }
      function u(p) {
        return p === "debug" && (p = "log"), typeof console === n ? false : p === "trace" && r2 ? c : console[p] !== void 0 ? a(console, p) : console.log !== void 0 ? a(console, "log") : t;
      }
      function l() {
        for (var p = this.getLevel(), g = 0; g < i2.length; g++) {
          var v = i2[g];
          this[v] = g < p ? t : this.methodFactory(v, p, this.name);
        }
        if (this.log = this.debug, typeof console === n && p < this.levels.SILENT)
          return "No console available for logging";
      }
      function f(p) {
        return function() {
          typeof console !== n && (l.call(this), this[p].apply(this, arguments));
        };
      }
      function h2(p, g, v) {
        return u(p) || f.apply(this, arguments);
      }
      function d(p, g) {
        var v = this, x, C, T2, S = "loglevel";
        typeof p == "string" ? S += ":" + p : typeof p == "symbol" && (S = void 0);
        function b(E) {
          var O = (i2[E] || "silent").toUpperCase();
          if (!(typeof window === n || !S)) {
            try {
              window.localStorage[S] = O;
              return;
            } catch {
            }
            try {
              window.document.cookie = encodeURIComponent(S) + "=" + O + ";";
            } catch {
            }
          }
        }
        function P() {
          var E;
          if (!(typeof window === n || !S)) {
            try {
              E = window.localStorage[S];
            } catch {
            }
            if (typeof E === n)
              try {
                var O = window.document.cookie, j2 = encodeURIComponent(S), k = O.indexOf(j2 + "=");
                k !== -1 && (E = /^([^;]+)/.exec(
                  O.slice(k + j2.length + 1)
                )[1]);
              } catch {
              }
            return v.levels[E] === void 0 && (E = void 0), E;
          }
        }
        function R() {
          if (!(typeof window === n || !S)) {
            try {
              window.localStorage.removeItem(S);
            } catch {
            }
            try {
              window.document.cookie = encodeURIComponent(S) + "=; expires=Thu, 01 Jan 1970 00:00:00 UTC";
            } catch {
            }
          }
        }
        function M2(E) {
          var O = E;
          if (typeof O == "string" && v.levels[O.toUpperCase()] !== void 0 && (O = v.levels[O.toUpperCase()]), typeof O == "number" && O >= 0 && O <= v.levels.SILENT)
            return O;
          throw new TypeError("log.setLevel() called with invalid level: " + E);
        }
        v.name = p, v.levels = {
          TRACE: 0,
          DEBUG: 1,
          INFO: 2,
          WARN: 3,
          ERROR: 4,
          SILENT: 5
        }, v.methodFactory = g || h2, v.getLevel = function() {
          return T2 ?? C ?? x;
        }, v.setLevel = function(E, O) {
          return T2 = M2(E), O !== false && b(T2), l.call(v);
        }, v.setDefaultLevel = function(E) {
          C = M2(E), P() || v.setLevel(E, false);
        }, v.resetLevel = function() {
          T2 = null, R(), l.call(v);
        }, v.enableAll = function(E) {
          v.setLevel(v.levels.TRACE, E);
        }, v.disableAll = function(E) {
          v.setLevel(v.levels.SILENT, E);
        }, v.rebuild = function() {
          if (s !== v && (x = M2(s.getLevel())), l.call(v), s === v)
            for (var E in o2)
              o2[E].rebuild();
        }, x = M2(
          s ? s.getLevel() : "WARN"
        );
        var W3 = P();
        W3 != null && (T2 = M2(W3)), l.call(v);
      }
      s = new d(), s.getLogger = function(g) {
        if (typeof g != "symbol" && typeof g != "string" || g === "")
          throw new TypeError("You must supply a name when creating a logger.");
        var v = o2[g];
        return v || (v = o2[g] = new d(
          g,
          s.methodFactory
        )), v;
      };
      var m = typeof window !== n ? window.log : void 0;
      return s.noConflict = function() {
        return typeof window !== n && window.log === s && (window.log = m), s;
      }, s.getLoggers = function() {
        return o2;
      }, s.default = s, s;
    });
  }(Ne)), Ne.exports;
}
var Hr = Vr();
var zr = Br(Hr);
var pt = function(e4, t) {
  return pt = Object.setPrototypeOf || { __proto__: [] } instanceof Array && function(n, r2) {
    n.__proto__ = r2;
  } || function(n, r2) {
    for (var i2 in r2) Object.prototype.hasOwnProperty.call(r2, i2) && (n[i2] = r2[i2]);
  }, pt(e4, t);
};
function oe(e4, t) {
  if (typeof t != "function" && t !== null)
    throw new TypeError("Class extends value " + String(t) + " is not a constructor or null");
  pt(e4, t);
  function n() {
    this.constructor = e4;
  }
  e4.prototype = t === null ? Object.create(t) : (n.prototype = t.prototype, new n());
}
function Yr(e4, t, n, r2) {
  function i2(o2) {
    return o2 instanceof n ? o2 : new n(function(s) {
      s(o2);
    });
  }
  return new (n || (n = Promise))(function(o2, s) {
    function a(l) {
      try {
        u(r2.next(l));
      } catch (f) {
        s(f);
      }
    }
    function c(l) {
      try {
        u(r2.throw(l));
      } catch (f) {
        s(f);
      }
    }
    function u(l) {
      l.done ? o2(l.value) : i2(l.value).then(a, c);
    }
    u((r2 = r2.apply(e4, t || [])).next());
  });
}
function dn(e4, t) {
  var n = { label: 0, sent: function() {
    if (o2[0] & 1) throw o2[1];
    return o2[1];
  }, trys: [], ops: [] }, r2, i2, o2, s = Object.create((typeof Iterator == "function" ? Iterator : Object).prototype);
  return s.next = a(0), s.throw = a(1), s.return = a(2), typeof Symbol == "function" && (s[Symbol.iterator] = function() {
    return this;
  }), s;
  function a(u) {
    return function(l) {
      return c([u, l]);
    };
  }
  function c(u) {
    if (r2) throw new TypeError("Generator is already executing.");
    for (; s && (s = 0, u[0] && (n = 0)), n; ) try {
      if (r2 = 1, i2 && (o2 = u[0] & 2 ? i2.return : u[0] ? i2.throw || ((o2 = i2.return) && o2.call(i2), 0) : i2.next) && !(o2 = o2.call(i2, u[1])).done) return o2;
      switch (i2 = 0, o2 && (u = [u[0] & 2, o2.value]), u[0]) {
        case 0:
        case 1:
          o2 = u;
          break;
        case 4:
          return n.label++, { value: u[1], done: false };
        case 5:
          n.label++, i2 = u[1], u = [0];
          continue;
        case 7:
          u = n.ops.pop(), n.trys.pop();
          continue;
        default:
          if (o2 = n.trys, !(o2 = o2.length > 0 && o2[o2.length - 1]) && (u[0] === 6 || u[0] === 2)) {
            n = 0;
            continue;
          }
          if (u[0] === 3 && (!o2 || u[1] > o2[0] && u[1] < o2[3])) {
            n.label = u[1];
            break;
          }
          if (u[0] === 6 && n.label < o2[1]) {
            n.label = o2[1], o2 = u;
            break;
          }
          if (o2 && n.label < o2[2]) {
            n.label = o2[2], n.ops.push(u);
            break;
          }
          o2[2] && n.ops.pop(), n.trys.pop();
          continue;
      }
      u = t.call(e4, n);
    } catch (l) {
      u = [6, l], i2 = 0;
    } finally {
      r2 = o2 = 0;
    }
    if (u[0] & 5) throw u[1];
    return { value: u[0] ? u[1] : void 0, done: true };
  }
}
function xe(e4) {
  var t = typeof Symbol == "function" && Symbol.iterator, n = t && e4[t], r2 = 0;
  if (n) return n.call(e4);
  if (e4 && typeof e4.length == "number") return {
    next: function() {
      return e4 && r2 >= e4.length && (e4 = void 0), { value: e4 && e4[r2++], done: !e4 };
    }
  };
  throw new TypeError(t ? "Object is not iterable." : "Symbol.iterator is not defined.");
}
function ve(e4, t) {
  var n = typeof Symbol == "function" && e4[Symbol.iterator];
  if (!n) return e4;
  var r2 = n.call(e4), i2, o2 = [], s;
  try {
    for (; (t === void 0 || t-- > 0) && !(i2 = r2.next()).done; ) o2.push(i2.value);
  } catch (a) {
    s = { error: a };
  } finally {
    try {
      i2 && !i2.done && (n = r2.return) && n.call(r2);
    } finally {
      if (s) throw s.error;
    }
  }
  return o2;
}
function Se(e4, t, n) {
  if (n || arguments.length === 2) for (var r2 = 0, i2 = t.length, o2; r2 < i2; r2++)
    (o2 || !(r2 in t)) && (o2 || (o2 = Array.prototype.slice.call(t, 0, r2)), o2[r2] = t[r2]);
  return e4.concat(o2 || Array.prototype.slice.call(t));
}
function be(e4) {
  return this instanceof be ? (this.v = e4, this) : new be(e4);
}
function Kr(e4, t, n) {
  if (!Symbol.asyncIterator) throw new TypeError("Symbol.asyncIterator is not defined.");
  var r2 = n.apply(e4, t || []), i2, o2 = [];
  return i2 = Object.create((typeof AsyncIterator == "function" ? AsyncIterator : Object).prototype), a("next"), a("throw"), a("return", s), i2[Symbol.asyncIterator] = function() {
    return this;
  }, i2;
  function s(d) {
    return function(m) {
      return Promise.resolve(m).then(d, f);
    };
  }
  function a(d, m) {
    r2[d] && (i2[d] = function(p) {
      return new Promise(function(g, v) {
        o2.push([d, p, g, v]) > 1 || c(d, p);
      });
    }, m && (i2[d] = m(i2[d])));
  }
  function c(d, m) {
    try {
      u(r2[d](m));
    } catch (p) {
      h2(o2[0][3], p);
    }
  }
  function u(d) {
    d.value instanceof be ? Promise.resolve(d.value.v).then(l, f) : h2(o2[0][2], d);
  }
  function l(d) {
    c("next", d);
  }
  function f(d) {
    c("throw", d);
  }
  function h2(d, m) {
    d(m), o2.shift(), o2.length && c(o2[0][0], o2[0][1]);
  }
}
function qr(e4) {
  if (!Symbol.asyncIterator) throw new TypeError("Symbol.asyncIterator is not defined.");
  var t = e4[Symbol.asyncIterator], n;
  return t ? t.call(e4) : (e4 = typeof xe == "function" ? xe(e4) : e4[Symbol.iterator](), n = {}, r2("next"), r2("throw"), r2("return"), n[Symbol.asyncIterator] = function() {
    return this;
  }, n);
  function r2(o2) {
    n[o2] = e4[o2] && function(s) {
      return new Promise(function(a, c) {
        s = e4[o2](s), i2(a, c, s.done, s.value);
      });
    };
  }
  function i2(o2, s, a, c) {
    Promise.resolve(c).then(function(u) {
      o2({ value: u, done: a });
    }, s);
  }
}
function A(e4) {
  return typeof e4 == "function";
}
function xt(e4) {
  var t = function(r2) {
    Error.call(r2), r2.stack = new Error().stack;
  }, n = e4(t);
  return n.prototype = Object.create(Error.prototype), n.prototype.constructor = n, n;
}
var st = xt(function(e4) {
  return function(n) {
    e4(this), this.message = n ? n.length + ` errors occurred during unsubscription:
` + n.map(function(r2, i2) {
      return i2 + 1 + ") " + r2.toString();
    }).join(`
  `) : "", this.name = "UnsubscriptionError", this.errors = n;
  };
});
function He(e4, t) {
  if (e4) {
    var n = e4.indexOf(t);
    0 <= n && e4.splice(n, 1);
  }
}
var Ie = function() {
  function e4(t) {
    this.initialTeardown = t, this.closed = false, this._parentage = null, this._finalizers = null;
  }
  return e4.prototype.unsubscribe = function() {
    var t, n, r2, i2, o2;
    if (!this.closed) {
      this.closed = true;
      var s = this._parentage;
      if (s)
        if (this._parentage = null, Array.isArray(s))
          try {
            for (var a = xe(s), c = a.next(); !c.done; c = a.next()) {
              var u = c.value;
              u.remove(this);
            }
          } catch (p) {
            t = { error: p };
          } finally {
            try {
              c && !c.done && (n = a.return) && n.call(a);
            } finally {
              if (t) throw t.error;
            }
          }
        else
          s.remove(this);
      var l = this.initialTeardown;
      if (A(l))
        try {
          l();
        } catch (p) {
          o2 = p instanceof st ? p.errors : [p];
        }
      var f = this._finalizers;
      if (f) {
        this._finalizers = null;
        try {
          for (var h2 = xe(f), d = h2.next(); !d.done; d = h2.next()) {
            var m = d.value;
            try {
              Nt(m);
            } catch (p) {
              o2 = o2 ?? [], p instanceof st ? o2 = Se(Se([], ve(o2)), ve(p.errors)) : o2.push(p);
            }
          }
        } catch (p) {
          r2 = { error: p };
        } finally {
          try {
            d && !d.done && (i2 = h2.return) && i2.call(h2);
          } finally {
            if (r2) throw r2.error;
          }
        }
      }
      if (o2)
        throw new st(o2);
    }
  }, e4.prototype.add = function(t) {
    var n;
    if (t && t !== this)
      if (this.closed)
        Nt(t);
      else {
        if (t instanceof e4) {
          if (t.closed || t._hasParent(this))
            return;
          t._addParent(this);
        }
        (this._finalizers = (n = this._finalizers) !== null && n !== void 0 ? n : []).push(t);
      }
  }, e4.prototype._hasParent = function(t) {
    var n = this._parentage;
    return n === t || Array.isArray(n) && n.includes(t);
  }, e4.prototype._addParent = function(t) {
    var n = this._parentage;
    this._parentage = Array.isArray(n) ? (n.push(t), n) : n ? [n, t] : t;
  }, e4.prototype._removeParent = function(t) {
    var n = this._parentage;
    n === t ? this._parentage = null : Array.isArray(n) && He(n, t);
  }, e4.prototype.remove = function(t) {
    var n = this._finalizers;
    n && He(n, t), t instanceof e4 && t._removeParent(this);
  }, e4.EMPTY = function() {
    var t = new e4();
    return t.closed = true, t;
  }(), e4;
}();
var pn = Ie.EMPTY;
function hn(e4) {
  return e4 instanceof Ie || e4 && "closed" in e4 && A(e4.remove) && A(e4.add) && A(e4.unsubscribe);
}
function Nt(e4) {
  A(e4) ? e4() : e4.unsubscribe();
}
var Gr = {
  Promise: void 0
};
var Qr = {
  setTimeout: function(e4, t) {
    for (var n = [], r2 = 2; r2 < arguments.length; r2++)
      n[r2 - 2] = arguments[r2];
    return setTimeout.apply(void 0, Se([e4, t], ve(n)));
  },
  clearTimeout: function(e4) {
    return clearTimeout(e4);
  },
  delegate: void 0
};
function vn(e4) {
  Qr.setTimeout(function() {
    throw e4;
  });
}
function ze() {
}
function Fe(e4) {
  e4();
}
var St = function(e4) {
  oe(t, e4);
  function t(n) {
    var r2 = e4.call(this) || this;
    return r2.isStopped = false, n ? (r2.destination = n, hn(n) && n.add(r2)) : r2.destination = Zr, r2;
  }
  return t.create = function(n, r2, i2) {
    return new Ae(n, r2, i2);
  }, t.prototype.next = function(n) {
    this.isStopped || this._next(n);
  }, t.prototype.error = function(n) {
    this.isStopped || (this.isStopped = true, this._error(n));
  }, t.prototype.complete = function() {
    this.isStopped || (this.isStopped = true, this._complete());
  }, t.prototype.unsubscribe = function() {
    this.closed || (this.isStopped = true, e4.prototype.unsubscribe.call(this), this.destination = null);
  }, t.prototype._next = function(n) {
    this.destination.next(n);
  }, t.prototype._error = function(n) {
    try {
      this.destination.error(n);
    } finally {
      this.unsubscribe();
    }
  }, t.prototype._complete = function() {
    try {
      this.destination.complete();
    } finally {
      this.unsubscribe();
    }
  }, t;
}(Ie);
var Xr = function() {
  function e4(t) {
    this.partialObserver = t;
  }
  return e4.prototype.next = function(t) {
    var n = this.partialObserver;
    if (n.next)
      try {
        n.next(t);
      } catch (r2) {
        Me(r2);
      }
  }, e4.prototype.error = function(t) {
    var n = this.partialObserver;
    if (n.error)
      try {
        n.error(t);
      } catch (r2) {
        Me(r2);
      }
    else
      Me(t);
  }, e4.prototype.complete = function() {
    var t = this.partialObserver;
    if (t.complete)
      try {
        t.complete();
      } catch (n) {
        Me(n);
      }
  }, e4;
}();
var Ae = function(e4) {
  oe(t, e4);
  function t(n, r2, i2) {
    var o2 = e4.call(this) || this, s;
    return A(n) || !n ? s = {
      next: n ?? void 0,
      error: r2 ?? void 0,
      complete: i2 ?? void 0
    } : s = n, o2.destination = new Xr(s), o2;
  }
  return t;
}(St);
function Me(e4) {
  vn(e4);
}
function Jr(e4) {
  throw e4;
}
var Zr = {
  closed: true,
  next: ze,
  error: Jr,
  complete: ze
};
var Ct = function() {
  return typeof Symbol == "function" && Symbol.observable || "@@observable";
}();
function tt(e4) {
  return e4;
}
function ei(e4) {
  return e4.length === 0 ? tt : e4.length === 1 ? e4[0] : function(n) {
    return e4.reduce(function(r2, i2) {
      return i2(r2);
    }, n);
  };
}
var L = function() {
  function e4(t) {
    t && (this._subscribe = t);
  }
  return e4.prototype.lift = function(t) {
    var n = new e4();
    return n.source = this, n.operator = t, n;
  }, e4.prototype.subscribe = function(t, n, r2) {
    var i2 = this, o2 = ni(t) ? t : new Ae(t, n, r2);
    return Fe(function() {
      var s = i2, a = s.operator, c = s.source;
      o2.add(a ? a.call(o2, c) : c ? i2._subscribe(o2) : i2._trySubscribe(o2));
    }), o2;
  }, e4.prototype._trySubscribe = function(t) {
    try {
      return this._subscribe(t);
    } catch (n) {
      t.error(n);
    }
  }, e4.prototype.forEach = function(t, n) {
    var r2 = this;
    return n = Ft(n), new n(function(i2, o2) {
      var s = new Ae({
        next: function(a) {
          try {
            t(a);
          } catch (c) {
            o2(c), s.unsubscribe();
          }
        },
        error: o2,
        complete: i2
      });
      r2.subscribe(s);
    });
  }, e4.prototype._subscribe = function(t) {
    var n;
    return (n = this.source) === null || n === void 0 ? void 0 : n.subscribe(t);
  }, e4.prototype[Ct] = function() {
    return this;
  }, e4.prototype.pipe = function() {
    for (var t = [], n = 0; n < arguments.length; n++)
      t[n] = arguments[n];
    return ei(t)(this);
  }, e4.prototype.toPromise = function(t) {
    var n = this;
    return t = Ft(t), new t(function(r2, i2) {
      var o2;
      n.subscribe(function(s) {
        return o2 = s;
      }, function(s) {
        return i2(s);
      }, function() {
        return r2(o2);
      });
    });
  }, e4.create = function(t) {
    return new e4(t);
  }, e4;
}();
function Ft(e4) {
  var t;
  return (t = e4 ?? Gr.Promise) !== null && t !== void 0 ? t : Promise;
}
function ti(e4) {
  return e4 && A(e4.next) && A(e4.error) && A(e4.complete);
}
function ni(e4) {
  return e4 && e4 instanceof St || ti(e4) && hn(e4);
}
function ri(e4) {
  return A(e4 == null ? void 0 : e4.lift);
}
function B(e4) {
  return function(t) {
    if (ri(t))
      return t.lift(function(n) {
        try {
          return e4(n, this);
        } catch (r2) {
          this.error(r2);
        }
      });
    throw new TypeError("Unable to lift unknown Observable type");
  };
}
function z(e4, t, n, r2, i2) {
  return new ii(e4, t, n, r2, i2);
}
var ii = function(e4) {
  oe(t, e4);
  function t(n, r2, i2, o2, s, a) {
    var c = e4.call(this, n) || this;
    return c.onFinalize = s, c.shouldUnsubscribe = a, c._next = r2 ? function(u) {
      try {
        r2(u);
      } catch (l) {
        n.error(l);
      }
    } : e4.prototype._next, c._error = o2 ? function(u) {
      try {
        o2(u);
      } catch (l) {
        n.error(l);
      } finally {
        this.unsubscribe();
      }
    } : e4.prototype._error, c._complete = i2 ? function() {
      try {
        i2();
      } catch (u) {
        n.error(u);
      } finally {
        this.unsubscribe();
      }
    } : e4.prototype._complete, c;
  }
  return t.prototype.unsubscribe = function() {
    var n;
    if (!this.shouldUnsubscribe || this.shouldUnsubscribe()) {
      var r2 = this.closed;
      e4.prototype.unsubscribe.call(this), !r2 && ((n = this.onFinalize) === null || n === void 0 || n.call(this));
    }
  }, t;
}(St);
var oi = xt(function(e4) {
  return function() {
    e4(this), this.name = "ObjectUnsubscribedError", this.message = "object unsubscribed";
  };
});
var re = function(e4) {
  oe(t, e4);
  function t() {
    var n = e4.call(this) || this;
    return n.closed = false, n.currentObservers = null, n.observers = [], n.isStopped = false, n.hasError = false, n.thrownError = null, n;
  }
  return t.prototype.lift = function(n) {
    var r2 = new Ut(this, this);
    return r2.operator = n, r2;
  }, t.prototype._throwIfClosed = function() {
    if (this.closed)
      throw new oi();
  }, t.prototype.next = function(n) {
    var r2 = this;
    Fe(function() {
      var i2, o2;
      if (r2._throwIfClosed(), !r2.isStopped) {
        r2.currentObservers || (r2.currentObservers = Array.from(r2.observers));
        try {
          for (var s = xe(r2.currentObservers), a = s.next(); !a.done; a = s.next()) {
            var c = a.value;
            c.next(n);
          }
        } catch (u) {
          i2 = { error: u };
        } finally {
          try {
            a && !a.done && (o2 = s.return) && o2.call(s);
          } finally {
            if (i2) throw i2.error;
          }
        }
      }
    });
  }, t.prototype.error = function(n) {
    var r2 = this;
    Fe(function() {
      if (r2._throwIfClosed(), !r2.isStopped) {
        r2.hasError = r2.isStopped = true, r2.thrownError = n;
        for (var i2 = r2.observers; i2.length; )
          i2.shift().error(n);
      }
    });
  }, t.prototype.complete = function() {
    var n = this;
    Fe(function() {
      if (n._throwIfClosed(), !n.isStopped) {
        n.isStopped = true;
        for (var r2 = n.observers; r2.length; )
          r2.shift().complete();
      }
    });
  }, t.prototype.unsubscribe = function() {
    this.isStopped = this.closed = true, this.observers = this.currentObservers = null;
  }, Object.defineProperty(t.prototype, "observed", {
    get: function() {
      var n;
      return ((n = this.observers) === null || n === void 0 ? void 0 : n.length) > 0;
    },
    enumerable: false,
    configurable: true
  }), t.prototype._trySubscribe = function(n) {
    return this._throwIfClosed(), e4.prototype._trySubscribe.call(this, n);
  }, t.prototype._subscribe = function(n) {
    return this._throwIfClosed(), this._checkFinalizedStatuses(n), this._innerSubscribe(n);
  }, t.prototype._innerSubscribe = function(n) {
    var r2 = this, i2 = this, o2 = i2.hasError, s = i2.isStopped, a = i2.observers;
    return o2 || s ? pn : (this.currentObservers = null, a.push(n), new Ie(function() {
      r2.currentObservers = null, He(a, n);
    }));
  }, t.prototype._checkFinalizedStatuses = function(n) {
    var r2 = this, i2 = r2.hasError, o2 = r2.thrownError, s = r2.isStopped;
    i2 ? n.error(o2) : s && n.complete();
  }, t.prototype.asObservable = function() {
    var n = new L();
    return n.source = this, n;
  }, t.create = function(n, r2) {
    return new Ut(n, r2);
  }, t;
}(L);
var Ut = function(e4) {
  oe(t, e4);
  function t(n, r2) {
    var i2 = e4.call(this) || this;
    return i2.destination = n, i2.source = r2, i2;
  }
  return t.prototype.next = function(n) {
    var r2, i2;
    (i2 = (r2 = this.destination) === null || r2 === void 0 ? void 0 : r2.next) === null || i2 === void 0 || i2.call(r2, n);
  }, t.prototype.error = function(n) {
    var r2, i2;
    (i2 = (r2 = this.destination) === null || r2 === void 0 ? void 0 : r2.error) === null || i2 === void 0 || i2.call(r2, n);
  }, t.prototype.complete = function() {
    var n, r2;
    (r2 = (n = this.destination) === null || n === void 0 ? void 0 : n.complete) === null || r2 === void 0 || r2.call(n);
  }, t.prototype._subscribe = function(n) {
    var r2, i2;
    return (i2 = (r2 = this.source) === null || r2 === void 0 ? void 0 : r2.subscribe(n)) !== null && i2 !== void 0 ? i2 : pn;
  }, t;
}(re);
var mn = function(e4) {
  oe(t, e4);
  function t(n) {
    var r2 = e4.call(this) || this;
    return r2._value = n, r2;
  }
  return Object.defineProperty(t.prototype, "value", {
    get: function() {
      return this.getValue();
    },
    enumerable: false,
    configurable: true
  }), t.prototype._subscribe = function(n) {
    var r2 = e4.prototype._subscribe.call(this, n);
    return !r2.closed && n.next(this._value), r2;
  }, t.prototype.getValue = function() {
    var n = this, r2 = n.hasError, i2 = n.thrownError, o2 = n._value;
    if (r2)
      throw i2;
    return this._throwIfClosed(), o2;
  }, t.prototype.next = function(n) {
    e4.prototype.next.call(this, this._value = n);
  }, t;
}(re);
var si = {
  now: function() {
    return Date.now();
  }
};
var ai = function(e4) {
  oe(t, e4);
  function t(n, r2) {
    return e4.call(this) || this;
  }
  return t.prototype.schedule = function(n, r2) {
    return this;
  }, t;
}(Ie);
var Wt = {
  setInterval: function(e4, t) {
    for (var n = [], r2 = 2; r2 < arguments.length; r2++)
      n[r2 - 2] = arguments[r2];
    return setInterval.apply(void 0, Se([e4, t], ve(n)));
  },
  clearInterval: function(e4) {
    return clearInterval(e4);
  },
  delegate: void 0
};
var ci = function(e4) {
  oe(t, e4);
  function t(n, r2) {
    var i2 = e4.call(this, n, r2) || this;
    return i2.scheduler = n, i2.work = r2, i2.pending = false, i2;
  }
  return t.prototype.schedule = function(n, r2) {
    var i2;
    if (r2 === void 0 && (r2 = 0), this.closed)
      return this;
    this.state = n;
    var o2 = this.id, s = this.scheduler;
    return o2 != null && (this.id = this.recycleAsyncId(s, o2, r2)), this.pending = true, this.delay = r2, this.id = (i2 = this.id) !== null && i2 !== void 0 ? i2 : this.requestAsyncId(s, this.id, r2), this;
  }, t.prototype.requestAsyncId = function(n, r2, i2) {
    return i2 === void 0 && (i2 = 0), Wt.setInterval(n.flush.bind(n, this), i2);
  }, t.prototype.recycleAsyncId = function(n, r2, i2) {
    if (i2 === void 0 && (i2 = 0), i2 != null && this.delay === i2 && this.pending === false)
      return r2;
    r2 != null && Wt.clearInterval(r2);
  }, t.prototype.execute = function(n, r2) {
    if (this.closed)
      return new Error("executing a cancelled action");
    this.pending = false;
    var i2 = this._execute(n, r2);
    if (i2)
      return i2;
    this.pending === false && this.id != null && (this.id = this.recycleAsyncId(this.scheduler, this.id, null));
  }, t.prototype._execute = function(n, r2) {
    var i2 = false, o2;
    try {
      this.work(n);
    } catch (s) {
      i2 = true, o2 = s || new Error("Scheduled action threw falsy error");
    }
    if (i2)
      return this.unsubscribe(), o2;
  }, t.prototype.unsubscribe = function() {
    if (!this.closed) {
      var n = this, r2 = n.id, i2 = n.scheduler, o2 = i2.actions;
      this.work = this.state = this.scheduler = null, this.pending = false, He(o2, this), r2 != null && (this.id = this.recycleAsyncId(i2, r2, null)), this.delay = null, e4.prototype.unsubscribe.call(this);
    }
  }, t;
}(ai);
var Bt = function() {
  function e4(t, n) {
    n === void 0 && (n = e4.now), this.schedulerActionCtor = t, this.now = n;
  }
  return e4.prototype.schedule = function(t, n, r2) {
    return n === void 0 && (n = 0), new this.schedulerActionCtor(this, t).schedule(r2, n);
  }, e4.now = si.now, e4;
}();
var ui = function(e4) {
  oe(t, e4);
  function t(n, r2) {
    r2 === void 0 && (r2 = Bt.now);
    var i2 = e4.call(this, n, r2) || this;
    return i2.actions = [], i2._active = false, i2;
  }
  return t.prototype.flush = function(n) {
    var r2 = this.actions;
    if (this._active) {
      r2.push(n);
      return;
    }
    var i2;
    this._active = true;
    do
      if (i2 = n.execute(n.state, n.delay))
        break;
    while (n = r2.shift());
    if (this._active = false, i2) {
      for (; n = r2.shift(); )
        n.unsubscribe();
      throw i2;
    }
  }, t;
}(Bt);
var li = new ui(ci);
function fi(e4) {
  return e4 && A(e4.schedule);
}
function di(e4) {
  return e4[e4.length - 1];
}
function Et(e4) {
  return fi(di(e4)) ? e4.pop() : void 0;
}
var Tt = function(e4) {
  return e4 && typeof e4.length == "number" && typeof e4 != "function";
};
function gn(e4) {
  return A(e4 == null ? void 0 : e4.then);
}
function bn(e4) {
  return A(e4[Ct]);
}
function yn(e4) {
  return Symbol.asyncIterator && A(e4 == null ? void 0 : e4[Symbol.asyncIterator]);
}
function wn(e4) {
  return new TypeError("You provided " + (e4 !== null && typeof e4 == "object" ? "an invalid object" : "'" + e4 + "'") + " where a stream was expected. You can provide an Observable, Promise, ReadableStream, Array, AsyncIterable, or Iterable.");
}
function pi() {
  return typeof Symbol != "function" || !Symbol.iterator ? "@@iterator" : Symbol.iterator;
}
var xn = pi();
function Sn(e4) {
  return A(e4 == null ? void 0 : e4[xn]);
}
function Cn(e4) {
  return Kr(this, arguments, function() {
    var n, r2, i2, o2;
    return dn(this, function(s) {
      switch (s.label) {
        case 0:
          n = e4.getReader(), s.label = 1;
        case 1:
          s.trys.push([1, , 9, 10]), s.label = 2;
        case 2:
          return [4, be(n.read())];
        case 3:
          return r2 = s.sent(), i2 = r2.value, o2 = r2.done, o2 ? [4, be(void 0)] : [3, 5];
        case 4:
          return [2, s.sent()];
        case 5:
          return [4, be(i2)];
        case 6:
          return [4, s.sent()];
        case 7:
          return s.sent(), [3, 2];
        case 8:
          return [3, 10];
        case 9:
          return n.releaseLock(), [7];
        case 10:
          return [2];
      }
    });
  });
}
function En(e4) {
  return A(e4 == null ? void 0 : e4.getReader);
}
function Q(e4) {
  if (e4 instanceof L)
    return e4;
  if (e4 != null) {
    if (bn(e4))
      return hi(e4);
    if (Tt(e4))
      return vi(e4);
    if (gn(e4))
      return mi(e4);
    if (yn(e4))
      return Tn(e4);
    if (Sn(e4))
      return gi(e4);
    if (En(e4))
      return bi(e4);
  }
  throw wn(e4);
}
function hi(e4) {
  return new L(function(t) {
    var n = e4[Ct]();
    if (A(n.subscribe))
      return n.subscribe(t);
    throw new TypeError("Provided object does not correctly implement Symbol.observable");
  });
}
function vi(e4) {
  return new L(function(t) {
    for (var n = 0; n < e4.length && !t.closed; n++)
      t.next(e4[n]);
    t.complete();
  });
}
function mi(e4) {
  return new L(function(t) {
    e4.then(function(n) {
      t.closed || (t.next(n), t.complete());
    }, function(n) {
      return t.error(n);
    }).then(null, vn);
  });
}
function gi(e4) {
  return new L(function(t) {
    var n, r2;
    try {
      for (var i2 = xe(e4), o2 = i2.next(); !o2.done; o2 = i2.next()) {
        var s = o2.value;
        if (t.next(s), t.closed)
          return;
      }
    } catch (a) {
      n = { error: a };
    } finally {
      try {
        o2 && !o2.done && (r2 = i2.return) && r2.call(i2);
      } finally {
        if (n) throw n.error;
      }
    }
    t.complete();
  });
}
function Tn(e4) {
  return new L(function(t) {
    yi(e4, t).catch(function(n) {
      return t.error(n);
    });
  });
}
function bi(e4) {
  return Tn(Cn(e4));
}
function yi(e4, t) {
  var n, r2, i2, o2;
  return Yr(this, void 0, void 0, function() {
    var s, a;
    return dn(this, function(c) {
      switch (c.label) {
        case 0:
          c.trys.push([0, 5, 6, 11]), n = qr(e4), c.label = 1;
        case 1:
          return [4, n.next()];
        case 2:
          if (r2 = c.sent(), !!r2.done) return [3, 4];
          if (s = r2.value, t.next(s), t.closed)
            return [2];
          c.label = 3;
        case 3:
          return [3, 1];
        case 4:
          return [3, 11];
        case 5:
          return a = c.sent(), i2 = { error: a }, [3, 11];
        case 6:
          return c.trys.push([6, , 9, 10]), r2 && !r2.done && (o2 = n.return) ? [4, o2.call(n)] : [3, 8];
        case 7:
          c.sent(), c.label = 8;
        case 8:
          return [3, 10];
        case 9:
          if (i2) throw i2.error;
          return [7];
        case 10:
          return [7];
        case 11:
          return t.complete(), [2];
      }
    });
  });
}
function ce(e4, t, n, r2, i2) {
  r2 === void 0 && (r2 = 0), i2 === void 0 && (i2 = false);
  var o2 = t.schedule(function() {
    n(), i2 ? e4.add(this.schedule(null, r2)) : this.unsubscribe();
  }, r2);
  if (e4.add(o2), !i2)
    return o2;
}
function Pn(e4, t) {
  return t === void 0 && (t = 0), B(function(n, r2) {
    n.subscribe(z(r2, function(i2) {
      return ce(r2, e4, function() {
        return r2.next(i2);
      }, t);
    }, function() {
      return ce(r2, e4, function() {
        return r2.complete();
      }, t);
    }, function(i2) {
      return ce(r2, e4, function() {
        return r2.error(i2);
      }, t);
    }));
  });
}
function On(e4, t) {
  return t === void 0 && (t = 0), B(function(n, r2) {
    r2.add(e4.schedule(function() {
      return n.subscribe(r2);
    }, t));
  });
}
function wi(e4, t) {
  return Q(e4).pipe(On(t), Pn(t));
}
function xi(e4, t) {
  return Q(e4).pipe(On(t), Pn(t));
}
function Si(e4, t) {
  return new L(function(n) {
    var r2 = 0;
    return t.schedule(function() {
      r2 === e4.length ? n.complete() : (n.next(e4[r2++]), n.closed || this.schedule());
    });
  });
}
function Ci(e4, t) {
  return new L(function(n) {
    var r2;
    return ce(n, t, function() {
      r2 = e4[xn](), ce(n, t, function() {
        var i2, o2, s;
        try {
          i2 = r2.next(), o2 = i2.value, s = i2.done;
        } catch (a) {
          n.error(a);
          return;
        }
        s ? n.complete() : n.next(o2);
      }, 0, true);
    }), function() {
      return A(r2 == null ? void 0 : r2.return) && r2.return();
    };
  });
}
function An(e4, t) {
  if (!e4)
    throw new Error("Iterable cannot be null");
  return new L(function(n) {
    ce(n, t, function() {
      var r2 = e4[Symbol.asyncIterator]();
      ce(n, t, function() {
        r2.next().then(function(i2) {
          i2.done ? n.complete() : n.next(i2.value);
        });
      }, 0, true);
    });
  });
}
function Ei(e4, t) {
  return An(Cn(e4), t);
}
function Ti(e4, t) {
  if (e4 != null) {
    if (bn(e4))
      return wi(e4, t);
    if (Tt(e4))
      return Si(e4, t);
    if (gn(e4))
      return xi(e4, t);
    if (yn(e4))
      return An(e4, t);
    if (Sn(e4))
      return Ci(e4, t);
    if (En(e4))
      return Ei(e4, t);
  }
  throw wn(e4);
}
function ye(e4, t) {
  return t ? Ti(e4, t) : Q(e4);
}
function ht() {
  for (var e4 = [], t = 0; t < arguments.length; t++)
    e4[t] = arguments[t];
  var n = Et(e4);
  return ye(e4, n);
}
function Pi(e4) {
  return e4 instanceof Date && !isNaN(e4);
}
var Oi = xt(function(e4) {
  return function(n) {
    n === void 0 && (n = null), e4(this), this.message = "Timeout has occurred", this.name = "TimeoutError", this.info = n;
  };
});
function Ai(e4, t) {
  var n = Pi(e4) ? { first: e4 } : typeof e4 == "number" ? { each: e4 } : e4, r2 = n.first, i2 = n.each, o2 = n.with, s = o2 === void 0 ? _i : o2, a = n.scheduler, c = a === void 0 ? li : a, u = n.meta, l = u === void 0 ? null : u;
  if (r2 == null && i2 == null)
    throw new TypeError("No timeout provided.");
  return B(function(f, h2) {
    var d, m, p = null, g = 0, v = function(x) {
      m = ce(h2, c, function() {
        try {
          d.unsubscribe(), Q(s({
            meta: l,
            lastValue: p,
            seen: g
          })).subscribe(h2);
        } catch (C) {
          h2.error(C);
        }
      }, x);
    };
    d = f.subscribe(z(h2, function(x) {
      m == null ? void 0 : m.unsubscribe(), g++, h2.next(p = x), i2 > 0 && v(i2);
    }, void 0, void 0, function() {
      (m == null ? void 0 : m.closed) || (m == null ? void 0 : m.unsubscribe()), p = null;
    })), !g && v(r2 != null ? typeof r2 == "number" ? r2 : +r2 - c.now() : i2);
  });
}
function _i(e4) {
  throw new Oi(e4);
}
function _2(e4, t) {
  return B(function(n, r2) {
    var i2 = 0;
    n.subscribe(z(r2, function(o2) {
      r2.next(e4.call(t, o2, i2++));
    }));
  });
}
var ki = Array.isArray;
function Li(e4, t) {
  return ki(t) ? e4.apply(void 0, Se([], ve(t))) : e4(t);
}
function Ii(e4) {
  return _2(function(t) {
    return Li(e4, t);
  });
}
function Ri(e4, t, n, r2, i2, o2, s, a) {
  var c = [], u = 0, l = 0, f = false, h2 = function() {
    f && !c.length && !u && t.complete();
  }, d = function(p) {
    return u < r2 ? m(p) : c.push(p);
  }, m = function(p) {
    u++;
    var g = false;
    Q(n(p, l++)).subscribe(z(t, function(v) {
      t.next(v);
    }, function() {
      g = true;
    }, void 0, function() {
      if (g)
        try {
          u--;
          for (var v = function() {
            var x = c.shift();
            s || m(x);
          }; c.length && u < r2; )
            v();
          h2();
        } catch (x) {
          t.error(x);
        }
    }));
  };
  return e4.subscribe(z(t, d, function() {
    f = true, h2();
  })), function() {
  };
}
function _e(e4, t, n) {
  return n === void 0 && (n = 1 / 0), A(t) ? _e(function(r2, i2) {
    return _2(function(o2, s) {
      return t(r2, o2, i2, s);
    })(Q(e4(r2, i2)));
  }, n) : (typeof t == "number" && (n = t), B(function(r2, i2) {
    return Ri(r2, i2, e4, n);
  }));
}
function Mi(e4) {
  return _e(tt, e4);
}
function Di() {
  return Mi(1);
}
function Ye() {
  for (var e4 = [], t = 0; t < arguments.length; t++)
    e4[t] = arguments[t];
  return Di()(ye(e4, Et(e4)));
}
var $i = ["addListener", "removeListener"];
var Ni = ["addEventListener", "removeEventListener"];
var Fi = ["on", "off"];
function vt(e4, t, n, r2) {
  if (A(n) && (r2 = n, n = void 0), r2)
    return vt(e4, t, n).pipe(Ii(r2));
  var i2 = ve(Bi(e4) ? Ni.map(function(a) {
    return function(c) {
      return e4[a](t, c, n);
    };
  }) : Ui(e4) ? $i.map(jt(e4, t)) : Wi(e4) ? Fi.map(jt(e4, t)) : [], 2), o2 = i2[0], s = i2[1];
  if (!o2 && Tt(e4))
    return _e(function(a) {
      return vt(a, t, n);
    })(Q(e4));
  if (!o2)
    throw new TypeError("Invalid event target");
  return new L(function(a) {
    var c = function() {
      for (var u = [], l = 0; l < arguments.length; l++)
        u[l] = arguments[l];
      return a.next(1 < u.length ? u : u[0]);
    };
    return o2(c), function() {
      return s(c);
    };
  });
}
function jt(e4, t) {
  return function(n) {
    return function(r2) {
      return e4[n](t, r2);
    };
  };
}
function Ui(e4) {
  return A(e4.addListener) && A(e4.removeListener);
}
function Wi(e4) {
  return A(e4.on) && A(e4.off);
}
function Bi(e4) {
  return A(e4.addEventListener) && A(e4.removeEventListener);
}
function nt(e4, t) {
  return B(function(n, r2) {
    var i2 = 0;
    n.subscribe(z(r2, function(o2) {
      return e4.call(t, o2, i2++) && r2.next(o2);
    }));
  });
}
function ji(e4, t, n, r2, i2) {
  return function(o2, s) {
    var a = n, c = t, u = 0;
    o2.subscribe(z(s, function(l) {
      var f = u++;
      c = a ? e4(c, l, f) : (a = true, l), s.next(c);
    }, i2));
  };
}
function Vi(e4, t) {
  return t === void 0 && (t = tt), e4 = e4 ?? Hi, B(function(n, r2) {
    var i2, o2 = true;
    n.subscribe(z(r2, function(s) {
      var a = t(s);
      (o2 || !e4(i2, a)) && (o2 = false, i2 = a, r2.next(s));
    }));
  });
}
function Hi(e4, t) {
  return e4 === t;
}
function zi(e4) {
  return B(function(t, n) {
    try {
      t.subscribe(n);
    } finally {
      n.add(e4);
    }
  });
}
function Ue(e4, t) {
  return B(ji(e4, t, arguments.length >= 2, true));
}
function Yi(e4) {
  e4 === void 0 && (e4 = {});
  var t = e4.connector, n = t === void 0 ? function() {
    return new re();
  } : t, r2 = e4.resetOnError, i2 = r2 === void 0 ? true : r2, o2 = e4.resetOnComplete, s = o2 === void 0 ? true : o2, a = e4.resetOnRefCountZero, c = a === void 0 ? true : a;
  return function(u) {
    var l, f, h2, d = 0, m = false, p = false, g = function() {
      f == null ? void 0 : f.unsubscribe(), f = void 0;
    }, v = function() {
      g(), l = h2 = void 0, m = p = false;
    }, x = function() {
      var C = l;
      v(), C == null ? void 0 : C.unsubscribe();
    };
    return B(function(C, T2) {
      d++, !p && !m && g();
      var S = h2 = h2 ?? n();
      T2.add(function() {
        d--, d === 0 && !p && !m && (f = at(x, c));
      }), S.subscribe(T2), !l && d > 0 && (l = new Ae({
        next: function(b) {
          return S.next(b);
        },
        error: function(b) {
          p = true, g(), f = at(v, i2, b), S.error(b);
        },
        complete: function() {
          m = true, g(), f = at(v, s), S.complete();
        }
      }), Q(C).subscribe(l));
    })(u);
  };
}
function at(e4, t) {
  for (var n = [], r2 = 2; r2 < arguments.length; r2++)
    n[r2 - 2] = arguments[r2];
  if (t === true) {
    e4();
    return;
  }
  if (t !== false) {
    var i2 = new Ae({
      next: function() {
        i2.unsubscribe(), e4();
      }
    });
    return Q(t.apply(void 0, Se([], ve(n)))).subscribe(i2);
  }
}
function Ki(e4) {
  return B(function(t, n) {
    var r2 = false, i2 = z(n, function() {
      i2 == null ? void 0 : i2.unsubscribe(), r2 = true;
    }, ze);
    Q(e4).subscribe(i2), t.subscribe(z(n, function(o2) {
      return r2 && n.next(o2);
    }));
  });
}
function U() {
  for (var e4 = [], t = 0; t < arguments.length; t++)
    e4[t] = arguments[t];
  var n = Et(e4);
  return B(function(r2, i2) {
    (n ? Ye(e4, r2, n) : Ye(e4, r2)).subscribe(i2);
  });
}
function _n(e4, t) {
  return B(function(n, r2) {
    var i2 = null, o2 = 0, s = false, a = function() {
      return s && !i2 && r2.complete();
    };
    n.subscribe(z(r2, function(c) {
      i2 == null ? void 0 : i2.unsubscribe();
      var u = 0, l = o2++;
      Q(e4(c, l)).subscribe(i2 = z(r2, function(f) {
        return r2.next(t ? t(c, f, l, u++) : f);
      }, function() {
        i2 = null, a();
      }));
    }, function() {
      s = true, a();
    }));
  });
}
function Vt(e4) {
  return B(function(t, n) {
    Q(e4).subscribe(z(n, function() {
      return n.complete();
    }, ze)), !n.closed && t.subscribe(n);
  });
}
function qi(e4, t, n) {
  var r2 = A(e4) || t || n ? { next: e4, error: t, complete: n } : e4;
  return r2 ? B(function(i2, o2) {
    var s;
    (s = r2.subscribe) === null || s === void 0 || s.call(r2);
    var a = true;
    i2.subscribe(z(o2, function(c) {
      var u;
      (u = r2.next) === null || u === void 0 || u.call(r2, c), o2.next(c);
    }, function() {
      var c;
      a = false, (c = r2.complete) === null || c === void 0 || c.call(r2), o2.complete();
    }, function(c) {
      var u;
      a = false, (u = r2.error) === null || u === void 0 || u.call(r2, c), o2.error(c);
    }, function() {
      var c, u;
      a && ((c = r2.unsubscribe) === null || c === void 0 || c.call(r2)), (u = r2.finalize) === null || u === void 0 || u.call(r2);
    }));
  }) : tt;
}
var Gi = Object.defineProperty;
var Qi = Object.defineProperties;
var Xi = Object.getOwnPropertyDescriptors;
var Ht = Object.getOwnPropertySymbols;
var Ji = Object.prototype.hasOwnProperty;
var Zi = Object.prototype.propertyIsEnumerable;
var zt = (e4, t) => (t = Symbol[e4]) ? t : Symbol.for("Symbol." + e4);
var Yt = (e4, t, n) => t in e4 ? Gi(e4, t, { enumerable: true, configurable: true, writable: true, value: n }) : e4[t] = n;
var J = (e4, t) => {
  for (var n in t || (t = {}))
    Ji.call(t, n) && Yt(e4, n, t[n]);
  if (Ht)
    for (var n of Ht(t))
      Zi.call(t, n) && Yt(e4, n, t[n]);
  return e4;
};
var fe = (e4, t) => Qi(e4, Xi(t));
var H = (e4, t, n) => new Promise((r2, i2) => {
  var o2 = (c) => {
    try {
      a(n.next(c));
    } catch (u) {
      i2(u);
    }
  }, s = (c) => {
    try {
      a(n.throw(c));
    } catch (u) {
      i2(u);
    }
  }, a = (c) => c.done ? r2(c.value) : Promise.resolve(c.value).then(o2, s);
  a((n = n.apply(e4, t)).next());
});
var eo = (e4, t, n) => (t = e4[zt("asyncIterator")]) ? t.call(e4) : (e4 = e4[zt("iterator")](), t = {}, n = (r2, i2) => (i2 = e4[r2]) && (t[r2] = (o2) => new Promise((s, a, c) => (o2 = i2.call(e4, o2), c = o2.done, Promise.resolve(o2.value).then((u) => s({ value: u, done: c }), a)))), n("next"), n("return"), t);
var kn = "lk";
function q(e4) {
  return typeof e4 > "u" ? false : to(e4) || no(e4);
}
function to(e4) {
  var t;
  return e4 ? e4.hasOwnProperty("participant") && e4.hasOwnProperty("source") && e4.hasOwnProperty("track") && typeof ((t = e4.publication) == null ? void 0 : t.track) < "u" : false;
}
function no(e4) {
  return e4 ? e4.hasOwnProperty("participant") && e4.hasOwnProperty("source") && e4.hasOwnProperty("publication") && typeof e4.publication < "u" : false;
}
function ke(e4) {
  return e4 ? e4.hasOwnProperty("participant") && e4.hasOwnProperty("source") && typeof e4.publication > "u" : false;
}
function V(e4) {
  if (typeof e4 == "string" || typeof e4 == "number")
    return `${e4}`;
  if (ke(e4))
    return `${e4.participant.identity}_${e4.source}_placeholder`;
  if (q(e4))
    return `${e4.participant.identity}_${e4.publication.source}_${e4.publication.trackSid}`;
  throw new Error(`Can't generate a id for the given track reference: ${e4}`);
}
function ts(e4, t) {
  return e4 === void 0 || t === void 0 ? false : q(e4) && q(t) ? e4.publication.trackSid === t.publication.trackSid : V(e4) === V(t);
}
function ns(e4, t) {
  return typeof t > "u" ? false : q(e4) ? t.some(
    (n) => n.participant.identity === e4.participant.identity && q(n) && n.publication.trackSid === e4.publication.trackSid
  ) : ke(e4) ? t.some(
    (n) => n.participant.identity === e4.participant.identity && ke(n) && n.source === e4.source
  ) : false;
}
function ro(e4, t) {
  return ke(e4) && q(t) && t.participant.identity === e4.participant.identity && t.source === e4.source;
}
function rs() {
  const e4 = document.createElement("p");
  e4.style.width = "100%", e4.style.height = "200px";
  const t = document.createElement("div");
  t.style.position = "absolute", t.style.top = "0px", t.style.left = "0px", t.style.visibility = "hidden", t.style.width = "200px", t.style.height = "150px", t.style.overflow = "hidden", t.appendChild(e4), document.body.appendChild(t);
  const n = e4.offsetWidth;
  t.style.overflow = "scroll";
  let r2 = e4.offsetWidth;
  return n === r2 && (r2 = t.clientWidth), document.body.removeChild(t), n - r2;
}
function is() {
  return typeof document < "u";
}
function io(e4) {
  e4 = J({}, e4);
  const t = "(?:(?:[a-z]+:)?//)?", n = "(?:\\S+(?::\\S*)?@)?", r2 = new RegExp(
    "(?:25[0-5]|2[0-4]\\d|1\\d\\d|[1-9]\\d|\\d)(?:\\.(?:25[0-5]|2[0-4]\\d|1\\d\\d|[1-9]\\d|\\d)){3}",
    "g"
  ).source, u = `(?:${t}|www\\.)${n}(?:localhost|${r2}|(?:(?:[a-z\\u00a1-\\uffff0-9][-_]*)*[a-z\\u00a1-\\uffff0-9]+)(?:\\.(?:[a-z\\u00a1-\\uffff0-9]-*)*[a-z\\u00a1-\\uffff0-9]+)*(?:\\.(?:[a-z\\u00a1-\\uffff]{2,}))\\.?)(?::\\d{2,5})?(?:[/?#][^\\s"]*)?`;
  return e4.exact ? new RegExp(`(?:^${u}$)`, "i") : new RegExp(u, "ig");
}
var Kt = "[^\\.\\s@:](?:[^\\s@:]*[^\\s@:\\.])?@[^\\.\\s@]+(?:\\.[^\\.\\s@]+)*";
function oo({ exact: e4 } = {}) {
  return e4 ? new RegExp(`^${Kt}$`) : new RegExp(Kt, "g");
}
function os(e4, t, n) {
  return $r(e4, t, () => H(null, null, function* () {
    const { x: i2, y: o2 } = yield Wr(e4, t, {
      placement: "top",
      middleware: [Nr(6), Ur(), Fr({ padding: 5 })]
    });
    n == null ? void 0 : n(i2, o2);
  }));
}
function ss(e4, t) {
  return !e4.contains(t.target);
}
var as = () => ({
  email: oo(),
  url: io({})
});
function cs(e4, t) {
  const n = Object.entries(t).map(
    ([o2, s], a) => Array.from(e4.matchAll(s)).map(({ index: c, 0: u }) => ({
      type: o2,
      weight: a,
      content: u,
      index: c ?? 0
    }))
  ).flat().sort((o2, s) => {
    const a = o2.index - s.index;
    return a !== 0 ? a : o2.weight - s.weight;
  }).filter(({ index: o2 }, s, a) => {
    if (s === 0) return true;
    const c = a[s - 1];
    return c.index + c.content.length <= o2;
  }), r2 = [];
  let i2 = 0;
  for (const { type: o2, content: s, index: a } of n)
    a > i2 && r2.push(e4.substring(i2, a)), r2.push({ type: o2, content: s }), i2 = a + s.length;
  return e4.length > i2 && r2.push(e4.substring(i2)), r2;
}
var so = [
  RoomEvent.ConnectionStateChanged,
  RoomEvent.RoomMetadataChanged,
  RoomEvent.ActiveSpeakersChanged,
  RoomEvent.ConnectionQualityChanged,
  RoomEvent.ParticipantConnected,
  RoomEvent.ParticipantDisconnected,
  RoomEvent.ParticipantPermissionsChanged,
  RoomEvent.ParticipantMetadataChanged,
  RoomEvent.ParticipantNameChanged,
  RoomEvent.ParticipantAttributesChanged,
  RoomEvent.TrackMuted,
  RoomEvent.TrackUnmuted,
  RoomEvent.TrackPublished,
  RoomEvent.TrackUnpublished,
  RoomEvent.TrackStreamStateChanged,
  RoomEvent.TrackSubscriptionFailed,
  RoomEvent.TrackSubscriptionPermissionChanged,
  RoomEvent.TrackSubscriptionStatusChanged
];
var Ln = [
  ...so,
  RoomEvent.LocalTrackPublished,
  RoomEvent.LocalTrackUnpublished
];
var ao = [
  ParticipantEvent.TrackPublished,
  ParticipantEvent.TrackUnpublished,
  ParticipantEvent.TrackMuted,
  ParticipantEvent.TrackUnmuted,
  ParticipantEvent.TrackStreamStateChanged,
  ParticipantEvent.TrackSubscribed,
  ParticipantEvent.TrackUnsubscribed,
  ParticipantEvent.TrackSubscriptionPermissionChanged,
  ParticipantEvent.TrackSubscriptionFailed,
  ParticipantEvent.LocalTrackPublished,
  ParticipantEvent.LocalTrackUnpublished
];
var co = [
  ParticipantEvent.ConnectionQualityChanged,
  ParticipantEvent.IsSpeakingChanged,
  ParticipantEvent.ParticipantMetadataChanged,
  ParticipantEvent.ParticipantPermissionsChanged,
  ParticipantEvent.TrackMuted,
  ParticipantEvent.TrackUnmuted,
  ParticipantEvent.TrackPublished,
  ParticipantEvent.TrackUnpublished,
  ParticipantEvent.TrackStreamStateChanged,
  ParticipantEvent.TrackSubscriptionFailed,
  ParticipantEvent.TrackSubscriptionPermissionChanged,
  ParticipantEvent.TrackSubscriptionStatusChanged
];
var In = [
  ...co,
  ParticipantEvent.LocalTrackPublished,
  ParticipantEvent.LocalTrackUnpublished
];
var I = zr.getLogger("lk-components-js");
I.setDefaultLevel("WARN");
function us(e4, t = {}) {
  var n;
  I.setLevel(e4), setLogLevel((n = t.liveKitClientLogLevel) != null ? n : e4);
}
function ls(e4, t = {}) {
  var n;
  const r2 = I.methodFactory;
  I.methodFactory = (i2, o2, s) => {
    const a = r2(i2, o2, s), c = LogLevel[i2], u = c >= o2 && c < LogLevel.silent;
    return (l, f) => {
      f ? a(l, f) : a(l), u && e4(c, l, f);
    };
  }, I.setLevel(I.getLevel()), setLogExtension((n = t.liveKitClientLogExtension) != null ? n : e4);
}
var fs = [
  {
    columns: 1,
    rows: 1
  },
  {
    columns: 1,
    rows: 2,
    orientation: "portrait"
  },
  {
    columns: 2,
    rows: 1,
    orientation: "landscape"
  },
  {
    columns: 2,
    rows: 2,
    minWidth: 560
  },
  {
    columns: 3,
    rows: 3,
    minWidth: 700
  },
  {
    columns: 4,
    rows: 4,
    minWidth: 960
  },
  {
    columns: 5,
    rows: 5,
    minWidth: 1100
  }
];
function uo(e4, t, n, r2) {
  if (e4.length < 1)
    throw new Error("At least one grid layout definition must be provided.");
  const i2 = lo(e4);
  if (n <= 0 || r2 <= 0)
    return i2[0];
  let o2 = 0;
  const s = n / r2 > 1 ? "landscape" : "portrait";
  let a = i2.find((c, u, l) => {
    o2 = u;
    const f = l.findIndex((h2, d) => {
      const m = !h2.orientation || h2.orientation === s, p = d > u, g = h2.maxTiles === c.maxTiles;
      return p && g && m;
    }) !== -1;
    return c.maxTiles >= t && !f;
  });
  if (a === void 0)
    if (a = i2[i2.length - 1], a)
      I.warn(
        `No layout found for: participantCount: ${t}, width/height: ${n}/${r2} fallback to biggest available layout (${a}).`
      );
    else
      throw new Error("No layout or fallback layout found.");
  if ((n < a.minWidth || r2 < a.minHeight) && o2 > 0) {
    const c = i2[o2 - 1];
    a = uo(
      i2.slice(0, o2),
      c.maxTiles,
      n,
      r2
    );
  }
  return a;
}
function lo(e4) {
  return [...e4].map((t) => {
    var n, r2;
    return {
      name: `${t.columns}x${t.rows}`,
      columns: t.columns,
      rows: t.rows,
      maxTiles: t.columns * t.rows,
      minWidth: (n = t.minWidth) != null ? n : 0,
      minHeight: (r2 = t.minHeight) != null ? r2 : 0,
      orientation: t.orientation
    };
  }).sort((t, n) => t.maxTiles !== n.maxTiles ? t.maxTiles - n.maxTiles : t.minWidth !== 0 || n.minWidth !== 0 ? t.minWidth - n.minWidth : t.minHeight !== 0 || n.minHeight !== 0 ? t.minHeight - n.minHeight : 0);
}
function ds() {
  return typeof navigator < "u" && navigator.mediaDevices && !!navigator.mediaDevices.getDisplayMedia;
}
function ps(e4, t) {
  var n;
  return fe(J({}, e4), {
    receivedAtMediaTimestamp: (n = t.rtpTimestamp) != null ? n : 0,
    receivedAt: t.timestamp
  });
}
function hs(e4, t, n) {
  return [...e4, ...t].reduceRight((r2, i2) => (r2.find((o2) => o2.id === i2.id) || r2.unshift(i2), r2), []).slice(0 - n);
}
var fo = ((e4) => (e4.AgentState = "lk.agent.state", e4.PublishOnBehalf = "lk.publish_on_behalf", e4.TranscriptionFinal = "lk.transcription_final", e4.TranscriptionSegmentId = "lk.segment_id", e4.TranscribedTrackId = "lk.transcribed_track_id", e4))(fo || {});
var Rn = [];
var Mn = {
  showChat: false,
  unreadMessages: 0,
  showSettings: false
};
function po(e4) {
  return typeof e4 == "object";
}
function vs(e4) {
  return Array.isArray(e4) && e4.filter(po).length > 0;
}
function Dn(e4, t) {
  return t.audioLevel - e4.audioLevel;
}
function $n(e4, t) {
  return e4.isSpeaking === t.isSpeaking ? 0 : e4.isSpeaking ? -1 : 1;
}
function Nn(e4, t) {
  var n, r2, i2, o2;
  return e4.lastSpokeAt !== void 0 || t.lastSpokeAt !== void 0 ? ((r2 = (n = t.lastSpokeAt) == null ? void 0 : n.getTime()) != null ? r2 : 0) - ((o2 = (i2 = e4.lastSpokeAt) == null ? void 0 : i2.getTime()) != null ? o2 : 0) : 0;
}
function Ke(e4, t) {
  var n, r2, i2, o2;
  return ((r2 = (n = e4.joinedAt) == null ? void 0 : n.getTime()) != null ? r2 : 0) - ((o2 = (i2 = t.joinedAt) == null ? void 0 : i2.getTime()) != null ? o2 : 0);
}
function ho(e4, t) {
  return q(e4) ? q(t) ? 0 : -1 : q(t) ? 1 : 0;
}
function vo(e4, t) {
  const n = e4.participant.isCameraEnabled, r2 = t.participant.isCameraEnabled;
  return n !== r2 ? n ? -1 : 1 : 0;
}
function ms(e4) {
  const t = [], n = [], r2 = [], i2 = [];
  e4.forEach((a) => {
    a.participant.isLocal && a.source === Track.Source.Camera ? t.push(a) : a.source === Track.Source.ScreenShare ? n.push(a) : a.source === Track.Source.Camera ? r2.push(a) : i2.push(a);
  });
  const o2 = mo(n), s = go(r2);
  return [...t, ...o2, ...s, ...i2];
}
function mo(e4) {
  const t = [], n = [];
  return e4.forEach((i2) => {
    i2.participant.isLocal ? t.push(i2) : n.push(i2);
  }), t.sort((i2, o2) => Ke(i2.participant, o2.participant)), n.sort((i2, o2) => Ke(i2.participant, o2.participant)), [...n, ...t];
}
function go(e4) {
  const t = [], n = [];
  return e4.forEach((r2) => {
    r2.participant.isLocal ? t.push(r2) : n.push(r2);
  }), n.sort((r2, i2) => r2.participant.isSpeaking && i2.participant.isSpeaking ? Dn(r2.participant, i2.participant) : r2.participant.isSpeaking !== i2.participant.isSpeaking ? $n(r2.participant, i2.participant) : r2.participant.lastSpokeAt !== i2.participant.lastSpokeAt ? Nn(r2.participant, i2.participant) : q(r2) !== q(i2) ? ho(r2, i2) : r2.participant.isCameraEnabled !== i2.participant.isCameraEnabled ? vo(r2, i2) : Ke(r2.participant, i2.participant)), [...t, ...n];
}
function gs(e4) {
  const t = [...e4];
  t.sort((r2, i2) => {
    if (r2.isSpeaking && i2.isSpeaking)
      return Dn(r2, i2);
    if (r2.isSpeaking !== i2.isSpeaking)
      return $n(r2, i2);
    if (r2.lastSpokeAt !== i2.lastSpokeAt)
      return Nn(r2, i2);
    const o2 = r2.videoTrackPublications.size > 0, s = i2.videoTrackPublications.size > 0;
    return o2 !== s ? o2 ? -1 : 1 : Ke(r2, i2);
  });
  const n = t.find((r2) => r2.isLocal);
  if (n) {
    const r2 = t.indexOf(n);
    r2 >= 0 && (t.splice(r2, 1), t.length > 0 ? t.splice(0, 0, n) : t.push(n));
  }
  return t;
}
function bo(e4, t) {
  return e4.reduce(
    (n, r2, i2) => i2 % t === 0 ? [...n, [r2]] : [...n.slice(0, -1), [...n.slice(-1)[0], r2]],
    []
  );
}
function qt(e4, t) {
  const n = Math.max(e4.length, t.length);
  return new Array(n).fill([]).map((r2, i2) => [e4[i2], t[i2]]);
}
function qe(e4, t, n) {
  return e4.filter((r2) => !t.map((i2) => n(i2)).includes(n(r2)));
}
function mt(e4) {
  return e4.map((t) => typeof t == "string" || typeof t == "number" ? `${t}` : V(t));
}
function yo(e4, t) {
  return {
    dropped: qe(e4, t, V),
    added: qe(t, e4, V)
  };
}
function wo(e4) {
  return e4.added.length !== 0 || e4.dropped.length !== 0;
}
function gt(e4, t) {
  const n = t.findIndex(
    (r2) => V(r2) === V(e4)
  );
  if (n === -1)
    throw new Error(
      `Element not part of the array: ${V(
        e4
      )} not in ${mt(t)}`
    );
  return n;
}
function xo(e4, t, n) {
  const r2 = gt(e4, n), i2 = gt(t, n);
  return n.splice(r2, 1, t), n.splice(i2, 1, e4), n;
}
function So(e4, t) {
  const n = gt(e4, t);
  return t.splice(n, 1), t;
}
function Co(e4, t) {
  return [...t, e4];
}
function ct(e4, t) {
  return bo(e4, t);
}
function bs(e4, t, n) {
  let r2 = Eo(e4, t);
  if (r2.length < t.length) {
    const s = qe(t, r2, V);
    r2 = [...r2, ...s];
  }
  const i2 = ct(r2, n), o2 = ct(t, n);
  if (qt(i2, o2).forEach(([s, a], c) => {
    if (s && a) {
      const u = ct(r2, n)[c], l = yo(u, a);
      wo(l) && (I.debug(
        `Detected visual changes on page: ${c}, current: ${mt(
          s
        )}, next: ${mt(a)}`,
        { changes: l }
      ), l.added.length === l.dropped.length && qt(l.added, l.dropped).forEach(([f, h2]) => {
        if (f && h2)
          r2 = xo(f, h2, r2);
        else
          throw new Error(
            `For a swap action we need a addition and a removal one is missing: ${f}, ${h2}`
          );
      }), l.added.length === 0 && l.dropped.length > 0 && l.dropped.forEach((f) => {
        r2 = So(f, r2);
      }), l.added.length > 0 && l.dropped.length === 0 && l.added.forEach((f) => {
        r2 = Co(f, r2);
      }));
    }
  }), r2.length > t.length) {
    const s = qe(r2, t, V);
    r2 = r2.filter(
      (a) => !s.map(V).includes(V(a))
    );
  }
  return r2;
}
function Eo(e4, t) {
  return e4.map((n) => {
    const r2 = t.find(
      (i2) => (
        // If the IDs match or ..
        V(n) === V(i2) || // ... if the current item is a placeholder and the new item is the track reference can replace it.
        typeof n != "number" && ke(n) && q(i2) && ro(n, i2)
      )
    );
    return r2 ?? n;
  });
}
function Y(e4) {
  return `${kn}-${e4}`;
}
function ys(e4) {
  const t = Gt(e4), n = Fn(e4.participant).pipe(
    _2(() => Gt(e4)),
    U(t)
  );
  return { className: Y(
    e4.source === Track.Source.Camera || e4.source === Track.Source.ScreenShare ? "participant-media-video" : "participant-media-audio"
  ), trackObserver: n };
}
function Gt(e4) {
  if (q(e4))
    return e4.publication;
  {
    const { source: t, name: n, participant: r2 } = e4;
    if (t && n)
      return r2.getTrackPublications().find((i2) => i2.source === t && i2.trackName === n);
    if (n)
      return r2.getTrackPublicationByName(n);
    if (t)
      return r2.getTrackPublication(t);
    throw new Error("At least one of source and name needs to be defined");
  }
}
function me(e4, ...t) {
  return new L((r2) => {
    const i2 = () => {
      r2.next(e4);
    };
    return t.forEach((s) => {
      e4.on(s, i2);
    }), () => {
      t.forEach((s) => {
        e4.off(s, i2);
      });
    };
  }).pipe(U(e4));
}
function Ee(e4, t) {
  return new L((r2) => {
    const i2 = (...s) => {
      r2.next(s);
    };
    return e4.on(t, i2), () => {
      e4.off(t, i2);
    };
  });
}
function ws(e4) {
  return Ee(e4, RoomEvent.ConnectionStateChanged).pipe(
    _2(([t]) => t),
    U(e4.state)
  );
}
function xs(e4) {
  return me(
    e4,
    RoomEvent.RoomMetadataChanged,
    RoomEvent.ConnectionStateChanged
  ).pipe(
    _2((n) => ({ name: n.name, metadata: n.metadata }))
  );
}
function Ss(e4) {
  return Ee(e4, RoomEvent.ActiveSpeakersChanged).pipe(
    _2(([t]) => t)
  );
}
function Cs(e4, t, n = true) {
  const r2 = new L((o2) => {
    Room.getLocalDevices(e4, n).then((s) => {
      o2.next(s), o2.complete();
    }).catch((s) => {
      t == null ? void 0 : t(s), o2.next([]), o2.complete();
    });
  }), i2 = new L((o2) => {
    var s;
    const a = () => H(null, null, function* () {
      try {
        const c = yield Room.getLocalDevices(e4, n);
        o2.next(c);
      } catch (c) {
        t == null ? void 0 : t(c);
      }
    });
    if (typeof window < "u") {
      if (!window.isSecureContext)
        throw new Error(
          "Accessing media devices is available only in secure contexts (HTTPS and localhost), in some or all supporting browsers. See: https://developer.mozilla.org/en-US/docs/Web/API/Navigator/mediaDevices"
        );
      (s = navigator == null ? void 0 : navigator.mediaDevices) == null || s.addEventListener("devicechange", a);
    }
    return () => {
      var c;
      (c = navigator == null ? void 0 : navigator.mediaDevices) == null || c.removeEventListener("devicechange", a);
    };
  });
  return Ye(r2, i2);
}
function To(e4) {
  return Ee(e4, RoomEvent.DataReceived);
}
function Po(e4) {
  return me(e4, RoomEvent.AudioPlaybackStatusChanged).pipe(
    _2((n) => ({ canPlayAudio: n.canPlaybackAudio }))
  );
}
function Oo(e4) {
  return me(e4, RoomEvent.VideoPlaybackStatusChanged).pipe(
    _2((n) => ({ canPlayVideo: n.canPlaybackVideo }))
  );
}
function Ao(e4, t) {
  return Ee(e4, RoomEvent.ActiveDeviceChanged).pipe(
    nt(([n]) => n === t),
    _2(([n, r2]) => (I.debug("activeDeviceObservable | RoomEvent.ActiveDeviceChanged", { kind: n, deviceId: r2 }), r2))
  );
}
function Es(e4, t) {
  return Ee(e4, RoomEvent.ParticipantEncryptionStatusChanged).pipe(
    nt(
      ([, n]) => (t == null ? void 0 : t.identity) === (n == null ? void 0 : n.identity) || !n && (t == null ? void 0 : t.identity) === e4.localParticipant.identity
    ),
    _2(([n]) => n),
    U(
      (t == null ? void 0 : t.isLocal) ? t.isE2EEEnabled : !!(t == null ? void 0 : t.isEncrypted)
    )
  );
}
function Ts(e4) {
  return Ee(e4, RoomEvent.RecordingStatusChanged).pipe(
    _2(([t]) => t),
    U(e4.isRecording)
  );
}
function Te(e4, ...t) {
  return new L((r2) => {
    const i2 = () => {
      r2.next(e4);
    };
    return t.forEach((s) => {
      e4.on(s, i2);
    }), () => {
      t.forEach((s) => {
        e4.off(s, i2);
      });
    };
  }).pipe(U(e4));
}
function Fn(e4) {
  return Te(
    e4,
    ParticipantEvent.TrackMuted,
    ParticipantEvent.TrackUnmuted,
    ParticipantEvent.ParticipantPermissionsChanged,
    // ParticipantEvent.IsSpeakingChanged,
    ParticipantEvent.TrackPublished,
    ParticipantEvent.TrackUnpublished,
    ParticipantEvent.LocalTrackPublished,
    ParticipantEvent.LocalTrackUnpublished,
    ParticipantEvent.MediaDevicesError,
    ParticipantEvent.TrackSubscriptionStatusChanged
    // ParticipantEvent.ConnectionQualityChanged,
  ).pipe(
    _2((n) => {
      const { isMicrophoneEnabled: r2, isCameraEnabled: i2, isScreenShareEnabled: o2 } = n, s = n.getTrackPublication(Track.Source.Microphone), a = n.getTrackPublication(Track.Source.Camera);
      return {
        isCameraEnabled: i2,
        isMicrophoneEnabled: r2,
        isScreenShareEnabled: o2,
        cameraTrack: a,
        microphoneTrack: s,
        participant: n
      };
    })
  );
}
function _o(e4) {
  return e4 ? Te(
    e4,
    ParticipantEvent.ParticipantMetadataChanged,
    ParticipantEvent.ParticipantNameChanged
  ).pipe(
    _2(({ name: n, identity: r2, metadata: i2 }) => ({
      name: n,
      identity: r2,
      metadata: i2
    })),
    U({
      name: e4.name,
      identity: e4.identity,
      metadata: e4.metadata
    })
  ) : void 0;
}
function ko(e4) {
  return rt(
    e4,
    ParticipantEvent.ConnectionQualityChanged
  ).pipe(
    _2(([n]) => n),
    U(e4.connectionQuality)
  );
}
function rt(e4, t) {
  return new L((r2) => {
    const i2 = (...s) => {
      r2.next(s);
    };
    return e4.on(t, i2), () => {
      e4.off(t, i2);
    };
  });
}
function Lo(e4) {
  var t, n, r2, i2;
  return Te(
    e4.participant,
    ParticipantEvent.TrackMuted,
    ParticipantEvent.TrackUnmuted,
    ParticipantEvent.TrackSubscribed,
    ParticipantEvent.TrackUnsubscribed,
    ParticipantEvent.LocalTrackPublished,
    ParticipantEvent.LocalTrackUnpublished
  ).pipe(
    _2((o2) => {
      var s, a;
      const c = (s = e4.publication) != null ? s : o2.getTrackPublication(e4.source);
      return (a = c == null ? void 0 : c.isMuted) != null ? a : true;
    }),
    U(
      (i2 = (r2 = (t = e4.publication) == null ? void 0 : t.isMuted) != null ? r2 : (n = e4.participant.getTrackPublication(e4.source)) == null ? void 0 : n.isMuted) != null ? i2 : true
    )
  );
}
function Ps(e4) {
  return rt(e4, ParticipantEvent.IsSpeakingChanged).pipe(
    _2(([t]) => t)
  );
}
function Os(e4, t = {}) {
  var n;
  let r2;
  const i2 = new L((c) => (r2 = c, () => a.unsubscribe())).pipe(U(Array.from(e4.remoteParticipants.values()))), o2 = (n = t.additionalRoomEvents) != null ? n : Ln, s = Array.from(
    /* @__PURE__ */ new Set([
      RoomEvent.ParticipantConnected,
      RoomEvent.ParticipantDisconnected,
      RoomEvent.ConnectionStateChanged,
      ...o2
    ])
  ), a = me(e4, ...s).subscribe(
    (c) => r2 == null ? void 0 : r2.next(Array.from(c.remoteParticipants.values()))
  );
  return e4.remoteParticipants.size > 0 && (r2 == null ? void 0 : r2.next(Array.from(e4.remoteParticipants.values()))), i2;
}
function As(e4, t, n = {}) {
  var r2;
  const i2 = (r2 = n.additionalEvents) != null ? r2 : In;
  return me(
    e4,
    RoomEvent.ParticipantConnected,
    RoomEvent.ParticipantDisconnected,
    RoomEvent.ConnectionStateChanged
  ).pipe(
    _n((s) => {
      const a = s.getParticipantByIdentity(t);
      return a ? Te(a, ...i2) : new L((c) => c.next(void 0));
    }),
    U(e4.getParticipantByIdentity(t))
  );
}
function _s(e4) {
  return rt(
    e4,
    ParticipantEvent.ParticipantPermissionsChanged
  ).pipe(
    _2(() => e4.permissions),
    U(e4.permissions)
  );
}
function ks(e4, { kind: t, identity: n }, r2 = {}) {
  var i2;
  const o2 = (i2 = r2.additionalEvents) != null ? i2 : In, s = (c) => {
    let u = true;
    return t && (u = u && c.kind === t), n && (u = u && c.identity === n), u;
  };
  return me(
    e4,
    RoomEvent.ParticipantConnected,
    RoomEvent.ParticipantDisconnected,
    RoomEvent.ConnectionStateChanged
  ).pipe(
    _n((c) => {
      const u = Array.from(c.remoteParticipants.values()).find(
        (l) => s(l)
      );
      return u ? Te(u, ...o2) : new L((l) => l.next(void 0));
    }),
    U(Array.from(e4.remoteParticipants.values()).find((c) => s(c)))
  );
}
function Ls(e4) {
  return typeof e4 > "u" ? new L() : rt(e4, ParticipantEvent.AttributesChanged).pipe(
    _2(([t]) => ({
      changed: t,
      attributes: e4.attributes
    })),
    U({ changed: e4.attributes, attributes: e4.attributes })
  );
}
function Is(e4, t, n, r2, i2) {
  const { localParticipant: o2 } = t, s = (f, h2) => {
    let d = false;
    switch (f) {
      case Track.Source.Camera:
        d = h2.isCameraEnabled;
        break;
      case Track.Source.Microphone:
        d = h2.isMicrophoneEnabled;
        break;
      case Track.Source.ScreenShare:
        d = h2.isScreenShareEnabled;
        break;
    }
    return d;
  }, a = Fn(o2).pipe(
    _2((f) => s(e4, f.participant)),
    U(s(e4, o2))
  ), c = new re(), u = (f, h2) => H(null, null, function* () {
    try {
      switch (h2 ?? (h2 = n), c.next(true), e4) {
        case Track.Source.Camera:
          return yield o2.setCameraEnabled(
            f ?? !o2.isCameraEnabled,
            h2,
            r2
          ), o2.isCameraEnabled;
        case Track.Source.Microphone:
          return yield o2.setMicrophoneEnabled(
            f ?? !o2.isMicrophoneEnabled,
            h2,
            r2
          ), o2.isMicrophoneEnabled;
        case Track.Source.ScreenShare:
          return yield o2.setScreenShareEnabled(
            f ?? !o2.isScreenShareEnabled,
            h2,
            r2
          ), o2.isScreenShareEnabled;
        default:
          throw new TypeError("Tried to toggle unsupported source");
      }
    } catch (d) {
      if (i2 && d instanceof Error) {
        i2 == null ? void 0 : i2(d);
        return;
      } else
        throw d;
    } finally {
      c.next(false);
    }
  });
  return {
    className: Y("button"),
    toggle: u,
    enabledObserver: a,
    pendingObserver: c.asObservable()
  };
}
function Rs() {
  let e4 = false;
  const t = new re(), n = new re(), r2 = (o2) => H(null, null, function* () {
    n.next(true), e4 = o2 ?? !e4, t.next(e4), n.next(false);
  });
  return {
    className: Y("button"),
    toggle: r2,
    enabledObserver: t.asObservable(),
    pendingObserver: n.asObservable()
  };
}
function Ms(e4, t, n) {
  const r2 = new mn(void 0), i2 = Ao(t, e4), o2 = (a, ...c) => H(null, [a, ...c], function* (u, l = {}) {
    var f, h2, d;
    if (n) {
      yield n.setDeviceId(l.exact ? { exact: u } : u);
      const m = yield n.getDeviceId(false);
      r2.next(
        u === "default" && n.mediaStreamTrack.label.startsWith("Default") ? u : m
      );
    } else if (t) {
      const m = getBrowser();
      if (e4 === "audiooutput" && ((m == null ? void 0 : m.name) === "Safari" || (m == null ? void 0 : m.os) === "iOS")) {
        I.warn("Switching audio output device is not supported on Safari and iOS.");
        return;
      }
      I.debug(`Switching active device of kind "${e4}" with id ${u}.`), yield t.switchActiveDevice(e4, u, l.exact);
      const p = (f = t.getActiveDevice(e4)) != null ? f : u;
      p !== u && u !== "default" && I.info(
        `We tried to select the device with id (${u}), but the browser decided to select the device with id (${p}) instead.`
      );
      let g;
      e4 === "audioinput" ? g = (h2 = t.localParticipant.getTrackPublication(Track.Source.Microphone)) == null ? void 0 : h2.track : e4 === "videoinput" && (g = (d = t.localParticipant.getTrackPublication(Track.Source.Camera)) == null ? void 0 : d.track);
      const v = u === "default" && !g || u === "default" && (g == null ? void 0 : g.mediaStreamTrack.label.startsWith("Default"));
      r2.next(v ? u : p);
    }
  });
  return {
    className: Y("media-device-select"),
    activeDeviceObservable: i2,
    setActiveMediaDevice: o2
  };
}
function Ds(e4) {
  const t = (r2) => {
    e4.disconnect(r2);
  };
  return { className: Y("disconnect-button"), disconnect: t };
}
function $s(e4) {
  const t = Y("connection-quality"), n = ko(e4);
  return { className: t, connectionQualityObserver: n };
}
function Ns(e4) {
  let t = "track-muted-indicator-camera";
  switch (e4.source) {
    case Track.Source.Camera:
      t = "track-muted-indicator-camera";
      break;
    case Track.Source.Microphone:
      t = "track-muted-indicator-microphone";
      break;
  }
  const n = Y(t), r2 = Lo(e4);
  return { className: n, mediaMutedObserver: r2 };
}
function Fs(e4) {
  return { className: "lk-participant-name", infoObserver: _o(e4) };
}
function Us() {
  return {
    className: Y("participant-tile")
  };
}
var Io = {
  CHAT: "lk.chat",
  TRANSCRIPTION: "lk.transcription"
};
var Ro = {
  CHAT: "lk-chat-topic"
};
function Un(e4, t) {
  return H(this, arguments, function* (n, r2, i2 = {}) {
    const { reliable: o2, destinationIdentities: s, topic: a } = i2;
    yield n.publishData(r2, {
      destinationIdentities: s,
      topic: a,
      reliable: o2
    });
  });
}
function Mo(e4, t, n) {
  const r2 = Array.isArray(t) ? t : [t], i2 = To(e4).pipe(
    nt(
      ([, , , c]) => t === void 0 || c !== void 0 && r2.includes(c)
    ),
    _2(([c, u, , l]) => {
      const f = {
        payload: c,
        topic: l,
        from: u
      };
      return n == null ? void 0 : n(f), f;
    })
  );
  let o2;
  const s = new L((c) => {
    o2 = c;
  });
  return { messageObservable: i2, isSendingObservable: s, send: (c, ...u) => H(null, [c, ...u], function* (l, f = {}) {
    o2.next(true);
    try {
      yield Un(e4.localParticipant, l, J({ topic: r2[0] }, f));
    } finally {
      o2.next(false);
    }
  }) };
}
var Do = class {
  constructor(e4, t) {
    this._isResolved = false, this.onFinally = t, this.promise = new Promise((n, r2) => H(this, null, function* () {
      this.resolve = n, this.reject = r2, e4 && (yield e4(n, r2));
    })).finally(() => {
      var n;
      this._isResolved = true, (n = this.onFinally) == null || n.call(this);
    });
  }
  get isResolved() {
    return this._isResolved;
  }
};
var De = /* @__PURE__ */ new WeakMap();
var $e = /* @__PURE__ */ new Map();
function $o(e4) {
  return e4.ignoreLegacy == true;
}
var No = (e4) => JSON.parse(new TextDecoder().decode(e4));
var Fo = (e4) => new TextEncoder().encode(JSON.stringify(e4));
function Ws(e4, t) {
  var n, r2, i2, o2, s, a;
  const c = () => {
    var S, b, P;
    return ((S = e4.serverInfo) == null ? void 0 : S.edition) === 1 || !!((b = e4.serverInfo) != null && b.version) && compareVersions((P = e4.serverInfo) == null ? void 0 : P.version, "1.8.2") > 0;
  }, u = new re(), l = (n = t == null ? void 0 : t.channelTopic) != null ? n : Io.CHAT, f = (r2 = t == null ? void 0 : t.channelTopic) != null ? r2 : Ro.CHAT;
  let h2 = false;
  De.has(e4) || (h2 = true);
  const d = (i2 = De.get(e4)) != null ? i2 : /* @__PURE__ */ new Map(), m = (o2 = d.get(l)) != null ? o2 : new re();
  d.set(l, m), De.set(e4, d);
  const p = (s = t == null ? void 0 : t.messageDecoder) != null ? s : No;
  if (h2) {
    e4.registerTextStreamHandler(l, (b, P) => H(null, null, function* () {
      const { id: R, timestamp: M2, attributes: W3, attachedStreamIds: E } = b.info, O = new Map(
        (E ?? []).map((k) => [
          k,
          new Do()
        ])
      );
      $e.set(R, O), ye(b).pipe(
        Ue((k, $2) => k + $2),
        _e((k) => O.size === 0 ? ht({ chunk: k, attachedFiles: [] }) : ye(O.values()).pipe(
          _e(($2) => ye($2.promise)),
          Ue(
            ($2, G2) => [...$2, new File(G2.buffer, G2.fileName)],
            []
          ),
          _2(($2) => ({ chunk: k, attachedFiles: $2 }))
        )),
        _2(({ chunk: k, attachedFiles: $2 }) => ({
          id: R,
          timestamp: M2,
          message: k,
          from: e4.getParticipantByIdentity(P.identity),
          type: "chatMessage",
          attributes: W3,
          attachedFiles: $2
          // editTimestamp: type === 'update' ? timestamp : undefined,
        })),
        zi(() => $e.delete(R))
      ).subscribe({
        next: (k) => m.next(k)
      });
    })), e4.registerByteStreamHandler(l, (b) => H(null, null, function* () {
      var P, R;
      const { id: M2 } = b.info, W3 = Array.from($e).find(
        ([, N]) => N.has(M2)
      );
      if (!W3)
        return;
      const E = W3[0], O = [];
      try {
        for (var j2 = eo(b), k, $2, G2; k = !($2 = yield j2.next()).done; k = false) {
          const N = $2.value;
          O.push(N);
        }
      } catch (N) {
        G2 = [N];
      } finally {
        try {
          k && ($2 = j2.return) && (yield $2.call(j2));
        } finally {
          if (G2)
            throw G2[0];
        }
      }
      const X = (P = $e.get(E)) == null ? void 0 : P.get(M2);
      X && ((R = X.resolve) == null || R.call(X, {
        fileName: b.info.name,
        buffer: O
      }));
    }));
    const { messageObservable: S } = Mo(e4, [f]);
    S.pipe(
      _2((b) => {
        const P = p(b.payload);
        return $o(P) ? void 0 : fe(J({}, P), {
          type: "chatMessage",
          from: b.from
        });
      }),
      nt((b) => !!b),
      Vt(u)
    ).subscribe(m);
  }
  const g = m.pipe(
    Ue((S, b) => {
      if ("id" in b && S.find((P) => {
        var R, M2;
        return ((R = P.from) == null ? void 0 : R.identity) === ((M2 = b.from) == null ? void 0 : M2.identity) && P.id === b.id;
      })) {
        const P = S.findIndex((R) => R.id === b.id);
        if (P > -1) {
          const R = S[P];
          S[P] = fe(J({}, b), {
            timestamp: R.timestamp,
            editTimestamp: b.timestamp
          });
        }
        return [...S];
      }
      return [...S, b];
    }, []),
    Vt(u)
  ), v = new mn(false), x = (a = t == null ? void 0 : t.messageEncoder) != null ? a : Fo, C = (S, b) => H(null, null, function* () {
    var P;
    b || (b = {}), (P = b.topic) != null || (b.topic = l), v.next(true);
    try {
      const M2 = {
        id: (yield e4.localParticipant.sendText(S, b)).id,
        timestamp: Date.now(),
        message: S
      }, W3 = fe(J({}, M2), {
        attachedFiles: b.attachments
      }), E = fe(J({}, W3), {
        type: "chatMessage",
        from: e4.localParticipant,
        attributes: b.attributes
      });
      m.next(E);
      const O = x(fe(J({}, M2), {
        ignoreLegacy: c()
      }));
      try {
        yield Un(e4.localParticipant, O, {
          reliable: true,
          topic: f
        });
      } catch (j2) {
        I.info("could not send message in legacy chat format", j2);
      }
      return E;
    } finally {
      v.next(false);
    }
  });
  function T2() {
    u.next(), u.complete(), m.complete(), De.delete(e4), e4.unregisterTextStreamHandler(l), e4.unregisterByteStreamHandler(l);
  }
  return e4.once(RoomEvent.Disconnected, T2), {
    messageObservable: g,
    isSendingObservable: v,
    send: C
  };
}
function Bs() {
  const e4 = (n) => H(null, null, function* () {
    I.info("Start Audio for room: ", n), yield n.startAudio();
  });
  return { className: Y("start-audio-button"), roomAudioPlaybackAllowedObservable: Po, handleStartAudioPlayback: e4 };
}
function js() {
  const e4 = (n) => H(null, null, function* () {
    I.info("Start Video for room: ", n), yield n.startVideo();
  });
  return { className: Y("start-audio-button"), roomVideoPlaybackAllowedObservable: Oo, handleStartVideoPlayback: e4 };
}
function Vs() {
  return { className: [Y("button"), Y("chat-toggle")].join(" ") };
}
function Hs() {
  return { className: [Y("button"), Y("focus-toggle-button")].join(" ") };
}
function zs() {
  return { className: "lk-clear-pin-button lk-button" };
}
function Ys() {
  return { className: "lk-room-container" };
}
function Qt(e4, t, n = true) {
  const i2 = [e4.localParticipant, ...Array.from(e4.remoteParticipants.values())], o2 = [];
  return i2.forEach((s) => {
    t.forEach((a) => {
      const c = Array.from(
        s.trackPublications.values()
      ).filter(
        (u) => u.source === a && // either return all or only the ones that are subscribed
        (!n || u.track)
      ).map((u) => ({
        participant: s,
        publication: u,
        source: u.source
      }));
      o2.push(...c);
    });
  }), { trackReferences: o2, participants: i2 };
}
function Xt(e4, t, n = false) {
  const { sources: r2, kind: i2, name: o2 } = t;
  return Array.from(e4.trackPublications.values()).filter(
    (a) => (!r2 || r2.includes(a.source)) && (!i2 || a.kind === i2) && (!o2 || a.trackName === o2) && // either return all or only the ones that are subscribed
    (!n || a.track)
  ).map((a) => ({
    participant: e4,
    publication: a,
    source: a.source
  }));
}
function Ks(e4, t, n) {
  var r2, i2;
  const o2 = (r2 = n.additionalRoomEvents) != null ? r2 : Ln, s = (i2 = n.onlySubscribed) != null ? i2 : true, a = Array.from(
    (/* @__PURE__ */ new Set([
      RoomEvent.ParticipantConnected,
      RoomEvent.ParticipantDisconnected,
      RoomEvent.ConnectionStateChanged,
      RoomEvent.LocalTrackPublished,
      RoomEvent.LocalTrackUnpublished,
      RoomEvent.TrackPublished,
      RoomEvent.TrackUnpublished,
      RoomEvent.TrackSubscriptionStatusChanged,
      ...o2
    ])).values()
  );
  return me(e4, ...a).pipe(
    _2((u) => {
      const l = Qt(u, t, s);
      return I.debug(`TrackReference[] was updated. (length ${l.trackReferences.length})`, l), l;
    }),
    U(Qt(e4, t, s))
  );
}
function qs(e4, t) {
  return Te(e4, ...ao).pipe(
    _2((r2) => {
      const i2 = Xt(r2, t);
      return I.debug(`TrackReference[] was updated. (length ${i2.length})`, i2), i2;
    }),
    U(Xt(e4, t))
  );
}
function Wn(e4, t) {
  return new L((r2) => {
    const i2 = (...s) => {
      r2.next(s);
    };
    return e4.on(t, i2), () => {
      e4.off(t, i2);
    };
  });
}
function Gs(e4) {
  return Wn(e4, TrackEvent.TranscriptionReceived);
}
function Qs(e4) {
  return Wn(e4, TrackEvent.TimeSyncUpdate).pipe(
    _2(([t]) => t)
  );
}
function Xs(e4, t = 1e3) {
  if (e4 === null) return ht(false);
  const n = vt(e4, "mousemove", { passive: true }).pipe(_2(() => true)), r2 = n.pipe(
    Ai({
      each: t,
      with: () => Ye(ht(false), r2.pipe(Ki(n)))
    }),
    Vi()
  );
  return r2;
}
function Uo(e4, t) {
  if (typeof localStorage > "u") {
    I.error("Local storage is not available.");
    return;
  }
  try {
    if (t) {
      const n = Object.fromEntries(
        Object.entries(t).filter(([, r2]) => r2 !== "")
      );
      localStorage.setItem(e4, JSON.stringify(n));
    }
  } catch (n) {
    I.error(`Error setting item to local storage: ${n}`);
  }
}
function Wo(e4) {
  if (typeof localStorage > "u") {
    I.error("Local storage is not available.");
    return;
  }
  try {
    const t = localStorage.getItem(e4);
    if (!t) {
      I.warn(`Item with key ${e4} does not exist in local storage.`);
      return;
    }
    return JSON.parse(t);
  } catch (t) {
    I.error(`Error getting item from local storage: ${t}`);
    return;
  }
}
function Bo(e4) {
  return {
    load: () => Wo(e4),
    save: (t) => Uo(e4, t)
  };
}
var jo = `${kn}-user-choices`;
var Pe = {
  videoEnabled: true,
  audioEnabled: true,
  videoDeviceId: "default",
  audioDeviceId: "default",
  username: ""
};
var { load: Vo, save: Ho } = Bo(jo);
function Js(e4, t = false) {
  t !== true && Ho(e4);
}
function Zs(e4, t = false) {
  var n, r2, i2, o2, s;
  const a = {
    videoEnabled: (n = e4 == null ? void 0 : e4.videoEnabled) != null ? n : Pe.videoEnabled,
    audioEnabled: (r2 = e4 == null ? void 0 : e4.audioEnabled) != null ? r2 : Pe.audioEnabled,
    videoDeviceId: (i2 = e4 == null ? void 0 : e4.videoDeviceId) != null ? i2 : Pe.videoDeviceId,
    audioDeviceId: (o2 = e4 == null ? void 0 : e4.audioDeviceId) != null ? o2 : Pe.audioDeviceId,
    username: (s = e4 == null ? void 0 : e4.username) != null ? s : Pe.username
  };
  if (t)
    return a;
  {
    const c = Vo();
    return J(J({}, a), c ?? {});
  }
}
var ut = null;
var lt = null;
var zo = 0;
function Jt() {
  return ut || (ut = /* @__PURE__ */ new Map()), ut;
}
function Yo() {
  return lt || (lt = /* @__PURE__ */ new WeakMap()), lt;
}
function Ko(e4, t) {
  const n = Yo();
  let r2 = n.get(e4);
  return r2 || (r2 = `room_${zo++}`, n.set(e4, r2)), `${r2}:${t}`;
}
function ea(e4, t) {
  const n = Ko(e4, t), r2 = Jt(), i2 = r2.get(n);
  if (i2)
    return i2;
  const o2 = new re();
  let s = [];
  const a = "lk.segment_id", c = o2.pipe(
    qi({
      subscribe: () => {
        e4.registerTextStreamHandler(t, (u, l) => H(null, null, function* () {
          var f;
          const h2 = ye(u).pipe(
            Ue((m, p) => m + p, "")
          ), d = !!((f = u.info.attributes) != null && f[a]);
          h2.subscribe((m) => {
            const p = s.findIndex(
              (g) => {
                var v, x;
                return g.streamInfo.id === u.info.id || d && ((v = g.streamInfo.attributes) == null ? void 0 : v[a]) === ((x = u.info.attributes) == null ? void 0 : x[a]);
              }
            );
            p !== -1 ? (s[p] = fe(J({}, s[p]), {
              text: m,
              // Carry the latest streamInfo forward. Transcription updates for a
              // segment arrive as separate streams sharing the same lk.segment_id;
              // keeping the original streamInfo would freeze attributes that change
              // over the segment's lifetime — notably lk.transcription_final flipping
              // "false" -> "true" on the final user STT result.
              streamInfo: u.info
            }), o2.next([...s])) : (s.push({
              text: m,
              participantInfo: l,
              streamInfo: u.info
            }), o2.next([...s]));
          });
        }));
      },
      finalize: () => {
        e4.unregisterTextStreamHandler(t);
      }
    }),
    Yi()
  );
  return r2.set(n, c), e4.on(RoomEvent.Disconnected, () => {
    Jt().delete(n), s = [], o2.next([]);
  }), c;
}
function Bn(e4, t) {
  if (t.msg === "show_chat")
    return { ...e4, showChat: true, unreadMessages: 0 };
  if (t.msg === "hide_chat")
    return { ...e4, showChat: false };
  if (t.msg === "toggle_chat") {
    const n = { ...e4, showChat: !e4.showChat };
    return n.showChat === true && (n.unreadMessages = 0), n;
  } else return t.msg === "unread_msg" ? { ...e4, unreadMessages: t.count } : t.msg === "toggle_settings" ? { ...e4, showSettings: !e4.showSettings } : { ...e4 };
}
function jn(e4, t) {
  return t.msg === "set_pin" ? [t.trackReference] : t.msg === "clear_pin" ? [] : { ...e4 };
}
var Vn = D.createContext(void 0);
function ta() {
  const e4 = D.useContext(Vn);
  if (!e4)
    throw Error("Tried to access LayoutContext context outside a LayoutContextProvider provider.");
  return e4;
}
function na(e4) {
  const t = qo();
  if (e4 ?? (e4 = t), !e4)
    throw Error("Tried to access LayoutContext context outside a LayoutContextProvider provider.");
  return e4;
}
function ra() {
  const [e4, t] = D.useReducer(jn, Rn), [n, r2] = D.useReducer(Bn, Mn);
  return {
    pin: { dispatch: t, state: e4 },
    widget: { dispatch: r2, state: n }
  };
}
function ia(e4) {
  const [t, n] = D.useReducer(jn, Rn), [r2, i2] = D.useReducer(Bn, Mn);
  return e4 ?? {
    pin: { dispatch: n, state: t },
    widget: { dispatch: i2, state: r2 }
  };
}
function qo() {
  return D.useContext(Vn);
}
var Hn = D.createContext(
  void 0
);
function oa() {
  const e4 = D.useContext(Hn);
  if (!e4)
    throw Error("tried to access track context outside of track context provider");
  return e4;
}
function zn() {
  return D.useContext(Hn);
}
function sa(e4) {
  const t = zn(), n = e4 ?? t;
  if (!n)
    throw new Error(
      "No TrackRef, make sure you are inside a TrackRefContext or pass the TrackRef explicitly"
    );
  return n;
}
var Yn = D.createContext(void 0);
function aa() {
  const e4 = D.useContext(Yn);
  if (!e4)
    throw Error("tried to access participant context outside of participant context provider");
  return e4;
}
function Go() {
  return D.useContext(Yn);
}
function ca(e4) {
  const t = Go(), n = zn(), r2 = e4 ?? t ?? (n == null ? void 0 : n.participant);
  if (!r2)
    throw new Error(
      "No participant provided, make sure you are inside a participant context or pass the participant explicitly"
    );
  return r2;
}
var Kn = D.createContext(void 0);
function ua() {
  const e4 = D.useContext(Kn);
  if (!e4)
    throw Error("tried to access room context outside of livekit room component");
  return e4;
}
function Qo() {
  return D.useContext(Kn);
}
function la(e4) {
  const t = Qo(), n = e4 ?? t;
  if (!n)
    throw new Error(
      "No room provided, make sure you are inside a Room context or pass the room explicitly"
    );
  return n;
}
var qn = D.createContext(void 0);
function fa() {
  const e4 = D.useContext(qn);
  if (!e4)
    throw Error("tried to access session context outside of SessionProvider component");
  return e4;
}
function Xo() {
  return D.useContext(qn);
}
function da(e4) {
  const t = Xo(), n = e4 ?? t;
  if (!n)
    throw new Error(
      "No session provided, make sure you are inside a Session context or pass the session explicitly"
    );
  return n;
}
var Jo = D.createContext(void 0);
function pa(e4) {
  const t = D.useContext(Jo);
  if (e4 === true) {
    if (t)
      return t;
    throw Error("tried to access feature context, but none is present");
  }
  return t;
}

// node_modules/@livekit/components-react/dist/room-BC_ml4G1.mjs
var i = __toESM(require_react(), 1);
function M(n) {
  var e4, t, o2 = "";
  if (typeof n == "string" || typeof n == "number") o2 += n;
  else if (typeof n == "object") if (Array.isArray(n)) {
    var r2 = n.length;
    for (e4 = 0; e4 < r2; e4++) n[e4] && (t = M(n[e4])) && (o2 && (o2 += " "), o2 += t);
  } else for (t in n) n[t] && (o2 && (o2 += " "), o2 += t);
  return o2;
}
function F() {
  for (var n, e4, t = 0, o2 = "", r2 = arguments.length; t < r2; t++) (n = arguments[t]) && (e4 = M(n)) && (o2 && (o2 += " "), o2 += e4);
  return o2;
}
function I2(...n) {
  return (...e4) => {
    for (const t of n)
      if (typeof t == "function")
        try {
          t(...e4);
        } catch (o2) {
          console.error(o2);
        }
  };
}
function L2(...n) {
  const e4 = { ...n[0] };
  for (let t = 1; t < n.length; t++) {
    const o2 = n[t];
    for (const r2 in o2) {
      const f = e4[r2], c = o2[r2];
      typeof f == "function" && typeof c == "function" && // This is a lot faster than a regex.
      r2[0] === "o" && r2[1] === "n" && r2.charCodeAt(2) >= /* 'A' */
      65 && r2.charCodeAt(2) <= /* 'Z' */
      90 ? e4[r2] = I2(f, c) : (r2 === "className" || r2 === "UNSAFE_className") && typeof f == "string" && typeof c == "string" ? e4[r2] = F(f, c) : e4[r2] = c !== void 0 ? c : f;
    }
  }
  return e4;
}
function J2(n) {
  return n !== void 0;
}
function G(...n) {
  return L2(...n.filter(J2));
}
function H2(n, e4, t) {
  return i.Children.map(n, (o2) => i.isValidElement(o2) && i.Children.only(n) ? (o2.props.className && (e4 ?? (e4 = {}), e4.className = F(o2.props.className, e4.className), e4.style = { ...o2.props.style, ...e4.style }), i.cloneElement(o2, { ...e4, key: t })) : o2);
}
function Q2(n) {
  if (typeof window < "u" && typeof process < "u" && // eslint-disable-next-line turbo/no-undeclared-env-vars
  true) {
    const e4 = document.querySelector(".lk-room-container");
    e4 && !getComputedStyle(e4).getPropertyValue("--lk-has-imported-styles") && I.warn(
      "It looks like you're not using the `@livekit/components-styles package`. To render the UI with the default styling, please import it in your layout or page."
    );
  }
}
function T(n, e4) {
  return n === "processor" && e4 && typeof e4 == "object" && "name" in e4 ? e4.name : (n === "e2ee" || n === "encryption") && e4 ? "e2ee-enabled" : e4;
}
var q2 = {
  connect: true,
  audio: false,
  video: false
};
function $(n) {
  const {
    token: e4,
    serverUrl: t,
    options: o2,
    room: r2,
    connectOptions: f,
    connect: c,
    audio: p,
    video: y,
    screen: g,
    onConnected: b,
    onDisconnected: C,
    onError: m,
    onMediaDeviceFailure: R,
    onEncryptionError: w,
    simulateParticipants: E,
    ...N
  } = { ...q2, ...n };
  o2 && r2 && I.warn(
    "when using a manually created room, the options object will be ignored. set the desired options directly when creating the room instead."
  );
  const [s, A3] = i.useState(), h2 = i.useRef(c);
  i.useEffect(() => {
    A3(r2 ?? new Room(o2));
  }, [r2, JSON.stringify(o2, T)]);
  const O = i.useMemo(() => {
    const { className: d } = Ys();
    return L2(N, { className: d });
  }, [N]);
  return i.useEffect(() => {
    if (!s) return;
    const d = () => {
      const u = s.localParticipant;
      I.debug("trying to publish local tracks"), Promise.all([
        u.setMicrophoneEnabled(!!p, typeof p != "boolean" ? p : void 0),
        u.setCameraEnabled(!!y, typeof y != "boolean" ? y : void 0),
        u.setScreenShareEnabled(!!g, typeof g != "boolean" ? g : void 0)
      ]).catch((v) => {
        I.warn(v), m == null ? void 0 : m(v);
      });
    }, P = (u, v) => {
      const K3 = MediaDeviceFailure.getFailure(u);
      R == null ? void 0 : R(K3, v);
    }, S = (u) => {
      w == null ? void 0 : w(u);
    }, k = (u) => {
      C == null ? void 0 : C(u);
    }, D2 = () => {
      b == null ? void 0 : b();
    };
    return s.on(RoomEvent.SignalConnected, d).on(RoomEvent.MediaDevicesError, P).on(RoomEvent.EncryptionError, S).on(RoomEvent.Disconnected, k).on(RoomEvent.Connected, D2), () => {
      s.off(RoomEvent.SignalConnected, d).off(RoomEvent.MediaDevicesError, P).off(RoomEvent.EncryptionError, S).off(RoomEvent.Disconnected, k).off(RoomEvent.Connected, D2);
    };
  }, [
    s,
    p,
    y,
    g,
    m,
    w,
    R,
    b,
    C
  ]), i.useEffect(() => {
    if (s) {
      if (E) {
        s.simulateParticipants({
          participants: {
            count: E
          },
          publish: {
            audio: true,
            useRealTracks: true
          }
        });
        return;
      }
      if (c) {
        if (h2.current = true, I.debug("connecting"), !e4) {
          I.debug("no token yet");
          return;
        }
        if (!t) {
          I.warn("no livekit url provided"), m == null ? void 0 : m(Error("no livekit url provided"));
          return;
        }
        s.connect(t, e4, f).catch((d) => {
          I.warn(d), h2.current === true && (m == null ? void 0 : m(d));
        });
      } else
        I.debug("disconnecting because connect is false"), h2.current = false, s.disconnect();
    }
  }, [
    c,
    e4,
    JSON.stringify(f),
    s,
    m,
    t,
    E
  ]), i.useEffect(() => {
    if (s)
      return () => {
        I.info("disconnecting on onmount"), s.disconnect();
      };
  }, [s]), { room: s, htmlProps: O };
}
var W = i.forwardRef(function(e4, t) {
  const { room: o2, htmlProps: r2 } = $(e4);
  return i.createElement("div", { ref: t, ...r2 }, o2 && i.createElement(Kn.Provider, { value: o2 }, i.createElement(Jo.Provider, { value: e4.featureFlags }, e4.children)));
});

// node_modules/@livekit/components-react/dist/hooks-C7fu1g_i.mjs
var Jt2 = (e4) => {
  const n = r.useRef(e4);
  return r.useEffect(() => {
    n.current = e4;
  }), n;
};
function Kt2(e4, n) {
  const t = Yt2(), o2 = Jt2(n);
  return r.useLayoutEffect(() => {
    let s = false;
    const a = e4.current;
    if (!a) return;
    function i2(u, l) {
      s || o2.current(u, l);
    }
    return t == null ? void 0 : t.subscribe(a, i2), () => {
      s = true, t == null ? void 0 : t.unsubscribe(a, i2);
    };
  }, [e4.current, t, o2]), t == null ? void 0 : t.observer;
}
function Qt2() {
  let e4 = false, n = [];
  const t = /* @__PURE__ */ new Map();
  if (typeof window > "u")
    return;
  const o2 = new ResizeObserver((s, a) => {
    n = n.concat(s), e4 || window.requestAnimationFrame(() => {
      var _a2;
      const i2 = /* @__PURE__ */ new Set();
      for (let u = 0; u < n.length; u++) {
        if (i2.has(n[u].target)) continue;
        i2.add(n[u].target), (_a2 = t.get(n[u].target)) == null ? void 0 : _a2.forEach((h2) => h2(n[u], a));
      }
      n = [], e4 = false;
    }), e4 = true;
  });
  return {
    observer: o2,
    subscribe(s, a) {
      o2.observe(s);
      const i2 = t.get(s) ?? [];
      i2.push(a), t.set(s, i2);
    },
    unsubscribe(s, a) {
      const i2 = t.get(s) ?? [];
      if (i2.length === 1) {
        o2.unobserve(s), t.delete(s);
        return;
      }
      const u = i2.indexOf(a);
      u !== -1 && i2.splice(u, 1), t.set(s, i2);
    }
  };
}
var me2;
var Yt2 = () => me2 || (me2 = Qt2());
var Xt2 = (e4) => {
  const [n, t] = r.useState({ width: 0, height: 0 });
  r.useLayoutEffect(() => {
    if (e4.current) {
      const { width: s, height: a } = e4.current.getBoundingClientRect();
      t({ width: s, height: a });
    }
  }, [e4.current]);
  const o2 = r.useCallback(
    (s) => t(s.contentRect),
    []
  );
  return Kt2(e4, o2), n;
};
function A2(e4, n, t = true) {
  const [o2, s] = r.useState(n);
  return r.useEffect(() => {
    if (t && s(n), typeof window > "u" || !e4) return;
    const a = e4.subscribe(s);
    return () => a.unsubscribe();
  }, [e4, t]), o2;
}
function Un2(e4) {
  const n = (a) => typeof window < "u" ? window.matchMedia(a).matches : false, [t, o2] = r.useState(n(e4));
  function s() {
    o2(n(e4));
  }
  return r.useEffect(() => {
    const a = window.matchMedia(e4);
    return s(), a.addListener ? a.addListener(s) : a.addEventListener("change", s), () => {
      a.removeListener ? a.removeListener(s) : a.removeEventListener("change", s);
    };
  }, [e4]), t;
}
function zn2(e4) {
  const n = la(e4), t = r.useCallback(async () => {
    await n.startAudio();
  }, [n]), o2 = r.useMemo(
    () => Po(n),
    [n]
  ), { canPlayAudio: s } = A2(o2, {
    canPlayAudio: n.canPlaybackAudio
  });
  return { canPlayAudio: s, startAudio: t };
}
function Hn2(e4) {
  const { state: n, dispatch: t } = ta().pin;
  return { buttonProps: r.useMemo(() => {
    const { className: s } = zs();
    return L2(e4, {
      className: s,
      disabled: !(n == null ? void 0 : n.length),
      onClick: () => {
        t && t({ msg: "clear_pin" });
      }
    });
  }, [e4, t, n]) };
}
function Vn2(e4 = {}) {
  const n = ca(e4.participant), { className: t, connectionQualityObserver: o2 } = r.useMemo(
    () => $s(n),
    [n]
  ), s = A2(o2, n.connectionQuality);
  return { className: t, quality: s };
}
function oe2(e4) {
  const n = la(e4), t = r.useMemo(() => ws(n), [n]);
  return A2(t, n.state);
}
function jn2(e4, n) {
  const t = typeof e4 == "function" ? e4 : n, o2 = typeof e4 == "string" ? e4 : void 0, s = ua(), { send: a, messageObservable: i2, isSendingObservable: u } = r.useMemo(
    () => Mo(s, o2, t),
    [s, o2, t]
  ), l = A2(i2, void 0), h2 = A2(u, false);
  return {
    message: l,
    send: a,
    isSending: h2
  };
}
function Bn2(e4) {
  const n = ua(), t = oe2(n);
  return { buttonProps: r.useMemo(() => {
    const { className: s, disconnect: a } = Ds(n);
    return L2(e4, {
      className: s,
      onClick: () => a(e4.stopTracks ?? true),
      disabled: t === ConnectionState.Disconnected
    });
  }, [n, e4, t]) };
}
function Zt(e4) {
  if (e4.publication instanceof LocalTrackPublication) {
    const n = e4.publication.track;
    if (n) {
      const { facingMode: t } = facingModeFromLocalTrack(n);
      return t;
    }
  }
  return "undefined";
}
function qn2({ trackRef: e4, props: n }) {
  const t = sa(e4), o2 = qo(), { className: s } = r.useMemo(() => Hs(), []), a = r.useMemo(() => ns(t, o2 == null ? void 0 : o2.pin.state), [t, o2 == null ? void 0 : o2.pin.state]);
  return { mergedProps: r.useMemo(
    () => L2(n, {
      className: s,
      onClick: (u) => {
        var _a2, _b2, _c, _d, _e2;
        (_a2 = n.onClick) == null ? void 0 : _a2.call(n, u), a ? (_c = o2 == null ? void 0 : (_b2 = o2.pin).dispatch) == null ? void 0 : _c.call(_b2, {
          msg: "clear_pin"
        }) : (_e2 = o2 == null ? void 0 : (_d = o2.pin).dispatch) == null ? void 0 : _e2.call(_d, {
          msg: "set_pin",
          trackReference: t
        });
      }
    }),
    [n, s, t, a, o2 == null ? void 0 : o2.pin]
  ), inFocus: a };
}
function Wn2(e4, n, t = {}) {
  const o2 = t.gridLayouts ?? fs, { width: s, height: a } = Xt2(e4), i2 = uo(o2, n, s, a);
  return r.useEffect(() => {
    e4.current && i2 && (e4.current.style.setProperty("--lk-col-count", i2 == null ? void 0 : i2.columns.toString()), e4.current.style.setProperty("--lk-row-count", i2 == null ? void 0 : i2.rows.toString()));
  }, [e4, i2]), {
    layout: i2,
    containerWidth: s,
    containerHeight: a
  };
}
function Pe2(e4, n = {}) {
  var _a2, _b2;
  const t = typeof e4 == "string" ? n.participant : e4.participant, o2 = ca(t), s = typeof e4 == "string" ? { participant: o2, source: e4 } : e4, [a, i2] = r.useState(
    !!(((_a2 = s.publication) == null ? void 0 : _a2.isMuted) || ((_b2 = o2.getTrackPublication(s.source)) == null ? void 0 : _b2.isMuted))
  );
  return r.useEffect(() => {
    const u = Lo(s).subscribe(i2);
    return () => u.unsubscribe();
  }, [V(s)]), a;
}
function en2(e4) {
  const n = ca(e4), t = r.useMemo(() => Ps(n), [n]);
  return A2(t, n.isSpeaking);
}
function Ve2(e4 = {}) {
  const n = la(e4.room), [t, o2] = r.useState(n.localParticipant), [s, a] = r.useState(
    t.isMicrophoneEnabled
  ), [i2, u] = r.useState(t.isCameraEnabled), [l, h2] = r.useState(
    t.isScreenShareEnabled
  ), [m, d] = r.useState(
    t.lastMicrophoneError
  ), [c, g] = r.useState(t.lastCameraError), [k, M2] = r.useState(
    void 0
  ), [T2, w] = r.useState(void 0), O = (L4) => {
    u(L4.isCameraEnabled), a(L4.isMicrophoneEnabled), h2(L4.isScreenShareEnabled), w(L4.cameraTrack), M2(L4.microphoneTrack), d(L4.participant.lastMicrophoneError), g(L4.participant.lastCameraError), o2(L4.participant);
  };
  return r.useEffect(() => {
    const L4 = Fn(n.localParticipant).subscribe(O);
    return () => L4.unsubscribe();
  }, [n]), {
    isMicrophoneEnabled: s,
    isScreenShareEnabled: l,
    isCameraEnabled: i2,
    microphoneTrack: k,
    cameraTrack: T2,
    lastMicrophoneError: m,
    lastCameraError: c,
    localParticipant: t
  };
}
function $n2() {
  const e4 = ua(), n = r.useMemo(
    () => _s(e4.localParticipant),
    [e4]
  );
  return A2(n, e4.localParticipant.permissions);
}
function Gn({
  kind: e4,
  room: n,
  track: t,
  requestPermissions: o2,
  onError: s
}) {
  const a = Qo(), i2 = r.useMemo(() => n ?? a ?? new Room(), [n, a]), u = r.useMemo(
    () => Cs(e4, s, o2),
    [e4, o2, s]
  ), l = A2(u, []), [h2, m] = r.useState(
    (i2 == null ? void 0 : i2.getActiveDevice(e4)) ?? "default"
  ), { className: d, activeDeviceObservable: c, setActiveMediaDevice: g } = r.useMemo(
    () => Ms(e4, i2, t),
    [e4, i2, t]
  );
  return r.useEffect(() => {
    const k = c.subscribe((M2) => {
      M2 && (I.info("setCurrentDeviceId", M2), m(M2));
    });
    return () => {
      k == null ? void 0 : k.unsubscribe();
    };
  }, [c]), { devices: l, className: d, activeDeviceId: h2, setActiveMediaDevice: g };
}
function Jn({
  kind: e4,
  onError: n
}) {
  const t = r.useMemo(
    () => Cs(e4, n),
    [e4, n]
  );
  return A2(t, []);
}
function tn2(e4, n, t = {}) {
  const o2 = r.useRef([]), s = r.useRef(-1), a = n !== s.current, i2 = typeof t.customSortFunction == "function" ? t.customSortFunction(e4) : ms(e4);
  let u = [...i2];
  if (a === false)
    try {
      u = bs(o2.current, i2, n);
    } catch (l) {
      I.error("Error while running updatePages(): ", l);
    }
  return a ? o2.current = i2 : o2.current = u, s.current = n, u;
}
function Kn2(e4, n) {
  const [t, o2] = r.useState(1), s = Math.max(Math.ceil(n.length / e4), 1);
  t > s && o2(s);
  const a = t * e4, i2 = a - e4, u = (d) => {
    o2((c) => d === "next" ? c === s ? c : c + 1 : c === 1 ? c : c - 1);
  }, l = (d) => {
    d > s ? o2(s) : d < 1 ? o2(1) : o2(d);
  }, m = tn2(n, e4).slice(i2, a);
  return {
    totalPageCount: s,
    nextPage: () => u("next"),
    prevPage: () => u("previous"),
    setPage: l,
    firstItemIndex: i2,
    lastItemIndex: a,
    tracks: m,
    currentPage: t
  };
}
function nn2(e4 = {}) {
  let n = Go();
  e4.participant && (n = e4.participant);
  const t = r.useMemo(() => _o(n), [n]), { identity: o2, name: s, metadata: a } = A2(t, {
    name: n == null ? void 0 : n.name,
    identity: n == null ? void 0 : n.identity,
    metadata: n == null ? void 0 : n.metadata
  });
  return { identity: o2, name: s, metadata: a };
}
function Qn(e4 = {}) {
  const n = ca(e4.participant), t = r.useMemo(() => _s(n), [n]);
  return A2(t, n.permissions);
}
function Yn2({
  trackRef: e4,
  onParticipantClick: n,
  disableSpeakingIndicator: t,
  htmlProps: o2
}) {
  const s = sa(e4), a = r.useMemo(() => {
    const { className: c } = Us();
    return L2(o2, {
      className: c,
      onClick: (g) => {
        var _a2;
        if ((_a2 = o2.onClick) == null ? void 0 : _a2.call(o2, g), typeof n == "function") {
          const k = s.publication ?? s.participant.getTrackPublication(s.source);
          n({ participant: s.participant, track: k });
        }
      }
    });
  }, [
    o2,
    n,
    s.publication,
    s.source,
    s.participant
  ]), i2 = s.participant.getTrackPublication(Track.Source.Microphone), u = r.useMemo(() => ({
    participant: s.participant,
    source: Track.Source.Microphone,
    publication: i2
  }), [i2, s.participant]), l = Pe2(s), h2 = Pe2(u), m = en2(s.participant), d = Zt(s);
  return {
    elementProps: {
      "data-lk-audio-muted": h2,
      "data-lk-video-muted": l,
      "data-lk-speaking": t === true ? false : m,
      "data-lk-local-participant": s.participant.isLocal,
      "data-lk-source": s.source,
      "data-lk-facing-mode": d,
      ...a
    }
  };
}
function Ce2(e4 = {}) {
  const n = la(e4.room), [t, o2] = r.useState([]);
  return r.useEffect(() => {
    const s = Os(n, {
      additionalRoomEvents: e4.updateOnlyOn
    }).subscribe(o2);
    return () => s.unsubscribe();
  }, [n, JSON.stringify(e4.updateOnlyOn)]), t;
}
function rn2(e4 = {}) {
  const n = Ce2(e4), { localParticipant: t } = Ve2(e4);
  return r.useMemo(
    () => [t, ...n],
    [t, n]
  );
}
function Xn(e4) {
  return e4 = na(e4), r.useMemo(() => (e4 == null ? void 0 : e4.pin.state) !== void 0 && e4.pin.state.length >= 1 ? e4.pin.state : [], [e4.pin.state]);
}
function Zn(e4, n = {}) {
  const t = ua(), [o2] = r.useState(n.updateOnlyOn), s = r.useMemo(() => typeof e4 == "string" ? As(t, e4, {
    additionalEvents: o2
  }) : ks(t, e4, {
    additionalEvents: o2
  }), [t, JSON.stringify(e4), o2]), [a, i2] = r.useState({
    p: void 0
  });
  return r.useEffect(() => {
    const u = s.subscribe((l) => i2({ p: l }));
    return () => u.unsubscribe();
  }, [s]), a.p;
}
function er2(e4 = {}) {
  const n = la(e4.room), t = r.useMemo(() => xs(n), [n]), { name: o2, metadata: s } = A2(t, {
    name: n.name,
    metadata: n.metadata
  });
  return { name: o2, metadata: s };
}
function sn2(e4) {
  const n = la(e4 == null ? void 0 : e4.room), t = r.useMemo(() => Ss(n), [n]);
  return A2(t, n.activeSpeakers);
}
function tr2(e4) {
  const [n, t] = r.useState(
    gs(e4)
  ), o2 = sn2();
  return r.useEffect(() => {
    t(gs(e4));
  }, [o2, e4]), n;
}
function nr2({ room: e4, props: n }) {
  const t = la(e4), { className: o2, roomAudioPlaybackAllowedObservable: s, handleStartAudioPlayback: a } = r.useMemo(
    () => Bs(),
    []
  ), i2 = r.useMemo(
    () => s(t),
    [t, s]
  ), { canPlayAudio: u } = A2(i2, {
    canPlayAudio: t.canPlaybackAudio
  });
  return { mergedProps: r.useMemo(
    () => L2(n, {
      className: o2,
      onClick: () => {
        a(t);
      },
      style: { display: u ? "none" : "block" }
    }),
    [n, o2, u, a, t]
  ), canPlayAudio: u };
}
function rr2({ room: e4, props: n }) {
  const t = la(e4), { className: o2, roomVideoPlaybackAllowedObservable: s, handleStartVideoPlayback: a } = r.useMemo(
    () => js(),
    []
  ), i2 = r.useMemo(
    () => s(t),
    [t, s]
  ), { canPlayVideo: u } = A2(i2, {
    canPlayVideo: t.canPlaybackVideo
  });
  return { mergedProps: r.useMemo(
    () => L2(n, {
      className: o2,
      onClick: () => {
        a(t);
      },
      style: { display: u ? "none" : "block" }
    }),
    [n, o2, u, a, t]
  ), canPlayVideo: u };
}
function sr2(e4, n = {}) {
  const t = r.useRef(null), o2 = r.useRef(null), s = n.minSwipeDistance ?? 50, a = (l) => {
    o2.current = null, t.current = l.targetTouches[0].clientX;
  }, i2 = (l) => {
    o2.current = l.targetTouches[0].clientX;
  }, u = r.useCallback(() => {
    if (!t.current || !o2.current)
      return;
    const l = t.current - o2.current, h2 = l > s, m = l < -s;
    h2 && n.onLeftSwipe && n.onLeftSwipe(), m && n.onRightSwipe && n.onRightSwipe();
  }, [s, n]);
  r.useEffect(() => {
    const l = e4.current;
    return l && (l.addEventListener("touchstart", a, { passive: true }), l.addEventListener("touchmove", i2, { passive: true }), l.addEventListener("touchend", u, { passive: true })), () => {
      l && (l.removeEventListener("touchstart", a), l.removeEventListener("touchmove", i2), l.removeEventListener("touchend", u));
    };
  }, [e4, u]);
}
function or2({ props: e4 }) {
  const { dispatch: n, state: t } = ta().widget, { className: o2 } = r.useMemo(() => Vs(), []);
  return { mergedProps: r.useMemo(() => L2(e4, {
    className: o2,
    onClick: () => {
      n && n({ msg: "toggle_chat" });
    },
    "aria-pressed": (t == null ? void 0 : t.showChat) ? "true" : "false",
    "data-lk-unread-msgs": t ? t.unreadMessages < 10 ? t.unreadMessages.toFixed(0) : "9+" : "0"
  }), [e4, o2, n, t]) };
}
function ar2(e4, n, t = {}) {
  const [o2, s] = r.useState(void 0);
  return r.useEffect(() => {
    var _a2;
    if (e4 === void 0)
      throw Error("token endpoint needs to be defined");
    if (((_a2 = t.userInfo) == null ? void 0 : _a2.identity) === void 0)
      return;
    (async () => {
      I.debug("fetching token");
      const i2 = new URLSearchParams({ ...t.userInfo, roomName: n }), u = await fetch(`${e4}?${i2.toString()}`);
      if (!u.ok) {
        I.error(
          `Could not fetch token. Server responded with status ${u.status}: ${u.statusText}`
        );
        return;
      }
      const { accessToken: l } = await u.json();
      s(l);
    })();
  }, [e4, n, JSON.stringify(t)]), o2;
}
function ir2(e4) {
  var _a2, _b2;
  const n = sa(e4), { className: t, mediaMutedObserver: o2 } = r.useMemo(
    () => Ns(n),
    [V(n)]
  );
  return { isMuted: A2(
    o2,
    !!(((_a2 = n.publication) == null ? void 0 : _a2.isMuted) || ((_b2 = n.participant.getTrackPublication(n.source)) == null ? void 0 : _b2.isMuted))
  ), className: t };
}
function cr2({
  source: e4,
  onChange: n,
  initialState: t,
  captureOptions: o2,
  publishOptions: s,
  onDeviceError: a,
  room: i2,
  ...u
}) {
  var _a2;
  const l = Qo(), h2 = r.useMemo(() => i2 ?? l, [i2, l]), m = (_a2 = h2 == null ? void 0 : h2.localParticipant) == null ? void 0 : _a2.getTrackPublication(e4), d = r.useRef(false), { toggle: c, className: g, pendingObserver: k, enabledObserver: M2 } = r.useMemo(
    () => h2 ? Is(e4, h2, o2, s, a) : Rs(),
    [h2, e4, JSON.stringify(o2), s]
  ), T2 = A2(k, false), w = A2(M2, t ?? !!(m == null ? void 0 : m.isEnabled));
  r.useEffect(() => {
    n == null ? void 0 : n(w, d.current), d.current = false;
  }, [w, n]), r.useEffect(() => {
    t !== void 0 && (I.debug("forcing initial toggle state", e4, t), c(t));
  }, []);
  const O = r.useMemo(() => L2(u, { className: g }), [u, g]), L4 = r.useCallback(
    (p) => {
      var _a3;
      d.current = true, c().catch(() => d.current = false), (_a3 = u.onClick) == null ? void 0 : _a3.call(u, p);
    },
    [u, c]
  );
  return {
    toggle: c,
    enabled: w,
    pending: T2,
    track: m,
    buttonProps: {
      ...O,
      "aria-pressed": w,
      "data-lk-source": e4,
      "data-lk-enabled": w,
      disabled: T2,
      onClick: L4
    }
  };
}
function ur2(e4 = [
  Track.Source.Camera,
  Track.Source.Microphone,
  Track.Source.ScreenShare,
  Track.Source.ScreenShareAudio,
  Track.Source.Unknown
], n = {}) {
  const t = la(n.room), [o2, s] = r.useState([]), [a, i2] = r.useState([]), u = r.useMemo(() => e4.map((h2) => po(h2) ? h2.source : h2), [JSON.stringify(e4)]);
  return r.useEffect(() => {
    const h2 = Ks(t, u, {
      additionalRoomEvents: n.updateOnlyOn,
      onlySubscribed: n.onlySubscribed
    }).subscribe(({ trackReferences: m, participants: d }) => {
      I.debug("setting track bundles", m, d), s(m), i2(d);
    });
    return () => h2.unsubscribe();
  }, [
    t,
    JSON.stringify(n.onlySubscribed),
    JSON.stringify(n.updateOnlyOn),
    JSON.stringify(e4)
  ]), r.useMemo(() => {
    if (vs(e4)) {
      const h2 = an2(e4, a), m = Array.from(o2);
      return a.forEach((d) => {
        h2.has(d.identity) && (h2.get(d.identity) ?? []).forEach((g) => {
          if (o2.find(
            ({ participant: M2, publication: T2 }) => d.identity === M2.identity && T2.source === g
          ))
            return;
          I.debug(
            `Add ${g} placeholder for participant ${d.identity}.`
          );
          const k = {
            participant: d,
            source: g
          };
          m.push(k);
        });
      }), m;
    } else
      return o2;
  }, [o2, a, e4]);
}
function on2(e4, n) {
  const t = new Set(e4);
  for (const o2 of n)
    t.delete(o2);
  return t;
}
function an2(e4, n) {
  const t = /* @__PURE__ */ new Map();
  if (vs(e4)) {
    const o2 = e4.filter((s) => s.withPlaceholder).map((s) => s.source);
    n.forEach((s) => {
      const a = s.getTrackPublications().map((u) => {
        var _a2;
        return (_a2 = u.track) == null ? void 0 : _a2.source;
      }).filter((u) => u !== void 0), i2 = Array.from(
        on2(new Set(o2), new Set(a))
      );
      i2.length > 0 && t.set(s.identity, i2);
    });
  }
  return t;
}
function cn2(e4) {
  const [n, t] = r.useState(Gt(e4)), { trackObserver: o2 } = r.useMemo(() => ys(e4), [e4.participant.sid ?? e4.participant.identity, e4.source]);
  return r.useEffect(() => {
    const s = o2.subscribe((a) => {
      t(a);
    });
    return () => s == null ? void 0 : s.unsubscribe();
  }, [o2]), {
    participant: e4.participant,
    source: e4.source ?? Track.Source.Unknown,
    publication: n
  };
}
function lr2(e4, n) {
  const t = ca(n);
  return cn2({ name: e4, participant: t });
}
function un2(e4) {
  const n = la(e4 == null ? void 0 : e4.room), t = oe2(n), o2 = r.useMemo(
    () => t === ConnectionState.Disconnected,
    [t]
  ), s = r.useMemo(
    () => Ws(n, e4),
    [n, e4, o2]
  ), a = A2(s.isSendingObservable, false), i2 = A2(s.messageObservable, []);
  return { send: s.send, chatMessages: i2, isSending: a };
}
function dr2(e4 = {}) {
  const [n, t] = r.useState(
    Zs(e4.defaults, e4.preventLoad ?? false)
  ), o2 = r.useCallback((l) => {
    t((h2) => ({ ...h2, audioEnabled: l }));
  }, []), s = r.useCallback((l) => {
    t((h2) => ({ ...h2, videoEnabled: l }));
  }, []), a = r.useCallback((l) => {
    t((h2) => ({ ...h2, audioDeviceId: l }));
  }, []), i2 = r.useCallback((l) => {
    t((h2) => ({ ...h2, videoDeviceId: l }));
  }, []), u = r.useCallback((l) => {
    t((h2) => ({ ...h2, username: l }));
  }, []);
  return r.useEffect(() => {
    Js(n, e4.preventSave ?? false);
  }, [n, e4.preventSave]), {
    userChoices: n,
    saveAudioInputEnabled: o2,
    saveVideoInputEnabled: s,
    saveAudioInputDeviceId: a,
    saveVideoInputDeviceId: i2,
    saveUsername: u
  };
}
function fr2(e4, n = {}) {
  const t = ca(e4), o2 = la(n.room), s = r.useMemo(() => Es(o2, t), [o2, t]);
  return A2(
    s,
    t.isLocal ? t.isE2EEEnabled : !!(t == null ? void 0 : t.isEncrypted)
  );
}
function pr2(e4, n = { fftSize: 32, smoothingTimeConstant: 0 }) {
  const t = q(e4) ? e4.publication.track : e4, [o2, s] = r.useState(0);
  return r.useEffect(() => {
    if (!t || !t.mediaStream)
      return;
    const { cleanup: a, analyser: i2 } = createAudioAnalyser(t, n), u = i2.frequencyBinCount, l = new Uint8Array(u), m = setInterval(() => {
      i2.getByteFrequencyData(l);
      let d = 0;
      for (let c = 0; c < l.length; c++) {
        const g = l[c];
        d += g * g;
      }
      s(Math.sqrt(d / l.length) / 255);
    }, 1e3 / 30);
    return () => {
      a(), clearInterval(m);
    };
  }, [t, t == null ? void 0 : t.mediaStream, JSON.stringify(n)]), o2;
}
var ln2 = (e4) => {
  const n = (t) => {
    let a = 1 - Math.max(-100, Math.min(-10, t)) * -1 / 100;
    return a = Math.sqrt(a), a;
  };
  return e4.map((t) => t === -1 / 0 ? 0 : n(t));
};
var dn2 = {
  bands: 5,
  loPass: 100,
  hiPass: 600,
  updateInterval: 32,
  analyserOptions: { fftSize: 2048 }
};
function mr2(e4, n = {}) {
  var _a2;
  const t = e4 instanceof Track ? e4 : (_a2 = e4 == null ? void 0 : e4.publication) == null ? void 0 : _a2.track, o2 = { ...dn2, ...n }, [s, a] = r.useState(
    new Array(o2.bands).fill(0)
  );
  return r.useEffect(() => {
    if (!t || !(t == null ? void 0 : t.mediaStream)) {
      a((c) => c.slice().fill(0));
      return;
    }
    const { analyser: i2, cleanup: u } = createAudioAnalyser(t, o2.analyserOptions), l = i2.frequencyBinCount, h2 = new Float32Array(l), d = setInterval(() => {
      i2.getFloatFrequencyData(h2);
      let c = new Float32Array(h2.length);
      for (let T2 = 0; T2 < h2.length; T2++)
        c[T2] = h2[T2];
      c = c.slice(o2.loPass, o2.hiPass);
      const g = ln2(c), k = g.length, M2 = [];
      for (let T2 = 0; T2 < o2.bands; T2++) {
        const w = Math.floor(T2 * k / o2.bands), O = Math.floor((T2 + 1) * k / o2.bands), L4 = g.slice(w, O), p = L4.length;
        if (p === 0)
          M2.push(0);
        else {
          const f = L4.reduce((b, v) => b += v, 0);
          M2.push(f / p);
        }
      }
      a(M2);
    }, o2.updateInterval);
    return () => {
      u(), clearInterval(d);
    };
  }, [t, t == null ? void 0 : t.mediaStream, JSON.stringify(n)]), s;
}
var fn2 = {
  barCount: 120,
  volMultiplier: 5,
  updateInterval: 20
};
function hr2(e4, n = {}) {
  var _a2;
  const t = e4 instanceof Track ? e4 : (_a2 = e4 == null ? void 0 : e4.publication) == null ? void 0 : _a2.track, o2 = { ...fn2, ...n }, s = r.useRef(new Float32Array()), a = r.useRef(performance.now()), i2 = r.useRef(0), [u, l] = r.useState([]), h2 = r.useCallback((m) => {
    l(
      Array.from(
        mn2(m, o2.barCount).map((d) => Math.sqrt(d) * o2.volMultiplier)
        // wave.slice(0, opts.barCount).map((v) => sigmoid(v * opts.volMultiplier, 0.08, 0.2)),
      )
    );
  }, []);
  return r.useEffect(() => {
    if (!t || !(t == null ? void 0 : t.mediaStream))
      return;
    const { analyser: m, cleanup: d } = createAudioAnalyser(t, {
      fftSize: Re2(o2.barCount)
    }), c = Re2(o2.barCount), g = new Float32Array(c), k = () => {
      if (M2 = requestAnimationFrame(k), m.getFloatTimeDomainData(g), s.current.map((T2, w) => T2 + g[w]), i2.current += 1, performance.now() - a.current >= o2.updateInterval) {
        const T2 = g.map((w) => w / i2.current);
        h2(T2), a.current = performance.now(), i2.current = 0;
      }
    };
    let M2 = requestAnimationFrame(k);
    return () => {
      d(), cancelAnimationFrame(M2);
    };
  }, [t, t == null ? void 0 : t.mediaStream, JSON.stringify(n), h2]), {
    bars: u
  };
}
function Re2(e4) {
  return e4 < 32 ? 32 : pn2(e4);
}
function pn2(e4) {
  let n = 2;
  for (; e4 >>= 1; )
    n <<= 1;
  return n;
}
function mn2(e4, n) {
  const t = Math.floor(e4.length / n), o2 = new Float32Array(n);
  for (let s = 0; s < n; s++) {
    const a = t * s;
    let i2 = 0;
    for (let u = 0; u < t; u++)
      i2 = i2 + Math.abs(e4[a + u]);
    o2[s] = i2 / t;
  }
  return o2;
}
function le2(e4, n = {}) {
  let t, o2;
  typeof n == "string" ? t = n : (t = n == null ? void 0 : n.participantIdentity, o2 = n == null ? void 0 : n.room);
  const s = Go(), a = rn2({ room: o2, updateOnlyOn: [] }), i2 = r.useMemo(() => t ? a.find((h2) => h2.identity === t) : s, [t, a, s]), u = r.useMemo(() => {
    if (i2)
      return qs(i2, { sources: e4 });
  }, [i2, JSON.stringify(e4)]);
  return A2(u, []);
}
function hn2(e4) {
  var _a2, _b2, _c;
  const n = r.useMemo(
    () => {
      var _a3;
      return ((_a3 = e4 == null ? void 0 : e4.publication) == null ? void 0 : _a3.track) ? Qs(e4 == null ? void 0 : e4.publication.track) : void 0;
    },
    [(_a2 = e4 == null ? void 0 : e4.publication) == null ? void 0 : _a2.track]
  );
  return A2(n, {
    timestamp: Date.now(),
    rtpTimestamp: (_c = (_b2 = e4 == null ? void 0 : e4.publication) == null ? void 0 : _b2.track) == null ? void 0 : _c.rtpTimestamp
  });
}
var bn2 = {
  bufferSize: 100
  // maxAge: 2_000,
};
function gn2(e4, n) {
  const t = { ...bn2, ...n }, [o2, s] = r.useState([]), a = hn2(e4), i2 = (u) => {
    var _a2;
    (_a2 = t.onTranscription) == null ? void 0 : _a2.call(t, u), s(
      (l) => hs(
        l,
        // when first receiving a segment, add the current media timestamp to it
        u.map((h2) => ps(h2, a)),
        t.bufferSize
      )
    );
  };
  return r.useEffect(() => {
    if (!(e4 == null ? void 0 : e4.publication))
      return;
    const u = Gs(e4.publication).subscribe((l) => {
      i2(...l);
    });
    return () => {
      u.unsubscribe();
    };
  }, [e4 && V(e4), i2]), { segments: o2 };
}
function vn2(e4 = {}) {
  const n = Go(), t = e4.participant ?? n, o2 = r.useMemo(
    // weird typescript constraint
    () => t ? Ls(t) : Ls(t),
    [t]
  );
  return A2(o2, {
    attributes: t == null ? void 0 : t.attributes
  });
}
function br2(e4, n = {}) {
  const t = ca(n.participant), [o2, s] = r.useState(t.attributes[e4]);
  return r.useEffect(() => {
    if (!t)
      return;
    const a = Ls(t).subscribe((i2) => {
      i2.changed[e4] !== void 0 && s(i2.attributes[e4]);
    });
    return () => {
      a.unsubscribe();
    };
  }, [t, e4]), o2;
}
var Le2 = fo.AgentState;
function gr2() {
  const e4 = Ce2(), n = e4.find(
    (d) => d.kind === ParticipantInfo_Kind.AGENT && !(fo.PublishOnBehalf in d.attributes)
  ), t = e4.find(
    (d) => d.kind === ParticipantInfo_Kind.AGENT && d.attributes[fo.PublishOnBehalf] === (n == null ? void 0 : n.identity)
  ), o2 = le2(
    [Track.Source.Microphone, Track.Source.Camera],
    n == null ? void 0 : n.identity
  ), s = le2(
    [Track.Source.Microphone, Track.Source.Camera],
    t == null ? void 0 : t.identity
  ), a = o2.find((d) => d.source === Track.Source.Microphone) ?? s.find((d) => d.source === Track.Source.Microphone), i2 = o2.find((d) => d.source === Track.Source.Camera) ?? s.find((d) => d.source === Track.Source.Camera), { segments: u } = gn2(a), l = oe2(), { attributes: h2 } = vn2({ participant: n }), m = r.useMemo(() => l === ConnectionState.Disconnected ? "disconnected" : l === ConnectionState.Connecting || !n || !(h2 == null ? void 0 : h2[Le2]) ? "connecting" : h2[Le2], [h2, n, l]);
  return {
    agent: n,
    state: m,
    audioTrack: a,
    videoTrack: i2,
    agentTranscriptions: u,
    agentAttributes: h2
  };
}
function vr2(e4) {
  const n = la(e4), t = oe2(n), o2 = r.useMemo(() => Ts(n), [n, t]);
  return A2(o2, n.isRecording);
}
function yn2(e4, n) {
  const t = la(n == null ? void 0 : n.room), s = oe2(t) === ConnectionState.Disconnected, a = r.useMemo(() => ea(t, e4), [t, e4]);
  return { textStreams: A2(s ? void 0 : a, []) };
}
function Sn2(e4) {
  const { participantIdentities: n, trackSids: t } = e4 ?? {}, { textStreams: o2 } = yn2(Io.TRANSCRIPTION, { room: e4 == null ? void 0 : e4.room });
  return r.useMemo(
    () => o2.filter(
      (a) => n ? n.includes(a.participantInfo.identity) : true
    ).filter(
      (a) => {
        var _a2;
        return t ? t.includes(
          ((_a2 = a.streamInfo.attributes) == null ? void 0 : _a2[fo.TranscribedTrackId]) ?? ""
        ) : true;
      }
    ),
    [o2, n, t]
  );
}
var Ae2 = 2;
var Oe2 = 400;
var Ie2 = 3;
var De2 = 1e3;
function yr2(e4) {
  const n = (0, import_react.useRef)([]), t = (0, import_react.useMemo)(() => new _(), []), o2 = (0, import_react.useCallback)(async () => t.lock().then(async (m) => {
    for (; ; ) {
      const d = n.current.pop();
      if (!d) {
        m();
        break;
      }
      switch (d.type) {
        case "connect":
          await d.room.connect(...d.args).then(d.resolve).catch(d.reject);
          break;
        case "disconnect":
          await d.room.disconnect(...d.args).then(d.resolve).catch(d.reject);
          break;
      }
    }
  }), []), s = (0, import_react.useRef)([]), a = (0, import_react.useCallback)((m) => {
    let d = 0;
    s.current = s.current.filter((c) => {
      const g = m.getTime() - c.getTime() < De2;
      return g && (d += 1), g;
    }), d > Ie2 && I.warn(
      `useSequentialRoomConnectDisconnect: room changed reference rapidly (over ${Ie2}x in ${De2}ms). This is not recommended.`
    );
  }, []);
  (0, import_react.useEffect)(() => {
    n.current = [];
    const m = /* @__PURE__ */ new Date();
    s.current.push(m), a(m);
  }, [e4, a]);
  const i2 = (0, import_react.useRef)([]), u = (0, import_react.useCallback)((m) => {
    let d = 0;
    i2.current = i2.current.filter((c) => {
      const g = m.getTime() - c.getTime() < Oe2;
      return g && (d += 1), g;
    }), d > Ae2 && I.warn(
      `useSequentialRoomConnectDisconnect: room connect / disconnect occurring in rapid sequence (over ${Ae2}x in ${Oe2}ms). This is not recommended and may be the sign of a bug like a useEffect dependency changing every render.`
    );
  }, []), l = (0, import_react.useCallback)(
    async (...m) => new Promise((d, c) => {
      if (!e4)
        throw new Error("Called connect(), but room was unset");
      const g = /* @__PURE__ */ new Date();
      u(g), n.current.push({ type: "connect", room: e4, args: m, resolve: d, reject: c }), i2.current.push(g), o2();
    }),
    [e4, u, o2]
  ), h2 = (0, import_react.useCallback)(
    async (...m) => new Promise((d, c) => {
      if (!e4)
        throw new Error("Called discconnect(), but room was unset");
      const g = /* @__PURE__ */ new Date();
      u(g), n.current.push({ type: "disconnect", room: e4, args: m, resolve: d, reject: c }), i2.current.push(g), o2();
    }),
    [e4, u, o2]
  );
  return {
    connect: e4 ? l : null,
    disconnect: e4 ? h2 : null
  };
}
var ce2 = { exports: {} };
var Ne2;
function Cn2() {
  if (Ne2) return ce2.exports;
  Ne2 = 1;
  var e4 = typeof Reflect == "object" ? Reflect : null, n = e4 && typeof e4.apply == "function" ? e4.apply : function(f, b, v) {
    return Function.prototype.apply.call(f, b, v);
  }, t;
  e4 && typeof e4.ownKeys == "function" ? t = e4.ownKeys : Object.getOwnPropertySymbols ? t = function(f) {
    return Object.getOwnPropertyNames(f).concat(Object.getOwnPropertySymbols(f));
  } : t = function(f) {
    return Object.getOwnPropertyNames(f);
  };
  function o2(p) {
    console && console.warn && console.warn(p);
  }
  var s = Number.isNaN || function(f) {
    return f !== f;
  };
  function a() {
    a.init.call(this);
  }
  ce2.exports = a, ce2.exports.once = w, a.EventEmitter = a, a.prototype._events = void 0, a.prototype._eventsCount = 0, a.prototype._maxListeners = void 0;
  var i2 = 10;
  function u(p) {
    if (typeof p != "function")
      throw new TypeError('The "listener" argument must be of type Function. Received type ' + typeof p);
  }
  Object.defineProperty(a, "defaultMaxListeners", {
    enumerable: true,
    get: function() {
      return i2;
    },
    set: function(p) {
      if (typeof p != "number" || p < 0 || s(p))
        throw new RangeError('The value of "defaultMaxListeners" is out of range. It must be a non-negative number. Received ' + p + ".");
      i2 = p;
    }
  }), a.init = function() {
    (this._events === void 0 || this._events === Object.getPrototypeOf(this)._events) && (this._events = /* @__PURE__ */ Object.create(null), this._eventsCount = 0), this._maxListeners = this._maxListeners || void 0;
  }, a.prototype.setMaxListeners = function(f) {
    if (typeof f != "number" || f < 0 || s(f))
      throw new RangeError('The value of "n" is out of range. It must be a non-negative number. Received ' + f + ".");
    return this._maxListeners = f, this;
  };
  function l(p) {
    return p._maxListeners === void 0 ? a.defaultMaxListeners : p._maxListeners;
  }
  a.prototype.getMaxListeners = function() {
    return l(this);
  }, a.prototype.emit = function(f) {
    for (var b = [], v = 1; v < arguments.length; v++) b.push(arguments[v]);
    var C = f === "error", R = this._events;
    if (R !== void 0)
      C = C && R.error === void 0;
    else if (!C)
      return false;
    if (C) {
      var y;
      if (b.length > 0 && (y = b[0]), y instanceof Error)
        throw y;
      var N = new Error("Unhandled error." + (y ? " (" + y.message + ")" : ""));
      throw N.context = y, N;
    }
    var x = R[f];
    if (x === void 0)
      return false;
    if (typeof x == "function")
      n(x, this, b);
    else
      for (var j2 = x.length, z3 = k(x, j2), v = 0; v < j2; ++v)
        n(z3[v], this, b);
    return true;
  };
  function h2(p, f, b, v) {
    var C, R, y;
    if (u(b), R = p._events, R === void 0 ? (R = p._events = /* @__PURE__ */ Object.create(null), p._eventsCount = 0) : (R.newListener !== void 0 && (p.emit(
      "newListener",
      f,
      b.listener ? b.listener : b
    ), R = p._events), y = R[f]), y === void 0)
      y = R[f] = b, ++p._eventsCount;
    else if (typeof y == "function" ? y = R[f] = v ? [b, y] : [y, b] : v ? y.unshift(b) : y.push(b), C = l(p), C > 0 && y.length > C && !y.warned) {
      y.warned = true;
      var N = new Error("Possible EventEmitter memory leak detected. " + y.length + " " + String(f) + " listeners added. Use emitter.setMaxListeners() to increase limit");
      N.name = "MaxListenersExceededWarning", N.emitter = p, N.type = f, N.count = y.length, o2(N);
    }
    return p;
  }
  a.prototype.addListener = function(f, b) {
    return h2(this, f, b, false);
  }, a.prototype.on = a.prototype.addListener, a.prototype.prependListener = function(f, b) {
    return h2(this, f, b, true);
  };
  function m() {
    if (!this.fired)
      return this.target.removeListener(this.type, this.wrapFn), this.fired = true, arguments.length === 0 ? this.listener.call(this.target) : this.listener.apply(this.target, arguments);
  }
  function d(p, f, b) {
    var v = { fired: false, wrapFn: void 0, target: p, type: f, listener: b }, C = m.bind(v);
    return C.listener = b, v.wrapFn = C, C;
  }
  a.prototype.once = function(f, b) {
    return u(b), this.on(f, d(this, f, b)), this;
  }, a.prototype.prependOnceListener = function(f, b) {
    return u(b), this.prependListener(f, d(this, f, b)), this;
  }, a.prototype.removeListener = function(f, b) {
    var v, C, R, y, N;
    if (u(b), C = this._events, C === void 0)
      return this;
    if (v = C[f], v === void 0)
      return this;
    if (v === b || v.listener === b)
      --this._eventsCount === 0 ? this._events = /* @__PURE__ */ Object.create(null) : (delete C[f], C.removeListener && this.emit("removeListener", f, v.listener || b));
    else if (typeof v != "function") {
      for (R = -1, y = v.length - 1; y >= 0; y--)
        if (v[y] === b || v[y].listener === b) {
          N = v[y].listener, R = y;
          break;
        }
      if (R < 0)
        return this;
      R === 0 ? v.shift() : M2(v, R), v.length === 1 && (C[f] = v[0]), C.removeListener !== void 0 && this.emit("removeListener", f, N || b);
    }
    return this;
  }, a.prototype.off = a.prototype.removeListener, a.prototype.removeAllListeners = function(f) {
    var b, v, C;
    if (v = this._events, v === void 0)
      return this;
    if (v.removeListener === void 0)
      return arguments.length === 0 ? (this._events = /* @__PURE__ */ Object.create(null), this._eventsCount = 0) : v[f] !== void 0 && (--this._eventsCount === 0 ? this._events = /* @__PURE__ */ Object.create(null) : delete v[f]), this;
    if (arguments.length === 0) {
      var R = Object.keys(v), y;
      for (C = 0; C < R.length; ++C)
        y = R[C], y !== "removeListener" && this.removeAllListeners(y);
      return this.removeAllListeners("removeListener"), this._events = /* @__PURE__ */ Object.create(null), this._eventsCount = 0, this;
    }
    if (b = v[f], typeof b == "function")
      this.removeListener(f, b);
    else if (b !== void 0)
      for (C = b.length - 1; C >= 0; C--)
        this.removeListener(f, b[C]);
    return this;
  };
  function c(p, f, b) {
    var v = p._events;
    if (v === void 0)
      return [];
    var C = v[f];
    return C === void 0 ? [] : typeof C == "function" ? b ? [C.listener || C] : [C] : b ? T2(C) : k(C, C.length);
  }
  a.prototype.listeners = function(f) {
    return c(this, f, true);
  }, a.prototype.rawListeners = function(f) {
    return c(this, f, false);
  }, a.listenerCount = function(p, f) {
    return typeof p.listenerCount == "function" ? p.listenerCount(f) : g.call(p, f);
  }, a.prototype.listenerCount = g;
  function g(p) {
    var f = this._events;
    if (f !== void 0) {
      var b = f[p];
      if (typeof b == "function")
        return 1;
      if (b !== void 0)
        return b.length;
    }
    return 0;
  }
  a.prototype.eventNames = function() {
    return this._eventsCount > 0 ? t(this._events) : [];
  };
  function k(p, f) {
    for (var b = new Array(f), v = 0; v < f; ++v)
      b[v] = p[v];
    return b;
  }
  function M2(p, f) {
    for (; f + 1 < p.length; f++)
      p[f] = p[f + 1];
    p.pop();
  }
  function T2(p) {
    for (var f = new Array(p.length), b = 0; b < f.length; ++b)
      f[b] = p[b].listener || p[b];
    return f;
  }
  function w(p, f) {
    return new Promise(function(b, v) {
      function C(y) {
        p.removeListener(f, R), v(y);
      }
      function R() {
        typeof p.removeListener == "function" && p.removeListener("error", C), b([].slice.call(arguments));
      }
      L4(p, f, R, { once: true }), f !== "error" && O(p, C, { once: true });
    });
  }
  function O(p, f, b) {
    typeof p.on == "function" && L4(p, "error", f, b);
  }
  function L4(p, f, b, v) {
    if (typeof p.on == "function")
      v.once ? p.once(f, b) : p.on(f, b);
    else if (typeof p.addEventListener == "function")
      p.addEventListener(f, function C(R) {
        v.once && p.removeEventListener(f, C), b(R);
      });
    else
      throw new TypeError('The "emitter" argument must be of type EventEmitter. Received type ' + typeof p);
  }
  return ce2.exports;
}
var ke2 = Cn2();
var kn2 = 2e4;
var En2 = ((e4) => (e4.CameraChanged = "cameraChanged", e4.MicrophoneChanged = "microphoneChanged", e4.StateChanged = "stateChanged", e4))(En2 || {});
var F2 = (e4) => ({
  isConnected: e4 === "listening" || e4 === "thinking" || e4 === "speaking",
  canListen: e4 === "pre-connect-buffering" || e4 === "listening" || e4 === "thinking" || e4 === "speaking",
  isFinished: e4 === "disconnected" || e4 === "failed",
  isPending: e4 === "connecting" || e4 === "initializing" || e4 === "idle"
});
var Tn2 = () => {
  const [e4, n] = r.useState(
    null
  ), [t, o2] = r.useState(
    null
  ), s = r.useRef("connecting"), a = r.useRef(false), i2 = (u) => setTimeout(() => {
    if (!a.current) {
      n("Agent did not join the room.");
      return;
    }
    const { isConnected: l } = F2(s.current);
    if (!l) {
      n("Agent joined the room but did not complete initializing.");
      return;
    }
  }, u ?? kn2);
  return {
    agentTimeoutFailureReason: e4,
    startAgentTimeout: r.useCallback(
      (u) => {
        t && clearTimeout(t), n(null), o2(i2(u)), s.current = "connecting", a.current = false;
      },
      [t]
    ),
    clearAgentTimeout: r.useCallback(() => {
      t && clearTimeout(t), n(null), o2(null), s.current = "connecting", a.current = false;
    }, [t]),
    clearAgentTimeoutFailureReason: r.useCallback(() => {
      n(null);
    }, []),
    updateAgentTimeoutState: r.useCallback((u) => {
      s.current = u;
    }, []),
    updateAgentTimeoutParticipantExists: r.useCallback((u) => {
      a.current = u;
    }, [])
  };
};
function Mn2(e4, n) {
  const t = r.useRef(n);
  r.useEffect(() => {
    t.current = n;
  }, [n]);
  const o2 = r.useCallback(
    async (i2) => {
      const { isConnected: u } = F2(t.current);
      if (!u)
        return new Promise((l, h2) => {
          const m = (g) => {
            const { isConnected: k } = F2(g);
            k && (c(), l());
          }, d = () => {
            c(), h2(new Error("useAgent(/* ... */).waitUntilConnected - signal aborted"));
          }, c = () => {
            e4.off("stateChanged", m), i2 == null ? void 0 : i2.removeEventListener("abort", d);
          };
          e4.on("stateChanged", m), i2 == null ? void 0 : i2.addEventListener("abort", d);
        });
    },
    [e4]
  ), s = r.useCallback(
    async (i2) => {
      const { canListen: u } = F2(t.current);
      if (!u)
        return new Promise((l, h2) => {
          const m = (g) => {
            const { canListen: k } = F2(g);
            k && (c(), l());
          }, d = () => {
            c(), h2(new Error("useAgent(/* ... */).waitUntilCouldBeListening - signal aborted"));
          }, c = () => {
            e4.off("stateChanged", m), i2 == null ? void 0 : i2.removeEventListener("abort", d);
          };
          e4.on("stateChanged", m), i2 == null ? void 0 : i2.addEventListener("abort", d);
        });
    },
    [e4]
  ), a = r.useCallback(
    async (i2) => {
      const { isFinished: u } = F2(t.current);
      if (!u)
        return new Promise((l, h2) => {
          const m = (g) => {
            const { isFinished: k } = F2(g);
            k && (c(), l());
          }, d = () => {
            c(), h2(new Error("useAgent(/* ... */).waitUntilFinished - signal aborted"));
          }, c = () => {
            e4.off("stateChanged", m), i2 == null ? void 0 : i2.removeEventListener("abort", d);
          };
          e4.on("stateChanged", m), i2 == null ? void 0 : i2.addEventListener("abort", d);
        });
    },
    [e4]
  );
  return { waitUntilConnected: o2, waitUntilCouldBeListening: s, waitUntilFinished: a };
}
function je2(e4) {
  const n = Xo();
  if (e4 = e4 ?? n, !e4)
    throw new Error(
      "No session provided, make sure you are inside a Session context or pass the session explicitly"
    );
  const {
    room: t,
    internal: {
      agentConnectTimeoutMilliseconds: o2,
      agentTimeoutFailureReason: s,
      startAgentTimeout: a,
      clearAgentTimeout: i2,
      clearAgentTimeoutFailureReason: u,
      updateAgentTimeoutState: l,
      updateAgentTimeoutParticipantExists: h2
    }
  } = e4, m = r.useMemo(() => new ke2.EventEmitter(), []), d = Ce2({ room: t }), c = r.useMemo(() => d.find(
    (S) => S.kind === ParticipantInfo_Kind.AGENT && !(fo.PublishOnBehalf in S.attributes)
  ) ?? null, [d]), g = r.useMemo(() => c ? d.find(
    (S) => S.kind === ParticipantInfo_Kind.AGENT && S.attributes[fo.PublishOnBehalf] === c.identity
  ) ?? null : null, [c, d]), k = (c == null ? void 0 : c.attributes) ?? {}, M2 = le2([Track.Source.Camera, Track.Source.Microphone], {
    room: t,
    participantIdentity: c == null ? void 0 : c.identity
  }), T2 = le2([Track.Source.Camera, Track.Source.Microphone], {
    room: t,
    participantIdentity: g == null ? void 0 : g.identity
  }), w = r.useMemo(
    () => M2.find((S) => S.source === Track.Source.Camera) ?? T2.find((S) => S.source === Track.Source.Camera),
    [M2, T2]
  );
  r.useEffect(() => {
    m.emit("cameraChanged", w);
  }, [m, w]);
  const O = r.useMemo(
    () => M2.find((S) => S.source === Track.Source.Microphone) ?? T2.find((S) => S.source === Track.Source.Microphone),
    [M2, T2]
  );
  r.useEffect(() => {
    m.emit("microphoneChanged", O);
  }, [m, O]);
  const [L4, p] = r.useState(t.state);
  r.useEffect(() => {
    const S = (_3) => {
      p(_3);
    };
    return t.on(RoomEvent.ConnectionStateChanged, S), () => {
      t.off(RoomEvent.ConnectionStateChanged, S);
    };
  }, [t]), r.useEffect(() => {
    c && u();
  }, [c]);
  const [f, b] = r.useState(null);
  r.useEffect(() => {
    if (!c)
      return;
    const S = (_3) => {
      _3.identity === (c == null ? void 0 : c.identity) && b("Agent left the room unexpectedly.");
    };
    return t.on(RoomEvent.ParticipantDisconnected, S), () => {
      t.off(RoomEvent.ParticipantDisconnected, S);
    };
  }, [c, t]), r.useEffect(() => {
    L4 === ConnectionState.Disconnected && b(null);
  }, [L4]);
  const [v, C] = r.useState(
    () => t.localParticipant.getTrackPublication(Track.Source.Microphone) ?? null
  );
  r.useEffect(() => {
    const S = () => {
      C(t.localParticipant.getTrackPublication(Track.Source.Microphone) ?? null);
    }, _3 = () => {
      C(null);
    };
    return t.localParticipant.on(
      ParticipantEvent.LocalTrackPublished,
      S
    ), t.localParticipant.on(
      ParticipantEvent.LocalTrackUnpublished,
      _3
    ), () => {
      t.localParticipant.off(
        ParticipantEvent.LocalTrackPublished,
        S
      ), t.localParticipant.off(
        ParticipantEvent.LocalTrackUnpublished,
        _3
      );
    };
  }, [t.localParticipant]);
  const R = r.useMemo(() => {
    const S = [];
    return s && S.push(s), f && S.push(f), S;
  }, [s, f]), y = r.useMemo(() => {
    if (R.length > 0)
      return "failed";
    let S = "disconnected";
    return L4 !== ConnectionState.Disconnected && (S = "connecting"), v && (S = "pre-connect-buffering"), c && k[fo.AgentState] && (S = k[fo.AgentState]), S;
  }, [
    R,
    L4,
    v,
    c,
    k
  ]);
  r.useEffect(() => {
    m.emit("stateChanged", y), l(y);
  }, [m, y]), r.useEffect(() => {
    h2(c !== null);
  }, [c]);
  const N = e4.connectionState === "disconnected";
  r.useEffect(() => {
    if (!N)
      return a(o2), () => {
        i2();
      };
  }, [N, o2]);
  const {
    identity: x,
    name: j2,
    metadata: z3
  } = nn2({ participant: c ?? void 0 }), B2 = r.useMemo(() => {
    const S = {
      attributes: k,
      internal: {
        agentParticipant: c,
        workerParticipant: g,
        emitter: m
      }
    };
    switch (y) {
      case "disconnected":
        return {
          ...S,
          identity: void 0,
          name: void 0,
          metadata: void 0,
          state: y,
          ...F2(y),
          failureReasons: null,
          // Clear inner values if no longer connected
          cameraTrack: void 0,
          microphoneTrack: void 0
        };
      case "connecting":
        return {
          ...S,
          identity: void 0,
          name: void 0,
          metadata: void 0,
          state: y,
          ...F2(y),
          failureReasons: null,
          // Clear inner values if no longer connected
          cameraTrack: void 0,
          microphoneTrack: void 0
        };
      case "initializing":
      case "idle":
        return {
          ...S,
          identity: x,
          name: j2,
          metadata: z3,
          state: y,
          ...F2(y),
          failureReasons: null,
          cameraTrack: w,
          microphoneTrack: O
        };
      case "pre-connect-buffering":
        return {
          ...S,
          identity: x,
          name: j2,
          metadata: z3,
          state: y,
          ...F2(y),
          failureReasons: null,
          cameraTrack: w,
          microphoneTrack: O
        };
      case "listening":
      case "thinking":
      case "speaking":
        return {
          ...S,
          identity: x,
          name: j2,
          metadata: z3,
          state: y,
          ...F2(y),
          failureReasons: null,
          cameraTrack: w,
          microphoneTrack: O
        };
      case "failed":
        return {
          ...S,
          identity: void 0,
          name: void 0,
          metadata: void 0,
          state: "failed",
          ...F2("failed"),
          failureReasons: R,
          // Clear inner values if no longer connected
          cameraTrack: void 0,
          microphoneTrack: void 0
        };
    }
  }, [
    x,
    j2,
    z3,
    k,
    m,
    c,
    y,
    w,
    O
  ]), { waitUntilConnected: K3, waitUntilCouldBeListening: Q3, waitUntilFinished: Y2 } = Mn2(m, y), ne2 = r.useCallback(
    (S) => new Promise((_3, Z3) => {
      const $2 = (D2) => {
        D2 && (E(), _3(D2));
      }, q3 = () => {
        E(), Z3(new Error("useAgent(/* ... */).waitUntilCamera - signal aborted"));
      }, E = () => {
        m.off("cameraChanged", $2), S == null ? void 0 : S.removeEventListener("abort", q3);
      };
      m.on("cameraChanged", $2), S == null ? void 0 : S.addEventListener("abort", q3);
    }),
    [m]
  ), re2 = r.useCallback(
    (S) => new Promise((_3, Z3) => {
      const $2 = (D2) => {
        D2 && (E(), _3(D2));
      }, q3 = () => {
        E(), Z3(new Error("useAgent(/* ... */).waitUntilMicrophone - signal aborted"));
      }, E = () => {
        m.off("microphoneChanged", $2), S == null ? void 0 : S.removeEventListener("abort", q3);
      };
      m.on("microphoneChanged", $2), S == null ? void 0 : S.addEventListener("abort", q3);
    }),
    [m]
  );
  return r.useMemo(() => ({
    ...B2,
    waitUntilConnected: K3,
    waitUntilCouldBeListening: Q3,
    waitUntilFinished: Y2,
    waitUntilCamera: ne2,
    waitUntilMicrophone: re2
  }), [
    B2,
    K3,
    Q3,
    Y2,
    ne2,
    re2
  ]);
}
var wn2 = ((e4) => (e4.ConnectionStateChanged = "connectionStateChanged", e4.MediaDevicesError = "mediaDevicesError", e4.EncryptionError = "encryptionError", e4))(wn2 || {});
function Pn2(e4) {
  return typeof e4 == "object" && e4 !== null && "room" in e4 && "connectionState" in e4 && "internal" in e4;
}
function Rn2(e4, n) {
  const t = /* @__PURE__ */ new Set([...Object.keys(e4), ...Object.keys(n)]);
  for (const o2 of t)
    switch (o2) {
      case "roomName":
      case "participantName":
      case "participantIdentity":
      case "participantMetadata":
      case "participantAttributes":
      case "agentName":
      case "agentMetadata":
        if (e4[o2] !== n[o2])
          return false;
        break;
      default:
        const s = o2;
        throw new Error(`Options key ${s} not being checked for equality!`);
    }
  return true;
}
function Ln2(e4, n) {
  const t = r.useRef(n);
  return r.useEffect(() => {
    t.current = n;
  }, [n]), r.useCallback(
    async (s, a) => {
      if (t.current !== s)
        return new Promise((i2, u) => {
          const l = (d) => {
            d === s && (m(), i2());
          }, h2 = () => {
            m(), u(
              new Error(
                `useSession(/* ... */).waitUntilConnectionState(${s}, /* signal */) - signal aborted`
              )
            );
          }, m = () => {
            e4.off("connectionStateChanged", l), a == null ? void 0 : a.removeEventListener("abort", h2);
          };
          e4.on("connectionStateChanged", l), a == null ? void 0 : a.addEventListener("abort", h2);
        });
    },
    [e4]
  );
}
function An2(e4, n) {
  const t = e4 instanceof TokenSourceConfigurable, o2 = r.useRef(
    t ? n : null
  );
  return r.useEffect(() => {
    if (!t) {
      o2.current = null;
      return;
    }
    o2.current !== null && Rn2(o2.current, n) || (o2.current = n);
  }, [t, n]), r.useCallback(
    async (a) => {
      if (t) {
        if (!o2.current)
          throw new Error(
            "AgentSession - memoized token fetch options are not set, but the passed tokenSource was an instance of TokenSourceConfigurable. If you are seeing this please make a new GitHub issue!"
          );
        return e4.fetch(o2.current, a);
      } else
        return e4.fetch();
    },
    [t, e4]
  );
}
function Sr2(e4, n = {}) {
  const {
    room: t,
    agentConnectTimeoutMilliseconds: o2,
    encryption: s,
    ...a
  } = n, i2 = s && "e2eeManager" in s ? s.e2eeManager : null, u = s && !("e2eeManager" in s) ? s.key ?? null : null, l = s && !("e2eeManager" in s) ? s.worker ?? null : null, h2 = Qo(), m = r.useRef(null), d = r.useMemo(() => typeof u == "string" || u instanceof ArrayBuffer ? (m.current === null && (m.current = new ExternalE2EEKeyProvider()), m.current.setKey(u).catch((E) => I.error(E)), m.current) : u, [u]), c = r.useMemo(() => {
    const E = h2 ?? t;
    if (E)
      return E;
    const D2 = !!(d && l), W3 = !!i2, fe3 = D2 || W3, ee3 = {};
    D2 ? ee3.encryption = {
      keyProvider: d,
      worker: l
    } : W3 ? ee3.encryption = {
      e2eeManager: i2
    } : s !== void 0 && I.warn(
      "useSession options encryption was set, but neither encryption.key with encryption.worker nor encryption.e2eeManager was provided."
    );
    const ae3 = new Room(ee3);
    return fe3 && ae3.setE2EEEnabled(true), ae3;
  }, [h2, t, d, l, i2]);
  r.useEffect(() => () => {
    c.disconnect();
  }, [c]);
  const g = r.useMemo(
    () => new ke2.EventEmitter(),
    []
  ), k = r.useCallback(
    (E) => ({
      isConnected: E === ConnectionState.Connected || E === ConnectionState.Reconnecting || E === ConnectionState.SignalReconnecting
    }),
    []
  ), [M2, T2] = r.useState(c.state);
  r.useEffect(() => {
    const E = (D2) => {
      T2(D2);
    };
    return c.on(RoomEvent.ConnectionStateChanged, E), () => {
      c.off(RoomEvent.ConnectionStateChanged, E);
    };
  }, [c]), r.useEffect(() => {
    const E = async (D2) => {
      g.emit("mediaDevicesError", D2);
    };
    return c.on(RoomEvent.MediaDevicesError, E), () => {
      c.off(RoomEvent.MediaDevicesError, E);
    };
  }, [c, g]), r.useEffect(() => {
    const E = async (D2) => {
      g.emit("encryptionError", D2);
    };
    return c.on(RoomEvent.EncryptionError, E), () => {
      c.off(RoomEvent.EncryptionError, E);
    };
  }, [c, g]);
  const { localParticipant: w } = Ve2({ room: c }), O = w.getTrackPublication(Track.Source.Camera), L4 = r.useMemo(() => {
    if (O)
      return {
        source: Track.Source.Camera,
        participant: w,
        publication: O
      };
  }, [w, O]), p = w.getTrackPublication(Track.Source.Microphone), f = r.useMemo(() => {
    if (p)
      return {
        source: Track.Source.Microphone,
        participant: w,
        publication: p
      };
  }, [w, p]), b = w.getTrackPublication(Track.Source.ScreenShare), v = r.useMemo(() => {
    if (b)
      return {
        source: Track.Source.ScreenShare,
        participant: w,
        publication: b
      };
  }, [w, b]), {
    agentTimeoutFailureReason: C,
    startAgentTimeout: R,
    clearAgentTimeout: y,
    clearAgentTimeoutFailureReason: N,
    updateAgentTimeoutState: x,
    updateAgentTimeoutParticipantExists: j2
  } = Tn2(), z3 = r.useMemo(
    () => ({
      emitter: g,
      tokenSource: e4,
      agentConnectTimeoutMilliseconds: o2,
      agentTimeoutFailureReason: C,
      startAgentTimeout: R,
      clearAgentTimeout: y,
      clearAgentTimeoutFailureReason: N,
      updateAgentTimeoutState: x,
      updateAgentTimeoutParticipantExists: j2
    }),
    [
      g,
      o2,
      e4,
      C,
      R,
      y,
      N,
      x,
      j2
    ]
  ), B2 = r.useMemo(() => {
    const E = {
      room: c,
      internal: z3
    };
    switch (M2) {
      case ConnectionState.Connecting:
        return {
          ...E,
          connectionState: ConnectionState.Connecting,
          ...k(ConnectionState.Connecting),
          local: {
            cameraTrack: void 0,
            microphoneTrack: void 0,
            screenShareTrack: void 0
          }
        };
      case ConnectionState.Connected:
      case ConnectionState.Reconnecting:
      case ConnectionState.SignalReconnecting:
        return {
          ...E,
          connectionState: M2,
          ...k(M2),
          local: {
            cameraTrack: L4,
            microphoneTrack: f,
            screenShareTrack: v
          }
        };
      case ConnectionState.Disconnected:
        return {
          ...E,
          connectionState: ConnectionState.Disconnected,
          ...k(ConnectionState.Disconnected),
          local: {
            cameraTrack: void 0,
            microphoneTrack: void 0,
            screenShareTrack: void 0
          }
        };
    }
  }, [
    z3,
    c,
    M2,
    L4,
    f,
    k
  ]);
  r.useEffect(() => {
    g.emit("connectionStateChanged", B2.connectionState);
  }, [g, B2.connectionState]);
  const K3 = Ln2(
    g,
    B2.connectionState
  ), Q3 = r.useCallback(
    async (E) => K3(
      ConnectionState.Connected,
      E
    ),
    [K3]
  ), Y2 = r.useCallback(
    async (E) => K3(ConnectionState.Disconnected, E),
    [K3]
  ), ne2 = r.useCallback(
    async (E) => c.setE2EEEnabled(E),
    [c]
  ), re2 = je2(
    r.useMemo(
      () => ({
        connectionState: B2.connectionState,
        room: c,
        internal: z3
      }),
      [B2, c, z3]
    )
  ), S = An2(e4, a), _3 = r.useRef(false), Z3 = r.useCallback(
    async (E = {}) => {
      var _a2, _b2, _c, _d, _e2, _f;
      const {
        signal: D2,
        tracks: W3 = { microphone: { enabled: true, publishOptions: { preConnectBuffer: true } } },
        roomConnectOptions: fe3
      } = E;
      await Y2(D2), _3.current = false;
      const ee3 = () => {
        c.disconnect();
      };
      D2 == null ? void 0 : D2.addEventListener("abort", ee3);
      const ae3 = () => {
        _3.current || S(true);
      };
      c.once(RoomEvent.Disconnected, ae3);
      let Ee2 = false;
      await Promise.all([
        S().then(({ serverUrl: Be2, participantToken: Te2 }) => {
          var _a3, _b3;
          return Ee2 = (((_b3 = (_a3 = decodeTokenPayload(Te2).roomConfig) == null ? void 0 : _a3.agents) == null ? void 0 : _b3.length) ?? 0) > 0, c.connect(Be2, Te2, fe3);
        }),
        // Start microphone (with preconnect buffer) by default
        ((_a2 = W3.microphone) == null ? void 0 : _a2.enabled) ? c.localParticipant.setMicrophoneEnabled(
          true,
          void 0,
          ((_b2 = W3.microphone) == null ? void 0 : _b2.publishOptions) ?? {}
        ) : Promise.resolve(),
        ((_c = W3.camera) == null ? void 0 : _c.enabled) ? c.localParticipant.setCameraEnabled(
          true,
          void 0,
          ((_d = W3.camera) == null ? void 0 : _d.publishOptions) ?? {}
        ) : Promise.resolve(),
        ((_e2 = W3.screenShare) == null ? void 0 : _e2.enabled) ? c.localParticipant.setScreenShareEnabled(
          true,
          void 0,
          ((_f = W3.screenShare) == null ? void 0 : _f.publishOptions) ?? {}
        ) : Promise.resolve()
      ]), await Q3(D2), Ee2 && await re2.waitUntilConnected(D2), D2 == null ? void 0 : D2.removeEventListener("abort", ee3);
    },
    [c, Y2, S, Q3, re2.waitUntilConnected]
  ), $2 = r.useCallback(async () => {
    _3.current = true, S(true), await c.disconnect();
  }, [c, S]), q3 = r.useCallback(async () => {
    const E = await S();
    await c.prepareConnection(E.serverUrl, E.participantToken);
  }, [S, c]);
  return r.useEffect(
    () => {
      q3().catch((E) => {
        console.warn("WARNING: Room.prepareConnection failed:", E);
      });
    },
    [
      /* note: no prepareConnection here, this effect should only ever run once! */
    ]
  ), r.useMemo(
    () => ({
      ...B2,
      waitUntilConnected: Q3,
      waitUntilDisconnected: Y2,
      prepareConnection: q3,
      start: Z3,
      end: $2,
      setEncryptionEnabled: ne2
    }),
    [
      B2,
      Q3,
      Y2,
      q3,
      Z3,
      $2,
      ne2
    ]
  );
}
function Cr2(e4, n, t, o2) {
  const s = r.useMemo(() => () => {
  }, []), a = r.useCallback(t ?? s, o2 ?? []), i2 = o2 ? a : t, u = r.useMemo(() => e4 ? "internal" in e4 ? e4.internal.emitter : e4 : null, [e4]);
  r.useEffect(() => {
    if (!(!u || !i2))
      return u.on(n, i2), () => {
        u.off(n, i2);
      };
  }, [u, n, i2]);
}
var On2 = ((e4) => (e4.MessageReceived = "messageReceived", e4))(On2 || {});
function kr2(e4) {
  const { room: n } = da(e4), t = r.useMemo(
    () => new ke2.EventEmitter(),
    []
  ), o2 = je2(e4), s = Sn2({ room: n }), a = r.useMemo(() => ({ room: n }), [n]), i2 = un2(a), u = r.useMemo(() => s.map((c) => {
    var _a2, _b2, _c;
    switch (c.participantInfo.identity) {
      case n.localParticipant.identity:
        return {
          type: "userTranscript",
          message: c.text,
          id: c.streamInfo.id,
          timestamp: c.streamInfo.timestamp,
          from: n.localParticipant
        };
      case ((_a2 = o2.internal.agentParticipant) == null ? void 0 : _a2.identity):
      case ((_b2 = o2.internal.workerParticipant) == null ? void 0 : _b2.identity):
        return {
          type: "agentTranscript",
          message: c.text,
          id: c.streamInfo.id,
          timestamp: c.streamInfo.timestamp,
          from: ((_c = o2.internal.agentParticipant) == null ? void 0 : _c.identity) === c.participantInfo.identity ? o2.internal.agentParticipant : o2.internal.workerParticipant
        };
      default:
        return {
          type: "agentTranscript",
          message: c.text,
          id: c.streamInfo.id,
          timestamp: c.streamInfo.timestamp,
          from: Array.from(n.remoteParticipants.values()).find(
            (g) => g.identity === c.participantInfo.identity
          )
        };
    }
  }), [s, n]), l = r.useMemo(() => [...u, ...i2.chatMessages], [u, i2.chatMessages]), h2 = r.useRef(/* @__PURE__ */ new Map()), m = r.useMemo(() => {
    const c = /* @__PURE__ */ new Date();
    for (const g of l)
      h2.current.has(g.id) || h2.current.set(g.id, c);
    return l.sort((g, k) => {
      const M2 = h2.current.get(g.id), T2 = h2.current.get(k.id);
      return typeof M2 > "u" || typeof T2 > "u" ? 0 : M2.getTime() - T2.getTime();
    });
  }, [l]), d = r.useRef(/* @__PURE__ */ new Set());
  return r.useEffect(() => {
    for (const c of m)
      d.current.has(c.id) || (d.current.add(c.id), t.emit("messageReceived", c));
  }, [m]), r.useMemo(
    () => ({
      messages: m,
      send: i2.send,
      isSending: i2.isSending,
      internal: { emitter: t }
    }),
    [m, i2.send, i2.isSending]
  );
}
function Er2(e4, n, t, o2) {
  let s, a, i2, u;
  Pn2(e4) ? (s = e4, a = n, i2 = t, u = o2) : (a = e4, i2 = n, u = t);
  const { room: l } = da(s), h2 = r.useRef(i2);
  h2.current = i2;
  const m = r.useRef(u);
  m.current = u, r.useEffect(() => {
    if (a)
      return l.registerRpcMethod(a, async (c) => {
        var _a2, _b2;
        const g = (_a2 = m.current) == null ? void 0 : _a2.fromIdentity;
        if (g && c.callerIdentity !== g)
          throw RpcError.builtIn(
            "UNSUPPORTED_METHOD",
            `Method not available for caller ${c.callerIdentity}`
          );
        const k = h2.current;
        if (!k)
          throw RpcError.builtIn(
            "APPLICATION_ERROR",
            `No handler registered for method "${a}"`
          );
        const M2 = ((_b2 = m.current) == null ? void 0 : _b2.serializer) ?? serializers.json();
        let T2;
        try {
          T2 = M2.parse(c.payload);
        } catch (O) {
          throw RpcError.builtIn("APPLICATION_ERROR", `Failed to parse RPC payload: ${O}`);
        }
        const w = await k(T2, c);
        try {
          return M2.serialize(w);
        } catch (O) {
          throw RpcError.builtIn("APPLICATION_ERROR", `Failed to serialize RPC response: ${O}`);
        }
      }), () => {
        l.unregisterRpcMethod(a);
      };
  }, [l, a]);
  const d = r.useCallback(
    async (c, g = serializers.json()) => {
      if (isSerializer(g)) {
        let k;
        try {
          k = g.serialize(c.payload);
        } catch (T2) {
          throw RpcError.builtIn("APPLICATION_ERROR", `Failed to serialize RPC payload: ${T2}`);
        }
        const M2 = await l.localParticipant.performRpc({
          destinationIdentity: c.destinationIdentity,
          method: c.method,
          payload: k,
          responseTimeout: c.responseTimeout
        });
        try {
          return g.parse(M2);
        } catch (T2) {
          throw RpcError.builtIn("APPLICATION_ERROR", `Failed to parse RPC response: ${T2}`);
        }
      } else
        return l.localParticipant.performRpc(c);
    },
    [l]
  );
  return r.useMemo(() => ({ perform: d }), [d]);
}

// node_modules/@livekit/components-react/dist/prefabs.mjs
var e3 = __toESM(require_react(), 1);

// node_modules/@livekit/components-react/dist/components-zhA5OhZT.mjs
var e2 = __toESM(require_react(), 1);
var import_react2 = __toESM(require_react(), 1);
var cn3 = e2.forwardRef(
  function(n, a) {
    const { buttonProps: c } = Hn2(n);
    return e2.createElement("button", { ref: a, ...c }, n.children);
  }
);
var ln3 = e2.forwardRef(
  function({ room: n, ...a }, c) {
    const r2 = oe2(n);
    return e2.createElement("div", { ref: c, ...a }, r2);
  }
);
var on3 = e2.forwardRef(
  function(n, a) {
    const { mergedProps: c } = or2({ props: n });
    return e2.createElement("button", { ref: a, ...c }, n.children);
  }
);
var sn3 = e2.forwardRef(
  function(n, a) {
    const { buttonProps: c } = Bn2(n);
    return e2.createElement("button", { ref: a, ...c }, n.children);
  }
);
var Et2 = (t) => e2.createElement("svg", { xmlns: "http://www.w3.org/2000/svg", width: 16, height: 16, fill: "currentColor", ...t }, e2.createElement("path", { d: "M1.354.646a.5.5 0 1 0-.708.708l14 14a.5.5 0 0 0 .708-.708L11 10.293V4.5A1.5 1.5 0 0 0 9.5 3H3.707zM0 4.5a1.5 1.5 0 0 1 .943-1.393l9.532 9.533c-.262.224-.603.36-.975.36h-8A1.5 1.5 0 0 1 0 11.5z" }), e2.createElement("path", { d: "m15.2 3.6-2.8 2.1a1 1 0 0 0-.4.8v3a1 1 0 0 0 .4.8l2.8 2.1a.5.5 0 0 0 .8-.4V4a.5.5 0 0 0-.8-.4" }));
var wt2 = (t) => e2.createElement("svg", { xmlns: "http://www.w3.org/2000/svg", width: 16, height: 16, fill: "currentColor", ...t }, e2.createElement("path", { d: "M0 4.5A1.5 1.5 0 0 1 1.5 3h8A1.5 1.5 0 0 1 11 4.5v7A1.5 1.5 0 0 1 9.5 13h-8A1.5 1.5 0 0 1 0 11.5zM15.2 3.6l-2.8 2.1a1 1 0 0 0-.4.8v3a1 1 0 0 0 .4.8l2.8 2.1a.5.5 0 0 0 .8-.4V4a.5.5 0 0 0-.8-.4" }));
var un3 = (t) => e2.createElement("svg", { xmlns: "http://www.w3.org/2000/svg", width: 16, height: 16, viewBox: "0 0 24 24", ...t }, e2.createElement(
  "path",
  {
    fill: "#FFF",
    d: "M4.99 3.99a1 1 0 0 0-.697 1.717L10.586 12l-6.293 6.293a1 1 0 1 0 1.414 1.414L12 13.414l6.293 6.293a1 1 0 1 0 1.414-1.414L13.414 12l6.293-6.293a1 1 0 0 0-.727-1.717 1 1 0 0 0-.687.303L12 10.586 5.707 4.293a1 1 0 0 0-.717-.303"
  }
));
var dn3 = (t) => e2.createElement("svg", { xmlns: "http://www.w3.org/2000/svg", width: 16, height: 18, fill: "none", ...t }, e2.createElement(
  "path",
  {
    fill: "currentColor",
    fillRule: "evenodd",
    d: "M0 2.75A2.75 2.75 0 0 1 2.75 0h10.5A2.75 2.75 0 0 1 16 2.75v13.594a.75.75 0 0 1-1.234.572l-3.691-3.12a1.25 1.25 0 0 0-.807-.296H2.75A2.75 2.75 0 0 1 0 10.75zM2.75 1.5c-.69 0-1.25.56-1.25 1.25v8c0 .69.56 1.25 1.25 1.25h7.518c.65 0 1.279.23 1.775.65l2.457 2.077V2.75c0-.69-.56-1.25-1.25-1.25z",
    clipRule: "evenodd"
  }
), e2.createElement(
  "path",
  {
    fill: "currentColor",
    fillRule: "evenodd",
    d: "M3 4.5a.5.5 0 0 1 .5-.5h9a.5.5 0 0 1 0 1h-9a.5.5 0 0 1-.5-.5m0 2a.5.5 0 0 1 .5-.5h9a.5.5 0 0 1 0 1h-9a.5.5 0 0 1-.5-.5m0 2a.5.5 0 0 1 .5-.5h5a.5.5 0 0 1 0 1h-5a.5.5 0 0 1-.5-.5",
    clipRule: "evenodd"
  }
));
var oe3 = (t) => e2.createElement("svg", { xmlns: "http://www.w3.org/2000/svg", width: 16, height: 16, fill: "none", ...t }, e2.createElement(
  "path",
  {
    fill: "currentcolor",
    fillRule: "evenodd",
    d: "M5.293 2.293a1 1 0 0 1 1.414 0l4.823 4.823a1.25 1.25 0 0 1 0 1.768l-4.823 4.823a1 1 0 0 1-1.414-1.414L9.586 8 5.293 3.707a1 1 0 0 1 0-1.414",
    clipRule: "evenodd"
  }
));
var kt2 = (t) => e2.createElement("svg", { xmlns: "http://www.w3.org/2000/svg", width: 16, height: 16, fill: "none", ...t }, e2.createElement("g", { stroke: "currentColor", strokeLinecap: "round", strokeLinejoin: "round", strokeWidth: 1.5 }, e2.createElement("path", { d: "M10 1.75h4.25m0 0V6m0-4.25L9 7M6 14.25H1.75m0 0V10m0 4.25L7 9" })));
var mn3 = (t) => e2.createElement("svg", { xmlns: "http://www.w3.org/2000/svg", width: 16, height: 16, fill: "none", ...t }, e2.createElement(
  "path",
  {
    fill: "currentcolor",
    fillRule: "evenodd",
    d: "M8.961.894C8.875-.298 7.125-.298 7.04.894c-.066.912-1.246 1.228-1.76.472-.67-.99-2.186-.115-1.664.96.399.824-.465 1.688-1.288 1.289-1.076-.522-1.95.994-.961 1.665.756.513.44 1.693-.472 1.759-1.192.086-1.192 1.836 0 1.922.912.066 1.228 1.246.472 1.76-.99.67-.115 2.186.96 1.664.824-.399 1.688.465 1.289 1.288-.522 1.076.994 1.95 1.665.961.513-.756 1.693-.44 1.759.472.086 1.192 1.836 1.192 1.922 0 .066-.912 1.246-1.228 1.76-.472.67.99 2.186.115 1.664-.96-.399-.824.465-1.687 1.288-1.289 1.076.522 1.95-.994.961-1.665-.756-.513-.44-1.693.472-1.759 1.192-.086 1.192-1.836 0-1.922-.912-.066-1.228-1.246-.472-1.76.99-.67.115-2.186-.96-1.664-.824.399-1.687-.465-1.289-1.288.522-1.076-.994-1.95-1.665-.961-.513.756-1.693.44-1.759-.472M8 13A5 5 0 1 0 8 3a5 5 0 0 0 0 10",
    clipRule: "evenodd"
  }
));
var fn3 = (t) => e2.createElement("svg", { xmlns: "http://www.w3.org/2000/svg", width: 16, height: 16, fill: "none", ...t }, e2.createElement(
  "path",
  {
    fill: "currentColor",
    fillRule: "evenodd",
    d: "M2 2.75A2.75 2.75 0 0 1 4.75 0h6.5A2.75 2.75 0 0 1 14 2.75v10.5A2.75 2.75 0 0 1 11.25 16h-6.5A2.75 2.75 0 0 1 2 13.25v-.5a.75.75 0 0 1 1.5 0v.5c0 .69.56 1.25 1.25 1.25h6.5c.69 0 1.25-.56 1.25-1.25V2.75c0-.69-.56-1.25-1.25-1.25h-6.5c-.69 0-1.25.56-1.25 1.25v.5a.75.75 0 0 1-1.5 0z",
    clipRule: "evenodd"
  }
), e2.createElement(
  "path",
  {
    fill: "currentColor",
    fillRule: "evenodd",
    d: "M8.78 7.47a.75.75 0 0 1 0 1.06l-2.25 2.25a.75.75 0 1 1-1.06-1.06l.97-.97H1.75a.75.75 0 0 1 0-1.5h4.69l-.97-.97a.75.75 0 0 1 1.06-1.06z",
    clipRule: "evenodd"
  }
));
var bt2 = (t) => e2.createElement("svg", { xmlns: "http://www.w3.org/2000/svg", width: 16, height: 16, fill: "none", ...t }, e2.createElement(
  "path",
  {
    fill: "currentcolor",
    fillRule: "evenodd",
    d: "M4 6.104V4a4 4 0 1 1 8 0v2.104c1.154.326 2 1.387 2 2.646v4.5A2.75 2.75 0 0 1 11.25 16h-6.5A2.75 2.75 0 0 1 2 13.25v-4.5c0-1.259.846-2.32 2-2.646M5.5 4a2.5 2.5 0 0 1 5 0v2h-5z",
    clipRule: "evenodd"
  }
));
var Rt2 = (t) => e2.createElement("svg", { xmlns: "http://www.w3.org/2000/svg", width: 16, height: 16, fill: "currentColor", ...t }, e2.createElement("path", { d: "M12.227 11.52a5.48 5.48 0 0 0 1.246-2.97.5.5 0 0 0-.995-.1 4.5 4.5 0 0 1-.962 2.359l-1.07-1.07C10.794 9.247 11 8.647 11 8V3a3 3 0 0 0-6 0v1.293L1.354.646a.5.5 0 1 0-.708.708l14 14a.5.5 0 0 0 .708-.708zM8 12.5c.683 0 1.33-.152 1.911-.425l.743.743c-.649.359-1.378.59-2.154.66V15h2a.5.5 0 0 1 0 1h-5a.5.5 0 0 1 0-1h2v-1.522a5.5 5.5 0 0 1-4.973-4.929.5.5 0 0 1 .995-.098A4.5 4.5 0 0 0 8 12.5" }), e2.createElement("path", { d: "M8.743 10.907 5 7.164V8a3 3 0 0 0 3.743 2.907" }));
var yt2 = (t) => e2.createElement("svg", { xmlns: "http://www.w3.org/2000/svg", width: 16, height: 16, fill: "currentColor", ...t }, e2.createElement(
  "path",
  {
    fillRule: "evenodd",
    d: "M2.975 8.002a.5.5 0 0 1 .547.449 4.5 4.5 0 0 0 8.956 0 .5.5 0 1 1 .995.098A5.5 5.5 0 0 1 8.5 13.478V15h2a.5.5 0 0 1 0 1h-5a.5.5 0 0 1 0-1h2v-1.522a5.5 5.5 0 0 1-4.973-4.929.5.5 0 0 1 .448-.547",
    clipRule: "evenodd"
  }
), e2.createElement("path", { d: "M5 3a3 3 0 1 1 6 0v5a3 3 0 0 1-6 0z" }));
var Mt2 = (t) => e2.createElement("svg", { xmlns: "http://www.w3.org/2000/svg", width: 16, height: 16, fill: "currentcolor", ...t }, e2.createElement("path", { d: "M0 11.5a.5.5 0 0 1 .5-.5h3a.5.5 0 0 1 .5.5v4a.5.5 0 0 1-.5.5h-3a.5.5 0 0 1-.5-.5zm6-5a.5.5 0 0 1 .5-.5h3a.5.5 0 0 1 .5.5v9a.5.5 0 0 1-.5.5h-3a.5.5 0 0 1-.5-.5zm6-6a.5.5 0 0 1 .5-.5h3a.5.5 0 0 1 .5.5v15a.5.5 0 0 1-.5.5h-3a.5.5 0 0 1-.5-.5z" }), e2.createElement("path", { d: "M0 11.5a.5.5 0 0 1 .5-.5h3a.5.5 0 0 1 .5.5v4a.5.5 0 0 1-.5.5h-3a.5.5 0 0 1-.5-.5zm6-5a.5.5 0 0 1 .5-.5h3a.5.5 0 0 1 .5.5v9a.5.5 0 0 1-.5.5h-3a.5.5 0 0 1-.5-.5zm6-6a.5.5 0 0 1 .5-.5h3a.5.5 0 0 1 .5.5v15a.5.5 0 0 1-.5.5h-3a.5.5 0 0 1-.5-.5z" }));
var St2 = (t) => e2.createElement("svg", { xmlns: "http://www.w3.org/2000/svg", width: 16, height: 16, fill: "currentcolor", ...t }, e2.createElement("path", { d: "M0 11.5a.5.5 0 0 1 .5-.5h3a.5.5 0 0 1 .5.5v4a.5.5 0 0 1-.5.5h-3a.5.5 0 0 1-.5-.5zm6-5a.5.5 0 0 1 .5-.5h3a.5.5 0 0 1 .5.5v9a.5.5 0 0 1-.5.5h-3a.5.5 0 0 1-.5-.5z" }), e2.createElement("path", { d: "M0 11.5a.5.5 0 0 1 .5-.5h3a.5.5 0 0 1 .5.5v4a.5.5 0 0 1-.5.5h-3a.5.5 0 0 1-.5-.5zm6-5a.5.5 0 0 1 .5-.5h3a.5.5 0 0 1 .5.5v9a.5.5 0 0 1-.5.5h-3a.5.5 0 0 1-.5-.5z" }), e2.createElement("g", { opacity: 0.25 }, e2.createElement("path", { d: "M12 .5a.5.5 0 0 1 .5-.5h3a.5.5 0 0 1 .5.5v15a.5.5 0 0 1-.5.5h-3a.5.5 0 0 1-.5-.5z" }), e2.createElement("path", { d: "M12 .5a.5.5 0 0 1 .5-.5h3a.5.5 0 0 1 .5.5v15a.5.5 0 0 1-.5.5h-3a.5.5 0 0 1-.5-.5z" })));
var Ct2 = (t) => e2.createElement("svg", { xmlns: "http://www.w3.org/2000/svg", width: 16, height: 16, fill: "currentcolor", ...t }, e2.createElement("path", { d: "M0 11.5a.5.5 0 0 1 .5-.5h3a.5.5 0 0 1 .5.5v4a.5.5 0 0 1-.5.5h-3a.5.5 0 0 1-.5-.5z" }), e2.createElement("path", { d: "M0 11.5a.5.5 0 0 1 .5-.5h3a.5.5 0 0 1 .5.5v4a.5.5 0 0 1-.5.5h-3a.5.5 0 0 1-.5-.5z" }), e2.createElement("g", { opacity: 0.25 }, e2.createElement("path", { d: "M6 6.5a.5.5 0 0 1 .5-.5h3a.5.5 0 0 1 .5.5v9a.5.5 0 0 1-.5.5h-3a.5.5 0 0 1-.5-.5z" }), e2.createElement("path", { d: "M6 6.5a.5.5 0 0 1 .5-.5h3a.5.5 0 0 1 .5.5v9a.5.5 0 0 1-.5.5h-3a.5.5 0 0 1-.5-.5zm6-6a.5.5 0 0 1 .5-.5h3a.5.5 0 0 1 .5.5v15a.5.5 0 0 1-.5.5h-3a.5.5 0 0 1-.5-.5z" }), e2.createElement("path", { d: "M12 .5a.5.5 0 0 1 .5-.5h3a.5.5 0 0 1 .5.5v15a.5.5 0 0 1-.5.5h-3a.5.5 0 0 1-.5-.5z" })));
var It2 = (t) => e2.createElement("svg", { xmlns: "http://www.w3.org/2000/svg", width: 16, height: 16, fill: "currentColor", ...t }, e2.createElement("g", { opacity: 0.25 }, e2.createElement("path", { d: "M0 11.5a.5.5 0 0 1 .5-.5h3a.5.5 0 0 1 .5.5v4a.5.5 0 0 1-.5.5h-3a.5.5 0 0 1-.5-.5zm6-5a.5.5 0 0 1 .5-.5h3a.5.5 0 0 1 .5.5v9a.5.5 0 0 1-.5.5h-3a.5.5 0 0 1-.5-.5zm6-6a.5.5 0 0 1 .5-.5h3a.5.5 0 0 1 .5.5v15a.5.5 0 0 1-.5.5h-3a.5.5 0 0 1-.5-.5z" }), e2.createElement("path", { d: "M0 11.5a.5.5 0 0 1 .5-.5h3a.5.5 0 0 1 .5.5v4a.5.5 0 0 1-.5.5h-3a.5.5 0 0 1-.5-.5zm6-5a.5.5 0 0 1 .5-.5h3a.5.5 0 0 1 .5.5v9a.5.5 0 0 1-.5.5h-3a.5.5 0 0 1-.5-.5zm6-6a.5.5 0 0 1 .5-.5h3a.5.5 0 0 1 .5.5v15a.5.5 0 0 1-.5.5h-3a.5.5 0 0 1-.5-.5z" })));
var be2 = (t) => e2.createElement("svg", { xmlns: "http://www.w3.org/2000/svg", width: 20, height: 16, fill: "none", ...t }, e2.createElement(
  "path",
  {
    fill: "currentColor",
    fillRule: "evenodd",
    d: "M0 2.75A2.75 2.75 0 0 1 2.75 0h14.5A2.75 2.75 0 0 1 20 2.75v10.5A2.75 2.75 0 0 1 17.25 16H2.75A2.75 2.75 0 0 1 0 13.25zM2.75 1.5c-.69 0-1.25.56-1.25 1.25v10.5c0 .69.56 1.25 1.25 1.25h14.5c.69 0 1.25-.56 1.25-1.25V2.75c0-.69-.56-1.25-1.25-1.25z",
    clipRule: "evenodd"
  }
), e2.createElement(
  "path",
  {
    fill: "currentColor",
    fillRule: "evenodd",
    d: "M9.47 4.22a.75.75 0 0 1 1.06 0l2.25 2.25a.75.75 0 0 1-1.06 1.06l-.97-.97v4.69a.75.75 0 0 1-1.5 0V6.56l-.97.97a.75.75 0 0 1-1.06-1.06z",
    clipRule: "evenodd"
  }
));
var xt2 = (t) => e2.createElement("svg", { xmlns: "http://www.w3.org/2000/svg", width: 20, height: 16, fill: "none", ...t }, e2.createElement("g", { fill: "currentColor" }, e2.createElement("path", { d: "M7.28 4.22a.75.75 0 0 0-1.06 1.06L8.94 8l-2.72 2.72a.75.75 0 1 0 1.06 1.06L10 9.06l2.72 2.72a.75.75 0 1 0 1.06-1.06L11.06 8l2.72-2.72a.75.75 0 0 0-1.06-1.06L10 6.94z" }), e2.createElement(
  "path",
  {
    fillRule: "evenodd",
    d: "M2.75 0A2.75 2.75 0 0 0 0 2.75v10.5A2.75 2.75 0 0 0 2.75 16h14.5A2.75 2.75 0 0 0 20 13.25V2.75A2.75 2.75 0 0 0 17.25 0zM1.5 2.75c0-.69.56-1.25 1.25-1.25h14.5c.69 0 1.25.56 1.25 1.25v10.5c0 .69-.56 1.25-1.25 1.25H2.75c-.69 0-1.25-.56-1.25-1.25z",
    clipRule: "evenodd"
  }
)));
var se2 = (t) => e2.createElement("svg", { xmlns: "http://www.w3.org/2000/svg", width: 16, height: 16, fill: "none", ...t }, e2.createElement(
  "path",
  {
    fill: "currentColor",
    fillRule: "evenodd",
    d: "M8 0a.75.75 0 0 1 .75.75v2.5a.75.75 0 0 1-1.5 0V.75A.75.75 0 0 1 8 0",
    clipRule: "evenodd"
  }
), e2.createElement(
  "path",
  {
    fill: "currentColor",
    fillRule: "evenodd",
    d: "M8 12a.75.75 0 0 1 .75.75v2.5a.75.75 0 0 1-1.5 0v-2.5A.75.75 0 0 1 8 12",
    clipRule: "evenodd",
    opacity: 0.7
  }
), e2.createElement(
  "path",
  {
    fill: "currentColor",
    fillRule: "evenodd",
    d: "M12 1.072a.75.75 0 0 1 .274 1.024l-1.25 2.165a.75.75 0 0 1-1.299-.75l1.25-2.165A.75.75 0 0 1 12 1.072",
    clipRule: "evenodd"
  }
), e2.createElement(
  "path",
  {
    fill: "currentColor",
    fillRule: "evenodd",
    d: "M6 11.464a.75.75 0 0 1 .274 1.025l-1.25 2.165a.75.75 0 0 1-1.299-.75l1.25-2.165A.75.75 0 0 1 6 11.464",
    clipRule: "evenodd",
    opacity: 0.6
  }
), e2.createElement(
  "path",
  {
    fill: "currentColor",
    fillRule: "evenodd",
    d: "M14.928 4a.75.75 0 0 1-.274 1.025l-2.165 1.25a.75.75 0 1 1-.75-1.3l2.165-1.25A.75.75 0 0 1 14.928 4",
    clipRule: "evenodd"
  }
), e2.createElement(
  "path",
  {
    fill: "currentColor",
    fillRule: "evenodd",
    d: "M4.536 10a.75.75 0 0 1-.275 1.024l-2.165 1.25a.75.75 0 0 1-.75-1.298l2.165-1.25A.75.75 0 0 1 4.536 10",
    clipRule: "evenodd",
    opacity: 0.5
  }
), e2.createElement(
  "path",
  {
    fill: "currentColor",
    fillRule: "evenodd",
    d: "M16 8a.75.75 0 0 1-.75.75h-2.5a.75.75 0 0 1 0-1.5h2.5A.75.75 0 0 1 16 8",
    clipRule: "evenodd"
  }
), e2.createElement(
  "path",
  {
    fill: "currentColor",
    fillRule: "evenodd",
    d: "M4 8a.75.75 0 0 1-.75.75H.75a.75.75 0 0 1 0-1.5h2.5A.75.75 0 0 1 4 8",
    clipRule: "evenodd",
    opacity: 0.4
  }
), e2.createElement(
  "path",
  {
    fill: "currentColor",
    fillRule: "evenodd",
    d: "M14.928 12a.75.75 0 0 1-1.024.274l-2.165-1.25a.75.75 0 0 1 .75-1.299l2.165 1.25A.75.75 0 0 1 14.928 12",
    clipRule: "evenodd",
    opacity: 0.9
  }
), e2.createElement(
  "path",
  {
    fill: "currentColor",
    fillRule: "evenodd",
    d: "M4.536 6a.75.75 0 0 1-1.025.275l-2.165-1.25a.75.75 0 1 1 .75-1.3l2.165 1.25A.75.75 0 0 1 4.536 6",
    clipRule: "evenodd",
    opacity: 0.3
  }
), e2.createElement(
  "path",
  {
    fill: "currentColor",
    fillRule: "evenodd",
    d: "M12 14.928a.75.75 0 0 1-1.024-.274l-1.25-2.165a.75.75 0 0 1 1.298-.75l1.25 2.165A.75.75 0 0 1 12 14.928",
    clipRule: "evenodd",
    opacity: 0.8
  }
), e2.createElement(
  "path",
  {
    fill: "currentColor",
    fillRule: "evenodd",
    d: "M6 4.536a.75.75 0 0 1-1.024-.275l-1.25-2.165a.75.75 0 1 1 1.299-.75l1.25 2.165A.75.75 0 0 1 6 4.536",
    clipRule: "evenodd",
    opacity: 0.2
  }
));
var Pt = (t) => e2.createElement("svg", { xmlns: "http://www.w3.org/2000/svg", width: 16, height: 16, fill: "none", ...t }, e2.createElement("g", { stroke: "currentColor", strokeLinecap: "round", strokeLinejoin: "round", strokeWidth: 1.5 }, e2.createElement("path", { d: "M13.25 7H9m0 0V2.75M9 7l5.25-5.25M2.75 9H7m0 0v4.25M7 9l-5.25 5.25" })));
var Tt2 = e2.forwardRef(
  function({ trackRef: n, ...a }, c) {
    const r2 = zn(), { mergedProps: i2, inFocus: s } = qn2({
      trackRef: n ?? r2,
      props: a
    });
    return e2.createElement(Vn.Consumer, null, (l) => l !== void 0 && e2.createElement("button", { ref: c, ...i2 }, a.children ? a.children : s ? e2.createElement(Pt, null) : e2.createElement(kt2, null)));
  }
);
var hn3 = e2.forwardRef(
  function({
    kind: n,
    initialSelection: a,
    onActiveDeviceChange: c,
    onDeviceListChange: r2,
    onDeviceSelectError: i2,
    exactMatch: s,
    track: l,
    requestPermissions: o2,
    onError: u,
    ...d
  }, f) {
    const m = Qo(), E = e2.useRef("default"), w = e2.useCallback(
      (p) => {
        m && m.emit(RoomEvent.MediaDevicesError, p), u == null ? void 0 : u(p);
      },
      [m, u]
    ), { devices: g, activeDeviceId: v, setActiveMediaDevice: y, className: M2 } = Gn({
      kind: n,
      room: m,
      track: l,
      requestPermissions: o2,
      onError: w
    });
    e2.useEffect(() => {
      a !== void 0 && y(a);
    }, [y]), e2.useEffect(() => {
      typeof r2 == "function" && r2(g);
    }, [r2, g]), e2.useEffect(() => {
      v !== E.current && (c == null ? void 0 : c(v)), E.current = v;
    }, [v]);
    const P = async (p) => {
      try {
        await y(p, { exact: s ?? true });
      } catch (R) {
        if (R instanceof Error)
          i2 == null ? void 0 : i2(R);
        else
          throw R;
      }
    }, S = e2.useMemo(
      () => G(d, { className: M2 }, { className: "lk-list" }),
      [M2, d]
    ), h2 = !!g.find((p) => p.label.toLowerCase().startsWith("default"));
    function b(p, R, A3) {
      return p === R || !h2 && A3 === 0 && R === "default";
    }
    return e2.createElement("ul", { ref: f, ...S }, g.map((p, R) => e2.createElement(
      "li",
      {
        key: p.deviceId,
        id: p.deviceId,
        "data-lk-active": b(p.deviceId, v, R),
        "aria-selected": b(p.deviceId, v, R),
        role: "option"
      },
      e2.createElement("button", { className: "lk-button", onClick: () => P(p.deviceId) }, p.label)
    )));
  }
);
var gn3 = e2.forwardRef(
  function({ label: n = "Allow Audio", ...a }, c) {
    const r2 = la(a.room), { mergedProps: i2 } = nr2({ room: r2, props: a });
    return e2.createElement("button", { ref: c, ...i2 }, n);
  }
);
var vn3 = e2.forwardRef(
  function({ label: n, ...a }, c) {
    const r2 = ua(), { mergedProps: i2, canPlayAudio: s } = nr2({ room: r2, props: a }), { mergedProps: l, canPlayVideo: o2 } = rr2({ room: r2, props: i2 }), { style: u, ...d } = l;
    return u.display = s && o2 ? "none" : "block", e2.createElement("button", { ref: c, style: u, ...d }, n ?? `Start ${s ? "Video" : "Audio"}`);
  }
);
function Re3(t, n) {
  switch (t) {
    case Track.Source.Microphone:
      return n ? e2.createElement(yt2, null) : e2.createElement(Rt2, null);
    case Track.Source.Camera:
      return n ? e2.createElement(wt2, null) : e2.createElement(Et2, null);
    case Track.Source.ScreenShare:
      return n ? e2.createElement(xt2, null) : e2.createElement(be2, null);
    default:
      return;
  }
}
function At2(t) {
  switch (t) {
    case ConnectionQuality.Excellent:
      return e2.createElement(Mt2, null);
    case ConnectionQuality.Good:
      return e2.createElement(St2, null);
    case ConnectionQuality.Poor:
      return e2.createElement(Ct2, null);
    default:
      return e2.createElement(It2, null);
  }
}
var pn3 = e2.forwardRef(function({ showIcon: n, ...a }, c) {
  const { buttonProps: r2, enabled: i2 } = cr2(a), [s, l] = e2.useState(false);
  return e2.useEffect(() => {
    l(true);
  }, []), s && e2.createElement("button", { ref: c, ...r2 }, (n ?? true) && Re3(a.source, i2), a.children);
});
var ye2 = e2.forwardRef(function(n, a) {
  const { className: c, quality: r2 } = Vn2(n), i2 = e2.useMemo(() => ({ ...G(n, { className: c }), "data-lk-quality": r2 }), [r2, n, c]);
  return e2.createElement("div", { ref: a, ...i2 }, n.children ?? At2(r2));
});
var K2 = e2.forwardRef(
  function({ participant: n, ...a }, c) {
    const r2 = ca(n), { className: i2, infoObserver: s } = e2.useMemo(() => Fs(r2), [r2]), { identity: l, name: o2 } = A2(s, {
      name: r2.name,
      identity: r2.identity,
      metadata: r2.metadata
    }), u = e2.useMemo(() => G(a, { className: i2, "data-lk-participant-name": o2 }), [a, i2, o2]);
    return e2.createElement("span", { ref: c, ...u }, o2 !== "" ? o2 : l, a.children);
  }
);
var Me2 = e2.forwardRef(
  function({ trackRef: n, show: a = "always", ...c }, r2) {
    const { className: i2, isMuted: s } = ir2(n), l = a === "always" || a === "muted" && s || a === "unmuted" && !s, o2 = e2.useMemo(
      () => G(c, {
        className: i2
      }),
      [i2, c]
    );
    return l ? e2.createElement("div", { ref: r2, ...o2, "data-lk-muted": s }, c.children ?? Re3(n.source, !s)) : null;
  }
);
var Nt2 = (t) => e2.createElement(
  "svg",
  {
    width: 320,
    height: 320,
    viewBox: "0 0 320 320",
    preserveAspectRatio: "xMidYMid meet",
    fill: "none",
    xmlns: "http://www.w3.org/2000/svg",
    ...t
  },
  e2.createElement(
    "path",
    {
      d: "M160 180C204.182 180 240 144.183 240 100C240 55.8172 204.182 20 160 20C115.817 20 79.9997 55.8172 79.9997 100C79.9997 144.183 115.817 180 160 180Z",
      fill: "white",
      fillOpacity: 0.25
    }
  ),
  e2.createElement(
    "path",
    {
      d: "M97.6542 194.614C103.267 191.818 109.841 192.481 115.519 195.141C129.025 201.466 144.1 205 159.999 205C175.899 205 190.973 201.466 204.48 195.141C210.158 192.481 216.732 191.818 222.345 194.614C262.703 214.719 291.985 253.736 298.591 300.062C300.15 310.997 291.045 320 280 320H39.9997C28.954 320 19.8495 310.997 21.4087 300.062C28.014 253.736 57.2966 214.72 97.6542 194.614Z",
      fill: "white",
      fillOpacity: 0.25
    }
  )
);
function Se2(t, n = {}) {
  const [a, c] = e2.useState(Gt(t)), [r2, i2] = e2.useState(a == null ? void 0 : a.isMuted), [s, l] = e2.useState(a == null ? void 0 : a.isSubscribed), [o2, u] = e2.useState(a == null ? void 0 : a.track), [d, f] = e2.useState("landscape"), m = e2.useRef(), { className: E, trackObserver: w } = e2.useMemo(() => ys(t), [
    t.participant.sid ?? t.participant.identity,
    t.source,
    q(t) && t.publication.trackSid
  ]);
  return e2.useEffect(() => {
    const g = w.subscribe((v) => {
      I.debug("update track", v), c(v), i2(v == null ? void 0 : v.isMuted), l(v == null ? void 0 : v.isSubscribed), u(v == null ? void 0 : v.track);
    });
    return () => g == null ? void 0 : g.unsubscribe();
  }, [w]), e2.useEffect(() => {
    var _a2, _b2;
    return o2 && (m.current && o2.detach(m.current), ((_a2 = n.element) == null ? void 0 : _a2.current) && !(t.participant.isLocal && (o2 == null ? void 0 : o2.kind) === "audio") && o2.attach(n.element.current)), m.current = (_b2 = n.element) == null ? void 0 : _b2.current, () => {
      m.current && (o2 == null ? void 0 : o2.detach(m.current));
    };
  }, [o2, n.element]), e2.useEffect(() => {
    var _a2, _b2;
    if (typeof ((_a2 = a == null ? void 0 : a.dimensions) == null ? void 0 : _a2.width) == "number" && typeof ((_b2 = a == null ? void 0 : a.dimensions) == null ? void 0 : _b2.height) == "number") {
      const g = a.dimensions.width > a.dimensions.height ? "landscape" : "portrait";
      f(g);
    }
  }, [a]), {
    publication: a,
    isMuted: r2,
    isSubscribed: s,
    track: o2,
    elementProps: G(n.props, {
      className: E,
      "data-lk-local-participant": t.participant.isLocal,
      "data-lk-source": a == null ? void 0 : a.source,
      ...(a == null ? void 0 : a.kind) === "video" && { "data-lk-orientation": d }
    })
  };
}
var J3;
var ue2;
function Lt2() {
  if (ue2) return J3;
  ue2 = 1;
  var t = "Expected a function", n = NaN, a = "[object Symbol]", c = /^\s+|\s+$/g, r2 = /^[-+]0x[0-9a-f]+$/i, i2 = /^0b[01]+$/i, s = /^0o[0-7]+$/i, l = parseInt, o2 = typeof es == "object" && es && es.Object === Object && es, u = typeof self == "object" && self && self.Object === Object && self, d = o2 || u || Function("return this")(), f = Object.prototype, m = f.toString, E = Math.max, w = Math.min, g = function() {
    return d.Date.now();
  };
  function v(h2, b, p) {
    var R, A3, O, L4, C, N, V2 = 0, re2 = false, B2 = false, G2 = true;
    if (typeof h2 != "function")
      throw new TypeError(t);
    b = S(b) || 0, y(p) && (re2 = !!p.leading, B2 = "maxWait" in p, O = B2 ? E(S(p.maxWait) || 0, b) : O, G2 = "trailing" in p ? !!p.trailing : G2);
    function Q3(k) {
      var T2 = R, H3 = A3;
      return R = A3 = void 0, V2 = k, L4 = h2.apply(H3, T2), L4;
    }
    function xe2(k) {
      return V2 = k, C = setTimeout(W3, b), re2 ? Q3(k) : L4;
    }
    function Pe3(k) {
      var T2 = k - N, H3 = k - V2, ie2 = b - T2;
      return B2 ? w(ie2, O - H3) : ie2;
    }
    function ce3(k) {
      var T2 = k - N, H3 = k - V2;
      return N === void 0 || T2 >= b || T2 < 0 || B2 && H3 >= O;
    }
    function W3() {
      var k = g();
      if (ce3(k))
        return le3(k);
      C = setTimeout(W3, Pe3(k));
    }
    function le3(k) {
      return C = void 0, G2 && R ? Q3(k) : (R = A3 = void 0, L4);
    }
    function Te2() {
      C !== void 0 && clearTimeout(C), V2 = 0, R = N = A3 = C = void 0;
    }
    function Ae3() {
      return C === void 0 ? L4 : le3(g());
    }
    function X() {
      var k = g(), T2 = ce3(k);
      if (R = arguments, A3 = this, N = k, T2) {
        if (C === void 0)
          return xe2(N);
        if (B2)
          return C = setTimeout(W3, b), Q3(N);
      }
      return C === void 0 && (C = setTimeout(W3, b)), L4;
    }
    return X.cancel = Te2, X.flush = Ae3, X;
  }
  function y(h2) {
    var b = typeof h2;
    return !!h2 && (b == "object" || b == "function");
  }
  function M2(h2) {
    return !!h2 && typeof h2 == "object";
  }
  function P(h2) {
    return typeof h2 == "symbol" || M2(h2) && m.call(h2) == a;
  }
  function S(h2) {
    if (typeof h2 == "number")
      return h2;
    if (P(h2))
      return n;
    if (y(h2)) {
      var b = typeof h2.valueOf == "function" ? h2.valueOf() : h2;
      h2 = y(b) ? b + "" : b;
    }
    if (typeof h2 != "string")
      return h2 === 0 ? h2 : +h2;
    h2 = h2.replace(c, "");
    var p = i2.test(h2);
    return p || s.test(h2) ? l(h2.slice(2), p ? 2 : 8) : r2.test(h2) ? n : +h2;
  }
  return J3 = v, J3;
}
var zt2 = Lt2();
var de2 = Br(zt2);
function Ft2(t) {
  const n = (0, import_react2.useRef)(t);
  n.current = t, (0, import_react2.useEffect)(
    () => () => {
      n.current();
    },
    []
  );
}
function Vt2(t, n = 500, a) {
  const c = (0, import_react2.useRef)();
  Ft2(() => {
    c.current && c.current.cancel();
  });
  const r2 = (0, import_react2.useMemo)(() => {
    const i2 = de2(t, n, a), s = (...l) => i2(...l);
    return s.cancel = () => {
      i2.cancel();
    }, s.isPending = () => !!c.current, s.flush = () => i2.flush(), s;
  }, [t, n, a]);
  return (0, import_react2.useEffect)(() => {
    c.current = de2(t, n, a);
  }, [t, n, a]), r2;
}
function Bt2(t, n, a) {
  const c = (u, d) => u === d, r2 = t instanceof Function ? t() : t, [i2, s] = (0, import_react2.useState)(r2), l = (0, import_react2.useRef)(r2), o2 = Vt2(
    s,
    n,
    a
  );
  return c(l.current, r2) || (o2(r2), l.current = r2), [i2, o2];
}
function Ht2({
  threshold: t = 0,
  root: n = null,
  rootMargin: a = "0%",
  freezeOnceVisible: c = false,
  initialIsIntersecting: r2 = false,
  onChange: i2
} = {}) {
  var s;
  const [l, o2] = (0, import_react2.useState)(null), [u, d] = (0, import_react2.useState)(() => ({
    isIntersecting: r2,
    entry: void 0
  })), f = (0, import_react2.useRef)();
  f.current = i2;
  const m = ((s = u.entry) == null ? void 0 : s.isIntersecting) && c;
  (0, import_react2.useEffect)(() => {
    if (!l || !("IntersectionObserver" in window) || m)
      return;
    const g = new IntersectionObserver(
      (v) => {
        const y = Array.isArray(g.thresholds) ? g.thresholds : [g.thresholds];
        v.forEach((M2) => {
          const P = M2.isIntersecting && y.some((S) => M2.intersectionRatio >= S);
          d({ isIntersecting: P, entry: M2 }), f.current && f.current(P, M2);
        });
      },
      { threshold: t, root: n, rootMargin: a }
    );
    return g.observe(l), () => {
      g.disconnect();
    };
  }, [
    l,
    // eslint-disable-next-line react-hooks/exhaustive-deps
    JSON.stringify(t),
    n,
    a,
    m,
    c
  ]);
  const E = (0, import_react2.useRef)(null);
  (0, import_react2.useEffect)(() => {
    var g;
    !l && ((g = u.entry) != null && g.target) && !c && !m && E.current !== u.entry.target && (E.current = u.entry.target, d({ isIntersecting: r2, entry: void 0 }));
  }, [l, u.entry, c, m, r2]);
  const w = [
    o2,
    !!u.isIntersecting,
    u.entry
  ];
  return w.ref = w[0], w.isIntersecting = w[1], w.entry = w[2], w;
}
var jt2 = e2.forwardRef(
  function({
    onTrackClick: n,
    onClick: a,
    onSubscriptionStatusChanged: c,
    trackRef: r2,
    manageSubscription: i2,
    ...s
  }, l) {
    const o2 = sa(r2), u = e2.useRef(null);
    e2.useImperativeHandle(l, () => u.current);
    const { ref: d, entry: f } = Ht2({
      root: null
    }), [m] = Bt2(f, 3e3), E = e2.useRef(null), w = e2.useRef(false);
    e2.useEffect(() => {
      w.current = !!i2, E.current = o2.publication instanceof RemoteTrackPublication ? o2.publication : null;
    }, [o2.publication, i2]), e2.useEffect(() => {
      i2 && o2.publication instanceof RemoteTrackPublication && (m == null ? void 0 : m.isIntersecting) === false && (f == null ? void 0 : f.isIntersecting) === false && o2.publication.setSubscribed(false);
    }, [m, f, o2, i2]), e2.useEffect(() => {
      i2 && o2.publication instanceof RemoteTrackPublication && (f == null ? void 0 : f.isIntersecting) === true && o2.publication.setSubscribed(true);
    }, [f, o2, i2]), e2.useEffect(() => () => {
      var _a2;
      w.current && ((_a2 = E.current) == null ? void 0 : _a2.setSubscribed(false));
    }, []);
    const {
      elementProps: g,
      publication: v,
      isSubscribed: y
    } = Se2(o2, {
      element: u,
      props: s
    });
    e2.useEffect(() => {
      c == null ? void 0 : c(!!y);
    }, [y, c]);
    const M2 = (S) => {
      a == null ? void 0 : a(S), n == null ? void 0 : n({ participant: o2 == null ? void 0 : o2.participant, track: v });
    }, P = e2.useCallback(
      (S) => {
        u.current = S, d(S);
      },
      [d]
    );
    return e2.createElement("video", { ref: P, ...g, muted: true, onClick: M2 });
  }
);
var ae2 = e2.forwardRef(
  function({ trackRef: n, onSubscriptionStatusChanged: a, volume: c, ...r2 }, i2) {
    const s = sa(n), l = e2.useRef(null);
    e2.useImperativeHandle(i2, () => l.current);
    const {
      elementProps: o2,
      isSubscribed: u,
      track: d,
      publication: f
    } = Se2(s, {
      element: l,
      props: r2
    });
    return e2.useEffect(() => {
      a == null ? void 0 : a(!!u);
    }, [u, a]), e2.useEffect(() => {
      d === void 0 || c === void 0 || (d instanceof RemoteAudioTrack ? d.setVolume(c) : I.warn("Volume can only be set on remote audio tracks."));
    }, [c, d]), e2.useEffect(() => {
      f === void 0 || r2.muted === void 0 || (f instanceof RemoteTrackPublication ? f.setEnabled(!r2.muted) : I.warn("Can only call setEnabled on remote track publications."));
    }, [r2.muted, f, d]), e2.createElement("audio", { ref: l, ...o2 });
  }
);
function Dt2(t) {
  const n = !!Go();
  return t.participant && !n ? e2.createElement(Yn.Provider, { value: t.participant }, t.children) : e2.createElement(e2.Fragment, null, t.children);
}
function _t2(t) {
  const n = !!zn();
  return t.trackRef && !n ? e2.createElement(Hn.Provider, { value: t.trackRef }, t.children) : e2.createElement(e2.Fragment, null, t.children);
}
var Ot = e2.forwardRef(
  function({
    trackRef: n,
    children: a,
    onParticipantClick: c,
    disableSpeakingIndicator: r2,
    ...i2
  }, s) {
    var _a2, _b2;
    const l = sa(n), { elementProps: o2 } = Yn2({
      htmlProps: i2,
      disableSpeakingIndicator: r2,
      onParticipantClick: c,
      trackRef: l
    }), u = fr2(l.participant), d = qo(), f = (_a2 = pa()) == null ? void 0 : _a2.autoSubscription, m = e2.useCallback(
      (E) => {
        l.source && !E && d && d.pin.dispatch && ns(l, d.pin.state) && d.pin.dispatch({ msg: "clear_pin" });
      },
      [l, d]
    );
    return e2.createElement("div", { ref: s, style: { position: "relative" }, ...o2 }, e2.createElement(_t2, { trackRef: l }, e2.createElement(Dt2, { participant: l.participant }, a ?? e2.createElement(e2.Fragment, null, q(l) && (((_b2 = l.publication) == null ? void 0 : _b2.kind) === "video" || l.source === Track.Source.Camera || l.source === Track.Source.ScreenShare) ? e2.createElement(
      jt2,
      {
        trackRef: l,
        onSubscriptionStatusChanged: m,
        manageSubscription: f
      }
    ) : q(l) && e2.createElement(
      ae2,
      {
        trackRef: l,
        onSubscriptionStatusChanged: m
      }
    ), e2.createElement("div", { className: "lk-participant-placeholder" }, e2.createElement(Nt2, null)), e2.createElement("div", { className: "lk-participant-metadata" }, e2.createElement("div", { className: "lk-participant-metadata-item" }, l.source === Track.Source.Camera ? e2.createElement(e2.Fragment, null, u && e2.createElement(bt2, { style: { marginRight: "0.25rem" } }), e2.createElement(
      Me2,
      {
        trackRef: {
          participant: l.participant,
          source: Track.Source.Microphone
        },
        show: "muted"
      }
    ), e2.createElement(K2, null)) : e2.createElement(e2.Fragment, null, e2.createElement(be2, { style: { marginRight: "0.25rem" } }), e2.createElement(K2, null, "'s screen"))), e2.createElement(ye2, { className: "lk-participant-metadata-item" }))), e2.createElement(Tt2, { trackRef: l }))));
  }
);
function En3(t) {
  const n = G(t, { className: "lk-focus-layout" });
  return e2.createElement("div", { ...n }, t.children);
}
function wn3({ trackRef: t, ...n }) {
  return e2.createElement(Ot, { trackRef: t, ...n });
}
function Ce3({ tracks: t, ...n }) {
  return e2.createElement(e2.Fragment, null, t.map((a) => e2.createElement(
    Hn.Provider,
    {
      value: a,
      key: V(a)
    },
    H2(n.children)
  )));
}
function Wt2({
  totalPageCount: t,
  nextPage: n,
  prevPage: a,
  currentPage: c,
  pagesContainer: r2
}) {
  const [i2, s] = e2.useState(false);
  return e2.useEffect(() => {
    let l;
    return r2 && (l = Xs(r2.current, 2e3).subscribe(
      s
    )), () => {
      l && l.unsubscribe();
    };
  }, [r2]), e2.createElement("div", { className: "lk-pagination-control", "data-lk-user-interaction": i2 }, e2.createElement("button", { className: "lk-button", onClick: a }, e2.createElement(oe3, null)), e2.createElement("span", { className: "lk-pagination-count" }, `${c} of ${t}`), e2.createElement("button", { className: "lk-button", onClick: n }, e2.createElement(oe3, null)));
}
var qt2 = e2.forwardRef(
  function({ totalPageCount: n, currentPage: a }, c) {
    const r2 = new Array(n).fill("").map((i2, s) => s + 1 === a ? e2.createElement("span", { "data-lk-active": true, key: s }) : e2.createElement("span", { key: s }));
    return e2.createElement("div", { ref: c, className: "lk-pagination-indicator" }, r2);
  }
);
function kn3({ tracks: t, ...n }) {
  const a = e2.createRef(), c = e2.useMemo(
    () => G(n, { className: "lk-grid-layout" }),
    [n]
  ), { layout: r2 } = Wn2(a, t.length), i2 = Kn2(r2.maxTiles, t);
  return sr2(a, {
    onLeftSwipe: i2.nextPage,
    onRightSwipe: i2.prevPage
  }), e2.createElement("div", { ref: a, "data-lk-pagination": i2.totalPageCount > 1, ...c }, e2.createElement(Ce3, { tracks: i2.tracks }, n.children), t.length > r2.maxTiles && e2.createElement(e2.Fragment, null, e2.createElement(
    qt2,
    {
      totalPageCount: i2.totalPageCount,
      currentPage: i2.currentPage
    }
  ), e2.createElement(Wt2, { pagesContainer: a, ...i2 })));
}
var $t2 = 130;
var Ut2 = 140;
var me3 = 1;
var Ie3 = 16 / 10;
var Gt2 = (1 - Ie3) * -1;
function bn3({ tracks: t, orientation: n, ...a }) {
  const c = e2.useRef(null), [r2, i2] = e2.useState(0), { width: s, height: l } = Xt2(c), o2 = n || (l >= s ? "vertical" : "horizontal"), u = o2 === "vertical" ? Math.max(s * Gt2, $t2) : Math.max(l * Ie3, Ut2), d = rs(), f = Math.max(o2 === "vertical" ? (l - d) / u : (s - d) / u, me3);
  let m = Math.round(f);
  Math.abs(f - r2) < 0.5 ? m = Math.round(r2) : r2 !== f && i2(f);
  const E = tn2(t, m);
  return e2.useLayoutEffect(() => {
    c.current && (c.current.dataset.lkOrientation = o2, c.current.style.setProperty("--lk-max-visible-tiles", m.toString()));
  }, [m, o2]), e2.createElement("aside", { key: o2, className: "lk-carousel", ref: c, ...a }, e2.createElement(Ce3, { tracks: E }, a.children));
}
function Rn3({
  value: t,
  onPinChange: n,
  onWidgetChange: a,
  children: c
}) {
  const r2 = ia(t);
  return e2.useEffect(() => {
    I.debug("PinState Updated", { state: r2.pin.state }), n && r2.pin.state && n(r2.pin.state);
  }, [r2.pin.state, n]), e2.useEffect(() => {
    I.debug("Widget Updated", { widgetState: r2.widget.state }), a && r2.widget.state && a(r2.widget.state);
  }, [a, r2.widget.state]), e2.createElement(Vn.Provider, { value: r2 }, c);
}
var yn3 = e2.forwardRef(
  function({ trackRef: n, ...a }, c) {
    const d = sa(n), f = mr2(d, { bands: 7, loPass: 300 });
    return e2.createElement(
      "svg",
      {
        ref: c,
        width: "100%",
        height: "100%",
        viewBox: "0 0 200 90",
        ...a,
        className: "lk-audio-visualizer"
      },
      e2.createElement("rect", { x: "0", y: "0", width: "100%", height: "100%" }),
      e2.createElement(
        "g",
        {
          style: {
            transform: `translate(${130 / 2}px, 0)`
          }
        },
        f.map((m, E) => e2.createElement(
          "rect",
          {
            key: E,
            x: E * 10,
            y: 90 / 2 - m * 50 / 2,
            width: 6,
            height: m * 50
          }
        ))
      )
    );
  }
);
function Mn3({ participants: t, ...n }) {
  return e2.createElement(e2.Fragment, null, t.map((a) => e2.createElement(Yn.Provider, { value: a, key: a.identity }, H2(n.children))));
}
function Sn3({ room: t, volume: n, muted: a }) {
  const c = ur2(
    [Track.Source.Microphone, Track.Source.ScreenShareAudio, Track.Source.Unknown],
    {
      updateOnlyOn: [],
      onlySubscribed: true,
      room: t
    }
  ).filter((r2) => !r2.participant.isLocal && r2.publication.kind === Track.Kind.Audio);
  return e2.createElement("div", { style: { display: "none" } }, c.map((r2) => e2.createElement(
    ae2,
    {
      key: V(r2),
      trackRef: r2,
      volume: n,
      muted: a
    }
  )));
}
var Cn3 = e2.forwardRef(function({ childrenPosition: n = "before", children: a, ...c }, r2) {
  const { name: i2 } = er2();
  return e2.createElement("span", { ref: r2, ...c }, n === "before" && a, i2, n === "after" && a);
});
function Qt3(t) {
  const n = e2.useMemo(() => G(t, { className: "lk-toast" }), [t]);
  return e2.createElement("div", { ...n }, t.children);
}
var Xt3 = (t) => {
  const n = [];
  for (let a = 0; a < t; a++)
    n.push([a, t - 1 - a]);
  return n;
};
var fe2 = (t) => [[Math.floor(t / 2)], [-1]];
var Yt3 = (t, n, a) => {
  const [c, r2] = (0, import_react2.useState)(0), [i2, s] = (0, import_react2.useState)([[]]);
  (0, import_react2.useEffect)(() => {
    if (t === "thinking")
      s(fe2(n));
    else if (t === "connecting" || t === "initializing") {
      const o2 = [...Xt3(n)];
      s(o2);
    } else s(t === "listening" ? fe2(n) : t === void 0 || t === "speaking" ? [new Array(n).fill(0).map((o2, u) => u)] : [[]]);
    r2(0);
  }, [t, n]);
  const l = (0, import_react2.useRef)(null);
  return (0, import_react2.useEffect)(() => {
    let o2 = performance.now();
    const u = (d) => {
      d - o2 >= a && (r2((m) => m + 1), o2 = d), l.current = requestAnimationFrame(u);
    };
    return l.current = requestAnimationFrame(u), () => {
      l.current !== null && cancelAnimationFrame(l.current);
    };
  }, [a, n, t, i2.length]), i2[c % i2.length];
};
var Zt2 = /* @__PURE__ */ new Map([
  ["connecting", 2e3],
  ["initializing", 2e3],
  ["listening", 500],
  ["thinking", 150]
]);
var Jt3 = (t, n) => {
  if (t === void 0)
    return 1e3;
  let a = Zt2.get(t);
  return a && t === "connecting" && (a /= n), a;
};
var Kt3 = e2.forwardRef(
  function({ state: n, options: a, barCount: c = 15, trackRef: r2, track: i2, children: s, ...l }, o2) {
    const u = G(l, { className: "lk-audio-bar-visualizer" });
    let d = zn();
    (r2 || i2) && (d = r2 || i2);
    const f = mr2(d, {
      bands: c,
      loPass: 100,
      hiPass: 200
    }), m = (a == null ? void 0 : a.minHeight) ?? 20, E = (a == null ? void 0 : a.maxHeight) ?? 100, w = Yt3(
      n,
      c,
      Jt3(n, c) ?? 100
    );
    return e2.createElement("div", { ref: o2, ...u, "data-lk-va-state": n }, f.map(
      (g, v) => s ? H2(s, {
        "data-lk-highlighted": w.includes(v),
        "data-lk-bar-index": v,
        className: "lk-audio-bar",
        style: { height: `${Math.min(E, Math.max(m, g * 100 + 5))}%` }
      }) : e2.createElement(
        "span",
        {
          key: v,
          "data-lk-highlighted": w.includes(v),
          "data-lk-bar-index": v,
          className: `lk-audio-bar ${w.includes(v) && "lk-highlighted"}`,
          style: {
            // TODO transform animations would be more performant, however the border-radius gets distorted when using scale transforms. a 9-slice approach (or 3 in this case) could work
            // transform: `scale(1, ${Math.min(maxHeight, Math.max(minHeight, volume))}`,
            height: `${Math.min(E, Math.max(m, g * 100 + 5))}%`
          }
        }
      )
    ));
  }
);
var In2 = e2.forwardRef(
  function({
    children: n,
    disableSpeakingIndicator: a,
    onParticipantClick: c,
    trackRef: r2,
    ...i2
  }, s) {
    const l = sa(r2), { elementProps: o2 } = Yn2({
      trackRef: l,
      htmlProps: i2,
      disableSpeakingIndicator: a,
      onParticipantClick: c
    });
    return e2.createElement("div", { ref: s, style: { position: "relative", minHeight: "160px" }, ...o2 }, e2.createElement(Hn.Provider, { value: l }, n ?? e2.createElement(e2.Fragment, null, q(l) && e2.createElement(ae2, { trackRef: l }), e2.createElement(Kt3, { barCount: 7, options: { minHeight: 8 } }), e2.createElement("div", { className: "lk-participant-metadata" }, e2.createElement("div", { className: "lk-participant-metadata-item" }, e2.createElement(Me2, { trackRef: l }), e2.createElement(K2, null)), e2.createElement(ye2, { className: "lk-participant-metadata-item" })))));
  }
);
function xn2(t) {
  const [n, a] = e2.useState(void 0), c = oe2(t.room);
  return e2.useEffect(() => {
    switch (c) {
      case ConnectionState.Reconnecting:
        a(
          e2.createElement(e2.Fragment, null, e2.createElement(se2, { className: "lk-spinner" }), " Reconnecting")
        );
        break;
      case ConnectionState.Connecting:
        a(
          e2.createElement(e2.Fragment, null, e2.createElement(se2, { className: "lk-spinner" }), " Connecting")
        );
        break;
      case ConnectionState.Disconnected:
        a(e2.createElement(e2.Fragment, null, "Disconnected"));
        break;
      default:
        a(void 0);
        break;
    }
  }, [c]), n ? e2.createElement(Qt3, { className: "lk-toast-connection-state" }, n) : e2.createElement(e2.Fragment, null);
}
var Pn3 = e2.forwardRef(
  function({ entry: n, hideName: a = false, hideTimestamp: c = false, messageFormatter: r2, ...i2 }, s) {
    var _a2, _b2, _c, _d;
    const l = e2.useMemo(() => r2 ? r2(n.message) : n.message, [n.message, r2]), o2 = !!n.editTimestamp, u = new Date(n.timestamp), d = typeof navigator < "u" ? navigator.language : "en-US", f = ((_a2 = n.from) == null ? void 0 : _a2.name) ?? ((_b2 = n.from) == null ? void 0 : _b2.identity);
    return e2.createElement(
      "li",
      {
        ref: s,
        className: "lk-chat-entry",
        title: u.toLocaleTimeString(d, { timeStyle: "full" }),
        "data-lk-message-origin": ((_c = n.from) == null ? void 0 : _c.isLocal) ? "local" : "remote",
        ...i2
      },
      (!c || !a || o2) && e2.createElement("span", { className: "lk-meta-data" }, !a && e2.createElement("strong", { className: "lk-participant-name" }, f), (!c || o2) && e2.createElement("span", { className: "lk-timestamp" }, o2 && "edited ", u.toLocaleTimeString(d, { timeStyle: "short" }))),
      e2.createElement("span", { className: "lk-message-body" }, l),
      e2.createElement("span", { className: "lk-message-attachements" }, (_d = n.attachedFiles) == null ? void 0 : _d.map(
        (m) => m.type.startsWith("image/") && e2.createElement(
          "img",
          {
            style: { maxWidth: "300px", maxHeight: "300px" },
            key: m.name,
            src: URL.createObjectURL(m),
            alt: m.name
          }
        )
      ))
    );
  }
);
function Tn3(t) {
  return cs(t, as()).map((n, a) => {
    if (typeof n == "string")
      return n;
    {
      const c = n.content.toString(), r2 = n.type === "url" ? /^http(s?):\/\//.test(c) ? c : `https://${c}` : `mailto:${c}`;
      return e2.createElement("a", { className: "lk-chat-link", key: a, href: r2, target: "_blank", rel: "noreferrer" }, c);
    }
  });
}
function An3(t) {
  return e2.createElement(qn.Provider, { value: t.session }, e2.createElement(Kn.Provider, { value: t.session.room }, t.children));
}

// node_modules/@livekit/components-react/dist/prefabs.mjs
function Z2({
  messageFormatter: n,
  messageDecoder: o2,
  messageEncoder: i2,
  channelTopic: u,
  ...g
}) {
  const h2 = e3.useRef(null), l = e3.useRef(null), d = e3.useMemo(() => ({ messageDecoder: o2, messageEncoder: i2, channelTopic: u }), [o2, i2, u]), { chatMessages: c, send: C, isSending: t } = un2(d), f = qo(), s = e3.useRef(0);
  async function v(a) {
    a.preventDefault(), l.current && l.current.value.trim() !== "" && (await C(l.current.value), l.current.value = "", l.current.focus());
  }
  return e3.useEffect(() => {
    var _a2;
    h2 && ((_a2 = h2.current) == null ? void 0 : _a2.scrollTo({ top: h2.current.scrollHeight }));
  }, [h2, c]), e3.useEffect(() => {
    var _a2, _b2, _c, _d, _e2;
    if (!f || c.length === 0)
      return;
    if (((_a2 = f.widget.state) == null ? void 0 : _a2.showChat) && c.length > 0 && s.current !== ((_b2 = c[c.length - 1]) == null ? void 0 : _b2.timestamp)) {
      s.current = (_c = c[c.length - 1]) == null ? void 0 : _c.timestamp;
      return;
    }
    const a = c.filter(
      (p) => !s.current || p.timestamp > s.current
    ).length, { widget: m } = f;
    a > 0 && ((_d = m.state) == null ? void 0 : _d.unreadMessages) !== a && ((_e2 = m.dispatch) == null ? void 0 : _e2.call(m, { msg: "unread_msg", count: a }));
  }, [c, f == null ? void 0 : f.widget]), e3.createElement("div", { ...g, className: "lk-chat" }, e3.createElement("div", { className: "lk-chat-header" }, "Messages", f && e3.createElement(on3, { className: "lk-close-button" }, e3.createElement(un3, null))), e3.createElement("ul", { className: "lk-list lk-chat-messages", ref: h2 }, g.children ? c.map(
    (a, m) => H2(g.children, {
      entry: a,
      key: a.id ?? m,
      messageFormatter: n
    })
  ) : c.map((a, m, p) => {
    const k = m >= 1 && p[m - 1].from === a.from, r2 = m >= 1 && a.timestamp - p[m - 1].timestamp < 6e4;
    return e3.createElement(
      Pn3,
      {
        key: a.id ?? m,
        hideName: k,
        hideTimestamp: k === false ? false : r2,
        entry: a,
        messageFormatter: n
      }
    );
  })), e3.createElement("form", { className: "lk-chat-form", onSubmit: v }, e3.createElement(
    "input",
    {
      className: "lk-form-control lk-chat-form-input",
      disabled: t,
      ref: l,
      type: "text",
      placeholder: "Enter a message...",
      onInput: (a) => a.stopPropagation(),
      onKeyDown: (a) => a.stopPropagation(),
      onKeyUp: (a) => a.stopPropagation()
    }
  ), e3.createElement("button", { type: "submit", className: "lk-button lk-chat-form-button", disabled: t }, "Send")));
}
function L3({
  kind: n,
  initialSelection: o2,
  onActiveDeviceChange: i2,
  tracks: u,
  requestPermissions: g = false,
  ...h2
}) {
  const [l, d] = e3.useState(false), [c, C] = e3.useState([]), [t, f] = e3.useState(true), [s, v] = e3.useState(g), a = (r2, D2) => {
    I.debug("handle device change"), d(false), i2 == null ? void 0 : i2(r2, D2);
  }, m = e3.useRef(null), p = e3.useRef(null);
  e3.useLayoutEffect(() => {
    l && v(true);
  }, [l]), e3.useLayoutEffect(() => {
    let r2;
    return m.current && p.current && (c || t) && (r2 = os(m.current, p.current, (D2, w) => {
      p.current && Object.assign(p.current.style, { left: `${D2}px`, top: `${w}px` });
    })), f(false), () => {
      r2 == null ? void 0 : r2();
    };
  }, [m, p, c, t]);
  const k = e3.useCallback(
    (r2) => {
      p.current && r2.target !== m.current && l && ss(p.current, r2) && d(false);
    },
    [l, p, m]
  );
  return e3.useEffect(() => (document.addEventListener("click", k), () => {
    document.removeEventListener("click", k);
  }), [k]), e3.createElement(e3.Fragment, null, e3.createElement(
    "button",
    {
      className: "lk-button lk-button-menu",
      "aria-pressed": l,
      ...h2,
      onClick: () => d(!l),
      ref: m
    },
    h2.children
  ), !h2.disabled && e3.createElement(
    "div",
    {
      className: "lk-device-menu",
      ref: p,
      style: { visibility: l ? "visible" : "hidden" }
    },
    n ? e3.createElement(
      hn3,
      {
        initialSelection: o2,
        onActiveDeviceChange: (r2) => a(n, r2),
        onDeviceListChange: C,
        kind: n,
        track: u == null ? void 0 : u[n],
        requestPermissions: s
      }
    ) : e3.createElement(e3.Fragment, null, e3.createElement("div", { className: "lk-device-menu-heading" }, "Audio inputs"), e3.createElement(
      hn3,
      {
        kind: "audioinput",
        onActiveDeviceChange: (r2) => a("audioinput", r2),
        onDeviceListChange: C,
        track: u == null ? void 0 : u.audioinput,
        requestPermissions: s
      }
    ), e3.createElement("div", { className: "lk-device-menu-heading" }, "Video inputs"), e3.createElement(
      hn3,
      {
        kind: "videoinput",
        onActiveDeviceChange: (r2) => a("videoinput", r2),
        onDeviceListChange: C,
        track: u == null ? void 0 : u.videoinput,
        requestPermissions: s
      }
    ))
  ));
}
function W2() {
  e3.useEffect(() => {
    Q2();
  }, []);
}
function Ke2(n, o2) {
  const [i2, u] = e3.useState(), g = e3.useMemo(() => new _(), []);
  return e3.useEffect(() => {
    let h2 = false, l = [];
    return g.lock().then(async (d) => {
      try {
        (n.audio || n.video) && (l = await createLocalTracks(n), h2 ? l.forEach((c) => c.stop()) : u(l));
      } catch (c) {
        o2 && c instanceof Error ? o2(c) : I.error(c);
      } finally {
        d();
      }
    }), () => {
      h2 = true, l.forEach((d) => {
        d.stop();
      });
    };
  }, [JSON.stringify(n, T), o2, g]), i2;
}
function et2(n, o2, i2) {
  const [u, g] = e3.useState(null), [h2, l] = e3.useState(false), d = Jn({ kind: i2 }), [c, C] = e3.useState(
    void 0
  ), [t, f] = e3.useState(), [s, v] = e3.useState(o2);
  e3.useEffect(() => {
    v(o2);
  }, [o2]);
  const a = async (k, r2) => {
    try {
      const D2 = r2 === "videoinput" ? await createLocalVideoTrack({
        deviceId: k,
        resolution: VideoPresets.h720.resolution
      }) : await createLocalAudioTrack({ deviceId: k }), w = await D2.getDeviceId(false);
      w && k !== w && (p.current = w, v(w)), f(D2);
    } catch (D2) {
      D2 instanceof Error && g(D2);
    }
  }, m = async (k, r2) => {
    await k.setDeviceId(r2), p.current = r2;
  }, p = e3.useRef(s);
  return e3.useEffect(() => {
    n && !t && !u && !h2 && (I.debug("creating track", i2), l(true), a(s, i2).finally(() => {
      l(false);
    }));
  }, [n, t, u, h2]), e3.useEffect(() => {
    t && (n ? (c == null ? void 0 : c.deviceId) && p.current !== (c == null ? void 0 : c.deviceId) ? (I.debug(`switching ${i2} device from`, p.current, c.deviceId), m(t, c.deviceId)) : (I.debug(`unmuting local ${i2} track`), t.unmute()) : (I.debug(`muting ${i2} track`), t.mute().then(() => I.debug(t.mediaStreamTrack))));
  }, [t, c, n, i2]), e3.useEffect(() => () => {
    t && (I.debug(`stopping local ${i2} track`), t.stop(), t.mute());
  }, []), e3.useEffect(() => {
    C(d == null ? void 0 : d.find((k) => k.deviceId === s));
  }, [s, d]), {
    selectedDevice: c,
    localTrack: t,
    deviceError: u
  };
}
function tt2({
  defaults: n = {},
  onValidate: o2,
  onSubmit: i2,
  onError: u,
  debug: g,
  joinLabel: h2 = "Join Room",
  micLabel: l = "Microphone",
  camLabel: d = "Camera",
  userLabel: c = "Username",
  persistUserChoices: C = true,
  videoProcessor: t,
  ...f
}) {
  const {
    userChoices: s,
    saveAudioInputDeviceId: v,
    saveAudioInputEnabled: a,
    saveVideoInputDeviceId: m,
    saveVideoInputEnabled: p,
    saveUsername: k
  } = dr2({
    defaults: n,
    preventSave: !C,
    preventLoad: !C
  }), [r2, D2] = e3.useState(s), [w, V2] = e3.useState(r2.audioEnabled), [M2, O] = e3.useState(r2.videoEnabled), [P, b] = e3.useState(r2.audioDeviceId), [y, te2] = e3.useState(r2.videoDeviceId), [A3, ae3] = e3.useState(r2.username);
  e3.useEffect(() => {
    a(w);
  }, [w, a]), e3.useEffect(() => {
    p(M2);
  }, [M2, p]), e3.useEffect(() => {
    v(P);
  }, [P, v]), e3.useEffect(() => {
    m(y);
  }, [y, m]), e3.useEffect(() => {
    k(A3);
  }, [A3, k]);
  const R = Ke2(
    {
      audio: w ? { deviceId: s.audioDeviceId } : false,
      video: M2 ? { deviceId: s.videoDeviceId, processor: t } : false
    },
    u
  ), x = e3.useRef(null), I3 = e3.useMemo(
    () => R == null ? void 0 : R.filter((S) => S.kind === Track.Kind.Video)[0],
    [R]
  ), ne2 = e3.useMemo(() => {
    if (I3) {
      const { facingMode: S } = facingModeFromLocalTrack(I3);
      return S;
    } else
      return "undefined";
  }, [I3]), j2 = e3.useMemo(
    () => R == null ? void 0 : R.filter((S) => S.kind === Track.Kind.Audio)[0],
    [R]
  );
  e3.useEffect(() => (x.current && I3 && (I3.unmute(), I3.attach(x.current)), () => {
    I3 == null ? void 0 : I3.detach();
  }), [I3]);
  const [ce3, se3] = e3.useState(), $2 = e3.useCallback(
    (S) => typeof o2 == "function" ? o2(S) : S.username !== "",
    [o2]
  );
  e3.useEffect(() => {
    const S = {
      username: A3,
      videoEnabled: M2,
      videoDeviceId: y,
      audioEnabled: w,
      audioDeviceId: P
    };
    D2(S), se3($2(S));
  }, [A3, M2, $2, w, P, y]);
  function re2(S) {
    S.preventDefault(), $2(r2) ? typeof i2 == "function" && i2(r2) : I.warn("Validation failed with: ", r2);
  }
  return W2(), e3.createElement("div", { className: "lk-prejoin", ...f }, e3.createElement("div", { className: "lk-video-container" }, I3 && e3.createElement("video", { ref: x, width: "1280", height: "720", "data-lk-facing-mode": ne2 }), (!I3 || !M2) && e3.createElement("div", { className: "lk-camera-off-note" }, e3.createElement(Nt2, null))), e3.createElement("div", { className: "lk-button-group-container" }, e3.createElement("div", { className: "lk-button-group audio" }, e3.createElement(
    pn3,
    {
      initialState: w,
      source: Track.Source.Microphone,
      onChange: (S) => V2(S)
    },
    l
  ), e3.createElement("div", { className: "lk-button-group-menu" }, e3.createElement(
    L3,
    {
      initialSelection: P,
      kind: "audioinput",
      disabled: !j2,
      tracks: { audioinput: j2 },
      onActiveDeviceChange: (S, U2) => b(U2)
    }
  ))), e3.createElement("div", { className: "lk-button-group video" }, e3.createElement(
    pn3,
    {
      initialState: M2,
      source: Track.Source.Camera,
      onChange: (S) => O(S)
    },
    d
  ), e3.createElement("div", { className: "lk-button-group-menu" }, e3.createElement(
    L3,
    {
      initialSelection: y,
      kind: "videoinput",
      disabled: !I3,
      tracks: { videoinput: I3 },
      onActiveDeviceChange: (S, U2) => te2(U2)
    }
  )))), e3.createElement("form", { className: "lk-username-container" }, e3.createElement(
    "input",
    {
      className: "lk-form-control",
      id: "username",
      name: "username",
      type: "text",
      defaultValue: A3,
      placeholder: c,
      onChange: (S) => ae3(S.target.value),
      autoComplete: "off"
    }
  ), e3.createElement(
    "button",
    {
      className: "lk-button lk-join-button",
      type: "submit",
      onClick: re2,
      disabled: !ce3
    },
    h2
  )), g && e3.createElement(e3.Fragment, null, e3.createElement("strong", null, "User Choices:"), e3.createElement("ul", { className: "lk-list", style: { overflow: "hidden", maxWidth: "15rem" } }, e3.createElement("li", null, "Username: ", `${r2.username}`), e3.createElement("li", null, "Video Enabled: ", `${r2.videoEnabled}`), e3.createElement("li", null, "Audio Enabled: ", `${r2.audioEnabled}`), e3.createElement("li", null, "Video Device: ", `${r2.videoDeviceId}`), e3.createElement("li", null, "Audio Device: ", `${r2.audioDeviceId}`))));
}
function Ge2({ props: n }) {
  const { dispatch: o2, state: i2 } = ta().widget, u = "lk-button lk-settings-toggle";
  return { mergedProps: e3.useMemo(() => L2(n, {
    className: u,
    onClick: () => {
      o2 && o2({ msg: "toggle_settings" });
    },
    "aria-pressed": (i2 == null ? void 0 : i2.showSettings) ? "true" : "false"
  }), [n, u, o2, i2]) };
}
var Je2 = e3.forwardRef(
  function(o2, i2) {
    const { mergedProps: u } = Ge2({ props: o2 });
    return e3.createElement("button", { ref: i2, ...u }, o2.children);
  }
);
var ze2 = (n) => {
  switch (n) {
    case Track.Source.Camera:
      return 1;
    case Track.Source.Microphone:
      return 2;
    case Track.Source.ScreenShare:
      return 3;
    default:
      return 0;
  }
};
function ee2({
  variation: n,
  controls: o2,
  saveUserChoices: i2 = true,
  onDeviceError: u,
  ...g
}) {
  var _a2;
  const [h2, l] = e3.useState(false), d = qo();
  e3.useEffect(() => {
    var _a3, _b2;
    ((_a3 = d == null ? void 0 : d.widget.state) == null ? void 0 : _a3.showChat) !== void 0 && l((_b2 = d == null ? void 0 : d.widget.state) == null ? void 0 : _b2.showChat);
  }, [(_a2 = d == null ? void 0 : d.widget.state) == null ? void 0 : _a2.showChat]);
  const C = Un2(`(max-width: ${h2 ? 1e3 : 760}px)`) ? "minimal" : "verbose";
  n ?? (n = C);
  const t = { leave: true, ...o2 }, f = $n2();
  if (!f)
    t.camera = false, t.chat = false, t.microphone = false, t.screenShare = false;
  else {
    const b = (y) => f.canPublish && (f.canPublishSources.length === 0 || f.canPublishSources.includes(ze2(y)));
    t.camera ?? (t.camera = b(Track.Source.Camera)), t.microphone ?? (t.microphone = b(Track.Source.Microphone)), t.screenShare ?? (t.screenShare = b(Track.Source.ScreenShare)), t.chat ?? (t.chat = f.canPublishData && (o2 == null ? void 0 : o2.chat));
  }
  const s = e3.useMemo(
    () => n === "minimal" || n === "verbose",
    [n]
  ), v = e3.useMemo(
    () => n === "textOnly" || n === "verbose",
    [n]
  ), a = ds(), [m, p] = e3.useState(false), k = e3.useCallback(
    (b) => {
      p(b);
    },
    [p]
  ), r2 = G({ className: "lk-control-bar" }, g), {
    saveAudioInputEnabled: D2,
    saveVideoInputEnabled: w,
    saveAudioInputDeviceId: V2,
    saveVideoInputDeviceId: M2
  } = dr2({ preventSave: !i2 }), O = e3.useCallback(
    (b, y) => y ? D2(b) : null,
    [D2]
  ), P = e3.useCallback(
    (b, y) => y ? w(b) : null,
    [w]
  );
  return e3.createElement("div", { ...r2 }, t.microphone && e3.createElement("div", { className: "lk-button-group" }, e3.createElement(
    pn3,
    {
      source: Track.Source.Microphone,
      showIcon: s,
      onChange: O,
      onDeviceError: (b) => u == null ? void 0 : u({ source: Track.Source.Microphone, error: b })
    },
    v && "Microphone"
  ), e3.createElement("div", { className: "lk-button-group-menu" }, e3.createElement(
    L3,
    {
      kind: "audioinput",
      onActiveDeviceChange: (b, y) => V2(y ?? "default")
    }
  ))), t.camera && e3.createElement("div", { className: "lk-button-group" }, e3.createElement(
    pn3,
    {
      source: Track.Source.Camera,
      showIcon: s,
      onChange: P,
      onDeviceError: (b) => u == null ? void 0 : u({ source: Track.Source.Camera, error: b })
    },
    v && "Camera"
  ), e3.createElement("div", { className: "lk-button-group-menu" }, e3.createElement(
    L3,
    {
      kind: "videoinput",
      onActiveDeviceChange: (b, y) => M2(y ?? "default")
    }
  ))), t.screenShare && a && e3.createElement(
    pn3,
    {
      source: Track.Source.ScreenShare,
      captureOptions: { audio: true, selfBrowserSurface: "include" },
      showIcon: s,
      onChange: k,
      onDeviceError: (b) => u == null ? void 0 : u({ source: Track.Source.ScreenShare, error: b })
    },
    v && (m ? "Stop screen share" : "Share screen")
  ), t.chat && e3.createElement(on3, null, s && e3.createElement(dn3, null), v && "Chat"), t.settings && e3.createElement(Je2, null, s && e3.createElement(mn3, null), v && "Settings"), t.leave && e3.createElement(sn3, null, s && e3.createElement(fn3, null), v && "Leave"), e3.createElement(vn3, null));
}
function at2({
  chatMessageFormatter: n,
  chatMessageDecoder: o2,
  chatMessageEncoder: i2,
  SettingsComponent: u,
  ...g
}) {
  var _a2, _b2;
  const [h2, l] = e3.useState({
    showChat: false,
    unreadMessages: 0,
    showSettings: false
  }), d = e3.useRef(null), c = ur2(
    [
      { source: Track.Source.Camera, withPlaceholder: true },
      { source: Track.Source.ScreenShare, withPlaceholder: false }
    ],
    { updateOnlyOn: [RoomEvent.ActiveSpeakersChanged], onlySubscribed: false }
  ), C = (a) => {
    I.debug("updating widget state", a), l(a);
  }, t = ra(), f = c.filter(q).filter((a) => a.publication.source === Track.Source.ScreenShare), s = (_a2 = Xn(t)) == null ? void 0 : _a2[0], v = c.filter((a) => !ts(a, s));
  return e3.useEffect(() => {
    var _a3, _b3, _c, _d, _e2, _f;
    if (f.some((a) => a.publication.isSubscribed) && d.current === null ? (I.debug("Auto set screen share focus:", { newScreenShareTrack: f[0] }), (_b3 = (_a3 = t.pin).dispatch) == null ? void 0 : _b3.call(_a3, { msg: "set_pin", trackReference: f[0] }), d.current = f[0]) : d.current && !f.some(
      (a) => {
        var _a4, _b4;
        return a.publication.trackSid === ((_b4 = (_a4 = d.current) == null ? void 0 : _a4.publication) == null ? void 0 : _b4.trackSid);
      }
    ) && (I.debug("Auto clearing screen share focus."), (_d = (_c = t.pin).dispatch) == null ? void 0 : _d.call(_c, { msg: "clear_pin" }), d.current = null), s && !q(s)) {
      const a = c.find(
        (m) => m.participant.identity === s.participant.identity && m.source === s.source
      );
      a !== s && q(a) && ((_f = (_e2 = t.pin).dispatch) == null ? void 0 : _f.call(_e2, { msg: "set_pin", trackReference: a }));
    }
  }, [
    f.map((a) => `${a.publication.trackSid}_${a.publication.isSubscribed}`).join(),
    (_b2 = s == null ? void 0 : s.publication) == null ? void 0 : _b2.trackSid,
    c
  ]), W2(), e3.createElement("div", { className: "lk-video-conference", ...g }, is() && e3.createElement(
    Rn3,
    {
      value: t,
      onWidgetChange: C
    },
    e3.createElement("div", { className: "lk-video-conference-inner" }, s ? e3.createElement("div", { className: "lk-focus-layout-wrapper" }, e3.createElement(En3, null, e3.createElement(bn3, { tracks: v }, e3.createElement(Ot, null)), s && e3.createElement(wn3, { trackRef: s }))) : e3.createElement("div", { className: "lk-grid-layout-wrapper" }, e3.createElement(kn3, { tracks: c }, e3.createElement(Ot, null))), e3.createElement(ee2, { controls: { chat: true, settings: !!u } })),
    e3.createElement(
      Z2,
      {
        style: { display: h2.showChat ? "grid" : "none" },
        messageFormatter: n,
        messageEncoder: i2,
        messageDecoder: o2
      }
    ),
    u && e3.createElement(
      "div",
      {
        className: "lk-settings-menu-modal",
        style: { display: h2.showSettings ? "block" : "none" }
      },
      e3.createElement(u, null)
    )
  ), e3.createElement(Sn3, null), e3.createElement(xn2, null));
}
function nt2({ ...n }) {
  const [o2, i2] = e3.useState({
    showChat: false,
    unreadMessages: 0
  }), u = ur2([Track.Source.Microphone]);
  return W2(), e3.createElement(Rn3, { onWidgetChange: i2 }, e3.createElement("div", { className: "lk-audio-conference", ...n }, e3.createElement("div", { className: "lk-audio-conference-stage" }, e3.createElement(Ce3, { tracks: u }, e3.createElement(In2, null))), e3.createElement(
    ee2,
    {
      controls: { microphone: true, screenShare: false, camera: false, chat: true }
    }
  ), o2.showChat && e3.createElement(Z2, null)));
}
function ct2({
  controls: n,
  saveUserChoices: o2 = true,
  onDeviceError: i2,
  ...u
}) {
  const g = { leave: true, microphone: true, ...n }, h2 = $n2(), { microphoneTrack: l, localParticipant: d } = Ve2(), c = e3.useMemo(() => ({
    participant: d,
    source: Track.Source.Microphone,
    publication: l
  }), [d, l]);
  h2 ? g.microphone ?? (g.microphone = h2.canPublish) : g.microphone = false;
  const C = G({ className: "lk-agent-control-bar" }, u), { saveAudioInputEnabled: t, saveAudioInputDeviceId: f } = dr2({
    preventSave: !o2
  }), s = e3.useCallback(
    (v, a) => {
      a && t(v);
    },
    [t]
  );
  return e3.createElement("div", { ...C }, g.microphone && e3.createElement("div", { className: "lk-button-group" }, e3.createElement(
    pn3,
    {
      source: Track.Source.Microphone,
      showIcon: true,
      onChange: s,
      onDeviceError: (v) => i2 == null ? void 0 : i2({ source: Track.Source.Microphone, error: v })
    },
    e3.createElement(Kt3, { trackRef: c, barCount: 7, options: { minHeight: 5 } })
  ), e3.createElement("div", { className: "lk-button-group-menu" }, e3.createElement(
    L3,
    {
      kind: "audioinput",
      onActiveDeviceChange: (v, a) => f(a ?? "default")
    }
  ))), g.leave && e3.createElement(sn3, null, "Disconnect"), e3.createElement(vn3, null));
}
export {
  En2 as AgentEvent,
  nt2 as AudioConference,
  ae2 as AudioTrack,
  yn3 as AudioVisualizer,
  Kt3 as BarVisualizer,
  Et2 as CameraDisabledIcon,
  wt2 as CameraIcon,
  bn3 as CarouselLayout,
  Z2 as Chat,
  un3 as ChatCloseIcon,
  Pn3 as ChatEntry,
  dn3 as ChatIcon,
  on3 as ChatToggle,
  oe3 as Chevron,
  cn3 as ClearPinButton,
  ye2 as ConnectionQualityIndicator,
  ln3 as ConnectionState,
  xn2 as ConnectionStateToast,
  ee2 as ControlBar,
  sn3 as DisconnectButton,
  wn3 as FocusLayout,
  En3 as FocusLayoutContainer,
  Tt2 as FocusToggle,
  kt2 as FocusToggleIcon,
  mn3 as GearIcon,
  kn3 as GridLayout,
  Jo as LKFeatureContext,
  Vn as LayoutContext,
  Rn3 as LayoutContextProvider,
  fn3 as LeaveIcon,
  W as LiveKitRoom,
  bt2 as LockLockedIcon,
  L3 as MediaDeviceMenu,
  hn3 as MediaDeviceSelect,
  On2 as MessagesEvent,
  Rt2 as MicDisabledIcon,
  yt2 as MicIcon,
  In2 as ParticipantAudioTile,
  Yn as ParticipantContext,
  Dt2 as ParticipantContextIfNeeded,
  Mn3 as ParticipantLoop,
  K2 as ParticipantName,
  Nt2 as ParticipantPlaceholder,
  Ot as ParticipantTile,
  tt2 as PreJoin,
  Mt2 as QualityExcellentIcon,
  St2 as QualityGoodIcon,
  Ct2 as QualityPoorIcon,
  It2 as QualityUnknownIcon,
  Sn3 as RoomAudioRenderer,
  Kn as RoomContext,
  Cn3 as RoomName,
  be2 as ScreenShareIcon,
  xt2 as ScreenShareStopIcon,
  wn2 as SessionEvent,
  An3 as SessionProvider,
  se2 as SpinnerIcon,
  gn3 as StartAudio,
  vn3 as StartMediaButton,
  Qt3 as Toast,
  Ce3 as TrackLoop,
  Me2 as TrackMutedIndicator,
  Hn as TrackRefContext,
  _t2 as TrackRefContextIfNeeded,
  pn3 as TrackToggle,
  Pt as UnfocusToggleIcon,
  at2 as VideoConference,
  jt2 as VideoTrack,
  ct2 as VoiceAssistantControlBar,
  Tn3 as formatChatMessageLinks,
  q as isTrackReference,
  Pn2 as isUseSessionReturn,
  ls as setLogExtension,
  us as setLogLevel,
  je2 as useAgent,
  zn2 as useAudioPlayback,
  hr2 as useAudioWaveform,
  un2 as useChat,
  or2 as useChatToggle,
  Hn2 as useClearPinButton,
  Vn2 as useConnectionQualityIndicator,
  oe2 as useConnectionState,
  ra as useCreateLayoutContext,
  jn2 as useDataChannel,
  Bn2 as useDisconnectButton,
  ia as useEnsureCreateLayoutContext,
  na as useEnsureLayoutContext,
  ca as useEnsureParticipant,
  la as useEnsureRoom,
  da as useEnsureSession,
  sa as useEnsureTrackRef,
  Cr2 as useEvents,
  Zt as useFacingMode,
  pa as useFeatureContext,
  qn2 as useFocusToggle,
  Wn2 as useGridLayout,
  fr2 as useIsEncrypted,
  Pe2 as useIsMuted,
  vr2 as useIsRecording,
  en2 as useIsSpeaking,
  ta as useLayoutContext,
  $ as useLiveKitRoom,
  Ve2 as useLocalParticipant,
  $n2 as useLocalParticipantPermissions,
  qo as useMaybeLayoutContext,
  Go as useMaybeParticipantContext,
  Qo as useMaybeRoomContext,
  Xo as useMaybeSessionContext,
  zn as useMaybeTrackRefContext,
  Gn as useMediaDeviceSelect,
  Jn as useMediaDevices,
  mr2 as useMultibandTrackVolume,
  Kn2 as usePagination,
  br2 as useParticipantAttribute,
  vn2 as useParticipantAttributes,
  aa as useParticipantContext,
  nn2 as useParticipantInfo,
  Qn as useParticipantPermissions,
  Yn2 as useParticipantTile,
  le2 as useParticipantTracks,
  rn2 as useParticipants,
  dr2 as usePersistentUserChoices,
  Xn as usePinnedTracks,
  et2 as usePreviewDevice,
  Ke2 as usePreviewTracks,
  Zn as useRemoteParticipant,
  Ce2 as useRemoteParticipants,
  ua as useRoomContext,
  er2 as useRoomInfo,
  Er2 as useRpc,
  yr2 as useSequentialRoomConnectDisconnect,
  Sr2 as useSession,
  fa as useSessionContext,
  kr2 as useSessionMessages,
  tr2 as useSortedParticipants,
  sn2 as useSpeakingParticipants,
  nr2 as useStartAudio,
  rr2 as useStartVideo,
  sr2 as useSwipe,
  yn2 as useTextStream,
  ar2 as useToken,
  lr2 as useTrackByName,
  ir2 as useTrackMutedIndicator,
  oa as useTrackRefContext,
  cr2 as useTrackToggle,
  gn2 as useTrackTranscription,
  pr2 as useTrackVolume,
  ur2 as useTracks,
  Sn2 as useTranscriptions,
  tn2 as useVisualStableUpdate,
  gr2 as useVoiceAssistant
};
//# sourceMappingURL=@livekit_components-react.js.map
